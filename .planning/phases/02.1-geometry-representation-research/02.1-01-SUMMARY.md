---
phase: 02.1-geometry-representation-research
plan: 01
subsystem: research
tags: [isomap, manifold-geometry, decision-rules, pre-registration, ricci-curvature, pseudo-euclidean, hyperbolic]

# Dependency graph
requires:
  - phase: 02-eigenspectrum-audit-validity-gate
    provides: "GATE_VERDICT=FAIL on fit_key=43cf438bc944c509 (r=0.052419 passes, m=0.412071 fails), the frozen k*=15 cache, and the 02-REFIT-PREREGISTRATION.md ordering discipline this plan copies"
provides:
  - "02.1-PREREGISTRATION.md: nine sections of decision rules (fork test, evaluation criterion, distortion statistic, working-dimension rule, delta-hyperbolicity reading rule, shortlist, falsifier, prohibited moves, scope constants) committed before any evidence exists"
  - "02.1-FORK.md: the ratified coordinate-producing vs graph-native verdict, applied to all six candidate families, both branches costed and accounted for at DEC/CURV requirement-ID granularity"
  - "Human ratification of the fork verdict (option: ratify, no amendment), unblocking wave 2"
affects: [02.1-02-geometry-representation-research, 02.1-03-geometry-representation-research, 02.1-04-geometry-representation-research, phase-3-decoder-curvature]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Decision-rule pre-registration proved by git ancestry (git merge-base --is-ancestor + git cat-file -e), mirroring 02-REFIT-PREREGISTRATION.md's discipline"
    - "Fork test derived from requirement text (DEC-01/CURV-01..CURV-03), not method preference; every candidate family scored against it in a table"
    - "Two-branch cost accounting: the losing branch is never dismissed on difficulty, only on requirement incompatibility, with its concrete cost (packages, SUS status, edge count, transport cost, missing fixture) recorded"
    - "Falsifier stated for both branches before any evidence exists, naming the exact later plan (02.1-03) that tests it"

key-files:
  created:
    - .planning/phases/02.1-geometry-representation-research/02.1-PREREGISTRATION.md
    - .planning/phases/02.1-geometry-representation-research/02.1-FORK.md
  modified: []

key-decisions:
  - "Fork verdict: coordinate-producing branch selected. Of the six candidate families, four (hyperbolic Poincare/Lorentz, mixed-curvature product manifold, pseudo-Euclidean/Krein, diffusion maps) produce a per-point coordinate vector a decoder can be fitted against; two (Ollivier-Ricci, Forman-Ricci) produce only per-edge/per-node scalars and cannot supply DEC-01's decoder input at all."
  - "Graph-native branch set aside strictly on requirement incompatibility (CURV-03 asks for an extrinsic mean-curvature vector; Ollivier-/Forman-Ricci compute an intrinsic Ricci-type scalar — a different mathematical object, not a re-specification), never on difficulty. Its cost is recorded concretely: three SUS-flagged packages (networkx not installed at all, GraphRicciCurvature, POT), the 150,000-edge k*=15 graph, a Sinkhorn-approximate optimal-transport requirement at that scale, and a synthetic tree-and-expander fixture that does not yet exist."
  - "Checkpoint resolved: ratify (not ratify-with-amendment, not flip, not halt). The human reviewed the six-family fork-test table, the graph-native cost accounting, and the falsifier's untested status, and selected ratify as-written. No rule in 02.1-PREREGISTRATION.md was found wrong; nothing was amended."
  - "The falsifier remains LIVE and UNTESTED at the moment of ratification. Ratification is of a structural argument (the fork test's type distinction), not of a measured result. Plan 02.1-03 produces the two numbers that could overturn it: delta-hyperbolicity (DELTA_REL) against tree/flat anchors, and the pseudo-Euclidean (p,q) distortion ladder against the q=0 classical-MDS baseline (already known to fail at m=0.412071). If BOTH trip — DELTA_REL sits near the flat anchor AND no (p,q) rung drops DISTORTION materially below the q=0 baseline — the verdict must be revisited before plan 02.1-04's recommendation, as a documented, dated amendment appended to 02.1-PREREGISTRATION.md, never a quiet edit."
  - "Shortlist fixed at three candidates before any evidence: pseudo-Euclidean/Krein-space retention (zero new dependencies, hand-rolled from the already-cached spectrum), hyperbolic/mixed-curvature (geoopt, PyTorch-native), and Ollivier-Ricci graph-native curvature (GraphRicciCurvature+POT+networkx, all SUS). Diffusion maps and Forman-Ricci are surveyed for GEOM-02 coverage but not carried into the evidence-gathering tasks."

patterns-established:
  - "A checkpoint:decision gate's outcome is recorded in the plan's SUMMARY.md, never by editing the artifact the decision ratifies. 02.1-FORK.md and 02.1-PREREGISTRATION.md are unchanged by ratification; the ratification itself lives here and in STATE.md."

requirements-completed: [GEOM-02, GEOM-04]

coverage:
  - id: D1
    description: "Every decision rule this phase later applies (fork test, evaluation criterion, distortion statistic, working-dimension rule, delta-hyperbolicity reading rule, shortlist rule, falsifier) exists as a git-committed document before any candidate evidence has been examined"
    requirement: "GEOM-04"
    verification:
      - kind: other
        ref: "git merge-base --is-ancestor 524a28d e49de67 (strict ancestry) plus git cat-file -e 524a28d:.../02.1-FORK.md failing (verdict absent from criterion commit's tree)"
        status: pass
      - kind: other
        ref: "python section/token assertion embedded in 02.1-01-PLAN.md Task 1 <verify> — 9/9 sections present, all pre-registered constants (DELTA_QUADRUPLES, DELTA_SEED, P_LADDER, Q_LADDER, 20260731, 43cf438bc944c509, SUS) recorded"
        status: pass
    human_judgment: false
  - id: D2
    description: "The coordinate-producing vs graph-native fork is resolved against the pre-registered fork test and recorded as an explicit verdict, applied to all six candidate families with DEC-01/CURV-01..CURV-03 quoted"
    requirement: "GEOM-02"
    verification:
      - kind: other
        ref: "python section/family-mention assertion embedded in 02.1-01-PLAN.md Task 1 <verify> — 7/7 sections present, all six families (Poincare, Lorentz, product, Krein, diffusion, Ollivier, Forman) present in 02.1-FORK.md"
        status: pass
    human_judgment: false
  - id: D3
    description: "A human ratifies the fork verdict before any later plan builds on it"
    requirement: "GEOM-04"
    verification: []
    human_judgment: true
    rationale: "Ratification is an explicit human decision at a blocking checkpoint (gate=blocking), not something automation can certify — the whole point of the gate is that a person, not a script, judges whether the verdict follows from requirement text or from method preference wearing that text as justification."

duration: N/A (continuation agent; Task 1 timing not observed by this agent)
completed: 2026-08-01
status: complete
---

# Phase 02.1 Plan 01: Pre-Registration and Fork Resolution Summary

**Coordinate-producing branch ratified for Phase 02.1's representation search, on a requirement-derived fork test proved to have been fixed before the verdict existed, with the graph-native branch's rejection costed rather than dismissed and its falsifier still untested.**

## Performance

- **Duration:** N/A — this agent resumed at Task 2 (a checkpoint carrying no execution time); Task 1's actual duration was not recorded by the prior executor session
- **Started:** 2026-08-01 (Task 1, prior session)
- **Completed:** 2026-08-01T19:36:03Z
- **Tasks:** 2 (Task 1: tracer, two commits; Task 2: checkpoint:decision, ratified)
- **Files modified:** 2 (both created: 02.1-PREREGISTRATION.md, 02.1-FORK.md)

## Accomplishments

- `02.1-PREREGISTRATION.md` committed alone at `524a28d`, carrying all nine required sections (Scope Constants, Fork Test, Evaluation Criterion, Distortion Statistic, Working-Dimension Rule, Delta-Hyperbolicity Reading Rule, Shortlist Rule, Falsifier, Prohibited Moves) and every pre-registered constant (`DELTA_QUADRUPLES=200_000`, `DELTA_SEED=20260801`, `P_LADDER`, `Q_LADDER`, `r2_pair_seed=20260731`, `fit_key=43cf438bc944c509`, `SUS`).
- `02.1-FORK.md` committed at `e49de67`, strictly descending from `524a28d`, applying the fork test to all six candidate families (hyperbolic Poincare/Lorentz, mixed-curvature product manifold, pseudo-Euclidean/Krein, diffusion maps, Ollivier-Ricci, Forman-Ricci) and resolving: four are coordinate-producing, two (Ollivier-Ricci, Forman-Ricci) are graph-native.
- Graph-native branch costed concretely rather than dismissed: three SUS-flagged packages (`networkx` not installed at all, `GraphRicciCurvature`, `POT`), the 150,000-edge k*=15 graph (mandatory reuse if pursued), the Sinkhorn-approximate optimal-transport requirement at n=10,000, and a synthetic tree-and-expander fixture that does not yet exist.
- Both branches' Phase 3 consequences recorded at DEC-01..DEC-05/CURV-01..CURV-08 requirement-ID granularity — the coordinate-producing branch inherits all 13 requirements with one amendment (the "Isomap coordinates" referent); the graph-native branch would have re-opened all 13.
- A stated, symmetric falsifier for both branches, naming plan `02.1-03` as the plan that tests it.
- **Checkpoint ratified.** The human reviewed the six-family fork-test table, the graph-native cost accounting, and the falsifier's untested status, and selected `ratify` — the verdict stands as written, with no amendment.

## Task Commits

Each task was committed atomically:

1. **Task 1a: Pre-register every decision rule** — `524a28d` (docs) — `02.1-PREREGISTRATION.md` alone, no other file staged
2. **Task 1b: Resolve the coordinate-producing vs graph-native fork** — `e49de67` (docs) — `02.1-FORK.md` alone, ancestry-proven descendant of `524a28d`
3. **Task 2: Ratify the fork verdict** — checkpoint:decision, gate=blocking; resolved `ratify`; no document edit, no new commit for the decision itself (recorded here and in STATE.md per this plan's `<output>` instruction)

**Plan metadata:** (this commit) `docs(02.1-01): complete pre-registration and fork resolution plan`

_Note: Task 1 is a `type="tracer"` task per plan frontmatter — it is a real, production-quality two-document deliverable, not a throwaway slice. It carries no separate tracer-feedback checkpoint of its own because Task 2's blocking decision checkpoint immediately follows it and serves the same integration-verification purpose against the actual artifact._

## Files Created/Modified

- `.planning/phases/02.1-geometry-representation-research/02.1-PREREGISTRATION.md` - Nine sections of decision rules fixed before any evidence; the fork test, evaluation criterion, distortion statistic, working-dimension rule (incl. `P_LADDER`/`Q_LADDER`), delta-hyperbolicity reading rule, shortlist, falsifier, prohibited moves, and scope constants (no package installs; SUS gate structural)
- `.planning/phases/02.1-geometry-representation-research/02.1-FORK.md` - The verdict: coordinate-producing branch selected, fork test applied to all six families, graph-native cost recorded, both branches' Phase 3 inherit/re-open accounts at requirement-ID granularity, falsifier restated with its testing plan named

## Decisions Made

See `key-decisions` in frontmatter. Summarized: coordinate-producing branch ratified as-written; no rule amended; falsifier remains live and untested, to be tested by plan 02.1-03's delta-hyperbolicity and (p,q)-ladder probes before plan 02.1-04's recommendation is written.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking, self-corrected within Task 1] Staging slip on `P_LADDER`/`Q_LADDER` transcription**
- **Found during:** Task 1 (prior executor session), first commit attempt for `02.1-PREREGISTRATION.md`
- **Issue:** An edit adding the `P_LADDER`/`Q_LADDER` constants was applied to the working tree but not re-staged before `git add` ran, so the first commit silently lacked those two tokens. The plan's own missing-token verify assertion (`for tok in ['DELTA_QUADRUPLES','DELTA_SEED','P_LADDER','Q_LADDER',...]`) caught this — it did not pass silently.
- **Fix:** The prior executor ran `git reset --soft` on its own two local, unpushed commits (no push had occurred), re-staged the corrected file, and redid both commits in the same required order (pre-registration alone, then fork document alone). Criterion-precedes-verdict ordering was preserved throughout.
- **Files modified:** `.planning/phases/02.1-geometry-representation-research/02.1-PREREGISTRATION.md` (staging corrected, no content change beyond what was always intended)
- **Verification:** Task 1's own automated verify (`git merge-base --is-ancestor` + `git cat-file -e` + the Python token/section assertions) passed on the redone commits; independently re-confirmed by this continuation agent (see Self-Check below).
- **Committed in:** `524a28d` (final, corrected pre-registration commit)

Recorded honestly per this plan's `## Prohibited Moves` spirit: the correction happened *before* any verdict document existed, via a soft-reset-and-redo of the executor's own unpushed local commits — not a violation of the ordering discipline, exactly the kind of mistake that discipline is designed to catch, and it was caught.

---

**Total deviations:** 1 auto-fixed (1 blocking, self-corrected before any downstream artifact existed)
**Impact on plan:** No impact on the final artifact or the ordering proof; the corrected commits are what `git merge-base --is-ancestor` and `git cat-file -e` verify against, and both checks pass on the final history.

## Issues Encountered

None beyond the staging slip documented above.

## User Setup Required

None - no external service configuration required. No package was installed (all seven candidates in RESEARCH.md's Package Legitimacy Audit returned SUS and none was installed by this plan, per the Scope Constants section).

## Next Phase Readiness

- Wave 2 (`02.1-02`, `02.1-03`) is unblocked: both depend on `02.1-01`'s ratified fork verdict and pre-registered rules.
- **The falsifier is live and untested — a ratified structural argument, not a settled result.** Plan `02.1-03` must run the delta-hyperbolicity probe (`DELTA_REL` against the tree and flat-Euclidean anchors) and the pseudo-Euclidean `(p,q)`-ladder probe (`DISTORTION` at every rung up to `q=40` against the `q=0` classical-MDS baseline, already known to fail at `m=0.412071`) before plan `02.1-04` writes its recommendation. If both probes trip, the verdict in `02.1-FORK.md` must be revisited as a dated, reasoned amendment to `02.1-PREREGISTRATION.md` — never a quiet edit.
- Shortlist for the evidence stage is fixed: pseudo-Euclidean/Krein-space retention, hyperbolic/mixed-curvature (`geoopt`), and Ollivier-Ricci graph-native curvature (surveyed for GEOM-02 coverage; not expected to be evidence-gathered unless the falsifier trips).
- No blockers for wave 2. Phase 2 remains open per STATE.md (Task 3 of `02-03` still awaiting separate approval) but this phase's work does not depend on that seal, only on Phase 2's already-settled FAIL verdict.

---
*Phase: 02.1-geometry-representation-research*
*Completed: 2026-08-01*

## Self-Check: PASSED

- FOUND: `.planning/phases/02.1-geometry-representation-research/02.1-01-SUMMARY.md`
- FOUND: `524a28d` (pre-registration commit) in `git log --oneline --all`
- FOUND: `e49de67` (fork verdict commit) in `git log --oneline --all`
