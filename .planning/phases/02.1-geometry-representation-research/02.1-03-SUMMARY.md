---
phase: 02.1-geometry-representation-research
plan: 03
subsystem: geometry-representation-research
tags: [numpy, scipy, eigendecomposition, pseudo-euclidean, krein-space, gromov-delta-hyperbolicity, kneedle-elbow, classical-mds, geodesic-distance]

requires:
  - phase: 02-eigenspectrum-audit-validity-gate
    provides: "frozen fit_key=43cf438bc944c509 cache: isomap joblib (dist_matrix_), mds_eigenspectrum npz (eigvals_all/top, eigvecs_top, geo_pairs_r2), gate_verdict json (elbow_criterion, d_frozen, d_provisional)"
  - phase: 02.1-geometry-representation-research (02.1-01)
    provides: "pre-registered rules applied verbatim (P/Q_LADDER, DELTA_QUADRUPLES, DELTA_SEED, distortion statistic, dimension rule, delta rule, falsifier) and the ratified fork verdict this evidence tests"
provides:
  - "geometry_probes.py: seven tested probe functions"
  - "geometry_probes_run.py: the runner producing every number the recommendation can cite"
  - "geometry_probes_43cf438bc944c509.json: pair-identity proof, delta with four anchors, the 81-rung (p,q) ladder, re-derived working dimension, correction blindness on three inputs (gitignored)"
  - "krein_bottom_43cf438bc944c509.npz: bottom-40 eigenpairs, cross-checked to rtol=1e-8 (gitignored)"
  - "Measured falsifier evidence: condition (a) trips unambiguously; condition (b) does not cleanly trip (real 18.4% relative reduction)"
affects: [02.1-04-recommendation, phase-3-decoder-curvature]

tech-stack:
  added: []
  patterns:
    - "Wave 0 synthetic fixtures with known answers precede every illustrative number"
    - "Bit-identity (not tolerance) as the data-provenance guard on the pair sample"
    - "Diagnostic blindness demonstrated on real + two synthetic controls, not cited"
    - "Gate verdict's kneedle criterion reused verbatim on a different curve"

key-files:
  created:
    - notebooks/pu_manifold/geometry_probes.py
    - notebooks/pu_manifold/tests/test_geometry_probes.py
    - notebooks/diagnostics/geometry_probes_run.py
  modified: []

key-decisions:
  - "Condition (b) reported as measured numbers (q0_best=0.0799 vs full-ladder-best=0.0652, 18.4%) without adjudicating 'materially below' — no threshold was pre-registered; left to 02.1-04"
  - "delta_rel_max governs the threshold comparison (p95 reported alongside) — the pre-registration names both without choosing"
  - "correction_blindness controls at n=1,000 — qualitative demonstration, budget kept"
  - "gate_stats/spectrum_from_distmatrix copied verbatim, not imported — gate_diagnostics.py runs a full diagnostic at import"

requirements-completed: [GEOM-03, GEOM-04, GEOM-05]

duration: 45min
completed: 2026-08-01
status: complete
---

# Phase 02.1 Plan 03: Geometry Probes Summary

**Both falsifier conditions measured: (a) trips unambiguously — the real manifold's delta exceeds
the flat-Euclidean anchor; (b) does not cleanly trip — an 18.4% relative distortion reduction from
retaining negative directions. The falsifier (both required) does not fire, but the result is
close enough to the edge that 02.1-04 must read both numbers, not treat it as a clean pass.**

Cold path 55.9 s (bottom-40 eigensolve 47.2 s), peak RSS 2.50 GB. 32/32 tests (18 new + 14
existing). Wave 0 gap closed. Pair identity **verified bit-identical on the first attempt** (draw_geo_pairs(default_rng(20260731), 10000, 200000) vs cached geo_pairs_r2).

## Measured Results

**Delta-hyperbolicity** (matched n=2,000, 200,000 quadruples, seed 20260801):

| Sample | `delta_rel_max` | `delta_rel_p95` |
|---|---|---|
| Tree (n=2,000) | 0.000000 | 0.000000 |
| Real manifold (n=2,000) | **0.386330** | 0.136450 |
| Flat R^20 Euclidean (n=2,000) | 0.360433 | 0.152891 |
| Real manifold (n=10,000) | 0.383921 | 0.108365 |

Rule applied verbatim: threshold = 0 + 0.1×(0.360433−0) = 0.036043. Real value 0.386330 —
**exceeds the flat-Euclidean anchor itself**. `single_curvature_defensible = False`.
**Condition (a) trips by a wide margin.**

**(p,q) ladder** (81 rungs; full ladder in the json):

| p | q | DISTORTION | mass_capture |
|---|---|---|---|
| 1 | 0 | 0.750521 | 0.064269 |
| 8 | 0 | 0.121024 | 0.160774 |
| **18** | **0** | **0.079864 (q=0 best)** | 0.182637 |
| 25 | 0 | 0.099228 | 0.192361 |
| 40 | 0 | 0.179641 | 0.208089 |
| 25 | 8 | 0.069532 | 0.209715 |
| **40** | **25** | **0.065190 (full-ladder best)** | 0.244267 |

18.4% relative reduction from the q=0 best; modest absolute (6.5% vs 8.0%); best rung captures
only 24.4% of mass at p+q=65 (long diffuse tail). **Condition (b) does not cleanly trip** —
"material" is not adjudicated here. Falsifier requires both → **does not fire; the verdict
stands** — flagged for a direct read at 02.1-04.

**Working dimension** (gate verdict's kneedle, verbatim): classical q=0 elbow **p=8**;
pseudo-Euclidean frontier elbow **(8, 0)**; d_frozen=5 for reference. **Unexpected:** retaining
negative directions does not move the elbow at all — the (40,25) optimum sits in the flat tail
the kneedle correctly rejects; the pseudo-Euclidean branch offers the same elbow-selected p,
differing only in whether q>0 is retained past it for marginal gain (contradicting RESEARCH.md's
implicit framing).

**Correction blindness:**

| Input | m_before | m_after_clip | m_after_shift |
|---|---|---|---|
| Real (`eigvals_all`) | 0.412071 | 0.000000 | 0.000000 |
| Synthetic flat (n=1,000, R^20) | 0.000000 | 0.000000 | 0.000000 |
| Synthetic non-metric (n=1,000) | 0.500649 | 0.000000 | 0.000000 |

Measured demonstration (plus 25 random spectra in the unit tests), not a citation.

## Commits

1. `a51a9ee` (test — RED) 2. `25dd345` (feat — GREEN) 3. `8e5cbb5` (feat — runner + artifact).

## Deviations / Issues

None — pair guard passed first try; compute well under budget.

## Next Phase Readiness

02.1-04 inherits: the falsifier's measured evidence (it must decide whether the 18.4% amends the
fork verdict — deliberately left open here); the re-derived dimensions (none silently inherited,
none declared wrong); the blindness table; the self-contained probes json. No blockers.

## Self-Check: PASSED

Files on disk; commits `a51a9ee`, `25dd345`, `8e5cbb5` in git log.

---
*Phase: 02.1-geometry-representation-research · Plan 03 · Completed: 2026-08-01*
