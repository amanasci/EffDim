---
phase: 02.1-geometry-representation-research
plan: 02
subsystem: research
tags: [manifold-learning, hyperbolic, pseudo-euclidean, diffusion-maps, ollivier-ricci, forman-ricci, isomap, flat-target]

# Dependency graph
requires:
  - phase: 02.1-geometry-representation-research (plan 01)
    provides: "02.1-PREREGISTRATION.md's fork test/shortlist/falsifier and 02.1-FORK.md's ratified coordinate-producing verdict, applied and cited (never re-derived) throughout this survey"
provides:
  - "02.1-SURVEY.md: GEOM-01 flat-target class-membership table (9 methods) and GEOM-02 non-flat-target candidate survey (6 families, 5 parts each, summary table, shortlist marking)"
affects: [02.1-03-geometry-representation-research, 02.1-04-geometry-representation-research]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "PSD-constrained-by-construction vs probability-based as the two distinct mechanisms behind 'cannot expose a negative-eigenvalue diagnostic', argued per-method against solver algebra rather than asserted by reputation"
    - "Per-candidate five-part comparison template (Assumptions / Cost at n=10,000 / Implementation maturity / Fork side / Phase 3 demand) applied identically across all six GEOM-02 families so they are genuinely comparable"
    - "[CITED] vs [VERIFIED] strength labelling carried forward from RESEARCH.md wherever a claim could not be checked against a primary source in this session (no WebFetch/WebSearch tool available)"

key-files:
  created:
    - .planning/phases/02.1-geometry-representation-research/02.1-SURVEY.md
  modified: []

key-decisions:
  - "GEOM-01's sharp claim stated and defended per-method: Isomap and classical MDS are the only two methods whose solver forms a double-centred Gram matrix without constraining it PSD as part of the optimisation, so they are the only ones that can expose a negative eigenvalue; LLE/Hessian-LLE/LTSA/Laplacian-eigenmaps/MVU are PSD-constrained by construction (five instances of one mechanism) and t-SNE/UMAP never form a Gram matrix at all (a second, distinct mechanism) — conflating the two would understate how structural the blindness is."
  - "Isomap.kernel_pca_.eigenvalues_'s n_components truncation is recorded as a second, within-family instance of the same blindness: even the one method family that can show the failure cannot show it through its own default sklearn attribute."
  - "All six GEOM-02 candidate families surveyed and fairly costed, including the two graph-native families the ratified fork verdict set aside — Ollivier-Ricci and Forman-Ricci carry the same five-part treatment as the four coordinate-producing candidates, per the requirement that the losing branch's numbers stay visible so a future reader can reopen the fork on evidence."
  - "Pseudo-Euclidean/Krein retention costed as the cheapest candidate surveyed (one bottom-40 eigensolve on the already-cached spectrum, zero new dependencies) — this was not asserted, it follows directly from what Phase 2 already computed and cached."
  - "MVU's semidefinite-program construction, Ollivier-Ricci's continuum-limit convergence claim (arXiv:2307.02378), and both under-extracted survey papers (arXiv:2510.22599, arXiv:2509.15517) are labelled [CITED] rather than [VERIFIED] throughout — this execution session had no WebFetch/WebSearch tool available, so none could be checked against a primary source here."

patterns-established:
  - "A narrative survey's per-candidate sections carry a fixed, identical part-ordering across every candidate (Assumptions/Cost/Maturity/Fork side/Phase 3 demand) so a reader can compare across families without re-deriving what each part means per row."

requirements-completed: [GEOM-01, GEOM-02]

coverage:
  - id: D1
    description: "GEOM-01 flat-target class-membership table covers all nine methods (Isomap, classical MDS, LLE, Hessian LLE, LTSA, Laplacian eigenmaps, MVU, t-SNE, UMAP), each checked against its own solver construction, with the can-it-produce-a-diagnostic column filled and the mechanism (PSD-constrained vs probability-based) named per row"
    requirement: "GEOM-01"
    verification:
      - kind: other
        ref: "python section/table/token assertion embedded in 02.1-02-PLAN.md Task 1 <verify> — 9/9 methods covered, 11 qualifying table lines (header+separator+9 rows), all measured anchors (0.412071, 5,029, positive-semidefinite, kernel_pca_.eigenvalues_, noise floor) present"
        status: pass
      - kind: other
        ref: "pytest notebooks/pu_manifold/tests/ -q (14 tests) plus git diff --quiet against pyproject.toml/src/effdim//notebooks/"
        status: pass
    human_judgment: true
    rationale: "This is a narrative, argued-prose deliverable per 02.1-VALIDATION.md's Manual-Only Verifications table — the automated checks above confirm structural completeness (every method covered, every required anchor present) but whether the class-membership argument itself is correctly reasoned per method is a judgment call for a human reviewer, exactly as 02.1-VALIDATION.md specifies for GEOM-01."
  - id: D2
    description: "GEOM-02 non-flat-target candidate survey covers all six families (hyperbolic Poincare/Lorentz, mixed-curvature product manifold, pseudo-Euclidean/Krein, diffusion maps, Ollivier-Ricci, Forman-Ricci) with assumptions, cost at n=10,000 in checkable units, implementation maturity (package/version/release date/SUS verdict), fork side, and Phase 3 demand by requirement ID, plus a side-by-side summary table marking the pre-registered shortlist"
    requirement: "GEOM-02"
    verification:
      - kind: other
        ref: "python section/family/parts assertion embedded in 02.1-02-PLAN.md Task 2 <verify> — 6+ family subsections, all 5 labelled parts present per family (>=6 occurrences each)"
        status: pass
      - kind: other
        ref: "python token/requirement-ID assertion embedded in 02.1-02-PLAN.md Task 2 <verify> — 150,000/SUS/unknown-downloads/pydiffmap/datafold/networkx/2019/geoopt/GraphRicciCurvature/POT/arXiv:2307.02378/arXiv:2510.22599/arXiv:2509.15517/02.1-FORK.md/Sinkhorn all present; 13 distinct DEC/CURV requirement IDs cited including CURV-01, CURV-02, CURV-03, DEC-01"
        status: pass
      - kind: other
        ref: "pytest notebooks/pu_manifold/tests/ -q (14 tests) plus git diff --quiet against pyproject.toml/src/effdim//notebooks/"
        status: pass
    human_judgment: true
    rationale: "Judgment about coverage completeness and fair costing per 02.1-VALIDATION.md's Manual-Only Verifications table — the automated checks confirm every required token, family, and requirement ID is present, but whether the survey is genuinely a fair accounting (not a case built to justify the ratified branch) requires a human reader, exactly as 02.1-VALIDATION.md specifies for GEOM-02."

duration: ~15min
completed: 2026-08-01
status: complete
---

# Phase 02.1 Plan 02: Flat-Target Class Membership and Non-Flat-Target Candidate Survey Summary

**`02.1-SURVEY.md` written: GEOM-01's nine-method flat-target class table (only Isomap/classical MDS can expose a negative-eigenvalue diagnostic; PSD-by-construction and probability-based solvers cannot, by two distinct mechanisms) and GEOM-02's six-family non-flat-target candidate survey (assumptions, cost, maturity, fork side, Phase 3 demand for hyperbolic, product-manifold, pseudo-Euclidean/Krein, diffusion maps, Ollivier-Ricci, Forman-Ricci), with the graph-native losing branch costed as fully as the winning one.**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-08-01T19:30:00Z (approx.)
- **Completed:** 2026-08-01T19:44:41Z
- **Tasks:** 2 (both `type="auto"`)
- **Files modified:** 1 (created: `02.1-SURVEY.md`)

## Accomplishments

- `## GEOM-01 — Flat-Target Class Membership` written: a nine-row table (Isomap, classical MDS,
  LLE, Hessian LLE, LTSA, Laplacian eigenmaps, MVU, t-SNE, UMAP) checking each method's defining
  solver against whether it can produce a computable Euclidean-validity diagnostic. Two distinct
  mechanisms separated and named per method — five methods PSD-constrained by construction
  (MVU's SDP, Laplacian eigenmaps' graph Laplacian, LLE/Hessian-LLE/LTSA's local Gram/covariance
  eigenproblems), two probability-based with no Gram matrix at all (t-SNE, UMAP).
- `Isomap.kernel_pca_.eigenvalues_`'s `n_components` truncation recorded as a second,
  within-family instance of the same blindness, distinct from the cross-family PSD/probability
  split above.
- `## GEOM-02 — Non-Flat-Target Candidates` written: six family subsections, each carrying the
  same five parts (Assumptions, Cost at n = 10,000, Implementation maturity, Fork side, Phase 3
  demand), plus a side-by-side summary table marking the pre-registered three-item shortlist.
- Costs quantified against Phase 2's measured anchors throughout — no adjective-only cost claim
  anywhere in the section. Pseudo-Euclidean/Krein retention identified as the cheapest candidate
  (one bottom-40 eigensolve on the already-cached spectrum); Ollivier-Ricci's Sinkhorn-approximate
  optimal transport over 150,000 edges identified as necessary, not optional, at this scale.
- Graph-native families (Ollivier-Ricci, Forman-Ricci) surveyed and costed with the same rigour
  as the four coordinate-producing families despite the ratified fork verdict setting them aside —
  their Phase 3 demand column states plainly that CURV-01..CURV-03 are re-opened, not amended.
- Phase 3 demand column cites 13 distinct DEC/CURV requirement IDs, consistent throughout with
  `02.1-FORK.md`'s `## What Phase 3 Inherits` / `## What Phase 3 Re-opens` accounting — checked
  by re-reading the fork document's own tables while writing each family's demand bucket, not
  independently re-derived.

## Task Commits

Each task was committed atomically:

1. **Task 1: GEOM-01 — the flat-target class, and which members can even show the failure** — `ecf5d00` (docs)
2. **Task 2: GEOM-02 — the non-flat-target candidates, costed and mapped onto Phase 3** — `8f9ca5f` (docs)

**Plan metadata:** (this commit) `docs(02.1-02): complete geometry survey plan`

## Files Created/Modified

- `.planning/phases/02.1-geometry-representation-research/02.1-SURVEY.md` - GEOM-01's nine-method
  flat-target class table and GEOM-02's six-family non-flat-target candidate survey, the two
  narrative deliverables plan `02.1-04`'s recommendation is argued from

## Decisions Made

See `key-decisions` in frontmatter. Summarized: the PSD-constrained-by-construction and
probability-based mechanisms were kept explicitly separate rather than merged into one "shares
the flat-target assumption" bucket, because they are different reasons for the same silence and
collapsing them would understate how structural it is. All six GEOM-02 families received the
identical five-part treatment, including the two graph-native families the fork verdict set
aside, so the survey documents both branches rather than only the winner.

## Deviations from Plan

None — plan executed exactly as written. The class-membership table checks each method against
its own construction rather than reputation; the MVU SDP claim and the Ollivier-Ricci
continuum-limit claim are labelled `[CITED]` per the plan's own instruction for claims that
cannot be verified against a primary source; all six GEOM-02 families received the full
five-part treatment with cost quantified against Phase 2's measured anchors.

**Environment note (not a deviation, a constraint carried forward honestly):** this session had
no WebFetch/WebSearch tool available, so the MVU semidefinite-program formulation, the
Ollivier-Ricci continuum-limit paper (arXiv:2307.02378), and the two under-extracted survey
papers (arXiv:2510.22599, arXiv:2509.15517) could not be read directly here either — each stays
at the `[CITED]` strength `02.1-RESEARCH.md` already left it at, recorded explicitly rather than
silently upgraded. A future session with browsing access should check these before `02.1-04`'s
recommendation leans on any of them.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required. No package was installed; this plan surveys
and costs candidate libraries, it installs none, per the plan's own critical-notes constraint.

## Next Phase Readiness

- Wave 2's other plan, `02.1-03` (delta-hyperbolicity and pseudo-Euclidean `(p,q)`-ladder probes,
  producing the two numbers that could overturn the fork verdict), is unaffected by this plan and
  proceeds independently — both depend only on `02.1-01`'s ratified fork and pre-registered
  rules, not on each other.
- `02.1-04`'s recommendation plan (wave 3) now has both required inputs from this plan: the
  flat-target class-membership table (so it need not re-argue why other methods' silence is
  uninformative) and the full six-family candidate survey (so its choice is argued among costed
  alternatives, not asserted).
- The falsifier remains live and untested — nothing in this survey should be read as though the
  branch question is closed. `02.1-03`'s probes are still outstanding.
- No blockers for `02.1-03` or `02.1-04`. Phase 2 remains open per STATE.md (unrelated to this
  plan's work).

---
*Phase: 02.1-geometry-representation-research*
*Completed: 2026-08-01*

## Self-Check: PASSED

- FOUND: `.planning/phases/02.1-geometry-representation-research/02.1-SURVEY.md`
- FOUND: `.planning/phases/02.1-geometry-representation-research/02.1-02-SUMMARY.md`
- FOUND: `ecf5d00` (Task 1 commit) in `git log --oneline --all`
- FOUND: `8f9ca5f` (Task 2 commit) in `git log --oneline --all`
