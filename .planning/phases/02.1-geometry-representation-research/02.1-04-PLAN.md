---
phase: 02.1-geometry-representation-research
plan: 04
type: execute
wave: 3
depends_on: ["02.1-01", "02.1-02", "02.1-03"]
files_modified:
  - .planning/phases/02.1-geometry-representation-research/02.1-RECOMMENDATION.md
  - notebooks/diagnostics/geometry_handoff.py
autonomous: false
requirements: [GEOM-03, GEOM-04, GEOM-05]

must_haves:
  truths:
    - "A reader sees what 41% negative mass means geometrically: 5,029 of 10,000 eigenvalues negative, none dominant (r=0.052419 passes), negative part comparable in magnitude to the positive part (GEOM-03)"
    - "An argued judgment on indefinite MDS vs distance-matrix correction, backed by 02.1-03's measured demonstration that both corrections zero negative mass on real, flat, and non-metric spectra alike (GEOM-03)"
    - "One representation recommended with rationale, rejected alternatives, and the evidence Phase 3 judges it by — sufficient to re-specify Phase 3 without re-opening the question (GEOM-04)"
    - "GEOM-03 stance and GEOM-04 recommendation internally consistent (Entanglement item 4: recommending Krein IS choosing retention) (GEOM-03, GEOM-04)"
    - "Generality stated: DINOv3 ViT-B/16 is the one variable never varied; the 35-model sweep's bearing recorded (GEOM-04)"
    - "Working dimension re-derived from the representation's own diagnostic under the pre-registered rule, placed beside D_FROZEN=5 with the relationship argued — 5 neither declared wrong nor silently inherited (GEOM-05)"
    - "A machine-readable handoff exists that Phase 3 reads before any expensive cell, mirroring gate_verdict's self-contained shape (GEOM-04)"
    - "A human reads the recommendation as the Phase 3 planner and confirms a PLAN.md could be written from it (GEOM-04)"
  artifacts:
    - path: ".planning/phases/02.1-geometry-representation-research/02.1-RECOMMENDATION.md"
      provides: "Terminal argued artifact: geometric reading, correction-vs-retention judgment, recommendation, working dimension, Phase 3 brief"
      contains: "## GEOM-04 — Recommendation"
    - path: "notebooks/diagnostics/geometry_handoff.py"
      provides: "Writer assembling the machine-readable Phase 3 handoff"
      contains: "geometry_handoff"
    - path: "notebooks/.cache/geometry_handoff_43cf438bc944c509.json"
      provides: "Self-contained handoff: representation, working dimension, evidence criteria, DEC/CURV amendments, falsifier (gitignored)"
      contains: "working_dimension"
  key_links:
    - {from: "02.1-RECOMMENDATION.md", to: "geometry_probes_43cf438bc944c509.json", via: "every measured number quoted from the artifact, never recomputed or remembered", pattern: "geometry_probes_43cf438bc944c509"}
    - {from: "02.1-RECOMMENDATION.md", to: "02.1-PREREGISTRATION.md", via: "dimension rule + evaluation criterion cited by section name", pattern: "02.1-PREREGISTRATION.md"}
    - {from: "02.1-RECOMMENDATION.md", to: "02.1-SURVEY.md", via: "rejected alternatives cited with the deciding assumption/cost/demand", pattern: "02.1-SURVEY.md"}
    - {from: "geometry_handoff_43cf438bc944c509.json", to: ".planning/REQUIREMENTS.md", via: "DEC/CURV ids bucketed unchanged/amend/rewrite — Phase 3 re-plans from a requirement diff", pattern: "CURV-03"}
  prohibitions:
    - {requirement_id: "GEOM-03", category: "transparency", status: "resolved", verification: "judgment", statement: "MUST NOT recommend Cailliez/Lingoes/clipping without an explicit principled-here argument — they force a passing gate on any input (measured on three spectra); silent adoption deletes the signal. A correction may appear only as a labelled cannot-fail contrast baseline."}
    - {requirement_id: "GEOM-05", category: "values", status: "resolved", verification: "judgment", statement: "MUST NOT declare D_FROZEN=5 simply wrong nor inherit it silently — a curved target can need fewer dimensions than a flat one, so 18-25 does not refute 5; re-derive under the pre-registered rule and argue the relationship."}
    - {requirement_id: "GEOM-04", category: "scope", status: "resolved", verification: "judgment", statement: "MUST NOT frame the recommendation as a first pass/placeholder — a recommendation that defers itself fails GEOM-04 by construction."}
    - {requirement_id: "GEOM-04", category: "transparency", status: "resolved", verification: "judgment", statement: "MUST NOT present claims above their source strength (Ollivier-Ricci continuum limit, class-membership list, Neuc-MDS description are search-summary strength — label or read directly)."}
    - {requirement_id: "GEOM-04", category: "dependencies", status: "resolved", verification: "automated", statement: "MUST NOT install any package — the writer reads two JSONs and writes a third."}
---

<objective>
The plan the phase exists for: the argued judgment on 41% negative mass and correction-vs-retention,
one recommended representation, the working dimension that follows, and the machine-readable Phase 3
handoff. GEOM-03/04/05 land together because RESEARCH.md's Entanglement says they cannot be
sequenced: choosing Krein IS choosing retention (item 4); dimension cannot be fixed independently of
representation (item 2 — the striking datum: hyperbolic MAP=0.989 in 2 dims where 200-dim Euclidean
did worse). The trap, measured by 02.1-03: correction methods are diagnostic-blind by construction —
zero negative mass on real, flat, and non-metric spectra alike.
</objective>

<key_interfaces>
From 02.1-01: the pre-registered Evaluation Criterion, Distortion Statistic, Working-Dimension Rule,
Shortlist, Falsifier, Prohibited Moves; the ratified fork verdict + Phase 3 accounting.
From 02.1-02: the survey's six families with per-family assumptions/costs/Phase 3 demands.
From 02.1-03: `geometry_probes_43cf438bc944c509.json` — r, m, pair_identity_verified, delta +
delta_anchors (real_n10000/real_n2000/tree_n2000/euclidean20d_n2000), krein_ladder (p, q,
median_abs_rel, median_signed_rel, p95_abs_rel, mass_capture, n_negative_sq), working_dimension
(classical_p, krein_p, krein_q, criterion), correction_blindness, d_frozen, d_provisional,
elbow_criterion.
From Phase 2: gate_verdict json for fit_key 43cf438bc944c509 (FAIL, r=0.0524192078526829,
m=0.4120712514841815, thresholds 0.10/0.05/0.25/0.15, spectrum: 4971/5029,
lambda_max_pos 3230.8539634646067, lambda_min_neg -169.35880545251558,
noise_floor 7.173936918879702e-09, dropoff 2/2.444713943099398,
d_frozen=5, d_provisional=18, spectrum, remediation — option 3 is what this phase acts on).
02-FINDINGS facts: diffuse tail (r passes at every k); 10-11 orders above noise floor; m
flat-to-rising while GEO_AMBIENT_RATIO fell 2.83→1.86 and LONG_EDGE_FRACTION rose 0.0065→0.0139;
L2 inversion moves m 0.28% (raw norms 16.029±0.504, cv 3.1% — shell geometry untested); intrinsic
dim 19.48 TwoNN / 25.0 local-PCA median (std 2.01, range 19-29); HSC m=0.4226; disjoint resample
m=0.411948 identical counts; CURVE_DIVERGENCE_MAX=0.697664 at d=5. §8 generality: model never
varied; 35-model sweep packaged, not run.
Handoff pattern: §5.3/§6.7 shape — flat dict, json_cache (cast numpy scalars), formatted dump,
required-keys asserts; standalone script per diagnostics convention.
</key_interfaces>

<tasks>

<task type="auto">
  <name>Task 1: GEOM-03 — what 41% negative mass means, and whether correcting it is principled or cosmetic</name>
  <produced>
    `02.1-RECOMMENDATION.md` preamble (self-contained standard per 02-FINDINGS) + two sections.
    `## GEOM-03 — What 41% Negative Mass Means`: the geometry, not the linear algebra — a negative
    eigenvalue is a direction where the metric behaves as if distance were subtracted, representable
    by no real Euclidean coordinate; this spectrum's shape (5,029 directions, none dominant, 41% of
    mass) rules out a single short-circuit edge and reads as pervasively non-Euclidean in half its
    directions; §8's threshold-independent claim; corroborating routes cited not re-argued
    (0.697664 divergence, noise-floor margin, the four experiments); the shell-geometry limit
    carried forward honestly.
    `## GEOM-03 — Correction or Retention`: mechanism (Cailliez/Lingoes/clipping force PSD on any
    input), shown with the measured three-input table; the principled-correction condition stated
    (negativity known on independent grounds to be measurement error) and this data tested against
    it (all measurement-error explanations eliminated); corrections retain one legitimate role — a
    labelled cannot-fail contrast baseline. Entanglement with GEOM-04 stated; Neuc-MDS recorded at
    abstract-only strength with the practical fallback being the measured top-k-per-sign retention.
  </produced>
  <done>GEOM-03: an argued judgment resting on a measured demonstration of blindness across three spectra, not a borrowed warning.</done>
</task>

<task type="auto">
  <name>Task 2: GEOM-04 + GEOM-05 — recommendation, working dimension, machine-readable handoff</name>
  <reversibility rating="costly">Phase 3 is re-specified from this; changing it later discards that plan. Gated by Task 3's human read.</reversibility>
  <produced>
    Four more sections + the writer.
    `## GEOM-04 — Recommendation`: one representation named in the first sentence, argued against
    the pre-registered criterion with the measured krein_ladder (q=0 baseline beside it),
    delta_anchors read by the unedited rule, and mass capture; states what it assumes that could be
    false.
    `## GEOM-04 — Alternatives Rejected`: five subsections, each losing on a named assumption /
    requirement incompatibility / costed number — never difficulty; near-misses say what would
    change the answer.
    `## GEOM-04 — Evidence It Will Be Judged Against`: checks Phase 3 can run; falsifier restated
    with status; generality stated explicitly with the sweep's bearing.
    `## GEOM-05 — Working Dimension`: the (p, q) pair beside D_FROZEN=5, classical_p, local PCA
    ~25, TwoNN ~19.5, estimator-median 18, n_components=18; both readings argued (elbow measured
    the failure — 0.697664 at exactly d=5 — vs curved targets need fewer dims);
    inherit/revise/discard stated outright; the dimension-starvation consequence recorded.
    `## Phase 3 Re-specification Brief`: representation, dimension, first step, DEC/CURV buckets
    consistent with the fork accounting, cached artifact paths.
    `geometry_handoff.py` (run, not just written): flat dict via json_cache under
    geometry_handoff_43cf438bc944c509 with cfg carrying fit_key + decision constants. Required keys
    (13): phase "02.1", fit_key, representation, retention_stance, working_dimension (pair +
    criterion), d_frozen_disposition (inherited/revised/discarded + reason), evidence_criteria,
    falsifier, generality, requirements {unchanged, amend, rewrite} bucketing all 13 DEC/CURV ids
    exactly once, source_artifacts, probe_artifact, alternatives_rejected ({candidate, reason} ×≥5).
  </produced>
  <done>GEOM-04/05 answered jointly: one recommendation with beaten alternatives and judge-by evidence; dimension from the representation's own diagnostic; both machine-readable for Phase 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Read the recommendation as the Phase 3 planner and confirm it is decision-grade</name>
  <produced>
    Nine-point human read: (1) could 03-01-PLAN.md be written from doc + JSON alone; (2) entanglement
    consistency — GEOM-03 stance and GEOM-04 recommendation could not be edited independently;
    (3) correction judgment argued with the three-input table and a stated principled-correction
    condition; (4) dimension treatment representation-specific, (p,q) pair,
    inherit/revise/discard explicit; (5) nothing deferred; (6) losers treated fairly; (7) evidence
    strength honest; (8) handoff self-contained, CURV-03 status consistent with the fork;
    (9) generality stated. Resume: "approved" or name the section a Phase 3 planner would re-open.
  </produced>
  <done>Phase sealed on a human confirmation of decision-grade.</done>
</task>

</tasks>

<threats>
T-02.1-SC (installs: none; verify asserts protected files). T-02.1-10 (prose/measurement drift:
verify cross-reads doc against the artifact; handoff criterion asserted to match the pre-registered
elbow string). T-02.1-11 (DEC/CURV bucketing: all 13 ids exactly once, no double-bucket, asserted).
T-02.1-12 (rules drift: handoff cfg-keyed on decision constants; manifest raises). T-02.1-02
(evidence-strength: checkpoint step 7 + prohibitions). T-02.1-07 (local JSON reads: accept).
T-02.1-04 (disclosure: accept).
</threats>

<verification>
Doc carries all seven sections; measured numbers present in the probes artifact; handoff writer
runs clean (`PYTHONPATH=notebooks`), writes json + sidecar, 13 keys, 13 ids bucketed once, ≥5
alternatives, dimension criterion matches the pre-registered rule; full pytest suite green;
`git diff --quiet` on protected files; human approval.
</verification>

<output>
`02.1-04-SUMMARY.md`: representation + retention stance; working dimension + D_FROZEN disposition
with reason; five rejected alternatives one line each; falsifier + tripped status; generality +
what the sweep would change; DEC/CURV bucketing; checkpoint outcome; anything flagged for
re-opening.
</output>
