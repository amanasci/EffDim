---
phase: 02.1-geometry-representation-research
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - .planning/phases/02.1-geometry-representation-research/02.1-SURVEY.md
autonomous: true
requirements: [GEOM-01, GEOM-02]

must_haves:
  truths:
    - "Flat-target class membership checked method by method against each method's own solver — Phase 2's failure is a class property, not an Isomap-implementation property (GEOM-01)"
    - "Sharp form stated: PSD-constrained methods (LLE, Hessian LLE, LTSA, Laplacian eigenmaps, MVU) and probability-based ones (t-SNE, UMAP) *cannot expose* a negative-eigenvalue statistic — a clean picture proves nothing (GEOM-01)"
    - "Each method states whether it exposes a computable Euclidean-validity diagnostic at all (GEOM-01)"
    - "All six non-flat-target families surveyed with assumptions, cost at n=10,000 in concrete units, maturity incl. package/SUS status, fork side, and Phase 3 demand by requirement ID (GEOM-02)"
    - "Cost in checkable units against Phase 2's measured anchors, never adjectives (GEOM-02)"
    - "Maturity is auditable: package, version, release date, SUS verdict; pydiffmap 2019 staleness and datafold preference recorded (GEOM-02)"
    - "Per-candidate Phase 3 demand consistent with the ratified fork verdict (GEOM-02)"
  artifacts:
    - path: ".planning/phases/02.1-geometry-representation-research/02.1-SURVEY.md"
      provides: "GEOM-01 class-membership analysis + GEOM-02 candidate survey"
      contains: "## GEOM-01 — Flat-Target Class Membership"
  key_links:
    - {from: "02.1-SURVEY.md", to: "02.1-FORK.md", via: "each GEOM-02 row records its fork side per the pre-registered test", pattern: "02.1-FORK.md"}
    - {from: "02.1-SURVEY.md", to: "02.1-RESEARCH.md", via: "package/version/date/SUS facts copied from the Legitimacy Audit, not re-researched", pattern: "SUS"}
    - {from: "02.1-SURVEY.md", to: "02-FINDINGS.md", via: "GEOM-01 anchored to measured m=0.412071 and the 5,029 negatives", pattern: "0.412071"}
  prohibitions:
    - {requirement_id: "GEOM-01", category: "transparency", status: "resolved", verification: "judgment", statement: "MUST NOT present absence of a negative-eigenvalue statistic as escaping the flat-target problem — PSD-constrained/probability-based methods cannot produce the statistic, so a clean result is uninformative."}
    - {requirement_id: "GEOM-02", category: "coverage", status: "resolved", verification: "automated", statement: "MUST NOT omit/merge any of the six families or drop graph-native because the fork went the other way — the losing branch stays visible with its costs."}
    - {requirement_id: "GEOM-02", category: "values", status: "resolved", verification: "judgment", statement: "MUST NOT state cost/maturity/impact as adjectives — bytes, edges, seconds vs Phase 2's ~100 s dense eigensolve and 78-105 s fits; package/version/date/verdict; DEC/CURV id lists."}
---

<objective>
Write the phase's two survey deliverables. GEOM-01 closes the escape route of reaching for another
manifold-learning method and reading a clean picture as resolution — the silence of PSD-constrained
and probability-based solvers is structural, not evidential; it upgrades "our Isomap run failed" to
"this method class cannot describe this geometry". GEOM-02 makes 02.1-04's recommendation a choice
among costed alternatives; both branches described fully regardless of the ratified fork.
Prose deliverable; verification structural + human reading per 02.1-VALIDATION.md.
</objective>

<key_interfaces>
From 02.1-01 (ratified, do not contradict): the Fork Test (applied, not restated), the
before-evidence shortlist, the fork verdict + inherit/re-open accounting.

Measured anchors (02-FINDINGS.md): m=0.412071, r=0.052419, 5,029/10,000 negative (~41% mass),
tail 10-11 orders above noise floor, robust across k {5,10,15,30}, HSC (m=0.4226), disjoint
resample (m=0.411948), inverted L2 (0.28%). Intrinsic dim 19.5-25, std 2.0. Cost anchors (§4.7):
800 MB dense matrix, fits 78.5/87.9/104.5 s, eigensolves 122.9/120.4/122.8 s, peak RSS 3.48 GiB,
150,000 edges at k*=15.

Package facts (copy from RESEARCH.md Legitimacy Audit, do not re-research):

| Package | Version | Latest release | Verdict |
|---|---|---|---|
| `geoopt` | 0.5.1 | 2025-06-19 | SUS (`unknown-downloads`, `no-repository`) |
| `geomstats` | 2.8.0 | 2024-09-09 | SUS (`unknown-downloads`, `no-repository`) |
| `GraphRicciCurvature` | 0.5.3.2 | 2024-06-26 | SUS (`unknown-downloads`) |
| `datafold` | 2.0.2 | 2026-03-13 | SUS (`unknown-downloads`) |
| `pydiffmap` | 0.2.0.1 | **2019-02-04** | SUS; stale, prefer `datafold` |
| `hyperlib` | 0.0.6 | 2022-06-08 | SUS |
| `POT` | 0.9.7.post1 | 2026-07-29 | SUS (`too-new` false positive; mature since ~2016) |
| `networkx` | 3.6.1 | — | not installed |

Every SUS = `unknown-downloads` signal gap, not a substantive finding — and the blocking gate
applies anyway.
</key_interfaces>

<tasks>

<task type="auto">
  <name>Task 1: GEOM-01 — the flat-target class, and which members can even show the failure</name>
  <produced>
    `## GEOM-01 — Flat-Target Class Membership` in 02.1-SURVEY.md. Table with one row each for
    Isomap, classical MDS, LLE, Hessian LLE, LTSA, Laplacian eigenmaps, MVU, t-SNE, UMAP; five
    columns ending in "can it produce a Euclidean-validity diagnostic at all" + why not. Two
    mechanisms distinguished: PSD-by-construction (MVU's SDP constrains the Gram matrix to the PSD
    cone; graph Laplacian PSD; local Gram/covariance eigenproblems in LLE/HLLE/LTSA) vs
    probability-based (t-SNE/UMAP never form a Gram matrix). MVU SDP claim verified against
    Weinberger & Saul or labelled [CITED]. `Isomap.kernel_pca_.eigenvalues_` truncation recorded as
    a within-family instance of the same blindness. Anchored to the measured numbers. Closing: a
    clean UMAP/t-SNE plot = failure made invisible, not absent; the honest test of a candidate is a
    validity diagnostic that could have failed.
  </produced>
  <done>GEOM-01: the class claim is method-by-method against each solver's own construction, so the next move rests on a class property.</done>
</task>

<task type="auto">
  <name>Task 2: GEOM-02 — the non-flat-target candidates, costed and mapped onto Phase 3</name>
  <produced>
    `## GEOM-02 — Non-Flat-Target Candidates` — six `###` subsections (hyperbolic Poincare/Lorentz,
    mixed-curvature product, pseudo-Euclidean/Krein, diffusion maps, Ollivier-Ricci, Forman-Ricci),
    each with five identical labelled parts:
    **Assumptions** (falsifiable about this data — hyperbolic: single constant negative curvature,
    flagged likely-wrong for heterogeneous morphology, tested by 02.1-03's delta probe; Krein: the
    indefinite form is the right object; Ollivier-Ricci: continuum-limit conditions cited-strength
    only, arXiv:2307.02378, uncertain at ~20-25 dims/n=10,000).
    **Cost at n=10,000** against measured anchors; Krein costs one bottom-block eigensolve since
    the spectrum and matrix are on disk; Ricci variants need Sinkhorn at 150,000 edges.
    **Implementation maturity** from the package table; Don't-Hand-Roll items in matching
    subsections (per-edge OT, Poincare exp/log maps + RSGD, diffusion kernel bandwidth) plus the
    one hand-roll exception (sampled Gromov delta).
    **Fork side** by the pre-registered test, one-clause reason.
    **Phase 3 demand** in three buckets (unchanged / amend "Isomap coordinates" / rewrite),
    agreeing with 02.1-FORK.md; graph-native: CURV-01..03 re-opened, not amended.
    Then a six-row summary table marking the pre-registered shortlist. Close with the two
    under-extracted surveys (arXiv:2510.22599, arXiv:2509.15517) read or explicitly recorded
    unreachable.
  </produced>
  <done>GEOM-02: every candidate with falsifiable assumptions, checkable costs, auditable maturity, and a requirement-ID Phase 3 consequence — the recommendation becomes a choice among alternatives.</done>
</task>

</tasks>

<threats>
T-02.1-SC (installs: none; verify asserts notebooks/pyproject untouched; SUS + gate documented at
point of temptation). T-02.1-05 (evidence-strength laundering: RESEARCH.md tags carried through;
MVU + class list WebSearch-strength verified-or-labelled; unlabelled upgrades forbidden).
T-02.1-06 (survey/fork inconsistency: Phase 3 column must agree with the fork accounting; human
reads both at 02.1-04's checkpoint). T-02.1-04 (disclosure: accept — public data, no PII).
</threats>

<verification>
Section/method/family presence checks; five parts per family; cost cells quantified; DEC/CURV ids
present and consistent with the fork; `pytest notebooks/pu_manifold/tests/ -q` green;
`git diff --quiet -- pyproject.toml src/effdim/ notebooks/` clean.
</verification>

<output>
`02.1-02-SUMMARY.md`: claims carried as [CITED] vs verified; whether the two survey papers were
reachable and what they changed; any candidate whose cost/maturity differs materially from
RESEARCH.md's account.
</output>
