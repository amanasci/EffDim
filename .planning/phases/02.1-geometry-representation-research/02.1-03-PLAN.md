---
phase: 02.1-geometry-representation-research
plan: 03
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - notebooks/pu_manifold/geometry_probes.py
  - notebooks/pu_manifold/tests/test_geometry_probes.py
  - notebooks/diagnostics/geometry_probes_run.py
autonomous: true
requirements: [GEOM-03, GEOM-04, GEOM-05]

must_haves:
  truths:
    - "Every reported number comes from a function first proved correct against a synthetic with a known answer — closing the Wave 0 gap 02.1-VALIDATION.md records (GEOM-04)"
    - "Correction blindness demonstrated, not argued: clipping and Lingoes shift applied to the real spectrum + two synthetic controls, zero negative mass by construction every time (GEOM-03)"
    - "Krein reconstruction measured over the same 200,000-pair sample at every (p,q) rung, q=0 as the classical-MDS baseline (GEOM-03, GEOM-05)"
    - "The pair sample proved bit-identical to Phase 2's cached geo_pairs_r2 before any distortion number is computed (GEOM-05)"
    - "Working dimension re-derived with the gate verdict's verbatim kneedle criterion, reported as a (p, q) pair (GEOM-05)"
    - "Sampled Gromov delta measured against tree and flat-Euclidean anchors at matched n, identical estimator (GEOM-04)"
    - "Runner reproduces r=0.052419 / m=0.412071 from cache before computing anything else (GEOM-03)"
    - "Recomputed B cross-checked: bottom-40 eigenvalues match the 40 most-negative of eigvals_all at rtol=1e-8 (GEOM-03)"
  artifacts:
    - path: "notebooks/pu_manifold/geometry_probes.py"
      provides: "draw_geo_pairs, sampled_delta_hyperbolicity, krein_mass_capture, pseudo_euclidean_sq_distances, distortion_stats, correction_blindness, kneedle_elbow — pure numpy/scipy, tested"
      contains: "def correction_blindness"
    - path: "notebooks/pu_manifold/tests/test_geometry_probes.py"
      provides: "Synthetic fixtures with known answers for every probe"
      contains: "def test_"
    - path: "notebooks/diagnostics/geometry_probes_run.py"
      provides: "Runner over the frozen Phase 2 cache"
      contains: "43cf438bc944c509"
    - path: "notebooks/.cache/krein_bottom_43cf438bc944c509.npz"
      provides: "eigvals_bottom (40,), eigvecs_bottom (10000, 40) — cached bottom eigenpairs (gitignored)"
      contains: "eigvals_bottom"
    - path: "notebooks/.cache/geometry_probes_43cf438bc944c509.json"
      provides: "Every measured number, manifest-bound to its fit (gitignored)"
      contains: "delta_rel_max"
  key_links:
    - {from: "geometry_probes_run.py", to: "isomap_43cf438bc944c509.joblib", via: "mmap_mode='r', dist_matrix_ only; np.array(..., copy=True) before in-place arithmetic (asarray on read-only memmap returns a view)", pattern: "mmap_mode"}
    - {from: "geometry_probes_run.py", to: "mds_eigenspectrum_43cf438bc944c509.npz", via: "eigvals_all (self-check + corrections), eigvals_top/eigvecs_top (positive block), geo_pairs_r2 (ground truth)", pattern: "geo_pairs_r2"}
    - {from: "geometry_probes_run.py", to: "geometry_probes_43cf438bc944c509.json", via: "json_cache with cfg carrying fit_key + every probe constant — edits raise a manifest mismatch", pattern: "json_cache"}
    - {from: "test_geometry_probes.py", to: "geometry_probes.py", via: "submodule import; __init__.py never touched", pattern: "from pu_manifold import geometry_probes"}
  prohibitions:
    - {requirement_id: "GEOM-03", category: "dependencies", status: "resolved", verification: "automated", statement: "MUST NOT install any package — probes are numpy/scipy/joblib over on-disk artifacts; SUS gate per pre-registration."}
    - {requirement_id: "GEOM-04", category: "scope", status: "resolved", verification: "automated", statement: "MUST NOT modify __init__.py, cache.py, subsample.py, either notebook, src/effdim/, or pyproject.toml."}
    - {requirement_id: "GEOM-05", category: "state", status: "resolved", verification: "automated", statement: "MUST NOT rebuild the graph, re-fit Isomap at any k, or regenerate under an existing cache key — new stems only."}
    - {requirement_id: "GEOM-03", category: "values", status: "resolved", verification: "judgment", statement: "MUST NOT report a corrected spectrum's zero negative mass as a result about this data — always emit beside its synthetic-control twin."}
    - {requirement_id: "GEOM-05", category: "values", status: "resolved", verification: "judgment", statement: "MUST NOT collapse a pseudo-Euclidean dimension to one integer — the pair (p, q) is the point."}
---

<objective>
Measure the three things measurable cheaply and honestly from artifacts already on disk, so
02.1-04's judgments rest on numbers: (1) correction blindness — clipping/Lingoes force a passing
gate on any input, demonstrated rather than cited; (2) the Krein (p,q) distortion ladder — nearly
free since spectrum, top-40 eigenvectors and pair sample are cached, only one bottom-block
eigensolve added, with q=0 as the classical-MDS baseline; (3) delta-hyperbolicity vs tree and
flat anchors — turning RESEARCH.md's A7 heterogeneity inference into a measurement. Deliberately
NOT done: hyperbolic/product embedding fits or graph-native curvature (pipeline work + SUS
installs; the survey costs them).
</objective>

<key_interfaces>
`pu_manifold.cache` is the only cache path: `config_key`, `cache_path`, `npz_cache`, `json_cache`,
manifest sidecars, raise-on-cfg-mismatch; json_cache cannot serialise numpy scalars — cast first.

Cache (fit_key 43cf438bc944c509): isomap joblib (~1.55 GiB, dist_matrix_ 10000² f8);
mds_eigenspectrum npz (eigvals_all 10000, eigvals_top/eigvecs_top 40-wide, mds_coords,
geo_pairs_r2 + check 200000, n_positive, k_eff) with meta (d_sweep_max 40, r2_pair_count 200000,
r2_pair_seed 20260731, check seed 20260732); mds_residuals npz; codiag_k15 npz (edge_lengths
150000); gate_verdict json (r, m, verdict, thresholds, d_frozen 5, d_provisional 18,
elbow_criterion).

Self-check values (02-FINDINGS §3.4/§3.6): r=0.0524192078526829, m=0.4120712514841815,
n_positive=4971, n_negative=5029, lambda_max_pos=3230.8539634646067,
lambda_min_neg=-169.35880545251558, noise_floor=7.173936918879702e-09.

Gotchas Phase 2 paid for: (1) np.asarray on a read-only float64 memmap returns a VIEW — use
np.array(..., copy=True); (2) mean-form double-centring, verified ≡ literal J-form at rtol=1e-12.
Pair draw idiom (02-PATTERNS): integers draw + reject/redraw self-pairs; seed 20260731, count
200000; bit-identity asserted against cache, notebook §6 cell is source of truth on mismatch.

Existing tests: test_pu_manifold.py, 14 tests, sys.path.insert(parents[2]) header — copy verbatim.
Compute envelope: ~800 MB matrix, ~120 s dense eigensolve, budget ~4 GiB peak; warm path seconds.
</key_interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The probe module and its Wave 0 fixtures</name>
  <produced>
    Tests first (committed failing), then the module; two commits. Behaviors proved:
    - `draw_geo_pairs`: no self-pairs, seed-reproducible bit-identical.
    - `sampled_delta_hyperbolicity`: 0 within float tolerance on a weighted tree (four-point
      condition exact); materially >0.05 on 300 normals in R^20; reproducible; returns delta_max,
      delta_p95, delta_mean, diam_sample, delta_rel_max, delta_rel_p95, n_quadruples.
    - `krein_mass_capture`: q-invariant on PSD spectra; monotone in p and q; =1.0 on full cover.
    - `pseudo_euclidean_sq_distances`: exact (rtol=1e-8) on a full signed eigendecomposition of an
      n=40 non-Euclidean graph metric; positive-only (q=0) restriction provably biased upward.
      Signed squared distances returned — no abs, no sqrt (negative squared distance is the
      property the representation exists to express).
    - `distortion_stats`: median_abs_rel, median_signed_rel, p95_abs_rel; zeros on identity;
      +0.10 on uniform 10% inflation.
    - `correction_blindness`: m_before per gate_stats; m_after_clip == m_after_shift == 0.0 on
      indefinite, PSD, and non-metric spectra, parameterised over 20+ random spectra (Cailliez not
      implemented: 2n×2n solve demonstrating nothing extra — documented).
    - `kneedle_elbow`: implements gate_verdict's elbow_criterion verbatim (quoted in docstring);
      lower-index tie-break on a straight line; affine-rescaling invariant.
    Module numpy/scipy only, no file I/O, arrays in / arrays-or-dicts out.
  </produced>
  <done>Every later number comes from a function with an independently known synthetic answer; correction blindness is a measured property of 25 arbitrary spectra.</done>
</task>

<task type="auto">
  <name>Task 2: Run the probes over the frozen cache and write the measured artifact</name>
  <precondition>isomap and mds_eigenspectrum artifacts on disk (gitignored, not producible here); halt and report if missing — regenerating changes provenance.</precondition>
  <produced>
    `geometry_probes_run.py` following gate-diagnostics shape (docstring, constants as literals
    with pre-registration section named, labelled step blocks):
    Step 0 self-check (r/m/counts vs published, nothing runs until it passes).
    Step 1 pair identity: re-draw under default_rng(20260731), assert dist_matrix_[rows,cols]
    bit-identical (exact equality) to cached geo_pairs_r2; halt naming §6 as source of truth.
    Step 2 delta: full matrix at DELTA_QUADRUPLES/DELTA_SEED + three matched-n=2000 controls
    (real subset, weighted spanning tree via shortest_path, 20-d normals) — real at both n removes
    the sample-size confound; four delta_rel values in one table; pre-registered rule applied
    unedited.
    Step 3 Krein bottom block: copy=True, square, mean-form centre (size printed before
    allocation); eigh(subset_by_index=[0,39]); npz_cache under krein_bottom_* stem; bottom-40
    cross-checked vs eigvals_all at rtol=1e-8 (proves same matrix); release B.
    Step 4 (p,q) ladder: P_LADDER × Q_LADDER from cached top + new bottom blocks; distortion_stats
    vs geo_pairs_r2²; per-rung mass_capture + count of negative reconstructed squared distances;
    kneedle twice (p at q=0 baseline; best-per-total-dimension frontier) → (p, q); d_frozen=5
    printed beside, no adjudication (02.1-04's job).
    Step 5 correction blindness on real + flat + non-metric spectra, one table, corrected columns
    identical by construction — never printed without twins.
    Step 6 artifact: flat dict (numpy cast), json_cache under geometry_probes_* with cfg carrying
    fit_key + all probe constants; echoes d_frozen/d_provisional/elbow_criterion/r/m/spectrum so
    self-contained; required-keys assert.
    Script run; the run is the deliverable.
  </produced>
  <done>Every claim 02.1-04 makes about corrections, negative-direction gains, and working dimension has a measured number computed on the proven-identical sample by proven functions.</done>
</task>

</tasks>

<threats>
T-02.1-SC (installs: none; module imports no torch; verify asserts untouched files). T-02.1-07
(joblib pickle: accept — locally generated, gitignored, path-contained by _assert_inside_cache;
re-fitting to avoid it would change provenance). T-02.1-08 (constant edits vs results: cfg-keyed
manifests raise on mismatch). T-02.1-09 (sample substitution: bit-identity + rtol=1e-8 eigenvalue
cross-check, both halting). T-02.1-04 (disclosure: accept — public data, summary stats,
gitignored).
</threats>

<verification>
`python -m pytest notebooks/pu_manifold/tests/ -q` (existing 14 + new, all green);
`PYTHONPATH=notebooks python notebooks/diagnostics/geometry_probes_run.py` end-to-end;
artifact keys present, pair_identity_verified true, ladder ≥40 rungs incl. q=0, working_dimension
a (p,q) int pair, 4/4 delta anchors; krein npz shapes + rtol=1e-8 agreement + sidecar;
`git diff --quiet` on protected files.
</verification>

<output>
`02.1-03-SUMMARY.md`: measured delta_rel values + what the reading rule says; (p,q) and q=0
baseline beside d_frozen=5; correction table; whether pair identity passed first try; cold-path
wall clock + peak RSS; any number contradicting RESEARCH.md expectations.
</output>
