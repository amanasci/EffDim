---
phase: 04-region-partitioning-regional-alignment-mknn
plan: 06
type: execute
wave: 5
depends_on: [04-05]
files_modified:
  - notebooks/04_region_partition_mknn.ipynb
  - .planning/phases/04-region-partitioning-regional-alignment-mknn/04-FINDINGS.md
  - .planning/phases/04-region-partitioning-regional-alignment-mknn/COVERAGE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [REGN-02, MKNN-07, MKNN-08]

flagged_assumptions:
  - "This plan's central deliverable is a set of prose statements. No mechanical check can confirm that a caveat is stated honestly rather than merely present as a string; the acceptance criteria below check presence and specific content, and the honesty of the framing is a human judgement recorded as such in 04-VALIDATION.md's Manual-Only table."
  - "D4-12: no new Swiss roll notebook is delivered for the direction-partition rule or for MKNN. The reasoning is stated in the phase record in the phase's own words rather than the notebook being silently omitted. Both the sign split and MKNN fall outside CLAUDE.md's rule, which targets models that map data to a lower-dimensional representation and back or that claim to recover manifold structure."

must_haves:
  truths:
    - "The phase record states, in the phase's own words rather than by reference, that the curvature field is accepted on split-half reliability alone and that split-half reliability cannot detect a bias both halves share"
    - "The phase record states, in the phase's own words, that the direction partition rests on codimension-1 evidence applied to a codimension-748 problem, and that no fixture validation was run"
    - "The phase record states, in the phase's own words, that the density confound is reported and not controlled, and that a regional MKNN difference cannot be separated from a regional density difference by anything in this phase"
    - "The phase record contains the sentence, or its clear equivalent, that Phase 4 produces its result with no known-answer anchor at any point in the chain — estimator, field, or partition — and presents that as a deliberate, consistently made developer choice"
    - "The phase record states why CLAUDE.md's standing Swiss roll rule is not triggered by the sign-split partition or by MKNN, rather than omitting the notebook silently"
    - "PU's measured ||H|| dynamic range appears in the record as a stated number, beside the bowl / cubic / ridge calibration that gives it meaning"
    - "'No detectable difference' is presented as a valid reported outcome wherever the verdict appears, never as a failure of the phase"
    - "The hubness caveat appears in the record with its substantiating k-occurrence skewness numbers beside it"
    - "REQUIREMENTS.md's coverage rows for REGN-01..06 and MKNN-01..08 record each requirement's actual outcome, including any answered-negatively or undefined result, rather than a blanket Complete"
    - "COVERAGE.md exists in the phase directory recording that this phase integrates no external API, with a one-line reason"
  prohibitions:
    - "MUST NOT present the codimension-1 cosine evidence as validating direction resolution within a high-dimensional normal space"
    - "MUST NOT present the density confound as controlled, or the field as validated, anywhere in the record"
    - "MUST NOT present 'no detectable difference' as a phase failure or hedge it into inconclusiveness"
    - "MUST NOT quote the decoder-versus-cloud head-to-head numbers as if both arms had been scored on the same fixture as the sealed saddle control, and MUST NOT quote them as a clean disqualification of a well-trained decoder"
    - "MUST NOT reopen, soften, recompute or reinterpret any sealed verdict from Phases 2, 02.x, 3 or 03.1"
    - "MUST NOT mark a requirement Complete whose evidence is an undefined cell or an unfired rule"
  artifacts:
    - path: ".planning/phases/04-region-partitioning-regional-alignment-mknn/04-FINDINGS.md"
      provides: "The phase record: results, the three accepted gaps, and the requirement outcomes"
      contains: "no known-answer anchor"
    - path: ".planning/phases/04-region-partitioning-regional-alignment-mknn/COVERAGE.md"
      provides: "The no-external-API declaration for the seal-time coverage gate"
    - path: "notebooks/04_region_partition_mknn.ipynb"
      provides: "Executed section 9: the accepted gaps, the Swiss roll reasoning, and the citations"
  key_links:
    - from: ".planning/phases/04-region-partitioning-regional-alignment-mknn/04-FINDINGS.md"
      to: "notebooks/04_region_partition_mknn.ipynb"
      via: "every number quoted in the findings is traceable to a printed notebook cell or a cache artifact"
      pattern: "04_region_partition_mknn"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/04-region-partitioning-regional-alignment-mknn/04-FINDINGS.md"
      via: "each Phase 4 coverage row's outcome text points at the findings section carrying its evidence"
      pattern: "REGN-0|MKNN-0"
---

<objective>
Close the phase with a record that states what was measured and, at the same standard, what was not.

Purpose: three accepted gaps travel with every number this phase produced, and CONTEXT.md, STATE.md
and `03-NOTE-phase-4-decisions.md` all require them stated in Phase 4's own words rather than by
reference, on the standard `03-FINDINGS.md` section 1 was held to. Taken together they mean this
phase produced its result with no known-answer anchor at any point in the chain — estimator, field,
or partition. That is a deliberate and consistently made developer choice, and the record has to
present it as one rather than let a future reader discover it.

Output: notebook section 9 executed; `04-FINDINGS.md`; `COVERAGE.md`; the Phase 4 rows of
`.planning/REQUIREMENTS.md` updated with honest outcomes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@.claude/skills/spike-findings-effdim/SKILL.md
@.planning/phases/04-region-partitioning-regional-alignment-mknn/04-CONTEXT.md
@.planning/phases/04-region-partitioning-regional-alignment-mknn/04-PREREGISTRATION.md
@.planning/phases/03-decoder-curvature-field/03-NOTE-phase-4-decisions.md
</context>

<artifacts_this_phase_produces>
Symbols and files this phase CREATES (new — exclude from drift/source-grounding checks):

**`notebooks/pu_manifold/mknn.py`** (plan `04-01`) — `_membership_matrix`, `mknn_score`,
`permutation_null`, `bootstrap_ci`, `hubness_skewness`, `chance_floor`.

**`notebooks/pu_manifold/region_partition.py`** (plan `04-03`) — constants `MIN_NORM_PERCENTILE`,
`MIN_REGION_N`, `MKNN_K_GRID`, `HEADLINE_K`, `NULL_QUANTILE`, `CONFIDENCE_LEVEL`, `N_PERMUTATIONS`,
`N_BOOTSTRAP`, `FIELD_D`, `K_DENSITY`, `SEED`, `K_FROZEN`, `K_FREEZE_RULE`, `COVARIANCE_FORM`,
`VERDICT_RULE`; functions `canonical_eigvec_sign`, `region_partition`, `region_counts`,
`assert_preregistered`.

**`notebooks/pu_manifold/tests/test_region_partition.py`** (plan `04-03`) — new test file.

**`notebooks/diagnostics/region_partition_mknn_run.py`** (plan `04-01`, extended in `04-04`/`04-05`) —
`load_pu_pair`, `run_global_cell`, `run_partition`, `run_regional_cell`, `apply_verdict`,
`selfcheck`, `build_arg_parser`, `main`; flags `--mode {global,partition,regional}`, `--mknn-k`,
`--d`, `--k-density`, `--n-permutations`, `--n-resamples`, `--seed`, `--record-path`,
`--partition-stem`, `--selfcheck`, `--smoke`.

**`notebooks/diagnostics/pu_curvature_rankability_run.py`** (plan `04-02`, extended additively) —
`K_FREEZE_RULE`, `measure_r_over_R`, `freeze_k`; flags `--density-correct`, `--k-density`,
`--freeze-out`; record keys `density_correct`, `k_density`, `r_knn`, `R_cloud`, `r_over_R`,
`subsample_file`.

**`notebooks/04_region_partition_mknn.ipynb`** — new notebook, sections 0-9.

**Cache artifact keys** (gitignored, reproducible from a runner): `04_region_partition_mknn.jsonl`,
`04_pu_curvature_rankability_corrected.jsonl`, `04_k_freeze.json`, `04_region_partition.npz`
(stem `04_region_partition`; keys `v`, `labels`, `keep_idx`, `excluded_idx`, `h_norm`,
`signed_projection`), `04_density_diagnostics.json`.

**Planning artifacts:** `04-PREREGISTRATION.md`, `04-FINDINGS.md`, `COVERAGE.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Notebook section 9 — the accepted gaps, the Swiss roll reasoning, and the citations</name>
  <files>notebooks/04_region_partition_mknn.ipynb</files>
  <read_first>
    - `notebooks/04_region_partition_mknn.ipynb` — sections 0-8. Append only; nothing above may be reordered or edited.
    - `.planning/phases/03-decoder-curvature-field/03-FINDINGS.md` section 1 — the standard this section must meet for how an inherited gap is stated in a phase's own words. Read it before writing; it is the calibration, not a template to copy.
    - `.planning/phases/03-decoder-curvature-field/03-NOTE-phase-4-decisions.md` — D4-03's blind spot, Amendment 01's codimension caveat, Amendment 02's undertrained-decoder caveat and the exact numbers each rests on.
    - `.planning/phases/04-region-partitioning-regional-alignment-mknn/04-CONTEXT.md` § Specific Ideas — the three gaps enumerated, and the sentence about no known-answer anchor.
    - `.planning/phases/04-region-partitioning-regional-alignment-mknn/04-RESEARCH.md` § Sources — the confidence markers each citation must carry.
  </read_first>
  <action>
Append section 9 to the notebook and re-execute it end to end. This section is markdown with a small
number of printed numbers pulled from the cache artifacts, so a reader sees each caveat next to the
figure it qualifies rather than as detached prose.

9.1 — The inherited field. State that the PU curvature field this phase partitioned is accepted on
split-half reliability alone: `R_H` measured 0.589 at `k = 231` uncorrected, and this phase's own
corrected value at `K_FROZEN` printed here from `04_k_freeze.json`. State that split-half reliability
cannot detect a bias both halves share, because both halves use the same estimator, the same `k` and
the same architecture, and that this was measured directly on the Swiss roll where the answer is
known: `R_H = 0.990` alongside `rho = 0.469` — near-perfect reproducibility beside mediocre accuracy.
State that there is no ground truth on PU, so this gap cannot be closed by more of the same
measurement, that cross-estimator agreement was recommended and declined (D4-03, reaffirmed by
D4-08), and that every number in this notebook therefore rests on an unvalidated field. Note that
the mitigation remains available at one cell and was declined for reasons recorded in D4-08, not
overlooked.

9.2 — The codimension gap. State that the partition splits on curvature direction (D4-01) and that
this decision rests on spike 003's measurement that direction survives at `d = 20` (cosine
0.77-1.000) while magnitude is roughly 50x attenuated and its ordering saturates at `rho` around
0.5-0.65. Then state the limit on that evidence in this phase's own words: every fixture in spike
003 is a codimension-1 graph, where `H = H_scalar * n_hat` and the curvature direction *is* the
surface normal, so a cosine near 1.000 demonstrates recovery of normal ORIENTATION — a tangent-space
problem known to converge — and not resolution of `H`'s direction within a high-dimensional normal
space. PU is roughly `d = 20` inside `D = 768`, codimension about 748, and that is exactly the
regime where the distinction matters. State that no known-answer fixture validation was run before
the split was frozen (D4-10, which overrides D4-01's body text naming the ridge check a Phase 4
precondition), and why: narrowing codimension 1 to 8 against 748 risks reading as closure rather
than narrowing. Print the top eigenvalues of the unit-`H` covariance from
`04_density_diagnostics.json` beside this, since a low effective rank is the shape the caveat is
about.

9.3 — The density confound. State that under D4-14 the battery is the REGN-02 correlation only:
no centroid-distance check, no partial regression, no density-matched stratification, no
density-matched null. Print this run's two Spearman correlations and the region-level Mann-Whitney
comparison. Then state the consequence plainly: MKNN is itself a k-NN statistic and therefore
directly density-sensitive, so without a density-matched null a regional MKNN difference cannot be
separated from a regional density difference by anything in this phase, and the correlations printed
above are the only evidence bearing on it.

9.4 — Taken together. One short paragraph stating that these three gaps mean Phase 4 produced its
result with no known-answer anchor at any point in the chain — estimator, field, or partition — and
that this is a deliberate and consistently made developer choice rather than an oversight, presented
as one.

9.5 — Why no Swiss roll notebook (D4-12). State the reasoning in this phase's own words:
`CLAUDE.md`'s standing rule targets models that map data to a lower-dimensional representation and
back, or that claim to recover manifold structure. The diametrical sign split reads an
already-computed vector field and produces two labels — no learned mapping, no latent space, no
reconstruction, no manifold-recovery claim. MKNN is a metric computed on two given embeddings and
learns nothing either. The estimator that produces the field is already covered by
`notebooks/02.5_swiss_roll_curvature_probe_check.ipynb`, and the sign split has its own known-answer
coverage in `notebooks/pu_manifold/tests/test_region_partition.py`'s two-antipodal-cone fixture,
which is a stricter test of the split than a Swiss roll would be. The rule is not waived; it does
not apply. Say so rather than leaving the absence unexplained.

9.6 — Citations, each with its confidence marker. arXiv:2509.19453 (Duraphe, Smith, Sourav & Wu,
*The Platonic Universe*) — read directly as a PDF; the source of MKNN-01's formula and MKNN-02's
0.34%-2.25% Legacy-vs-HSC range at n around 102k. The paper attributes the metric to Chechik et al.
2010; that attribution was not independently re-verified here and is repeated as the paper's own
citation choice. The paper does not state the `k` behind Table 2 in its visible text; this phase's
`k` grid comes from MKNN-06 and the comparison that carries meaning is ratio-over-chance, not raw
matching. Radovanovic, Nanopoulos & Ivanovic, JMLR 2010 — the k-occurrence definition behind the
hubness statistic; read in secondary summary only. Dhillon, Marcotte & Roshan, Bioinformatics 19(13),
2003 — the literature name (diametrical clustering) for D4-09's already-locked sign split; read in
secondary summary only, and a naming claim rather than a methodological dependency.
  </action>
  <verify>
    <automated>.venv/bin/python -m jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=1800 notebooks/04_region_partition_mknn.ipynb && .venv/bin/python -c "
import json
nb=json.load(open('notebooks/04_region_partition_mknn.ipynb'))
code=[c for c in nb['cells'] if c['cell_type']=='code']
assert all(c.get('execution_count') for c in code), 'unexecuted code cell present'
counts=[c['execution_count'] for c in code]
assert counts == sorted(counts), 'execution counts not in cell order'
blob=' '.join(''.join(c['source']) for c in nb['cells']).lower()
for needle in ('0.990','0.469','codimension','748','no known-answer anchor','swiss roll','2509.19453','radovanovic','dhillon','density-matched'):
    assert needle.lower() in blob, needle
pre=min(i for i,c in enumerate(nb['cells']) if 'assert_preregistered' in ''.join(c['source']))
reg=min(i for i,c in enumerate(nb['cells']) if 'mknn_regional' in ''.join(c['source']))
assert pre < reg, (pre, reg)
print('section 9 present; pre-registration cell', pre, 'precedes first regional cell', reg)
"</automated>
  </verify>
  <acceptance_criteria>
    - The notebook contains a section 9 with subsections covering the inherited field, the codimension gap, the density confound, the taken-together statement, the Swiss roll reasoning and the citations
    - The notebook text contains `0.990` and `0.469` (the Swiss roll reliability-versus-accuracy pair), `748` (PU's codimension) and the phrase `no known-answer anchor`
    - Section 9.5 names `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb` and `notebooks/pu_manifold/tests/test_region_partition.py` as the existing known-answer coverage
    - Section 9.6 lists arXiv:2509.19453 as directly read, and marks the Radovanovic and Dhillon citations as read in secondary summary only
    - Section 9.3 prints this run's two Spearman correlations and the Mann-Whitney statistic as numbers, not as references
    - Every code cell has a non-null `execution_count` and the counts are strictly increasing in cell order
    - The `assert_preregistered` cell index remains strictly less than the first `mknn_regional` cell index
    - No cell in sections 0-8 was reordered, edited or deleted
  </acceptance_criteria>
  <done>The notebook states all three accepted gaps in the phase's own words beside the numbers they qualify, explains the absent Swiss roll notebook rather than omitting it, and carries its citations with confidence markers.</done>
</task>

<task type="auto">
  <name>Task 2: 04-FINDINGS.md, COVERAGE.md, and honest requirement outcomes</name>
  <files>.planning/phases/04-region-partitioning-regional-alignment-mknn/04-FINDINGS.md, .planning/phases/04-region-partitioning-regional-alignment-mknn/COVERAGE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/phases/03-decoder-curvature-field/03-FINDINGS.md` — the structure and the honesty standard for a phase findings document in this project, particularly section 1 and section 9.
    - `.planning/REQUIREMENTS.md` lines 100-125 and 218-245 — the REGN and MKNN requirement text (as re-minted at plan time) and the coverage table rows to update. Note how CURV-07 is recorded as `Answered (negative, conditioned on the override)` rather than Complete; that is the precedent for an honest outcome.
    - All five prior SUMMARY files in this phase directory, plus `04-PREREGISTRATION.md`.
    - `notebooks/.cache/04_k_freeze.json`, `04_density_diagnostics.json` and `04_region_partition_mknn.jsonl` — every number quoted in the findings must come from one of these or from a printed notebook cell.
  </read_first>
  <action>
Write `04-FINDINGS.md` in the phase directory. Structure it so a reader who opens it cold gets the
result and its qualifications in that order, never one without the other.

Section 1 — What this phase claims and what it deliberately does not. Lead with the three accepted
gaps from notebook section 9, in the phase's own words at full length (the notebook carries the
short form beside the numbers; this is the long form), and the no-known-answer-anchor sentence.
Section 1 comes first for the same reason `03-FINDINGS.md` section 1 does: a reader must not reach
the result before the conditions on it.

Section 2 — The frozen configuration. `K_FROZEN` and whether D4-07's rule fired; the corrected
sweep's per-`k` table with `median_R_H`, the direction axis `median r_dir`, and `r/R` for every `k`;
`d = 20`, `k_density = 30`, `density_correct = True`, `SEED`, and the resolved `subsample_*.npz`.
State that the four earlier uncorrected `R_H` rows are superseded, not extended, and that the
density correction is adopted on its measured ~8-10% median-relative-error reduction on a curved,
skewed fixture — not on the retracted flat-fixture claim.

Section 3 — The partition. The exclusion floor and excluded count, both region counts and
percentages, the exact-zero-projection count, `mean_unit_norm`, the unit-`H` covariance eigenvalue
spectrum, and whether either region fell below `MIN_REGION_N`. Report PU's measured `||H||` spread
as a stated number beside the bowl / cubic / ridge calibration (D4-05: roughly 4.8x, against 1.4x
unrankable and 28.2x / 34.3x rankable), noting PU sits far nearer the unrankable end, that this
gates nothing because direction is a unit vector and does not consume the magnitude spread, and that
dynamic range does not predict rankability in any case — spike 003 measured `rho = +0.48` at a
spread of 1.1x and `rho = +0.36` at 36x on the same fixture family.

Section 4 — Density. Both Spearman correlations with p-values and `n`, the region-level medians,
IQRs and Mann-Whitney result, and the reported-not-controlled statement.

Section 5 — MKNN. The global reproduction against the paper's range with the chance floor and the
`n` mismatch; the eight-cell regional table; the hubness statistics; the verbatim pre-registered
verdict rule and the headline verdict. If the verdict is "no detectable difference", write it as a
result, in a sentence that stands on its own without hedging — MKNN-07 makes it explicitly valid.

Section 6 — Requirement outcomes. One row per requirement REGN-01..06 and MKNN-01..08 with its
outcome and the evidence location. Where a requirement's evidence is an undefined cell, an unfired
freeze rule, or a reported-rather-than-controlled statistic, say so in the row rather than writing
Complete.

Section 7 — What a follow-on phase would need. The four declined mitigations, each with what it
would buy and what it costs, quoted from CONTEXT.md's Deferred Ideas rather than re-argued:
cross-estimator agreement on PU (one cell; converts D4-03's blind spot into a number); codimension
narrowing via `make_multinormal_ridge_control` at `m = 4, 8` (narrows 1 to 8 against 748, does not
close it); density-matched null / partial regression / centroid-distance checks (the controls that
would let a difference be attributed to curvature rather than density); `tests/test_mknn.py` with
exact known answers (restores the package's one-test-per-module convention).

Nothing in this document reopens, softens, recomputes or reinterprets any sealed verdict from
Phases 2, 02.x, 3 or 03.1. Where the decoder-versus-cloud head-to-head is referenced, carry its own
caveat with it: the decoder arm was undertrained relative to Phase 3's sealed fits, so it is not a
clean disqualification of a well-trained decoder, and its numbers were not measured on the same
fixture as the sealed saddle control.

Write `COVERAGE.md` in the phase directory with exactly the reasoned no-external-API declaration the
seal-time coverage gate accepts: a single line stating that this phase integrates no external API,
with its reason — it reads a locally generated `.npz`, computes with numpy, scipy and scikit-learn,
and writes to a gitignored local cache; there is no network call, no SDK and no service anywhere in
its scope.

Update `.planning/REQUIREMENTS.md`'s Phase 4 coverage rows (REGN-01..06, MKNN-01..08) from `Pending`
to the outcome recorded in findings section 6, following the format the Phase 3 rows already use.
Use a scoped edit of those rows only; do not rewrite the file.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import re, pathlib
d = pathlib.Path('.planning/phases/04-region-partitioning-regional-alignment-mknn')
f = (d/'04-FINDINGS.md').read_text()
for needle in ['no known-answer anchor','codimension','748','0.990','0.469','density-matched','R_H','r/R','MIN_REGION_N']:
    assert needle in f, needle
for rid in ['REGN-0%d'%i for i in range(1,7)] + ['MKNN-0%d'%i for i in range(1,9)]:
    assert rid in f, rid
c = (d/'COVERAGE.md').read_text()
assert c.strip().startswith('#') or 'No external API integration' in c, c[:200]
assert 'No external API integration' in c
r = pathlib.Path('.planning/REQUIREMENTS.md').read_text()
rows = [l for l in r.splitlines() if re.match(r'^\| (REGN|MKNN)-0\d ', l)]
assert len(rows) >= 13, len(rows)
assert not any('| Pending' in l for l in rows), [l for l in rows if '| Pending' in l]
print('findings, coverage and requirement rows OK;', len(rows), 'phase-4 rows updated')
" && .venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `04-FINDINGS.md` exists and its section 1 states all three accepted gaps at full length in the phase's own words, ending with the no-known-answer-anchor sentence
    - `04-FINDINGS.md` contains `0.990`, `0.469`, `748`, the phrase `no known-answer anchor`, and PU's measured `||H||` spread beside the bowl / cubic / ridge calibration numbers
    - `04-FINDINGS.md` section 2 reports `median_R_H`, `median r_dir` and `r/R` for every swept `k`, and states whether D4-07's rule fired
    - `04-FINDINGS.md` section 6 has one outcome row for each of REGN-01, REGN-02, REGN-03, REGN-04, REGN-05, REGN-06, MKNN-01, MKNN-02, MKNN-03, MKNN-04, MKNN-05, MKNN-06, MKNN-07 and MKNN-08
    - `04-FINDINGS.md` section 7 lists all four declined mitigations with what each would buy and cost
    - `COVERAGE.md` exists and contains a `No external API integration:` declaration with a one-line reason
    - Every Phase 4 coverage row in `.planning/REQUIREMENTS.md` carries an outcome rather than the pre-execution placeholder status
    - Every number quoted in `04-FINDINGS.md` is traceable to `04_k_freeze.json`, `04_density_diagnostics.json`, `04_region_partition_mknn.jsonl`, or a printed cell in `notebooks/04_region_partition_mknn.ipynb`
    - `git diff .planning/REQUIREMENTS.md` shows changes confined to the Phase 4 coverage rows and, if needed, the coverage arithmetic line — no other section rewritten
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
  </acceptance_criteria>
  <done>The phase record states the result and its three qualifications at the same standard, the requirement coverage table reflects what was actually established rather than what was attempted, and the seal-time coverage gate has its declaration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem -> `.planning/` | This plan writes documentation into the planning tree and appends to one notebook. No new executable surface is created. |
| cache artifacts -> findings prose | Every number in the record is copied from a runner-produced artifact or a printed notebook cell; nothing is recomputed here, so a figure cannot silently diverge from its source. |

No network surface, no authentication, no session, no multi-user access model, no user-supplied
input path (04-RESEARCH.md § Security Domain). ASVS L1; block on `high`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Repudiation | a caveat quietly dropped from the record | high | mitigate | The verify step greps for the specific load-bearing literals (`0.990`, `0.469`, `748`, `no known-answer anchor`, `density-matched`); a dropped caveat fails the task rather than passing silently |
| T-04-11 | Tampering (of the requirement contract) | a whole-file rewrite of `REQUIREMENTS.md` losing rows outside Phase 4 | high | mitigate | The edit is scoped to the Phase 4 coverage rows; the verify step counts the rows and the acceptance criteria require the diff to be confined to them |
| T-04-08 | Information Disclosure | git provenance printed into a committed notebook | low | accept | Only commit hashes and ISO timestamps of files already in this repository are printed |
| T-04-SC | Tampering | package installs | low | accept | This plan installs no package |
</threat_model>

<verification>
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
- `04-FINDINGS.md` and `COVERAGE.md` exist with the content above
- The notebook is committed with executed outputs through section 9
- `.planning/REQUIREMENTS.md`'s Phase 4 rows carry outcomes, and no other section of the file changed
- No sealed verdict from Phases 2, 02.x, 3 or 03.1 is reopened, softened, recomputed or reinterpreted
</verification>

<success_criteria>
The phase record carries its result and its three accepted gaps at the same standard, states in the
phase's own words that there is no known-answer anchor anywhere in the chain, explains rather than
omits the absent Swiss roll notebook, and reports "no detectable difference" — if that is the
outcome — as a valid result. REGN-02's reported-not-controlled posture, MKNN-07's verdict and
MKNN-08's substantiated caveat are all recorded in a document a cold reader can trust.
</success_criteria>

<output>
Create `.planning/phases/04-region-partitioning-regional-alignment-mknn/04-06-SUMMARY.md` when done
</output>
