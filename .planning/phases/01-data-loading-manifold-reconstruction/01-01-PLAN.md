---
phase: 01-data-loading-manifold-reconstruction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/requirements-notebooks.txt
  - notebooks/pu_manifold/__init__.py
  - notebooks/pu_manifold/cache.py
  - notebooks/pu_manifold/subsample.py
  - notebooks/pu_manifold/curvature.py
  - notebooks/pu_manifold/mknn.py
  - notebooks/pu_manifold/tests/test_pu_manifold.py
  - notebooks/01_manifold_and_gate.ipynb
autonomous: false
requirements: [DATA-01, DATA-03, DATA-05, ISO-05]

must_haves:
  truths:
    - "A reader can load exactly the legacysurvey_dinov3_vitb16 config of UniverseTBD/pu-embeddings and no other config, and the notebook asserts the loaded config reports exactly 101725 rows (DATA-01)"
    - "notebooks/01_manifold_and_gate.ipynb section 0 hard-asserts sys.version_info >= (3, 11) with a message naming pyproject.toml's stale >=3.8 floor as LIB-03 drift, then prints a reproducibility header containing the seed, the sklearn/numpy/scipy/faiss versions, and the git commit SHA (DATA-05, D-04)"
    - "notebooks/01_manifold_and_gate.ipynb installs its own dependencies from notebooks/requirements-notebooks.txt in a notebook cell, and pyproject.toml is unmodified (DATA-05, D-03)"
    - "The smoke-config end-to-end path runs subsample -> row-alignment assert -> L2 normalize -> npz cache write -> npz cache read-back -> connectivity check -> Isomap fit -> joblib cache write -> joblib read-back, and the read-back arrays are bit-identical to the written arrays (ISO-05)"
    - "Changing any single field of the cache config dict produces a different cache filename, and loading an artifact whose sidecar manifest cfg differs from the current cfg raises rather than silently reusing it (ISO-05, D-14)"
    - "row_indices is produced by np.sort over np.random.default_rng(seed).choice(n_total, n_rows, replace=False); both hsc and legacysurvey read off that single sorted array in one indexing pass, never via two independent shuffle/select calls (DATA-03)"
    - "load_subsample raises ValueError before reaching rng.choice when n_total < n_rows, n_rows < 2, or n_rows > MAX_N_ROWS (DATA-03)"
  artifacts:
    - path: "notebooks/pu_manifold/cache.py"
      provides: "Config-hash-keyed npz/joblib/json cache helpers with sidecar manifest verification and CACHE_DIR containment guard"
      contains: "def config_key"
    - path: "notebooks/pu_manifold/subsample.py"
      provides: "Seeded row_indices draw, L2 normalization at cache-write time, structural + statistical row-alignment assertions"
      contains: "def load_subsample"
    - path: "notebooks/pu_manifold/__init__.py"
      provides: "Package surface re-exporting cache and subsample symbols; declares curvature and mknn as Phase 3/4 stubs"
    - path: "notebooks/pu_manifold/curvature.py"
      provides: "Phase 3 stub surface (CURV-01..04) raising NotImplementedError"
      contains: "NotImplementedError"
    - path: "notebooks/pu_manifold/mknn.py"
      provides: "Phase 4 stub surface (MKNN-01/04/05) raising NotImplementedError"
      contains: "NotImplementedError"
    - path: "notebooks/pu_manifold/tests/test_pu_manifold.py"
      provides: "Fast synthetic-array tests for the cache key/round-trip/manifest/containment contracts and the subsample guards"
    - path: "notebooks/requirements-notebooks.txt"
      provides: "Notebook-only pinned dependencies (torch, datasets, matplotlib); core numpy/scipy/scikit-learn/faiss-cpu deliberately not re-pinned"
    - path: "notebooks/01_manifold_and_gate.ipynb"
      provides: "Sections 0 and 1: environment/reproducibility header, dependency install, smoke-config end-to-end self-test, section-numbering and output-hygiene policy"
  key_links:
    - from: "notebooks/01_manifold_and_gate.ipynb"
      to: "notebooks/pu_manifold/subsample.py"
      via: "plain import of the notebook-local helper package (never installed, never imported from src/effdim/)"
      pattern: "from pu_manifold import|import pu_manifold"
    - from: "notebooks/pu_manifold/subsample.py"
      to: "notebooks/pu_manifold/cache.py"
      via: "load_subsample wraps its compute path in npz_cache so the subsample is written once per config key"
      pattern: "npz_cache"
    - from: "notebooks/pu_manifold/cache.py"
      to: "notebooks/.cache/"
      via: "CACHE_DIR resolved from __file__ parents[1] / '.cache', already gitignored"
      pattern: "CACHE_DIR"
  prohibitions:
    - requirement_id: "DATA-03"
      category: "values"
      status: "resolved"
      verification: "judgment"
      statement: "MUST NOT weaken, skip, downgrade to a warning, or make optional either half of the row-alignment check (structural sha256/shape equality, or the statistical true-pair-vs-permuted margin). A failing alignment check must halt the notebook — there is no object_id in this dataset."

---

<objective>
Establish `notebooks/` from zero and prove the whole Phase 1 pipeline end-to-end on a cheap
smoke config (n=500) before paying for the minutes-long real Isomap fit.

Purpose: prove the cache-key contract Phases 2-4 all load from (ISO-05, D-14) and the
row-alignment invariant that has no join key to catch it (DATA-03, D-08) before the ~1 GB
analysis artifact exists.

Output: a complete `notebooks/pu_manifold/` package (two modules implemented, two stubbed per
D-02), `notebooks/requirements-notebooks.txt`, a fast pytest suite, and sections 0-1 of
`notebooks/01_manifold_and_gate.ipynb` including a permanent smoke-config self-test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-loading-manifold-reconstruction/01-CONTEXT.md
@.planning/phases/01-data-loading-manifold-reconstruction/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@src/effdim/geometry.py
@src/effdim/metrics.py
@pyproject.toml
</context>

<decisions_implemented>

| ID | Decision | Reversibility | Where |
|---|---|---|---|
| D-01 | Three-notebook layout; Phase 1 writes sections 1-3 of `01_manifold_and_gate.ipynb`, Phase 2 appends to the same file | costly | Task 3, 4 |
| D-02 | Scaffold all four `pu_manifold/` modules; `subsample.py`+`cache.py` implemented, `curvature.py`+`mknn.py` stubbed | reversible | Task 3, 4 |
| D-03 | Pins live in `notebooks/requirements-notebooks.txt`; every notebook's first cell runs `%pip install -r` | reversible | Task 3 |
| D-04 | Hard `sys.version_info >= (3, 11)` assert naming the stale `pyproject.toml` floor, then a reproducibility header | reversible | Task 3 |
| D-05 | L2-normalize always, then Euclidean | one-way — gated by Task 2 | Task 2, 3 |
| D-06 | Both paired columns normalized in `subsample.py` at cache-write time; raw norms stored separately | one-way (follows D-05) | Task 3 |
| D-07 | `row_indices = np.sort(default_rng(SEED).choice(n_total, n_rows, replace=False))`, applied once to both columns; ARCHITECTURE.md wins over STACK.md's `.shuffle().select()` recipe | reversible | Task 3 |
| D-08 | Structural + statistical alignment assertion | reversible | Task 3 |
| D-13 | Persisted-artifact set and sizes | reversible | Task 3 |
| D-14 | Cache key = sha256 over analysis params plus sklearn/numpy/scipy versions; git SHA deliberately excluded | reversible | Task 3 |
| D-15 | `eigen_solver="dense"` pinned explicitly | reversible | Task 3 |

**Discretion resolved this plan** (2 of CONTEXT.md's 6 open items; the other 4 answered in plans 02-04):
- D-08 statistical smoke-test margin: a scale-free z-score against a 50-permutation null (`ALIGNMENT_N_PERMUTATIONS=50`), strict threshold `z > ALIGNMENT_MARGIN_Z` where `ALIGNMENT_MARGIN_Z=5.0` (exactly 5.0 FAILS). Scale-free so it can't false-alarm on the paper's (arXiv:2509.19453) weak-but-correct 0.4-2% Legacy Survey signal.
- Notebook output-stripping policy: `01_manifold_and_gate.ipynb` is committed WITH outputs (deliberate Restart-and-Run-All), no `nbstripout`/`.gitattributes` filter, no cell output embeds a bulk array (10000x768 or the fitted `Isomap` object).

</decisions_implemented>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification before the first dependency install</name>
  <what-built>Approved: torch==2.13.0+cpu (PyTorch CPU wheel index), datasets==5.0.1 (Hugging Face), matplotlib==3.11.1 — all confirmed the legitimate PyPI projects at the pinned versions. numpy/scipy/scikit-learn/faiss-cpu deliberately not re-pinned (already core effdim deps).</what-built>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm the D-05 normalization artifact shape before the cache format is baked in</name>
  <what-built>Decided: normalized-only. `subsample_{seed}_{key}.npz` stores only the L2-normalized `hsc`/`legacysurvey` arrays plus raw norms (`hsc_norms`/`ls_norms`); no raw 768-d arrays cached. One-way: recovering raw vectors later means re-streaming the 553 MiB parquet. Ambient space is unit sphere S^767 — Phase 3's synthetic controls (CURV-06) must match curvature on the sphere, not a flat plane.</what-built>
</task>

<task type="tracer">
  <name>Task 3: End-to-end smoke-config slice — subsample through both cache backends and back</name>
  <what-built>Wired the full path at n=500 smoke scale (`SEED = 20260729`): `pu_manifold/cache.py` (`config_key`/`cache_path`/`npz_cache`/`joblib_cache`/`json_cache`, `KEY_LEN=16`, sidecar `.meta.json` manifest, `_assert_inside_cache` containment guard), `pu_manifold/subsample.py` (`draw_row_indices`, `l2_normalize`, `assert_alignment`, `load_subsample`; constants `N_FEATURES=768`, `EXPECTED_N_TOTAL=101_725`, `MAX_N_ROWS=20_000`), `requirements-notebooks.txt` (3 pins: torch/datasets/matplotlib), and notebook §0-§1 (Python-floor assert, reproducibility header, output-hygiene/section-numbering policy, smoke subsample+alignment+cache-roundtrip+connectivity+Isomap fit+joblib read-back). All cache round-trips bit-identical (`np.array_equal`); a one-field cfg change produces a different `config_key`. `pytest notebooks/pu_manifold/tests/test_pu_manifold.py` passes.</what-built>
</task>

<task type="auto">
  <name>Task 4: Scaffold the Phase 3/4 stub modules and record the inherited conventions</name>
  <what-built>`pu_manifold/curvature.py`: `first_fundamental_form` (CURV-01), `second_fundamental_form` (CURV-02), `mean_curvature_vector` (CURV-03), `metric_condition_number` (CURV-04), each `NotImplementedError` with a full NumPy-style docstring, naming CURV-06's S^767 sphere-matching consequence of D-05. `pu_manifold/mknn.py`: `mknn_score` (MKNN-01), `permutation_null` (MKNN-04), `bootstrap_ci` (MKNN-05), docstring stating `MKNN(z1,z2) = k^-1 * |N_k(z1) ∩ N_k(z2)|` (Chechik et al. 2010, adopted verbatim by arXiv:2509.19453), the MKNN-08 hubness caveat. Neither imports torch/faiss at module level. `__init__.py` extended with a four-module ownership map.</what-built>
</task>

</tasks>

<artifacts_this_phase_produces>

Every symbol/path below is new in this phase (`notebooks/` was empty at plan time). `src/effdim/`
and `pyproject.toml` are read-only for the whole milestone.

| Path | Status |
|---|---|
| `notebooks/pu_manifold/__init__.py` | new |
| `notebooks/pu_manifold/cache.py` | new, implemented |
| `notebooks/pu_manifold/subsample.py` | new, implemented |
| `notebooks/pu_manifold/curvature.py` | new, stubbed (Phase 3 fills) |
| `notebooks/pu_manifold/mknn.py` | new, stubbed (Phase 4 fills) |
| `notebooks/pu_manifold/tests/test_pu_manifold.py` | new |
| `notebooks/requirements-notebooks.txt` | new |
| `notebooks/01_manifold_and_gate.ipynb` | new (§0-§1 in this plan) |

`cache.py` symbols: `CACHE_DIR`, `KEY_LEN`, `config_key`, `cache_path`, `npz_cache`, `joblib_cache`,
`json_cache`, `_assert_inside_cache`. `subsample.py` symbols: `N_FEATURES`, `EXPECTED_N_TOTAL`,
`MAX_N_ROWS`, `ALIGNMENT_MARGIN_Z`, `ALIGNMENT_N_PERMUTATIONS`, `draw_row_indices`, `l2_normalize`,
`row_indices_sha256`, `assert_structural_alignment`, `alignment_smoke_test`, `assert_alignment`,
`load_subsample`. `curvature.py` stub symbols: `first_fundamental_form`, `second_fundamental_form`,
`mean_curvature_vector`, `metric_condition_number`. `mknn.py` stub symbols: `mknn_score`,
`permutation_null`, `bootstrap_ci`. Notebook-level: `SEED`, `SMOKE_CFG`, `ANALYSIS_CFG`, sections
`§0`-`§0.5`, `§1`-`§1.5`. Cache stems introduced here: `subsample_{seed}_{subsample_key}.npz`,
`isomap_{fit_key}.joblib`, sidecar `{stem}.meta.json`. Cache-key fields: `dataset`, `seed`,
`n_rows`, `normalize`, `n_neighbors`, `n_components`, `eigen_solver`, `sklearn_version`,
`numpy_version`, `scipy_version`.

**Cache-key refinement (stated deviation from D-14):** two derived keys via the same `config_key`
— `subsample_key = config_key({dataset, seed, n_rows, normalize, datasets_version, numpy_version})`
(narrower, so an `n_neighbors` change doesn't force a re-download) and `fit_key` (all of D-14's
fields). `KEY_LEN=16` (64 bits) is a deliberate deviation from ARCHITECTURE.md's illustrative `[:8]`
(32 bits) — the collision margin for threat T-01-03.

</artifacts_this_phase_produces>

<verification>
Run from the repository root.

```bash
python -m pytest notebooks/pu_manifold/tests/test_pu_manifold.py -q
python -c "import sys; sys.path.insert(0,'notebooks'); import pu_manifold; from pu_manifold import curvature, mknn; print('package imports OK')"
test -f notebooks/requirements-notebooks.txt && grep -c '==' notebooks/requirements-notebooks.txt
! grep -q 'trust_remote_code' notebooks/pu_manifold/subsample.py notebooks/01_manifold_and_gate.ipynb
! grep -q 'eigvalsh' notebooks/01_manifold_and_gate.ipynb
git diff --quiet -- pyproject.toml src/effdim/ && echo "core untouched"
```

Then, in the notebook: Restart and Run All of §0-§1. Confirm the 3.11 assert passes, the
reproducibility header prints seed/versions/git SHA, both cache round-trips print CACHE HIT, the
two `config_key` values differ, the smoke connected-component count is 1, and `dist_matrix_`
reloads at shape `(500, 500)`.
</verification>

<success_criteria>
- `notebooks/` contains the four-module `pu_manifold` package, its test file,
  `requirements-notebooks.txt`, and `01_manifold_and_gate.ipynb` with §0-§1.
- The tracer's end-to-end smoke path runs and both cache backends round-trip to identical arrays.
- A one-field config change produces a different cache filename; a mismatched sidecar manifest
  raises instead of returning a stale artifact.
- `subsample.py`'s alignment assertion halts on an off-by-one and uses a strict margin comparison.
- Both stub modules raise `NotImplementedError` naming their owning phase and requirement.
- `pyproject.toml` and `src/effdim/` are byte-identical to their pre-plan state.
- The package legitimacy gate cleared with explicit human confirmation before any install.
</success_criteria>

<output>
Create `.planning/phases/01-data-loading-manifold-reconstruction/01-01-SUMMARY.md` when done.
Record: the Task 2 decision outcome and its consequence for `subsample_*.npz` contents; the exact
`SEED` value chosen; the resolved `sklearn`/`numpy`/`scipy`/`faiss`/`datasets` versions from the
§0.3 header; the observed smoke-config `subsample_key` and `fit_key`; the observed smoke alignment
stats (`s_true`, `mu_perm`, `sd_perm`, `z`).
</output>
</content>
