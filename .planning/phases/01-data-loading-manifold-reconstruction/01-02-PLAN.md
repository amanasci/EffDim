---
phase: 01-data-loading-manifold-reconstruction
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - notebooks/01_manifold_and_gate.ipynb
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04, ISO-03]

must_haves:
  truths:
    - "A reader gets a reproducible 10,000-row subsample drawn from the 101,725 available rows under an explicit, printed seed, cached as subsample_{seed}_{subsample_key}.npz, and re-running the cell prints CACHE HIT rather than re-streaming (DATA-02)"
    - "The hsc and legacysurvey arrays are guaranteed row-aligned by an assertion that halts the notebook, not by convention: equal shapes, both read off one row_indices array, the row_indices sha256 re-verified on load, and the true-pair-vs-permuted cosine margin cleared (DATA-03)"
    - "The D-08 alignment margin comparison is strict — z > 5.0 — so a value exactly at the stated margin FAILS rather than passes; a np.roll(legacysurvey, 1, axis=0) off-by-one negative control drives z to roughly zero and the assertion raises (DATA-03; edge probe DATA-03/adjacency)"
    - "A reader can see the raw embedding norm distribution for both hsc and legacysurvey as histograms plus a printed coefficient of variation, reproduced from the cached hsc_norms and ls_norms without re-streaming the dataset (DATA-04)"
    - "A reader can see an explicit, unconditional statement that the pipeline L2-normalizes and then uses Euclidean distance (Isomap's default minkowski p=2, monotone with cosine once normalized so the k-NN sets match cosine exactly), stated as a locked decision and not branched on the histogram (DATA-04, D-05)"
    - "A reader can see the full effdim.compute_dim panel — both the spectral and the geometric estimators — computed on the normalized legacysurvey array and cached as effdim_panel_{seed}_{subsample_key}.json (ISO-03)"
    - "n_components is derived as math.ceil of the median over exactly the eight geometric result-dict keys mle_dimensionality, two_nn_dimensionality, danco_dimensionality, mind_mli_dimensionality, mind_mlk_dimensionality, ess_dimensionality, tle_dimensionality, gmst_dimensionality, with no headroom added, and the spectral keys are excluded from the rule while still being reported (ISO-03, D-12)"
    - "The notebook prints d_provisional and states in the same output that it equals n_components, that its source is the compute_dim geometric median, and that Phase 2's residual-variance elbow may exceed it and force a re-fit (D-10 circularity, D-12 accepted risk)"
  artifacts:
    - path: "notebooks/01_manifold_and_gate.ipynb"
      provides: "Sections 2 and 3 plus the full-scale section 1 run: the 10,000-row subsample, norm histograms and the metric statement, the compute_dim panel, and the D-12 n_components derivation"
      contains: "gmst_dimensionality"
  key_links:
    - from: "notebooks/01_manifold_and_gate.ipynb"
      to: "src/effdim/api.py"
      via: "read-only call to effdim.compute_dim on the normalized legacysurvey array; src/effdim/ is not modified"
      pattern: "compute_dim\\("
    - from: "notebooks/01_manifold_and_gate.ipynb"
      to: "notebooks/pu_manifold/subsample.py"
      via: "load_subsample(ANALYSIS_CFG) followed by assert_alignment on the returned arrays"
      pattern: "load_subsample|assert_alignment"
    - from: "notebooks/01_manifold_and_gate.ipynb"
      to: "notebooks/.cache/effdim_panel_{seed}_{subsample_key}.json"
      via: "json_cache wrapping the compute_dim call so the panel is paid once per subsample key"
      pattern: "json_cache"
  prohibitions:
    - requirement_id: "DATA-03"
      category: "values"
      status: "resolved"
      verification: "judgment"
      statement: "MUST NOT weaken, skip, downgrade to a warning, or make optional either half of the row-alignment check (structural sha256/shape equality, or the statistical true-pair-vs-permuted margin) to let the full-scale run proceed. A failing alignment check must halt the notebook — there is no object_id in this dataset."
    - requirement_id: "ISO-03"
      category: "values"
      status: "resolved"
      verification: "judgment"
      statement: "MUST NOT alter the D-12 selection rule after seeing its output or any downstream sweep/eigenvalue/gate number. The rule is the ceiling of the median over the eight geometric keys with no headroom; the accepted consequence is that a Phase 2 elbow above it forces a re-fit."

---

<objective>
Scale the proven tracer path to the real 10,000-row analysis subsample, then answer DATA-04 and
ISO-03 — the two diagnostics that must be settled before any `n_neighbors` sweep is defensible.

Purpose: produces the actual cached artifact Phases 2-4 load from, shows the norm distribution
justifying the locked L2 decision, and derives `n_components` — which doubles as the provisional
`d` the leading-eigenvalue stability metric needs (resolving D-10's circularity), frozen before
the sweep exists.

Output: `subsample_{seed}_{subsample_key}.npz` at the real scale,
`effdim_panel_{seed}_{subsample_key}.json`, and sections 2-3 of `notebooks/01_manifold_and_gate.ipynb`
with the norm histograms, the metric statement, the full estimator panel, and the frozen
`N_COMPONENTS` / `D_PROVISIONAL`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-loading-manifold-reconstruction/01-CONTEXT.md
@.planning/phases/01-data-loading-manifold-reconstruction/01-PATTERNS.md
@.planning/phases/01-data-loading-manifold-reconstruction/01-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@src/effdim/api.py
@src/effdim/geometry.py
@src/effdim/metrics.py
</context>

<interface_from_plan_01>

**`pu_manifold.cache`** — `CACHE_DIR`, `KEY_LEN = 16`, `config_key(cfg) -> str` (16 hex chars,
sha256 over `json.dumps(cfg, sort_keys=True)`), `cache_path`, `npz_cache`, `joblib_cache`,
`json_cache`. Every write emits a `{stem}.meta.json` sidecar; every load asserts the stored cfg
equals the passed cfg and raises `ValueError` otherwise.

**`pu_manifold.subsample`** — `N_FEATURES = 768`, `EXPECTED_N_TOTAL = 101_725`,
`MAX_N_ROWS = 20_000`, `ALIGNMENT_MARGIN_Z = 5.0`, `ALIGNMENT_N_PERMUTATIONS = 50`,
`draw_row_indices`, `l2_normalize`, `row_indices_sha256`, `assert_structural_alignment`,
`alignment_smoke_test`, `assert_alignment`, `load_subsample(cfg) -> dict` returning `hsc`,
`legacysurvey`, `hsc_norms`, `ls_norms`, `row_indices`.

Notebook state already defined: `SEED`, `SMOKE_CFG`, `ANALYSIS_CFG` (`n_components=None`),
`§0.1`-`§0.5`, `§1.1`-`§1.5`.

**`effdim.compute_dim`** (`src/effdim/api.py:29`) — raises `ValueError` on empty list, wrong
type, `ndim != 2`, `n_samples < 2`, or non-finite values. Centers internally via
`_ensure_centered` (tol `1e-5`); computes k-NN distances once via FAISS at `k=10`, shared across
geometric estimators — the notebook does neither of those itself.

**Verified correction.** `_do_svd` (`src/effdim/api.py:144`) takes the full SVD when
`min(n_samples, n_features) < 1000`; at `(10000, 768)`, `min = 768 < 1000`, so this call takes
the **full-SVD** path, not randomized SVD.

**Verified correction — eight literal geometric keys** (`src/effdim/api.py:102-123`), not seven
(CONTEXT.md's `MiND-ML*` maps to two functions): `mle_dimensionality`, `two_nn_dimensionality`,
`danco_dimensionality`, `mind_mli_dimensionality`, `mind_mlk_dimensionality`, `ess_dimensionality`,
`tle_dimensionality`, `gmst_dimensionality`. Spectral keys, EXCLUDED from the D-12 rule but
STILL REPORTED (`src/effdim/api.py:75-93`): `pca_explained_variance_95`, `participation_ratio`,
`shannon_entropy`, `stable_rank`, `numerical_rank`, `cumulative_eigenvalue_ratio`,
`renyi_eff_dimensionality_alpha_2`/`_3`/`_4`/`_5`, `geometric_mean_eff_dimensionality`.

</interface_from_plan_01>

<discretion_resolved>

**Open item 3 — provisional `d` for the D-10 leading-eigenvalue metric.** Answer:
`d_provisional` IS `n_components` — no new quantity. Not circular in the harmful direction
(`compute_dim` never sees the geodesic graph); removes an arbitrary knob rather than adding one
(every sweep fit uses `n_components=N_COMPONENTS`, so "leading `d`" means "all of them"); frozen
structurally before the §4 sweep exists (§3 in this plan, sweep is §4 in plan 03). Notebook must
print `d_provisional = n_components = <N>`, its D-12-median source, and that Phase 2's elbow may
exceed it and force a re-fit.

Cross-references: plan 01 answers items 2 (D-08 margin) and 4 (output-hygiene); plan 03 answers
item 1 (plateau thresholds); plan 04 answers items 5-6 (section numbering, Phase 1->2 interface).

</discretion_resolved>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Section 1 at full scale — the real 10,000-row subsample with both alignment halves</name>
  <what-built>Appended §1.6/§1.7 to §1 (§1.1-§1.5 smoke cells unmodified). `load_subsample(ANALYSIS_CFG)` returns `(10000, 768)` for `HSC`/`LS`, `(10000,)` for norms/`ROW_INDICES`; every row unit-norm to `1e-5`; `ROW_INDICES` strictly increasing, all in `[0, 101725)`. `assert_alignment` returns `z > 5.0` (printed `s_true`/`mu_perm`/`sd_perm`/`z`; margin comparison strict, restating plan 01's z-score-vs-absolute-cosine justification against the paper's (arXiv:2509.19453) weak 0.4-2% Legacy Survey signal); the `np.roll(LS, 1, axis=0)` negative control raises `ValueError` with `z` near zero. Second `load_subsample` call is a CACHE HIT, bit-identical. Analysis `subsample_key` differs from the smoke key (n_rows differs), so plan 01's artifact isn't clobbered. `try`/`except` appears exactly once, in the §1.7 negative control.</what-built>
</task>

<task type="auto">
  <name>Task 2: Section 2 — norm distribution and the explicit metric statement</name>
  <what-built>§2.1: matplotlib histograms of `HSC_NORMS`/`LS_NORMS` side by side plus a min/median/mean/max/std/CV table for each; reproduced from cache, no re-streaming. §2.2: states unconditionally (not branched on the histogram) that the pipeline L2-normalizes then uses Euclidean (`minkowski p=2`) distance — monotone with cosine on unit-norm vectors, so k-NN sets match cosine exactly; normalization happens in `subsample.py` at cache-write time on both columns (D-06); states the S^767 sphere consequence for Phase 3's CURV-06 synthetic controls. No cell output embeds a bulk array.</what-built>
</task>

<task type="auto">
  <name>Task 3: Section 3 — the compute_dim panel, the D-12 n_components rule, and the frozen provisional d</name>
  <what-built>§3.1: `compute_dim(LS)` on the normalized array, wrapped in `json_cache` (stem `effdim_panel_{SEED}_{subsample_key}`), reported as two tables — the eight geometric keys (INCLUDED in D-12) and the eleven spectral keys (EXCLUDED, reported for ISO-03) — asserting all eight geometric keys present and finite. Notes the full-SVD path (`min(10000,768)=768<1000`) and the ARCHITECTURE.md caveat that spectral estimates read higher than geometric under curvature (supporting evidence, not noise). §3.2: `N_COMPONENTS = math.ceil(median(eight geometric values))`, no headroom added, round-up (never half-to-even) stated explicitly; accepted risk that a Phase 2 elbow above `N_COMPONENTS` forces a re-fit (cheap: `n_components` is a cache-key field, D-14). Sets `ANALYSIS_CFG["n_components"] = N_COMPONENTS`, prints the resulting `fit_key`. §3.3: `D_PROVISIONAL = N_COMPONENTS`, printed with source/pre-sweep-ordering/Phase-2-divergence facts. States SPEC-01..07 (the eigenspectrum audit) are Phase 2's and absent here; `eigvalsh` appears nowhere in the notebook.</what-built>
</task>

</tasks>

<artifacts_this_phase_produces>

Phase-wide inventory lives in `01-01-PLAN.md`; this lists what plan 02 adds and consumes.

**Created (01-02):**

| Filename | Contents | Approx size |
|---|---|---|
| `subsample_{seed}_{subsample_key}.npz` | normalized `hsc`, normalized `legacysurvey`, `hsc_norms`, `ls_norms`, `row_indices` at n=10,000 | ~60 MB |
| `effdim_panel_{seed}_{subsample_key}.json` | full `compute_dim` result dict on normalized legacysurvey | <1 KB |
| `{stem}.meta.json` (per artifact) | full cfg dict, asserted on every load | <1 KB |

Notebook sections: `§1.6`, `§1.7`, `§2`, `§2.1`, `§2.2`, `§3`, `§3.1`, `§3.2`, `§3.3`. Notebook
symbols: `HSC`, `LS`, `HSC_NORMS`, `LS_NORMS`, `ROW_INDICES`, `ALIGNMENT_STATS`, `LS_ROLLED`,
`PANEL`, `GEOMETRIC_KEYS`, `N_COMPONENTS`, `D_PROVISIONAL`. Mutated: `ANALYSIS_CFG["n_components"]`
goes from `None` to `N_COMPONENTS` in §3.2, making the analysis `fit_key` well-defined for plans
03/04.

**Consumed from plan 01** (not new here): `pu_manifold.cache`/`subsample` full symbol set (see
`<interface_from_plan_01>`); notebook `SEED`, `SMOKE_CFG`, `ANALYSIS_CFG`, `§0.1`-`§1.5`.

</artifacts_this_phase_produces>

<verification>
Run from the repository root.

```bash
python -m pytest notebooks/pu_manifold/tests/test_pu_manifold.py -q
! grep -q 'eigvalsh' notebooks/01_manifold_and_gate.ipynb
! grep -Eq 'randomized_svd' notebooks/01_manifold_and_gate.ipynb
git diff --quiet -- pyproject.toml src/effdim/ && echo "core untouched"
ls -la notebooks/.cache/ | grep -E 'subsample_|effdim_panel_'
```

Then, in the notebook: Restart and Run All of §0-§3. Confirm §1.6 prints the five expected
shapes, `ALIGNMENT_STATS` with `z` above 5.0, two differing `subsample_key` values, and a CACHE
HIT on the second call; §1.7 prints the rolled control's near-zero `z` and confirms the raise;
§2.1 renders both histograms and the CV table; §2.2 states the locked metric decision and the
sphere consequence; §3.1 prints both estimator tables; §3.2 prints the eight geometric values,
their median, and `N_COMPONENTS`; §3.3 prints `D_PROVISIONAL` with all four facts.
</verification>

<success_criteria>
- The real 10,000-row row-aligned subsample is cached and reloads to identical arrays.
- The alignment assertion halts on an off-by-one; the negative control is present and required to
  raise.
- Both norm histograms and the coefficient of variation are visible, and the metric decision is
  stated unconditionally with the S^767 consequence carried forward.
- The full `compute_dim` panel is reported as two separated tables covering all eight geometric
  and all eleven spectral keys.
- `N_COMPONENTS` equals the ceiling of the median over exactly the eight geometric keys, with no
  headroom, and `ANALYSIS_CFG["n_components"]` is set from it.
- `D_PROVISIONAL` is frozen and printed with its source, its pre-sweep ordering, and the Phase 2
  divergence branch stated.
- `pyproject.toml` and `src/effdim/` are byte-identical to their pre-plan state.
</success_criteria>

<output>
Create `.planning/phases/01-data-loading-manifold-reconstruction/01-02-SUMMARY.md` when done.
Record: the observed `ALIGNMENT_STATS` (`s_true`, `mu_perm`, `sd_perm`, `z`) and the rolled
control's `z`; the observed coefficient of variation for both norm columns and which side of the
~5% figure it fell on; the eight geometric estimator values, their median, and the resulting
`N_COMPONENTS`; the resolved `D_PROVISIONAL`; the analysis `subsample_key` and the now-well-defined
`fit_key`; and the `row_indices` sha256. Plans 03 and 04 need `N_COMPONENTS`, `D_PROVISIONAL`, and
`fit_key` directly.
</output>
</content>
