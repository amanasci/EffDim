---
phase: 01-data-loading-manifold-reconstruction
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - notebooks/01_manifold_and_gate.ipynb
autonomous: false
requirements: [ISO-04, ISO-05]

must_haves:
  truths:
    - "k* is frozen by applying the plateau rule pre-registered in section 4.0 — the centre of the widest run in which all three adjacent-pair metrics pass, lower member on an even-length tie — and never by taking the smallest connected k or any post-hoc substitute (ISO-04, D-10)"
    - "If no all-three-passing run reaches MIN_PLATEAU_RUN, the notebook halts with remediation options enumerated rather than silently falling back to a different criterion (ISO-04, D-10)"
    - "An Isomap fit at n=10,000 with eigen_solver='dense' completes at k* and is cached as isomap_{fit_key}.joblib carrying dist_matrix_, embedding_, nbrs_, and kernel_pca_, so Phase 2 never re-pays the fit cost (ISO-04, D-13)"
    - "Re-running the notebook reproduces identical results from cache: the reloaded fit's embedding_ is array-equal to the freshly-fitted one and dist_matrix_ has shape (10000, 10000), and the cell prints CACHE HIT rather than re-fitting (ISO-05)"
    - "Any change to dataset, seed, n_rows, normalize, n_neighbors, n_components, eigen_solver, or the sklearn/numpy/scipy versions produces a different fit_key and therefore a different artifact filename, so a stale or incompatible artifact cannot be silently reused (ISO-05, D-14)"
    - "The git commit SHA is deliberately excluded from the cache key, so a docstring-only commit does not invalidate the ~1 GB artifact (ISO-05, D-14)"
    - "phase1_handoff_{fit_key}.json exists as a machine-readable artifact carrying the cache keys, the artifact filenames, k_star and its selection provenance, n_components, d_provisional and its source, the row_indices sha256, the alignment statistics, and the three flags short_circuit_risk, k_auto_extended, and n_components_no_headroom (ISO-05)"
    - "The short-circuit-risk flag surfaces in three independent places — a warnings.warn plus a boxed print at selection time, the machine-readable flags block of the handoff JSON, and a markdown cell that survives in the committed notebook outputs — so it cannot be consumed and dropped by Phase 1 (D-11)"
    - "A terminal section 6 placeholder heading exists naming Phase 2's scope (SPEC-01 through SPEC-07), giving Phase 2 an unambiguous append point and ending Phase 1's notebook deliberately (D-01)"
  artifacts:
    - path: "notebooks/01_manifold_and_gate.ipynb"
      provides: "Section 5: k* freeze, the cached k* Isomap fit, the cache-key demonstration, the Phase 1 to Phase 2 handoff artifact, and the reserved section 6 placeholder"
      contains: "K_STAR"
  key_links:
    - from: "notebooks/01_manifold_and_gate.ipynb"
      to: "notebooks/.cache/isomap_{fit_key}.joblib"
      via: "joblib_cache wrapping the single k* fit; the only artifact in this phase that carries dist_matrix_"
      pattern: "joblib_cache"
    - from: "notebooks/01_manifold_and_gate.ipynb"
      to: "notebooks/.cache/phase1_handoff_{fit_key}.json"
      via: "json_cache writing the machine-readable Phase 1 to Phase 2 interface"
      pattern: "PHASE1_HANDOFF"
    - from: "notebooks/.cache/phase1_handoff_{fit_key}.json"
      to: "Phase 2's gate"
      via: "the flags block, read by Phase 2 before its verdict is computed; short_circuit_risk and n_components_no_headroom are the two the gate must weigh"
      pattern: "short_circuit_risk"
  prohibitions:
    - requirement_id: "ISO-04"
      category: "values"
      status: "resolved"
      verification: "judgment"
      statement: "MUST NOT select or adjust k* by any rule other than the plateau criterion pre-registered in section 4.0, and MUST NOT substitute smallest-connected-k, a hand-picked value, or a widened threshold when no run qualifies. When the criterion is not met the prescribed outcome is a documented halt with remediation options, itself a legitimate complete result."
    - requirement_id: "ISO-04"
      category: "transparency"
      status: "resolved"
      verification: "judgment"
      statement: "MUST NOT present the cached k* fit as a clean or unqualified manifold reconstruction when it required graph bridging or sits above the k=30 short-circuit-risk boundary. The qualifying flags must travel to Phase 2 in the machine-readable handoff artifact and the committed notebook narrative, never resolved away or dropped at the phase boundary."
    - requirement_id: "ISO-05"
      category: "transparency"
      status: "resolved"
      verification: "judgment"
      statement: "MUST NOT report Isomap.kernel_pca_.eigenvalues_ — truncated to n_components by construction — as the full classical-MDS eigenspectrum, and MUST NOT draw any negative-eigenvalue, non-Euclideanity, or Euclidean-embeddability conclusion in this phase. The full spectrum audit and PASS/MARGINAL/FAIL verdict are Phase 2's SPEC-01..07 deliverables and this notebook's §0-§5 must not pre-empt them."

---

<objective>
Freeze `k*` by the pre-registered plateau rule, cache the single full Isomap fit Phases 2-4 all
load from, and write the concrete machine-readable interface Phase 1 hands Phase 2.

Purpose: this is where the phase becomes irreversible. `k*` is baked into the ~1 GB
`isomap_{fit_key}.joblib` and into `fit_key` itself. Plan 03's terminal checkpoint already
confirmed the candidate; this plan applies the rule, writes the artifact, and makes sure the two
qualifications Phase 1 cannot resolve travel forward: the above-30 short-circuit-risk flag (D-11)
and the no-headroom `n_components` that may force a Phase 2 re-fit (D-12).

Output: `isomap_{fit_key}.joblib` (the only artifact in this phase carrying `dist_matrix_`),
`phase1_handoff_{fit_key}.json`, section 5 of `notebooks/01_manifold_and_gate.ipynb`, and the
reserved §6 placeholder that gives Phase 2 an unambiguous append point.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-data-loading-manifold-reconstruction/01-CONTEXT.md
@.planning/phases/01-data-loading-manifold-reconstruction/01-03-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<interface_from_prior_plans>

**`pu_manifold.cache`** — `CACHE_DIR`, `KEY_LEN = 16`, `config_key`, `cache_path`, `npz_cache`,
`joblib_cache`, `json_cache`. Every write emits `{stem}.meta.json`; every load asserts stored cfg
equals passed cfg, raises `ValueError` otherwise.

Notebook state bound: `SEED`, `SMOKE_CFG`, `ANALYSIS_CFG`, `HSC`, `LS`, `HSC_NORMS`, `LS_NORMS`,
`ROW_INDICES`, `ALIGNMENT_STATS`, `PANEL`, `GEOMETRIC_KEYS`, `N_COMPONENTS`, `D_PROVISIONAL`, and
all §4 sweep symbols (`SWEEP_K_RANGE` .. `PLATEAU_RUNS`). Sections `§0.1`-`§4.3` written; this
plan adds `§5` and terminal `§6`.

**The plateau rule (§4.0, apply not redefine):** `k*` is the centre of the widest maximal run of
consecutive `STAGE2_K` values where every adjacent pair has `all_three_pass` true.
`MIN_PLATEAU_RUN = 3`; even-length run centre ties to the **lower-index** k
(`PLATEAU_TIE_BREAK = "lower"`). No qualifying run -> halt.

Plan 03's terminal `checkpoint:decision` already fired and recorded the confirmed `k*` — read
`01-03-SUMMARY.md` for the outcome before doing anything here. `D_PROVISIONAL` equals
`N_COMPONENTS` (plan 02's D-10 resolution); both go into the handoff so Phase 2 sees provenance.

</interface_from_prior_plans>

<discretion_resolved>

**Open item 5 — section-numbering convention.** Top-level `## §N. Title`, subsections
`### §N.M Title`. Phase 1 owns `§0`-`§5` (env/repro, subsample+alignment, norm/metric,
compute_dim panel, sweep, k*-freeze); Phase 2 appends `§6`+ (SPEC-01..07). Existing sections are
never renumbered. D-01 rates this `costly` to change later (every cache path/doc reference is
written against it) — flagged, not gated. Phase 1 ends by writing the `§6` heading as an explicit
placeholder naming Phase 2's scope, so Phase 2 has an unambiguous insertion point.

**Open item 6 — the concrete Phase 1->2 interface.** `notebooks/.cache/phase1_handoff_{fit_key}.json`
plus the same content rendered as a markdown table in §5.3 so it survives committed outputs.
Top-level keys: `phase`, `fit_key`, `subsample_key`, `config` (the 10 D-14 fields), `artifacts`
(resolved filenames incl. `sweep_npz` list), `k_star`, `k_star_selection` (`swept_k`,
`connected_k`, `stage2_k`, `plateau_runs`, `chosen_run`, `criterion`, `thresholds` — a copy of
`PLATEAU_THRESH` so the verdict carries what it was judged against), `n_components`,
`d_provisional`, `d_provisional_source`, `row_indices_sha256`, `alignment_check` (full
`ALIGNMENT_STATS`), `flags` (exactly `short_circuit_risk`, `k_auto_extended`,
`n_components_no_headroom` — the last always `True`, D-12 adds no headroom), `notes_for_phase2`.

D-11's above-30 warning must surface in three independent places so it can't be dropped at the
phase boundary: (1) `warnings.warn` + boxed print at §5.1 selection time, (2)
`flags.short_circuit_risk` in the handoff JSON, (3) a markdown cell in §5.3 that survives in
committed notebook outputs. The D-12 re-fit branch is cheap because `n_components` is a `fit_key`
field — a Phase 2 re-fit at a larger dimension produces a new key automatically, no manual cache
invalidation.

</discretion_resolved>

<tasks>

<task type="auto">
  <name>Task 1: Sections 5.1 and 5.2 — apply the plateau rule, freeze k*, and cache the fit</name>
  <precondition>At least 4 GB free RAM and 2 GB free disk under notebooks/.cache/: the k* fit holds a ~763 MiB dense float64 geodesic matrix with a 2-3 GB transient peak during .fit(), and the resulting joblib artifact is ~1 GB</precondition>
  <reversibility rating="one-way">k* is baked into the cached ~1 GB isomap artifact and the fit_key Phases 2-4 all load from; changing it invalidates the Phase 2 gate verdict, the Phase 3 decoder and curvature field, and every Phase 4 MKNN number. Gated by plan 03 Task 4, which already confirmed the candidate.</reversibility>
  <what-built>`## §5. Freeze k* and cache the fit`. §5.1 (`K_STAR_SELECT=True` marker): reads `MIN_PLATEAU_RUN`/`PLATEAU_TIE_BREAK`/`PLATEAU_THRESH` from §4.0 (no redefinition), selects the widest `PLATEAU_RUNS` entry (lower-k tie-break), asserts length `>= MIN_PLATEAU_RUN` or halts with 3 remediation options (widen range / resample / relax threshold as a *new* pre-registration + full re-run), binds `K_STAR`, cross-checks it against `01-03-SUMMARY.md`'s confirmed value (halts on divergence), re-evaluates `SHORT_CIRCUIT_RISK = K_STAR > K_WARN_ABOVE` (strict; fires `warnings.warn` + boxed print if true), binds `K_AUTO_EXTENDED`, sets `ANALYSIS_CFG["n_neighbors"] = K_STAR` and prints the resulting `fit_key`. §5.2: prints expected ~763 MiB matrix / ~1 GB joblib size and the 2-3 GB transient note, fits `Isomap(n_neighbors=K_STAR, n_components=N_COMPONENTS, eigen_solver="dense", n_jobs=-1, path_method="auto")` on `LS` inside `joblib_cache` (stem `isomap_{fit_key}`), asserts `dist_matrix_.shape==(10000,10000)`, `embedding_.shape==(10000,N_COMPONENTS)`, `nbrs_`/`kernel_pca_` present, independently re-verified component count == 1, and prints `fit_seconds`. Demonstrates ISO-05: second `joblib_cache` call is a CACHE HIT with array-equal `embedding_`; a one-field `ANALYSIS_CFG` mutation produces a different `config_key` (not fit). Markdown notes the deliberate git-SHA exclusion from the key (D-14) and that SPEC-01..07 remain Phase 2's and absent here.</what-built>
</task>

<task type="auto">
  <name>Task 2: Sections 5.3 and 5.4 — the Phase 1 to Phase 2 handoff artifact and the reserved section 6</name>
  <what-built>§5.3: builds `PHASE1_HANDOFF` with the 14 top-level keys specified in `<discretion_resolved>`, populated from already-bound state (not recomputed); `notes_for_phase2` carries >=5 entries covering `dist_matrix_` availability + the truncated-eigenvalues prohibition, the SPEC-04-elbow re-fit branch and why the cache key makes it cheap, the short-circuit-risk flag when set, the S^767 sphere consequence for Phase 3's CURV-06, and the §6-onward append convention. Written via `json_cache` (stem `phase1_handoff_{fit_key}`) and rendered as a printed table; a markdown cell states the short-circuit-risk flag in prose (surface 3 of 3). §5.4 (Reproducibility close-out): prints `SEED`, `subsample_key`, `fit_key`, `K_STAR`, `N_COMPONENTS`, `D_PROVISIONAL`, all three flags, `row_indices` sha256, total cache size; restates the §0.4 output-hygiene commit-with-outputs policy. Terminal `## §6.` placeholder (last cell in the notebook): names Phase 2's scope (Eigenspectrum Audit & Validity Gate, SPEC-01..07), states §0-§5 ownership and no-renumbering, implements no Phase 2 content.</what-built>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the Phase 1 deliverable end to end</name>
  <what-built>
    The complete Phase 1 deliverable: notebook §0-§5 plus §6 placeholder, the `pu_manifold/`
    package (two implemented, two stubbed), `requirements-notebooks.txt`, the pytest suite, and
    four cache artifact kinds — subsample npz (~60 MB), panel JSON, per-k sweep npz (~1 MB each),
    `isomap_{fit_key}.joblib` (~1 GB), `phase1_handoff_{fit_key}.json`. All ten Phase 1
    requirements (DATA-01..05, ISO-01..05) claimed complete.
  </what-built>
  <how-to-verify>
    Restart-and-Run-All twice (confirm CACHE HIT lines and no re-fit on the second pass); confirm
    the §4.3 cell-order assertion passes (thresholds cell before the stage-2 sweep cell); confirm
    §4.1/§4.3 tables cover all six/3-4 k values; confirm printed `K_STAR` matches plan 03's Task 4
    confirmation; open `phase1_handoff_<fit_key>.json` and confirm all 14 keys, exactly 3 flags,
    >=5 `notes_for_phase2` entries, and `k_star_selection.thresholds` matching §4.0; if
    `short_circuit_risk` is true confirm it appears in all three required surfaces; confirm §0-§5
    contain no Phase 2 content (no full spectrum, no elbow, no verdict) and the notebook's last
    cell is the §6 placeholder; confirm `git diff --quiet -- pyproject.toml src/effdim/`; confirm
    the notebook is committed with outputs intact and no cell embeds a bulk array.
  </how-to-verify>
  <resume-signal>Type "approved" to seal Phase 1, or describe which of the ten checks failed</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>

Phase-wide inventory lives in `01-01-PLAN.md`; this closes the set.

**Created (01-04):**

| Filename | Contents | Approx size |
|---|---|---|
| `isomap_{fit_key}.joblib` | fitted `Isomap` at `k*` only — `dist_matrix_`, `embedding_`, `nbrs_`, `kernel_pca_` | ~1 GB |
| `phase1_handoff_{fit_key}.json` | machine-readable Phase 1 to Phase 2 interface | <4 KB |
| `{stem}.meta.json` (per artifact) | full cfg dict, asserted on every load | <1 KB |

Notebook sections: `§5`, `§5.1`-`§5.4`, terminal `§6` placeholder. Symbols: `K_STAR_SELECT`,
`K_STAR`, `K_AUTO_EXTENDED`, `PHASE1_HANDOFF`. Mutated: `ANALYSIS_CFG["n_neighbors"] = K_STAR` in
§5.1 (the moment `fit_key` becomes fully determined); `SHORT_CIRCUIT_RISK` re-evaluated against
`K_STAR` rather than `K_SMALLEST_CONNECTED`.

`phase1_handoff_{fit_key}.json` top-level and nested keys are the same set listed in
`<discretion_resolved>` open item 6 above.

**Consumed from plans 01-03** (not new here): `pu_manifold.cache`/`subsample` full surface plus
`curvature`/`mknn` stubs; all notebook symbols and sections through `§4.3`.

**Reserved for Phase 2** (named, not created, here): sections `§6`+; `mds_eigenspectrum_{fit_key}.npz`; `gate_verdict_{fit_key}.json`.

</artifacts_this_phase_produces>

<verification>
Run from the repository root.

```bash
python -c "
import json
nb=json.load(open('notebooks/01_manifold_and_gate.ipynb'))
src=[''.join(c['source']) for c in nb['cells']]
t=next(i for i,s in enumerate(src) if 'PLATEAU_THRESH' in s)
f=next(i for i,s in enumerate(src) if 'STAGE2_SWEEP' in s)
k=next(i for i,s in enumerate(src) if 'K_STAR_SELECT' in s)
h=next(i for i,s in enumerate(src) if 'PHASE1_HANDOFF' in s)
assert t < f < k < h, f'ordering violated: {t} {f} {k} {h}'
assert '6.' in src[-1], 'last cell must be the section 6 placeholder'
print(f'ordering OK: thresholds {t} < sweep {f} < kstar {k} < handoff {h}; terminal cell is the section 6 placeholder')"
! grep -q 'eigvalsh' notebooks/01_manifold_and_gate.ipynb
python -m pytest notebooks/pu_manifold/tests/test_pu_manifold.py -q
ls -la notebooks/.cache/ | grep -E 'isomap_|phase1_handoff_'
python -c "
import json,glob
p=glob.glob('notebooks/.cache/phase1_handoff_*.json')[0]
h=json.load(open(p))
need=['phase','fit_key','subsample_key','config','artifacts','k_star','k_star_selection','n_components','d_provisional','d_provisional_source','row_indices_sha256','alignment_check','flags','notes_for_phase2']
missing=[k for k in need if k not in h]
assert not missing, 'handoff missing keys: '+str(missing)
assert set(h['flags'])=={'short_circuit_risk','k_auto_extended','n_components_no_headroom'}, h['flags']
assert len(h['notes_for_phase2'])>=5, 'need at least 5 notes for phase 2'
assert 'thresholds' in h['k_star_selection'], 'verdict must carry the thresholds it was judged against'
print('handoff artifact valid:', p, '| k_star =', h['k_star'], '| flags =', h['flags'])"
du -sh notebooks/.cache/
git diff --quiet -- pyproject.toml src/effdim/ && echo "core untouched"
```

Then perform the ten-step human verification in Task 3.
</verification>

<success_criteria>
- `k*` is frozen by the rule pre-registered in §4.0, cross-checked against the human-confirmed
  value, with no new threshold literal introduced in the selection cell.
- The single full Isomap fit at `k*` completes with `eigen_solver="dense"` and is cached as
  `isomap_{fit_key}.joblib` carrying `dist_matrix_`, `embedding_`, `nbrs_`, and `kernel_pca_`.
- A reload produces an array-equal `embedding_` and prints CACHE HIT; a one-field config change
  demonstrably produces a different key; the git SHA is excluded from the key.
- `phase1_handoff_{fit_key}.json` carries all fourteen top-level keys, exactly three flags, at
  least five `notes_for_phase2` entries, and the thresholds the verdict was judged against.
- The above-30 warning surfaces in all three required places.
- The notebook's last cell is the §6 placeholder naming Phase 2's scope, and §0-§5 contain no
  Phase 2 content.
- The cell-order chain `PLATEAU_THRESH` < `STAGE2_SWEEP` < `K_STAR_SELECT` < `PHASE1_HANDOFF` holds.
- `pyproject.toml` and `src/effdim/` are byte-identical to their pre-plan state.
- Phase 1's ten requirements — DATA-01 through DATA-05 and ISO-01 through ISO-05 — are all satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-data-loading-manifold-reconstruction/01-04-SUMMARY.md` when done.
Record: the frozen `K_STAR` and the run it came from; the `k*` fit's `fit_seconds` and the
on-disk size of `isomap_{fit_key}.joblib`; the final `fit_key` and `subsample_key`; the resolved
values of all three flags; the full `phase1_handoff_{fit_key}.json` contents inline; the total
`notebooks/.cache/` size; and the Task 3 verification outcome. If `flags.short_circuit_risk` is
true, say so prominently at the top of the summary.
</output>
</content>
