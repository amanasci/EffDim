---
phase: 02.7-manifold-template-inference-front-end-inserted
plan: 08
subsystem: topological-data-analysis
tags: [screening-rule, ratify-blind, ancestry-proof, decision-rule, compute-budget, checkpoint]

requires:
  - phase: 02.7-manifold-template-inference-front-end-inserted (plan 03)
    provides: "local_dimension.py's ESTIMATOR_NAMES/K_INVARIANT_ESTIMATORS/DUPLICATE_ESTIMATOR_PAIRS -- the two verified structural facts this document's consensus rule is written against"
  - phase: 02.7-manifold-template-inference-front-end-inserted (plan 06)
    provides: "template_decision.py's _REQUIRED_BOUNDS = (dispersion_bound, spread_bound) -- the exact bound-key set this document supplies"
  - phase: 02.7-manifold-template-inference-front-end-inserted (plan 07)
    provides: "02.7-BUDGET-CALIBRATION.md -- the only measured input this document ratifies against; a compute-feasibility record, no benchmark cell scored"
provides:
  - "02.7-SCREENING-RULE.md -- the ratified decision rule for SC-1: every constant template_decision.py/local_dimension.py/confidence_band.py/geodesic_graph.py/template_immersion.py leave default-free, fixed blind before any synthetic cloud is scored"
  - "check_rule_ancestry.sh -- the re-runnable git merge-base --is-ancestor check plan 02.7-12 repeats at phase close against every commit that scores a cloud"
  - "Task 1 checkpoint resolved: budget Option A (n_ph=300, B=10, 3 draws, ~17.05h), consensus rule (count_distinct=True, majority=5 of 7, k_values=[5,10,15,20,30], min_run=3, tolerance=0.5), bounds (dispersion_bound=3.0, spread_bound=3.0, flagged as judgment calls), noise levels {0.01,0.05,0.1}"
affects: [02.7-09, 02.7-10, 02.7-11, 02.7-12]

tech-stack:
  added: []
  patterns:
    - "Ratify-blind, prove-ancestry, one commit: the rule document is committed alone (git show --name-only --format= <SHA> | grep -c . == 1), no notebooks/ path touched, git merge-base --is-ancestor asserted here and re-checked at phase close -- exact 02.6-07/02.6-15 precedent, second instance in this project"
    - "Calibration-grounded vs judgment-call constants visibly distinguished in the same document: n_ph/B/alpha/grid-anchor values traceable to a 02.7-BUDGET-CALIBRATION.md measurement; dispersion_bound/spread_bound/BALL_D's D-grid floor/density levels explicitly flagged as judgment calls with no calibration behind them"
    - "A recorded compute-budget choice with named, costed alternatives (B/C/D rejected with what each gives up), never a silent reduction -- same discipline as 02.6-07's five ratified option groups"

key-files:
  created:
    - .planning/phases/02.7-manifold-template-inference-front-end-inserted/02.7-SCREENING-RULE.md
    - .planning/phases/02.7-manifold-template-inference-front-end-inserted/check_rule_ancestry.sh
  modified: []

key-decisions:
  - "Budget Option A ratified at the checkpoint: n_ph=300, B=10, 3 draws, 60 combos, ~17.05h (worst measured template S2). Option B (1 draw, ~5.68h) rejected because it eliminates D-08's cross-draw Betti-spread read-out entirely, not narrows it -- the instrument that would measure assumption A-04's unmeasured PH draw variance disappears. Option C (B=5, ~9-10h) and D (n_ph=250) rejected because both rest on unmeasured extrapolations rather than direct measurement."
  - "Ratified ball-timeout fallback, itself part of the blind ratification: if ball's wall clock during the actual grid run exceeds ~2x S2's per-cell cost, the runner reports ball's cells as incomplete/timeout rather than blocking the whole grid -- matches 02.7-BUDGET-CALIBRATION.md §4's own precedent (ball's euclidean H2 bootstrap leg timed out even at B=3)."
  - "count_distinct=True, majority=5 of 7 distinct estimators (after collapsing the verified mle/tle duplicate pair), k_values=[5,10,15,20,30] (k=15 present only as one interior point, explicitly not inherited as THE value per D-06), min_run=3, tolerance=0.5 -- one shared k-sweep-and-stability convention reused for both D-06's geodesic component-count stability and D-10's dimension plateau consensus."
  - "dispersion_bound=3.0 and spread_bound=3.0 ratified as explicitly-flagged judgment calls -- 02.7-BUDGET-CALIBRATION.md measures wall-clock/memory cost only and structurally cannot bear on typical spread/dispersion magnitude without scoring a benchmark cloud through the decision layer, which would violate ratify-blind. Stated in the document in a form visibly distinguishable from the calibration-grounded constants."
  - "Additional constants needed for the grid/immersion to be internally consistent, decided with the same reasoning discipline and flagged accordingly since the checkpoint's four ratified groups did not individually name them: BALL_D=18 (carried forward from the calibration's own proxy value, for cost-transfer rather than correctness reasons), D grid levels {18,100,768} (18 is the floor BALL_D sets, 768 is D-15's required level, 100 is an unmeasured bridging judgment call), n OFAT arm {200,300,400} (all three directly measured in the calibration, no extrapolation), density OFAT arm {0.5,1.0,2.0} (1.0 is the module's own uniform baseline; 0.5/2.0 are judgment calls), warp_params={strength:0.05,freq:1.0} and jacobian_rank(n_check=30,rank_tol=1e-6) (both carried forward from 02.7-05's own measured wide-margin values), D-12's neighbourhood_size=30/anchor_count=40 (informed by 02.7-03's own precedent test, not independently calibrated)."
  - "H0 significance-band calibration ratified blind: no special-cased band for H0 (identical B=10/alpha=0.05 mechanism as H1/H2); the residual risk (non-uniform density inflating beta_0 via spurious H0 significance, per the tracer's own 251/189 counts and 02.7-04's Gaussian-blob 11/79 finding) is named, not resolved, and the document states explicitly that abstain condition (b) already reads geodesic_graph.component_readout's direct n_components rather than the persistence-derived beta_0 -- but the Euclidean arm has no such direct instrument, so an inflated Euclidean beta_0 remains a live, named risk to abstain (a)/(c) rates."

patterns-established:
  - "Pattern: a document section explicitly comparing its own bound-key list against the consuming module's _REQUIRED_BOUNDS constant, verified by direct source read rather than accepted from a prior SUMMARY's prose -- second instance of the 02.6-07 precedent's disposition-table discipline, applied here to a smaller, code-verifiable claim."

requirements-completed: [D-01, D-02, D-03, D-04, D-06, D-08, D-10, D-11, D-15, D-16, SC-1]

coverage:
  - id: D1
    description: "02.7-SCREENING-RULE.md committed ALONE, its commit touching no other file, no notebooks/ path"
    requirement: "SC-1"
    verification:
      - kind: other
        ref: "git show --name-only --format= 7aa699d | grep -c . == 1; git show --name-only --format= 7aa699d | grep -c '^notebooks/' == 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "git merge-base --is-ancestor proves the rule commit precedes HEAD; check_rule_ancestry.sh committed separately (0468217) so plan 02.7-12 can re-run the identical check at phase close"
    requirement: "backstop"
    verification:
      - kind: other
        ref: "git merge-base --is-ancestor 7aa699d HEAD; exit 0. bash check_rule_ancestry.sh 7aa699d -> all checks passed"
        status: pass
    human_judgment: false
  - id: D3
    description: "The document supplies exactly template_decision._REQUIRED_BOUNDS = (dispersion_bound, spread_bound), no gaps, no extras"
    requirement: "D-03"
    verification:
      - kind: other
        ref: "grep -n _REQUIRED_BOUNDS notebooks/pu_manifold/template_decision.py -> ('dispersion_bound', 'spread_bound'); 02.7-SCREENING-RULE.md §5 supplies exactly these two keys"
        status: pass
    human_judgment: false
  - id: D4
    description: "The consensus rule is written against the two verified structural facts (mle==tle algebraically, not unqualified bit-identical; two_nn/mind_mli/gmst k-invariant) rather than a naive majority of eight"
    requirement: "D-10"
    verification:
      - kind: other
        ref: "grep -cE 'mle|tle' 02.7-SCREENING-RULE.md >= 1; document §4 states the 15/20-seed/1-2-ULP finding explicitly, never 'bit-identical' unqualified"
        status: pass
    human_judgment: false
  - id: D5
    description: "D-11's H_2 amendment to SC-3's literal degree range is stated in the document, not left silent; the literal 768 grid level is present"
    requirement: "D-11, D-15"
    verification:
      - kind: other
        ref: "02.7-SCREENING-RULE.md §6 quotes SC-3's literal text ('Degrees H_0 through H_dhat') and states the H_2 amendment explicitly; §8 states D in {18,100,768}"
        status: pass
    human_judgment: false
  - id: D6
    description: "The compute budget resolution (Option A) is stated and recorded, never applied silently; Options B/C/D's rejections and costs are named"
    requirement: "D-08"
    verification:
      - kind: other
        ref: "02.7-SCREENING-RULE.md §9 states Option A's projected ~17.05h and each rejected option's cost"
        status: pass
    human_judgment: false
  - id: D7
    description: "No regression to notebooks/pu_manifold/tests/; src/effdim/ and pyproject.toml untouched by this plan's commits"
    requirement: "SC-1 (inherited convention)"
    verification:
      - kind: integration
        ref: ".venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q (266 passed, 105.82s)"
        status: pass
      - kind: other
        ref: "git status --porcelain src/effdim pyproject.toml (empty); git log --oneline efd523a..HEAD -- src/effdim pyproject.toml (empty)"
        status: pass
    human_judgment: false

duration: ~1h10min (includes a blocking checkpoint round-trip)
completed: 2026-08-12
status: complete
---

# Phase 02.7 Plan 08: Screening Rule Ratification (SC-1's Structural Gate) Summary

**`02.7-SCREENING-RULE.md` (commit `7aa699d`) ratifies every constant SC-1's decision rule needs — compute budget Option A (`n_ph=300`, `B=10`, 3 draws, ~17.05h, with B/C/D's rejections and costs named), the consensus rule against the two verified estimator facts (`count_distinct=True`, `majority=5` of 7), the two `bounds` keys flagged as judgment calls, D-11's `H_2` amendment to SC-3, `mle`/`tle` stated at its correct algebraic-not-bit-identical strength, and both calibration gaps carried forward named — committed alone, ancestry proved, before any synthetic cloud exists in this repository under this phase.**

## Performance

- **Duration:** ~1h10min active work (includes reading all required context, a blocking `checkpoint:decision` round-trip to the user, drafting the ~390-line document, and post-commit verification)
- **Tasks:** 2 (Task 1: blocking checkpoint, resolved by the user; Task 2: write and commit the rule alone, prove ancestry)
- **Files created:** 2 (`02.7-SCREENING-RULE.md`, `check_rule_ancestry.sh`)

## Accomplishments

- **`02.7-SCREENING-RULE.md` committed alone** (`7aa699d`) — `git show --name-only --format= 7aa699d | grep -c .` returns `1`; `grep -c "^notebooks/"` on the same output returns `0`; `git merge-base --is-ancestor 7aa699d HEAD` exits `0`. No `notebooks/pu_manifold/template_decision.py` change, no benchmark measurement, and no other planning document exists in this commit.
- **`check_rule_ancestry.sh` committed separately** (`0468217`) — a re-runnable helper asserting the same shape (one file, no `notebooks/` path, ancestor of HEAD, optionally ancestor of any supplied measurement SHAs), so plan `02.7-12` repeats the identical check at phase close against every commit that scores a cloud, per the `02.6-07`/`02.6-15` precedent.
- **Compute budget: Option A ratified** at the Task 1 checkpoint — `n_ph=300`, `B=10`, 3 independent draws, the 60-combination grid (3×3 `(D,noise)` + 6 OFAT cells × 4 templates), projected **~17.05h** on the worst measured template (`S2`). Options B (1 draw, ~5.68h — rejected because it eliminates D-08's cross-draw Betti-spread instrument entirely, not merely narrows it), C (`B=5`, ~9–10h — rejected as an unmeasured extrapolation), and D (`n_ph=250` — rejected as trading against an unmeasured power margin) were each rejected with their specific cost stated, never silently. A **ratified `ball`-timeout fallback** (report `ball`'s cells as incomplete rather than blocking the grid if its wall clock exceeds ~2x `S2`'s) was recorded as part of the blind rule itself, not a reaction to the run.
- **Consensus rule written against the two verified structural facts, not a naive majority of eight.** `count_distinct=True` collapses the verified `(mle, tle)` duplicate pair to one vote (7 distinct estimators) and exempts the three verified `k`-invariant estimators (`two_nn`, `mind_mli`, `gmst`) from the `min_run` run-length requirement; `majority=5` of 7 is a comfortable (>70%) bar, not a bare knife-edge. `mle`/`tle`'s equality is stated at its measured strength — algebraically identical, exactly equal at 15 of 20 seeds, differing by 1–2 float32 ULPs at the other 5 — never "bit-identical" unqualified.
- **The two `bounds` keys (`dispersion_bound=3.0`, `spread_bound=3.0`) supplied exactly**, matching `template_decision._REQUIRED_BOUNDS` with no gaps and no extras (verified by direct source read, not accepted from a prior SUMMARY's prose alone), and **explicitly flagged in the document as judgment calls with no calibration behind them** — visibly distinguished from `n_ph`/`B`/`alpha` and the grid-anchor values, which are traceable to specific `02.7-BUDGET-CALIBRATION.md` measurements.
- **D-11's `H_2` ceiling stated as an explicit amendment to SC-3's literal text** ("degrees `H_0` through `H_dhat`"), with assumption A-01's bound stated at its correct strength: "off-library as detectable through `H_0..H_2`," never "off-library, full stop."
- **Both calibration gaps carried forward named, not resolved**: `ball`'s whole-cell cost remains unmeasured (§10, mitigated only by the ratified timeout fallback, not fixed); the PU-regime-shaped `H_2` power check's two timeouts mean `H_2` power is established for `S^2`/`T^2` (both `d=2`) but **not** at higher effective dimensionality — and `BALL_D=18` (carried forward for cost-transfer reasons) sits squarely inside that unresolved regime, stated explicitly as an open question the `ball` results should be read against.
- **The H0 significance-band calibration question (flagged by plan `02.7-01`'s tracer, large `beta_0` counts of 251/189) ratified blind** — no special-cased band for H0, and the residual risk (non-uniform density can inflate a Euclidean-arm `beta_0` via spurious bootstrap significance, unrelated to genuine multi-component structure) is named explicitly, together with the structural mitigation already present (abstain (b) reads `component_readout`'s direct `n_components`, not the Betti-derived `beta_0`) and its own gap (the Euclidean arm has no analogous direct instrument).
- **Assumption A-05 recorded as not fired**: `02.7-BUDGET-CALIBRATION.md` §5a's clean template-native power result is the calibration's finding, stated in the document — `H_2` is resolvable on `S^2`/`T^2` at the ratified `n_ph=300, B=10, D=768`.
- **Full `notebooks/pu_manifold/tests/` suite: 266 passed, no regression** (105.82s). `src/effdim/`/`pyproject.toml` untouched by this plan's commits (`git status --porcelain` empty on both; no commit since `efd523a` touches either path).

## Task Commits

1. **Task 1 (checkpoint:decision, blocking):** resolved by the user — budget Option A, consensus rule (`count_distinct=True`, `majority=5`, `k_values=[5,10,15,20,30]`, `min_run=3`, `tolerance=0.5`), bounds (`dispersion_bound=3.0`, `spread_bound=3.0`), noise levels `{0.01,0.05,0.1}` — no commit of its own (a decision, not code).
2. **Task 2: write and commit `02.7-SCREENING-RULE.md` alone** — `7aa699d` (docs)
3. **Task 2 continued: `check_rule_ancestry.sh`, committed separately** — `0468217` (test)

**Plan metadata:** (this commit, docs: complete plan)

## Files Created

- `.planning/phases/02.7-manifold-template-inference-front-end-inserted/02.7-SCREENING-RULE.md` — 13 sections: the mechanism (D-01/D-04), significance (D-02), the abstain disjunction (D-03), the dimension protocol (D-09/D-10/D-12), the bounds (D-03 b/d), the homology ceiling and its amendment (D-11), the metric arms (D-05..D-08), the benchmark grid (D-13/D-15), the compute budget stated, the two calibration gaps named, the H0 significance-band calibration ratified blind, the Swiss roll contract (D-16), and the ancestry proof.
- `.planning/phases/02.7-manifold-template-inference-front-end-inserted/check_rule_ancestry.sh` — re-runnable, five assertions (file count, filename, no `notebooks/` path, ancestor of HEAD, ancestor of any supplied measurement SHAs).

## Bound-Key Comparison (per plan's `<output>` instruction)

| Source | Keys |
|---|---|
| `template_decision._REQUIRED_BOUNDS` (source, `notebooks/pu_manifold/template_decision.py:194`) | `("dispersion_bound", "spread_bound")` |
| `02.7-06-SUMMARY.md`'s stated required set | `{"dispersion_bound", "spread_bound"}` |
| `02.7-SCREENING-RULE.md` §5's supplied set | `dispersion_bound`, `spread_bound` |

**Exact match, no gaps, no extras**, across all three sources — verified by direct source read of `template_decision.py`, not accepted from prior-plan prose alone.

## Ratified Constant Set (full, per plan's `<output>` instruction)

| Constant | Value | Basis |
|---|---|---|
| `n_ph` | 300 | Calibration-grounded (`02.7-BUDGET-CALIBRATION.md`, Option A) |
| `B` | 10 | Calibration-grounded; stated reduction from Fasy's `B=30` default |
| `alpha` | 0.05 | Standard 95%-band convention |
| draws | 3 | RESEARCH.md's recommendation, preserved under Option A |
| `k_values` | `[5, 10, 15, 20, 30]` | Judgment call (k=15 present only as one interior point, not inherited as THE value) |
| `min_run` | 3 | Judgment call, shared by D-06 and D-10 |
| `tolerance` | 0.5 | Judgment call |
| `count_distinct` | `True` | Structural (collapses the verified `mle`/`tle` pair, exempts the verified `k`-invariant three) |
| `majority` | 5 of 7 distinct | Judgment call (>70% bar) |
| `dispersion_bound` | 3.0 | **Judgment call, explicitly flagged — no calibration behind it** |
| `spread_bound` | 3.0 | **Judgment call, explicitly flagged — no calibration behind it** |
| `neighbourhood_size` / anchor count (D-12) | 30 / 40 | Judgment call, informed by `02.7-03`'s own precedent test |
| `D` grid levels | `{18, 100, 768}` | 18 = `BALL_D` floor; 768 = D-15 required; 100 = judgment call |
| noise levels | `{0.01, 0.05, 0.1}` | 0.05 = calibration-grounded (anchor cell); 0.01/0.1 = OFAT-adjacent judgment calls |
| `n` OFAT arm | `{200, 300, 400}` | Calibration-grounded — all three directly measured |
| density OFAT arm | `{0.5, 1.0, 2.0}` | 1.0 = module's own uniform baseline; 0.5/2.0 = judgment calls |
| `warp_params` | `{strength: 0.05, freq: 1.0}` | Calibration-grounded (`02.7-05`'s measured pass at D=768, all four templates) |
| `jacobian_rank(n_check, rank_tol)` | `(30, 1e-6)` | Carried forward from `02.7-05`'s measured wide-margin value |
| `BALL_D` | 18 | Carried forward from the calibration's own proxy (cost-transfer reasons, not correctness) |
| Compute budget | Option A, ~17.05h, `ball`-timeout fallback ratified | Calibration-grounded; recorded choice with named alternatives |

## Decisions Made

See `key-decisions` in frontmatter for the full set. Summarized: budget Option A ratified over B/C/D with each rejection's specific cost stated; the `ball`-timeout fallback ratified as part of the blind rule; the consensus rule's `count_distinct`/`majority`/`k_values`/`min_run`/`tolerance` set fixed and reused across D-06 and D-10; `dispersion_bound`/`spread_bound` ratified as explicitly-flagged judgment calls; several additional constants (`BALL_D`, `D` grid levels, density OFAT levels, `warp_params`, `jacobian_rank` args, D-12's local-probe sizing) decided with the same reasoning discipline since the checkpoint's four named groups did not individually enumerate them but the document could not be internally consistent without them; the H0 significance-band question ratified blind (no special-cased band, residual risk named rather than resolved).

## Deviations from Plan

None that trigger Rules 1–4 — no bug, no missing critical functionality, no blocking issue, and no architectural change was encountered. The additional constants listed above (`BALL_D`, `D` grid low/mid levels, density OFAT levels, `warp_params`, `jacobian_rank` args, D-12's local-probe sizing) are not deviations from the plan's instructions; they are constants the plan's own Task 1 `<decision>` text implicitly required ("fix, as a set... D-15's grid levels for `(D, noise)` and the OFAT arms for `n` and density") but did not individually enumerate in the checkpoint's four ratified groups. Each is stated in the document with its own justification and explicitly flagged as calibration-grounded or a judgment call, per the plan's own `must_haves` truth: "every constant informed by the calibration is marked as a compute-feasibility number rather than a decision threshold."

## Issues Encountered

None. The blocking checkpoint round-trip resolved cleanly with all four decision groups answered explicitly by the user, with no ambiguity requiring a second round-trip.

## Known Stubs

None. This plan writes one markdown document and one shell script; no code path in either is a stub standing in for unfinished logic.

## Threat Flags

None beyond the three already registered in this plan's own `<threat_model>` (T-02.7-20, T-02.7-21, T-02.7-22), all mitigated as specified: T-02.7-20 (unprovable ordering) by the single-file commit shape and the ancestry assertion; T-02.7-21 (post-hoc constant) by the exact `_REQUIRED_BOUNDS` match; T-02.7-22 (silent scope reduction) by the compute-budget section's explicit Option A/B/C/D comparison with costs stated.

## Next Phase Readiness

- Plan `02.7-09` (the CLAUDE.md-mandated Swiss roll notebook with in-library `S^1`/`T^2` positive controls) can now read `02.7-SCREENING-RULE.md` §12 for D-16's contract and every other section for the constants its own run needs.
- Plan `02.7-10` (the benchmark grid and three-way tally) has every constant this document ratifies, including the ratified `ball`-timeout fallback and the OFAT/factorial grid levels.
- Plan `02.7-12` has `check_rule_ancestry.sh` ready to re-run against every commit that scores a cloud, plus this document's own recorded gaps (`ball`'s unmeasured cost, the PU-regime `H_2` power timeout, the H0 significance-band residual risk) to check the phase's own findings against.
- No blockers.

## Self-Check: PASSED

Both created files found on disk (`02.7-SCREENING-RULE.md`, `check_rule_ancestry.sh`); both task commit hashes (`7aa699d`, `0468217`) found in `git log --oneline --all`; `git merge-base --is-ancestor 7aa699d HEAD` exits `0`; `bash check_rule_ancestry.sh 7aa699d` reports all checks passed.

Re-verified independently post-write: `[ -f 02.7-SCREENING-RULE.md ]` → FOUND; `[ -f check_rule_ancestry.sh ]` → FOUND; `git log --oneline --all | grep 7aa699d` → FOUND; `git log --oneline --all | grep 0468217` → FOUND.

---
*Phase: 02.7-manifold-template-inference-front-end-inserted*
*Completed: 2026-08-12*
