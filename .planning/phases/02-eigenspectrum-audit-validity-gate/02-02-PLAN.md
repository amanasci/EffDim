---
phase: 02-eigenspectrum-audit-validity-gate
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - notebooks/01_manifold_and_gate.ipynb
autonomous: false
requirements: [SPEC-04, SPEC-05]

must_haves:
  truths:
    - "Both residual curves on one axis: Tenenbaum 1-R² and eigenvalue 1-cumsum(pos)/sum(pos) (SPEC-04, D-06)"
    - "Elbow read from the Tenenbaum curve only; eigenvalue curve is a cross-check (SPEC-04, D-06)"
    - "Max divergence between curves reported as a non-Euclideanity signal (SPEC-04, D-06)"
    - "Elbow located by deterministic max-curvature (kneedle) over the §6.0 pre-registered range, ties to lower d (SPEC-04, D-05)"
    - "Elbow stability demonstrated on a second disjoint pair draw; disagreement halts (SPEC-04, D-06)"
    - "Elbow <= n_components: d frozen at elbow, coordinates = columns 0..d-1 of the cached embedding via classical-MDS nesting (SPEC-05, D-07)"
    - "Elbow > n_components: documented halt with costs and exact remediation; never auto-re-fit, never cap at 18 (SPEC-05, D-08)"
    - "d frozen before any decoder exists; d_frozen <= n_components by construction (SPEC-05)"
  artifacts:
    - path: "notebooks/01_manifold_and_gate.ipynb"
      provides: "§6.5-§6.6: residual curves, deterministic elbow + stability check, figure, frozen d or documented halt"
      contains: "D_FROZEN"
  key_links:
    - {from: "§6.5", to: "notebooks/.cache/mds_eigenspectrum_{fit_key}.npz", via: "geodesic pair samples + top-k_eff MDS coordinates sampled in §6.1", pattern: "GEO_PAIRS_R2"}
    - {from: "§6.6", to: "notebooks/.cache/mds_residuals_{fit_key}.npz", via: "npz_cache under the fit_key + sweep-ceiling cfg contract", pattern: "mds_residuals"}
    - {from: "§6.6 D_FROZEN", to: "Phase 3 decoder width / Phase 4 partition", via: "d_frozen in gate_verdict_{fit_key}.json (plan 02-03); consumers never read n_components", pattern: "D_FROZEN"}
  prohibitions:
    - {requirement_id: "SPEC-04", category: "values", status: "resolved", verification: "judgment", statement: "MUST NOT choose, swap, or tune the elbow criterion, d-range, or pair-sample size after seeing the curves."}
    - {requirement_id: "SPEC-05", category: "values", status: "resolved", verification: "judgment", statement: "MUST NOT let re-fit cost influence the elbow or frozen d; capping d at 18 to avoid a fresh fit is the named failure. Halt + human decision is a legitimate outcome."}
---

<objective>
Turn the spectrum into SPEC-04's residual-variance curve, locate its elbow deterministically, and
freeze the embedding dimension d that Phases 3-4 are defined on. One-way door: d is the decoder input
width, the curvature field's domain, and the region-partition basis. Pre-registration (§6.0) and a
human gate before the freeze are the defences against choosing d by convenience.
</objective>

<key_interfaces>
From 02-01 (§6.0-§6.4): `D_SWEEP_MAX=40`, `R2_PAIR_COUNT=200_000`, `R2_PAIR_SEED=SEED+2`,
`R2_PAIR_SEED_CHECK=SEED+3`, `ELBOW_TIE_BREAK="lower"`; `GEO_PAIRS_R2`/`GEO_PAIRS_R2_CHECK` (drawn
while the geodesic matrix was mapped — unavailable afterwards), `EMBEDDING_ISOMAP` (10,000x18),
`EIGVALS_ALL`, `MDS_COORDS`, `N_POSITIVE`, `K_EFF=min(D_SWEEP_MAX, N_POSITIVE)`, `R_STAT`, `M_STAT`,
`GATE_VERDICT`; gate thresholds r<0.10/0.25, m<0.05/0.15. From Phase 1: `N_COMPONENTS=18`, `fit_key=43cf438bc944c509`;
`n_components_no_headroom=true` makes the D-08 branch live, not hypothetical.

Classical-MDS nesting (D-07): eigenvectors of the double-centred matrix are orthogonal and ordered, so
the d-dimensional solution is exactly the first d columns of the wider one; §6.2's rtol=1e-8 sklearn
agreement licenses the claim for this artifact.

Discretion resolved: pair scheme = 200,000 pairs at SEED+2, stability shown by full recompute on the
disjoint SEED+3 draw (elbow argmax is a sharper demand than any single correlation — Phase 1 §4.0 used
100,000 for a Spearman). Vectorised evaluation allocates a 200,000 x 40 float64 (~64 MiB), printed
before allocation, released between draws. Figure = one axis, both curves, four annotations (kneedle
point, same d on both curves, max-divergence band, vertical line at n_components=18).
</key_interfaces>

<tasks>

<task type="auto">
  <name>Task 1: §6.5 — both residual curves, deterministic elbow, stability check</name>
  <produced>
    `D_GRID = arange(1, K_EFF+1)`. `RESIDUAL_EIGEN = 1 - cumsum(pos desc)[:K_EFF]/sum(all pos)`,
    asserted non-increasing in [0,1]. `RESIDUAL_TENENBAUM = 1 - Pearson²(geodesic pairs, embedded
    pairs at each d)`, vectorised via cumsum over coordinates. Both curves + second-draw curve cached
    as `mds_residuals_{fit_key}.npz` (cfg: fit_key, d_sweep_max, k_eff, r2_pair_count, both seeds).
    `_kneedle(x,y)`: normalize axes to [0,1], max perpendicular distance from endpoint chord, ties to
    lower index, raises on <3 points or zero y-range; contains no gate-threshold literal. `ELBOW_D` and
    `ELBOW_D_CHECK` asserted equal (halt names both values, both seeds, remediation = raise
    R2_PAIR_COUNT and re-register). Eigenvalue curve's elbow printed as labelled cross-check.
    `CURVE_DIVERGENCE_MAX` printed beside R_STAT/M_STAT — corroborates the gate, does not enter the
    verdict (D-01 fixed the verdict as r/m only). One figure, both curves, elbow + divergence + d=18
    branch boundary marked.
  </produced>
  <done>SPEC-04: elbow from a pre-registered deterministic criterion, stability demonstrated, divergence read.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm the one-way freeze of d</name>
  <produced>
    Human gate on the branch. Options: freeze-at-elbow (only when ELBOW_D <= 18; nesting slice is
    exact, no re-fit), halt-for-refit (required when ELBOW_D > 18; fresh fit ~minutes + ~1.55 GiB
    under a new fit_key), halt-elbow-unstable (if the stability assert fired). Reviewer sees ELBOW_D,
    cross-check elbow, CURVE_DIVERGENCE_MAX, R_STAT, M_STAT, GATE_VERDICT, DROPOFF_INDEX, N_POSITIVE,
    K_EFF. Outcome recorded with the confirmed integer d.
  </produced>
  <done>Branch selected by a human before anything freezes.</done>
</task>

<task type="auto">
  <name>Task 3: §6.6 — freeze d, or halt with the re-fit decision documented</name>
  <reversibility rating="one-way">Changing d after Phase 3 invalidates decoder, curvature field, and every MKNN number.</reversibility>
  <produced>
    `D_FREEZE = True` ordering anchor (prereg < kneedle < freeze asserted). Single
    `if ELBOW_D <= N_COMPONENTS` branch. Freeze: `D_FROZEN = int(ELBOW_D)`,
    `ISOMAP_COORDS_D = EMBEDDING_ISOMAP[:, :D_FROZEN]`, shape/dtype asserted, cross-checked against
    Task 2's confirmed integer; nesting verified numerically — pairwise distances from the slice vs
    from `MDS_COORDS[:, :D_FROZEN]` over the pair sample, np.allclose, worst relative difference
    printed (rotation-invariant, immune to sign flips). Halt: `assert False` naming observed ELBOW_D,
    required n_components, ~1.55 GiB + minutes cost, the exact constant `ANALYSIS_CFG["n_components"]`
    (§3.2), automatic fit_key change, regeneration of every §6 artifact, and Phase 1's
    notes_for_phase2 quote. No auto-re-fit, no pre-emptive widening, no capping.
  </produced>
  <done>SPEC-05: d frozen from the SPEC-04 elbow with the slice's exactness demonstrated, or a fully documented halt.</done>
</task>

</tasks>

<artifacts>
Created: `mds_residuals_{fit_key}.npz` (+ meta.json) — `d_grid`, `residual_tenenbaum`,
`residual_tenenbaum_check`, `residual_eigen`, `residual_seconds`. Symbols: `D_GRID`,
`RESIDUAL_TENENBAUM`, `RESIDUAL_TENENBAUM_CHECK`, `RESIDUAL_EIGEN`, `_kneedle`, `ELBOW_D`,
`ELBOW_D_CHECK`, `ELBOW_D_EIGEN`, `CURVE_DIVERGENCE_MAX`, `D_FREEZE`, `D_FROZEN`, `ISOMAP_COORDS_D`.
Constant of record: `D_FROZEN` (written to gate_verdict json by 02-03).

Edge-probe accounting (phase-wide in 02-01-PLAN): 8 probe edges = 2 authored (SPEC-03 boundary +
precision) + 6 flagged (SPEC-01/02/04/05/06/07). This plan's two — SPEC-04, SPEC-05 — carried as
explicit assumptions: the chord-distance knee is stated/deterministic/fixed-in-advance, not uniquely
correct; R² estimated on a 200,000-pair subsample with the disjoint-draw agreement as stability
evidence; "recorded" = machine-readable artifact keyed to fit_key so silent revision is detectable.
</artifacts>

<threats>
ASVS level 1, blocking on high; phase register in 02-01-PLAN. T-02-07 (post-hoc criterion
substitution: mitigated by §6.0-only declarations, cell-order assert, no-threshold-literal check on
_kneedle, manifest raises on edit). T-02-08 (cost-avoidance capping: single if/else, halt content
asserted, D_FROZEN cross-checked against human-confirmed integer). T-02-09 (draw-dependent elbow: two
seeded disjoint draws, elbows asserted equal). T-02-10 (slice presented as exact while approximate:
nesting verified numerically, rotation-invariant). T-02-11 (oversized intermediate: ~64 MiB printed
before allocation, pre-registered size). T-02-12 (committed outputs: accept — public data, no PII,
cache gitignored).
</threats>

<verification>
```bash
# ordering: prereg < eigensolve < classify < kneedle < freeze (cell-index scan of notebook JSON)
# independent knee recompute from mds_residuals_*.npz must agree with the notebook's ELBOW_D
python -m pytest notebooks/pu_manifold/tests/test_pu_manifold.py -q
git diff --quiet -- pyproject.toml src/effdim/ notebooks/pu_manifold/
```
</verification>

<output>
`02-02-SUMMARY.md` records at top: D_FROZEN + branch taken; then ELBOW_D vs ELBOW_D_CHECK, eigen
cross-check elbow, CURVE_DIVERGENCE_MAX and its d vs R_STAT/M_STAT, the d-grid swept, nesting
worst-relative-difference, Task 2 outcome, npz size. A fired halt is a complete reportable outcome.
</output>
