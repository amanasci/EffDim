---
phase: 02-eigenspectrum-audit-validity-gate
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - notebooks/01_manifold_and_gate.ipynb
autonomous: false
requirements: [SPEC-06, SPEC-07]

must_haves:
  truths:
    - "Verdict written as machine-readable gate_verdict_{fit_key}.json via json_cache, keyed by fit_key so a re-fit cannot be read against a stale PASS (SPEC-06, D-13)"
    - "Artifact self-contained: verdict, r, m, four thresholds verbatim, elbow + criterion, d_frozen, sweep ceiling, fit_key, k_star, three Phase 1 flags, timestamp, versions (SPEC-06, D-16)"
    - "Verdict is spectral only; flags are provenance and never move it (SPEC-06, D-04)"
    - "On FAIL: halt with three remediation options, list also written into the artifact (SPEC-07, D-16)"
    - "FAIL path exercised against synthetic PASS/MARGINAL/FAIL records on every run so the branch cannot rot (SPEC-07)"
    - "Copyable self-contained enforcement block for downstream first cells; no new pu_manifold module (SPEC-07, D-14)"
    - "MARGINAL proceeds but re-prints verdict/r/m so downstream results always carry the caveat (SPEC-07, D-15)"
    - "json_cache cfg carries the four thresholds + sweep ceiling; editing a pre-registered constant raises a manifest mismatch instead of silently re-verdicting (SPEC-06, D-02, D-13)"
  artifacts:
    - path: "notebooks/01_manifold_and_gate.ipynb"
      provides: "§6.7-§6.9: verdict artifact, copyable enforcement block + three-way self-test, close-out"
      contains: "GATE_VERDICT_RECORD"
  key_links:
    - {from: "§6.7", to: "notebooks/.cache/gate_verdict_{fit_key}.json", via: "json_cache with thresholds + ceiling in cfg", pattern: "gate_verdict"}
    - {from: "gate_verdict json", to: "first cell of every downstream notebook", via: "§6.8 copyable block: open, assert verdict, bind d_frozen", pattern: "_require_gate"}
    - {from: "§6.6 D_FROZEN", to: "artifact d_frozen field", via: "dimension of record; consumers read it here, never n_components", pattern: "d_frozen"}
  prohibitions:
    - {requirement_id: "SPEC-07", category: "values", status: "resolved", verification: "judgment", statement: "MUST NOT treat FAIL as an error to work around, retry, downgrade, or suppress. A documented FAIL with remediation enumerated is a complete, legitimate milestone outcome."}
    - {requirement_id: "SPEC-06", category: "transparency", status: "resolved", verification: "judgment", statement: "MUST NOT emit a verdict actionable without its thresholds, statistics, and audited-fit identity. Self-containment is the only defence against downstream threshold drift, since D-14 keeps the rule in prose."}
---

<objective>
Write the verdict that decides whether the milestone continues, machine-checkable and reconstructable
without the notebook. D-14 keeps the enforcement in notebook prose (no importable module) — D-16's
self-contained schema is the mitigation, and the never-fires-on-real-data FAIL branch is kept alive by
a three-way synthetic self-test inside the notebook.
</objective>

<key_interfaces>
Everything recorded is already bound (§6.0-§6.6): thresholds (r<0.10/0.25, m<0.05/0.15), `R_STAT`, `M_STAT`, `GATE_VERDICT`,
`_gate_classify`, spectrum stats, `ELBOW_D`/`ELBOW_D_CHECK`/`ELBOW_D_EIGEN`, `CURVE_DIVERGENCE_MAX`,
`D_FROZEN`. Phase 1: `fit_key=43cf438bc944c509`, `K_STAR=15`, `N_COMPONENTS=18`, `D_PROVISIONAL=18`,
flags (`short_circuit_risk=false`, `k_auto_extended=false`, `n_components_no_headroom=true`).
Write pattern = §5.3 cell 84 (flat dict from bound state, json_cache, formatted dump, required-keys
asserts). `json_cache`'s `_manifest_matches` raises on cfg mismatch — thresholds in the cfg make
post-hoc §6.0 edits fail loudly.

Discretion resolved: the FAIL-path test lives in the notebook (three synthetic records through
`_require_gate`), not pytest — a test the notebook never mentions would restore the invisibility D-14
declined. `remediation` written on every verdict (fixed schema, no verdict-dependent keys).

Artifact top-level keys (21): `phase`, `verdict`, `r`, `m`, `thresholds` (r_max_pass, m_max_pass,
r_max_marginal, m_max_marginal), `verdict_rule`, `spectrum` (n_positive, n_negative, lambda_max_pos,
lambda_min_neg, noise_floor, dropoff_index, dropoff_ratio), `elbow`, `elbow_criterion`, `elbow_check`
(elbow_check_draw, elbow_eigen_curve, curve_divergence_max), `d_frozen`, `d_sweep_max`, `fit_key`,
`k_star`, `n_components`, `d_provisional`, `flags`, `remediation` (3 entries), `artifacts`
(spectrum_npz, residuals_npz, isomap_joblib), `timestamp`, `versions` (numpy, scipy, scikit_learn,
python).
</key_interfaces>

<tasks>

<task type="auto">
  <name>Task 1: §6.7 — the self-contained gate_verdict artifact and the FAIL halt</name>
  <produced>
    `GATE_VERDICT_RECORD` built from bound state only (numpy scalars cast to Python types), written via
    `json_cache` under `gate_verdict_{fit_key}` with cfg = fit_key + four thresholds + d_sweep_max.
    `GATE_REMEDIATION` = three options with costs: re-fit at different n_neighbors (new fit_key,
    regenerate all artifacts); resample with new seed (new subsample_key + fit_key); accept the
    documented FAIL as the milestone outcome. `verdict_rule` and `elbow_criterion` as actionable prose.
    Required-keys asserts including `verdict == _gate_classify(record["r"], record["m"])`. Artifact
    written BEFORE the halt fires. FAIL branch: `assert False` naming r, m, violated thresholds, all
    three remediations, and the complete-outcome framing. MARGINAL: boxed print + `warnings.warn`.
    Markdown records D-13's accepted cost (cache gitignored; unkeyed committed copy declined).
  </produced>
  <done>SPEC-06: verdict artifact bound to its fit, self-contained, internally consistent.</done>
</task>

<task type="auto">
  <name>Task 2: §6.8-§6.9 — copyable enforcement block, three-way self-test, close-out</name>
  <produced>
    `_require_gate(record, *, context="")`: validates keys, PASS prints clear-to-proceed, MARGINAL
    boxed print + warnings.warn + returns, FAIL raises with remediation read FROM the record (no
    re-typed threshold literals — verified by automated check); returns `d_frozen`. Three synthetic
    records exercise all branches every run; FAIL's message asserted to contain all three remediation
    strings; then `_require_gate(GATE_VERDICT_RECORD)` on the real record. Copyable fenced block in
    markdown: imports, `cache_path` composition from the downstream notebook's own fit_key, three-way
    branch, binds `D_FROZEN` from `d_frozen`. §6.9 close-out prints all values of record + artifact
    sizes + cache total; hygiene checks (no >20,000-char outputs; §0-§6 sequence intact; all ten §6.N
    subsections present).
  </produced>
  <done>SPEC-07: FAIL halts with remediation, branch provably exercised every run, downstream check reads its rule from the artifact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Seal Phase 2 — verify the full §6 deliverable and the terminal verdict</name>
  <produced>
    Ten-step human verification: clean Restart-and-Run-All; pre-registration ordering; spectrum length
    10,000/float64/sklearn cross-check; equivalence guard; gate statistics vs thresholds + nine
    boundary cases; figures; elbow agreement + freeze branch + nesting check; artifact read in
    isolation (D-16 self-containment judged directly); enforcement self-test + copyable block run in a
    scratch session; boundary cleanliness (`git diff --quiet -- pyproject.toml src/effdim/
    notebooks/pu_manifold/`). Approving a FAIL seals the phase with the FAIL as its complete outcome.
  </produced>
  <done>Phase 2 sealed by a human against the real artifacts.</done>
</task>

</tasks>

<artifacts>
Created: `gate_verdict_{fit_key}.json` (<6 KB) + meta.json (cfg: fit_key, four thresholds,
d_sweep_max). Sections §6.7-§6.9 complete §6 at ten subsections. Symbols: `GATE_VERDICT_RECORD`,
`GATE_REMEDIATION`, `_require_gate`, `GATE_ENFORCE_SELFTEST`. Phase 2→3 interface in one line:
Phase 3's first cell pastes §6.8's block — reads the json, halts on FAIL, re-prints MARGINAL caveat,
binds `D_FROZEN`. Explicitly NOT created: `pu_manifold/gate.py` (D-14), new tests, `__init__` changes,
requirements changes, the Phase 3 notebook.

Edge-probe accounting (phase-wide in 02-01-PLAN): 8 edges = 2 authored (SPEC-03) + 6 flagged. This
plan's two — SPEC-06, SPEC-07 — carried as assumptions: "machine-readable" = fixed-key JSON parseable
without the notebook (Phase 3 doesn't exist yet, so the consuming end is unverifiable here);
fit_key-named file + threshold manifest is the staleness defence, and the copyable block computes
fit_key from the consumer's own config; "halts" = `assert False` per Phase 1's idiom; the
complete-outcome framing rests on judgment and is flagged as such.
</artifacts>

<threats>
ASVS level 1, blocking on high; phase register in 02-01-PLAN. T-02-13 (stale PASS vs re-fit:
fit_key-keyed filename, block computes fit_key from consumer config, inline fit_key/k_star/
n_components for independent check). T-02-14 (verdict drifts from its statistics: write-cell assert +
external re-derivation + thresholds in manifest). T-02-15 (FAIL worked around: assert False with
remediation, artifact written before halt, human gate pre-states that approving a FAIL seals it).
T-02-16 (FAIL branch rot: three-way synthetic self-test every run). T-02-17 (MARGINAL caveat dropped:
three channels — boxed print + warnings.warn, artifact fields, block re-print per D-15). T-02-18
(incomplete artifact: 21-key contract asserted twice; verdict_rule/elbow_criterion prose in-artifact;
human gate reads artifact in isolation). T-02-19 (committed outputs: accept — public data, cache
gitignored, §0.4 hygiene enforced).
</threats>

<verification>
```bash
# cell order: GATE_PREREG < SPECTRUM_COMPUTE < _gate_classify < DROPOFF_INDEX < _kneedle < D_FREEZE
#             < GATE_VERDICT_RECORD < def _require_gate
# external re-derivation: verdict from artifact's own r/m/thresholds must equal stored verdict;
#             flags/k_star/n_components must equal the Phase 1 handoff; 1 <= d_frozen <= n_components
python -m pytest notebooks/pu_manifold/tests/test_pu_manifold.py -q
git diff --quiet -- pyproject.toml src/effdim/ notebooks/pu_manifold/
```
Then the ten-step human verification in Task 3.
</verification>

<output>
`02-03-SUMMARY.md` records at the very top: **the verdict**, r, m, d_frozen — the phase's entire
product. Then: the full gate_verdict json inline (cache is gitignored; the summary is where it
survives in-repo), artifact sizes + cache total, self-test outcomes, Task 3 outcome, and what Phase 3
inherits. On FAIL: state the three remediations verbatim and that Phase 3 is not to be planned until a
remediation is chosen — the ROADMAP's hard gate operating as designed.
</output>
