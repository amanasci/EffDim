---
phase: 03-decoder-curvature-field
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/diagnostics/swiss_roll_curvature_sweep_run.py
autonomous: false
requirements: [CURV-01, CURV-03, DEC-05]

must_haves:
  truths:
    - "The 0.65 absolute floor and the swept n_charts set are written into this plan and ratified at a blocking checkpoint BEFORE any rho_chart value exists (D-02, D-15)"
    - "The gate override (no PASS exists anywhere in this milestone) and the D-05 n_charts scope ruling are stated in this phase's own artifacts, not inherited silently"
    - "One end-to-end Swiss roll cell runs fit -> chart decoder -> torch.func curvature -> Spearman against analytic H and reproduces rho_chart = -0.0604 at n_charts=8, seed=0"
    - "The runner records charts ACTUALLY USED (chart_curvature_field's n_charts_used), not only the configured n_charts, because D-05's monotone mechanism is stated in charts used"
    - "The raw-point centroid baseline reproduces 0.6712 at k=30, d=2 on the FIXTURE_SEED=20260807 roll, reported as context only"
    - "Curvature runs in float64 throughout; chart_curvature._assert_float64 refuses anything else"
    - statement: "The runner distinguishes a genuinely near-zero ||H|| from a degenerate pullback metric by reporting cond(g) per cell alongside ||H||, never substituting one for the other"
      verification: backstop
    - statement: "Every point measured is evaluated at its own chart-assigned coordinate via chart_curvature_field's internal assignment, never at an extrapolated or off-manifold grid point"
      verification: backstop
  artifacts:
    - path: "notebooks/diagnostics/swiss_roll_curvature_sweep_run.py"
      provides: "Resumable n_charts x seed Swiss roll curvature sweep runner with --smoke/--dry-run/--resume"
      contains: "ROLL_FLOOR"
  key_links:
    - from: "notebooks/diagnostics/swiss_roll_curvature_sweep_run.py"
      to: "notebooks/pu_manifold/chart_curvature.py"
      via: "chart_curvature_field(model, x_all.double()) on a trained cae.ChartAutoEncoder"
      pattern: "chart_curvature_field"
    - from: "notebooks/diagnostics/swiss_roll_curvature_sweep_run.py"
      to: "notebooks/pu_manifold/curvature_probe.py"
      via: "make_swiss_roll_fixture + spearman_gate_statistic against analytic H"
      pattern: "spearman_gate_statistic"
  prohibitions:
    - statement: "MUST NOT move, soften, reinterpret or replace the 0.65 floor after any rho_chart value has been observed, and MUST NOT promote the 0.6712 raw-point centroid baseline into a gate it was explicitly demoted from (D-02) -- changing the bar after seeing the result is the forking-paths move this milestone's discipline exists to prevent"
      status: resolved
      verification: judgment
    - statement: "MUST NOT present a synthetic-control pass, or any single-seed draw, as evidence that the PU curvature field is free of parameterization damage -- the control provably cannot reproduce the atlas-fragmentation pathology, and the reported unit is the seed spread, never one draw"
      status: resolved
      verification: judgment
    - statement: "MUST NOT let a value measured on the Swiss roll (the winning n_charts, the roll's rho) or inherited from a sealed prior phase (d_frozen = 5) select any Phase 3 PU hyperparameter (D-06, D-11)"
      status: resolved
      verification: judgment
---

<objective>
Declare Phase 3's only bar before any number exists, then prove the whole Step-1 path end to end
on one Swiss roll cell.

This is the phase's tracer. It wires the complete measurement chain -- roll fixture -> fresh
`cae.ChartAutoEncoder` fit -> `chart_curvature.chart_curvature_field` -> Spearman against the roll's
analytic `H` -> the four-axis fidelity split -- through one configuration only, at production
quality, before any expansion to the sweep or to PU. If that path is wrong, it is wrong after one
CAE fit rather than after ten.

**The gate override, recorded here rather than inherited.** Phase 3's `Depends on` line named a
PASS. No PASS exists anywhere in this milestone: Phase 2 (`m = 0.412071`), 02.2 (T1/T3), 02.4
(T1/T2) and 02.5 stage 1 are all FAIL. The override is deliberate (user decision 2026-08-12), and
its consequence travels with every number this phase produces: **a curvature field decoded from an
unvalidated parameterization conflates real curvature with parameterization damage, and CURV-06/07's
synthetic control provably cannot detect that.** Partial mitigation on record (`02.4-FINDINGS.md`):
every FAIL in this milestone is *global*-scoped, no local-scoped gate has ever failed here, and mean
curvature is a local invariant. That is an argument for proceeding, not a verdict.

**The D-05 scope ruling, also recorded here.** `n_charts` is an in-scope Phase 3 hyperparameter by
explicit user ruling, overriding Phase 02.3's on-hold status for this knob only. Nothing else in the
phase-2 stage is reopened.

Purpose: make the bar unfalsifiable-after-the-fact, and prove the measurement chain works.
Output: `notebooks/diagnostics/swiss_roll_curvature_sweep_run.py`, plus one recorded end-to-end cell.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-decoder-curvature-field/03-CONTEXT.md
@.planning/phases/03-decoder-curvature-field/03-RESEARCH.md
@.planning/phases/03-decoder-curvature-field/03-PATTERNS.md
@.planning/phases/03-decoder-curvature-field/03-VALIDATION.md
</context>

<!-- planner-discipline-allow: ROLL_FLOOR = 0.65 -->
<!-- The literal is load-bearing: D-15 makes this plan's source the whole of the gate machinery,
     so the floor constant must be nameable in the action AND positively grep-asserted in the
     acceptance criteria. No criterion negative-greps for it. -->

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Ratify the Step-1 floor before any rho_chart exists (D-02, one-way)</name>
  <read_first>
    - `.planning/phases/03-decoder-curvature-field/03-CONTEXT.md` (D-01, D-02, D-04, D-05a, D-15)
    - `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-09-SUMMARY.md` section 3 (why 0.6712 is not a validated reference point)
    - `.planning/ROADMAP.md` section "Phase 3: Decoder & Curvature Field" (step 1 and its stopping rule)
  </read_first>
  <decision>
    Phase 3's Step-1 gate statistic, its bar, and what the raw-point baseline is allowed to do.
    The choice below is already made in `03-CONTEXT.md` D-02; this checkpoint exists because the
    decision is **one-way** — declaring the bar before the sweep runs is the entire mechanism, and
    a bar confirmed after the first `rho_chart` is visible is not the same bar.
  </decision>
  <context>
    `02.5-09` measured `rho_chart` values of `{-0.1444, -0.0604, 0.4250, 0.8665}` on identical code,
    data and fixture, so a single-seed gate is a draw from a distribution. The raw-point centroid
    baseline measured `0.6712` at `k=30, d=2` — and `02.5-09-SUMMARY.md` section 3 records that this
    number missed its own notebook's `>0.90` sanity bar (3 of 4 read-out lines, not 4), so it is a
    baseline that *works*, not one validated as correct.

    Undo cost: none, mechanically. The cost is to the record — once any `rho_chart` from this phase
    exists, no later reader can distinguish "we learned something" from "we moved the bar until it
    cleared." `02.6-FINDINGS.md` section 4 already records one criterion changed after an
    unfavourable result in this milestone; a second occurrence for a different question is what this
    checkpoint is spending a human round-trip to prevent.
  </context>
  <options>
    <option id="ratify-d02">
      <name>Ratify D-02 exactly as written (RECOMMENDED — this is the user's locked decision)</name>
      <pros>Statistic: median `rho_chart` over 5 torch seeds, full spread reported alongside (D-01). Bar: absolute floor `median rho_chart > 0.65`. The `0.6712` raw-point baseline is reported as context and **gates nothing**. Under the `n_charts` sweep the floor applies to the best config — the roll passes if the median clears 0.65 at **any** swept `n_charts` — with the full sweep table printed and the multiple-comparisons loosening named as a caveat in the read-out (D-04). Swiss roll only; PU has no analytic `H`, so no equivalent gate exists there (D-02). If no swept config clears, Phase 3 **stops and reports** — a complete outcome, not a failure to work around (D-05a).</pros>
      <cons>N swept configs give N shots at a fixed bar. This is named as a caveat rather than corrected for, because the question Step 1 asks is "can this code recover a known answer at all," not "is this the best config."</cons>
    </option>
    <option id="amend-before-running">
      <name>Amend the floor or the statistic now, before any Phase 3 number exists</name>
      <pros>The only moment an amendment is legitimate. Nothing has been measured, so no forking-paths concern applies.</pros>
      <cons>Departs from a locked user decision recorded in `03-CONTEXT.md` D-02, which already overrode all three alternatives offered at discussion time (beat-the-baseline, absolute-0.90, two-tier).</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The developer's response names exactly one option id (`ratify-d02` or `amend-before-running`).
    - No file under `notebooks/` or `notebooks/.cache/` contains a Phase 3 `rho_chart` value at the moment this checkpoint is answered; `ls notebooks/.cache/03_swiss_roll_curvature_sweep.jsonl` exits non-zero.
    - The plan's SUMMARY records the ratified floor value, the ratified statistic, and the disposition of the `0.6712` baseline verbatim.
  </acceptance_criteria>
  <resume-signal>Reply `ratify-d02`, or `amend-before-running` followed by the replacement floor and statistic.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Ratify the n_charts scope ruling and the swept set (D-05, one-way)</name>
  <read_first>
    - `.planning/phases/03-decoder-curvature-field/03-CONTEXT.md` (D-05, D-06, and the Deferred Ideas block)
    - `.planning/phases/02-eigenspectrum-audit-validity-gate/02-NOTE-phase-2-stage-on-hold.md` sections 4 and 5 (where each held phase stopped; what ends the hold)
    - `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-09-SUMMARY.md` (the monotone-in-charts-used measurement and the fragmentation mechanism)
  </read_first>
  <decision>
    Whether `n_charts` is opened as an in-scope Phase 3 hyperparameter across the Phase 02.3 hold
    boundary, and which values the roll sweep spans.
  </decision>
  <context>
    `02.5-09` found `rho_chart` monotone in **charts actually used**: 3 charts gave `0.8665`, 5 gave
    `0.4250`, 8 gave `-0.0604` and `-0.1444`, with `max cond(g)` tracking in lockstep at `3.26`,
    `7.64`, `63.19`, `122.22`. The mechanism is visible in the plots — the learned surface fragments
    into chart-sized pieces joined by near-straight chords, and `||H||` bands along those artificial
    seams rather than along the spiral. Each chart decoder is C2-smooth internally; the **atlas** is
    not. This is the only axis ever measured to move `rho` in this milestone.

    Undo cost: this ruling crosses a phase boundary the hold note drew (Phase 02.3, CAE Iteration, is
    on hold). Retracting it after the phase starts leaves Phase 3 with no measured lever at all — the
    substrate is fixed, the architecture is fixed, and every other knob is either sealed or untested.
    That is why it is rated one-way rather than costly.
  </context>
  <options>
    <option id="ratify-d05">
      <name>Ratify D-05 as written, roll sweep spans {2, 3, 5, 8} (RECOMMENDED — user's locked ruling plus the planner's discretionary value set)</name>
      <pros>`n_charts` is in scope for Phase 3 and **nothing else** in the phase-2 stage is reopened. The roll sweep spans the measured monotone range `{3, 5, 8}` plus one untested lower value `2`, which is the roll's true chart-count floor for a single-sheet manifold. Five torch seeds per config: `{0, 1, 2, 3, 4}` — seed 0 is the exact `02.5-09` configuration and is the reproduction anchor. D-06 stands: nothing from the roll constrains the PU fit; the roll's winning `n_charts` is context, never a selector.</pros>
      <cons>20 roll fits at roughly 60-90 s each. The value `2` has never been measured and may fail to train a usable atlas at all — that is itself a reportable row, not a defect.</cons>
    </option>
    <option id="ratify-d05-different-set">
      <name>Ratify the scope ruling but change the swept values or the seed count</name>
      <pros>Exact `n_charts` values and seed count above 5 are explicitly Claude's Discretion in `03-CONTEXT.md`; the developer may prefer a different span (for example adding `16` for continuity with the sealed 02.2 architecture).</pros>
      <cons>Widening the sweep multiplies the D-04 multiple-comparisons loosening — more configs, more shots at the same fixed bar.</cons>
    </option>
    <option id="decline-d05">
      <name>Decline the scope ruling — leave `n_charts` fixed at 8</name>
      <pros>Keeps the Phase 02.3 hold boundary intact with no exception.</pros>
      <cons>Phase 3 has no measured lever. Step 1 then reduces to reproducing `-0.0604` and stopping under D-05a with nothing tried.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The developer's response names exactly one option id.
    - The plan's SUMMARY records the ratified `n_charts` set and seed set verbatim, and restates in Phase 3's own words that the ruling opens `n_charts` and nothing else — Phases 02.3, 02.5, 02.6 and 02.7 remain on hold and no sealed verdict is reopened, softened, recomputed or reinterpreted.
    - The SUMMARY restates the gate override and its parameterization-damage consequence in Phase 3's own words (not by reference alone).
  </acceptance_criteria>
  <resume-signal>Reply `ratify-d05`, `ratify-d05-different-set` with the replacement values, or `decline-d05`.</resume-signal>
</task>

<task type="tracer">
  <name>Task 3: End-to-end Swiss roll curvature cell -- one config, whole path, production quality</name>
  <reversibility rating="one-way">Implements the D-02 floor and the D-05 swept set ratified in Tasks 1 and 2; the floor constant lands in source before any sweep number exists, and the sweep design sizes the phase's whole compute budget.</reversibility>
  <files>notebooks/diagnostics/swiss_roll_curvature_sweep_run.py</files>
  <read_first>
    - `notebooks/02.5_swiss_roll_chart_curvature_check.ipynb` — the reference measurement being reproduced; cells 2, 4, 6, 8 and 10 carry the exact fixture, split, training config, and metric call sequence
    - `notebooks/diagnostics/template_benchmark_run.py` lines 673-812 — `load_completed` / `append_record` / the argparse block to copy
    - `notebooks/diagnostics/curvature_feasibility_sweep_run.py` lines 377-583 — the same-domain per-cell registration and measurement idiom
    - `notebooks/pu_manifold/chart_curvature.py` — `chart_curvature_field`, `chart_mean_curvature`, `curvature_fidelity_report`, `assert_c2_activation`, `_assert_float64`, `CURVATURE_CONVENTION`, `VMAP_CHUNK`
    - `notebooks/pu_manifold/curvature_probe.py` — `make_swiss_roll_fixture`, `centroid_mean_curvature`, `mean_curvature_norm`, `spearman_gate_statistic`, `median_relative_error`, `CURVATURE_CONVENTION`
    - `notebooks/pu_manifold/cae.py` — `ChartAutoEncoder.__init__`, `train_cae`, `PlainAutoEncoder`, `train_plain_ae`, `reconstruction_stats`
  </read_first>
  <action>
Create `notebooks/diagnostics/swiss_roll_curvature_sweep_run.py`: a resumable, per-cell-timed sweep
runner over the Swiss roll, built now and exercised on exactly ONE cell in this task. The full grid
runs in plan 03-02.

Module constants, all declared at the top of the file so the bar is in source before any measurement
exists (D-15: this file and this plan are the whole of the gate machinery — no `PREREGISTRATION.md`,
no ratification commit, no git-ancestry proof script, no verdict JSON, no threshold table):
`ROLL_FLOOR = 0.65` (D-02, ratified in Task 1), `RAW_BASELINE_CONTEXT = 0.6712` documented in its own
docstring as **context only, gating nothing**, `N_CHARTS_SWEEP = (2, 3, 5, 8)`,
`TORCH_SEEDS = (0, 1, 2, 3, 4)`, `FIXTURE_SEED = 20260807`, `N_POINTS = 3000`, `CHART_DIM = 2`,
`EMBED_DIM = 8`, `HIDDEN = [64, 64]`, `K_BASELINE = 30`, and
`BASE_CFG = dict(lr=1e-3, weight_decay=1e-4, batch=64, max_epochs=300, early_stop_patience=25,
early_stop_min_delta=1e-4, fps_pretrain_epochs=20, lip_weight=1e-3, lip_every_n_steps=1)`. These are
`02.5_swiss_roll_chart_curvature_check.ipynb`'s values verbatim — do not adjust them, because
reproducing `-0.0604` at seed 0 is the whole point of this task.

Per-cell measurement function, keyed `(n_charts, seed)`, following the notebook's sequence exactly:
build the fixture with `curvature_probe.make_swiss_roll_fixture(n=N_POINTS, seed=FIXTURE_SEED)`;
derive the analytic mean-curvature **vector** `H_true` from the fixture's `t` and `global_std` using
the notebook's cell-4 spiral algebra (the x and z components carry the planar curvature vector, the y
component is zero), and pin it against the fixture's own sealed `H_norm` — the notebook's `pin`
check, which must stay below `1e-12`; split 80/20 with `np.random.default_rng(seed).permutation(N_POINTS)`
(the split rng is seeded by the torch seed, coupling split and initialization exactly as `02.5-09`
did — record that coupling as a stated limitation in the module docstring rather than silently
changing it); `torch.manual_seed(seed)` then fit `cae.ChartAutoEncoder(in_dim=3, embed_dim=EMBED_DIM,
chart_dim=CHART_DIM, n_charts=n_charts, hidden=HIDDEN, activation="silu")` via `cae.train_cae` with
`{**BASE_CFG, "seed": seed, "n_charts": n_charts}`; fit a matched `cae.PlainAutoEncoder(3, CHART_DIM,
hidden=tuple(HIDDEN), activation="silu")` with `cae.train_plain_ae`; call `model.eval()` and
`model.double()`, then `chart_curvature.chart_curvature_field(model, x_all.double())` over all
`N_POINTS` rows.

Record per cell, appended as one JSONL line to `notebooks/.cache/03_swiss_roll_curvature_sweep.jsonl`:
`n_charts_configured`, `n_charts_used` (from the field dict — D-05's monotone mechanism is stated in
charts **actually used**, so the configured value alone is not enough to read the table),
`seed`, `rho_chart` from `curvature_probe.spearman_gate_statistic(H_norm, H_true_norm)`,
`mre_chart` from `median_relative_error`, the four fidelity axes from
`chart_curvature.curvature_fidelity_report(H_vec, H_true)` never collapsed into one score
(`median_cosine_similarity`, `median_magnitude_ratio`, `magnitude_ratio_cv`, and the calibration
slope / intercept / R-squared), `cond_median` and `cond_max` from `metric_condition_number`,
`cae_mse_per_dim` and `plain_mse_per_dim` from `cae.reconstruction_stats` on the holdout,
`curvature_convention` from the field dict, `train_wall_s`, `curv_wall_s`, and the `activation`
string returned by `chart_curvature.assert_c2_activation(model)`.

Compute the raw-point context baseline ONCE per invocation, not per cell: it depends only on the
fixture. `curvature_probe.centroid_mean_curvature(X, k=K_BASELINE, d=CHART_DIM)` then
`mean_curvature_norm` then `spearman_gate_statistic` against `H_true_norm`; print it labelled as
context that gates nothing, and assert it agrees with `RAW_BASELINE_CONTEXT` to four decimal places
so that any drift in the raw-point path is caught here rather than mistaken for a decoder effect.

CLI, copying `template_benchmark_run.py`'s argparse block: `--smoke`, `--dry-run`, `--resume`,
`--record-path`, `--max-combos`, plus `--n-charts` and `--seeds` (comma-separated overrides that
default to the module constants). `--dry-run` prints the planned `(n_charts, seed)` grid and the
declared floor, then exits without running or writing anything. `--smoke` runs a single cheap cell
(`n_charts=2`, `seed=0`, `max_epochs=5`, 300 points) purely to prove the wiring executes end to end,
and prints a tally. `--resume` loads completed keys via a `load_completed`-shaped JSONL reader and
skips them.

Do not implement the median / floor / read-out summary in this task; plan 03-02 owns it. This task
proves the measurement chain, not the verdict.

Finally, run the single reproduction cell `--n-charts 8 --seeds 0 --max-combos 1` and record the
measured `rho_chart` in the plan SUMMARY next to `02.5-09`'s `-0.0604`.
  </action>
  <verify>
    <automated>.venv/bin/python notebooks/diagnostics/swiss_roll_curvature_sweep_run.py --dry-run && .venv/bin/python notebooks/diagnostics/swiss_roll_curvature_sweep_run.py --smoke && .venv/bin/python -m pytest notebooks/pu_manifold/tests/test_curvature_probe.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python notebooks/diagnostics/swiss_roll_curvature_sweep_run.py --dry-run` exits 0 and prints 20 planned cells (4 `n_charts` values x 5 seeds) and the line containing `0.65`.
    - `--dry-run` writes nothing: `notebooks/.cache/03_swiss_roll_curvature_sweep.jsonl` does not exist, or its byte count is unchanged before and after the invocation.
    - `.venv/bin/python notebooks/diagnostics/swiss_roll_curvature_sweep_run.py --smoke` exits 0 and prints a tally.
    - `grep -v '^#' notebooks/diagnostics/swiss_roll_curvature_sweep_run.py | grep -c 'ROLL_FLOOR = 0.65'` returns at least 1.
    - `grep -v '^#' notebooks/diagnostics/swiss_roll_curvature_sweep_run.py | grep -c 'n_charts_used'` returns at least 1.
    - The single reproduction cell `--n-charts 8 --seeds 0 --max-combos 1` exits 0 and its recorded `rho_chart` agrees with `-0.0604` to within `2e-3`; if it does not, the plan HALTS and the discrepancy is reported rather than the constants being adjusted to make it match.
    - The recorded raw-point baseline Spearman agrees with `0.6712` to four decimal places, and the printed label for it contains the word `context`.
    - The recorded `curvature_convention` field equals `trace`.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_curvature_probe.py -x -q` exits 0 (the sealed chart-curvature tests are untouched by this plan).
  </acceptance_criteria>
  <done>The runner exists with the floor and the swept grid declared in source, `--dry-run` and `--smoke` both pass, and one end-to-end cell at `n_charts=8, seed=0` reproduces `02.5-09`'s `-0.0604` measurement through the full fit -> chart decoder -> `torch.func` curvature -> Spearman chain.</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged Assumptions — Edge Probe (15 unresolved rows)

No phase SPEC.md exists, so `## Edge Coverage` and `## Prohibitions` are both absent and the
spec-less probe fallback fired for both. The deterministic edge probe returned **15 applicable
items, 0 resolved, 15 unresolved** across the phase's 13 requirement IDs. Twelve are
`unclassified`; the three it did classify (all against `CURV-01`) are off-target — its category
vocabulary (adjacency / empty / ordering) is built for CRUD- and API-shaped requirements and does
not classify a batched Jacobian computation. Per the protocol an `unclassified` row stays
`unresolved` and is **never** auto-backstopped, so all fifteen are surfaced here as explicit flagged
assumptions rather than converted into fabricated acceptance criteria.

| # | Requirement | Probe category | Probe question | Disposition |
|---|-------------|----------------|----------------|-------------|
| 1 | DEC-01 | unclassified | unclassified — review manually | flagged assumption |
| 2 | DEC-02 | unclassified | unclassified — review manually | flagged assumption |
| 3 | DEC-03 | unclassified | unclassified — review manually | flagged assumption |
| 4 | DEC-04 | unclassified | unclassified — review manually | flagged assumption |
| 5 | DEC-05 | unclassified | unclassified — review manually | flagged assumption |
| 6 | CURV-01 | adjacency | When two things are exactly equal or just touch, do they merge, collide, or separate? | flagged assumption — off-target for a batched Jacobian |
| 7 | CURV-01 | empty | What is the result for empty, single-element, or null input? | flagged assumption — `chart_curvature_field` already raises on an empty `x`; the probe did not know that |
| 8 | CURV-01 | ordering | When elements compare equal, is output order specified and stable? | flagged assumption — row order is pinned by `test_chart_curvature_field_reassembles_in_row_order`; the probe did not know that |
| 9 | CURV-02 | unclassified | unclassified — review manually | flagged assumption |
| 10 | CURV-03 | unclassified | unclassified — review manually | flagged assumption |
| 11 | CURV-04 | unclassified | unclassified — review manually | flagged assumption |
| 12 | CURV-05 | unclassified | unclassified — review manually | flagged assumption |
| 13 | CURV-06 | unclassified | unclassified — review manually | flagged assumption |
| 14 | CURV-07 | unclassified | unclassified — review manually | flagged assumption |
| 15 | CURV-08 | unclassified | unclassified — review manually | flagged assumption |

**No-silent-drop equality:** 15 probe-surfaced items == 0 authored into `must_haves` + 15 surfaced
as flagged assumptions. Nothing was dismissed and nothing was auto-backstopped.

**Separately** (additive, not counted against the equality above): the phase's genuine edge cases are
numerical and are already enumerated with evidence in `03-RESEARCH.md` (Numerical Hazards at
`d~20, D=768`) and `03-VALIDATION.md` (Phase Step -> Evidence Map). Those are lifted into the
`must_haves.truths` of the plans that own them — near-singular `g` detected and flagged rather than
averaged in (03-09), genuine near-zero curvature distinguished from a degenerate metric (03-04,
03-09), the trace-convention regression pinning a unit `d`-sphere at `||H|| = d` and never `1`
(03-04), float64 enforced (03-01, 03-05, 03-03), chunking preserved with exact short-chunk padding
(03-05), and chart-assigned coordinates only, never extrapolated (03-01, 03-09). Criteria that
cannot be made explicitly checkable are authored as structured `{ statement, verification: backstop }`
markers so they abstain to `human_needed` at verify time rather than passing silently.

**Prohibition probe (section B), run in-prompt.** Stage 1 over-produced eleven raw must-NOT
candidates; stage 2 dropped the routine-engineering ones (averaged-convention transcription, use of
the known-broken `cae.chart_survival` helper as an occupancy criterion, and forward mode defaulting
on — all owned by regression tests, code review, or an explicit acceptance criterion) and kept the
three values / research-integrity items authored into `must_haves.prohibitions` above,
descriptor-less. No canon security/compliance candidate surfaced, so there is nothing to breadcrumb
to `/gsd-secure-phase`.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trained decoder -> `torch.func` transform | A model whose activation family or dtype violates the curvature math's preconditions crosses into autodiff; nothing outside the module guards catches it |
| transform composition -> returned tensor | A wrongly-composed `torch.func` transform returns a plausible, correctly-typed, wrongly-shaped tensor without raising |
| declared bar -> measured result | A bar that can still move after the result is visible is not a bar |

No network surface, no authentication surface, no untrusted external input path, no secrets, and no
persistence beyond gitignored `notebooks/.cache/`. ASVS L1, block on `high`. V2 / V3 / V4 / V6 do not
apply; V5 applies only as internal contract checks (`03-VALIDATION.md` Security Domain, consistent
with `02.6-REVIEW.md`'s prior assessment of this exact codebase area). Every threat below is
data-integrity — a silent wrong answer — not adversarial. No finding is manufactured to fill a row.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Tampering | `cae.ChartAutoEncoder` activation, consumed by `chart_curvature` | high | mitigate | `chart_curvature.assert_c2_activation(model)` is called by `chart_curvature_field` before any differentiation and **raises** on a ReLU-family activation; the runner records its returned string per cell |
| T-3-02 | Tampering | `vmap(jacrev)` / `vmap(hessian)` composition in `chart_mean_curvature` | high | mitigate | The exact-tuple shape assertions at `chart_curvature.py:351` and `:360` are left in place unmodified; this plan adds no new composition |
| T-3-03 | Tampering | analytic `H_true` derived in the runner from the roll's `t` | high | mitigate | The derived vector is pinned against the fixture's own sealed `H_norm` (`max abs diff < 1e-12`) rather than trusted by inspection, and `curvature_convention` is recorded per cell and asserted equal to `trace` |
| T-3-04 | Tampering | float64 discipline through the roll path | high | mitigate | `model.double()` before the field call; `chart_curvature._assert_float64` refuses anything else and is not bypassed |
| T-3-08 | Tampering | the Step-1 bar itself (research record integrity) | high | mitigate | `ROLL_FLOOR = 0.65` lands in source before any sweep record exists; Task 1's blocking checkpoint asserts no `rho_chart` record file is present when it is answered; prohibition P-1 carries the constraint forward |
| T-3-SC | Tampering | package supply chain | low | accept | No package is installed this phase. `03-RESEARCH.md` Package Legitimacy Audit records zero new package names and zero `[ASSUMED]` / `[SUS]` / `[SLOP]` verdicts; every import is a module already in production use across six prior phases |
</threat_model>

## Artifacts this phase produces

Symbols Phase 3 creates. None of these exist in the tree before this phase; exclude them from drift
verification.

**New files**
`notebooks/pu_manifold/synthetic_controls.py`,
`notebooks/pu_manifold/tests/test_synthetic_controls.py`,
`notebooks/diagnostics/swiss_roll_curvature_sweep_run.py`,
`notebooks/diagnostics/curvature_field_pu_run.py`,
`notebooks/diagnostics/synthetic_control_run.py`,
`notebooks/03_swiss_roll_chart_curvature_field_check.ipynb`,
`notebooks/03_pu_curvature_field.ipynb`,
`.planning/phases/03-decoder-curvature-field/03-FINDINGS.md`.

**New functions / classes / constants**
`chart_curvature._jacobian_hessian`, `chart_curvature.CURVATURE_MODES`,
`derivative_bridge._assert_decode_batch_float64`, `derivative_bridge._chunked_vmap_hessian`,
`synthetic_controls.CURVATURE_CONVENTION`, `synthetic_controls.rotate_and_pad`,
`synthetic_controls.make_flat_control`, `synthetic_controls.make_sphere_control`,
`synthetic_controls.make_saddle_control`.

**New keyword parameters and return keys on existing functions**
`chart_curvature.chart_mean_curvature(..., mode="reverse")`,
`chart_curvature.chart_curvature_field(..., mode="reverse")`,
`derivative_bridge._agreement_stats(..., rel_floor=...)` and its new
`near_zero_reference_fraction` return key.

**New module-level constants in runners**
`ROLL_FLOOR`, `RAW_BASELINE_CONTEXT`, `N_CHARTS_SWEEP`, `TORCH_SEEDS`, `FIXTURE_SEED`,
`PU_N_CHARTS_SWEEP`, `PU_SEEDS`, `PU_CHART_DIM`, `PU_PH_PRIMARY_CELLS`, `COND_FLAG_PERCENTILE`,
`BRIDGE_N_POINTS_PU`, `CONTROL_FIXTURES`.

**New CLI flags**
`swiss_roll_curvature_sweep_run.py`: `--smoke --dry-run --resume --record-path --max-combos
--n-charts --seeds`.
`curvature_field_pu_run.py`: `--smoke --dry-run --resume --record-path --max-combos --timing-probe
--pu-n --select-only`.
`synthetic_control_run.py`: `--smoke --dry-run --resume --record-path --fixture`.

**New test names**
`test_chart_curvature_forward_mode_matches_reverse_to_float64_round_off`,
`test_chart_curvature_reverse_mode_is_bit_identical_to_sealed_baseline`,
`test_chart_curvature_rejects_unknown_mode`,
`test_chart_curvature_forward_mode_calls_c2_guard`,
`test_chart_curvature_forward_mode_keeps_shape_assertions`,
`test_derivative_bridge_float32_model_raises_friendly_value_error`,
`test_derivative_bridge_reports_near_zero_reference_fraction`,
`test_calibrate_fd_step_chunks_above_vmap_chunk`,
`test_synthetic_flat_control_is_exactly_zero`,
`test_synthetic_sphere_control_matches_d_over_R`,
`test_synthetic_saddle_control_matches_finite_difference`,
`test_synthetic_controls_convention_is_trace_not_averaged`.

<verification>
- `.venv/bin/python notebooks/diagnostics/swiss_roll_curvature_sweep_run.py --dry-run` exits 0, prints 20 cells and the declared floor, writes nothing.
- `.venv/bin/python notebooks/diagnostics/swiss_roll_curvature_sweep_run.py --smoke` exits 0.
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/` exits 0 (full suite, unchanged by this plan).
- The single reproduction cell's `rho_chart` matches `-0.0604` to within `2e-3`.
- Both blocking checkpoints are answered and their dispositions recorded verbatim in the SUMMARY.
</verification>

<success_criteria>
1. The 0.65 floor, the median-of-5-seeds statistic, the best-of-swept-config rule and the D-05a stopping rule are all recorded in Phase 3's own artifacts before any `rho_chart` value exists.
2. The gate override and its parameterization-damage consequence, and the D-05 `n_charts` scope ruling, are each restated in Phase 3's own words rather than inherited by reference.
3. `swiss_roll_curvature_sweep_run.py` exists, is resumable, per-cell timed, and supports `--smoke` / `--dry-run` / `--resume`.
4. One end-to-end cell reproduces `02.5-09`'s `-0.0604` through the complete fit -> chart decoder -> `torch.func` curvature -> Spearman chain.
5. The raw-point context baseline reproduces `0.6712` and is labelled as context that gates nothing.
</success_criteria>

<output>
Create `.planning/phases/03-decoder-curvature-field/03-01-SUMMARY.md` when done.
</output>
