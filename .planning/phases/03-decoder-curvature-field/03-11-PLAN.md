---
phase: 03-decoder-curvature-field
plan: 11
type: execute
wave: 8
depends_on: ["03-02", "03-06", "03-09", "03-10"]
files_modified:
  - notebooks/03_pu_curvature_field.ipynb
  - .planning/phases/03-decoder-curvature-field/03-FINDINGS.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [DEC-01, DEC-02, DEC-03, DEC-04, DEC-05, CURV-01, CURV-02, CURV-03, CURV-04, CURV-05, CURV-06, CURV-07, CURV-08]

must_haves:
  truths:
    - "All 13 stale requirement IDs are re-minted with rewritten text under the same DEC- / CURV- namespace, none silently dropped, with an old-to-new mapping table recorded in REQUIREMENTS.md"
    - "03-FINDINGS.md states the gate override and its parameterization-damage consequence in Phase 3's own words, not by reference"
    - "03-FINDINGS.md states the D-05 n_charts scope ruling in Phase 3's own words, and states that it opened n_charts and nothing else"
    - "03-FINDINGS.md restates the chart_dim = 20 justification and records the rejection of d_frozen = 5 with its reason"
    - "Every reported statistic in 03-FINDINGS.md carries its seed spread; no single draw is presented as a result"
    - "The multiple-comparisons caveat, the parameterization-damage caveat, and the 0.6712-is-context caveat each appear alongside the numbers they qualify"
    - "The presentation notebook reads the runners' JSONL records and performs no training and no expensive computation inline"
    - "If the Step-1 gate did not clear, 03-FINDINGS.md is written as a Step-1-only stop-and-report record and is a complete phase outcome, not an error report"
    - statement: "The synthetic control is reported before Phase 4 starts, with the caveat that a passing control never reproduces the fragmentation pathology stated plainly beside it (ROADMAP success criterion 5)"
      verification: backstop
    - statement: "No sealed verdict from Phase 2, 02.1, 02.2, 02.4, 02.5, 02.6 or 02.7 is reopened, softened, recomputed or reinterpreted anywhere in this phase's record"
      verification: backstop
  artifacts:
    - path: ".planning/phases/03-decoder-curvature-field/03-FINDINGS.md"
      provides: "The phase record: override, scope ruling, dimension justification, four steps, caveats, re-mint mapping"
      contains: "d_frozen"
    - path: ".planning/REQUIREMENTS.md"
      provides: "13 re-minted DEC / CURV requirements plus a dated old-to-new mapping section"
      contains: "Phase 3 Requirement Re-Mint"
    - path: "notebooks/03_pu_curvature_field.ipynb"
      provides: "Presentation of the PU field and the synthetic controls from the runners' recorded output"
      contains: "load"
  key_links:
    - from: "notebooks/03_pu_curvature_field.ipynb"
      to: "notebooks/.cache/03_curvature_field_pu.jsonl"
      via: "JSONL loader reading recorded runner output; no training inline"
      pattern: "jsonl"
    - from: ".planning/phases/03-decoder-curvature-field/03-FINDINGS.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the re-mint mapping table is written in both, and the requirement IDs match"
      pattern: "CURV-0"
  prohibitions:
    - statement: "MUST NOT present the synthetic-control result, or any single seed, as evidence that the PU curvature field is free of parameterization damage -- the control is structurally blind to the fragmentation pathology and the reported unit is always the seed spread"
      status: resolved
      verification: judgment
    - statement: "MUST NOT reopen, soften, recompute or reinterpret any sealed verdict from Phases 2, 02.1, 02.2, 02.4, 02.5, 02.6 or 02.7 in this record, and MUST NOT describe the 0.6712 raw-point baseline as a validated benchmark rather than the context number it is"
      status: resolved
      verification: judgment
    - statement: "MUST NOT silently drop, retire or re-point any of the 13 stale requirement IDs -- each is re-minted with rewritten text or explicitly retired with a recorded reason, and the mapping is written down"
      status: resolved
      verification: judgment
---

<objective>
Write the phase record and re-mint the requirement set.

`03-FINDINGS.md` is where the four steps, their caveats and their conditions become one readable
document, and where two things that must never be inherited silently are stated in Phase 3's own
words: the **gate override** (no PASS exists anywhere in this milestone, and a curvature field decoded
from an unvalidated parameterization conflates real curvature with parameterization damage) and the
**D-05 `n_charts` scope ruling** (which opened one knob across the Phase 02.3 hold boundary and
nothing else).

`REQUIREMENTS.md`'s DEC-01..05 and CURV-01..08 were written against Isomap coordinates and a global
chart. Both premises are gone. All 13 are re-minted here with rewritten text under the same ID
namespace, with the old-to-new mapping recorded so a later reader can see what changed and why.

**The D-05a branch.** If plan 03-02's Step-1 gate did not clear, this plan's `depends_on` is
re-pointed to `["03-02", "03-03", "03-04"]`, plans 03-05 through 03-10 do not run, and
`03-FINDINGS.md` is written as a Step-1-only record: the sweep table, the floor, the outcome, the
library work that landed anyway, and the statement that Phase 3 stopped and reported. That is a
complete phase outcome, exactly as the ROADMAP and D-05a say, and it is written as a result rather
than as a failure report.

Purpose: leave a record a reader can act on without re-deriving the phase.
Output: one presentation notebook, one findings document, one re-minted requirement set.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-decoder-curvature-field/03-CONTEXT.md
@.planning/phases/03-decoder-curvature-field/03-RESEARCH.md
@.planning/phases/03-decoder-curvature-field/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: notebooks/03_pu_curvature_field.ipynb -- presentation from recorded output only</name>
  <files>notebooks/03_pu_curvature_field.ipynb</files>
  <read_first>
    - `notebooks/02.6_swiss_roll_plainae_curvature_check.ipynb` — the presentation-notebook shape used in this milestone
    - `notebooks/diagnostics/curvature_field_pu_run.py` — the JSONL record schema for the grid, the `field` records and the `bridge` record
    - `notebooks/diagnostics/synthetic_control_run.py` — the JSONL record schema for the three control fixtures
    - `.planning/phases/03-decoder-curvature-field/03-08-SUMMARY.md`, `03-09-SUMMARY.md` and `03-10-SUMMARY.md` — the numbers to present
  </read_first>
  <action>
Create `notebooks/03_pu_curvature_field.ipynb` as a presentation of the recorded runner output for
ROADMAP steps 2, 3 and 4. It performs no training, calls no expensive computation inline, and fits
nothing — it loads `notebooks/.cache/03_curvature_field_pu.jsonl` and
`notebooks/.cache/03_synthetic_control.jsonl` with a `load_completed`-shaped reader and renders.

Cells, in order:

1. Imports, record paths, and a provenance cell printing which records were found and their counts.
2. The D-07 selection table: nine fits, four uncollapsed diagnostics, per-seed values and medians,
   with the pre-declared lexicographic rule printed above it and the selected `n_charts` named.
3. The PU `‖H‖` field: histogram per seed and the three-seed spread table, labelled a mean-curvature
   **vector norm** in the axis label and the title, never Gaussian or principal.
4. The `cond(g)` distribution per seed, and the joint `‖H‖`-versus-`cond(g)` two-dimensional
   histogram, with the flagged-point count annotated and the flagged points shown separately.
5. The finite-difference bridge table: `full_hessian_agreement` and
   `reduced_mean_curvature_agreement` side by side under separate headings, with
   `near_zero_reference_fraction` beside any relative column above 1.0, and no threshold line drawn
   on any axis.
6. The synthetic controls: one row per fixture with the four fidelity axes as four columns, the flat
   fixture's measured noise floor drawn on the same axis as the PU `‖H‖` histogram so the two are
   directly comparable, and the sphere's estimated-versus-analytic ratio.
7. A markdown cell carrying the three caveats in full — the gate override and its
   parameterization-damage consequence, the D-04 multiple-comparisons loosening on the Step-1 gate,
   and the statement that a passing synthetic control cannot detect parameterization damage.

This is a presentation notebook, not a CLAUDE.md sanity check, and the two must not be confused: the
mandated sanity notebook is `notebooks/03_swiss_roll_chart_curvature_field_check.ipynb` from plan
03-06, which trains from scratch in under two minutes. This one trains nothing and may read the
gitignored cache freely. State that distinction in the first markdown cell.

Execute the notebook end to end and commit it with its outputs.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json
nb=json.load(open('notebooks/03_pu_curvature_field.ipynb'))
cells=nb['cells']
src=''.join(''.join(c['source']) for c in cells)
assert 'train_cae' not in src, 'presentation notebook trains inline'
assert 'jsonl' in src.lower(), 'presentation notebook does not read the runner records'
assert sum(1 for c in cells if c['cell_type']=='code' and c.get('outputs')) > 0, 'notebook not executed'
print('ok', len(cells), 'cells')
"</automated>
  </verify>
  <acceptance_criteria>
    - The notebook parses as JSON, every code cell has a non-null `execution_count`, and at least one carries outputs.
    - The concatenated source contains no `train_cae`, no `train_plain_ae` and no `make_swiss_roll` call — nothing is fitted inline.
    - The concatenated source reads both `03_curvature_field_pu` and `03_synthetic_control` record paths.
    - At least one axis label or title in the `‖H‖` figures contains the words `vector norm`.
    - The final markdown cell contains all three caveats: the gate override with its parameterization-damage consequence, the multiple-comparisons loosening, and the synthetic control's blindness to fragmentation.
    - The first markdown cell states that this notebook is a presentation and names `notebooks/03_swiss_roll_chart_curvature_field_check.ipynb` as the separate CLAUDE.md sanity check.
    - `git status --porcelain notebooks/03_swiss_roll_chart_curvature_field_check.ipynb` is empty — the sanity notebook is not modified by this plan.
  </acceptance_criteria>
  <done>The presentation notebook exists, committed executed, rendering the recorded step-2, step-3 and step-4 output with its three caveats and without fitting anything.</done>
</task>

<task type="auto">
  <name>Task 2: 03-FINDINGS.md and the 13-requirement re-mint</name>
  <files>.planning/phases/03-decoder-curvature-field/03-FINDINGS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` lines 70-100 (the DEC and CURV blocks and the 2026-07-31 amendment blockquote above them) and lines 195-240 (the Traceability table and the per-phase requirement counts)
    - `.planning/phases/03-decoder-curvature-field/03-RESEARCH.md` — the Phase Requirements re-minting table mapping each stale ID to what changes and what supports it
    - Every `03-*-SUMMARY.md` produced by this phase, for the numbers and outcomes to record
    - `.planning/phases/02-eigenspectrum-audit-validity-gate/02-NOTE-phase-2-stage-on-hold.md` sections 3 and 4 — the override and the carried debt
    - `.planning/phases/02-eigenspectrum-audit-validity-gate/02-FINDINGS.md` sections 6.3 and 6.4 — the dimension evidence and why `d_frozen = 5` is suspect
  </read_first>
  <action>
**A. Write `03-FINDINGS.md`.** Sections, in this order:

1. *The override, in Phase 3's own words.* Phase 3's `Depends on` line named a PASS. No PASS exists
   anywhere in this milestone — Phase 2's `m = 0.412071`, 02.2's T1 and T3, 02.4's T1 and T2, and
   02.5 stage 1 are all FAIL. The override was a deliberate user decision on 2026-08-12. Its
   consequence, carried rather than lost: a curvature field decoded from an unvalidated
   parameterization conflates real curvature with parameterization damage, and the synthetic control
   provably cannot detect that. The partial mitigation on record: every FAIL in this milestone is
   global-scoped, no local-scoped gate has ever failed here, and mean curvature is a local invariant.
   State that this is an argument for proceeding and not a verdict.
2. *The D-05 scope ruling, in Phase 3's own words.* `n_charts` was opened as an in-scope Phase 3
   hyperparameter by explicit user ruling, overriding Phase 02.3's on-hold status for that knob only.
   State the rationale (`02.5-09`'s monotone-in-charts-used measurement and the fragmentation
   mechanism), and state that Phases 02.3, 02.5, 02.6 and 02.7 remain on hold and no sealed verdict is
   reopened, softened, recomputed or reinterpreted.
3. *The working dimension.* `chart_dim = 20`, justified against TwoNN 19.5, local-PCA median 25.0
   (std 2.0), and a median of 18 across eight geometric estimators. `d_frozen = 5` explicitly
   rejected, with the reason: at 41% negative eigenvalue mass the residual curve saturates early, so
   the elbow measured the flatness failure rather than the geometry.
4. *Step 1 — the Swiss roll known-answer gate.* The declared floor, the full sweep table, the
   per-config medians and spreads, the outcome, and — printed with the outcome, not after it — the
   D-04 multiple-comparisons caveat and the note that the `0.6712` raw-point baseline is context that
   gated nothing and itself missed its own notebook's `>0.90` bar.
5. *Step 2 and Step 3 — the PU field.* The selected `n_charts` and the deciding axis, the `‖H‖`
   distribution across three seeds, the `cond(g)` distribution and the flagged near-singular counts,
   the chart-assignment and second-derivative evidence, the finite-difference bridge at PU scale, and
   the first measured full-cloud curvature wall clock at `d = 20, D = 768`.
6. *Step 4 — the synthetic control.* The three fixtures' four-axis comparisons, the flat fixture's
   noise floor beside the PU `‖H‖` distribution, and CURV-07's answer stated conditioned on the
   override. The parameterization-damage caveat goes in this section beside the numbers.
7. *Library work.* D-08's `add-alongside` mode toggle with its spike outcome and its measured
   forward-versus-reverse timing; D-09's equivalence and the reverse-path bit-identity evidence;
   D-14's WR-01, WR-02 and WR-03 closures; `synthetic_controls.py` and its tests.
8. *Carried limitations, each with its own line.* The override and its consequence; D-12's trigger
   outcome and the decision recorded at plan 03-08's checkpoint; the known limitation that
   reconstruction and topology do not predict curvature quality; the undeclared `ripser` / `persim`
   dependency, inherited and not resolved because `pyproject.toml` is frozen this milestone; the
   coupling of split seed and torch seed in the roll sweep; and the fact that forward mode remains
   opt-in with a named condition for a later promote.
9. *Handoff to Phase 4.* What Phase 4 receives — a per-point `‖H‖` field to quantile-partition — and
   under what conditions it receives it. State that ordering is the quantity Phase 4 consumes, which
   is why Spearman gated Step 1 and magnitude did not.

**The D-05a branch.** If plan 03-02's gate did not clear, write sections 1, 2, 3, 4, 7, 8 and 9 only,
with section 4 carrying the stop-and-report outcome and section 9 stating that Phase 4 receives no
field. Write it as a result: the phase asked whether this code can recover a known answer, it
measured that it cannot at any swept `n_charts`, and that is the finding. Do not frame it as a
failure to work around, and do not propose remediation inside the findings document.

**B. Re-mint the requirements in `.planning/REQUIREMENTS.md`, using `Edit` and never `Write`.** Keep
the `DEC-` / `CURV-` namespace and the ID numbers so the Traceability table stays structurally valid;
rewrite the text of all 13. Target texts:

- **DEC-01**: Train a per-chart decoder — `cae.ChartAutoEncoder`'s `chart_decoders[i]` composed with
  the single shared `embedding_decoder` — mapping each point's own chart coordinate to the 768-d
  embedding, with a C2-smooth activation throughout the forward path.
- **DEC-02**: No ReLU-family activation anywhere in the decoder, enforced by a guard that raises
  (`chart_curvature.assert_c2_activation`, `decoder_curvature.assert_c2_decoder`) rather than merely
  verified once.
- **DEC-03**: Held-out reconstruction quality shown per `n_charts` configuration in the PU sweep, not
  training loss and not from a single fit.
- **DEC-04**: Both an aggregate reconstruction metric and a per-output-dimension distribution shown
  for every configuration, so a good average cannot hide a subset failure.
- **DEC-05**: Every fit reproducible from a recorded torch seed, and every result reported across
  seeds — at least 5 on the Swiss roll gate, 3 on PU — never from a single draw.
- **CURV-01**: First fundamental form `g = J^T J` from the chart decoder's Jacobian via `torch.func`,
  batched with `vmap` rather than looped, under a selectable reverse or forward differentiation mode
  whose outputs agree to float64 round-off.
- **CURV-02**: Second fundamental form as the normal-projected ambient Hessian, computed in the
  trace-first-then-project form — the `g`-trace and the normal projection commute, so the trace is
  taken first and the projection applied by a `chart_dim` by `chart_dim` solve; no `(D, D)` projector
  and no full `II` tensor is ever materialized.
- **CURV-03**: Mean curvature vector field and its norm shown, labelled a vector norm and never
  Gaussian or principal curvature, under the pinned `CURVATURE_CONVENTION = "trace"`, `H = tr_g(II)`
  — a unit `d`-sphere gives a norm of `d`, not 1.
- **CURV-04**: Conditioning of the pullback metric shown as a distribution — histogram, median, 90th
  and 99th percentile, maximum — with points above a within-config percentile flagged and excluded
  from the reported `‖H‖` summary rather than averaged in; no fixed absolute threshold.
- **CURV-05**: Decoder second derivatives verified non-zero and finite at held-out points, and
  independently cross-checked at PU scale against finite differences by
  `derivative_bridge.derivative_agreement`, reported and never gated on.
- **CURV-06**: PU curvature compared against the same architecture and training protocol fitted to
  flat plane, sphere and saddle at matched `chart_dim` and ambient 768, with the statement, alongside
  the numbers, that this control cannot detect parameterization damage.
- **CURV-07**: Whether the measured curvature is a property of the data manifold or an artifact of
  the fitted decoder, answered on CURV-06's evidence and explicitly conditioned on Phase 3's gate
  override, never presented as if the parameterization were independently validated.
- **CURV-08**: Curvature evaluated only at each point's own chart coordinate as assigned by
  `model.chart_probs(z).argmax(dim=1)`, never at an extrapolated, interpolated or grid coordinate.

Add a dated section `## Phase 3 Requirement Re-Mint (2026-08-13)` recording, as a table, each of the
13 IDs with a one-line statement of what changed (the two dead premises being "Isomap coordinates"
and "a single global chart") and the artifact supporting the new text. Record explicitly that all 13
were re-minted and none retired. Supersede the `AMENDED 2026-07-31 (Phase 2 FAIL)` blockquote above
the DEC block with a dated note pointing at this section, using a scoped `Edit` — do not delete
surrounding requirement rows and do not rewrite the file.

Update the Traceability table rows for the 13 IDs from `Pending` to the outcome this phase reached,
and leave every other row untouched.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import re,sys
t=open('.planning/REQUIREMENTS.md').read()
ids=['DEC-0%d'%i for i in range(1,6)]+['CURV-0%d'%i for i in range(1,9)]
missing=[i for i in ids if t.count(i)<2]
assert not missing, 'ids under-referenced: %s'%missing
assert 'Phase 3 Requirement Re-Mint' in t, 'remint section missing'
assert 'Isomap' not in re.search(r'DEC-01.*?CURV-08', t, re.S).group(0), 'stale Isomap text survives in the re-minted block'
print('ok')
" && test -f .planning/phases/03-decoder-curvature-field/03-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-decoder-curvature-field/03-FINDINGS.md` exists and contains sections for the override, the D-05 scope ruling, the `chart_dim = 20` justification, Step 1, and carried limitations; and, unless the D-05a branch was taken, sections for Steps 2, 3 and 4 as well.
    - `03-FINDINGS.md` contains the literal strings `d_frozen`, `parameterization damage`, `multiple-comparisons`, and `0.6712`.
    - `03-FINDINGS.md` describes `0.6712` as context and not as a validated benchmark; `grep -c 'validated benchmark' .planning/phases/03-decoder-curvature-field/03-FINDINGS.md` returns 0.
    - Every one of the 13 IDs appears at least twice in `REQUIREMENTS.md` — once in its rewritten requirement line and once in the Traceability table.
    - The re-minted DEC and CURV block contains no occurrence of the word `Isomap`.
    - `REQUIREMENTS.md` contains a section headed `## Phase 3 Requirement Re-Mint (2026-08-13)` with 13 mapping rows and an explicit statement that none was retired.
    - `git diff --stat .planning/REQUIREMENTS.md` shows changes confined to the DEC / CURV block, the amendment blockquote, the Traceability rows for those 13 IDs, and the new re-mint section — no other requirement family is touched.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0.
  </acceptance_criteria>
  <done>The phase record exists with the override, the scope ruling, the dimension justification, the four steps and their caveats; all 13 requirements are re-minted with a recorded old-to-new mapping and none dropped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Close the phase -- read the record against what was decided</name>
  <read_first>
    - `.planning/phases/03-decoder-curvature-field/03-FINDINGS.md`
    - `.planning/REQUIREMENTS.md` — the re-minted DEC / CURV block and the re-mint mapping section
    - `notebooks/03_pu_curvature_field.ipynb` and `notebooks/03_swiss_roll_chart_curvature_field_check.ipynb`
    - `.planning/phases/03-decoder-curvature-field/03-CONTEXT.md` — D-01 through D-15, to check each is honoured
  </read_first>
  <what-built>
    The complete Phase 3 record: a Swiss roll known-answer gate with its declared floor and printed
    sweep, a per-point mean-curvature field over the PU cloud across three seeds with its metric
    conditioning and flagged near-singular points, a finite-difference bridge at PU scale, three
    matched synthetic controls, a CLAUDE.md sanity notebook, a presentation notebook, and 13 re-minted
    requirements.
  </what-built>
  <how-to-verify>
    1. Read `03-FINDINGS.md`. Does it state the gate override and its parameterization-damage consequence in Phase 3's own words, or does it point at another document to say it?
    2. Does it state the D-05 `n_charts` scope ruling in its own words, and say that nothing else in the phase-2 stage was opened?
    3. Does it justify `chart_dim = 20` and record the rejection of `d_frozen = 5` with the reason?
    4. Is every number reported with its seed spread? Find any statistic reported from one seed and say so.
    5. Do the three caveats sit beside the numbers they qualify, rather than in a trailing limitations section — the multiple-comparisons loosening beside the Step-1 verdict, the parameterization-damage caveat beside the Step-4 numbers, and the `0.6712`-is-context note beside the Step-1 table?
    6. Check the re-mint: all 13 IDs rewritten, none dropped, none retired without a reason, and no `Isomap` text surviving in the re-minted block.
    7. Confirm the two notebooks are distinct and correctly scoped — the sanity check trains from scratch in under two minutes, the presentation trains nothing.
    8. Walk `03-CONTEXT.md`'s D-01 through D-15 and name any decision the record does not honour.
  </how-to-verify>
  <acceptance_criteria>
    - The developer's response states whether each of the eight checks passed, naming any that did not.
    - The plan SUMMARY records the response verbatim and lists any decision from D-01 through D-15 flagged as unhonoured.
    - The SUMMARY states whether the phase reached Step 4 or stopped at the D-05a branch, and in either case names the outcome as complete.
  </acceptance_criteria>
  <resume-signal>Reply `approved`, or list the checks that failed by number.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a caveat in a trailing section -> a number quoted elsewhere | A caveat separated from its number does not travel with it |
| a re-minted requirement -> a silently dropped one | An ID rewritten without a recorded mapping is indistinguishable from one quietly retired |
| a sealed verdict -> a phase record that reinterprets it | A record that softens a prior FAIL changes history without measuring anything |
| a whole-file rewrite of REQUIREMENTS.md -> unrelated requirement families | A `Write` over a shared spec destroys rows outside the intended diff |

ASVS L1, block on `high`. No network, auth, untrusted-input or secret surface. All threats are
data-integrity and record-integrity, not adversarial.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-36 | Tampering | a caveat detached from the number it qualifies | high | mitigate | The findings structure places each caveat inside the section carrying its numbers; the closing checkpoint asks the developer to verify placement, not merely presence |
| T-3-40 | Tampering | a requirement silently dropped in the re-mint | high | mitigate | An acceptance criterion asserts every one of the 13 IDs appears at least twice; the dated re-mint section records 13 mapping rows and states that none was retired; carried as a prohibition |
| T-3-41 | Tampering | a sealed verdict softened in the phase record | high | mitigate | The findings state explicitly that no sealed verdict is reopened, softened, recomputed or reinterpreted; the closing checkpoint re-reads it; carried as a prohibition |
| T-3-42 | Tampering | a whole-file rewrite of `REQUIREMENTS.md` | high | mitigate | The action mandates scoped `Edit` and forbids `Write`; a `git diff --stat` acceptance criterion asserts changes are confined to the DEC / CURV block, the amendment blockquote, the 13 Traceability rows and the new section |
| T-3-43 | Repudiation | the two notebooks confused for one another | medium | mitigate | The presentation notebook's first markdown cell names the sanity notebook and states the distinction; an acceptance criterion asserts the sanity notebook is unmodified by this plan |
| T-3-07 | Repudiation | undeclared `ripser` / `persim` dependency | medium | accept | Inherited, not resolved — `pyproject.toml` is frozen by CLAUDE.md this milestone. Recorded as a named carried limitation in `03-FINDINGS.md` rather than left implicit |
| T-3-SC | Tampering | package supply chain | low | accept | No package installed this phase; `03-RESEARCH.md` Package Legitimacy Audit records zero new names and zero `[ASSUMED]` / `[SUS]` / `[SLOP]` verdicts |
</threat_model>

## Artifacts this phase produces

Symbols Phase 3 creates (none exist before this phase; exclude from drift verification).

**New files:** `notebooks/pu_manifold/synthetic_controls.py`,
`notebooks/pu_manifold/tests/test_synthetic_controls.py`,
`notebooks/diagnostics/swiss_roll_curvature_sweep_run.py`,
`notebooks/diagnostics/curvature_field_pu_run.py`,
`notebooks/diagnostics/synthetic_control_run.py`,
`notebooks/03_swiss_roll_chart_curvature_field_check.ipynb`,
`notebooks/03_pu_curvature_field.ipynb`,
`.planning/phases/03-decoder-curvature-field/03-FINDINGS.md`.

**New sections in existing documents:** `.planning/REQUIREMENTS.md`
`## Phase 3 Requirement Re-Mint (2026-08-13)`.

**New functions / constants:** `chart_curvature._jacobian_hessian`,
`chart_curvature.CURVATURE_MODES`, `derivative_bridge._assert_decode_batch_float64`,
`derivative_bridge._chunked_vmap_hessian`, `synthetic_controls.CURVATURE_CONVENTION`,
`synthetic_controls.rotate_and_pad`, `synthetic_controls.make_flat_control`,
`synthetic_controls.make_sphere_control`, `synthetic_controls.make_saddle_control`.

**New keyword parameters / return keys:** `chart_mean_curvature(..., mode="reverse")` and its `"mode"`
provenance key, `chart_curvature_field(..., mode="reverse")`,
`derivative_bridge._agreement_stats(..., rel_floor=...)` and its `near_zero_reference_fraction` key.

**New runner constants:** `ROLL_FLOOR`, `RAW_BASELINE_CONTEXT`, `N_CHARTS_SWEEP`, `TORCH_SEEDS`,
`FIXTURE_SEED`, `PU_CHART_DIM`, `PU_EMBED_DIM`, `PU_HIDDEN_WIDTH`, `PU_ACTIVATION`,
`PU_N_CHARTS_SWEEP`, `PU_SEEDS`, `PU_SPLIT_SEED`, `PU_HOLDOUT_FRACTION`, `PU_PH_PRIMARY_CELLS`,
`SUBSAMPLE_STEM`, `RECORD_STEM`, `COND_FLAG_PERCENTILE`, `BRIDGE_N_POINTS_PU`, `CONTROL_FIXTURES`.

**New CLI flags:** `swiss_roll_curvature_sweep_run.py` `--smoke --dry-run --resume --record-path
--max-combos --n-charts --seeds --summary`; `curvature_field_pu_run.py` `--smoke --dry-run --resume
--record-path --max-combos --timing-probe --pu-n --select-only --field --bridge`;
`synthetic_control_run.py` `--smoke --dry-run --resume --record-path --fixture`.

**New test names:** `test_chart_curvature_forward_mode_matches_reverse_to_float64_round_off`,
`test_chart_curvature_reverse_mode_is_bit_identical_to_sealed_baseline`,
`test_chart_curvature_rejects_unknown_mode`, `test_chart_curvature_forward_mode_calls_c2_guard`,
`test_chart_curvature_forward_mode_keeps_shape_assertions`,
`test_derivative_bridge_float32_model_raises_friendly_value_error`,
`test_derivative_bridge_reports_near_zero_reference_fraction`,
`test_calibrate_fd_step_chunks_above_vmap_chunk`,
`test_synthetic_controls_convention_agrees_with_sealed_modules`,
`test_synthetic_flat_control_is_exactly_zero`, `test_synthetic_sphere_control_matches_d_over_R`,
`test_synthetic_controls_convention_is_trace_not_averaged`,
`test_synthetic_controls_construct_at_pu_scale`,
`test_synthetic_saddle_control_matches_finite_difference`,
`test_synthetic_saddle_control_field_genuinely_varies`,
`test_synthetic_saddle_control_constructs_at_pu_scale`.

<verification>
- `03-FINDINGS.md` exists with the override, the scope ruling, the dimension justification and the caveats in place.
- All 13 requirement IDs are re-minted, present in both their requirement line and the Traceability table, with a dated mapping section and no surviving `Isomap` text.
- `git diff --stat .planning/REQUIREMENTS.md` shows a scoped change only.
- Both notebooks parse, are committed executed, and are correctly distinguished.
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/` exits 0.
- The closing checkpoint is answered and recorded.
</verification>

<success_criteria>
1. The gate override, its consequence, and the D-05 scope ruling are all stated in Phase 3's own artifacts, in Phase 3's own words.
2. `chart_dim = 20` is justified and `d_frozen = 5` is recorded as rejected with its reason.
3. Every reported statistic carries its seed spread, and each caveat sits beside the number it qualifies.
4. All 13 stale requirements are re-minted with a recorded old-to-new mapping and none silently dropped.
5. Whether the phase reached Step 4 or stopped at D-05a, the record reads as a complete outcome.
</success_criteria>

<output>
Create `.planning/phases/03-decoder-curvature-field/03-11-SUMMARY.md` when done.
</output>
