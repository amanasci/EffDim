---
phase: 03-decoder-curvature-field
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - notebooks/diagnostics/swiss_roll_curvature_sweep_run.py
autonomous: false
requirements: [DEC-05, CURV-03, CURV-04]

must_haves:
  truths:
    - "The Step-1 statistic reported is the MEDIAN rho_chart over 5 torch seeds per n_charts, with the full per-seed spread printed alongside -- never a single draw (D-01)"
    - "The full sweep table is printed with one row per (n_charts, seed) pair, so the best-of-N is visible rather than hidden (D-04)"
    - "The floor median rho_chart > 0.65 is applied to the BEST swept config, and the multiple-comparisons loosening is named as a caveat in the printed read-out (D-04)"
    - "Charts actually used is printed beside configured n_charts for every row, because D-05's monotone mechanism is stated in charts used"
    - "The raw-point 0.6712 baseline is printed as context and labelled as gating nothing (D-02)"
    - "If no swept config clears the floor, the read-out prints the D-05a stop-and-report branch as a complete outcome, not an error"
    - "The Hutchinson randomized-trace estimator does not appear in the gate path; only the exact g-trace path may gate (D-03)"
    - statement: "The sweep table shows the measured monotone-in-charts-used direction reproducing 02.5-09 (fewer charts used -> higher rho) rather than seed noise, or the read-out says plainly that it does not"
      verification: backstop
  artifacts:
    - path: "notebooks/diagnostics/swiss_roll_curvature_sweep_run.py"
      provides: "Sweep summary, median-over-seeds statistic, floor application, D-05a branch"
      contains: "multiple-comparisons"
  key_links:
    - from: "notebooks/diagnostics/swiss_roll_curvature_sweep_run.py"
      to: "notebooks/.cache/03_swiss_roll_curvature_sweep.jsonl"
      via: "append-only JSONL record read back by the summary step"
      pattern: "03_swiss_roll_curvature_sweep"
  prohibitions:
    - statement: "MUST NOT move, soften, reinterpret or replace the 0.65 floor after any rho_chart value has been observed, and MUST NOT promote the 0.6712 raw-point centroid baseline into a gate it was explicitly demoted from (D-02)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT report a single seed, a best seed, or a mean over seeds as the Step-1 result -- the statistic is the median over 5 seeds with the full spread shown (D-01)"
      status: resolved
      verification: judgment
---

<objective>
Run the ratified Swiss roll sweep and decide the Step-1 gate.

Four `n_charts` values x five torch seeds = 20 fresh CAE fits on the roll, each measured through the
chain plan 03-01 proved. The read-out reports the median `rho_chart` per config with the full spread,
applies the 0.65 floor to the best config, and names the multiple-comparisons loosening. If nothing
clears, the phase stops and reports — that branch is planned here as a terminal outcome, not handled
as an error.

Purpose: answer whether this code can recover a known answer at all, before any PU compute is spent.
Output: the sweep table, the gate decision, and (on a clear) an unblocked Step 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/03-decoder-curvature-field/03-CONTEXT.md
@.planning/phases/03-decoder-curvature-field/03-VALIDATION.md
@.planning/phases/03-decoder-curvature-field/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep summary, median-of-5-seeds statistic, floor application, D-05a branch</name>
  <files>notebooks/diagnostics/swiss_roll_curvature_sweep_run.py</files>
  <read_first>
    - `notebooks/diagnostics/swiss_roll_curvature_sweep_run.py` — the runner as plan 03-01 left it, including `ROLL_FLOOR`, `RAW_BASELINE_CONTEXT`, the per-cell record schema and the JSONL reader
    - `.planning/phases/03-decoder-curvature-field/03-01-SUMMARY.md` — the ratified floor, statistic and swept set
    - `.planning/phases/03-decoder-curvature-field/03-CONTEXT.md` D-01, D-02, D-03, D-04, D-05, D-05a, D-15
    - `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-09-SUMMARY.md` section 3 — the warning against reading `0.6712` as validated
  </read_first>
  <action>
Add a `--summary` mode (and make it the default trailing step of a full run) that reads the JSONL
record back and prints, in this order:

1. **The full sweep table**, one row per `(n_charts_configured, seed)` pair — 20 rows — with columns
   `n_charts_configured`, `n_charts_used`, `seed`, `rho_chart`, `median_cosine_similarity`,
   `median_magnitude_ratio`, `magnitude_ratio_cv`, `calibration_slope`, `calibration_r2`,
   `cond_median`, `cond_max`, `cae_mse_per_dim`, `plain_mse_per_dim`, `curv_wall_s`. The four fidelity
   axes stay separate columns; do not form any composite score. `n_charts_used` sits immediately
   beside `n_charts_configured` because D-05's monotone finding is stated in charts actually used, and
   a config that collapses to fewer live charts is a different measurement from the one requested.

2. **The per-config summary**, one row per `n_charts` value: `median rho_chart` over the five seeds,
   the min and max (the full spread), the per-seed values listed inline, and the median
   `n_charts_used`. The median is the statistic (D-01). Do not print a mean, and do not print a
   best-seed row as if it were the result.

3. **The context line**: the raw-point centroid baseline Spearman, printed with an explicit label
   stating it is context and gates nothing, plus the one-sentence caveat from
   `02.5-09-SUMMARY.md` section 3 — it missed that notebook's own `>0.90` bar, so it is a baseline
   that works, not one validated as correct.

4. **The floor decision** (D-02, D-04): compare the best config's median against `ROLL_FLOOR`.
   Print `STEP-1 GATE: CLEARS` with the winning `n_charts`, its median and its spread, or
   `STEP-1 GATE: DOES NOT CLEAR` with the best median observed. Immediately below the verdict line,
   unconditionally print the multiple-comparisons caveat naming the exact number of configs swept —
   N configs give N shots at a fixed bar, and the floor was applied to the best of them.

5. **The D-05a branch text**, printed only when the gate does not clear: a short block stating that
   Phase 3 stops and reports, that this is a complete outcome rather than a failure to work around,
   and naming the two follow-on actions (re-point plan 03-11's `depends_on` to
   `["03-02", "03-03", "03-04"]` and write a Step-1-only `03-FINDINGS.md`).

Do not add a verdict JSON artifact, a threshold-table file, a pre-registration document, a
ratification commit or a git-ancestry proof script — D-15 declares the floor written into plan 03-01
sufficient, and this printed read-out plus the plan SUMMARY is the whole of the record.

Do not call `chart_curvature.randomized_trace_mean_curvature_nongating` or
`randomized_trace_convergence_check` from any gate path: D-03 keeps the Hutchinson estimator
non-gating, and the exact `g`-trace path is the only thing that may decide this.
  </action>
  <verify>
    <automated>.venv/bin/python notebooks/diagnostics/swiss_roll_curvature_sweep_run.py --dry-run && .venv/bin/python notebooks/diagnostics/swiss_roll_curvature_sweep_run.py --smoke && .venv/bin/python notebooks/diagnostics/swiss_roll_curvature_sweep_run.py --summary</automated>
  </verify>
  <acceptance_criteria>
    - `--summary` exits 0 against the partial record left by plan 03-01 (one cell) without raising, and reports how many of the 20 planned cells are present.
    - `grep -v '^#' notebooks/diagnostics/swiss_roll_curvature_sweep_run.py | grep -ci 'multiple-comparisons'` returns at least 1.
    - `grep -v '^#' notebooks/diagnostics/swiss_roll_curvature_sweep_run.py | grep -c 'median'` returns at least 3.
    - `grep -v '^#' notebooks/diagnostics/swiss_roll_curvature_sweep_run.py | grep -c 'D-05a'` returns at least 1.
    - The summary code path computes the statistic with a median function over the per-seed list, and the source contains no call to `statistics.mean`, `np.mean` or `.mean()` inside the per-config summary function.
    - No file matching `*verdict*.json` or `*THRESHOLD*` is created anywhere under `.planning/phases/03-decoder-curvature-field/` or `notebooks/`.
  </acceptance_criteria>
  <done>`--summary` prints the full 20-row table, the per-config median-and-spread rows, the labelled context baseline, the floor decision with its unconditional multiple-comparisons caveat, and the D-05a branch on a non-clear.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the full 4 x 5 Swiss roll sweep</name>
  <files>notebooks/diagnostics/swiss_roll_curvature_sweep_run.py</files>
  <read_first>
    - `notebooks/diagnostics/swiss_roll_curvature_sweep_run.py` — the runner including its `--resume` and `--max-combos` handling
    - `.planning/phases/03-decoder-curvature-field/03-01-SUMMARY.md` — the reproduction cell already recorded at `n_charts=8, seed=0`
  </read_first>
  <precondition>`notebooks/.cache/03_swiss_roll_curvature_sweep.jsonl` exists and already holds plan 03-01's `n_charts=8, seed=0` reproduction cell; if it is absent the sweep would silently re-measure the anchor under different conditions, so halt and re-run plan 03-01's Task 3 first.</precondition>
  <action>
Execute the remaining 19 cells with `--resume`, in `--max-combos` batches sized so that no single
invocation exceeds the executor's command timeout. Each roll cell is roughly 60-90 s of CPU
(3,000 points, `d=2`, `D=3`, `max_epochs=300` with early stopping at patience 25), so batches of
four cells per invocation are safe; the append-only JSONL plus `--resume` makes the batching
numerically inert.

Do not change `FIXTURE_SEED`, `BASE_CFG`, `HIDDEN`, `EMBED_DIM`, `CHART_DIM`, `N_POINTS` or
`K_BASELINE` between batches — a mid-sweep constant change silently makes two halves of the table
incomparable, which is exactly the defect class `02.6-FINDINGS-02.md` section 12 records twice.

Record per-cell wall clock. If any single cell exceeds 300 s, stop the sweep and report the timing
rather than silently extending the run.

After the last cell lands, run `--summary` and capture its complete output into the plan SUMMARY
verbatim — the printed table IS the artifact (D-15).
  </action>
  <verify>
    <automated>.venv/bin/python notebooks/diagnostics/swiss_roll_curvature_sweep_run.py --summary</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/.cache/03_swiss_roll_curvature_sweep.jsonl` contains exactly 20 records, one per `(n_charts, seed)` pair in `{2,3,5,8} x {0,1,2,3,4}`, with no duplicate key.
    - `--summary` prints 20 table rows and 4 per-config summary rows.
    - The `n_charts=8, seed=0` record's `rho_chart` is byte-identical to the value plan 03-01 recorded (the `--resume` path did not re-measure it).
    - Every record's `curvature_convention` field equals `trace`.
    - Every record's `activation` field equals `silu`.
    - The maximum recorded `curv_wall_s` across all 20 cells is printed in the SUMMARY.
  </acceptance_criteria>
  <done>All 20 cells are recorded, the summary prints the full table and the four per-config median-and-spread rows, and the anchor cell is unchanged from plan 03-01.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Read the sweep table and record the Step-1 gate outcome</name>
  <read_first>
    - The `--summary` output captured in Task 2
    - `.planning/phases/03-decoder-curvature-field/03-CONTEXT.md` D-02, D-04, D-05a
    - `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-09-SUMMARY.md` — the monotone-in-charts-used direction this table either reproduces or does not
  </read_first>
  <what-built>
    A 20-cell Swiss roll sweep over `n_charts` in `{2,3,5,8}` x seeds `{0,1,2,3,4}`, each cell a fresh
    CAE fit measured by `chart_curvature.chart_curvature_field` against the roll's analytic mean
    curvature, reported as median `rho_chart` per config with the full spread, the four fidelity axes
    kept separate, and `cond(g)` alongside. The 0.65 floor is applied to the best config with the
    multiple-comparisons caveat printed.
  </what-built>
  <how-to-verify>
    1. Run `.venv/bin/python notebooks/diagnostics/swiss_roll_curvature_sweep_run.py --summary` and read the printed table.
    2. Confirm the best config's **median** (not its best seed) is the number compared against 0.65.
    3. Confirm the multiple-comparisons caveat is printed and names 4 configs.
    4. Confirm the raw-point baseline line is labelled as context that gates nothing and reads `0.6712`.
    5. Read the `n_charts_used` column against `rho_chart`: does the table reproduce `02.5-09`'s monotone direction (fewer charts actually used giving higher rho), or is it noise? Say which, plainly, in the resume signal.
    6. **If the gate does NOT clear:** approve the D-05a terminal branch. Phase 3 stops and reports. Plans 03-05 through 03-10 are not executed; plan 03-11's `depends_on` is re-pointed to `["03-02", "03-03", "03-04"]` and it writes a Step-1-only `03-FINDINGS.md` from this SUMMARY. That is a complete phase outcome.
    7. **If the gate DOES clear:** approve continuation to Step 2. Note explicitly that clearing licenses the pipeline, not the PU parameterization — the gate override's parameterization-damage consequence is unchanged by a roll pass.
  </how-to-verify>
  <acceptance_criteria>
    - The developer's response states CLEARS or DOES NOT CLEAR, the winning `n_charts`, and its median `rho_chart` and spread.
    - The response states whether the monotone-in-charts-used direction reproduced.
    - The plan SUMMARY records the gate outcome, the full printed table, and — on a non-clear — the explicit re-pointing instruction for plan 03-11.
    - The SUMMARY does not contain any floor value other than `0.65`.
  </acceptance_criteria>
  <resume-signal>Reply `clears &lt;n_charts&gt; &lt;median&gt;` or `does-not-clear &lt;best median&gt;`, followed by one sentence on whether the monotone direction reproduced.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| append-only JSONL record -> summary statistic | A partial, duplicated or mid-run-reconfigured record produces a table that reads as complete |
| declared bar -> measured result | A bar restated after the result is visible is not the same bar |
| exact `g`-trace path -> gate | A non-gating estimator leaking into the gate path changes what was measured |

ASVS L1, block on `high`. No network, auth, untrusted-input, secret or persistence surface beyond
gitignored `notebooks/.cache/`. All threats are data-integrity, not adversarial.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-08 | Tampering | the Step-1 bar (research record integrity) | high | mitigate | `ROLL_FLOOR = 0.65` was committed in plan 03-01 before any record existed; the acceptance criteria assert the SUMMARY contains no other floor value; prohibition P-1 carries it forward |
| T-3-10 | Tampering | mid-sweep constant change splitting the table | high | mitigate | Task 2 forbids changing `FIXTURE_SEED`, `BASE_CFG`, `HIDDEN`, `EMBED_DIM`, `CHART_DIM`, `N_POINTS`, `K_BASELINE` between batches; the anchor cell's byte-identity across `--resume` is an acceptance criterion |
| T-3-11 | Tampering | non-gating Hutchinson estimator leaking into the gate | medium | mitigate | D-03; the summary path calls only `spearman_gate_statistic` on the exact `g`-trace field, and the randomized-trace functions are not invoked |
| T-3-12 | Repudiation | a partial JSONL read as a complete sweep | medium | mitigate | `--summary` reports present-vs-planned cell counts; the acceptance criterion pins exactly 20 records with no duplicate key |
| T-3-SC | Tampering | package supply chain | low | accept | No package installed this phase; `03-RESEARCH.md` Package Legitimacy Audit records zero new names and zero `[ASSUMED]` / `[SUS]` / `[SLOP]` verdicts |
</threat_model>

## Artifacts this phase produces

Symbols Phase 3 creates (none exist before this phase; exclude from drift verification).

**New files:** `notebooks/pu_manifold/synthetic_controls.py`,
`notebooks/pu_manifold/tests/test_synthetic_controls.py`,
`notebooks/diagnostics/swiss_roll_curvature_sweep_run.py`,
`notebooks/diagnostics/curvature_field_pu_run.py`,
`notebooks/diagnostics/synthetic_control_run.py`,
`notebooks/03_swiss_roll_chart_curvature_field_check.ipynb`,
`notebooks/03_pu_curvature_field.ipynb`,
`.planning/phases/03-decoder-curvature-field/03-FINDINGS.md`.

**New functions / constants:** `chart_curvature._jacobian_hessian`,
`chart_curvature.CURVATURE_MODES`, `derivative_bridge._assert_decode_batch_float64`,
`derivative_bridge._chunked_vmap_hessian`, `synthetic_controls.CURVATURE_CONVENTION`,
`synthetic_controls.rotate_and_pad`, `synthetic_controls.make_flat_control`,
`synthetic_controls.make_sphere_control`, `synthetic_controls.make_saddle_control`.

**New keyword parameters / return keys:** `chart_mean_curvature(..., mode="reverse")`,
`chart_curvature_field(..., mode="reverse")`, `derivative_bridge._agreement_stats(..., rel_floor=...)`
and its `near_zero_reference_fraction` key.

**New runner constants:** `ROLL_FLOOR`, `RAW_BASELINE_CONTEXT`, `N_CHARTS_SWEEP`, `TORCH_SEEDS`,
`FIXTURE_SEED`, `PU_N_CHARTS_SWEEP`, `PU_SEEDS`, `PU_CHART_DIM`, `PU_PH_PRIMARY_CELLS`,
`COND_FLAG_PERCENTILE`, `BRIDGE_N_POINTS_PU`, `CONTROL_FIXTURES`.

**New CLI flags:** `swiss_roll_curvature_sweep_run.py` `--smoke --dry-run --resume --record-path
--max-combos --n-charts --seeds --summary`; `curvature_field_pu_run.py` `--smoke --dry-run --resume
--record-path --max-combos --timing-probe --pu-n --select-only`; `synthetic_control_run.py`
`--smoke --dry-run --resume --record-path --fixture`.

**New test names:** `test_chart_curvature_forward_mode_matches_reverse_to_float64_round_off`,
`test_chart_curvature_reverse_mode_is_bit_identical_to_sealed_baseline`,
`test_chart_curvature_rejects_unknown_mode`, `test_chart_curvature_forward_mode_calls_c2_guard`,
`test_chart_curvature_forward_mode_keeps_shape_assertions`,
`test_derivative_bridge_float32_model_raises_friendly_value_error`,
`test_derivative_bridge_reports_near_zero_reference_fraction`,
`test_calibrate_fd_step_chunks_above_vmap_chunk`, `test_synthetic_flat_control_is_exactly_zero`,
`test_synthetic_sphere_control_matches_d_over_R`,
`test_synthetic_saddle_control_matches_finite_difference`,
`test_synthetic_controls_convention_is_trace_not_averaged`.

<verification>
- 20 records in `notebooks/.cache/03_swiss_roll_curvature_sweep.jsonl`, no duplicates.
- `--summary` exits 0 and prints table, per-config medians, context baseline, floor decision, caveat.
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/` exits 0.
- The blocking checkpoint is answered and the outcome recorded verbatim.
</verification>

<success_criteria>
1. The Step-1 gate outcome is decided on a median over 5 seeds per config, with the full spread visible.
2. The full sweep table is printed and captured in the SUMMARY.
3. The multiple-comparisons caveat is printed unconditionally alongside the verdict.
4. On a non-clear, the D-05a stop-and-report branch is approved and recorded as a complete outcome with concrete follow-on instructions.
5. No verdict JSON, threshold table, pre-registration document or ratification commit is produced.
</success_criteria>

<output>
Create `.planning/phases/03-decoder-curvature-field/03-02-SUMMARY.md` when done.
</output>
