---
phase: 02.6-decoder-substrate-screening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md
autonomous: false
requirements: [SC-4, SC-5]
# SC-1..SC-5 are Phase 02.6's own ROADMAP-stated success criteria, NOT milestone REQUIREMENTS.md
# REQ-IDs. No milestone-level REQ-IDs exist for this phase; the ROADMAP entry instructs coverage be
# derived from its five success criteria. Recorded as a stated choice, not an omission.

must_haves:
  truths:
    - "A screening rule document exists at .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md and is committed BEFORE any four-seed measurement is taken by plan 02.6-05"
    - "The rule states, for each of the four read-out axes, the exact bar a candidate must clear, and whether that bar is an absolute constant or a comparison against a reference arm recomputed in the same run"
    - "The rule states the seed-spread disqualifying condition, the seed set, and what SPREAD is computed over"
    - "The rule states the promotion procedure's five steps and the at-most-one cap, including what a zero-candidates-clear outcome is (a complete, reportable result, not a stall)"
    - statement: "Every bar in the rule was chosen without any Phase 02.6 measurement in hand; the only numbers cited are ones already on the record from Phases 02.2, 02.4 and 02.5"
      verification: backstop
    - statement: "The user selected each open option at the checkpoint rather than the plan silently locking a RESEARCH.md proposal"
      verification: backstop
  artifacts:
    - .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md
  key_links:
    - "02.6-SCREENING-RULE.md -> 02.6-FINDINGS.md (plan 02.6-06): FINDINGS applies this rule and cites it; the rule is never restated with different numbers"
    - "02.6-SCREENING-RULE.md -> 02.6-RESEARCH.md Sections 'Seed Protocol' and 'Promotion Rule': the rule ratifies, amends or declines each proposal explicitly, naming which"
  prohibitions:
    - statement: "The rule document is not a pre-registration, carries no git-ancestry proof, and produces no verdict artifact; this phase screens and does not gate"
      status: open
      verification: "manual read: the document contains no ancestry SHA, no PASS/FAIL string, and states in its own opening line that it gates nothing"
    - statement: "No numeric bar is chosen with a Phase 02.6 measurement in hand; the rule is committed before plan 02.6-05 runs"
      status: open
      verification: "git log ordering: the SCREENING-RULE commit precedes every commit from plan 02.6-05"
    - statement: "The four read-outs are never combined into a single composite score by this rule"
      status: open
      verification: "manual read: the rule states four separate bars and no weighting, sum, product or ranking function over them"
    - statement: "No sealed verdict (GATE_VERDICT, CAE_VERDICT, TOPOAE_VERDICT, CURVATURE_VERDICT) is reopened, softened or recomputed by this document"
      status: open
      verification: "manual read: the document cites the CAE's measured Stage-A numbers as a negative control only, and changes no sealed value"
    - statement: "No source file is modified by this plan; its only file is a planning document"
      status: open
      verification: "git diff --name-only for this plan's commit lists exactly one path, under .planning/"
---

<objective>
Decide, and write down, the exact bars Phase 02.6 will screen candidates against — before any
Phase 02.6 number is measured. Produces
`.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md`.

Purpose: 02.6-RESEARCH.md's § Seed Protocol disqualifying condition, its numeric floor, and its
§ Promotion Rule steps 2(b)/2(c) are all explicitly flagged as **proposals pending ratification** —
no CONTEXT.md exists for this phase, so there is no locked decision to defer to. Choosing a bar
after seeing the numbers is a door that cannot be closed again: you cannot un-see a measurement.
This plan is the only point in the phase where these bars can be chosen blind, so it runs in wave 1
and its output is committed before plan 02.6-05 takes a single reading.
Output: one short planning document, ratified at a blocking checkpoint.
</objective>

<why_this_is_a_checkpoint_and_not_an_assumption>
Per this phase's `<no_context_handling>` contract, an open decision is adopted as a stated assumption
**only when its value does not change what gets built**. Everything in this plan fails that test:

- The rank-axis floor determines whether the plain-AE and TopoAE candidates are recorded as clearing
  Stage A, which determines whether Tier-2+ implementation work (Witness AE / RTD-AE / GRAE — each
  phase-sized, two of the three carrying real licensing and provenance costs) is scheduled at all.
- The promotion bars determine which single candidate, if any, a follow-on gate phase is built around.

02.6-RESEARCH.md line 416 says plainly that `curvature_probe.py`'s `SPEARMAN_ABSOLUTE_FLOOR = 0.50`
"is available for reuse if the planner judges the stage-1 calibration transfers ... but that is
itself a decision worth stating explicitly rather than inheriting silently." Inheriting it silently
is exactly what this plan refuses to do.

**Correction the executor must carry:** `SPEARMAN_ABSOLUTE_FLOOR` is **not an importable module
constant**. 02.6-PATTERNS.md confirms by direct grep that no module-level constant of that name
exists in `curvature_probe.py`; the value `0.50` appears only inside an f-string/log message at
`curvature_probe.py:1457`. If option A1 below is chosen, `0.50` must be written into the rule
document as a literal with that provenance stated. Never write a task that imports it.
</why_this_is_a_checkpoint_and_not_an_assumption>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.6-decoder-substrate-screening/02.6-RESEARCH.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-NOTE-substrate-selection.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-09-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by **this plan**:
- `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md`

Created by the rest of Phase 02.6 (listed so drift checks see the whole set and do not flag these as
unresolved references):
- `notebooks/pu_manifold/decoder_curvature.py` — `CURVATURE_CONVENTION`, `assert_c2_decoder`,
  `plain_decoder_map`, `plain_decoder_curvature`, `swiss_roll_analytic_H_vector` (plan 02.6-01)
- `notebooks/pu_manifold/tests/test_decoder_curvature.py` (plan 02.6-01)
- `notebooks/02.6_swiss_roll_plainae_curvature_check.ipynb` (plan 02.6-03)
- `notebooks/02.6_swiss_roll_topoae_curvature_check.ipynb` (plan 02.6-04)
- `notebooks/diagnostics/decoder_substrate_screen_run.py` — `FIXTURE_SEED`, `N_POINTS`,
  `LATENT_DIM`, `HIDDEN`, `K_BASELINE`, `CFG_COMMON`, `LAMBDA_TOPO`, `build_fixture`,
  `train_candidate`, `screen_seed`, `markdown_table`, `main` (plan 02.6-05)
- `.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS.md` (plan 02.6-06)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Ratify the screening bars, blind — before any Phase 02.6 measurement</name>

  <decision>
Five open decisions, all flagged by 02.6-RESEARCH.md as proposals rather than locked rules, because
no CONTEXT.md exists for this phase. Each must be settled before plan 02.6-05 measures anything.
  </decision>

  <context>
Phase 02.6 screens decoder substrates against a Swiss roll admission read-out with four axes
reported separately (SC-3): direction (median cosine to analytic `H`), magnitude (median
`||H_est||/||H_true||` **and** per-point CV), calibration (least-squares slope/intercept/R² of
`||H_est||` on `||H_true||`), and rank (Spearman). At most one candidate may be promoted (SC-5),
and four seeds minimum with spread reported are mandatory (SC-4).

Numbers already on the record, all measured before this phase and all available to inform a blind
choice (none of them is a Phase 02.6 measurement):

- CAE chart decoder, Swiss roll, four torch seeds (`02.5-09-SUMMARY.md`): Spearman
  `-0.0604 / -0.1444 / 0.8665 / 0.4250`, spread `1.0109`; at seed 0 median cosine `0.9706`, median
  magnitude ratio `0.3356`, CV `0.7888`, calibration slope `-0.1856` at R² `0.0533`. **Disqualified
  as measured; retained as this phase's negative control.**
- Raw-point centroid estimator at `k=30, d=2` on the identical fixture, seed-independent: Spearman
  `0.6712`, median cosine `0.9996`, median magnitude ratio `0.8934`, CV `0.2925`, calibration slope
  `0.7061` at R² `0.3828`. **Caveat that must travel with this number:** 02.5-09 recorded that this
  baseline itself cleared only 3 of its own notebook's 4 sanity lines — it missed a `> 0.90` bar. It
  is a reference that *works*, not one that is perfect.
- Effect sizes this phase must argue against: single-chart versus multi-chart is ~3.4x on H0
  retention; the topological-loss axis is worth ~6% (TopoAE `0.668` vs plain AE `0.628`).
- `curvature_probe.py:1457` cites `SPEARMAN_ABSOLUTE_FLOOR = 0.50` inside a log string. It is **not**
  an importable constant — confirmed by direct grep. It was derived from a stage-1 noise-oracle
  ceiling calibration on the same fixture family under the same trace convention.
  </context>

  <options>
    <option id="A1">
      <name>Rank axis: absolute floor 0.50, transferred from stage 1</name>
      <pros>An externally-derived floor (noise-oracle ceiling calibration), same fixture family, same trace convention; consistent with this milestone's own precedent of absolute floors derived from a null rather than from taste.</pros>
      <cons>It is a stage-1 point-cloud calibration being applied to a decoder arm that has a different noise structure; it must be hardcoded as a literal because no importable constant exists.</cons>
    </option>
    <option id="A2">
      <name>Rank axis: absolute floor 0.6712, the raw-point arm's measured value</name>
      <pros>Concrete, measured on the identical fixture, and strictly stricter than A1.</pros>
      <cons>Freezes a number that is itself imperfect (it missed its own notebook's 0.90 bar) into an absolute bar, when the raw-point arm is cheap to recompute in every run.</cons>
    </option>
    <option id="A3">
      <name>Rank axis: purely comparative — the candidate must beat the raw-point arm recomputed in the same run, with no absolute floor (RESEARCH § Promotion Rule step 2(c) as written)</name>
      <pros>Invents no constant at all; the comparator is recomputed from the same fixture in the same process every time; matches RESEARCH's own stated step 2(c) and 02.5-NOTE § 3's comparative posture.</pros>
      <cons>A candidate could clear it while both arms are poor; requires the baseline's own imperfection to be restated wherever the bar is invoked.</cons>
    </option>
    <option id="A4">
      <name>Rank axis: A3 AND A1 together — beat the raw-point arm in the same run, and clear 0.50 absolutely</name>
      <pros>Neither a lucky comparison against a weak comparator nor an absolute floor divorced from the run can carry a candidate alone.</pros>
      <cons>Two bars to satisfy; a candidate landing between them produces a split result that must be reported as such rather than resolved.</cons>
    </option>
    <option id="B1">
      <name>Calibration axis: comparative only — |slope − 1| no worse than the raw-point arm's |slope − 1| in the same run</name>
      <pros>No invented constant; automatically tracks the comparator.</pros>
      <cons>Says nothing absolute about whether magnitude carries information.</cons>
    </option>
    <option id="B2">
      <name>Calibration axis: absolute — slope ≥ 0.5 and R² ≥ 0.25</name>
      <pros>Directly encodes RESEARCH § Promotion Rule 2(b)'s "slope not close to zero", calibrated against the CAE's own disqualifying −0.1856 at R² 0.0533; catches the exact failure mode SC-3 exists for (a decoder that compresses every magnitude by a constant scores a perfect rank).</pros>
      <cons>0.5 and 0.25 are chosen, not derived from a null.</cons>
    </option>
    <option id="B3">
      <name>Calibration axis: B1 and B2 together</name>
      <pros>Both an absolute floor on information content and a comparison to a working reference.</pros>
      <cons>Same split-result risk as A4.</cons>
    </option>
    <option id="C1">
      <name>Seed-spread disqualifier: RESEARCH § Seed Protocol as proposed — disqualify if ANY one of the four seeds falls below the chosen floor on ANY axis, OR if SPREAD > (mean_rho − floor)</name>
      <pros>Directly paraphrases 02.5-NOTE § 3's own rule ("a substrate whose across-seed spread exceeds its margin to threshold has not passed, it has drawn well"), and is calibrated against a measured negative example (the CAE's spread of 1.0109).</pros>
      <cons>The single-seed clause is strict: one unlucky draw disqualifies a candidate whose other three seeds are clean.</cons>
    </option>
    <option id="C2">
      <name>Seed-spread disqualifier: SPREAD rule only; single-seed excursions reported, not disqualifying</name>
      <pros>Less brittle; the full per-seed table still makes every excursion visible.</pros>
      <cons>Weakens the milestone's own repeated finding that a single lucky or unlucky seed is not a result.</cons>
    </option>
    <option id="C3">
      <name>No disqualifier at all — report the spread and let the promotion argument in FINDINGS weigh it</name>
      <pros>Maximally consistent with "this phase screens, it does not gate".</pros>
      <cons>Leaves the hardest judgement to prose written after the numbers are in.</cons>
    </option>
    <option id="D1">
      <name>Seeds: torch/split seeds 0, 1, 2, 3 — the CAE negative control's own set</name>
      <pros>SC-4's stated minimum; directly comparable, seed for seed, against 02.5-09's already-published four-seed table.</pros>
      <cons>Four points is a thin sample for a spread statistic.</cons>
    </option>
    <option id="D2">
      <name>Seeds: 0, 1, 2, 3, 4 — five, matching curvature_probe.SEED_REPLICATE_N</name>
      <pros>Five is the only n at which `curvature_probe.seed_replicate_lower_bound`'s `t_multiplier = 2.132` is valid, so a confidence-bounded number becomes available if wanted later.</pros>
      <cons>~25% more compute; breaks seed-for-seed comparability with the CAE control's four-seed table; RESEARCH recommends the plain table over the collapsed bound anyway.</cons>
    </option>
    <option id="E1">
      <name>Promotion procedure: ratify RESEARCH § Promotion Rule steps 1-5 as written</name>
      <pros>A checkable five-step procedure a reader can verify was followed; step 3 makes "zero candidates clear" a complete, reportable outcome consistent with four prior FAILs in this milestone; step 5's tie-break prefers a candidate needing no new implementation and no new PU training.</pros>
      <cons>Its step 5 ordering is argued, not measured.</cons>
    </option>
    <option id="E2">
      <name>Promotion procedure: ratify steps 1-5 but strike step 5's tie-break, requiring any multi-clear outcome to return to the user</name>
      <pros>Removes the one step in the procedure that is a preference rather than a measurement.</pros>
      <cons>Adds a checkpoint round-trip in an outcome that may not occur.</cons>
    </option>
  </options>

  <recommendation>
**A3 + B3 + C1 + D1 + E1.**

A3 for rank because it invents no constant and its comparator is recomputed in every run — and
because it is RESEARCH's own stated step 2(c). B3 for calibration because the CAE's failure was
invisible to rank and visible only in calibration (slope `-0.1856` at R² `0.0533`), so this is the
axis where an absolute floor genuinely earns its keep, paired with the comparative check so the bar
cannot be cleared by a candidate that is merely less bad than a weak comparator. C1 because
02.5-NOTE § 3 already wrote this rule down and the CAE's measured spread of `1.0109` is a concrete
calibrating example. D1 because SC-4 says four and seed-for-seed comparability against the published
negative-control table is worth more than a confidence bound RESEARCH does not recommend using. E1
because a multi-clear outcome is unlikely and step 5's ordering (no new implementation, no new PU
training) is argued from this project's own recorded costs rather than from taste.

Direction and magnitude axes are not offered as options: no source document proposes an absolute
bar for either, so both are recorded as **comparative against the raw-point arm in the same run**
(median cosine no worse; `|median ratio − 1|` no worse; CV no worse), reported separately and never
combined. Say so if you disagree.
  </recommendation>

  <resume-signal>
Reply with one option id per group, e.g. `A3, B3, C1, D1, E1` — or `approve recommendation` to take
all five as recommended. Amendments in prose are fine; name the group being amended.
  </resume-signal>

  <reversibility rating="one-way">
Choosing a bar *blind* is a one-way door: after plan 02.6-05 runs, no bar can ever again be chosen
without a measurement in hand, and the ordering property that makes a screening result trustworthy
cannot be restored by any later edit. The bars themselves are editable text; the ordering is not.
  </reversibility>
</task>

<task type="auto">
  <name>Task 2: Write and commit 02.6-SCREENING-RULE.md</name>

  <files>.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md</files>

  <read_first>
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-RESEARCH.md` § "Seed Protocol"
      (lines 406-416) and § "Promotion Rule" (lines 418-428) — the proposals being ratified, amended
      or declined. Every step must be dispositioned explicitly, naming which of the three it received.
    - `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-NOTE-substrate-selection.md`
      Sections 3 and 4 — the Stage A / Stage B structure and the three inferences that must not be
      drawn from a Swiss roll result.
    - `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-09-SUMMARY.md`
      § "Seed Sensitivity" — the exact per-seed table format the rule requires FINDINGS to reproduce.
    - `.planning/ROADMAP.md` lines 359-392 — the Phase 02.6 entry, for the five success criteria and
      the two effect sizes the promotion argument must be made against.
  </read_first>

  <action>
Write `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md`, at most ~120 lines,
with these sections and nothing more:

1. **Opening line, verbatim in substance:** this document gates nothing, seals nothing, and produces
   no verdict artifact. It records the bars Phase 02.6 screens against, chosen before any Phase 02.6
   number was measured. It is not a pre-registration and carries no ancestry proof, because this
   phase screens rather than gates.
2. **The four axes and their bars**, one subsection each — direction, magnitude, calibration, rank —
   in that order, each stating its bar exactly as ratified at Task 1's checkpoint and whether the
   bar is absolute or comparative-against-the-raw-point-arm-in-the-same-run. State in a single
   sentence, under the rank subsection, that the raw-point comparator itself cleared only 3 of its
   own notebook's 4 sanity lines (it missed a `> 0.90` bar) — the caveat travels with the number
   every time it is invoked. Add an explicit sentence stating that the four bars are evaluated and
   reported **separately** and are never weighted, summed, multiplied or ranked into one score.
3. **The seed protocol**: the ratified seed set; that `FIXTURE_SEED = 20260807` and `N_POINTS = 3000`
   are held fixed while only the torch/split seed varies (02.5-09's own protocol); the required
   per-seed table columns, reproduced from RESEARCH § Seed Protocol and extended with median cosine
   so all four axes appear — `torch seed | reconstruction rel. err | median cosine | rho | median
   magnitude ratio | magnitude CV | calibration slope | calibration R² | max metric condition
   number`; that `SPREAD = max − min` over the four seeds' Spearman rho is reported alongside mean
   and median; and the ratified disqualifying condition.
4. **The promotion procedure**, as ratified, as a numbered five-step list a reader can check was
   followed, including the at-most-one cap and the statement that zero candidates clearing is a
   complete, reportable outcome for this phase rather than a stall.
5. **The two effect sizes any promotion argument must be made against**: ~3.4x on H0 retention for
   single-chart versus multi-chart, and ~6% for the topological-loss axis (TopoAE `0.668` vs plain AE
   `0.628`). State that a candidate justified only by a stronger topological loss is arguing inside
   the 6%.
6. **The three inferences that must not be drawn**, transcribed from
   `02.5-NOTE-substrate-selection.md` Section 4 — read them from the note, do not paraphrase from
   memory: Swiss roll topology does not transfer; reconstruction quality validates nothing about
   curvature; H0 preservation is not curvature fidelity.
7. **Disposition of RESEARCH.md's proposals**: a short table with one row per proposal — the seed
   spread statistic, the two disqualifying conditions, the numeric floor, and Promotion Rule steps
   2(b) and 2(c) — each marked `ratified`, `amended` (with what changed) or `declined` (with why).
8. **Provenance note**: if the ratified rank bar uses the literal `0.50`, state that it appears only
   inside an f-string at `curvature_probe.py:1457` and is **not** an importable module constant, so
   it is written here as a literal with that provenance rather than imported.

Commit this file alone, so its ordering relative to plan 02.6-05's measurement commits is legible in
`git log`. Message: `docs(02.6-02): screening rule ratified before any 02.6 measurement`.
  </action>

  <verify>
    <automated>RULE=.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md; test -f "$RULE" && grep -qi "gates nothing" "$RULE" && grep -qi "SPREAD" "$RULE" && grep -qi "20260807" "$RULE" && echo RULE_OK</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md` exists and the
      verify command prints `RULE_OK`.
    - The file contains all four axis names — `direction`, `magnitude`, `calibration`, `rank` — as
      section headings, and contains no heading combining them (no "composite", "overall score",
      "total score" or weighting expression anywhere in the file).
    - `grep -ci "spread" <rule>` returns at least `2`, and the file contains the literal
      `max` and `min` in the sentence defining SPREAD.
    - The file contains the literal `0.6712` (the raw-point comparator) and the literal `1.0109`
      (the CAE control's measured spread), so both calibrating references are on the page.
    - The file contains the literals `3.4` and `6%`, the two effect sizes any promotion argument must
      be made against.
    - The file contains all three of the Section-4 must-not-infer statements, each recognisable by a
      distinctive phrase transcribed from `02.5-NOTE-substrate-selection.md` rather than paraphrased.
    - The file contains a table with one row per RESEARCH.md proposal, each row ending in exactly one
      of `ratified`, `amended` or `declined`.
    - `git show --stat HEAD --name-only` lists exactly one path, and it is the rule document.
    - `git log --oneline` shows the rule commit and no commit from plan 02.6-05 before it.
  </acceptance_criteria>

  <reversibility rating="reversible">
    A planning document. Its text is editable; the property that makes it valuable — that it was
    written before any Phase 02.6 measurement — is fixed by its position in `git log` and is what
    Task 1's `one-way` rating covers.
  </reversibility>

  <done>
    `02.6-SCREENING-RULE.md` exists, states four separate bars and no composite, states the seed set
    and the spread statistic and the disqualifying condition, reproduces the required per-seed table
    columns, lists the promotion procedure with its at-most-one cap, records the three inferences
    that must not be drawn, and dispositions every RESEARCH.md proposal as ratified, amended or
    declined. Committed alone, before any measurement.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan writes one markdown document under `.planning/`. No code runs, no network is touched, no input is parsed, no artifact under `notebooks/` or `src/` is read or written. |
| record integrity: rule -> measurement ordering | The only boundary with real consequences. A rule edited after a measurement is indistinguishable, in the document alone, from one written before it; `git log` ordering is the enforcing mechanism. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.6-06 | Repudiation | `02.6-SCREENING-RULE.md` vs plan 02.6-05's measurements | high | mitigate | The rule is committed alone, in wave 1, before plan 02.6-05 exists in the wave order; the acceptance criteria assert `git log` ordering directly, and plan 02.6-06's FINDINGS cites the rule rather than restating its numbers. |
| T-02.6-07 | Tampering | sealed verdicts cited as context | medium | mitigate | The document cites `02.5-09-SUMMARY.md`'s measured numbers as a negative control and changes no sealed value; `GATE_VERDICT`, `CAE_VERDICT`, `TOPOAE_VERDICT` and `CURVATURE_VERDICT` are not written, recomputed or softened anywhere in this plan. |
| T-02.6-08 | Tampering | supply chain | high | accept | No package is installed and no dependency file is touched by this plan; there is no install surface to mitigate. Recorded rather than omitted because Tier-2+ work would introduce one, and plan 02.6-06 carries the blocking checkpoint that would gate it. |
| T-02.6-09 | Information disclosure | n/a | low | accept | No secret, credential, network call or user data anywhere in this plan. |
</threat_model>

<verification>
- The blocking `checkpoint:decision` in Task 1 was resolved by the user, and the resolution is
  recorded verbatim in the SUMMARY.
- `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md` exists and satisfies
  every Task 2 acceptance criterion.
- `git show --stat HEAD` lists exactly one file.
- No file under `notebooks/`, `src/effdim/` or `pyproject.toml` appears in this plan's diff.
</verification>

<success_criteria>
- Every open bar named in 02.6-RESEARCH.md's § Seed Protocol and § Promotion Rule is either ratified,
  amended or declined by an explicit user decision — none silently inherited.
- The rule states four separate bars and no composite score.
- The rule is committed before any Phase 02.6 measurement is taken.
</success_criteria>

<output>
Create `.planning/phases/02.6-decoder-substrate-screening/02.6-02-SUMMARY.md` when done.
Record the user's exact option selections (one per group A-E), any amendment they made in prose, the
commit SHA of the rule document, and — for plan 02.6-06 to cite — the final wording of each of the
four axis bars and the disqualifying condition.
</output>
