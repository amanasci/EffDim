---
phase: 02.6-decoder-substrate-screening
plan: 07
subsystem: research-planning
tags: [persistent-homology, ripser, persim, ratification, pre-registration, swiss-roll]

requires:
  - phase: 02.6-decoder-substrate-screening (plans 01-06, halted)
    provides: "02.6-FINDINGS.md's halt record and the identified confound that motivated the axis change to persistent-homology agreement"
  - phase: 02.5-local-curvature-feasibility-cae-re-gate
    provides: "02.5-NOTE-substrate-selection.md's Stage A/Stage B split and the three inferences that must not be drawn"
provides:
  - "02.6-SCREENING-RULE-02.md: the ratified, non-gating persistent-homology agreement criterion (D-01)"
  - "The 16-cell read-out matrix's canonical cell labels (D-03..D-06), committed before any candidate diagram exists"
  - "Five open design questions (thin-denominator handling, pre-scaling, CAE latent definition, bridge comparison scope, separator eval set) individually dispositioned: P1, Q1, R1, S1, T1"
  - "Both travelling caveats (thin (H1,intrinsic) denominator; (H1,ambient) bottleneck saturation) in canonical wording, with planning-time measured values (0.3348, 0.7198, 0.35988, 11.2886)"
affects: ["02.6-09 (PH probe module)", "02.6-11 (screening runner)", "02.6-12 (plainAE/TopoAE PH notebooks)", "02.6-13 (CAE PH notebook)", "02.6-14 (derivative bridge runner)", "02.6-15 (phase record)"]

tech-stack:
  added: []
  patterns: ["pre-registration before measurement (ancestry-provable via git log)", "travelling-caveat convention (state once, cite everywhere)", "non-gating ratification (ranks, does not promote)"]

key-files:
  created:
    - .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md
  modified: []

key-decisions:
  - "P1: thin (H1,intrinsic) denominator reported as raw + denominator + normalized, no floor applied — no invented constant enters the record"
  - "Q1: symmetric per-cloud pre-scaling via topoae.latent_unit_scale applied to every cloud (model and reference alike), scalar printed"
  - "R1: CAE's encoder latent is the global embed_dim embedding via model.encode(x), not chart-local coordinates"
  - "S1: derivative bridge compares both the full d x d Hessian tensor and the reduced H_vec/H_norm, reported separately"
  - "T1: separator evaluated on both fixture points and a dense held-out grid uniform in t, reported separately"
  - "SPREAD > mean - floor disqualifier dropped a second time (first at 02.6-02 as C1/C2, now by D-11) — non-gating criterion has nothing to disqualify"

patterns-established:
  - "Ratification-before-measurement: acceptance criteria assert commit ancestry (git log over persistence_probe.py returns empty at the ratification commit) rather than merely asserting the ordering in prose"

requirements-completed: [SC-1]

coverage:
  - id: D1
    description: "02.6-SCREENING-RULE-02.md ratifies the PH-agreement criterion, its 16-cell read-out matrix, and its seed protocol, committed before any PH number exists in the repo"
    requirement: "SC-1"
    verification:
      - kind: other
        ref: "git log --oneline -1 -- .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md (returns b768ee4); git log --oneline -- notebooks/pu_manifold/persistence_probe.py (empty)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Document declares itself non-gating in its own opening paragraph (D-09, D-10) and drops the SPREAD disqualifier (D-11)"
    requirement: "SC-1"
    verification:
      - kind: other
        ref: "grep -q non-gating 02.6-SCREENING-RULE-02.md; grep -c SPREAD (3 occurrences, disqualifying condition stated as NONE)"
        status: pass
    human_judgment: false
  - id: D3
    description: "All five open design questions (P/Q/R/S/T) individually dispositioned per the user's checkpoint selection (P1,Q1,R1,S1,T1), all 9 declined options recorded"
    requirement: "SC-1"
    verification:
      - kind: other
        ref: "grep -cE '^\\| *(P[123]|Q[123]|R[123]|S[123]|T[12]) *\\|' 02.6-SCREENING-RULE-02.md (returns 14)"
        status: pass
    human_judgment: false

duration: ~20min
completed: 2026-08-10
status: complete
---

# Phase 02.6 Plan 07: Ratify the PH-Agreement Screening Rule Summary

**Blind-ratified `02.6-SCREENING-RULE-02.md` — persistent-homology agreement criterion, 16-cell read-out matrix, and seed protocol, declared explicitly non-gating and committed before any PH number exists in the repo.**

## Performance

- **Duration:** ~20 min (checkpoint round-trip included)
- **Completed:** 2026-08-10T20:49:06-04:00 (commit `b768ee4`)
- **Tasks:** 2/2 (1 blocking `checkpoint:decision`, 1 `auto`)
- **Files modified:** 1 created

## Accomplishments
- Presented the plan's five option groups (P, Q, R, S, T) at the blocking checkpoint; the user selected the recommended option in every group (P1, Q1, R1, S1, T1).
- Wrote and committed `02.6-SCREENING-RULE-02.md`: states its own non-gating posture in its opening paragraph, defines all 16 canonical read-out cells (`{space}|{reference}|H{k}|{distance}_norm`), records provenance-column requirements, bars (absolute on Swiss roll / comparative on PU), the seed protocol with the `SPREAD > mean - floor` disqualifier recorded as dropped a second time, and both travelling caveats in canonical wording with their planning-time measured values.
- Verified — before committing — that the document's commit has no PH-measuring code as an ancestor (`git log` over `notebooks/pu_manifold/persistence_probe.py` returns empty), satisfying SC-1's ordering requirement by ancestry rather than by assertion.
- Left `02.6-SCREENING-RULE.md` (the halted axis's own ratification document) byte-unchanged, and touched no file under `notebooks/`, `src/effdim/`, or `pyproject.toml`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Ratify the PH-agreement criterion and its five open design questions** — checkpoint:decision, no code commit (decision recorded in Task 2's document)
2. **Task 2: Write and commit 02.6-SCREENING-RULE-02.md** - `b768ee4` (docs)

**Plan metadata:** committed with this SUMMARY (see final commit below)

## Files Created/Modified
- `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md` - the ratified, non-gating PH-agreement screening rule: criterion, 16-cell matrix, provenance columns, bars, seed protocol, both travelling caveats, full 14-row disposition table, and ratification stamp.

## Decisions Made
- **P1** (thin `(H1,intrinsic)` denominator): report raw + denominator + normalized, no floor. Declined P2 (invented floor constant) and P3 (raw-only, breaks matrix uniformity).
- **Q1** (pre-scaling): symmetric per-cloud `topoae.latent_unit_scale`, scalar printed. Declined Q2 (no pre-scaling, scale-covariant comparison) and Q3 (asymmetric, model-only pre-scaling).
- **R1** (CAE latent): global `embed_dim` embedding via `model.encode(x)`. Declined R2 (chart-local, not globally comparable) and R3 (both, breaks 16-cell uniformity).
- **S1** (bridge scope): both full Hessian tensor and reduced `H_vec`/`H_norm`, reported separately. Declined S2 (reduced only, can hide tensor disagreement) and S3 (tensor only, says nothing about the downstream threshold quantity).
- **T1** (separator eval): both fixture points and a dense held-out grid uniform in `t`. Declined T2 (fixture-only, leaves overfitting unanswered).
- All five decisions ratified together at the blocking `checkpoint:decision`; D-01 and D-09 are rated one-way in `02.6-CONTEXT.md`, so this is the last point at which the ranking axis or its non-gating posture can change.

## Deviations from Plan

None — plan executed exactly as written. Both planning-time measured values cited in the plan's own `<context>` block (`0.3348`/`0.7198`/`0.35988`/`11.2886`) were transcribed into the document verbatim; `02.6-RESEARCH.md` Pitfall 2's `0.420` was recorded explicitly as not pinned, per the plan's instruction, without being presented as a constant.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness

`02.6-SCREENING-RULE-02.md` is on disk and committed, giving plans `02.6-09` (PH probe module), `02.6-11` (screening runner), `02.6-12`/`02.6-13` (candidate PH notebooks), and `02.6-14` (derivative bridge runner) a single, already-ratified source for cell definitions, the seed set, the scaling convention, and both travelling caveats — none of them need to re-derive or re-decide these. No blockers. The document's ancestry (`git log` shows no `persistence_probe.py` commit preceding it) satisfies SC-1 for any downstream audit.

---
*Phase: 02.6-decoder-substrate-screening*
*Completed: 2026-08-10*
