---
phase: 02.6-decoder-substrate-screening
plan: 03
status: superseded-not-executed
subsystem: research-notebook
tags: [superseded, halted-run, curvature-axis, plain-autoencoder, swiss-roll]

# Dependency graph
requires:
  - phase: 02.6-01
    provides: "decoder_curvature.py -- available and used by the partial notebook before the halt"
provides: []
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns: []
---

# Plan 02.6-03 — SUPERSEDED, NOT EXECUTED

**This plan did not run to completion and produced no accepted result. Nothing in this file
records work done or measurements obtained.** It exists so the phase's plan ledger is honest
about why plan `03` has no execution record, rather than leaving it indefinitely pending.

## Disposition

`02.6-03` planned a CLAUDE.md Swiss roll **curvature** admission notebook for the plain-AE
decoder, scoring agreement between decoder-pullback mean curvature and the Swiss roll's
analytic `H`. Phase 02.6 was **halted mid-execution on 2026-08-10** and replanned onto a new
ranking axis — **persistent-homology agreement** — because the curvature-agreement score was
found to be a composite of three separable properties (did the architecture learn the right
surface, are that trained net's second derivatives trustworthy, is pullback the right
approximator). Measurements on that axis cannot be attributed to substrate choice. Full record:
`02.6-FINDINGS.md`, §4 in particular.

The plan is therefore **superseded, not deferred**. It is not resumed, and its remaining work
is not carried into any later plan.

## Partial state left on disk, and its verification status

`notebooks/02.6_swiss_roll_plainae_curvature_check.ipynb` **exists and was executed with
committed outputs** (`bec17ef` wrote it, `028188f` executed it). Its blocking
`checkpoint:human-verify` gate **never closed**, so:

- The notebook's soundness was never accepted by a human reviewer.
- Its numbers were never admitted as evidence about the plain AE or any other substrate.
- It is retained as an execution artifact of the halted run, **not** as a result.

Do not cite its output. Plan `02.6-12` builds the replacement,
`notebooks/02.6_swiss_roll_plainae_ph_check.ipynb`, on the PH axis, and explicitly notes that
this notebook's gate remains open.

## Replaced by

`02.6-12-PLAN.md` — CLAUDE.md Swiss roll **PH-agreement** notebooks for the plain-AE and
TopoAE-trained decoders (SC-6). The curvature quantity survives in the phase only through
`02.6-08`'s separating experiment, in a strictly non-ranking role.
