---
phase: 02.6
slug: decoder-substrate-screening
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-10
---

# Phase 02.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `02.6-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `pytest` (no project-level version pin; invoked as `.venv/bin/python -m pytest`, matching every prior phase's recorded command shape) |
| **Config file** | none — no `pytest.ini` and no `[tool.pytest]` section; discovery relies on the default `test_*.py` convention across `tests/` and `notebooks/pu_manifold/tests/` |
| **Quick run command** | `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_decoder_curvature.py -x` (or the analogous file for whichever module the task touches) |
| **Full suite command** | `.venv/bin/python -m pytest tests/ notebooks/pu_manifold/tests/` |
| **Estimated runtime** | ~60–120 seconds full suite (baseline **286 passed** = 120 root + 166 `pu_manifold`; a new module must grow that count, never shrink it) |

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/python -m pytest notebooks/pu_manifold/tests/<module under change>.py -x`
- **After every plan wave:** Run `.venv/bin/python -m pytest tests/ notebooks/pu_manifold/tests/`
- **Before `/gsd-verify-work`:** Full suite must be green at ≥286 passed
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

> Task IDs are filled by `/gsd-validate-phase` once PLAN.md files exist. Rows below are seeded from
> RESEARCH.md § Validation Architecture's success-criterion map — the criteria are stable, the task IDs are not.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| {TBD} | {TBD} | 0 | SC-2/SC-3 (new `decoder_curvature` module) | — | `assert_c2_activation` refuses a non-C2 decoder before differentiating it | unit | `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_decoder_curvature.py` | ❌ W0 | ⬜ pending |
| {TBD} | {TBD} | 1 | SC-3 (four-axis metric) | — | N/A | unit | `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_curvature_probe.py -k fidelity` | ✅ | ⬜ pending |
| {TBD} | {TBD} | 1 | SC-2 (plain AE admission notebook) | — | N/A | manual | none — CLAUDE.md bars caching/verdict-JSON/threshold machinery inside a sanity notebook | ✅ by design | ⬜ pending |
| {TBD} | {TBD} | 1 | SC-2 (TopoAE admission notebook) | — | N/A | manual | none — same CLAUDE.md constraint | ✅ by design | ⬜ pending |
| {TBD} | {TBD} | 2 | SC-4 (four-seed spread reported) | — | N/A | manual + source assertion | per-seed table present in the phase FINDINGS doc, 4 rows minimum | ❌ W0 | ⬜ pending |
| {TBD} | {TBD} | 2 | SC-1/SC-5 (audit protocol, promotion rule) | — | N/A | manual | document review against RESEARCH.md § Faithful-to-Paper Audit Protocol / § Promotion Rule | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `notebooks/pu_manifold/decoder_curvature.py` — new module (Pattern 1: `chart_curvature.py` minus the `chart_decoders[idx]` routing); does not exist
- [ ] `notebooks/pu_manifold/tests/test_decoder_curvature.py` — new test file; minimum coverage is a sphere known-answer test (`H = d/R`, the form `test_curvature_probe.py` already uses) and a flat-plane test (`H_norm == 0.0` exactly), both differentiated through a trivial linear / `PlainAutoEncoder`-shaped decoder
- [ ] `notebooks/02.6_swiss_roll_plainae_curvature_check.ipynb` — new CLAUDE.md-mandated sanity notebook
- [ ] `notebooks/02.6_swiss_roll_topoae_curvature_check.ipynb` — new CLAUDE.md-mandated sanity notebook

*No framework install needed — `pytest` and every runtime dependency are already present. `pyproject.toml` is frozen this milestone.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Swiss roll admission notebook trains from scratch, runs end to end under two minutes, imports model code unchanged | SC-2 | CLAUDE.md explicitly bars caching, verdict JSON, threshold tables, and pre-registration machinery inside a sanity notebook — there is nothing left to assert against programmatically by design | Run the notebook top to bottom on a clean kernel; read the three-or-four printed pass/fail lines and the one-sentence read-out; confirm the x-z scatter shows the spiral with `t` colour bands in order |
| Four read-outs reported separately, never collapsed to a single score | SC-3 | The failure this guards against is a *presentational* collapse (a summary that averages direction/magnitude/calibration/rank into one number), which no assertion can catch | Read the notebook output and the phase FINDINGS doc: four labelled numbers per seed, no composite score anywhere |
| Per-seed spread reported, not averaged away | SC-4 | Same character — the defect is an omitted table, not a wrong value | Confirm a 4-row-minimum per-seed table exists with `SPREAD = max − min` alongside mean/median |
| At most one candidate promoted, argued against the recorded effect sizes (3.4× chart axis, ~6% topological-loss axis) | SC-5 | A judgement about whether an argument was made, not whether a number cleared a bar | Read the promotion argument against RESEARCH.md § Promotion Rule steps 1–5; confirm each step is visibly followed |
| Faithful-to-paper deviation audit, if any new candidate is implemented | SC-1 | Requires diffing an implementation against a paper and a reference repo | Follow RESEARCH.md § Faithful-to-Paper Audit Protocol items 1–5; each deviation named with a measured or argued cost |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`decoder_curvature.py` + its test file)
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
