---
phase: 02.6
slug: decoder-substrate-screening
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-10
reseeded: 2026-08-10
---

# Phase 02.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `02.6-RESEARCH.md` § Validation Architecture.
>
> **Reseeded 2026-08-10 after the mid-execution halt.** The prior version was keyed to the
> abandoned decoder-pullback-curvature ranking axis: its Wave 0 list named modules that plan
> `02.6-01` has since built, and its per-task map asserted `assert_c2_activation` guards a
> plain decoder — which is false (`PlainAutoEncoder` sets no `self.activation`; plan `01`
> introduced `assert_c2_decoder`). Test Infrastructure and Sampling Rate below are measured
> repo facts and carry over unchanged; everything axis-keyed is reset for
> `/gsd-validate-phase` to refill against the new PLAN.md files.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `pytest` (no project-level version pin; invoked as `.venv/bin/python -m pytest`, matching every prior phase's recorded command shape) |
| **Config file** | none — no `pytest.ini` and no `[tool.pytest]` section; discovery relies on the default `test_*.py` convention across `tests/` and `notebooks/pu_manifold/tests/` |
| **Quick run command** | `.venv/bin/python -m pytest notebooks/pu_manifold/tests/<module under change>.py -x` |
| **Full suite command** | `.venv/bin/python -m pytest tests/ notebooks/pu_manifold/tests/` |
| **Estimated runtime** | ~60–120 seconds full suite (baseline **286 passed** = 120 root + 166 `pu_manifold`, plus whatever plan `02.6-01` added; a new module must grow that count, never shrink it) |

---

## Sampling Rate

- **After every task commit:** Run `.venv/bin/python -m pytest notebooks/pu_manifold/tests/<module under change>.py -x`
- **After every plan wave:** Run `.venv/bin/python -m pytest tests/ notebooks/pu_manifold/tests/`
- **Before `/gsd-verify-work`:** Full suite must be green, at or above the pre-phase baseline count
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

> Task IDs are filled by `/gsd-validate-phase` once the replanned PLAN.md files exist.
> Rows are seeded from `02.6-RESEARCH.md` § Validation Architecture against the phase's five
> success criteria (SC-1..SC-5); the criteria are stable, the task IDs are not.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| {TBD} | {TBD} | {TBD} | SC-{N} | — | {expected secure behavior or "N/A"} | unit | `{command}` | ✅ / ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

> Reset. To be filled by `/gsd-validate-phase` from the replanned PLAN.md files against the
> persistent-homology agreement axis. Note that `notebooks/pu_manifold/decoder_curvature.py`
> and its test file — Wave 0 items in the prior version — **already exist**, delivered by the
> executed plan `02.6-01`; they are not Wave 0 work for the replan.

- [ ] `{module}` — {why missing}
- [ ] `{tests/test_module.py}` — stubs for SC-{N}

*No framework install needed — `pytest` and every runtime dependency are already present. `pyproject.toml` is frozen this milestone (`ripser`/`persim` are venv-installed and undeclared — a known, recorded reproducibility gap).*

---

## Manual-Only Verifications

> Reset except for the entries below, which are properties of the phase's reporting contract
> and are axis-independent — they guard against a *presentational* collapse that no assertion
> can catch.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Swiss roll admission notebook trains from scratch, runs end to end under two minutes, imports model code unchanged | SC-2 | CLAUDE.md explicitly bars caching, verdict JSON, threshold tables, and pre-registration machinery inside a sanity notebook — there is nothing left to assert against programmatically by design | Run the notebook top to bottom on a clean kernel; read the three-or-four printed pass/fail lines and the one-sentence read-out; confirm the x-z scatter shows the spiral with `t` colour bands in order |
| Read-outs reported separately, never collapsed to a single score | SC-3 | The failure guarded against is presentational (a summary that averages the separate read-outs into one number), which no assertion can catch | Read the notebook output and the phase FINDINGS doc: each labelled read-out present per seed, no composite score anywhere |
| Per-seed spread reported, not averaged away | SC-4 | Same character — the defect is an omitted table, not a wrong value | Confirm a 4-row-minimum per-seed table exists with `SPREAD = max − min` alongside mean/median |
| At most one candidate promoted, argued against the recorded effect sizes (3.4× chart axis, ~6% topological-loss axis) | SC-5 | A judgement about whether an argument was made, not whether a number cleared a bar | Read the promotion argument against `02.6-RESEARCH.md` § Promotion Rule; confirm each step is visibly followed |
| Faithful-to-paper deviation audit, if any new candidate is implemented | SC-1 | Requires diffing an implementation against a paper and a reference repo | Follow `02.6-RESEARCH.md` § Faithful-to-Paper Audit Protocol; each deviation named with a measured or argued cost |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
