---
phase: 02.6-decoder-substrate-screening
plan: 15
type: execute
wave: 5
depends_on: [02.6-08, 02.6-11, 02.6-12, 02.6-13, 02.6-14]
files_modified:
  - .planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [SC-7]
user_setup: []

must_haves:
  truths:
    - "A findings document exists that carries the full read-out matrix, the ranking it supports, the derivative-usability table and the separating experiment's result — and promotes no substrate (D-10, SC-7)."
    - "`02.6-FINDINGS.md`, the halt record, is byte-unchanged: the replan's findings live in a new document beside it, so the halt record is not overwritten."
    - "A substrate topping the persistent-homology ranking but failing the bridge is recorded as NOT promoted, and the ranking is explicitly NOT walked to the runner-up (D-19)."
    - "Every number in hand at the point `02.5-10` inherits this phase is disclosed in one place, per `02.5-NOTE-substrate-selection.md` §6's ordering-transparency obligation."
    - "The three inferences that must not be drawn, and assumptions A-01 and A-02, are restated so a later reader cannot take the ranking for a curvature claim."
    - "ROADMAP.md and STATE.md record the replan's outcome, the unblocking of `02.5-10`, and the fact that the phase's own criterion was ratified non-gating and therefore constrains no published conclusion."
  artifacts:
    - .planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md
  key_links:
    - "`02.5-10` reads this one document for everything it inherits; anything not disclosed here is effectively hidden from the decision that matters."
    - "The commit ancestry between `02.6-SCREENING-RULE-02.md` and the first measurement commit is the phase's only evidence that the criterion preceded the numbers — the findings document cites it by hash."
---

<objective>
Write the replan's findings and hand `02.5-10` a ranking, a derivative-usability table and the
separating experiment's result — and promote nothing (SC-7; D-10, D-19).

`02.6-FINDINGS.md` §4 records what this phase is correcting: the halted axis's criterion was changed
after an unfavourable result, and the record cannot distinguish "we learned something" from "we kept
going until a candidate won". D-09 answers that by claiming less — the criterion ranks and is ratified
explicitly non-gating — and D-10 moves the promotion decision wholly to `02.5-10`, under its own
seal-before-measure discipline. This document is where that reduction in claim is made concrete: it
reports everything and decides nothing.

`02.5-NOTE-substrate-selection.md` §6 sets the obligation this document discharges: every number
already in hand at `02.5-10`'s ratification must be disclosed there, and by now that includes
02.5-09's four-seed table, everything the quick tasks measured, the halted run's own tables, and this
replan's 192-number matrix and bridge table.

Purpose: Phase 02.5 stage 2 has been blocked since 2026-08-10 with no substrate decision to inherit.
This unblocks it without making that decision for it.
Output: `.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md`, plus ROADMAP.md and
STATE.md updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02.6-decoder-substrate-screening/02.6-CONTEXT.md
@.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-NOTE-substrate-selection.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 02.6-FINDINGS-02.md — the full matrix, the ranking, the bridge, and no promotion</name>

  <files>.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md</files>

  <read_first>
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS.md` — read in full. This is the halt record and the shape this document mirrors: frontmatter carrying the phase disposition, a bolded opening paragraph stating plainly what happened, numbered sections for what was built, what was measured, what must not be inferred, and a plan-disposition table. **Do not edit it.** Its §4 (the confound and the after-measurement criterion change), §5 (what the fixture pin does not establish) and §7 (what must not be inferred) are cited by this document rather than restated in new words.
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md` — the ratified criterion, the 16 canonical cell labels, both travelling caveats in canonical wording, and the disposition table.
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-08-SUMMARY.md` — the separator's floor row, its trained-net rows at the fixture points and the held-out grid, its matched plain-AE baseline row, and the D-15 branch recorded at its human-verify gate.
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-11-SUMMARY.md` — the full 48-row measurement table and 48-row spread table, verbatim.
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-12-SUMMARY.md` and `02.6-13-SUMMARY.md` — the three notebooks' single-seed read-outs and the judgements recorded at their human-verify gates.
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-14-SUMMARY.md` — both bridge tables, the calibration ladders, and whether D-21's amendment was exercised.
    - `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-NOTE-substrate-selection.md` — §2, §4, §5, §6.
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-05-SUMMARY.md` and `02.6-01-SUMMARY.md` — the halted run's own measured tables, which this document discloses as prior numbers in hand and does not carry forward as evidence about any substrate.
  </read_first>

  <action>
Create `.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md`. Additive:
`02.6-FINDINGS.md` is not edited, moved or deleted — it is the halt record, and overwriting it would
destroy the very thing this phase's discipline depends on. Neither are plans `02.6-01` through
`02.6-06` or their SUMMARYs.

Frontmatter: the phase, `status: complete`, the completion date, `promoted_candidate: none`,
`ranking_axis: persistent-homology agreement`, `gating: none`, and a `supersedes` field that is
explicitly empty with a one-line note that `02.6-FINDINGS.md` is retained as the halt record.

Sections, in this order:

1. **Opening paragraph, bolded, plain.** This phase measured and ranked three in-repo decoder
   substrates on persistent-homology agreement and **promoted none**. State that the criterion was
   ratified before any measurement and ratified explicitly non-gating, so it ranks and constrains no
   published conclusion by design; that `02.5-10` inherits a ranking, a derivative-usability table
   and the separating experiment's result, and makes the promotion decision itself; and that
   `02.6-FINDINGS.md` remains the halt record for the abandoned axis and is untouched.

2. **What the replan was for, and how it differs from the halted run.** Cite `02.6-FINDINGS.md` §4
   for the three-way composite and the after-measurement criterion change. State that nothing
   measured under the old axis is carried forward as evidence about any substrate, and that the old
   axis's instruments are retained as instruments — `decoder_curvature.py` is reused unchanged by
   this replan's own separator.

3. **The ordering proof.** Give the commit hash of `02.6-SCREENING-RULE-02.md` and the commit hash of
   the first commit that recorded a persistent-homology number, and state that the first is an
   ancestor of the second. This is the whole of SC-1's claim and it is checkable rather than
   asserted. Also state plainly what that proof does and does not buy: the criterion preceded the
   numbers, and because it is ratified non-gating it still constrains no conclusion — that reduction
   is deliberate, per D-09.

4. **What was built.** A table of artifacts, one row each for the ratification document, the three
   new modules and their tests, the two new diagnostics runners, and the four new notebooks, with
   the plan that produced each and its status.

5. **The separating experiment (SC-2).** The floor row, the trained-net rows at the fixture points and
   at the held-out grid, and the matched plain-AE baseline row, transcribed from `02.6-08-SUMMARY.md`.
   State which of D-15's two branches the numbers select and the consequence for `02.5-10` that
   branch carries, in the wording D-15 fixes: a negative result means `02.5-10` inherits "Arm B
   through a trained decoder is not viable" and must redesign stage 2 around it; a positive result
   means the halted phase's low scores were about architectures failing to learn the sheet rather
   than about net derivatives in general. Record that this addresses the general question — can any
   net do this — and not the per-substrate one.

6. **The full read-out matrix (SC-3, SC-4).** Transcribe `02.6-11-SUMMARY.md`'s two tables in full:
   48 measurement rows and 48 spread rows, every number, no rounding, no omission, no candidate or
   seed dropped. Name every saturated cell and every thin-denominator cell, citing the two travelling
   caveats by their canonical wording from `02.6-SCREENING-RULE-02.md` rather than restating them.
   Add the three notebooks' single-seed read-outs beside them, with the judgements recorded at their
   human-verify gates.

7. **The ranking, explicitly non-gating.** State the ordering the matrix supports, cell by cell,
   and — more importantly — state where the cells disagree. Per D-05's own stated reason for keeping
   both distances, and per RESEARCH Pitfall 3, a ranking that is unanimous across all sixteen cells
   is a warning sign rather than a clean result, since real substrate differences are more likely to
   show some cells favouring one candidate and others disagreeing. If the observed seed spread swamps
   the between-candidate differences, report that as a finding about the criterion, per A-03, rather
   than absorbing it. Argue the ranking against the two effect sizes this phase must argue against
   rather than around: single-chart versus multi-chart at about 3.4x on H0 retention, and the
   topological-loss axis at about 6 percent — a difference living inside that 6 percent is arguing
   inside it.

8. **The derivative-usability bridge (SC-5).** Both regimes' tables from `02.6-14-SUMMARY.md`,
   reported separately and never merged, with the calibrated steps and both agreement levels. State
   assumption A-01 in CONTEXT.md's own terms: this establishes that the derivative computation is
   stable, not that the surface is right, and a net that learned a smooth but wrong surface passes it
   cleanly. State whether D-21's amendment was exercised. Apply no threshold — `02.5-10` ratifies the
   bar under its own discipline (D-18).

9. **What `02.5-10` inherits, and what it does not (SC-7).** A ranking, a derivative-usability table
   and the separating experiment's result. **Not** a substrate decision. State D-19 explicitly: a
   substrate that tops the persistent-homology ranking but fails the bridge is not promoted, and the
   ranking is **not** walked to the runner-up — walking it would turn the bridge into a second
   ranking axis applied after seeing results, structurally the same move `02.6-FINDINGS.md` §4
   recorded.

10. **Full disclosure of every number in hand.** Per `02.5-NOTE-substrate-selection.md` §6's
    ordering-transparency obligation, list in one place everything measured that is available to
    `02.5-10` at its ratification: 02.5-09's four-seed curvature table; the quick tasks' H0-retention
    and persistence numbers; the halted run's own two four-seed tables from `02.6-05-SUMMARY.md`,
    labelled as measurements on an abandoned axis that are not evidence about any substrate; this
    replan's 192-number matrix; and this replan's bridge table. Anything omitted here is effectively
    hidden from the decision that matters.

11. **What must NOT be inferred.** Transcribe in substance `02.5-NOTE-substrate-selection.md` §4's
    three inferences — Swiss roll topology does not transfer; reconstruction quality validates
    nothing about curvature; H0 preservation is not curvature fidelity — and CONTEXT.md's A-01 and
    A-02. State the sharpest of them plainly: a persistent-homology-ranked winner is not thereby a
    curvature-capable winner, the H0-is-not-curvature-fidelity objection stands unretired, and Phase
    02.1 has recorded precedent for selecting on a proxy criterion that later proved suspect.

12. **Open items carried forward.** `02.6-03`'s never-closed human-verify gate, moot for ranking
    under this axis and recorded so it is not silently forgotten. The RTD-AE / Witness AE / GRAE
    scoped follow-on phase, with the note that RTD-AE became more aligned the moment persistent
    homology was chosen as the axis since it constrains the map induced on homology groups toward an
    isomorphism, and that `02.6-RESEARCH.md`'s A5 flags an unverified differentiation-scheme port
    that must be re-searched before costing. The `ripser`/`persim` declaration gap, inherited and not
    resolved here because `pyproject.toml` is frozen for this milestone. The distortion instruments,
    not computed here by D-08 and available to `02.5-10` under its own pre-registration.

13. **Plan disposition table** for plans `02.6-07` through `02.6-15`, in the shape of
    `02.6-FINDINGS.md` §6's own table.

Write measurements and their reading. Do not write a promotion, a verdict artifact, a sealed gate, or
a threshold a later phase is instructed to compare against.
  </action>

  <verify>
    <automated>test -f .planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md && git diff --stat -- .planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS.md | wc -l | grep -qx 0 && grep -c "promoted_candidate: none" .planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md` exists and its frontmatter contains `promoted_candidate: none` and `gating: none`.
    - `git diff --stat -- .planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS.md .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md` is empty, and `git status --porcelain .planning/phases/02.6-decoder-substrate-screening/02.6-0[1-6]-PLAN.md` is empty — the halt record and the halted run's plans are byte-unchanged.
    - The document contains two commit hashes and an explicit statement that the first is an ancestor of the second; `git merge-base --is-ancestor <rule_commit> <first_measurement_commit>` exits 0.
    - The document contains 48 measurement rows and 48 spread rows transcribed from `02.6-11-SUMMARY.md`; `grep -c '^| ' ` over the document's §6 returns at least `96`.
    - The document contains the literal strings `promotes no substrate`, `not walked to the runner-up`, and `H0 preservation is not curvature fidelity`.
    - The document names, for each candidate, all four seeds' values for at least the `(latent, intrinsic, H1, wasserstein)` cell — no seed is omitted.
    - The document contains a disclosure section listing at least five distinct prior measurement sources by name.
    - The document contains no sealed-verdict token and instructs no later phase to compare a number against a threshold written here; `grep -cE "GATE_VERDICT|CAE_VERDICT|TOPOAE_VERDICT|CURVATURE_VERDICT" <file>` returns a count consisting only of references to those verdicts as history, and no line writes a new one.
    - `git status --porcelain notebooks/ src/effdim/ pyproject.toml` is empty — this task touches no code.
  </acceptance_criteria>

  <done>
`02.6-FINDINGS-02.md` exists carrying the ordering proof by commit hash, the separator's result and
its D-15 branch, the full 96-row matrix, the ranking with its cell-level disagreements named, both
bridge tables reported separately, the full disclosure list, the inferences that must not be drawn,
and a plan disposition table — promoting nothing and sealing nothing, with `02.6-FINDINGS.md`
byte-unchanged beside it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the outcome in ROADMAP.md and STATE.md and hand off to 02.5-10</name>

  <files>.planning/ROADMAP.md, .planning/STATE.md</files>

  <read_first>
    - `.planning/ROADMAP.md` — the Phase 02.6 entry in full (the Goal, SC-1..SC-7, the cross-cutting constraints, the Status paragraph carrying the 2026-08-10 halt record, the Prior history and Replan history paragraphs, and the plan list with its Wave 1-3 halted-run history block). Also the Phase 02.5 entry's plan list, whose Waves 9-12 wait on this phase.
    - `.planning/STATE.md` — the Current Position section and the halt narrative it carries.
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md` — as written by Task 1.
  </read_first>

  <action>
Update `.planning/ROADMAP.md` and `.planning/STATE.md` with scoped edits only. Do not rewrite either
file wholesale, and do not delete or reword any existing halt-record text — the 2026-08-10 Status
paragraph, the Prior history paragraph, and the Wave 1-3 halted-run plan list are the historical
record and are retained verbatim.

In `.planning/ROADMAP.md`:
- Mark plans `02.6-07` through `02.6-15` complete in the "Replan — new plans" block, replacing the
  `*Pending planner output.*` placeholder with the executed plan list and its wave structure.
- **Append** a new dated Outcome paragraph after the existing Status paragraph rather than editing
  it: the replan completed on the persistent-homology agreement axis; the criterion was ratified
  before measurement and ratified non-gating; three candidates were screened across four seeds
  producing 192 separately reported numbers; the separating experiment ran and selected a named D-15
  branch; the derivative-usability bridge ran on both regimes; **no substrate was promoted and none
  eliminated**; and `02.5-10` is unblocked and inherits a ranking, a bridge table and the separator's
  result rather than a decision. Point at `02.6-FINDINGS-02.md` by name.
- Update the one-line Phase 02.6 entry in the roadmap's own phase list near the top of the file so it
  no longer reads as awaiting a replan.
- Update the Phase 02.5 entry's blocking note so it records that its stage-2 dependency on Phase 02.6
  is satisfied, and that what `02.5-10` receives is a ranking and two tables, not a substrate.

In `.planning/STATE.md`:
- Update the frontmatter `status`, `stopped_at`, `last_activity`, `last_activity_desc` and the
  completed-plan counters.
- Replace the Current Position section's "Why halted" and "Next" narrative with the replan's outcome
  and the next action — `02.5-10`'s stage-2 pre-registration — while keeping the halt narrative
  available by pointing at `02.6-FINDINGS.md` rather than by deleting the reference to it.
- Record the two planning-time source corrections this replan verified against source, so they do not
  have to be rediscovered: `chart_curvature.assert_c2_activation` cannot be called on a
  `cae.PlainAutoEncoder` and `decoder_curvature.assert_c2_decoder` is the fix (already recorded, kept);
  and `cae.ChartAutoEncoder.forward` returns no `y` key, so the CAE's decoder image is
  `model.reconstruct(x)` — a claim `02.6-RESEARCH.md` Pattern 3 and `02.6-PATTERNS.md` both state the
  opposite of.
- Record that D-21's amended fence was or was not exercised, taking the answer from
  `02.6-14-SUMMARY.md`.
  </action>

  <verify>
    <automated>grep -c "02.6-FINDINGS-02.md" .planning/ROADMAP.md .planning/STATE.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/ROADMAP.md` no longer contains the literal `*Pending planner output.*`.
    - `.planning/ROADMAP.md` contains an entry for each of `02.6-07-PLAN.md` through `02.6-15-PLAN.md` in the replan block.
    - The 2026-08-10 halt Status paragraph is retained verbatim: `git diff -- .planning/ROADMAP.md` shows the halt paragraph's lines unchanged, and the Outcome text is added rather than substituted.
    - The Wave 1-3 halted-run plan list, including the `[~]` entries for `02.6-03`, `02.6-04` and `02.6-06` and the note about the old `(SC-N)` tags, is unchanged.
    - Both files contain the literal `02.6-FINDINGS-02.md`.
    - `.planning/STATE.md` frontmatter `status` is no longer `halted`, and its Current Position section names `02.5-10` as the next action.
    - `.planning/STATE.md` contains the literal `model.reconstruct` and a sentence recording that the chart model's forward pass carries no `y` key.
    - `git status --porcelain notebooks/ src/effdim/ pyproject.toml` is empty.
  </acceptance_criteria>

  <done>
ROADMAP.md and STATE.md record the replan's outcome additively, with every halt-record paragraph and
the halted-run plan list retained verbatim; both point at `02.6-FINDINGS-02.md`; and `02.5-10` is
recorded as unblocked, inheriting a ranking and two tables rather than a substrate decision.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human reads the findings and confirms the phase promotes nothing</name>

  <what-built>
`.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md` — the replan's findings:
the ordering proof by commit hash, the separating experiment's result and its D-15 branch, the full
192-number read-out matrix with per-seed spreads, the ranking with its cell-level disagreements
named, both derivative-usability tables reported separately, the full disclosure list for `02.5-10`,
and the inferences that must not be drawn. Plus ROADMAP.md and STATE.md updated additively, with
`02.6-FINDINGS.md` and plans `02.6-01` through `02.6-06` byte-unchanged.
  </what-built>

  <how-to-verify>
1. Read `02.6-FINDINGS-02.md` top to bottom.
2. **Check §3's ordering proof yourself.** Run
   `git merge-base --is-ancestor <rule_commit> <first_measurement_commit>; echo $?` with the two
   hashes the document gives. A non-zero exit means the criterion did not precede the numbers and the
   document's central procedural claim is false.
3. **Check that the phase promotes nothing.** §9 must hand `02.5-10` a ranking, a bridge table and the
   separator's result, and must state D-19 explicitly — that a substrate topping the ranking but
   failing the bridge is not promoted, and that the ranking is not walked to the runner-up. Language
   anywhere in the document that reads as selecting a substrate is the failure to catch here.
4. **Check §7 names where the cells disagree**, not only where they agree. A ranking unanimous across
   all sixteen cells is a warning sign, and the document is required to say so rather than present it
   as a clean result.
5. **Check §6 carries every number.** Four seeds per candidate, no cell dropped, no seed averaged
   away, every saturated and thin-denominator cell named with the travelling caveat cited rather than
   paraphrased.
6. **Check §10's disclosure list is complete.** This is the ordering-transparency obligation
   `02.5-NOTE-substrate-selection.md` §6 puts on `02.5-10`; anything not listed here is effectively
   hidden from the decision that matters. Cross-check it against what you know exists.
7. **Check §11.** The three inferences that must not be drawn, and A-01 and A-02, must be present and
   must not have been softened. In particular, the document must not let the ranking read as a
   curvature claim.
8. Confirm `02.6-FINDINGS.md` and plans `02.6-01` through `02.6-06` are unchanged:
   `git status --porcelain .planning/phases/02.6-decoder-substrate-screening/` should show only new
   files and the two updated planning documents.

This phase exists because a criterion was changed after an unfavourable result and the record could
not absorb it. The document you are reading is the record. Its value is entirely in whether it claims
exactly what it can support and nothing more.
  </how-to-verify>

  <resume-signal>Type "approved" and state whether the ancestry check passed and whether the document promotes anything — or describe what overclaims.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's measurements → `02.5-10`'s ratification | The only boundary that matters here: everything `02.5-10` can see is what this document discloses, and anything omitted is hidden from the decision. |
| the replan's findings → the halt record | A findings document that overwrote `02.6-FINDINGS.md` would destroy the record of the criterion change this whole phase exists to correct. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.6R-27 | Repudiation | the halt record overwritten, or the halted run's plans edited | high | mitigate | The findings live in a new file; `git diff --stat` over `02.6-FINDINGS.md` and `02.6-SCREENING-RULE.md` and `git status --porcelain` over plans `02.6-01`..`06` returning empty are acceptance criteria; the human-verify step checks the whole phase directory. |
| T-02.6R-28 | Elevation of privilege | the phase promoting a substrate it was ratified not to promote, or the ranking being walked to a runner-up | high | mitigate | Required literals `promotes no substrate` and `not walked to the runner-up`; the human-verify resume signal asks for that judgement by name; D-19's rationale is written into §9. |
| T-02.6R-29 | Information disclosure (by omission) | a prior measurement left out of the disclosure list, so `02.5-10` ratifies without it | high | mitigate | §10 must list at least five named prior sources; the human-verify step cross-checks completeness against what the reader knows exists. |
| T-02.6R-03 | Repudiation | the ordering claim (criterion before measurement) asserted rather than shown | high | mitigate | Two commit hashes are given and the ancestry is checkable with one command, which the human-verify step runs; the document also states what the proof does not buy. |
| T-02.6R-30 | Tampering | a seed or a cell omitted from the transcribed matrix | high | mitigate | Row-count acceptance criteria on both tables, plus a per-candidate all-four-seeds requirement on a named cell. |
| T-02.6R-01 | Tampering | `src/effdim/`, `pyproject.toml`, notebooks | high | mitigate | `git status --porcelain` over those paths returning empty is an acceptance criterion on both tasks; this plan touches only planning documents. |
| T-02.6R-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan. |
| T-02.6R-99 | Spoofing / DoS | — | low | accept | No network surface, no auth surface, no user input path. |
</threat_model>

<verification>
- `02.6-FINDINGS-02.md` exists with the ordering proof, the full 96-row matrix, both bridge tables, and the disclosure list.
- `02.6-FINDINGS.md`, `02.6-SCREENING-RULE.md` and plans `02.6-01`..`06` are byte-unchanged.
- ROADMAP.md and STATE.md record the outcome additively, retaining every halt-record paragraph verbatim.
- The blocking human-verify gate is closed with the ancestry check run and the no-promotion judgement stated.
</verification>

<success_criteria>
SC-7 is satisfied: the phase promotes no substrate; `02.5-10` inherits a ranking, a
derivative-usability table and the separating experiment's result; a substrate topping the ranking
but failing the bridge is recorded as not promoted; and the ranking is not walked to the runner-up.
</success_criteria>

## Artifacts this phase produces

Every symbol and path below is **created by this phase** and does not exist before it. The
source-grounding pass should treat all of these as new, not as drift.

- `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md` (plan 07)
- `.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md` (this plan)
- `notebooks/pu_manifold/analytic_param.py` — `SWISS_ROLL_PARAM_CONVENTION`, `swiss_roll_arc_length`,
  `swiss_roll_intrinsic_plane`, `swiss_roll_exact_ambient`, `AnalyticSwissRollDecoder`,
  `AnalyticParamDecoder` (plan 08)
- `notebooks/pu_manifold/tests/test_analytic_param.py` (plan 08)
- `notebooks/pu_manifold/persistence_probe.py` — `PH_MAXDIM`, `READOUT_CELLS`, `finite_pairs`,
  `persistence_diagram`, `max_persistence`, `saturation_value`, `is_saturated`, `ph_agreement`,
  `cloud_distance_matrix`, `readout_matrix` (plan 09)
- `notebooks/pu_manifold/tests/test_persistence_probe.py` (plan 09)
- `notebooks/pu_manifold/derivative_bridge.py` — `FD_STENCIL_CONVENTION`, `DEFAULT_FD_STEP`,
  `MAX_FD_ROWS`, `finite_difference_jacobian`, `finite_difference_hessian`, `reduce_to_H_vec`,
  `calibrate_fd_step`, `derivative_agreement` (plan 10)
- `notebooks/pu_manifold/tests/test_derivative_bridge.py` (plan 10)
- `notebooks/diagnostics/decoder_substrate_ph_screen_run.py` (plan 11)
- `notebooks/diagnostics/derivative_bridge_run.py` (plan 14)
- `notebooks/02.6_swiss_roll_separator_check.ipynb` (plan 08)
- `notebooks/02.6_swiss_roll_plainae_ph_check.ipynb`, `notebooks/02.6_swiss_roll_topoae_ph_check.ipynb` (plan 12)
- `notebooks/02.6_swiss_roll_cae_ph_check.ipynb` (plan 13)

<output>
Create `.planning/phases/02.6-decoder-substrate-screening/02.6-15-SUMMARY.md` when done.
</output>
