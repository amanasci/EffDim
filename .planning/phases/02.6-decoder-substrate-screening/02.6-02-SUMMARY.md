---
phase: 02.6-decoder-substrate-screening
plan: 02
subsystem: research-planning
tags: [screening-rule, curvature, swiss-roll, decision-checkpoint, planning-document]

# Dependency graph
requires:
  - phase: 02.6-01
    provides: decoder_curvature.py's four-axis machinery and the confirmed 0.50-literal-not-constant correction that this rule's provenance note relies on
  - phase: 02.5
    provides: the raw-point arm's measured values (rho=0.6712, median cosine 0.9996, median ratio 0.8934, CV 0.2925, slope 0.7061 at R²=0.3828) and the CAE negative control's measured seed table (spread 1.0109, calibration slope -0.1856 at R²=0.0533)
provides:
  - "02.6-SCREENING-RULE.md: the ratified, numeric-and-procedural screening bars for all four admission axes, the seed protocol, and the promotion procedure, committed before any Phase 02.6 measurement"
affects: [02.6-03, 02.6-04, 02.6-05, 02.6-06]

# Tech tracking
tech-stack:
  added: []
  patterns: ["blind ratification before measurement", "comparative-only bars recomputed in the same run rather than frozen literals"]

key-files:
  created:
    - .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md
  modified: []

key-decisions:
  - "Group A (rank axis): A3 ratified — purely comparative, beat the raw-point arm recomputed in the same run, no absolute floor. A1 (0.50 literal), A2 (0.6712 literal), A4 (A3+A1) all declined."
  - "Group B (calibration axis): B1 ratified — |slope-1| no worse than the raw-point arm's in the same run, comparative ONLY. B3 (RESEARCH's own recommendation, B1+B2) explicitly declined; no absolute slope>=0.5/R²>=0.25 bar exists."
  - "Group C (seed-spread disqualifier): C3 ratified — no disqualifier at all; SPREAD reported and weighed in FINDINGS prose only. C1 (RESEARCH's proposed rule) and C2 both declined."
  - "Group D (seed set): D1 ratified — torch/split seeds 0,1,2,3, matching the CAE control's own table. D2 (five seeds) declined."
  - "Group E (promotion procedure): E1 ratified — RESEARCH's five steps as written, with steps 2(a) and 2(b) amended to match the C3/B1 dispositions; step 5's tie-break stands. E2 (strike the tie-break) declined."
  - "User was shown, and confirmed deliberately, that B1+C3 together remove both guards that caught the CAE's negative-control failure (invisible to rank, visible only in calibration); recorded in the rule document as an explicit, named disposition, not an oversight."

patterns-established:
  - "Comparative-only bars recomputed against a same-run reference arm are the ratified norm for this phase's rank and calibration axes, not frozen numeric literals."

requirements-completed: [SC-4, SC-5]

coverage:
  - id: D1
    description: "02.6-SCREENING-RULE.md exists, records all four axis bars (direction, magnitude, calibration, rank) each stated as absolute or comparative, and states the four axes are never combined into one score"
    requirement: "SC-4"
    verification:
      - kind: manual_procedural
        ref: "grep -qi 'gates nothing' && grep -qi SPREAD && grep -qi 20260807 02.6-SCREENING-RULE.md; grep -Ei 'composite|overall score|total score' returns clean"
        status: pass
    human_judgment: false
  - id: D2
    description: "The rule states the seed-spread disqualifying condition (none, per C3), the seed set (0,1,2,3, per D1), and the SPREAD statistic definition (max-min over the four seeds' rho)"
    requirement: "SC-4"
    verification:
      - kind: manual_procedural
        ref: "grep -ci spread returns 9; literal 'max' and 'min' present in the SPREAD-defining sentence"
        status: pass
    human_judgment: false
  - id: D3
    description: "The rule states the five-step promotion procedure with its at-most-one cap and names zero-candidates-clear as a complete, reportable outcome"
    requirement: "SC-5"
    verification:
      - kind: manual_procedural
        ref: "02.6-SCREENING-RULE.md § 'The promotion procedure'"
        status: pass
    human_judgment: false
  - id: D4
    description: "Every bar in the rule was chosen without any Phase 02.6 measurement in hand — committed at 69d044e before any plan-02.6-05 commit exists"
    verification:
      - kind: manual_procedural
        ref: "git log --oneline --all --grep=02.6-05 returns empty; git show --stat HEAD --name-only lists exactly one file"
        status: pass
    human_judgment: false
  - id: D5
    description: "The user selected each open option at the checkpoint (A3, B1, C3, D1, E1) rather than the plan silently locking a RESEARCH.md proposal, including three explicit declines (B3, C1, C2) of RESEARCH's own recommended/proposed options"
    verification: []
    human_judgment: true
    rationale: "The fact of user selection is recorded verbatim from the orchestrator-supplied <user_response>, and cannot be independently re-verified by an automated check within this execution; a human reader confirming the checkpoint transcript against the rule document is the appropriate verification."

# Metrics
duration: ~10min (continuation session; resumes a checkpoint left open by a prior executor)
completed: 2026-08-10
status: complete
---

# Phase 02.6 Plan 02: Screening Bars Ratified Summary

**Screening bars for all four Swiss-roll admission axes ratified blind and committed to `02.6-SCREENING-RULE.md` before any Phase 02.6 measurement — comparative-only bars throughout, with the user's deliberate, explicitly-recorded decision to decline both proposed guards (an absolute calibration floor and a seed-spread disqualifier) that caught the CAE's negative-control failure.**

## Performance

- **Duration:** ~10min (continuation agent; the blocking checkpoint itself was reached and returned by a prior executor session)
- **Completed:** 2026-08-10T14:33:45Z
- **Tasks:** 2/2 completed (Task 1: checkpoint resolution recorded; Task 2: rule document written and committed)
- **Files modified:** 1

## Accomplishments

- Recorded the user's ratified disposition of all five option groups (A3, B1, C3, D1, E1) from `02.6-02-PLAN.md`'s Task 1 blocking `checkpoint:decision`, including three explicit declines of RESEARCH.md's own proposed/recommended options (B3, C1, C2).
- Wrote and committed `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md`: four separately-reported axis bars (direction and magnitude comparative-only with no absolute bar proposed by any source; calibration comparative-only per B1; rank purely comparative per A3), the seed protocol (seeds 0-3, `FIXTURE_SEED=20260807`, `SPREAD=max-min` over rho, no disqualifier per C3), the five-step promotion procedure (steps 2(a)/2(b) amended to match C3/B1, tie-break retained per E1), the two effect sizes (~3.4x, ~6%), the three Section-4 must-not-infer statements transcribed from `02.5-NOTE-substrate-selection.md`, and a 16-row disposition table covering every RESEARCH.md proposal.
- Named, in the rule document itself, the deliberate sensitivity reduction the user confirmed: B1 + C3 together remove both guards (calibration's absolute floor, the seed-spread disqualifier) that caught the CAE's negative-control failure, which was invisible to the rank axis and visible only in calibration (slope `-0.1856` at `R²=0.0533`).

## Task Commits

Each task was committed atomically:

1. **Task 1: Ratify the screening bars, blind — before any Phase 02.6 measurement** — no separate commit; this task is a `checkpoint:decision` whose resolution (the user's ratified selections, supplied by the orchestrator) is recorded above and folded into Task 2's committed artifact. No code or document changes are attributable to Task 1 alone.
2. **Task 2: Write and commit 02.6-SCREENING-RULE.md** — `69d044e` (docs)

**Plan metadata:** this SUMMARY and its STATE.md/ROADMAP.md updates are committed separately per the standard final-commit step.

## Files Created/Modified

- `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md` — the ratified screening rule: four axis bars, seed protocol, promotion procedure, effect sizes, must-not-infer statements, and full disposition table.

## Decisions Made

- **A3 (rank axis, ratified):** purely comparative — beat the raw-point arm recomputed in the same run, no absolute floor. `A1` (`0.50` literal), `A2` (`0.6712` literal), and `A4` (`A3`+`A1`) all declined.
- **B1 (calibration axis, ratified):** comparative ONLY — `|slope-1|` no worse than the raw-point arm's `|slope-1|` in the same run. `B3` (RESEARCH's own recommendation, `B1`+`B2`) explicitly declined by the user; no absolute `slope>=0.5 / R²>=0.25` bar exists anywhere in the rule.
- **C3 (seed-spread disqualifier, ratified):** no disqualifier at all — `SPREAD` and the full per-seed table are reported and weighed in FINDINGS prose only. `C1` (RESEARCH's own proposed rule) and `C2` both declined by the user.
- **D1 (seed set, ratified):** torch/split seeds `0, 1, 2, 3`, matching the CAE negative control's own published table. `D2` (a fifth seed) declined.
- **E1 (promotion procedure, ratified):** RESEARCH's five steps as written, with steps 2(a) and 2(b) amended in substance to match the `C3`/`B1` dispositions above; step 5's tie-break retained. `E2` (strike the tie-break) declined.
- **Direction and magnitude axes:** no absolute bar for either (no source document proposed one); both comparative against the raw-point arm in the same run (median cosine no worse; `|median ratio - 1|` no worse; CV no worse), reported separately, never combined — matching the plan's own recommendation framing, not offered as lettered options.
- **The deliberate sensitivity reduction:** the orchestrator surfaced to the user, before ratification, that `B1`+`C3` together remove both guards that caught the CAE's negative control (rank-axis-invisible, calibration-visible failure). The user confirmed anyway. This is recorded in `02.6-SCREENING-RULE.md`'s own dedicated section as an explicit, deliberate disposition — never as a silently-dropped guard.

## Final wording of each bar, for `02.6-06` (`02.6-FINDINGS.md`) to cite

- **Direction:** comparative — median cosine similarity no worse than the raw-point arm's (`0.9996`), recomputed in the same run.
- **Magnitude:** comparative on both sub-statistics — `|median ratio - 1|` no worse than the raw-point arm's (`|0.8934-1|=0.1066`), and CV no worse than the raw-point arm's (`0.2925`), recomputed in the same run.
- **Calibration:** comparative ONLY — `|slope-1|` no worse than the raw-point arm's (`|0.7061-1|=0.2939`), recomputed in the same run. No absolute floor.
- **Rank:** purely comparative — Spearman `rho` beats the raw-point arm's `rho`, both recomputed on the identical fixture in the same run. No absolute floor (`0.50` and `0.6712` both explicitly declined as literals).
- **Disqualifying condition:** none. `SPREAD = max(rho) - min(rho)` over the four seeds is computed and reported alongside mean/median, but disqualifies nothing mechanically; weighed in FINDINGS prose only.

## Deviations from Plan

None — plan executed exactly as written, substituting the orchestrator-supplied ratified dispositions for the checkpoint's open questions per the resume instructions. No Rule 1-4 auto-fixes were needed; this plan produces one planning document and touches no source code.

## Issues Encountered

None. The prior executor session's checkpoint return was resolved cleanly with the user's response supplied verbatim by the orchestrator; no ambiguity required re-opening the checkpoint.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

`02.6-SCREENING-RULE.md` is committed (`69d044e`), ordered before any plan `02.6-05` commit (none exist yet), satisfying the plan's core ordering guarantee. Plans `02.6-03` and `02.6-04` (the plain-AE and TopoAE admission notebooks) and plan `02.6-05` (the four-seed runner) can now proceed with a fixed, ratified rule to screen against. Plan `02.6-06` (`02.6-FINDINGS.md`) should cite this document's bars verbatim rather than restating them with different numbers, and should give particular weight in its prose to the seed-spread table now that no mechanical disqualifier exists — `C3` moves that judgment entirely into `02.6-06`'s hands.

---
*Phase: 02.6-decoder-substrate-screening*
*Completed: 2026-08-10*

## Self-Check: PASSED

- FOUND: `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md`
- FOUND: `.planning/phases/02.6-decoder-substrate-screening/02.6-02-SUMMARY.md`
- FOUND: commit `69d044e` in `git log --oneline --all`
