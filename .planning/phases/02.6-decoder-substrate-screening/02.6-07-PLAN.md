---
phase: 02.6-decoder-substrate-screening
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md
autonomous: false
requirements: [SC-1]
user_setup: []

must_haves:
  truths:
    - "A ratification document exists that states the persistent-homology agreement criterion, its bars, and the 16-cell read-out matrix, and it is committed BEFORE any number on the new axis has been measured (D-09, SC-1)."
    - "The document declares its bars explicitly NON-GATING in its own opening paragraph: the criterion ranks, it promotes nothing, and no promotion is sealed under it (D-09, D-10, SC-1)."
    - "The `SPREAD > mean - floor` seed disqualifier is recorded as DROPPED, with the reason stated (D-11)."
    - "Every one of the five open design questions carried into this replan has a recorded disposition with the option chosen and the options declined named individually (D-01, D-05, D-06, D-16)."
    - "The document names the two travelling caveats (thin `(H1, intrinsic)` denominator; bottleneck saturation) once each, in canonical wording, to be cited rather than restated downstream."
  artifacts:
    - .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md
  key_links:
    - "Every measurement plan (02.6-09, 02.6-11, 02.6-12, 02.6-13, 02.6-14) reads its cell definitions, seed set, scaling convention and caveat wording from this one document rather than re-deriving them."
    - "The git commit of this document precedes, in ancestry, every commit that records a new-axis number — that ordering is what SC-1 asserts."
---

<objective>
Ratify the persistent-homology agreement criterion, its bars, its 16-cell read-out matrix and its
seed protocol **blind** — before any candidate has been measured on the new axis — and ratify them
as **explicitly non-gating** (SC-1; D-01, D-09, D-10, D-11).

This is the ordering discipline `02.6-FINDINGS.md` §4 records the halted run as having failed to
supply: the old `02.6-SCREENING-RULE.md` was ratified blind and still constrained no published
conclusion, because the axis it gated was changed after an unfavourable result. D-01 is rated
**one-way** precisely so that cannot happen a second time: this plan's `checkpoint:decision` is the
last point at which the ranking quantity can change.

The document also resolves the five open design questions that `02.6-RESEARCH.md` and this planning
pass left open, each as a recorded, individually-dispositioned choice rather than a silent default.

Purpose: nothing downstream may measure a new-axis number until the criterion that reads it is on
disk and committed.
Output: `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02.6-decoder-substrate-screening/02.6-CONTEXT.md
@.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS.md
@.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-NOTE-substrate-selection.md
</context>

<assumption_delta_decision>
Three `plan:pre` assumption-delta signals were surfaced by the orchestrator and are assessed here,
in the plan that owns the phase's identity model. This block is the recorded outcome; a reasoned
`no-change` is a valid result and is recorded as such rather than skipped.

| # | Signal | Noun now primary | Decision | Rationale |
|---|--------|------------------|----------|-----------|
| 1 | `pluralization` / "alongside" — *"alongside reconstruction 2.96x better…"* | none | **no-change** | Lexical hit on ROADMAP prose describing a *prior* measurement (02.5-09's CAE result). "Alongside" is a preposition in a historical clause, not a signal that a singular entity became plural. No identity model changes. |
| 2 | `pluralization` / "second" — *"whether a net's second derivatives survive"* | none | **no-change** | "Second derivatives" is a fixed mathematical term of art, already plural in every prior use in this project (`chart_curvature.py`, `decoder_curvature.py`, `02.6-FINDINGS.md` §4). It is not a singular→plural change of a modelled entity. |
| 3 | `chosen` / "chosen" — *"the moment PH was chosen as the axis"* | **the phase's ranking quantity** | **promote** | This is a genuine identity-model change and it is the whole reason this replan exists. The primary noun for "what this phase ranks on" was *decoder-pullback curvature agreement against analytic `H`*; it is now *persistent-homology diagram agreement* (D-01). The curvature quantity is not deleted — it is demoted to a strictly non-ranking role as the SC-2 separator's instrument (D-12..D-15), where it answers "can any net carry usable second derivatives" and never "which substrate is better". The ratification document written by this plan is where that promotion is made explicit and permanent. |
</assumption_delta_decision>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Ratify the PH-agreement criterion and its five open design questions — blind, before any measurement</name>

  <read_first>
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-CONTEXT.md` — D-01..D-22, the locked decisions this checkpoint operates inside; `<assumptions>` A-01..A-04.
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md` — the halted axis's own ratification document: read its section structure (criterion statement, lettered option groups, seed protocol, travelling-caveat convention, full disposition table). The new document copies that structure and none of its bars.
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS.md` §4, §5, §7, §8 — why the axis changed, what the fixture pin does not establish, what must not be inferred.
    - `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-NOTE-substrate-selection.md` §2, §4, §6 — the Swiss-roll-has-truth/PU-does-not asymmetry, the three inferences that must not be drawn, what `02.5-10` owns.
  </read_first>

  <decision>
Five design questions that the read-out matrix's meaning depends on, none settled by CONTEXT.md.
All five must be answered before any candidate is measured, because each one changes what a number
in the matrix means.
  </decision>

  <context>
D-09 is rated **one-way**: a pre-registration declared exploratory cannot later be claimed to have
constrained a published conclusion. D-01 is rated **one-way**: `02.6-FINDINGS.md` §4 already records
one criterion changed after an unfavourable result, and a second change for the same question would
be the third criterion tried. Whatever is ratified here is what the phase reports.

Two facts measured during this planning pass, which the options below turn on, and which are NOT in
`02.6-RESEARCH.md`:

1. On the standard fixture (`make_swiss_roll_fixture(n=3000, seed=20260807)`, 600-point subsample
   via `np.random.default_rng(20260807).permutation(3000)[:600]`, `ripser 0.6.15`, `maxdim=1`):
   the intrinsic-plane reference's own top H1 life is **0.3348** (diameter 11.2886, life fraction
   0.0297) against the ambient reference's **0.7198** (diameter 4.0360, life fraction 0.1783) — a
   6.0x gap in normalized terms. `02.6-RESEARCH.md` Pitfall 2 reports 0.420 for the same cell; that
   value is subsample-procedure-dependent and must not be pinned as a constant.
2. `persim.bottleneck(ambient_H1_reference, intrinsic_H1_reference) = 0.35988`, which is **exactly**
   half the ambient reference's own top H1 life — i.e. the two references are already at the
   ambient reference's bottleneck saturation value with respect to each other, and identical to
   `persim.bottleneck(empty_diagram, ambient_H1_reference)`. The `(H1, ambient)` bottleneck cell is
   therefore expected to be saturated for any candidate whose latent has correctly unrolled the
   roll. This is `02.6-RESEARCH.md` Pitfall 1 biting at the reference level, and it is a stronger
   statement than that document makes.
  </context>

  <options>
    <option id="P1">
      <name>Thin `(H1, intrinsic)` denominator — report raw + denominator + normalized, apply no floor (RECOMMENDED)</name>
      <pros>D-05 applied literally; every cell carries the three numbers a reader needs to judge it (raw distance, the reference's own max persistence, the quotient); no invented constant enters the record; matches the "report, do not absorb" posture of A-03.</pros>
      <cons>The normalized `(H1, intrinsic)` number is genuinely less interpretable than its siblings and a careless reader could rank on it.</cons>
    </option>
    <option id="P2">
      <name>Apply a floor to the denominator for the `(H1, intrinsic)` cell only</name>
      <pros>Bounds the quotient.</pros>
      <cons>The floor is an invented constant with no source, chosen while the hazard is already known — the exact shape of decision `02.6-FINDINGS.md` §4 warns about. Also unstable: the measured denominator moves with the subsample.</cons>
    </option>
    <option id="P3">
      <name>Report the `(H1, intrinsic)` cell raw only, unnormalized</name>
      <pros>No degenerate quotient exists.</pros>
      <cons>Breaks the matrix's uniformity — 15 cells scale-free, one not — and D-05 asks for normalization.</cons>
    </option>

    <option id="Q1">
      <name>Per-cloud pre-scaling — apply `topoae.latent_unit_scale` to every cloud, model and reference alike, and print the applied scalar (RECOMMENDED)</name>
      <pros>The 2-D latent (arbitrary units) and the intrinsic reference (arc-length units) become comparable at all; `latent_unit_scale` is a single global isotropic scalar, already sealed and tested, and is isometric up to uniform scale so it cannot reorder neighbours; matches the quick task's own already-executed precedent.</pros>
      <cons>It is a convention D-05 does not name, so it must be written down here or it is a silent choice.</cons>
    </option>
    <option id="Q2">
      <name>No pre-scaling — native coordinates, D-05 taken literally</name>
      <pros>Nothing beyond the ratified text enters the pipeline.</pros>
      <cons>Persistence is scale-covariant: a latent that is globally 10x larger produces a 10x larger distance to any reference, and normalizing by the reference's max persistence does not remove the model side of that. Cross-candidate comparison would partly measure latent scale (Pitfall 4).</cons>
    </option>
    <option id="Q3">
      <name>Pre-scale model clouds only, leave references native</name>
      <pros>Cheaper to describe.</pros>
      <cons>Asymmetric — the distance is then between clouds in two different unit systems, which is worse than either symmetric choice.</cons>
    </option>

    <option id="R1">
      <name>The CAE's "encoder latent" is its global `embed_dim` embedding, `model.encode(x)` (RECOMMENDED)</name>
      <pros>Globally comparable, the same sense in which the other two candidates' latents are meant; `cae.embedding_distortion` refuses chart-local coordinates as incomparable; matches the quick task's own resolved choice and `notebooks/02.2_swiss_roll_cae_check.ipynb`'s precedent.</pros>
      <cons>It is not the CAE's chart coordinate, so the read-out says less about the atlas than a chart-local one would.</cons>
    </option>
    <option id="R2">
      <name>Chart-local `chart_dim` coordinates at the argmax chart</name>
      <pros>Closer to what the CAE's decoder actually consumes.</pros>
      <cons>Chart-local coordinates are not a single global coordinate system; a diagram built from them is not the same object as the other candidates' diagrams.</cons>
    </option>
    <option id="R3">
      <name>Both, reported separately</name>
      <pros>Never-collapse discipline applied one level deeper.</pros>
      <cons>Adds 8 more cells for one candidate only, breaking the matrix's 16-per-candidate uniformity.</cons>
    </option>

    <option id="S1">
      <name>Bridge compares BOTH the full `d x d` Hessian tensor and the reduced `H_vec`/`H_norm`, reported separately (RECOMMENDED)</name>
      <pros>The never-collapse rule applied to the bridge; the reduced quantity is what the downstream curvature claim actually uses, the full tensor is what "second derivatives" literally means in D-16; batching makes the `d=40` PU cost affordable (2d + 4·d(d-1)/2 = 3200 decoder evaluations per point, issued as one batched forward).</pros>
      <cons>Two numbers per point per model instead of one.</cons>
    </option>
    <option id="S2">
      <name>Reduced `H_vec`/`H_norm` only</name>
      <pros>Cheapest; it is the quantity the downstream use cares about.</pros>
      <cons>A reduction can agree while the tensor it reduces disagrees; that is exactly the class of hiding this phase exists to stop doing.</cons>
    </option>
    <option id="S3">
      <name>Full `d x d` tensor only</name>
      <pros>Literal reading of "second derivatives".</pros>
      <cons>Says nothing directly about the quantity `02.5-10` would threshold.</cons>
    </option>

    <option id="T1">
      <name>Separator evaluated on BOTH its own fixture points and a dense held-out grid, reported separately (RECOMMENDED)</name>
      <pros>The analytic parameterization is noise-free everywhere, so a held-out grid costs nothing and removes any ambiguity about whether the separator's result is overfitting; the grid is built in `t` (uniform over the fixture's `t` range) so no arc-length inversion is needed.</pros>
      <cons>Two tables instead of one.</cons>
    </option>
    <option id="T2">
      <name>Fixture points only</name>
      <pros>Matches D-12's literal text, which specifies no split.</pros>
      <cons>Leaves "was this just memorised at the sample points" unanswered, and it is free to answer.</cons>
    </option>
  </options>

  <resume-signal>Select one from each group, e.g. "P1, Q1, R1, S1, T1" — or name the alternative per group. Any group may be answered with a different choice; every declined option is recorded individually in the document's disposition table.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write and commit 02.6-SCREENING-RULE-02.md — non-gating from the outset, before any measurement</name>

  <files>.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md</files>

  <read_first>
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md` — the section structure to mirror: criterion statement, per-axis bars, seed protocol, the deliberate-choices-recorded-plainly section, the full disposition table, the provenance note. Its *content* is history and binds nothing here.
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-CONTEXT.md` — D-01..D-22 verbatim; the read-out-matrix consequence paragraph.
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-RESEARCH.md` § Common Pitfalls (Pitfalls 1, 2, 3, 4) and § Open Questions.
    - `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-NOTE-substrate-selection.md` §4 — the three inferences that must not be drawn, transcribed into the new document as the old one transcribed them.
  </read_first>

  <reversibility rating="one-way">
Implements D-01 (the ranking quantity) and D-09 (non-gating ratification), both rated one-way in
CONTEXT.md. Once this document is committed, the phase reports what this criterion measures; a third
criterion for the same question is not recoverable by the record.
  </reversibility>

  <action>
Write `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md` recording the
choices ratified in Task 1. Additive: `02.6-SCREENING-RULE.md` is not edited, moved, or deleted.

Required content, in this order:

1. **Opening paragraph — non-gating from the outset (D-09, D-10).** State in the document's own
   first paragraph that its bars gate nothing, seal nothing, promote nothing, and produce no verdict
   artifact; that the criterion ranks and the promotion decision belongs wholly to `02.5-10` under
   its own seal-before-measure discipline; and that this is a deliberate choice to claim less, made
   because `02.6-FINDINGS.md` §4 records the previous rule as having constrained nothing. Do not let
   this document read as the old one renamed.

2. **The criterion (D-01).** Persistent-homology agreement: the distance between a model's
   persistence diagram and a reference diagram. State that it was chosen over isometry distortion
   against exact geodesics and over k-NN neighbourhood overlap, and that D-01 is one-way.

3. **The read-out matrix — exact cell definitions (D-03, D-04, D-05, D-06).** Define all 16 cells
   per (candidate, seed) by their canonical labels, in the form
   `{space}|{reference}|H{k}|{distance}_norm` over
   `space in {latent, decoder_image}`, `reference in {intrinsic, ambient}`, `k in {0, 1}`,
   `distance in {bottleneck, wasserstein}`. State explicitly: 16 numbers per seed per candidate, 64
   across four seeds, 192 across three candidates, Swiss-roll side only; reported separately, never
   weighted, summed, averaged or ranked into one score; no composite figure exists anywhere in this
   phase. State that H2 is excluded because ROADMAP.md records no power analysis for `beta_2`.
   State the two references: (a) the true 2-manifold in exact intrinsic coordinates
   `(arc-length s(t), y)`, (b) the ambient 3-D point cloud; and that a correct unroll matches (a)
   and diverges from (b), so divergence from (b) is not failure.

4. **Provenance columns required beside every cell.** Each normalized cell is reported with its raw
   (unnormalized) distance, the reference diagram's own max persistence used as the denominator, and
   a saturation flag for bottleneck cells. State the applied per-cloud scale scalar per cloud if Q1
   was ratified.

5. **Bars (D-07, D-22).** Absolute on the Swiss roll, comparative on PU. State that an absolute bar
   is possible on the roll because its answer is exact, and not computable on PU per
   `02.5-NOTE-substrate-selection.md` §2, so the PU side stays relative to a matched plain-AE
   baseline and a null. State that Stage A admission is **descriptive**: failing it bars nothing
   from Stage B (D-22), because a hard gate inside a phase whose criterion is ratified non-gating
   would be a contradiction.

6. **Seed protocol (D-11).** Torch/split seeds `0, 1, 2, 3`; `FIXTURE_SEED = 20260807` and
   `N_POINTS = 3000` held fixed; only the torch/split seed varies. `SPREAD = max - min` reported
   alongside mean and median. **Disqualifying condition: NONE** — record that the
   `SPREAD > mean - floor` rule is DROPPED, that this is its second independent rejection (declined
   as `C1`/`C2` at the old plan `02.6-02`, dropped again by D-11), and the reason: with a non-gating
   criterion there is nothing for a disqualifier to disqualify. Record A-03 as a live caveat — PH
   seed variance is unmeasured in this project, and if the observed spread swamps the
   between-candidate differences that is a finding about the criterion and is reported as one.

7. **The two travelling caveats — canonical wording, stated once.** Follow
   `02.6-SCREENING-RULE.md`'s own travelling-caveat convention: state each once, cite it everywhere,
   never restate it with different wording.
   - *Thin `(H1, intrinsic)` denominator.* The intrinsic-plane reference is a flat, simply-connected
     rectangle and carries only sampling-noise H1. Measured at planning time on the standard fixture
     (600-point subsample, `np.random.default_rng(20260807)`): top H1 life `0.3348` at diameter
     `11.2886` (fraction `0.0297`) against the ambient reference's `0.7198` at diameter `4.0360`
     (fraction `0.1783`). Record explicitly that `02.6-RESEARCH.md` Pitfall 2's `0.420` is
     subsample-procedure-dependent and is not pinned as a constant anywhere.
   - *Bottleneck saturation.* Bottleneck is a max over a matching; once a model diagram has nothing
     close enough to match the reference's longest bar, that bar goes to the diagonal at exactly half
     its own life and the distance stops responding. Measured at planning time:
     `persim.bottleneck(ambient_H1_ref, intrinsic_H1_ref) = 0.35988`, exactly half the ambient
     reference's top H1 life and identical to the empty-diagram distance — so the `(H1, ambient)`
     bottleneck cell is expected to be saturated for a correctly unrolled latent. A saturated cell
     is reported with its flag and is not ranked on.

8. **Disposition of every option offered (mirroring the old document's table).** One row per option
   in groups P, Q, R, S, T: the option, ratified or declined, and the detail. Nothing silently
   inherited.

9. **What this document inherits from `02.6-SCREENING-RULE.md`: nothing binding.** State that the
   old document's bars are history, that they constrained no published conclusion (§4), and that the
   only things carried across are its *structure* and its travelling-caveat convention.

10. **The three inferences that must not be drawn**, transcribed in substance from
    `02.5-NOTE-substrate-selection.md` §4: Swiss roll topology does not transfer; reconstruction
    quality validates nothing about curvature; H0 preservation is not curvature fidelity. Add
    CONTEXT.md's A-01 and A-02 verbatim in substance: the bridge tests numerical consistency, not
    correctness, and a PH-ranked winner is not thereby a curvature-capable winner.

11. **Ratification stamp.** Date, the plan and checkpoint at which each group was ratified, and the
    statement that no 02.6 number on the persistent-homology axis existed at the time of writing.

Do not write any threshold that a later plan is instructed to compare a candidate against as a
pass/fail. There is no candidate-facing gate in this phase.
  </action>

  <verify>
    <automated>test -f .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md && grep -q "non-gating" .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md && grep -c "latent|intrinsic|H1|wasserstein_norm" .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md` exists.
    - `git diff --name-only HEAD~1 HEAD -- .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE.md` is empty — the old ratification document is byte-unchanged.
    - The document's first paragraph contains the literal `non-gating`.
    - The document contains all 16 canonical cell labels; `grep -o 'decoder_image|ambient|H[01]|\(bottleneck\|wasserstein\)_norm' <file> | sort -u | wc -l` returns `4`, and the same grep for `latent|intrinsic`, `latent|ambient`, `decoder_image|intrinsic` each return `4` — 16 distinct labels in total.
    - The document contains the literals `0.3348`, `0.7198`, `0.35988` and `11.2886` (the planning-time measurements) and does **not** contain `0.420` presented as a pinned constant.
    - The document contains a disposition table with one row per option id in `P1 P2 P3 Q1 Q2 Q3 R1 R2 R3 S1 S2 S3 T1 T2`; `grep -cE '^\| *(P[123]|Q[123]|R[123]|S[123]|T[12]) *\|' <file>` returns `14`.
    - The document states `SPREAD` is reported and that no disqualifying condition exists.
    - `git log --oneline -1 -- .planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md` returns a commit, and
      `git log --oneline -- notebooks/pu_manifold/persistence_probe.py` returns empty at the moment of that commit — the criterion precedes the instrument.
    - `git status --porcelain notebooks/ src/effdim/ pyproject.toml` is empty — this plan touches no code.
  </acceptance_criteria>

  <done>
`02.6-SCREENING-RULE-02.md` is committed, declares itself non-gating in its own opening paragraph,
defines all 16 cells by canonical label, records the ratified disposition of all fourteen options
individually, states both travelling caveats in canonical wording, and its commit precedes every
commit that records a persistent-homology number.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → notebook/runner process | Reads sealed `notebooks/.cache/*.npz` fits and sealed module source. No network, no auth, no user-supplied input path exists anywhere in this milestone. |
| this phase's measurements → the phase record | The only consequential adversary is a future reader who cannot distinguish "we learned something" from "we kept going until a candidate won". |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.6R-03 | Repudiation | the phase record's ordering claim (criterion before measurement) | high | mitigate | This plan's document is committed in its own commit with no code in the tree that computes a PH number; the acceptance criteria assert that ancestry directly by `git log` over `persistence_probe.py`. D-01/D-09 are one-way and gated behind this plan's blocking `checkpoint:decision`. |
| T-02.6R-01 | Tampering | sealed modules, sealed fits, `src/effdim/`, `pyproject.toml` | high | mitigate | `git status --porcelain notebooks/ src/effdim/ pyproject.toml` empty is an acceptance criterion; this plan writes one markdown file. |
| T-02.6R-06 | Tampering | seed set redrawn after seeing results | medium | mitigate | Seeds `0,1,2,3` are fixed in this document before any measurement and are read from it by every downstream runner rather than chosen there. |
| T-02.6R-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no package. `ripser 0.6.15` / `persim 0.3.8` are already venv-installed and audited in `02.6-RESEARCH.md` § Package Legitimacy Audit ([SUS] `unknown-downloads`, both approved with rationale). No install task exists in any plan of this phase, so no legitimacy checkpoint is warranted. |
| T-02.6R-99 | Spoofing / DoS | — | low | accept | No network surface, no auth surface, no user input path; single-process local scripts on already-vetted local artifacts. |
</threat_model>

<verification>
- The ratification document exists, is committed, and its commit has no PH-measuring code as an ancestor.
- The old `02.6-SCREENING-RULE.md` and plans `02.6-01`..`02.6-06` are byte-unchanged.
- All five open questions have an individually recorded disposition.
</verification>

<success_criteria>
SC-1 is satisfied: the criterion, its bars and the read-out matrix are written down and committed
before any measurement on the new axis, and ratified as explicitly non-gating, with the
`SPREAD > mean - floor` disqualifier recorded as dropped.
</success_criteria>

## Artifacts this phase produces

Every symbol and path below is **created by this phase** and does not exist before it. The
source-grounding pass should treat all of these as new, not as drift.

- `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md` (this plan)
- `.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md` (plan 15)
- `notebooks/pu_manifold/analytic_param.py` — `SWISS_ROLL_PARAM_CONVENTION`, `swiss_roll_arc_length`,
  `swiss_roll_intrinsic_plane`, `swiss_roll_exact_ambient`, `AnalyticSwissRollDecoder`,
  `AnalyticParamDecoder`, `train_analytic_param_decoder` (plan 08)
- `notebooks/pu_manifold/tests/test_analytic_param.py` (plan 08)
- `notebooks/pu_manifold/persistence_probe.py` — `PH_MAXDIM`, `finite_pairs`,
  `persistence_diagram`, `max_persistence`, `saturation_value`, `is_saturated`, `ph_agreement`,
  `readout_matrix`, `READOUT_CELLS` (plan 09)
- `notebooks/pu_manifold/tests/test_persistence_probe.py` (plan 09)
- `notebooks/pu_manifold/derivative_bridge.py` — `DEFAULT_FD_STEP`, `finite_difference_hessian`,
  `calibrate_fd_step`, `hessian_agreement`, `reduced_curvature_agreement` (plan 10)
- `notebooks/pu_manifold/tests/test_derivative_bridge.py` (plan 10)
- `notebooks/diagnostics/decoder_substrate_ph_screen_run.py` (plan 11)
- `notebooks/diagnostics/derivative_bridge_run.py` (plan 14)
- `notebooks/02.6_swiss_roll_separator_check.ipynb` (plan 08)
- `notebooks/02.6_swiss_roll_plainae_ph_check.ipynb`, `notebooks/02.6_swiss_roll_topoae_ph_check.ipynb` (plan 12)
- `notebooks/02.6_swiss_roll_cae_ph_check.ipynb` (plan 13)

<output>
Create `.planning/phases/02.6-decoder-substrate-screening/02.6-07-SUMMARY.md` when done.
</output>
