---
phase: 02.6-decoder-substrate-screening
plan: 13
type: execute
wave: 4
depends_on: [02.6-09, 02.6-12]
files_modified:
  - notebooks/02.6_swiss_roll_cae_ph_check.ipynb
autonomous: false
requirements: [SC-6]
user_setup: []

must_haves:
  truths:
    - "The CAE — the third in-repo candidate and this phase's measured negative control — has its own CLAUDE.md-mandated Swiss roll notebook on the persistent-homology axis, importing model code unchanged, generating its data in-notebook and training from scratch (SC-6, D-20)."
    - "The CAE's decoder image is taken from `model.reconstruct(x)`, not from a `y` key on its forward pass, because `ChartAutoEncoder.forward` returns no such key — a claim in `02.6-RESEARCH.md` and `02.6-PATTERNS.md` verified false against source during planning."
    - "The CAE's encoder latent is its global embedding, in the sense the ratified document fixes, and its dimension is printed beside every number so the asymmetry against the other two candidates' 2-D latents is visible rather than inferred."
    - "No C2 guard is applied anywhere in this notebook: persistence diagrams are built from point coordinates, so the chart fragmentation that excluded the CAE from the halted axis's first tier is not an obstruction here (D-22)."
    - "The notebook is a negative control and is read as one: a poor result is the expected outcome and is reported without being read as a verdict, and a good result is reported without being read as a promotion (D-10)."
  artifacts:
    - notebooks/02.6_swiss_roll_cae_ph_check.ipynb
  key_links:
    - "`model.reconstruct(x)` is the CAE's argmax-chart decoder image, the same call `notebooks/02.2_swiss_roll_cae_check.ipynb` itself uses; substituting a forward-pass key would raise, and substituting `decode_all` would silently score a per-chart stack instead of a reconstruction."
    - "The same `persistence_probe.readout_matrix` the runner calls is called here, so this notebook's seed-0 numbers and the runner's seed-0 CAE rows are the same quantity by construction."
---

<objective>
Deliver the CLAUDE.md-mandated Swiss roll sanity notebook for the CAE on the persistent-homology
axis (SC-6; D-20, D-22, project CLAUDE.md).

The CAE enters this axis unconditionally. It was excluded from the halted axis's first tier entirely
because a chart-fragmented decoder cannot be safely differentiated twice, and its admission FAILED
there (02.5-09, curvature Spearman `-0.0604`). Persistence diagrams are built from point coordinates
alone, so none of that applies: the CAE needs no C2 guard, no chart routing awareness and no special
casing on this axis, and D-22 makes Stage A descriptive so nothing bars it from being screened.

It is retained as the **measured** negative control, and the emphasis is on measured. The CAE
reconstructs the roll about 2.96x better than a matched plain autoencoder while preserving H0 merge
structure on PU roughly 3.4x worse — the largest effect size this milestone has recorded. Whether
that pattern reappears on the roll's persistent homology is exactly what this notebook measures, and
its result is not a verdict in either direction (D-10).

Purpose: a negative control that is never run is not a control.
Output: `notebooks/02.6_swiss_roll_cae_ph_check.ipynb`, executed with outputs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02.6-decoder-substrate-screening/02.6-CONTEXT.md
@.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md
@notebooks/02.2_swiss_roll_cae_check.ipynb
@notebooks/pu_manifold/persistence_probe.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: The CAE persistent-homology Swiss roll notebook, executed with outputs</name>

  <files>notebooks/02.6_swiss_roll_cae_ph_check.ipynb</files>

  <read_first>
    - `/home/akagi/Documents/Projects/EffDim/CLAUDE.md` — the Swiss roll sanity-check contract in full, both the seven numbered requirements and the "what the notebook must not do" list.
    - `notebooks/02.2_swiss_roll_cae_check.ipynb` — all 15 cells. This is both CLAUDE.md's reference implementation and this candidate's own existing Swiss roll notebook; reuse its constants verbatim (`SEED = 0`, `N_POINTS = 3000`, `CHART_DIM = 2`, `EMBED_DIM = 8`, `N_CHARTS = 8`, `HIDDEN = [64, 64]`) and its `cfg` dict verbatim (`lr=1e-3`, `weight_decay=1e-4`, `batch=64`, `max_epochs=300`, `early_stop_patience=25`, `early_stop_min_delta=1e-4`, `n_charts`, `fps_pretrain_epochs=20`, `lip_weight=1e-3`, `lip_every_n_steps=1`), and reuse its §4 and §5 cells' `model.reconstruct(x)` call and its `rel_err` definition. Do NOT edit that notebook.
    - `notebooks/pu_manifold/cae.py` — `ChartAutoEncoder.__init__` (lines 156-180), `encode` (line 182, returning the global embedding from the initial encoder), `chart_coords`, `decode_all`, `forward` (lines 205-211, returning `z`, `z_charts`, `y_charts`, `p`, `e` and **no** `y` key), `reconstruct` (lines 213-218, the argmax-chart reconstruction), `train_cae`, `chart_survival`, `reconstruction_stats`, `PlainAutoEncoder`, `train_plain_ae`.
    - `notebooks/pu_manifold/persistence_probe.py` — `readout_matrix`, `READOUT_CELLS`, and that `prescale` has no default.
    - `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md` — the ratified disposition of option group R (which of the CAE's two latent spaces counts as its encoder latent), of group Q (pre-scaling) and of group P (thin denominator), plus both travelling caveats in canonical wording.
    - `notebooks/02.6_swiss_roll_plainae_ph_check.ipynb` — as built by plan 02.6-12, for the cell structure this notebook mirrors.
    - `notebooks/pu_manifold/analytic_param.py` — `swiss_roll_intrinsic_plane`.
  </read_first>

  <action>
Create `notebooks/02.6_swiss_roll_cae_ph_check.ipynb` and execute it end to end, committing it with
its outputs. Additive: no existing notebook is edited or deleted, including
`notebooks/02.2_swiss_roll_cae_check.ipynb`.

Mirror `notebooks/02.6_swiss_roll_plainae_ph_check.ipynb`'s fifteen-cell structure exactly — same
section headers, same plots, same reference-provenance block, same reproducibility-gap note, same
prohibition list, same two-minute budget — with the following candidate-specific content.

**§1 setup constants.** Reuse `notebooks/02.2_swiss_roll_cae_check.ipynb`'s own values verbatim:
`SEED = 0`, `N_POINTS = 3000`, `CHART_DIM = 2`, `EMBED_DIM = 8`, `N_CHARTS = 8`, `HIDDEN = [64, 64]`,
and its `cfg` dict verbatim. Use `random_state=20260807` for the roll so this notebook's numbers are
comparable to the diagnostics runner's CAE rows at seed 0. Print the CAE's latent dimension
explicitly and note in prose that it is `EMBED_DIM`, not `CHART_DIM`, and therefore not the same
dimension as the other two candidates' latents — an asymmetry to be seen, not inferred.

**§3 fits the Chart Auto-Encoder.** `cae.ChartAutoEncoder(in_dim=3, embed_dim=EMBED_DIM,
chart_dim=CHART_DIM, n_charts=N_CHARTS, hidden=HIDDEN, activation="silu")` after seeding torch, under
`cae.train_cae` with that `cfg`. Print `epochs_run`, `wallclock_s`, `early_stopped`, plot the main
stage's training curve, and print `cae.chart_survival(model, prune_tol=1e-2)`.

**§4 the reconstruction.** Take the decoder image from `model.reconstruct(x_holdout)` — the
argmax-chart reconstruction, exactly as `02.2_swiss_roll_cae_check.ipynb`'s own §4 and §5 cells do.
**This is load-bearing and is a correction carried into this plan:** `02.6-RESEARCH.md` Pattern 3 and
`02.6-PATTERNS.md`'s Shared Patterns both state that this class's forward pass returns a `y`-keyed
dict; verified against source during planning, it does not — it returns `z`, `z_charts`, `y_charts`,
`p` and `e`. Reading a `y` key would raise; reading `decode_all` would silently score a per-chart
stack rather than a reconstruction. Plot the input beside the reconstruction as a 3-D scatter and an
x-z scatter, all four panels coloured by the roll's parameter. Print `cae.reconstruction_stats` and
the mean relative error, beside the CLAUDE.md-mandated matched baseline: a
`cae.PlainAutoEncoder(3, CHART_DIM, hidden=tuple(HIDDEN), activation="silu")` trained under
`cae.train_plain_ae` with the same protocol at the same 2-D bottleneck, exactly as
`02.2_swiss_roll_cae_check.ipynb` does it.

**§5 the latent.** Take the latent from `model.encode(x_holdout)`, which returns the global embedding
rather than chart-local coordinates, per the ratified disposition of option group R. Because that
embedding is 8-dimensional it cannot be scattered directly: plot its first two principal coordinates,
coloured by the roll's parameter, and say in the section markdown that this is a projection of an
8-dimensional latent and not the latent itself, so the picture is suggestive and the persistence
numbers in §6 are the measurement. Print the Spearman correlation between the first principal
coordinate and the roll's parameter, as the sibling notebooks do. Also print, for contrast and
clearly labelled as such, the same Spearman on the matched plain AE's genuine 2-D latent.

**§6 persistence agreement, this seed.** One `persistence_probe.readout_matrix` call over the CAE's
two spaces and the two references on the identical held-out rows, with the ratified `prescale`.
Print all 16 cells with raw values, denominators and saturation flags, in `READOUT_CELLS` order,
with no combined figure. Then the same 16 for the matched plain AE and for a chance null drawn from
`np.random.default_rng(SEED + 100)` at the CAE's own latent dimension — three labelled blocks, no
ratio column between them. Apply no C2 guard anywhere in this notebook: persistence needs none, and
applying one to a ReLU-free chart decoder would be a needless incorrect gate.

**§7 read-out.** Four printed boolean lines and a one-sentence read-out:
(a) the roll is reconstructed — held-out mean relative error below `0.10`;
(b) more than one chart survived;
(c) the signature D-03 predicts holds — the latent's H1 Wasserstein distance to the intrinsic
reference is smaller than its H1 Wasserstein distance to the ambient reference;
(d) the CAE beats the chance null on H0 and H1 Wasserstein against the intrinsic reference.
Then one sentence stating what the notebook licenses. It must say, in substance: this is the
measured negative control, a poor result here is the expected outcome and is not a verdict, and a
good result here is not a promotion — this phase promotes nothing (D-10), and `02.5-10` makes that
decision. Note also, in that closing paragraph, the standing pattern this notebook is testing
against: on PU the CAE preserves H0 merge structure roughly 3.4x worse than a plain autoencoder at
the same dimension while reconstructing the roll about 2.96x better, and reconstruction quality has
already been recorded twice in this milestone as validating nothing about the geometry.

No caching, no pre-registration, no git-ancestry proof, no cfg-hash key, no verdict artifact, no
threshold table. Execute with
`.venv/bin/python -m jupyter nbconvert --to notebook --execute --inplace notebooks/02.6_swiss_roll_cae_ph_check.ipynb`,
run so the kernel's working directory is `notebooks/`.
  </action>

  <verify>
    <automated>.venv/bin/python -m jupyter nbconvert --to notebook --execute --inplace notebooks/02.6_swiss_roll_cae_ph_check.ipynb; grep -c 'output_type.: .error' notebooks/02.6_swiss_roll_cae_ph_check.ipynb</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'output_type.: .error' notebooks/02.6_swiss_roll_cae_ph_check.ipynb` returns `0` after a clean-kernel execute.
    - `.venv/bin/python -c "import json;print(len(json.load(open('notebooks/02.6_swiss_roll_cae_ph_check.ipynb'))['cells']))"` returns a value at most `15`.
    - `grep -c "reconstruct(" notebooks/02.6_swiss_roll_cae_ph_check.ipynb` is at least `1`, and `grep -cE 'forward\(x[^)]*\)\[.y.\]|model\(x[^)]*\)\[.y.\]' notebooks/02.6_swiss_roll_cae_ph_check.ipynb` returns a count matching only the matched plain-AE baseline's own call and never the chart model's.
    - `grep -cE "assert_c2_activation|assert_c2_decoder" notebooks/02.6_swiss_roll_cae_ph_check.ipynb` returns `0`.
    - `grep -c "chart_survival" notebooks/02.6_swiss_roll_cae_ph_check.ipynb` is at least `1`.
    - The notebook's stdout contains three labelled 16-cell blocks — the CAE, the matched plain AE, and the chance null — each with all 16 `READOUT_CELLS` labels, and no ratio or combined column.
    - The notebook's stdout contains four boolean read-out lines and a closing paragraph containing the phrases `negative control` and `promotes nothing`.
    - The notebook's stdout prints the CAE's latent dimension as `8` beside its numbers.
    - `grep -cE "npz_cache|joblib_cache|json_cache|config_key|cache_path|load_subsample|\.cache" notebooks/02.6_swiss_roll_cae_ph_check.ipynb` returns `0`.
    - `ls notebooks/.cache | wc -l` returns `252` before and after the run.
    - The notebook's printed total wall clock is under 120 seconds.
    - `git status --porcelain notebooks/02.2_swiss_roll_cae_check.ipynb notebooks/02.6_swiss_roll_plainae_ph_check.ipynb notebooks/02.6_swiss_roll_topoae_ph_check.ipynb notebooks/pu_manifold/ src/effdim/ pyproject.toml` is empty.
  </acceptance_criteria>

  <done>
`notebooks/02.6_swiss_roll_cae_ph_check.ipynb` exists, is at most 15 cells, executes clean under two
minutes, trains the CAE from scratch with 02.2's own verbatim configuration, takes its decoder image
from the argmax-chart reconstruction and its latent from the global embedding, applies no C2 guard,
prints three labelled 16-cell blocks against a matched plain-AE baseline and a chance null, and ends
with four boolean lines and a closing paragraph framing it explicitly as a measured negative control
that promotes nothing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human reads the CAE negative control's plots and read-out</name>

  <what-built>
`notebooks/02.6_swiss_roll_cae_ph_check.ipynb`, executed with outputs: a Chart Auto-Encoder trained
from scratch on a 3000-point Swiss roll at 8 charts and a 2-dimensional chart dimension, with its
argmax-chart reconstruction and its 8-dimensional global embedding scored on all sixteen
persistent-homology read-out cells against both references, beside a matched `cae.PlainAutoEncoder`
at the same width, depth, protocol and 2-D bottleneck, and beside a chance null.
  </what-built>

  <how-to-verify>
1. Open the notebook, or re-run it with
   `.venv/bin/python -m jupyter nbconvert --to notebook --execute --inplace notebooks/02.6_swiss_roll_cae_ph_check.ipynb`.
2. **Look at §4's x-z scatters.** The CAE is known to reconstruct this roll well — about 2.96x better
   than a matched plain autoencoder — so the reconstruction's colour bands should stay in order.
   If they do not, the fit is broken rather than the model being informative, and the persistence
   numbers below are not about the CAE.
3. **Look at §5's latent projection.** It is the first two principal coordinates of an 8-dimensional
   embedding, not the latent itself; read it as suggestive and let §6 carry the measurement. Compare
   it against the matched plain AE's genuine 2-D latent printed beside it.
4. **Read §3's surviving-chart count.** A collapse to one chart would mean the atlas did not form,
   and the negative control would then be measuring a degenerate single-chart model rather than the
   architecture this phase means by "CAE".
5. **Read §6's three labelled blocks.** Confirm all sixteen labels appear in each, that every line
   carries a raw value and a denominator, that saturated cells are flagged, and that no ratio or
   combined column has crept in between the blocks.
6. **Read §7's four boolean lines and the closing paragraph.** The key judgement: confirm the
   closing paragraph reads this as a measured negative control in both directions — that a poor
   result is the expected outcome and not a verdict, and that a good result is not a promotion,
   because this phase promotes nothing and `02.5-10` owns that decision. A closing paragraph that
   slides into ranking language is the failure to catch here.
7. Confirm the notebook applies no C2 guard, never reads a `y` key off the chart model's forward
   pass, and does not touch the milestone artifact directory.

CLAUDE.md's standing rule is that the model is not done until its Swiss roll notebook exists **and**
passes, and this phase's own `02.6-03` is the recorded precedent for what an unclosed gate costs.
  </how-to-verify>

  <resume-signal>Type "approved" and state whether the CAE's charts survived, whether the D-03 intrinsic-versus-ambient signature held, and whether the closing paragraph stays inside negative-control framing — or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → notebook process | Generates its own data in-process; the milestone artifact directory is neither read nor written, and the sealed 02.2 CAE PU fits are not opened at all. No network, no auth, no user-supplied input path. |
| a negative control's number → the phase's ranking language | The specific risk here: a control result sliding into a verdict or a promotion in either direction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.6R-21 | Information disclosure (silent wrong answer) | reading the chart model's decoder image from a key its forward pass does not return, or from the per-chart stack | high | mitigate | The action names `model.reconstruct(x)` explicitly and records the verified-false claim; acceptance criteria grep for the call and against the wrong one; the 02.2 notebook is the in-repo precedent read first. |
| T-02.6R-22 | Elevation of privilege | a negative control's result read as a verdict or a promotion | high | mitigate | The closing paragraph is required to state both directions explicitly, the acceptance criteria grep for `negative control` and `promotes nothing`, and the human-verify resume signal asks for that judgement by name. |
| T-02.6R-19 | Repudiation | a mandated sanity notebook whose plots no human reads | high | mitigate | A blocking `checkpoint:human-verify` with a resume signal requiring three specific stated judgements. |
| T-02.6R-23 | Tampering | a C2 guard applied where none is needed, gating a valid candidate out | medium | mitigate | Negative grep for both guard names; the action states why persistence needs none. |
| T-02.6R-01 | Tampering | existing notebooks, sealed modules, sealed fits, `src/effdim/`, `pyproject.toml` | high | mitigate | `git status --porcelain` over each path returning empty is an acceptance criterion; the artifact-directory entry count is checked before and after; the sealed 02.2 CAE PU fits are never opened. |
| T-02.6R-20 | Tampering | threshold or pre-registration machinery drifting into a sanity notebook | medium | mitigate | Cell-count and negative-grep acceptance criteria; the matrix belongs to plan 02.6-11's runner. |
| T-02.6R-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan. |
| T-02.6R-99 | Spoofing / DoS | — | low | accept | No network surface, no auth surface, no user input path. |
</threat_model>

<verification>
- The notebook executes clean, is at most 15 cells, runs under two minutes, and is committed with outputs.
- The decoder image comes from the argmax-chart reconstruction and the latent from the global embedding; no C2 guard is applied.
- Three labelled 16-cell blocks are printed with no ratio or combined column.
- The blocking human-verify gate is closed with the three stated judgements.
- No existing notebook, sealed module, sealed fit or frozen path is modified; the artifact directory count stays 252.
</verification>

<success_criteria>
SC-6 is satisfied for the third candidate: the CAE has a CLAUDE.md-mandated Swiss roll notebook on
the persistent-homology axis, importing model code unchanged, with absolute bars on the roll, a
genuinely matched baseline, and negative-control framing that neither promotes nor condemns.
</success_criteria>

## Artifacts this phase produces

Every symbol and path below is **created by this phase** and does not exist before it. The
source-grounding pass should treat all of these as new, not as drift.

- `.planning/phases/02.6-decoder-substrate-screening/02.6-SCREENING-RULE-02.md` (plan 07)
- `.planning/phases/02.6-decoder-substrate-screening/02.6-FINDINGS-02.md` (plan 15)
- `notebooks/pu_manifold/analytic_param.py` — `SWISS_ROLL_PARAM_CONVENTION`, `swiss_roll_arc_length`,
  `swiss_roll_intrinsic_plane`, `swiss_roll_exact_ambient`, `AnalyticSwissRollDecoder`,
  `AnalyticParamDecoder` (plan 08)
- `notebooks/pu_manifold/tests/test_analytic_param.py` (plan 08)
- `notebooks/pu_manifold/persistence_probe.py` — `PH_MAXDIM`, `READOUT_CELLS`, `finite_pairs`,
  `persistence_diagram`, `max_persistence`, `saturation_value`, `is_saturated`, `ph_agreement`,
  `cloud_distance_matrix`, `readout_matrix` (plan 09)
- `notebooks/pu_manifold/tests/test_persistence_probe.py` (plan 09)
- `notebooks/pu_manifold/derivative_bridge.py` — `FD_STENCIL_CONVENTION`, `DEFAULT_FD_STEP`,
  `MAX_FD_ROWS`, `finite_difference_jacobian`, `finite_difference_hessian`, `reduce_to_H_vec`,
  `calibrate_fd_step`, `derivative_agreement` (plan 10)
- `notebooks/pu_manifold/tests/test_derivative_bridge.py` (plan 10)
- `notebooks/diagnostics/decoder_substrate_ph_screen_run.py` (plan 11)
- `notebooks/diagnostics/derivative_bridge_run.py` (plan 14)
- `notebooks/02.6_swiss_roll_separator_check.ipynb` (plan 08)
- `notebooks/02.6_swiss_roll_plainae_ph_check.ipynb`, `notebooks/02.6_swiss_roll_topoae_ph_check.ipynb` (plan 12)
- `notebooks/02.6_swiss_roll_cae_ph_check.ipynb` (this plan)

<output>
Create `.planning/phases/02.6-decoder-substrate-screening/02.6-13-SUMMARY.md` when done.
</output>
