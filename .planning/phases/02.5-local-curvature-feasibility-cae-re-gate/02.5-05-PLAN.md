---
phase: 02.5-local-curvature-feasibility-cae-re-gate
plan: 05
type: execute
wave: 4
depends_on: ["02.5-03"]
files_modified:
  - notebooks/02.5_swiss_roll_curvature_probe_check.ipynb
autonomous: false
requirements: [D-03, D-05, D-07]
must_haves:
  truths:
    - "A Swiss roll notebook exists for the local mean-curvature estimator, executed end to end with outputs committed, and prints three or four pass/fail lines plus a one-sentence read-out (CLAUDE.md, D-03)."
    - "The notebook imports the estimator unchanged from notebooks/pu_manifold/curvature_probe.py and reimplements no part of it, so what the notebook tests is the code that will run on real data (CLAUDE.md)."
    - "The notebook generates its own Swiss roll with sklearn.datasets.make_swiss_roll at ~3,000 points, noise=0.0, a fixed random_state, centred and divided by one global scalar standard deviation (CLAUDE.md)."
    - "The estimator's working dimension is set to 2, the roll's true intrinsic dimension (CLAUDE.md, D-07)."
    - "The estimated curvature field is compared against a matched baseline — the non-gating quadric fit at d=2, where 3 coefficients from k=30 samples is well determined and the route is known to succeed (CLAUDE.md's no-decoder carve-out, D-05)."
    - statement: "A human read the printed pass/fail read-out and the plots and judged the estimator to have recovered the Swiss roll's curvature field, or recorded plainly that it did not."
      verification: backstop
  artifacts:
    - notebooks/02.5_swiss_roll_curvature_probe_check.ipynb
  key_links:
    - "make_swiss_roll's t parameter -> swiss_roll_analytic_H_scaled -> the colour scale on every plot: colour bands staying in order is what makes a visual read of the curvature field meaningful, and the same t drives both the analytic truth and the plot ordering."
    - "one global scalar std -> swiss_roll_analytic_H_scaled(t, global_std): the estimator sees scaled coordinates, so the ground truth must be scaled by the same factor or the printed relative error is wrong by that factor."
  prohibitions:
    - statement: "The notebook contains no cache calls, no cfg-hash cache keys, no verdict JSON artifact, no threshold table, and no git-ancestry proof — those belong to the gated runners, not the sanity check"
      status: planned
      verification: manual
    - statement: "The notebook never reads from or writes to notebooks/.cache/"
      status: planned
      verification: manual
    - statement: "The notebook never reimplements, simplifies, or inlines a variant of the estimator; it imports curvature_probe unchanged"
      status: planned
      verification: manual
    - statement: "The Swiss roll is not substituted by another toy dataset"
      status: planned
      verification: manual
    - statement: "No existing notebook is deleted or rewritten; this notebook is additive"
      status: planned
      verification: manual
    - statement: "Any number printed in this notebook is a sanity read-out, never a gate; stage 1's gate is fixed only by the ratified 02.5-PREREGISTRATION.md"
      status: planned
      verification: manual
---

<objective>
Deliver CLAUDE.md's mandatory Swiss roll sanity check for the new local mean-curvature estimator.

Purpose: CLAUDE.md names curvature estimators explicitly among the models this rule binds, and
states the reason plainly — a model that fails on real data has two possible causes, no structure
in the data or a broken implementation, and only a manifold whose answer is known in advance
separates them. Without this notebook, a stage-1 FAIL on the graph-of-function family at the PU
regime is uninterpretable, and every conclusion drawn from it is unsupported.

This notebook is **not** the stage-1 gate. Stage 1's gate is null-calibrated, pre-registered in
plan 02.5-06, and executed by the runner in plan 02.5-07. CLAUDE.md forbids the pre-registration
apparatus inside a sanity notebook, and `02.5-PATTERNS.md` flags the two file families as
convention-incompatible. Keeping them apart is a requirement of this plan, not an accident of it.

Output: `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb`, executed end to end, outputs
committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-CONTEXT.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-RESEARCH.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PATTERNS.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Swiss roll curvature-probe sanity notebook</name>
  <files>notebooks/02.5_swiss_roll_curvature_probe_check.ipynb</files>
  <read_first>
    - CLAUDE.md § "Swiss roll sanity check — required for every new manifold model" (all of it — the ten numbered requirements, the prohibitions, and the no-decoder carve-out)
    - notebooks/02.2_swiss_roll_cae_check.ipynb (all 15 cells — CLAUDE.md names this as the reference shape; copy its cell structure, its section headings, and its final read-out style)
    - notebooks/pu_manifold/curvature_probe.py (the functions to import unchanged)
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PATTERNS.md § "notebooks/02.5_swiss_roll_curvature_probe_check.ipynb" (the cell-by-cell shape and the explicit list of prohibitions binding on this file)
  </read_first>
  <action>
Create `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb`, at most 15 cells, following
`notebooks/02.2_swiss_roll_cae_check.ipynb`'s structure cell for cell:

1. **Markdown** — problem framing. Why a Swiss roll separates "the data has no curvature structure"
   from "the estimator is broken", and what this notebook is not: it carries no gate, no
   pre-registration, and no threshold table, because those live in `02.5-PREREGISTRATION.md` and
   `notebooks/diagnostics/curvature_feasibility_sweep_run.py`.
2. **Markdown** — `## §1. Setup`.
3. **Code** — `sys.path.insert(0, ...)` for the relative `pu_manifold` import, exactly as the 02.2
   notebook does. Never import from `src/effdim/`. Import
   `from pu_manifold import curvature_probe as cp` and nothing else from the package.
4. **Markdown** — `## §2. The Swiss roll`. Describe the centring / one-global-scalar-std
   convention and the `t`-colouring convention, and state that curvature has units of inverse
   length so the analytic ground truth is rescaled by the same scalar.
5. **Code** — `X_raw, t = make_swiss_roll(n_samples=3000, noise=0.0, random_state=20260807)`; then
   `global_std = float(X_raw.std())` — one global scalar, `.std()` with no axis argument — and
   `X = (X_raw - X_raw.mean(axis=0)) / global_std`. Print `X.shape` and `global_std`.
6. **Markdown + Code** — `## §3. The estimator`. Call
   `cp.centroid_mean_curvature(X, k=30, d=2)` and `cp.mean_curvature_norm(...)`, and
   `cp.swiss_roll_analytic_H_scaled(t, global_std)` for the truth. `d=2` is the roll's true
   intrinsic dimension per CLAUDE.md; `k=30` is this notebook's own choice and is explicitly not
   the pre-registered gating `k` — say so in the markdown. Import the functions unchanged; do not
   copy any estimator body into the notebook.
7. **Markdown + Code** — `## §4. Does the field track the truth, visually`. Two side-by-side
   3-D scatters of the roll, the first coloured by analytic `||H||` and the second by estimated
   `||H||`, then the same pair as x-z scatters — CLAUDE.md requires both views because the default
   3-D view hides the spiral and the x-z plane shows it unambiguously. Use one shared colour
   normalization across each pair so the two are visually comparable.
8. **Markdown + Code** — a third figure: the roll coloured by `t` (CLAUDE.md's colour-bands-stay-
   in-order check) beside a scatter of estimated `||H||` against analytic `||H||` with the
   identity line drawn. The scatter is where a systematic scale error would be visible as a slope
   away from 1.
9. **Markdown + Code** — `## §5. How faithful is it, in numbers`. Print
   `cp.spearman_gate_statistic(h_est, h_true)` and `cp.median_relative_error(h_est, h_true)`, and
   state in the markdown that Spearman is the statistic the phase gates on because Phase 4
   consumes ordering, while relative error is context.
10. **Markdown + Code** — `## §6. Against a matched baseline`. CLAUDE.md's decoder-model baseline
    (`cae.PlainAutoEncoder`) does not apply to an estimator with no decoder; CLAUDE.md's own
    carve-out for a model with no decoder is to compare against "a baseline that is known to
    succeed". At `d=2` the quadric fit needs 3 quadratic coefficients and has `k=30` samples, so it
    is comfortably determined — that is the known-good route. Run
    `cp.quadric_mean_curvature(X, k=30, d=2)` and `cp.estimator_agreement(...)`, print both
    agreement numbers, and print `underdetermined` (expected `False` here). State the carve-out
    reasoning in the markdown so the substitution is a recorded decision, not a silent one.
11. **Markdown + Code** — `## §7. Read-out`. Four printed pass/fail lines and one sentence,
    copying the 02.2 notebook's final-cell print style:
    - `Spearman vs analytic H > 0.90` with the measured value
    - `median relative error < 0.25` with the measured value
    - `agrees with the quadric baseline (median rel diff < 0.25)` with the measured value
    - `quadric baseline is determined at d=2 (not underdetermined)` with the boolean
    Then one sentence naming what the notebook showed. If any line reads False, say so plainly —
    CLAUDE.md is explicit that a model failing on a manifold it was designed for is a real result
    about the model, and this phase's whole design treats a negative as reportable.
    Add a final printed line stating in words that these are sanity thresholds chosen for this
    notebook and are not the phase gate.

Do not add: any `cache.` call, any cfg dict, any cfg-hash key, any verdict JSON write, any
threshold table, any git-ancestry proof, any read or write under `notebooks/.cache/`. Do not
delete or modify any existing notebook.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; python -c "
import json,sys
nb=json.load(open('notebooks/02.5_swiss_roll_curvature_probe_check.ipynb'))
cells=nb['cells']
src=''.join(''.join(c['source']) for c in cells)
assert len(cells)<=15, f'cell budget exceeded: {len(cells)}'
for bad in ['cache.','json_cache','joblib_cache','config_key','.cache/','PREREGISTRATION','merge-base','write_curvature_verdict','write_curvature_handoff']:
    assert bad not in src, f'prohibited construct in sanity notebook: {bad}'
assert 'make_swiss_roll' in src and 'noise=0.0' in src
assert 'curvature_probe' in src
assert 'd=2' in src
print('notebook shape OK,', len(cells), 'cells')
"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb` exists and has at most 15 cells
    - The notebook source contains `make_swiss_roll`, `noise=0.0`, `random_state=`, `curvature_probe`, `d=2`
    - The notebook source contains none of: `cache.`, `json_cache`, `joblib_cache`, `config_key`, `.cache/`, `PREREGISTRATION`, `merge-base`, `write_curvature_verdict`, `write_curvature_handoff`
    - The notebook computes `global_std` with `.std()` and no `axis` argument: `python -c "import json; s=''.join(''.join(c['source']) for c in json.load(open('notebooks/02.5_swiss_roll_curvature_probe_check.ipynb'))['cells']); assert '.std()' in s and 'std(axis' not in s"` exits 0
    - `git status --porcelain notebooks/02.1_swiss_roll_geometry_probes_check.ipynb notebooks/02.2_swiss_roll_cae_check.ipynb notebooks/02.4_swiss_roll_topoae_check.ipynb notebooks/02_k_sensitivity_refit.ipynb` produces no output
  </acceptance_criteria>
  <done>A ≤15-cell Swiss roll notebook exists that imports the estimator unchanged, generates its own data under CLAUDE.md's exact preprocessing, plots both required views, compares against a known-good baseline, and ends with four pass/fail lines and a sentence — carrying none of the gated-runner apparatus CLAUDE.md prohibits here.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the notebook end to end and commit it with outputs</name>
  <files>notebooks/02.5_swiss_roll_curvature_probe_check.ipynb</files>
  <read_first>
    - notebooks/02.5_swiss_roll_curvature_probe_check.ipynb (as written by Task 1)
    - notebooks/02.2_swiss_roll_cae_check.ipynb (confirm how its outputs are stored, so this notebook is committed the same way)
    - CLAUDE.md § General ("Notebooks are committed with their outputs, executed end to end")
  </read_first>
  <action>
Execute the notebook top to bottom in the notebook venv and save it with its outputs. Use
`jupyter nbconvert --to notebook --execute --inplace` (or the project's equivalent if
`02.2_swiss_roll_cae_check.ipynb`'s history shows a different invocation) with an execution
timeout that comfortably exceeds the target. Target under two minutes on CPU per CLAUDE.md; if it
exceeds that, reduce `n_samples` toward 3,000 rather than weakening any check, and record the
actual wall-clock time in the SUMMARY.

Confirm after execution that every code cell carries an `execution_count` and that no cell raised.
Confirm the four printed pass/fail lines and the one-sentence read-out are present in the stored
outputs. Record the four measured numbers verbatim in the SUMMARY — Spearman, median relative
error, agreement median relative difference, and the `underdetermined` boolean — so the stage-1
pre-registration in plan 02.5-06 can disclose what was already known when it was written.

Confirm the notebook wrote nothing under `notebooks/.cache/`: capture `ls notebooks/.cache | wc -l`
before and after execution and assert the count is unchanged.

Commit the notebook with its outputs.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; python -c "
import json
nb=json.load(open('notebooks/02.5_swiss_roll_curvature_probe_check.ipynb'))
code=[c for c in nb['cells'] if c['cell_type']=='code']
assert all(c.get('execution_count') is not None for c in code), 'an unexecuted code cell remains'
errs=[o for c in code for o in c.get('outputs',[]) if o.get('output_type')=='error']
assert not errs, f'notebook raised: {errs[0].get(\"ename\")}'
text=''.join(o.get('text','') if isinstance(o.get('text'),str) else ''.join(o.get('text',[])) for c in code for o in c.get('outputs',[]) if o.get('output_type')=='stream')
assert 'Spearman' in text, 'read-out line missing from stored outputs'
print('executed, no errors, read-out present')
"</automated>
  </verify>
  <acceptance_criteria>
    - Every code cell in the committed notebook has a non-null `execution_count`
    - No cell output has `output_type == "error"`
    - The stored stream output contains the four pass/fail lines and the one-sentence read-out
    - `ls notebooks/.cache | wc -l` is unchanged by execution
    - `git log -1 --name-only` shows the notebook committed with outputs
    - Wall-clock execution time recorded in the SUMMARY
  </acceptance_criteria>
  <done>The notebook is executed end to end with no errors, its four measured numbers are recorded, it touched nothing under notebooks/.cache/, and it is committed with outputs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human read of the Swiss roll sanity check</name>
  <what-built>
`notebooks/02.5_swiss_roll_curvature_probe_check.ipynb` — the mandatory CLAUDE.md sanity check for
this phase's new local mean-curvature estimator. It generates a 3,000-point Swiss roll with
`noise=0.0`, centres it and divides by one global scalar standard deviation, runs
`curvature_probe.centroid_mean_curvature(X, k=30, d=2)` imported unchanged, and scores the result
against the closed-form analytic mean curvature `H(t) = (t^2 + 2)/(1 + t^2)^{3/2}` rescaled by the
same global standard deviation. It plots the roll coloured by analytic and by estimated `||H||`
in both the 3-D and the x-z view, plots the estimated-vs-analytic scatter against the identity
line, and compares against the non-gating quadric fit at `d=2` as the known-good baseline.
  </what-built>
  <how-to-verify>
1. Open `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb` and read it top to bottom. It
   should take one sitting; if it does not, it has drifted from its purpose.
2. Look at the two 3-D scatters side by side, then the two x-z scatters. The estimated field
   should show the same spatial pattern as the analytic one: highest curvature at the tight inner
   turn of the spiral (small `t`), falling smoothly outward. A field that looks like noise, or
   that is banded along a coordinate axis rather than along the spiral, is a broken estimator.
3. Look at the estimated-vs-analytic scatter against the identity line. A slope visibly away from
   1 is a scale or convention error — the factor-of-`d` trace-vs-averaged mismatch that
   `02.5-01-PLAN.md`'s OQ-CONV resolution exists to prevent would show here as a factor-of-2
   slope.
4. Read the four printed pass/fail lines and the one-sentence read-out. Confirm each printed
   number matches what the plots suggest.
5. Confirm what is absent: no `cache.` call, no cfg-hash key, no verdict JSON, no threshold table,
   no git-ancestry proof, and nothing read from or written to `notebooks/.cache/`. CLAUDE.md
   prohibits all of these in a sanity notebook and `02.5-PATTERNS.md` flags the two file families
   as convention-incompatible.
6. Decide. A FAIL here is a legitimate outcome and blocks the rest of the phase: if the estimator
   cannot recover curvature on a `d=2` manifold with an exactly known answer and even sampling,
   nothing measured at `d=20`, `D=768` on real data would be interpretable. Do not soften a check
   to pass it.

**On approval, note for the record:** the four measured numbers become an input to
`02.5-PREREGISTRATION.md` (plan 02.5-06), which must disclose that this notebook had already been
run and what it showed, so the pre-registration's ordering is transparent rather than merely
asserted. This follows 02.4's own sequencing, where the Swiss roll fixture and its sweep preceded
the pre-registration.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to plan 02.5-06 (stage-1 pre-registration), or describe what the plots or numbers show that they should not.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New files (none exist before this phase; every symbol below is created by phase 02.5):

- `notebooks/pu_manifold/curvature_probe.py` — `CURVATURE_CONVENTION`, `swiss_roll_analytic_H`,
  `swiss_roll_analytic_H_scaled`, `make_swiss_roll_fixture`, `local_tangent_basis`,
  `centroid_mean_curvature`, `mean_curvature_norm`, `spearman_gate_statistic`,
  `median_relative_error`, `local_density_weights`, `gaussian_bump_values`,
  `graph_mean_curvature`, `make_graph_of_function_fixture`, `make_flat_fixture`,
  `quadric_fit_curvature`, `quadric_mean_curvature`, `estimator_agreement`, `permutation_null`,
  `measure_cell`, `STAGE1_GATING_METRICS`, `STAGE2_GATING_METRICS`, `GATE_DIRECTIONS`,
  `STAGE1_VERDICT_RULE`, `STAGE2_VERDICT_RULE`, `_apply_gates`, `verdict_from_stage1_metrics`,
  `verdict_from_stage2_metrics`, `write_curvature_verdict`, `write_curvature_handoff`,
  `clear_stale_curvature_handoff`
- `notebooks/pu_manifold/chart_curvature.py` — `assert_c2_activation`, `chart_decoder_map`,
  `chart_mean_curvature`, `chart_mean_curvature_norm`, `chart_curvature_field`
- `notebooks/pu_manifold/tests/test_curvature_probe.py` — all `test_*` functions therein
- `notebooks/diagnostics/curvature_feasibility_sweep_run.py` — `SWEEP_CFG`, `BASE_CELL`,
  `SWEEP_AXES`, `main`
- `notebooks/diagnostics/cae_local_regate_run.py` — `FIT_KEY`, `SEALED_SEEDS`, `main`
- `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb`,
  `notebooks/02.5_swiss_roll_chart_curvature_check.ipynb`
- `.planning/phases/02.5-.../02.5-PREREGISTRATION.md`,
  `.planning/phases/02.5-.../02.5-PREREGISTRATION-STAGE2.md`,
  `.planning/phases/02.5-.../02.5-FINDINGS.md`, `.planning/phases/02.5-.../COVERAGE.md`
- Cache artifacts (gitignored): `curvature_feasibility_{stage1_key}.json`,
  `curvature_verdict_stage1_{stage1_key}.json`, `curvature_verdict_stage2_{fit_key}.json`,
  `curvature_handoff_{fit_key}.json`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sanity notebook -> gated runner conventions | `02.5-PATTERNS.md` flags these two file families as convention-incompatible; a cache call or threshold table crossing into the notebook makes a sanity check look like a gate |
| notebook read-out -> phase gate | Every number printed here is a sanity read-out; treating one as the phase gate would bypass the pre-registration entirely |
| notebook execution -> notebooks/.cache/ | CLAUDE.md forbids the notebook reading or writing the cache; a write here could collide with a gated artifact stem |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.5-09 | Repudiation | a notebook read-out later presented as the pre-registered gate | high | mitigate | The notebook's final printed line states in words that its thresholds are sanity values, not the phase gate; Task 1's verify asserts the string `PREREGISTRATION` does not appear in the notebook; plan 02.5-06's pre-registration discloses that this notebook had already run and what it showed. |
| T-02.5-13 | Tampering | sanity notebook writing into the gated artifact cache | medium | mitigate | Task 1's verify negative-greps `cache.`, `json_cache`, `joblib_cache`, `config_key`, and `.cache/` in the notebook source; Task 2 asserts the cache file count is unchanged across execution. |
| T-02.5-05 | Tampering | existing notebooks deleted or rewritten | high | mitigate | Additive-only; Task 1's acceptance criterion asserts `git status --porcelain` on the four pre-existing notebooks is empty. |
| T-02.5-07 | Tampering | curvature convention drift between the notebook and the module | high | mitigate | The notebook imports `swiss_roll_analytic_H_scaled` and the estimator unchanged rather than transcribing either; the identity-line scatter in cell 8 makes a factor-of-2 slope visible to the human checkpoint. |
| T-02.5-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; the notebook imports only already-pinned `numpy`, `matplotlib`, `scikit-learn`, and the local `pu_manifold` package. |
</threat_model>

<verification>
- The notebook has at most 15 cells, every code cell executed, no error outputs
- The notebook source contains none of `cache.`, `json_cache`, `joblib_cache`, `config_key`, `.cache/`, `PREREGISTRATION`, `merge-base`
- `ls notebooks/.cache | wc -l` unchanged across execution
- `git status --porcelain` shows no modification to the four pre-existing notebooks
- The blocking human-verify checkpoint in Task 3 is resolved with a recorded decision
</verification>

<success_criteria>
CLAUDE.md's mandatory Swiss roll check exists for this phase's curvature estimator, executed with
outputs committed, comparing the estimated field against the closed-form analytic answer and a
known-good baseline in both required plot views, ending with four pass/fail lines and a sentence —
and a human has read it and recorded a decision.
</success_criteria>

<output>
Create `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-05-SUMMARY.md` when done
</output>
