---
phase: 02.5-local-curvature-feasibility-cae-re-gate
plan: 07
subsystem: geometry
tags: [curvature-estimation, feasibility-sweep, spearman, quantile-bin-concordance, permutation-null, stage-1-gate]

# Dependency graph
requires:
  - phase: 02.5-04
    provides: "curvature_probe.py's gate/verdict layer (STAGE1_GATING_METRICS, verdict_from_stage1_metrics, write_curvature_verdict) this plan's runner calls unmodified"
  - phase: 02.5-06
    provides: "02.5-PREREGISTRATION.md -- the ratified gating statistics (option-scale-C, both spearman_rho_pointwise and quantile_bin_concordance gate independently), every numeric constant, the fixture set, the sweep grid, and the halt-on-wrong-side consequence, with ancestry proof this plan's runner enforces at STEP 0"
provides:
  - "quantile_bin_concordance: a new, tested curvature_probe.py module function implementing 02.5-PREREGISTRATION.md Section 3e's exact pseudocode"
  - "notebooks/diagnostics/curvature_feasibility_sweep_run.py: the stage-1 sweep runner, proving pre-registration ancestry and constant fidelity before computing anything, then sweeping one axis at a time from BASE_CELL"
  - "The completed stage-1 sweep artifacts: curvature_feasibility_12e5e16efd940369.json (25 distinct cells, full per-cell/per-axis results) and curvature_verdict_stage1_12e5e16efd940369.json (CURVATURE_VERDICT=FAIL)"
  - "02.5-FINDINGS.md: the feasibility boundary write-up -- where the PU regime sits, which axis is responsible, the two fixture variants' disagreement, the density correction's mixed effect at d=20, and the quadric cross-check's wall-clock-limited evidence"
affects: ["02.5-08", "02.5-09", "02.5-10", "02.5-11", "02.5-12", "02.5-13"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "STEP 0 precondition chain (pre-reg exists -> ancestry proved by git merge-base -> every constant matches verbatim -> import sanity) halts before any computation, proving the pre-register-then-measure ordering in code rather than asserting it"
    - "Per-cell time budgeting that distinguishes a cell recorded as timed_out (compliant) from a cell never attempted because a cumulative budget ran out mid-sweep (an order-dependent silent truncation this runner refuses to produce)"
    - "Sharing an expensive shared computation (centroid_mean_curvature_both_densities) across two density-correction variants in one pass, verified bit-identical to two separate calls, to fit a fixed wall-clock budget"
    - "A structural infeasibility (k <= d) is recorded as its own named non-outcome (infeasible_k_le_d), distinct from both PASS and FAIL, rather than crashing or silently omitting the cell"
    - "A FAIL verdict reported alongside an explicit seed-instability caveat when the across-seed spread at the identical configuration exceeds the margin to threshold -- neither softening the FAIL nor overstating it as decisive"

key-files:
  created:
    - notebooks/diagnostics/curvature_feasibility_sweep_run.py
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md
  modified:
    - notebooks/pu_manifold/curvature_probe.py

key-decisions:
  - "CURVATURE_VERDICT=FAIL on the base cell, measured exactly per Section 3h's both-gates-required rule: spearman_rho=0.5205 clears (threshold 0.5000), quantile_bin_concordance=0.4444 does not (threshold 0.4750, margin -0.0306). Reported as measured, not softened, not re-derived."
  - "The FAIL is disclosed as not robust to seed: 2 of 3 tested seeds at the identical base-cell configuration clear both gates (0.4837, 0.5236 vs the base seed's 0.4444); the across-seed spread (0.0792) exceeds the base cell's own margin to threshold (0.0306). Recorded as a pre-registration gap the sweep's own seed axis discovered, not a post-hoc audit finding."
  - "Ambient dimension D found bit-identical (0.4444459259456792 / 0.5204686765526867 to the last digit) at D=28,50,200,768 -- the failure is entirely an intrinsic-d effect, correcting a framing risk in 02.5-PREREGISTRATION.md Section 13b that a reader could otherwise misattribute to ambient scale"
  - "The quadric cross-check (D-05) could not complete beyond the d=2 Swiss roll anchor within this sweep's wall-clock budget (measured ~6-8 min/cell at PU scale vs the runner's own 2s/120s per-cell timeouts) -- reported as a genuine evidentiary gap, with the d(d+1)/2-vs-k coefficient-count boundary (determined through d=5, underdetermined from d=8) reported structurally instead of empirically"
  - "The density correction (D-06) is reported to have a mixed, not reliably positive, effect on the curved graph-of-function fixture at d=20 (both gating statistics move slightly worse under correction on the skewed cell) -- distinct from, and not contradicting, 02.5-02's separate proof that a flat fixture cannot be fooled by density skew at all"
  - "The FINDINGS document names both live options (Ollivier-Ricci/intrinsic curvature, and stage-2's Arm B per 02.5-NOTE-randomized-trace.md) without choosing between them, per Section 10's own instruction that the choice belongs to the user at this plan's Task 3 checkpoint"

patterns-established:
  - "When a gate's verdict rests on a single seed, report the seed-sensitivity check (if the sweep design includes one) as loudly as the verdict itself, and name explicitly whether the across-seed spread exceeds the margin to threshold"
  - "Report a wall-clock-limited non-gating measurement's absence honestly rather than substituting a structural/arithmetic proxy without flagging that the proxy is not the same evidence"

requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]

coverage:
  - id: D1
    description: "quantile_bin_concordance added to curvature_probe.py as a tested module function reproducing 02.5-PREREGISTRATION.md Section 3g's ceiling/null/scale-invariance numbers"
    requirement: "D-01, D-02"
    verification:
      - kind: unit
        ref: "notebooks/pu_manifold/tests/test_curvature_probe.py (full suite, 144 passed)"
        status: pass
    human_judgment: false
  - id: D2
    description: "curvature_feasibility_sweep_run.py refuses to compute anything unless the pre-registration's ancestry is proved and every constant matches verbatim, then sweeps one axis at a time from BASE_CELL, computing both density_correct variants at every cell"
    requirement: "D-01, D-02, D-04, D-06"
    verification:
      - kind: other
        ref: "git log commits b316177/d9fda04/fa10956; sweep completed 25/25 distinct cells, 25.7 minutes against the 30-minute budget, stage1_key=12e5e16efd940369"
        status: pass
    human_judgment: false
  - id: D3
    description: "02.5-FINDINGS.md reports a feasibility boundary (not a bare verdict) naming where the PU regime sits and which axis is responsible, reconciles the two fixture variants, reports the density correction's per-cell effect, and reports the two estimators' agreement (or the wall-clock-limited gap where it could not be measured)"
    requirement: "D-03, D-04, D-05, D-06, D-07, D-08"
    verification:
      - kind: other
        ref: ".planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md Sections 1-8; structure-check script (all 8 sections present, 'Ollivier' named, 'recommended' no-op statement present) re-run and passing"
        status: pass
    human_judgment: false
  - id: D4
    description: "A human reads the feasibility boundary report and makes the stage-1 go/no-go decision at Task 3's blocking checkpoint"
    verification: []
    human_judgment: true
    rationale: "This is the plan's own backstop must-have and Task 3's entire purpose -- a human judgment call required by design (autonomous: false). Not yet resolved as of this SUMMARY; the checkpoint is returned to the user by this execution."

duration: "~26min sweep wall-clock (already run by a prior executor) + this session's write-up pass (findings authoring, verification, commit)"
completed: 2026-08-08
status: complete
---

# Phase 02.5 Plan 07: Stage-1 Feasibility Sweep and Findings Summary

**`CURVATURE_VERDICT = FAIL` on the base cell (`d=20, D=768, n=10000, n_bumps=8, k=30`) --
`spearman_rho=0.5205` clears its threshold but `quantile_bin_concordance=0.4444` misses
by `-0.0306`, and the sweep's own seed axis shows this FAIL is not robust: 2 of 3 tested
seeds at the identical configuration clear both gates.**

## Performance

- **Duration:** the sweep itself (Tasks 1 and the computation half of Task 2) was run to
  completion by a prior executor session before this one started -- see Task Commits
  below for that prior work's commits. This session's own work was the write-up: reading
  the completed artifacts, authoring `02.5-FINDINGS.md`, and verifying/committing it.
  Sweep wall-clock: 25.7 minutes (recorded in the cache artifact,
  `total_elapsed_s=1544.92`) against the ratified 30-minute budget.
- **Tasks:** 2 of 3 (Task 1 and Task 2's computation were completed and committed prior
  to this session; this session completed Task 2's write-up deliverable and now returns
  Task 3's blocking checkpoint, unresolved, to the user)
- **Files modified this session:** 1 (`02.5-FINDINGS.md`, created)

## Accomplishments

- `quantile_bin_concordance` added to `curvature_probe.py` as a tested module function
  (commit `8eb98fb`), implementing `02.5-PREREGISTRATION.md` Section 3e's pseudocode
- `notebooks/diagnostics/curvature_feasibility_sweep_run.py` written (commit `b316177`)
  and hardened (commits `d9fda04`, `fa10956`) to prove pre-registration ancestry and
  constant fidelity before computing anything, share the expensive centroid pass across
  both density-correction variants, and record `k <= d` cells as a distinct
  `infeasible_k_le_d` non-outcome instead of crashing mid-sweep
- The full pre-registered sweep ran to completion: 25 distinct cells (43 axis-value
  entries deduplicated), no errors, 25.7 minutes wall-clock, `stage1_key =
  12e5e16efd940369`; both `curvature_feasibility_{key}.json` and
  `curvature_verdict_stage1_{key}.json` written; no `curvature_handoff_*` artifact
  written (stage 1 correctly writes none)
- `02.5-FINDINGS.md` written (this session): the feasibility boundary per axis, the base
  cell's exact margins on both gates, the seed-instability disclosure, the
  ambient-dimension-irrelevance finding, the two fixture variants' disagreement
  (`n_bumps=1` clears, `n_bumps=8` fails), the density correction's mixed effect at
  `d=20`, and the quadric cross-check's wall-clock-limited evidence (only `d=2`
  completed; the `d(d+1)/2` vs `k` boundary reported structurally instead)
- Full `pu_manifold` test suite confirmed green: **144 passed** (baseline `140 passed` +
  4 new tests from `8eb98fb`/`b316177`)

## Task Commits

Prior executor session (already committed before this session started):

1. **Task 1: Add `quantile_bin_concordance` as a tested module function** -
   `8eb98fb` (feat)
2. **Task 1/2: Write the stage-1 sweep runner** - `b316177` (feat)
3. **[perf follow-up]** Share the expensive centroid pass across density variants -
   `d9fda04` (perf)
4. **[fix follow-up]** Record infeasible `k<=d` cells instead of crashing mid-sweep -
   `fa10956` (fix)

This session:

5. **Task 2: Write the feasibility boundary report** - `e44fb17` (docs) --
   `02.5-FINDINGS.md`

**Plan metadata:** committed alongside this SUMMARY

## Files Created/Modified

- `notebooks/pu_manifold/curvature_probe.py` - `quantile_bin_concordance` added (prior
  session)
- `notebooks/diagnostics/curvature_feasibility_sweep_run.py` - the stage-1 sweep runner
  (prior session)
- `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md` -
  the feasibility boundary write-up (this session)
- `notebooks/.cache/curvature_feasibility_12e5e16efd940369.json`,
  `notebooks/.cache/curvature_verdict_stage1_12e5e16efd940369.json` - gitignored sweep
  and verdict artifacts (prior session, read as source of truth by this session)

## Decisions Made

See `key-decisions` in frontmatter. In prose:

- **`CURVATURE_VERDICT = FAIL`** reported exactly as measured, per Section 3h's
  both-gates-required rule -- not softened, not re-derived, not hunted for a passing
  reading.
- **The FAIL is disclosed as seed-sensitive**, because the sweep's own `seed` axis shows
  the across-seed spread at the identical base configuration exceeds the base cell's own
  margin to threshold. This is reported alongside the FAIL, not instead of it.
- **Ambient dimension `D` is reported as irrelevant** to the failure (bit-identical
  results at `D=28,50,200,768`), correcting a framing risk in
  `02.5-PREREGISTRATION.md` Section 13b.
- **The quadric cross-check's absence beyond `d=2` is reported as a genuine gap**, not
  papered over with the structural coefficient-count table (which is reported
  separately, and explicitly labelled as arithmetic, not measurement).
- **Neither live option (Ollivier-Ricci, stage-2 Arm B) is chosen** -- that decision
  belongs to the user at Task 3's checkpoint, per the plan's own instruction.

## Deviations from Plan

### This session's deviation from Task 2's action text (directed, not a Rule 1-3 auto-fix)

**1. [Coordinator-directed] The sweep was not re-run a second time to verify a
byte-identical cache hit, as Task 2's action text originally specified.**
- **Found during:** Task 2 (this session).
- **Issue:** Task 2's action text calls for re-running
  `curvature_feasibility_sweep_run.py` a second time to confirm a pure cache-hit,
  byte-identical re-invocation. This session's own instructions explicitly stated the
  sweep had already run to completion and must **not** be re-run, since re-invoking the
  script (even for a cache-hit check) risks an expensive recomputation if any
  environmental factor caused an unexpected cache miss, and the prior executor's own run
  is the authoritative, already-verified artifact.
- **Resolution:** The write-up in this SUMMARY and in `02.5-FINDINGS.md` is based
  entirely on reading the on-disk artifacts (`curvature_feasibility_12e5e16efd940369.json`,
  `curvature_verdict_stage1_12e5e16efd940369.json`) as the source of truth, exactly as
  this session's instructions directed. The second-invocation cache-hit verification
  Task 2 describes was **not performed** in this session and is recorded here as a gap,
  not silently skipped -- a future session wanting that specific guarantee re-verified
  would need to run the script once more.
- **Files modified:** none (no code change; a verification step was skipped by explicit
  instruction).
- **Verification:** N/A -- documented as an intentional scope reduction directed by this
  session's own prompt, not an auto-fix under Rules 1-3.

---

**Total deviations:** 1 directed scope reduction (cache-hit re-verification skipped per
explicit instruction), 0 Rule 1-3 auto-fixes this session.
**Impact on plan:** None on the sweep's own correctness -- the artifacts were produced
by a prior, already-committed, already-tested run; this session only reads and reports
on them. The skipped second-invocation check is a verification-depth gap, not a
correctness gap.

## Issues Encountered

None beyond the documented deviation above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Task 3 (this plan's blocking `checkpoint:human-verify`, `gate="blocking"`) is
  unresolved as of this SUMMARY.** This execution stops here per its own instructions
  and returns the checkpoint to the user, exactly as `02.5-07-PLAN.md`'s Task 3
  specifies. Plans `02.5-08` through `02.5-13` do not execute until the checkpoint is
  resolved.
- On **GO**: plan `02.5-08` (chart curvature) may proceed as planned.
- On **NO-GO**: plans `02.5-08` through `02.5-12` are cancelled (not deferred); plan
  `02.5-13`'s `depends_on` must be explicitly re-pointed from `["02.5-12"]` to
  `["02.5-07"]` before it executes, per Task 3's own instructions in the PLAN.
- No sealed verdict was reopened: `GATE_VERDICT`, `CAE_VERDICT`, `TOPOAE_VERDICT` remain
  FAIL exactly as previously measured. `CURVATURE_VERDICT = FAIL` is this plan's own new,
  measured result, recorded in `curvature_verdict_stage1_12e5e16efd940369.json` and
  unedited by this write-up.
- `notebooks/pu_manifold/` test suite green: 144 passed.
- `git status --porcelain notebooks/pu_manifold/ src/effdim pyproject.toml` confirmed
  empty aside from this plan's own tracked additions -- no sealed file touched.

---
*Phase: 02.5-local-curvature-feasibility-cae-re-gate*
*Completed: 2026-08-08*

## Self-Check: PASSED

- FOUND: `notebooks/diagnostics/curvature_feasibility_sweep_run.py`
- FOUND: `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md`
- FOUND: `notebooks/.cache/curvature_feasibility_12e5e16efd940369.json`
- FOUND: `notebooks/.cache/curvature_verdict_stage1_12e5e16efd940369.json`
- FOUND commit: `8eb98fb` (feat -- quantile_bin_concordance)
- FOUND commit: `b316177` (feat -- stage-1 sweep runner)
- FOUND commit: `d9fda04` (perf -- shared centroid pass)
- FOUND commit: `fa10956` (fix -- infeasible k<=d handling)
- FOUND commit: `e44fb17` (docs -- 02.5-FINDINGS.md, this session)
