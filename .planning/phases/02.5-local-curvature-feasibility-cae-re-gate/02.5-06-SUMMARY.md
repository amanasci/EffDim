---
phase: 02.5-local-curvature-feasibility-cae-re-gate
plan: 06
subsystem: geometry
tags: [pre-registration, gating-statistic, spearman, quantile-partitioning, permutation-null, curvature-estimation]

# Dependency graph
requires:
  - phase: 02.5-04
    provides: "curvature_probe.py gate/verdict/handoff layer (STAGE1_GATING_METRICS, GATE_DIRECTIONS, write_curvature_verdict) that plan 02.5-07 will call with this document's ratified thresholds"
  - phase: 02.5-05
    provides: "the Swiss roll sanity notebook's measured read-out (per-point Spearman 0.6712, the scale-bias/noise decomposition, the region-scale diagnostic) and the explicit open question this plan was created to resolve: per-point vs region-scale gating"
provides:
  - "02.5-PREREGISTRATION.md: stage-1's gating statistics (BOTH spearman_rho_pointwise AND quantile_bin_concordance, gating independently -- option-scale-C), null-calibration protocol, every numeric constant (NULL_QUANTILE, N_PERMUTATIONS, SPEARMAN_ABSOLUTE_FLOOR, REGION_N_BINS, REGION_PAIR_COUNT, REGION_PAIR_SEED, REGION_ABSOLUTE_FLOOR, K_GATING, K_DENSITY, D_PRIMARY, ladder, n_bumps sweep, sweep shape, wall-clock budget), the fixture set, the sweep grid, and the halt-on-wrong-side consequence, ratified and committed with ancestry proof before any gated measurement runs"
  - "quantile_bin_concordance: a new, precisely-specified (pseudocode + acceptance criteria, not yet implemented) region-scale gating statistic -- cross-true-bin pairwise concordance -- that survived characterization against four rejected alternatives"
  - "A measured, disclosed finding that the graph-of-function fixture family's own heavy-tailed dynamic range (BASE_CELL max/median = 4708.8x) makes noise-oracle threshold calibration saturate for every region-scale statistic design tried, at the phase's actual gating dimension d=20"
  - "A disclosed k-sensitivity diagnostic on the PU-regime graph fixture (n=1500): per-point Spearman peaks near k=60 and declines to 0.339 at k=400; median relative error ~0.87-0.90 at every k -- evidence the estimator is likely infeasible at the PU regime for a structural (curse-of-dimensionality) reason, in hand at ratification and not used to adjust either floor"
affects: ["02.5-07"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pre-register -> ratify at a blocking checkpoint -> execute, ordering proved by git ancestry (02.2's D-10), applied to a gating-STATISTIC choice, not just numeric thresholds"
    - "When a proposed statistic design is rejected as saturated at a checkpoint, characterize multiple candidates end-to-end against the real module functions (not scratch approximations) before re-proposing, and report every rejected candidate's failure mode rather than only the survivor"
    - "Disclose in-hand evidence at ratification time explicitly, name what was and was not adjusted in response to it, and state this is the point where post-hoc pressure could otherwise enter unnoticed (D-11's principle, generalized beyond its original stage-2 scope)"
    - "A threshold derived by mapping a stated tolerance through a statistic's own noise-oracle ceiling table, computed on the fixture the technique is validated on, with an explicit statement of which fixture the technique does NOT transfer to and why"

key-files:
  created:
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PREREGISTRATION.md
  modified: []

key-decisions:
  - "Decision 2 (numeric constants): ratified as drafted (option-a), no amendment -- NULL_QUANTILE=0.99, N_PERMUTATIONS=999, SPEARMAN_ABSOLUTE_FLOOR=0.50 (ratified with the ~50% per-point-noise tolerance it encodes stated explicitly), K_GATING=30, K_DENSITY=30, D_PRIMARY=20 (D_FROZEN=5 barred), ladder {8,16,20,24,32,40}, n_bumps sweep {1,8}, one-axis-at-a-time sweep shape, 30-minute wall-clock budget"
  - "Decision 1 (gating-statistic scale) required a full redraft: the first proposal (spearman_rho_quantile_region, a Spearman correlation of quantile-bin labels against bin-median true values) was returned at checkpoint as saturated on the graph-of-function fixture -- it could pass a useless estimator. Five candidate replacement designs were characterized end-to-end against curvature_probe.py's real functions on both the Swiss roll and the phase's own BASE_CELL fixture before quantile_bin_concordance (cross-true-bin pairwise concordance) was proposed as the one that both diverges materially from raw per-point Spearman and shows genuine non-saturating dynamic range on the Swiss roll"
  - "Ratified option-scale-C: spearman_rho_pointwise AND quantile_bin_concordance gate INDEPENDENTLY (both must clear their own null-calibrated threshold and their own absolute floor for a cell to PASS), per the user's stated rationale that the gate's job is attribution rather than selection given the estimator is likely infeasible at the PU regime, and the divergence between the two statistics is itself diagnostic of a structural (not noise-shaped) failure"
  - "REGION_ABSOLUTE_FLOOR=0.4750 derived by mapping the SAME ~50%-per-point-noise tolerance SPEARMAN_ABSOLUTE_FLOOR encodes through quantile_bin_concordance's own noise-oracle ceiling table, computed on the Swiss roll (the same fixture the per-point floor was derived from) -- not on BASE_CELL, because BASE_CELL's own heavy tail saturates the noise-oracle technique for every design tried (measured, not assumed)"
  - "Scope choice, flagged explicitly: quantile_bin_concordance is specified in the pre-registration by exact pseudocode plus numeric acceptance criteria (ceiling table, null, scale-invariance check) that plan 02.5-07 must reproduce when it implements the tested module function -- curvature_probe.py is NOT touched by this plan, keeping files_modified scoped to the pre-registration document alone"
  - "Mandatory D-11-style disclosure added before commit: a k-sensitivity measurement on the phase's own graph-of-function fixture at the PU regime (n=1500, pessimistic relative to BASE_CELL's n=10000) was in hand at ratification and is reproduced verbatim in a new Section 13b -- neither absolute floor nor the option-scale-C choice was adjusted in response to it, stated explicitly in the record"

patterns-established:
  - "When a proposed gating statistic is rejected as saturated, do not patch the same design -- build and characterize multiple structurally different candidates (contrast-of-medians, rank-on-coarse-labels, cross-bin pairwise concordance) against the real codebase functions, and report every rejected candidate's specific failure mode as part of the record, not just the survivor's success"
  - "A noise-oracle calibration technique validated on one fixture does not automatically transfer to a harder fixture in the same family -- test it explicitly on both before trusting a threshold derived from only one, and state plainly which term of a max(null, floor)-style threshold is actually load-bearing where the technique breaks down"

requirements-completed: [D-00, D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-12]

coverage:
  - id: D1
    description: "A stage-1 pre-registration document exists that fixes the gating statistic(s), null-calibration protocol, every numeric constant, the fixture set, the sweep grid, and the halt-on-wrong-side consequence, before any gated measurement runs (D-01..D-08)"
    requirement: "D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08"
    verification:
      - kind: other
        ref: "python structure-check script (sections 1-15, all constants named, D-00..D-08/D-12 cited, Ollivier named, no stray 'recommended k') -- see plan's <verify> block, re-run after every edit"
        status: pass
    human_judgment: false
  - id: D2
    description: "The document states D-00's correction in its own words: the d(d+1)/2 coefficient count is the cost of the full second fundamental form, mean curvature is its trace, and the trace is one unknown averaged over k samples"
    requirement: "D-00"
    verification:
      - kind: other
        ref: "02.5-PREREGISTRATION.md Section 2"
        status: pass
    human_judgment: false
  - id: D3
    description: "The document names d=20 as the gating working dimension with the sensitivity ladder {8,16,20,24,32,40} reported non-gating, and states why D_FROZEN=5 is barred from inheritance"
    requirement: "D-07"
    verification:
      - kind: other
        ref: "02.5-PREREGISTRATION.md Section 7; grep -c D_FROZEN and 'not inherited' both present"
        status: pass
    human_judgment: false
  - id: D4
    description: "The document pre-registers the estimator's own neighbourhood rule with an explicit bias-variance justification rather than inheriting k*=15"
    requirement: "D-08"
    verification:
      - kind: other
        ref: "02.5-PREREGISTRATION.md Section 8; 'recommended k' appears exactly once, in the sentence stating none is emitted"
        status: pass
    human_judgment: false
  - id: D5
    description: "The document names the density correction and its single constant K_DENSITY up front, and states it is proven on deliberately non-uniform fixtures"
    requirement: "D-06"
    verification:
      - kind: other
        ref: "02.5-PREREGISTRATION.md Section 6"
        status: pass
    human_judgment: false
  - id: D6
    description: "The document states that on the wrong side of the boundary the phase halts for a user decision with no auto-fallback, and names Ollivier-Ricci as the live remaining option; emits no recommended k"
    requirement: "D-04"
    verification:
      - kind: other
        ref: "02.5-PREREGISTRATION.md Section 10; grep -c Ollivier -> present"
        status: pass
    human_judgment: false
  - id: D7
    description: "The document carries D-12's neither-clears-the-null branch forward as an explicit named open item for stage 2's own ratification, rather than resolving it here"
    requirement: "D-12"
    verification:
      - kind: other
        ref: "02.5-PREREGISTRATION.md Section 11"
        status: pass
    human_judgment: false
  - id: D8
    description: "The document discloses that the Swiss roll sanity notebook had already been run before it was written, and records its four measured numbers; additionally discloses a k-sensitivity diagnostic on the PU-regime graph fixture that was in hand at ratification"
    requirement: "D-03"
    verification:
      - kind: other
        ref: "02.5-PREREGISTRATION.md Section 13 (Swiss roll disclosure) and Section 13b (k-sensitivity disclosure, added per the coordinator's mandatory D-11 requirement before commit)"
        status: pass
    human_judgment: false
  - id: D9
    description: "The document is committed alone, and its commit is proved an ancestor of HEAD by git merge-base --is-ancestor, with the SHA recorded in the document's own record section"
    verification:
      - kind: other
        ref: "git merge-base --is-ancestor eecdb15c70dc3f833e31c54ec7864543579dddb7 HEAD -> exit 0; git show --name-only eecdb15... -> only the PREREGISTRATION.md file"
        status: pass
    human_judgment: false
  - id: D10
    description: "A human ratified every numeric constant AND the gating-statistic-scale question at a blocking checkpoint before the document was committed, and the checkpoint recorded what was known at the time it was made"
    verification: []
    human_judgment: true
    rationale: "This is the plan's own backstop must-have -- a human judgment call required by design. Resolved across two checkpoint rounds: Decision 2 (numeric constants) ratified as option-a on the first round; Decision 1 (gating-statistic scale) was returned for redraft on the first round (the initial proposal was rejected as saturated) and ratified as option-scale-C on the second round, with the mandatory k-sensitivity disclosure recorded in Section 13b before commit."

duration: ~66min wall-clock across two checkpoint rounds (active characterization/drafting work; excludes the coordinator's own review time between rounds)
completed: 2026-08-08
status: complete
---

# Phase 02.5 Plan 06: Stage-1 Pre-Registration Summary

**Stage-1's gate is fixed in a ratified, ancestry-proved document: BOTH per-point Spearman and a newly-designed, characterized-from-scratch region-scale statistic (`quantile_bin_concordance`, cross-true-bin pairwise concordance) gate independently -- option-scale-C -- after a first region-statistic proposal was rejected at checkpoint as saturated and a k-sensitivity diagnostic in hand at ratification (rho declining from k=60 to k=400, median relative error ~0.87-0.90 at every k on the PU-regime fixture) was disclosed and left unused to move either floor.**

## Performance

- **Duration:** ~66 min wall-clock (2026-08-08T08:35 -> 09:41), across two checkpoint rounds: Task 1 drafted and verified, held at Task 2's blocking checkpoint for Decision 2 (ratified as-drafted) and Decision 1 (returned for redraft); Task 1 redrafted with a newly-characterized region statistic and held again at checkpoint; Decision 1 ratified as option-scale-C with a mandatory pre-commit disclosure requirement; Task 3 committed the document alone and proved ancestry in a follow-up commit
- **Tasks:** 3 (Task 1 auto -- drafted, then redrafted; Task 2 checkpoint:decision -- two ratification rounds; Task 3 auto -- commit + ancestry proof)
- **Files modified:** 1 (`02.5-PREREGISTRATION.md`)

## Accomplishments

- `02.5-PREREGISTRATION.md` (803 lines, 15 sections plus 13b): fixes stage 1's gating statistics, null-calibration protocol, every numeric constant, the fixture set, the sweep grid, and the halt-on-wrong-side consequence, following 02.2's D-10 pre-register-ratify-execute-prove-ancestry discipline
- **Resolved the per-point-vs-region gating question 02.5-05 explicitly deferred to this plan.** The first proposed region statistic (`spearman_rho_quantile_region`) was rejected at checkpoint as saturated -- it could pass a useless estimator on the graph-of-function fixture. Five candidate replacement designs (bin-median contrast in linear and log scale, bin-trend Spearman, double-binned Spearman, cross-bin concordance) were characterized end-to-end against `curvature_probe.py`'s real functions on both the Swiss roll and the phase's own `BASE_CELL` graph-of-function fixture (`d=20, D=768, n=10000, n_bumps=8, k=30`); `quantile_bin_concordance` was the one design that both diverges materially from raw per-point Spearman and shows genuine, non-saturating dynamic range on the Swiss roll's noise-oracle ceiling table
- **Ratified option-scale-C:** `spearman_rho_pointwise` and `quantile_bin_concordance` gate independently -- a cell PASSES only if both clear their own null-calibrated threshold and their own absolute floor. The user's rationale, recorded in Section 3h: with the estimator likely infeasible at the PU regime, the gate's job is attribution rather than selection, and the divergence between the two statistics is itself diagnostic of a structural (not noise-shaped) failure
- `REGION_ABSOLUTE_FLOOR = 0.4750`, derived (not invented) by mapping the same ~50%-per-point-noise tolerance `SPEARMAN_ABSOLUTE_FLOOR = 0.50` encodes through `quantile_bin_concordance`'s own Swiss-roll noise-oracle ceiling table -- disclosed honestly that the `BASE_CELL` graph-fixture characterization value (`0.4483`) sits below this floor, and that this was not used to move it
- **A measured, disclosed limitation:** every region-scale statistic design tried saturates under noise-oracle calibration on `BASE_CELL`'s own heavy-tailed true-curvature field (max/median `4708.8x`, vs the Swiss roll's `3.05x`) -- a property of the fixture, not of any one statistic. At `d=20`, the `null_threshold` term rather than the floor is what is actually load-bearing (Section 12(g), Section 3g)
- **Mandatory pre-commit disclosure (Section 13b):** a k-sensitivity measurement on the phase's own graph-of-function fixture at the PU regime (`n=1500`), in hand at ratification: per-point Spearman peaks near `k=60` (`0.4549`) and *declines* to `0.3385` at `k=400` (the bias-variance turn where the neighbourhood outgrows the local quadratic regime); median relative error `~0.87-0.90` at every tested `k`. Recorded explicitly that neither absolute floor nor the option-scale-C choice was adjusted in response
- Full `pu_manifold` test suite: **135 passed** (unchanged baseline, `curvature_probe.py` and every other sealed file untouched by this plan)
- Document committed alone (Task 3, `eecdb15c`), `git merge-base --is-ancestor eecdb15c... HEAD` exits `0`, the SHA and ancestry-proof output recorded back into Section 15 in a follow-up commit (`6d178f0`) that touched only the Record section

## Task Commits

Each task was committed atomically:

1. **Task 1: Draft the stage-1 pre-registration** (drafted, then redrafted twice per checkpoint feedback) -- no commit until ratified, by the plan's own design (D-10 ordering)
2. **Task 2: Ratify the stage-1 constants** -- `checkpoint:decision`, resolved across two rounds: Decision 2 (option-a) and Decision 1 (returned for redraft, then ratified option-scale-C)
3. **Task 3: Commit the pre-registration alone and prove git ancestry** -- `eecdb15c` (docs, the document itself) + `6d178f0` (docs, the ancestry-proof follow-up touching only Section 15)

**Plan metadata:** committed alongside this SUMMARY

## Files Created/Modified

- `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PREREGISTRATION.md` - stage-1's complete pre-registration: trigger, D-00's correction, gating statistics (both), fixtures, estimator, density correction, working dimension, neighbourhood rule, sweep design, wrong-side consequence, D-12 carry-forward, limitations, prior-work disclosure (Swiss roll + k-sensitivity), amendment-consequence template, and the fully-filled Record section

## Decisions Made

See `key-decisions` in frontmatter for the full list. Summary:

- **Decision 2 (numeric constants): ratified as drafted, option-a.** No amendment.
- **Decision 1 (gating-statistic scale): required a redraft before ratification.** The first proposal was rejected as saturated; the redrafted `quantile_bin_concordance` design was characterized against four rejected alternatives before being re-proposed and ratified as **option-scale-C** (both statistics gate independently).
- **`REGION_ABSOLUTE_FLOOR` derivation:** mapped from the same 50%-noise tolerance as the per-point floor, computed on the Swiss roll specifically because the graph-of-function `BASE_CELL` fixture's own heavy tail was measured to saturate the noise-oracle technique for every design tried -- a finding disclosed rather than hidden.
- **Scope: pseudocode-only specification, not implementation.** `quantile_bin_concordance` is fully specified with acceptance criteria for plan 02.5-07 to implement and test; `curvature_probe.py` is not touched by this plan.
- **Mandatory disclosure before commit:** a k-sensitivity diagnostic on the PU-regime fixture, in hand at ratification, added to Section 13b per the coordinator's explicit D-11-principle requirement -- disclosed in full, and stated explicitly that it did not move either floor or the option-scale-C choice.

## Deviations from Plan

### Checkpoint-directed changes (not Rule 1-3 auto-fixes)

**1. [Coordinator-directed redraft] Decision 1's first proposal rejected as saturated; region statistic redesigned from scratch**
- **Found during:** Task 2's first checkpoint round.
- **Issue:** The originally-drafted region statistic (`spearman_rho_quantile_region`, a Spearman correlation between quantile-bin labels and bin-median true values) was shown by the coordinator's own measurement to saturate near 1.0 across essentially the entire noise range while the null sat far below -- it could pass a useless estimator, exactly the opposite failure mode Decision 1 exists to avoid.
- **Fix:** Characterized five candidate designs end-to-end against `curvature_probe.py`'s real functions (not scratch approximations) on both the Swiss roll and the phase's own `BASE_CELL` graph-of-function fixture; four were rejected (documented with their specific failure modes in Section 3f); `quantile_bin_concordance` was proposed as the survivor, with its full ceiling table, null characterization, and scale-invariance check reported.
- **Files modified:** `02.5-PREREGISTRATION.md` (Sections 3e-3h rewritten; Section 12(g) added).
- **Verification:** Re-ran the plan's structure-check script after every edit; confirmed no stray references to the rejected statistic remained (one intentional mention in Section 15's record of what was tried and rejected).
- **Committed in:** `eecdb15c` (bundled into the ratified document, since Task 1 is not committed until ratification per this plan's own design).

**2. [Coordinator-directed disclosure] Mandatory k-sensitivity diagnostic added to Section 13b before commit**
- **Found during:** Task 2's second checkpoint round, immediately before Task 3's commit.
- **Issue:** The coordinator had run a k-sensitivity measurement on the phase's own PU-regime graph fixture before ratifying Decision 1, and required it disclosed in the document per this document's own §13/D-11 disclosure discipline -- a pre-registration that hides what was known at ratification time is not a pre-registration.
- **Fix:** Added Section 13b reproducing the measurement verbatim (fixture parameters, the five-row `k` table, and five stated findings), and explicitly recorded in Section 15 that neither floor nor the option-scale-C choice was adjusted in response to it.
- **Files modified:** `02.5-PREREGISTRATION.md` (Section 13b added; Section 15 updated).
- **Verification:** Structure-check re-run; `git status --porcelain notebooks/ src/effdim pyproject.toml` confirmed empty (no sealed file touched by the disclosure).
- **Committed in:** `eecdb15c`.

---

**Total deviations:** 0 Rule 1-3 auto-fixes. 2 coordinator-directed changes at the Task 2 checkpoint (a required redraft, and a required disclosure) -- both are exactly the human-in-the-loop mechanism this plan's `autonomous: false` / blocking-checkpoint design exists to produce, not deviations from it.
**Impact on plan:** Substantially strengthened the pre-registration's honesty and rigor relative to the first draft -- the ratified gate now carries a genuinely characterized, honestly-caveated region statistic instead of a saturated one, and the record shows exactly what was known and not acted on at ratification time.

## Issues Encountered

None beyond the checkpoint-directed redraft and disclosure documented above.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Plan 02.5-07** (stage-1 GO/NO-GO sweep) can now proceed. It must:
  1. Add a TESTED module function to `curvature_probe.py` implementing `quantile_bin_concordance` exactly per Section 3e's pseudocode, reproducing Section 3g's ceiling/null/scale-invariance numbers as its own acceptance criteria.
  2. Gate every sweep cell on **both** `spearman_rho_pointwise` and `quantile_bin_concordance` independently (option-scale-C) -- a cell PASSES only if both clear.
  3. Run the full sweep (every axis in Section 9, the complete `D_PRIMARY` ladder, 3 base-cell seeds) regardless of what Section 13b's k-sensitivity diagnostic suggests -- that diagnostic anticipates but does not substitute for the pre-registered sweep.
  4. Expect (per Section 13b's disclosed findings) that `k` sensitivity on the `d=20` regime may show a peak-then-decline shape rather than a monotone improvement, and that magnitude recovery (`median_relative_error`) will likely be poor (~0.87-0.90) regardless of `k` -- neither is a bug if reproduced.
- No sealed file touched: `cae.py`, `topoae.py`, `curvature.py`, `curvature_probe.py`, `cache.py`, `mknn.py`, `geometry_probes.py`, `subsample.py`, `src/effdim/`, and `pyproject.toml` all confirmed unmodified (`git status --porcelain` empty) across both commits in this plan.
- D-12's "neither clears the null" branch remains carried forward, unresolved, for plan 02.5-10's own stage-2 ratification checkpoint.
- Full `pu_manifold` test suite confirmed green: 135 passed, matching the baseline this plan inherited.

---
*Phase: 02.5-local-curvature-feasibility-cae-re-gate*
*Completed: 2026-08-08*

## Self-Check: PASSED

- FOUND: `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PREREGISTRATION.md`
- FOUND commit: `eecdb15c70dc3f833e31c54ec7864543579dddb7` (Task 3, document commit)
- FOUND commit: `6d178f04fd8f2fd59cc8c06b406da8b5070e184f` (Task 3, ancestry-proof follow-up)
