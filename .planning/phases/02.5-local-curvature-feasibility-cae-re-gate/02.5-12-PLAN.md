---
phase: 02.5-local-curvature-feasibility-cae-re-gate
plan: 12
type: execute
wave: 11
depends_on: ["02.5-11"]
files_modified:
  - notebooks/diagnostics/cae_local_regate_run.py
autonomous: false
requirements: [D-10, D-11, D-12, D-15]
must_haves:
  truths:
    - "Gate B is measured: the three sealed 02.2 fits' chart-decoder curvature fields over Phase 1's frozen 10,000-row subsample are compared pairwise, and seed_spread is one minus the minimum pairwise Spearman (D-10, D-12)."
    - "The stage-2 verdict is computed by curvature_probe.verdict_from_stage2_metrics from both gates, and written on PASS and on FAIL alike, with the stored verdict unable to disagree with its own gates (D-12, D-15)."
    - "The Phase 3 handoff is written only on PASS, and it names the full estimator contract — substrate, working dimension, neighbourhood rule, density correction, estimator variant, curvature convention, cache stems, fit_key, gate values, thresholds, and the pre-registration SHA (D-15)."
    - "On any non-PASS outcome, any stale handoff and its .meta.json sidecar are actively deleted (D-15)."
    - "D-11's retrain trigger is evaluated against its pre-registered condition, and a retrain runs only after a blocking human-verify checkpoint that records what was known when it was made (D-11)."
    - "The outcome is routed by D-12's ratified rule — charts win, raw points win or tie, or the neither-clears branch resolved at plan 02.5-10's checkpoint — and no fallback is selected automatically (D-12)."
    - "The sealed CAE_VERDICT = FAIL is untouched; this measures a different property of the same artifacts (D-10)."
    - statement: "A human read the sealed stage-2 verdict and its routing, confirmed no threshold or rule was adjusted after the measurement, and recorded the decision."
      verification: backstop
  artifacts:
    - notebooks/diagnostics/cae_local_regate_run.py
  key_links:
    - "three sealed seeds -> three ||H|| fields over the same frozen subsample -> pairwise Spearman -> seed_spread: this is the only measurement that tests the CAE's actual claim, since noise-averaging that varies across seeds is not noise-averaging."
    - "verdict != PASS -> clear_stale_curvature_handoff -> both .json and .meta.json unlinked: without this, a PASS handoff from an earlier run survives a later FAIL and Phase 3 reads it as live."
  prohibitions:
    - statement: "No threshold, constant, or rule is adjusted after any Gate A or Gate B number is seen; a needed change requires a fresh amendment and a full re-run"
      status: planned
      verification: manual
    - statement: "The sealed CAE_VERDICT = FAIL, GATE_VERDICT = FAIL, and TOPOAE_VERDICT = FAIL are not reopened, softened, or recomputed"
      status: planned
      verification: manual
    - statement: "The three sealed 02.2 fits are read only; they are never retrained, overwritten, or re-keyed, including by a fired D-11 retrain, which fits new artifacts under new stems"
      status: planned
      verification: manual
    - statement: "A retrain never runs without its own blocking human-verify checkpoint ratifying its hyperparameters first"
      status: planned
      verification: manual
    - statement: "No fallback is selected automatically on any non-PASS branch"
      status: planned
      verification: manual
    - statement: "notebooks/pu_manifold/cae.py, topoae.py, curvature.py, cache.py are imported and never edited; src/effdim/ and pyproject.toml are untouched"
      status: planned
      verification: manual
---

<objective>
Measure Gate B on the three sealed fits, seal the stage-2 verdict, write or clear the Phase 3
handoff, and route the outcome by D-12's ratified rule.

Purpose: Gate B is the clause D-12 makes material for a specific reason — the CAE's claimed
advantage **is** noise-averaging, and a curvature field that varies across the three sealed seeds
would refute exactly that claim, regardless of how well Gate A went. And this is where the R6
contract has to hold: a verdict on either outcome, a handoff only on PASS, and any stale handoff
actively deleted, because a stale PASS surviving a FAIL is read by Phase 3 as live.

Output: the completed `cae_local_regate_run.py`, the sealed
`curvature_verdict_stage2_43cf438bc944c509.json`, the handoff or its deletion, and a resolved
routing decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-CONTEXT.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-RESEARCH.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PREREGISTRATION-STAGE2.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-11-SUMMARY.md
@CLAUDE.md
</context>

<preconditions>
Plan 02.5-11 completed: `curvature_gate_a_{gate_a_key}.json` exists and the three sealed fit
artifacts are verified unmodified. Halt if either is not the case.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Measure Gate B — seed stability across the three sealed PU fits</name>
  <files>notebooks/diagnostics/cae_local_regate_run.py</files>
  <precondition>Phase 1's frozen 10,000-row subsample artifact and the three sealed cae_fit_43cf438bc944c509_seed* artifacts exist under notebooks/.cache/ and pass their manifest checks.</precondition>
  <read_first>
    - notebooks/diagnostics/cae_local_regate_run.py (as written by plan 02.5-11)
    - notebooks/pu_manifold/cae.py lines 25-43 (FIT_ARTIFACT_CONTRACT — the npz keys, including `z_all`, `p_all`, `chart_argmax_all`, `holdout_idx`) and 1143-1152 (arrays_to_state_dict)
    - notebooks/pu_manifold/chart_curvature.py (`chart_curvature_field`, `assert_c2_activation`)
    - notebooks/pu_manifold/subsample.py (how Phase 1's frozen 10,000-row subsample is loaded and keyed)
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PREREGISTRATION-STAGE2.md §3 Gate B and §5 `THRESH_SEED_SPREAD`
  </read_first>
  <action>
Add STEP 3 to `notebooks/diagnostics/cae_local_regate_run.py`.

Load Phase 1's frozen 10,000-row subsample under `SUBSAMPLE_STEM` through the `cache` helpers so
its manifest check runs. This is the identical row set all three sealed fits were trained on, and
using anything else would make the three fields incomparable.

For each of `SEALED_SEEDS`: reconstruct the `ChartAutoEncoder` from
`cae_fit_43cf438bc944c509_seed{seed}.npz` via `cae.arrays_to_state_dict` at the architecture
recorded in its companion meta json, call `chart_curvature.assert_c2_activation`, then run
`chart_curvature.chart_curvature_field(model, X_subsample)` and take
`chart_mean_curvature_norm`. Read only — never write back to any sealed stem.

Compute all three pairwise Spearman correlations between the three `||H||` fields over the same
row order, and set `seed_spread = 1.0 - min(pairwise)`. Record all three pairwise values, not just
the minimum, so a single divergent seed is distinguishable from three-way disagreement.

Record as non-gating context per seed: the median and maximum metric condition number, the number
of charts used, the chart-assignment distribution, and the fraction of rows whose assigned chart
differs between seeds — because if two seeds partition the manifold into different atlases, a low
pairwise Spearman may be an atlas difference rather than a curvature disagreement, and that
distinction belongs in the record rather than in a later argument.

Guard: halt naming the seed if any field contains a non-finite value, or if any pairwise Spearman
is non-finite (which happens if a field is constant).

Persist STEP 3 through `cache.json_cache(f"curvature_gate_b_{FIT_KEY}", cfg, _compute)` with `cfg`
carrying every relevant constant plus
`{"phase": "02.5", "stage": 2, "component": "gate_b", "prereg_sha": STAGE2_PREREG_SHA}`.

Print a Gate B read-out: the three pairwise Spearman values, `seed_spread`,
`THRESH_SEED_SPREAD`, and whether it clears — labelled as **Gate B only, not the verdict**.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; python -c "
import ast
src=open('notebooks/diagnostics/cae_local_regate_run.py').read(); ast.parse(src)
code='\n'.join(l for l in src.splitlines() if not l.strip().startswith('#'))
for c in ['seed_spread','curvature_gate_b_','SUBSAMPLE_STEM','arrays_to_state_dict','assert_c2_activation','pairwise']:
    assert c in code, f'missing {c}'
print('gate B step OK')
" &amp;&amp; PYTHONPATH=notebooks python notebooks/diagnostics/cae_local_regate_run.py 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/.cache/curvature_gate_b_43cf438bc944c509.json` exists and records all three pairwise Spearman values plus `seed_spread`
    - The three sealed fit artifacts' hashes are unchanged before and after the run
    - Behavior: a non-finite or constant field halts with a message naming the seed
    - The per-seed chart-assignment agreement fraction is recorded as context alongside the pairwise Spearman values
    - `ls notebooks/.cache | grep -c 'curvature_verdict_stage2_\|curvature_handoff_'` still returns 0 at the end of this task
  </acceptance_criteria>
  <done>The three sealed fits' curvature fields are measured over the identical frozen subsample, all three pairwise agreements are recorded, seed_spread is computed, and the atlas-difference-versus-curvature-disagreement distinction is captured in the artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Seal the stage-2 verdict, write or clear the handoff, and evaluate D-11's trigger</name>
  <files>notebooks/diagnostics/cae_local_regate_run.py</files>
  <read_first>
    - notebooks/diagnostics/cae_local_regate_run.py (as written by Task 1)
    - notebooks/pu_manifold/curvature_probe.py (`verdict_from_stage2_metrics`, `write_curvature_verdict`, `write_curvature_handoff`, `clear_stale_curvature_handoff`, `STAGE2_GATING_METRICS`, `GATE_DIRECTIONS`, `STAGE2_VERDICT_RULE`)
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PREREGISTRATION-STAGE2.md §6 (the ratified routing, including the neither-clears resolution), §7 (D-11's trigger), §8 (the handoff contract)
    - notebooks/diagnostics/topoae_evaluate_run.py (how 02.4's runner sequenced verdict-then-handoff-or-clear, and its `gate_scope` annotation)
  </read_first>
  <action>
Add STEP 4 to the runner.

**Assemble the metrics.** `metrics = {"chart_vs_raw_margin": <from Gate A>, "seed_spread": <from
Gate B>}`, `thresholds = {"chart_vs_raw_margin": THRESH_MARGIN, "seed_spread": THRESH_SEED_SPREAD}`.
Compute `verdict, gate_detail = curvature_probe.verdict_from_stage2_metrics(metrics, thresholds)` —
the strict, direction-aware comparison happens only there, never re-derived inline here.

**Write the verdict, always.** Call
`curvature_probe.write_curvature_verdict(key=FIT_KEY, stage=2, metrics=metrics,
thresholds=thresholds, verdict=verdict, extra=<the full non-gating context>)`. The `extra` block
carries everything reported but never gated: Arm A's Spearman, the per-seed Arm B Spearman table,
the margin's min/max/range across fixture seeds, all three Gate B pairwise Spearman values, the
per-seed reconstruction statistics, the metric condition-number summaries, the chart-assignment
agreement fractions, the quadric cross-check and `estimator_agreement`, and the median relative
errors. Reconstruction quality and curvature agreement stay separate keys — never combined.
`write_curvature_verdict` recomputes the verdict internally and refuses to write if it disagrees;
that is deliberate and must not be bypassed.

**The handoff, on PASS only.** If `verdict == "PASS"`, call
`curvature_probe.write_curvature_handoff(key=FIT_KEY, verdict=verdict, payload=...)` with every
field §8 enumerates: `substrate="cae_charts"`, `working_dimension=GATE_A_CELL["d"]`,
`neighbourhood_rule={"k": ..., "justification": <the bias-variance prose from the pre-registration>}`,
`density_correction={"enabled": True, "k_density": ...}`,
`estimator_variant="centroid_laplace_beltrami+chart_decoder_autodiff"`,
`curvature_convention=curvature_probe.CURVATURE_CONVENTION`, `cache_stems=<the three sealed stems,
the three fixture-CAE stems, the subsample stem, and both Gate artifacts>`, `fit_key=FIT_KEY`,
`gate_values`, `thresholds`, `evidence_criteria` (prose naming what Phase 3 should judge the
substrate by), `preregistration_sha=STAGE2_PREREG_SHA`, and `activation`. Naming the substrate
alone would be insufficient — D-15's stated reason is that Phase 3 could otherwise reconstruct the
field under different settings than the ones that passed the gate.

**Clear the stale handoff, on any non-PASS.** If `verdict != "PASS"`, call
`curvature_probe.clear_stale_curvature_handoff(FIT_KEY)` and print whether anything was removed.
This is R6's active deletion and it must run on every non-PASS path, not only the ones anticipated.

**Annotate the scope.** The verdict payload carries `gate_scope = "local"`, following the additive
annotation plan 02.4-07 added to its own sealed verdict — every gate in this phase is a local
neighbourhood statistic, and a bare verdict string would lose that.

**Evaluate D-11's trigger and stop.** Compute the pre-registered `RETRAIN_TRIGGER` condition: Arm B's
Spearman below stage 1's null-calibrated threshold for the Gate A cell **while** Arm A's clears it.
Print whether it fired. Do **not** retrain. If it fired, print the message that a retrain requires
Task 3's blocking human-verify checkpoint to ratify its hyperparameters first, per D-11, and that
the residual risk — the hyperparameters would be ratified with the failing result already visible —
is recorded in `02.5-PREREGISTRATION-STAGE2.md` §7.

**Print the routing.** State the verdict, both gate values against both thresholds with their
directions, and which of D-12's three branches applies under the rule ratified at plan 02.5-10's
checkpoint: charts win, raw points win or tie inside the margin, or the neither-clears branch.
Print the branch's consequence in the ratified wording. Select nothing automatically.

Adjust no threshold, constant, or rule. If any number looks wrong, that is a finding for the
checkpoint, not a reason to edit — `02.5-PREREGISTRATION-STAGE2.md` §10 states the consequence: a
fresh, separately-ratified amendment committed alone with its own ancestry proof, and a full
re-run.

Run the completed script and commit it.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; PYTHONPATH=notebooks python notebooks/diagnostics/cae_local_regate_run.py 2>&amp;1 | tail -40 &amp;&amp; python -c "
import json,glob,sys
v=glob.glob('notebooks/.cache/curvature_verdict_stage2_*.json')
assert len(v)==1, f'expected exactly one stage-2 verdict, found {len(v)}'
d=json.load(open(v[0]))
assert d['CURVATURE_VERDICT'] in ('PASS','FAIL')
assert set(d['gating_metrics'])=={'chart_vs_raw_margin','seed_spread'}
assert d.get('gate_scope'), 'gate_scope annotation missing'
h=glob.glob('notebooks/.cache/curvature_handoff_*.json')
if d['CURVATURE_VERDICT']=='PASS':
    assert len(h)==1, 'PASS must write exactly one handoff'
    p=json.load(open(h[0]))
    for k in ['substrate','working_dimension','neighbourhood_rule','density_correction','estimator_variant','curvature_convention','cache_stems','fit_key','gate_values','thresholds','evidence_criteria','preregistration_sha','activation']:
        assert k in p, f'handoff missing contract field {k}'
else:
    assert len(h)==0, 'non-PASS must leave no handoff on disk'
print('verdict/handoff contract OK:', d['CURVATURE_VERDICT'])
"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one `curvature_verdict_stage2_43cf438bc944c509.json` exists, carrying both gating metric names, both thresholds, `gate_detail` with directions, `verdict_rule`, `gate_scope`, and a `context_metrics` block
    - On PASS: exactly one `curvature_handoff_43cf438bc944c509.json` exists carrying all thirteen contract fields §8 enumerates
    - On non-PASS: no `curvature_handoff_*` file and no `curvature_handoff_*.meta.json` sidecar exists on disk
    - The `RETRAIN_TRIGGER` boolean is printed and recorded; no retrain has run
    - Reconstruction statistics and curvature Spearman appear under separate keys in `context_metrics`
    - The three sealed PU fit artifacts' hashes are unchanged
    - `git diff` shows no change to any threshold or constant in `cae_local_regate_run.py` after the first run
    - `git status --porcelain notebooks/pu_manifold/ src/effdim pyproject.toml` produces no output
  </acceptance_criteria>
  <reversibility rating="one-way">Sealing the stage-2 verdict fixes D-12's outcome for the record. Once written, the verdict artifact is a measured result and joins the milestone's three sealed FAILs as something that is never reopened, softened, or recomputed. Task 3's blocking checkpoint is the gate.</reversibility>
  <done>The stage-2 verdict is computed from both gates by the tested comparison function and written on either outcome; the handoff naming the full estimator contract is written on PASS only and any stale handoff is actively deleted otherwise; D-11's trigger is evaluated and recorded without acting on it; and the outcome is routed by the ratified rule with nothing selected automatically.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Seal the stage-2 verdict and route the outcome</name>
  <what-built>
Stage 2 is complete. `notebooks/diagnostics/cae_local_regate_run.py` measured both gates under
`02.5-PREREGISTRATION-STAGE2.md`, whose commit is proved an ancestor of the run:

- **Gate A** (`chart_vs_raw_margin`, strictly greater than `THRESH_MARGIN`): the raw-point centroid
  estimator and the CAE chart-decoder field, both scored by Spearman against the same
  analytic-`H` fixture at the PU-matched regime (`d = 20`, `D = 768`, `n = 10,000`), with the
  fixture CAEs fitted at the three sealed fits' verbatim architecture and a drift check enforcing
  it.
- **Gate B** (`seed_spread`, strictly less than `THRESH_SEED_SPREAD`): the three sealed 02.2 fits'
  curvature fields over Phase 1's frozen 10,000-row subsample, compared pairwise.

`curvature_verdict_stage2_43cf438bc944c509.json` is written with both gates, their thresholds and
directions, the `gate_scope = "local"` annotation, and a full non-gating `context_metrics` block.
The handoff is written or the stale one deleted per R6.
  </what-built>
  <how-to-verify>
1. Open `curvature_verdict_stage2_43cf438bc944c509.json`. Read `gate_detail` for both gates: the
   measured value, the threshold, the direction, and the boolean. Confirm the verdict follows from
   them under `verdict_rule` — the writer recomputes and refuses to disagree, but read it anyway.
2. Read `context_metrics`. Look specifically at the pair D-09 warns about: the per-seed
   reconstruction statistics beside the per-seed curvature Spearman. Strong reconstruction with weak
   curvature agreement is the Hessian-wiggle risk, and it means the CAE arm should not be trusted
   even if Gate A's margin happened to clear.
3. Read Gate B's three pairwise Spearman values, not only `seed_spread`. One divergent seed against
   two that agree is a different situation from three-way disagreement. Then read the
   chart-assignment agreement fractions: if the seeds partition the manifold into different
   atlases, a low pairwise Spearman may be an atlas difference rather than a curvature
   disagreement, and the distinction changes what the number means.
4. Confirm the R6 contract on disk: on PASS exactly one handoff exists and carries all thirteen
   contract fields; on non-PASS neither the handoff nor its `.meta.json` sidecar exists.
5. Confirm no threshold, constant, or rule moved after the measurement — `git diff` on
   `cae_local_regate_run.py` and on `02.5-PREREGISTRATION-STAGE2.md` since their commits should
   show nothing but the §12 Record additions.
6. Confirm the three sealed 02.2 fits are byte-unchanged, and that `CAE_VERDICT = FAIL`,
   `GATE_VERDICT = FAIL`, and `TOPOAE_VERDICT = FAIL` are all untouched. This phase adds a
   differently-scoped measurement; it revises none of them.
7. Check whether D-11's `RETRAIN_TRIGGER` fired.
8. Route, using the rule ratified at plan 02.5-10's checkpoint.

**If `RETRAIN_TRIGGER` fired** — Arm B fell below stage 1's null-calibrated threshold while Arm A
cleared it — the fenced fallback is available. It is not automatic. To use it, ratify the retrain's
hyperparameters here, in writing, and record what is known at this moment: which gate failed, by
how much, and what the retrain is expected to change. D-11 states the residual risk plainly and it
applies now: the hyperparameters are being ratified with the failing result already visible, so
this is precisely where post-hoc pressure enters. A retrain fits new artifacts under new stems; it
never overwrites a sealed fit, and it requires a fresh pre-registration amendment before it runs.

**Routing (D-12, as ratified):**
- **Charts win** (both gates clear) -> Phase 3 uses CAE charts. The handoff is on disk. Proceed to
  plan 02.5-13, which discharges D-13's ROADMAP amendment and D-14's obligation to revisit 02.1's
  sealed falsifier.
- **Raw points win, or tie inside the margin** -> fork into the Ollivier-Ricci / intrinsic-curvature
  phase. This is *not* "the CAE is unnecessary, move on" — it is a fork that finally exercises
  02.1's sealed `GEOM-04` recommendation, unused since 2026-08-05. Note that mean curvature `H` is
  extrinsic and Ollivier-Ricci is intrinsic — a different mathematical object, not a renaming — and
  that adopting it would require amending Phase 4's requirement text, which currently refers to
  `|H|` quantiles. Proceed to plan 02.5-13; D-14 still fires, because D-14's trigger is **any**
  PASS including the raw-points branch.
- **Neither clears** -> apply the resolution ratified at plan 02.5-10's checkpoint and recorded in
  `02.5-PREREGISTRATION-STAGE2.md` §6. Read it there rather than deciding again here; deciding
  twice is how a pre-registered rule quietly becomes a post-hoc one.

**In every branch:** no sealed verdict is reopened. A negative result is a complete and reportable
outcome — three sealed FAILs already stand on this milestone's record unaltered, and D-09 makes
"the CAE is not needed" a legitimate outcome by design.
  </how-to-verify>
  <resume-signal>State the branch: "charts-win", "raw-win", or "neither-clears" (and confirm the §6 resolution being applied). If `RETRAIN_TRIGGER` fired and you want the fallback, say "retrain" and name the hyperparameters to ratify.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New files (none exist before this phase; every symbol below is created by phase 02.5):

- `notebooks/pu_manifold/curvature_probe.py` — `CURVATURE_CONVENTION`, `swiss_roll_analytic_H`,
  `swiss_roll_analytic_H_scaled`, `make_swiss_roll_fixture`, `local_tangent_basis`,
  `centroid_mean_curvature`, `mean_curvature_norm`, `spearman_gate_statistic`,
  `median_relative_error`, `local_density_weights`, `gaussian_bump_values`,
  `graph_mean_curvature`, `make_graph_of_function_fixture`, `make_flat_fixture`,
  `quadric_fit_curvature`, `quadric_mean_curvature`, `estimator_agreement`, `permutation_null`,
  `measure_cell`, `STAGE1_GATING_METRICS`, `STAGE2_GATING_METRICS`, `GATE_DIRECTIONS`,
  `STAGE1_VERDICT_RULE`, `STAGE2_VERDICT_RULE`, `_apply_gates`, `verdict_from_stage1_metrics`,
  `verdict_from_stage2_metrics`, `write_curvature_verdict`, `write_curvature_handoff`,
  `clear_stale_curvature_handoff`
- `notebooks/pu_manifold/chart_curvature.py` — `ZERO_SECOND_DERIVATIVE_ACTIVATIONS`,
  `assert_c2_activation`, `chart_decoder_map`, `chart_mean_curvature`,
  `chart_mean_curvature_norm`, `chart_curvature_field`
- `notebooks/pu_manifold/tests/test_curvature_probe.py` — all `test_*` functions therein
- `notebooks/diagnostics/curvature_feasibility_sweep_run.py` — `SWEEP_CFG`, `BASE_CELL`,
  `SWEEP_AXES`, `NULL_QUANTILE`, `N_PERMUTATIONS`, `SPEARMAN_ABSOLUTE_FLOOR`, `K_GATING`,
  `K_DENSITY`, `D_PRIMARY`, `LADDER`, `WALL_CLOCK_BUDGET_S`, `PREREG_SHA`, `main`
- `notebooks/diagnostics/cae_local_regate_run.py` — `FIT_KEY`, `SEALED_SEEDS`,
  `FIXTURE_CAE_ARCH`, `FIXTURE_CAE_TRAIN_CFG`, `FIXTURE_SEEDS`, `GATE_A_CELL`, `THRESH_MARGIN`,
  `THRESH_SEED_SPREAD`, `RETRAIN_TRIGGER`, `STAGE2_PREREG_SHA`, `SUBSAMPLE_STEM`, `main`
- `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb`,
  `notebooks/02.5_swiss_roll_chart_curvature_check.ipynb`
- `.planning/phases/02.5-.../02.5-PREREGISTRATION.md`,
  `.planning/phases/02.5-.../02.5-PREREGISTRATION-STAGE2.md`,
  `.planning/phases/02.5-.../02.5-FINDINGS.md`, `.planning/phases/02.5-.../COVERAGE.md`
- Cache artifacts (gitignored): `curvature_feasibility_{stage1_key}.json`,
  `curvature_verdict_stage1_{stage1_key}.json`, `curvature_gate_a_{gate_a_key}.json`,
  `curvature_gate_b_{fit_key}.json`, `curvature_fixture_cae_{gate_a_key}_seed{seed}.npz`,
  `curvature_verdict_stage2_{fit_key}.json`, `curvature_handoff_{fit_key}.json`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured gate values -> stored verdict | `write_curvature_verdict`'s internal recompute-and-compare; a stored verdict may never disagree with its own gates |
| non-PASS verdict -> an earlier run's PASS handoff | `clear_stale_curvature_handoff` on every non-PASS path, removing the `.meta.json` sidecar too |
| sealed 02.2 fits -> this measurement | Read-only through the `cache` helpers; a fired D-11 retrain writes new stems and never overwrites |
| a failing result -> a retrain's hyperparameters | Task 3's blocking checkpoint; D-11 names it as where post-hoc pressure enters and requires it record its own epistemic position |
| the verdict -> the routing | The routing rule was ratified at plan 02.5-10; deciding again at Task 3 would convert a pre-registered rule into a post-hoc one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.5-11 | Repudiation | a stored verdict disagreeing with its own gates | high | mitigate | `write_curvature_verdict` recomputes via `verdict_from_stage2_metrics` and raises rather than writing on disagreement; Task 2's verify re-derives the check from the artifact on disk. |
| T-02.5-12 | Information Disclosure | a stale PASS handoff surviving a FAIL and read by Phase 3 as live | high | mitigate | `clear_stale_curvature_handoff` runs on every non-PASS path and removes both the `.json` and its `.meta.json` sidecar; Task 2's verify asserts neither exists on a non-PASS outcome. |
| T-02.5-01 | Tampering | a non-finite Spearman or a constant field reaching the verdict | high | mitigate | STEP 3 halts naming the seed on any non-finite field or pairwise value; `_apply_gates` raises naming the gate before comparing. |
| T-02.5-14 | Tampering | a threshold adjusted after the gate values are seen | high | mitigate | Task 3 step 5 requires `git diff` on the runner and the pre-registration to show nothing but §12 Record additions; §10 states the consequence of a real change is a fresh amendment and a full re-run. |
| T-02.5-23 | Elevation of Privilege | a retrain running without ratified hyperparameters | high | mitigate | STEP 4 evaluates `RETRAIN_TRIGGER` and prints it but never retrains; the retrain requires Task 3's blocking checkpoint, a written record of what was known, and a fresh pre-registration amendment. |
| T-02.5-25 | Tampering | the sealed PU fits modified by the measurement or a retrain | high | mitigate | Read-only loading through the `cache` helpers; Task 1 and Task 2 both assert the three artifacts' hashes are unchanged; a fired retrain writes new stems only. |
| T-02.5-22 | Spoofing | good reconstruction taken as evidence of trustworthy curvature | high | mitigate | Reconstruction statistics and curvature Spearman are stored under separate `context_metrics` keys and never combined; Task 3 step 2 asks the reviewer for exactly that pair. |
| T-02.5-26 | Spoofing | an atlas difference between seeds read as a curvature disagreement | medium | mitigate | Chart-assignment agreement fractions are recorded per seed pair alongside the pairwise Spearman, and Task 3 step 3 names the distinction. |
| T-02.5-05 | Tampering | sealed source, sealed verdicts, or `src/effdim/` | high | mitigate | Import-only; acceptance criteria assert `git status --porcelain` is empty for `notebooks/pu_manifold/`, `src/effdim`, and `pyproject.toml`, and Task 3 step 6 confirms all three sealed verdicts are untouched. |
| T-02.5-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; the runner imports already-pinned dependencies and the local package. |
</threat_model>

<verification>
- `PYTHONPATH=notebooks python notebooks/diagnostics/cae_local_regate_run.py` completes and prints both gate read-outs, the verdict, and the routing
- Exactly one `curvature_verdict_stage2_43cf438bc944c509.json` exists with both gates, `gate_detail`, `verdict_rule`, `gate_scope`, and `context_metrics`
- On PASS exactly one handoff exists with all thirteen contract fields; on non-PASS no handoff and no sidecar exists
- The three sealed fit artifacts' hashes are unchanged
- No threshold or constant moved after the measurement
- The Task 3 blocking checkpoint is resolved with the branch recorded
</verification>

<success_criteria>
Both stage-2 gates are measured under a pre-registration proved to precede them, the verdict is
computed by the tested comparison function and sealed on either outcome with its full non-gating
context, the R6 handoff contract holds exactly, D-11's trigger is evaluated without being acted on
unilaterally, and the outcome is routed by the rule ratified before any of it was seen.
</success_criteria>

<output>
Create `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-12-SUMMARY.md` when done
</output>