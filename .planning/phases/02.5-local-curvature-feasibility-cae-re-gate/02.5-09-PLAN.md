---
phase: 02.5-local-curvature-feasibility-cae-re-gate
plan: 09
type: execute
wave: 8
depends_on: ["02.5-08"]
files_modified:
  - notebooks/02.5_swiss_roll_chart_curvature_check.ipynb
autonomous: false
requirements: [D-03, D-09]
must_haves:
  truths:
    - "A Swiss roll notebook exists for the chart-decoder curvature model introduced in stage 2, because CLAUDE.md's rule binds any new model and the ROADMAP repeats it: any model introduced in stage 2 needs its own (CLAUDE.md, ROADMAP)."
    - "The notebook trains a ChartAutoEncoder from scratch inside itself at chart_dim = 2 on a Swiss roll, then computes curvature through its chart decoder with chart_curvature imported unchanged (CLAUDE.md, D-09)."
    - "The chart-decoder curvature field is scored against the Swiss roll's closed-form analytic H and against a matched baseline — the raw-point centroid estimator at d = 2, which plan 02.5-05 already showed succeeds on this fixture (CLAUDE.md, D-09)."
    - "The notebook makes D-09's counter-risk visible: a smooth-looking reconstruction guarantees nothing about its second derivative, so the reconstruction quality and the curvature agreement are reported as two separate numbers and never conflated (D-09)."
    - statement: "A human read the printed pass/fail read-out and the plots and judged whether curvature through a trained chart decoder recovers a known answer, or recorded plainly that it does not."
      verification: backstop
  artifacts:
    - notebooks/02.5_swiss_roll_chart_curvature_check.ipynb
  key_links:
    - "trained chart decoder -> torch.func Hessian -> ||H|| field -> analytic H(t): this is the only place before the gated run where a neural network's second derivative is checked against a known answer, and D-09 names it as the one thing that can distinguish exact differentiation's benefit from a network wiggling where the manifold does not."
    - "reconstruction MSE and curvature Spearman reported separately: conflating them is exactly the error D-09 warns about, and separating them in the read-out is what makes the warning operational."
  prohibitions:
    - statement: "The notebook contains no cache calls, no cfg-hash cache keys, no verdict JSON artifact, no threshold table, and no git-ancestry proof"
      status: planned
      verification: manual
    - statement: "The notebook never reads from or writes to notebooks/.cache/, and never loads one of the sealed 02.2 PU fits"
      status: planned
      verification: manual
    - statement: "The notebook imports cae and chart_curvature unchanged and reimplements no part of either"
      status: planned
      verification: manual
    - statement: "The Swiss roll is not substituted by another toy dataset"
      status: planned
      verification: manual
    - statement: "No existing notebook is deleted or rewritten; this notebook is additive"
      status: planned
      verification: manual
    - statement: "Any number printed here is a sanity read-out, never a gate; stage 2's gate is fixed only by the ratified 02.5-PREREGISTRATION-STAGE2.md"
      status: planned
      verification: manual
---

<objective>
Deliver the mandatory Swiss roll check for stage 2's new model — curvature computed through a
trained chart decoder.

Purpose: `02.5-RESEARCH.md` § Project Constraints states it directly — stage 1 *is* the Swiss roll
check for the centroid estimator by construction, but if stage 2 introduces new curvature-
differentiation logic on the CAE decoder, that logic is itself new, untested code and must be
exercised against the same Swiss roll fixture before being trusted on the real fits. The ROADMAP
says the same: any model introduced in stage 2 needs its own. And D-09 names the specific risk
only a known-answer fixture can settle: a neural network's second derivative can wiggle where the
true manifold does not, so the CAE's noise-averaging advantage and its Hessian-wiggle risk look
identical on data with no known answer.

Output: `notebooks/02.5_swiss_roll_chart_curvature_check.ipynb`, executed end to end, outputs
committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-CONTEXT.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-RESEARCH.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PATTERNS.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-08-SUMMARY.md
@CLAUDE.md
</context>

<preconditions>
Plan 02.5-07's stage-1 go/no-go checkpoint resolved GO. Halt if `02.5-07-SUMMARY.md` records
NO-GO.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Write the Swiss roll chart-curvature sanity notebook</name>
  <files>notebooks/02.5_swiss_roll_chart_curvature_check.ipynb</files>
  <precondition>Plan 02.5-07's stage-1 go/no-go checkpoint resolved GO, recorded in 02.5-07-SUMMARY.md.</precondition>
  <read_first>
    - CLAUDE.md § "Swiss roll sanity check — required for every new manifold model" (all of it, including the no-decoder carve-out — which does NOT apply here, because this model has a decoder and the standard `cae.PlainAutoEncoder` comparison is available)
    - notebooks/02.2_swiss_roll_cae_check.ipynb (all 15 cells — the reference shape, and the one notebook in this milestone whose model passed outright: 4.8% relative error, 2.2x better than a matched plain-AE, 8/8 charts surviving)
    - notebooks/pu_manifold/chart_curvature.py (the functions to import unchanged)
    - notebooks/pu_manifold/curvature_probe.py (`make_swiss_roll_fixture`, `centroid_mean_curvature`, `spearman_gate_statistic`, `median_relative_error` — the baseline arm)
    - notebooks/pu_manifold/cae.py (`ChartAutoEncoder`, `train_cae`, `PlainAutoEncoder`, `reconstruction_stats`, `chart_survival` — imported unchanged)
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-CONTEXT.md D-09 (the noise-averaging benefit and the Hessian-wiggle counter-risk)
  </read_first>
  <action>
Create `notebooks/02.5_swiss_roll_chart_curvature_check.ipynb`, at most 15 cells, following
`notebooks/02.2_swiss_roll_cae_check.ipynb`'s structure. That notebook already trains a
`ChartAutoEncoder` on a Swiss roll inside itself in under two minutes on CPU — reuse its training
cell's shape and its hyperparameters rather than choosing new ones, and say in the markdown that
they were reused so no new tunable appears in a sanity check.

1. **Markdown** — framing. Stage 2's Arm B is new, untested code that takes second derivatives of
   a trained neural network. D-09's case for it is noise-averaging plus exact differentiation;
   D-09's counter-risk is that a network's second derivative can wiggle where the true manifold
   does not, and only a fixture with analytic `H` can tell those apart. State that this notebook
   carries no gate — stage 2's gate lives in `02.5-PREREGISTRATION-STAGE2.md` and
   `notebooks/diagnostics/cae_local_regate_run.py`.
2. **Markdown** — `## §1. Setup`. **Code** — `sys.path.insert` for the relative `pu_manifold`
   import; import `cae`, `chart_curvature`, `curvature_probe`. Never `src/effdim/`.
3. **Markdown + Code** — `## §2. The Swiss roll`. Call
   `curvature_probe.make_swiss_roll_fixture(n=3000, seed=20260807)` so the notebook uses the same
   preprocessing path the rest of the phase does (CLAUDE.md's `make_swiss_roll`, `noise=0.0`,
   fixed `random_state`, centre, one global scalar std) rather than transcribing it. Print
   `X.shape`, `global_std`.
4. **Markdown + Code** — `## §3. Train a Chart Auto-Encoder from scratch`. `chart_dim = 2`, the
   roll's true intrinsic dimension per CLAUDE.md. `activation="silu"`. Reuse the 02.2 notebook's
   width/depth/epochs and its eq.-5 FPS pre-training — STATE.md records that without FPS
   pre-training a two-chart model never activates its second chart, so omitting it would test a
   degenerate model. Target under two minutes on CPU; print the wall-clock. Never read or write
   `notebooks/.cache/` and never load a sealed 02.2 PU fit — this notebook trains its own.
5. **Markdown + Code** — `## §4. Reconstruction, first`. Print `cae.reconstruction_stats` for the
   CAE and for a matched `cae.PlainAutoEncoder` at the same 2-D bottleneck and the same training
   protocol, plus `cae.chart_survival`. This is CLAUDE.md's step-6 matched baseline for a
   reconstruction model, and it establishes the premise the next cell then tests: the
   reconstruction is good.
6. **Markdown + Code** — `## §5. Curvature through the chart decoder`. Call
   `chart_curvature.chart_curvature_field(model, X)` and `chart_mean_curvature_norm`. Print the
   returned `metric_condition_number` summary so a near-singular pullback metric is visible.
   State in the markdown that this is the step nothing before it tested.
7. **Markdown + Code** — `## §6. Against the analytic answer and a baseline that succeeds`. Score
   the chart-decoder field against the fixture's `H_norm` with
   `curvature_probe.spearman_gate_statistic` and `median_relative_error`. Then run
   `curvature_probe.centroid_mean_curvature(X, k=30, d=2)` as the matched baseline — plan 02.5-05
   already showed the raw-point estimator succeeds on this exact fixture, which is what makes it
   "a baseline that is known to succeed" — and score it the same way. Print both Spearman values
   side by side.
8. **Markdown + Code** — plots. Three panels, all coloured consistently: the roll coloured by
   analytic `||H||`, by chart-decoder `||H||`, and by raw-point `||H||`, as 3-D scatters; then the
   same three as x-z scatters, since CLAUDE.md notes the default 3-D view hides the spiral. Plus
   one scatter of chart-decoder `||H||` against analytic `||H||` with the identity line, and the
   same for the raw-point arm on shared axes.
9. **Markdown + Code** — `## §7. Read-out`. Four printed pass/fail lines and one sentence:
   - `reconstruction beats the matched plain-AE` with the ratio
   - `chart-decoder curvature Spearman vs analytic H > 0.90` with the value
   - `chart-decoder curvature beats the raw-point baseline's Spearman` with both values
   - `metric condition number is finite and below 1e6 at every point` with the max
   Then one sentence. **The read-out must state the two numbers separately and never combine
   them:** if reconstruction is excellent and curvature Spearman is poor, that is D-09's
   Hessian-wiggle risk showing up on a manifold where the answer is known, and it is the single
   most important thing this notebook can find. Say so in the markdown above the read-out, so the
   reader knows what pattern to look for. If the chart-decoder arm loses to the raw-point
   baseline here, print that plainly — CLAUDE.md is explicit that a model not being better than
   the baseline on a manifold it was designed for is a real result about the model, and D-09 makes
   "the CAE is not needed" a legitimate, reportable outcome rather than a phase failure.
   Add a final printed line stating in words that these are sanity thresholds, not the phase gate.

Do not add: any `cache.` call, cfg dict, cfg-hash key, verdict JSON write, threshold table,
git-ancestry proof, or read/write under `notebooks/.cache/`. Do not load a sealed 02.2 fit. Do not
delete or modify any existing notebook.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; python -c "
import json
nb=json.load(open('notebooks/02.5_swiss_roll_chart_curvature_check.ipynb'))
cells=nb['cells']; src=''.join(''.join(c['source']) for c in cells)
assert len(cells)<=15, f'cell budget exceeded: {len(cells)}'
for bad in ['cache.','json_cache','joblib_cache','config_key','.cache/','PREREGISTRATION','merge-base','cae_fit_43cf438bc944c509','write_curvature_verdict']:
    assert bad not in src, f'prohibited construct: {bad}'
for need in ['chart_curvature','curvature_probe','PlainAutoEncoder','chart_dim','silu']:
    assert need in src, f'missing required element: {need}'
print('notebook shape OK,', len(cells), 'cells')
"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/02.5_swiss_roll_chart_curvature_check.ipynb` exists with at most 15 cells
    - Source contains `chart_curvature`, `curvature_probe`, `PlainAutoEncoder`, `chart_dim`, `silu`
    - Source contains none of `cache.`, `json_cache`, `joblib_cache`, `config_key`, `.cache/`, `PREREGISTRATION`, `merge-base`, `cae_fit_43cf438bc944c509`
    - The read-out cell prints reconstruction quality and curvature agreement as separate lines
    - `git status --porcelain notebooks/02.1_swiss_roll_geometry_probes_check.ipynb notebooks/02.2_swiss_roll_cae_check.ipynb notebooks/02.4_swiss_roll_topoae_check.ipynb notebooks/02.5_swiss_roll_curvature_probe_check.ipynb` produces no output
  </acceptance_criteria>
  <done>A ≤15-cell Swiss roll notebook exists that trains a chart auto-encoder from scratch at chart_dim 2, computes curvature through its decoder with the module imported unchanged, and scores it against both the analytic answer and a baseline known to succeed — with reconstruction and curvature reported as separate numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the notebook end to end and commit it with outputs</name>
  <files>notebooks/02.5_swiss_roll_chart_curvature_check.ipynb</files>
  <read_first>
    - notebooks/02.5_swiss_roll_chart_curvature_check.ipynb (as written by Task 1)
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-05-SUMMARY.md (how plan 02.5-05 executed and committed its notebook, and the four numbers its raw-point arm produced on the same fixture — directly comparable here)
    - CLAUDE.md § General ("Notebooks are committed with their outputs, executed end to end")
  </read_first>
  <action>
Execute the notebook top to bottom in the notebook venv with
`jupyter nbconvert --to notebook --execute --inplace`, matching how plan 02.5-05 executed its
notebook. Target under two minutes on CPU; record the actual wall-clock. If training pushes past
it, reduce epochs rather than weakening any check, and record the reduction.

Confirm every code cell carries an `execution_count`, that no cell raised, and that the four
pass/fail lines and the sentence are present in the stored outputs.

Record verbatim in the SUMMARY, for plan 02.5-10's pre-registration to reference: the CAE
reconstruction ratio against the matched plain-AE, the chart-decoder curvature Spearman, the
raw-point baseline curvature Spearman, the two arms' median relative errors, and the maximum
metric condition number. Plan 02.5-10 proposes `THRESH_MARGIN` and must disclose that these
numbers were already visible when it did — the same ordering-transparency obligation
`02.5-PREREGISTRATION.md` §13 discharged for stage 1.

Confirm the notebook wrote nothing under `notebooks/.cache/`: capture the file count before and
after and assert it is unchanged. Confirm no sealed fit artifact was read: the notebook trains its
own model.

Commit the notebook with its outputs.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; python -c "
import json
nb=json.load(open('notebooks/02.5_swiss_roll_chart_curvature_check.ipynb'))
code=[c for c in nb['cells'] if c['cell_type']=='code']
assert all(c.get('execution_count') is not None for c in code), 'an unexecuted code cell remains'
errs=[o for c in code for o in c.get('outputs',[]) if o.get('output_type')=='error']
assert not errs, f'notebook raised: {errs[0].get(\"ename\")}'
text=''.join(''.join(o.get('text',[])) if isinstance(o.get('text'),list) else str(o.get('text','')) for c in code for o in c.get('outputs',[]) if o.get('output_type')=='stream')
assert 'Spearman' in text and 'plain-AE' in text, 'read-out lines missing from stored outputs'
print('executed, no errors, read-out present')
"</automated>
  </verify>
  <acceptance_criteria>
    - Every code cell has a non-null `execution_count`; no output has `output_type == "error"`
    - Stored stream output contains the four pass/fail lines and the one-sentence read-out
    - `ls notebooks/.cache | wc -l` unchanged across execution
    - The six recorded numbers appear verbatim in `02.5-09-SUMMARY.md`
    - Wall-clock execution time recorded in the SUMMARY
    - `git log -1 --name-only` shows the notebook committed with outputs
  </acceptance_criteria>
  <done>The stage-2 Swiss roll notebook is executed end to end with no errors, its six measured numbers are recorded for the stage-2 pre-registration to disclose, and it is committed with outputs having touched nothing in the cache.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human read of the chart-decoder curvature sanity check</name>
  <what-built>
`notebooks/02.5_swiss_roll_chart_curvature_check.ipynb` — the mandatory CLAUDE.md sanity check for
stage 2's new model. It trains a `ChartAutoEncoder` from scratch on a 3,000-point Swiss roll at
`chart_dim = 2` with SiLU and eq.-5 FPS pre-training, reusing 02.2's notebook hyperparameters
unchanged, then computes mean curvature exactly through the trained chart decoder via
`torch.func` and scores it against the roll's closed-form analytic `H` and against the raw-point
centroid estimator, which plan 02.5-05 already showed succeeds on this fixture.
  </what-built>
  <how-to-verify>
1. Read the §4 reconstruction numbers and the §7 read-out together, and look for the specific
   pattern D-09 warns about: **excellent reconstruction beside poor curvature agreement**. That
   combination means the decoder's second derivative wiggles where the manifold does not, and it
   is the single most informative thing this notebook can find. It would not be visible on real
   data at all.
2. Look at the three 3-D panels and then the three x-z panels. The chart-decoder `||H||` field
   should show the same spatial pattern as the analytic one — highest at the tight inner turn,
   falling smoothly outward. Banding along a chart boundary rather than along the spiral would
   mean the field is tracking the atlas rather than the geometry.
3. Look at the two identity-line scatters on shared axes. Compare the chart-decoder arm's spread
   against the raw-point arm's. D-09's claim is that the learned model averages noise away; if its
   scatter is not tighter than the raw-point arm's on a `noise=0.0` fixture, the noise-averaging
   argument has nothing to show here — which is worth knowing before it is relied on at `D = 768`.
4. Check the maximum metric condition number. A near-singular pullback metric at some points means
   the chart decoder is not an immersion there, and curvature at those points is not meaningful
   regardless of what the Spearman says.
5. Confirm what is absent: no `cache.` call, no cfg-hash key, no verdict JSON, no threshold table,
   no git-ancestry proof, no read of a sealed 02.2 fit, and nothing written under
   `notebooks/.cache/`.
6. Decide. A poor result here does not end the phase — D-09 makes "the CAE is not needed" a
   legitimate, reportable outcome and routes it to the Ollivier-Ricci fork rather than treating it
   as failure. But it should change what `02.5-PREREGISTRATION-STAGE2.md` says about `THRESH_MARGIN`
   and about D-11's retrain trigger, and plan 02.5-10 must disclose that these numbers were
   already visible when it proposed them.

**Note for the record on approval:** the six measured numbers are inputs to plan 02.5-10's
pre-registration, which must disclose them under the same ordering-transparency obligation
`02.5-PREREGISTRATION.md` §13 discharged for stage 1. This checkpoint is the point at which they
became known.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to plan 02.5-10 (stage-2 pre-registration), or describe what the plots or numbers show that they should not.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New files (none exist before this phase; every symbol below is created by phase 02.5):

- `notebooks/pu_manifold/curvature_probe.py` — `CURVATURE_CONVENTION`, `swiss_roll_analytic_H`,
  `swiss_roll_analytic_H_scaled`, `make_swiss_roll_fixture`, `local_tangent_basis`,
  `centroid_mean_curvature`, `mean_curvature_norm`, `spearman_gate_statistic`,
  `median_relative_error`, `local_density_weights`, `gaussian_bump_values`,
  `graph_mean_curvature`, `make_graph_of_function_fixture`, `make_flat_fixture`,
  `quadric_fit_curvature`, `quadric_mean_curvature`, `estimator_agreement`, `permutation_null`,
  `measure_cell`, `STAGE1_GATING_METRICS`, `STAGE2_GATING_METRICS`, `GATE_DIRECTIONS`,
  `STAGE1_VERDICT_RULE`, `STAGE2_VERDICT_RULE`, `_apply_gates`, `verdict_from_stage1_metrics`,
  `verdict_from_stage2_metrics`, `write_curvature_verdict`, `write_curvature_handoff`,
  `clear_stale_curvature_handoff`
- `notebooks/pu_manifold/chart_curvature.py` — `ZERO_SECOND_DERIVATIVE_ACTIVATIONS`,
  `assert_c2_activation`, `chart_decoder_map`, `chart_mean_curvature`,
  `chart_mean_curvature_norm`, `chart_curvature_field`
- `notebooks/pu_manifold/tests/test_curvature_probe.py` — all `test_*` functions therein
- `notebooks/diagnostics/curvature_feasibility_sweep_run.py` — `SWEEP_CFG`, `BASE_CELL`,
  `SWEEP_AXES`, `NULL_QUANTILE`, `N_PERMUTATIONS`, `SPEARMAN_ABSOLUTE_FLOOR`, `K_GATING`,
  `K_DENSITY`, `D_PRIMARY`, `LADDER`, `WALL_CLOCK_BUDGET_S`, `PREREG_SHA`, `main`
- `notebooks/diagnostics/cae_local_regate_run.py` — `FIT_KEY`, `SEALED_SEEDS`,
  `FIXTURE_CAE_ARCH`, `THRESH_MARGIN`, `THRESH_SEED_SPREAD`, `RETRAIN_TRIGGER`, `main`
- `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb`,
  `notebooks/02.5_swiss_roll_chart_curvature_check.ipynb`
- `.planning/phases/02.5-.../02.5-PREREGISTRATION.md`,
  `.planning/phases/02.5-.../02.5-PREREGISTRATION-STAGE2.md`,
  `.planning/phases/02.5-.../02.5-FINDINGS.md`, `.planning/phases/02.5-.../COVERAGE.md`
- Cache artifacts (gitignored): `curvature_feasibility_{stage1_key}.json`,
  `curvature_verdict_stage1_{stage1_key}.json`, `curvature_verdict_stage2_{fit_key}.json`,
  `curvature_handoff_{fit_key}.json`, `curvature_fixture_cae_{key}_seed{seed}.npz`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| good reconstruction -> trustworthy second derivative | D-09's named counter-risk; the boundary is the separate reporting of reconstruction quality and curvature agreement, which only a known-answer fixture can measure |
| sanity notebook -> gated runner conventions | `02.5-PATTERNS.md` flags the two file families as convention-incompatible; a cache call or a sealed-fit load crossing into the notebook makes a sanity check look like a gate |
| notebook read-out -> stage-2 pre-registered thresholds | The numbers here are visible before plan 02.5-10 proposes `THRESH_MARGIN`; the boundary is disclosure, not concealment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.5-22 | Spoofing | a good reconstruction taken as evidence for a good second derivative | high | mitigate | Reconstruction and curvature agreement are printed as separate read-out lines with the markdown above naming the pattern to look for; the human checkpoint step 1 asks for exactly that combination. |
| T-02.5-06 | Spoofing | a zero-second-derivative activation | high | mitigate | The notebook trains with `activation="silu"` and `chart_curvature.assert_c2_activation` raises on any zero-second-derivative activation before differentiating. |
| T-02.5-13 | Tampering | sanity notebook writing into the gated artifact cache or reading a sealed fit | medium | mitigate | Task 1's verify negative-greps `cache.`, `json_cache`, `joblib_cache`, `config_key`, `.cache/`, and `cae_fit_43cf438bc944c509`; Task 2 asserts the cache file count is unchanged. |
| T-02.5-15 | Repudiation | stage-2 thresholds chosen after seeing these numbers without disclosure | medium | mitigate | Task 2 records the six numbers explicitly for plan 02.5-10 to disclose, and the human checkpoint states that obligation on approval. |
| T-02.5-05 | Tampering | existing notebooks or sealed modules | high | mitigate | Additive-only; acceptance criteria assert `git status --porcelain` is empty for the four pre-existing notebooks and the 02.5-05 notebook. |
| T-02.5-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; the notebook imports already-pinned `torch`, `numpy`, `matplotlib`, `scikit-learn` and the local package. |
</threat_model>

<verification>
- The notebook has at most 15 cells, every code cell executed, no error outputs
- Source contains none of `cache.`, `json_cache`, `config_key`, `.cache/`, `PREREGISTRATION`, `merge-base`, `cae_fit_43cf438bc944c509`
- `ls notebooks/.cache | wc -l` unchanged across execution
- The six measured numbers recorded in `02.5-09-SUMMARY.md`
- The blocking human-verify checkpoint in Task 3 is resolved with a recorded decision
</verification>

<success_criteria>
Curvature through a trained chart decoder has been exercised on a Swiss roll with a known answer,
scored against both the analytic truth and a baseline known to succeed, with reconstruction
quality and curvature agreement reported as separate numbers so D-09's counter-risk is visible —
and a human has read it and recorded a decision.
</success_criteria>

<output>
Create `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-09-SUMMARY.md` when done
</output>
