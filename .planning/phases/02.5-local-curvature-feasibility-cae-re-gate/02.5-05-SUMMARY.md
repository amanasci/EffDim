---
phase: 02.5-local-curvature-feasibility-cae-re-gate
plan: 05
subsystem: geometry
tags: [jupyter, swiss-roll, curvature-estimation, spearman, sanity-check]

# Dependency graph
requires:
  - phase: 02.5-03
    provides: "curvature_probe.py: centroid_mean_curvature, mean_curvature_norm, swiss_roll_analytic_H_scaled, spearman_gate_statistic, median_relative_error, quadric_mean_curvature, estimator_agreement -- every function this notebook imports unchanged"
provides:
  - "notebooks/02.5_swiss_roll_curvature_probe_check.ipynb: CLAUDE.md's mandatory Swiss roll sanity check for the local mean-curvature estimator, executed end to end with outputs committed"
  - "Measured stage-1 read-out at k=30, d=2, random_state=20260807: per-point Spearman rho=0.6712, median relative error=0.1493, quadric-agreement median rel diff=0.1456, quadric determined (not underdetermined) -- 3 of 4 notebook sanity lines pass"
  - "A scale-bias/noise decomposition of the per-point Spearman miss, computed as a notebook-level diagnostic and reported for plan 02.5-06 to weigh: median ratio h_est/h_true=0.8934 (an ~11% near-uniform finite-radius scale underestimate), within-band noise CV 0.42-0.52 at the two free arclength edges vs 0.20-0.28 interior, and a region-median (20-band) Spearman of 0.8406 / relative error 0.1092 on the same field"
  - "An open question for plan 02.5-06 to propose and ratify: whether stage 1's gate should be evaluated at the per-point scale or the region scale Phase 4's own quantile partitioning actually consumes"
affects: ["02.5-06", "02.5-07"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Notebook-level diagnostic decomposition (plain numpy, arclength banding) to separate a scale-bias component from a noise component in a per-point rank-correlation miss, without adding new functions to the sealed module or changing gate semantics"
    - "h_true depending on a single scalar parameter (t) makes narrow percentile bands of that parameter an almost-zero-true-variance probe: any within-band spread in the estimate is measurement noise by construction, not signal"

key-files:
  created:
    - notebooks/02.5_swiss_roll_curvature_probe_check.ipynb
  modified: []

key-decisions:
  - "Notebook built at 14 cells (budget is <=15): framing+setup, imports, Swiss roll generation, the estimator call, both required plot views plus a third (t-coloured + identity-line scatter) figure, faithfulness numbers, the quadric baseline, and the read-out -- following notebooks/02.2_swiss_roll_cae_check.ipynb's shape"
  - "Baseline for this no-decoder estimator: the local quadric ('bowl') fit at d=2, k=30 (well-determined -- 3 coefficients from 30 samples), per CLAUDE.md's no-decoder carve-out; not cae.PlainAutoEncoder, which does not apply to a model with no decoder"
  - "Checkpoint amendment (Task 3, approved with a required change, not a plain approve): the coordinator's read was that per-point Spearman may be the wrong stage-1 gating statistic and asked the notebook to REPORT the decomposition, not to change any gate or threshold. No pass/fail line, threshold, or semantics changed -- exactly 4 pass/fail lines before and after"
  - "Band aggregation (scale-bias ratio, within-band CV, region-median Spearman/relative error) implemented as plain numpy inline in the notebook, explicitly marked non-gating and notebook-level-only -- no new function added to curvature_probe.py, per the coordinator's explicit constraint"
  - "Region-median statistics reuse cp.spearman_gate_statistic and cp.median_relative_error unchanged, called on the 20 per-band median arrays rather than on the raw per-point arrays -- no new statistic implementation, only a different input array"

patterns-established:
  - "When a per-point rank-correlation miss is found, decompose it (scale bias vs. noise) before treating it as a broken implementation -- an independently-derived second estimator (here, the quadric baseline) landing on a similarly imperfect answer is evidence the miss is a shared measurement limitation, not a bug in one code path"

requirements-completed: [D-03, D-05, D-07]

coverage:
  - id: D1
    description: "A Swiss roll notebook exists for the local mean-curvature estimator, executed end to end with outputs committed, importing curvature_probe.py unchanged, generating its own Swiss roll under CLAUDE.md's exact preprocessing, at d=2, plotting both required views plus the t-coloured/identity-line checks, comparing against the quadric baseline, and printing four pass/fail lines plus a one-sentence read-out"
    requirement: "D-03, D-05, D-07"
    verification:
      - kind: other
        ref: "notebooks/02.5_swiss_roll_curvature_probe_check.ipynb -- 14 cells, every code cell executed (non-null execution_count), no error outputs, 8s wall-clock; notebooks/.cache/ entry count unchanged (244 before, 244 after)"
        status: pass
      - kind: other
        ref: "python -c \"...\" negative-grep for cache./json_cache/joblib_cache/config_key/.cache//PREREGISTRATION/merge-base/write_curvature_verdict/write_curvature_handoff -> none present"
        status: pass
    human_judgment: false
  - id: D2
    description: "A human read the printed pass/fail read-out and the plots and judged the estimator's result on the Swiss roll, and recorded a decision"
    verification: []
    human_judgment: true
    rationale: "This is the plan's own backstop must-have -- a human judgment call is required by design, not automatable. Resolved: APPROVED, with a required amendment (report the scale-bias/noise decomposition in the notebook) rather than a plain pass; delivered in the amendment commit b7ac57f."

duration: ~20min active work across two segments (Task 1-2, then the Task 3 amendment), separated by a checkpoint hold awaiting the human decision
completed: 2026-08-08
status: complete
---

# Phase 02.5 Plan 05: Swiss Roll Sanity Check for the Curvature Probe Summary

**CLAUDE.md's mandatory Swiss roll notebook for `centroid_mean_curvature`, executed end to end: per-point Spearman rho=0.6712 misses its own 0.90 sanity bar, but a notebook-level scale-bias/noise decomposition shows the miss is an ~11% near-uniform finite-radius bias plus per-point noise on a ~3x-range field, not a broken estimator -- region-scale aggregation over the same field recovers rho=0.8406.**

## Performance

- **Duration:** ~20 min of active work, in two segments separated by a checkpoint hold: Tasks 1-2 (notebook authored, executed, committed) then a hold awaiting the Task 3 human-verify decision, then the required amendment (decomposition added, re-executed, committed) once the decision arrived
- **Started:** 2026-08-08T04:22:55Z
- **Completed:** 2026-08-08T12:33:08Z (wall-clock includes the checkpoint hold; active work is the ~20 min above)
- **Tasks:** 3 (Task 1 auto, Task 2 auto, Task 3 checkpoint:human-verify -- resolved APPROVED with a required amendment)
- **Files modified:** 1 (`notebooks/02.5_swiss_roll_curvature_probe_check.ipynb`)

## Accomplishments

- `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb`: 14-cell Swiss roll sanity check, importing `centroid_mean_curvature`, `mean_curvature_norm`, `swiss_roll_analytic_H_scaled`, `quadric_mean_curvature`, and `estimator_agreement` unchanged from `curvature_probe.py`; generates its own 3,000-point, `noise=0.0`, `random_state=20260807` Swiss roll, centred and divided by one global scalar standard deviation
- Both CLAUDE.md-required plot views (3-D and x-z) for analytic vs. estimated `||H||` under one shared colour normalization, plus a third figure (roll coloured by `t`, and estimated-vs-analytic scatter against the identity line)
- Baseline comparison against the local quadric fit at `d=2, k=30` (well-determined -- 3 coefficients, 30 samples), the no-decoder carve-out CLAUDE.md specifies
- **Measured stage-1 read-out** (recorded verbatim for plan 02.5-06's disclosure): per-point Spearman rho=0.6712 (misses the notebook's own >0.90 sanity bar), median relative error=0.1493 (clears <0.25), quadric-agreement median relative difference=0.1456 (clears <0.25), quadric fit determined at `d=2` (clears "not underdetermined")
- **A required checkpoint amendment**, delivered as a notebook-level diagnostic decomposition (not a gate or threshold change): median ratio `h_est/h_true` = 0.8934 (IQR 0.2838) -- an ~11% near-uniform scale underestimate, consistent with the module's own documented `O(r^2)` finite-radius bias; within-band coefficient of variation 0.42-0.52 at the two free arclength edges vs. 0.20-0.28 interior (using 10 percentile bands of the arclength parameter `t`, inside which `h_true` is almost constant by construction, so within-band spread is pure estimator noise); region-median (20-band) Spearman = 0.8406 and region-median relative error = 0.1092 on the same field
- Full `pu_manifold` test suite: **135 passed** (unchanged baseline), no sealed file touched, `notebooks/.cache/` entry count unchanged across both executions (244 -> 244)

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the Swiss roll curvature-probe sanity notebook** - `4768fa3` (feat)
2. **Task 2: Execute the notebook end to end and commit it with outputs** - `0d0c05a` (feat)
3. **Task 3 checkpoint amendment: report the scale-bias/noise decomposition** - `b7ac57f` (docs)

**Plan metadata:** committed alongside this SUMMARY

## Files Created/Modified

- `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb` - the Swiss roll sanity check: setup, Swiss roll generation, the estimator call, both required plot views plus the t-coloured/identity-line figure, faithfulness numbers plus the scale-bias/noise decomposition, the quadric baseline, and the four-line read-out

## Decisions Made

- **Baseline is the local quadric fit, not `cae.PlainAutoEncoder`.** `centroid_mean_curvature` has no decoder, so CLAUDE.md's usual reconstruction-baseline form does not apply; its own carve-out for a no-decoder model ("a baseline that is known to succeed") is satisfied here by the quadric fit at `d=2, k=30`, comfortably determined (3 coefficients, 30 samples) unlike the same fit at the phase's real working dimension of 20.
- **Checkpoint resolution: APPROVED, with a required amendment, not a plain approve.** The coordinator's read: what matters for the phase is recovery of the curvature field, not necessarily a global per-point rank ordering; per-point Spearman may be the wrong stage-1 gating statistic, but that question is explicitly deferred to plan 02.5-06's own pre-registration proposal, not decided or pre-empted here. This executor's job was narrowly to make the notebook REPORT the decomposition as evidence, not to change any gate.
- **Decomposition implemented as a notebook-level diagnostic, not a new module function.** Per the coordinator's explicit constraint, arclength banding (scale-bias ratio, within-band CV, region-median statistics) is plain numpy inline in the notebook, clearly marked as such in the markdown; `curvature_probe.py` is untouched by this plan (confirmed unmodified in every commit). Region-median Spearman and relative error reuse `cp.spearman_gate_statistic`/`cp.median_relative_error` unchanged, called on the 20 per-band median arrays.
- **Exactly four pass/fail lines preserved, before and after the amendment.** The new diagnostic numbers print separately, above the four-line read-out, never as additional pass/fail lines; no threshold changed.

## Deviations from Plan

### Auto-fixed Issues

None in the Rule 1-3 sense -- both tasks executed exactly as the plan specified, and the plan's own verify commands all passed on the first attempt for Task 1 and Task 2.

### Checkpoint-directed change (not a deviation-rule auto-fix)

**1. [Coordinator-directed amendment, delivered post-checkpoint] Added the scale-bias/noise decomposition to §5 and reframed §7's read-out prose**
- **Found during:** Task 3's checkpoint resolution -- the coordinator ran independent diagnostics against the executed notebook and found the per-point Spearman miss decomposes cleanly into an ~11% uniform scale bias (does not disturb ranking) plus per-point noise (does disturb ranking, on a field with only ~3x dynamic range).
- **Issue:** The as-executed notebook (Task 2) reported only the two headline numbers (per-point Spearman, median relative error) and a read-out sentence that did not distinguish a scale-bias failure mode from a noise-limited one -- both would print the same `False` on the Spearman line, but they mean different things for whether stage 1's gate is well-specified.
- **Fix:** Extended §5's code cell with four new printed diagnostic lines (median ratio/scale bias, within-band noise CV split, region-median Spearman, region-median relative error) and their supporting markdown explanation (why within-band spread is pure noise); reframed §7's `else` branch to name the decomposition and the region-scale result, without touching any pass/fail threshold or adding a pass/fail line. Re-executed end to end and re-committed with outputs.
- **Files modified:** `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb`
- **Verification:** Re-ran the plan's Task 1 shape/prohibition checks (still 14 cells, still no prohibited substrings) and Task 2's execution checks (all code cells executed, no errors, `notebooks/.cache/` entry count unchanged 244->244) against the amended notebook; full `pu_manifold` suite re-confirmed 135 passed.
- **Committed in:** `b7ac57f`

---

**Total deviations:** 0 Rule 1-3 auto-fixes. 1 coordinator-directed amendment delivered at the Task 3 checkpoint (not a deviation rule -- an explicit human decision, executed as specified).
**Impact on plan:** The amendment adds evidence to the committed artifact without touching any gate, threshold, or module function -- exactly the scope the coordinator specified. No file outside the plan's declared `files_modified` was touched; `curvature_probe.py` confirmed unmodified across all three commits in this plan.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb` is committed with outputs, satisfying CLAUDE.md's mandatory Swiss roll requirement for the local mean-curvature estimator before any real-data run.
- **Plan 02.5-06** (stage-1 pre-registration) has two disclosure obligations flowing from this notebook: (1) the full measured read-out above, including the scale-bias/noise decomposition, as input to what the pre-registration proposes; (2) an explicit open decision to propose and ratify -- whether stage 1's gate should be evaluated at the per-point scale (rho=0.6712 here) or the region scale Phase 4's own quantile partitioning actually consumes (rho=0.8406 here). This plan does not resolve that question; it only supplies the measured evidence on both sides. Framing note for 02.5-06: this is a question about which scale correctly matches Phase 4's downstream consumption pattern, not a response to the 0.6712 number falling short of a threshold.
- **Forward dependency for plan 02.5-07:** if 02.5-06 ratifies a region-level gate (or any region-level metric as non-gating context), plan 02.5-07's sweep runner will need a TESTED module function for region aggregation in `curvature_probe.py`, not the notebook-inline numpy used here -- this notebook's banding code is diagnostic-only by design and was deliberately not promoted to the module.
- No sealed file touched: `cae.py`, `topoae.py`, `curvature.py`, `curvature_probe.py`, `cache.py`, `mknn.py`, `geometry_probes.py`, `subsample.py`, `src/effdim/`, and `pyproject.toml` all confirmed unmodified (`git status --porcelain` empty for all of them) across all three commits in this plan.
- Plan 02.5-04 (parallel wave-4 sibling, disjoint `files_modified`) is already complete; this plan closes out wave 4.

---
*Phase: 02.5-local-curvature-feasibility-cae-re-gate*
*Completed: 2026-08-08*

## Self-Check: PASSED

- FOUND: `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb`
- FOUND: `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-05-SUMMARY.md`
- FOUND commit: `4768fa3` (Task 1)
- FOUND commit: `0d0c05a` (Task 2)
- FOUND commit: `b7ac57f` (Task 3 checkpoint amendment)
