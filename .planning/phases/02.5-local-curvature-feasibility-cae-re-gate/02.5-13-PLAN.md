---
phase: 02.5-local-curvature-feasibility-cae-re-gate
plan: 13
type: execute
wave: 12
depends_on: ["02.5-12"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-AMENDMENT-01.md
  - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md
  - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/COVERAGE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [D-04, D-13, D-14, D-15]
must_haves:
  truths:
    - "Phase 3's dependency in ROADMAP.md is retargeted by a dated, additive amendment that retracts nothing, because it currently names a global-scoped PASS from Phase 02.4 which sealed FAIL — a dead pointer, not a live gate (D-13)."
    - "The amendment states what Phase 3 may now start on, in the branch this phase actually reached, and says why in its own words (D-13)."
    - "A dated amendment revisits 02.1's sealed graph-native answer against 02.1-AMENDMENT-02.md's symmetric falsifier, arguing explicitly why a locally-scoped curvature result is or is not close enough to a falsifier written on DISTORTION, a global distance-preservation statistic — and nothing sealed is reopened by the revisit itself (D-14)."
    - "02.5-FINDINGS.md carries a stage-2 section recording both gates, the routing taken, and the limitations, alongside the stage-1 boundary it already holds (D-04)."
    - "COVERAGE.md exists carrying the reasoned no-external-API declaration."
    - "REQUIREMENTS.md records this phase's outcome without marking any CURV-0N requirement complete, because this phase deliberately built parallel machinery rather than filling Phase 3's stubs."
    - statement: "A human confirmed both amendments read correctly, that they are additive and retract nothing, and that no sealed verdict was reopened."
      verification: backstop
  artifacts:
    - .planning/ROADMAP.md
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-AMENDMENT-01.md
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/COVERAGE.md
  key_links:
    - "Phase 3's Depends-on line -> Phase 02.4 PASS -> sealed FAIL: this is the dead pointer D-13 exists to fix, and leaving it makes Phase 3 unstartable no matter what this phase measured."
    - "02.5's local-scoped result -> 02.1-AMENDMENT-02.md's DISTORTION-written falsifier: the two are measured on different statistics, and D-14's requirement is that the difference be argued in the open rather than assumed either way."
  prohibitions:
    - statement: "Every ROADMAP and amendment change is additive; nothing above a dated note is deleted, rewritten, or retracted"
      status: planned
      verification: manual
    - statement: "No sealed verdict is reopened, softened, or recomputed — GATE_VERDICT, CAE_VERDICT, and TOPOAE_VERDICT all remain FAIL as measured, and the stage-2 verdict sealed in plan 02.5-12 is not revisited"
      status: planned
      verification: manual
    - statement: "02.1's sealed GEOM-04 answer is revisited by argument in a dated amendment and is not itself reopened or overturned by this plan"
      status: planned
      verification: manual
    - statement: "No CURV-0N requirement is marked complete; this phase built parallel machinery and left Phase 3's stubs unimplemented"
      status: planned
      verification: manual
    - statement: "No code file is modified by this plan; it is documentation only"
      status: planned
      verification: manual
    - statement: "src/effdim/, pyproject.toml, and every notebooks/ file are untouched"
      status: planned
      verification: manual
---

<objective>
Discharge the phase's outward-facing obligations: retarget Phase 3's dead-pointer dependency,
argue 02.1's falsifier revisit in the open, complete the phase record, and close the coverage gate.

Purpose: Phase 3's `Depends on` line currently reads "Phase 02.4 **PASS** (the operative
precondition)". Phase 02.4 sealed FAIL. That makes the line unsatisfiable as written — a dead
pointer, not a live gate — and it stays dead whatever this phase measured. D-13 fixes it. D-14
obliges a dated revisit of 02.1's sealed graph-native answer on **any** PASS, including the
raw-points branch, on the user's stated reasoning that a stale sealed decision should not be left
standing unexamined.

Output: a dated additive amendment to ROADMAP.md, `02.5-AMENDMENT-01.md`, the stage-2 section of
`02.5-FINDINGS.md`, `COVERAGE.md`, and a REQUIREMENTS.md ledger note.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-CONTEXT.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-12-SUMMARY.md
@CLAUDE.md
</context>

<preconditions>
This plan runs on **both** phase branches, with branch-dependent content.

**On a stage-1 GO followed by any stage-2 outcome:** `depends_on: ["02.5-12"]` as written, and
plan 02.5-12's blocking checkpoint has recorded the routing branch.

**On a stage-1 NO-GO:** plans 02.5-08 through 02.5-12 were cancelled at plan 02.5-07's checkpoint —
stage 2 is moot, not deferred. Before executing this plan, amend its `depends_on` from
`["02.5-12"]` to `["02.5-07"]`, an explicit human-gated re-pointing recorded at that checkpoint
rather than a silent dependency skip. On that branch, Task 2 (D-14) does **not** fire, because
D-14 is conditioned on a PASS and there is none; record that plainly rather than omitting the task.

The executor asserts which branch applies by reading `02.5-07-SUMMARY.md` and, if it exists,
`02.5-12-SUMMARY.md`, and halts if the branch cannot be determined.
</preconditions>

<tasks>

<task type="auto">
  <name>Task 1: Retarget Phase 3's dependency by a dated, additive ROADMAP amendment (D-13)</name>
  <files>.planning/ROADMAP.md</files>
  <precondition>Plan 02.5-07's go/no-go checkpoint is resolved, and on a GO branch plan 02.5-12's routing checkpoint is also resolved; the branch is determinable from the SUMMARYs.</precondition>
  <read_first>
    - .planning/ROADMAP.md § "### Phase 3: Decoder & Curvature Field" in full — including its existing amendment stack and the 2026-08-07 re-scope note, so the new note matches their voice and placement
    - .planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-PREREGISTRATION-AMENDMENT-02.md (how a change is recorded additively without rewriting anything above it)
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md (the measured result the new dependency points at)
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-12-SUMMARY.md (the sealed stage-2 verdict and the routing branch), or 02.5-07-SUMMARY.md on the no-go branch
  </read_first>
  <action>
Edit `.planning/ROADMAP.md` with scoped `Edit` calls only. **Never rewrite the file with `Write`** —
a whole-file write destroys every phase entry outside the diff window.

Append a dated note to Phase 3's entry, below the existing 2026-08-07 re-scope note, in the same
`>` blockquote style the existing amendments use. Delete nothing above it. Title it
`**DEPENDENCY RETARGETED (<today>, additive — nothing above is deleted or retracted).**`

Content:
- **The dead pointer.** Quote the current `Depends on` line's operative clause — "Phase 02.4
  **PASS** (the operative precondition — Phase 3 must check its verdict artifact before running any
  expensive cell; until 02.4 exists and passes, Phase 3 stays blocked)" — and state that Phase 02.4
  sealed `TOPOAE_VERDICT = FAIL` on 2026-08-07, making the clause unsatisfiable as written: a dead
  pointer rather than a live gate. State that this is true independently of anything Phase 02.5
  measured, which is why the retarget happens on every branch.
- **What it now points at**, per the branch reached:
  - *Charts win:* Phase 3's precondition becomes Phase 02.5's stage-2 substrate verdict reading
    PASS with `substrate = "cae_charts"`, and the machine-readable contract Phase 3 must consume is
    `notebooks/.cache/curvature_handoff_43cf438bc944c509.json`, which names the working dimension,
    neighbourhood rule, density correction, estimator variant, curvature convention, and cache
    stems the field must be reproduced under. Name the specific reason the handoff and not just the
    verdict is the precondition: reconstructing the field under different settings than the ones
    that passed the gate is how a validated result quietly stops being validated.
  - *Raw points win, or tie inside the margin:* Phase 3's precondition becomes the same stage-2
    verdict with `substrate = "raw_points"`, and the note records that the fork into the
    Ollivier-Ricci / intrinsic-curvature phase is now open. Restate that this is not "the CAE is
    unnecessary, move on", and that Ollivier-Ricci is a different mathematical object — intrinsic,
    graph-and-optimal-transport, no ambient space — so adopting it would require amending Phase 4's
    requirement text, which currently refers to `|H|` quantiles.
  - *Neither clears:* apply the resolution ratified in `02.5-PREREGISTRATION-STAGE2.md` §6 and quote
    it; do not decide again here.
  - *Stage-1 NO-GO:* Phase 3's precondition becomes Phase 02.5's stage-1 negative finding — local
    curvature is not estimable at the PU regime — and the note names the responsible axis from
    `02.5-FINDINGS.md` §3 and points at the Ollivier-Ricci fork as the live remaining option, with
    the reason it is not subject to the sampling constraint that failed. State that stage 2 is moot
    rather than deferred.
- **What is not changed.** Phase 3's success criteria and DEC/CURV requirement list are already
  recorded as stale by the 2026-08-07 re-scope note and are not revised here — this note retargets
  the dependency only. Every sealed verdict remains as measured. Nothing above the note is deleted.

Then update Phase 02.5's own ROADMAP entry. Its `**Requirements**` line (D-00..D-15 as the
de-facto set), its `**Status**` line, and its thirteen-plan checklist were already filled at
planning time — verify they are present and accurate rather than rewriting them. What this task
adds is the outcome: tick each completed plan's checkbox, and append an `**Outcome**` paragraph to
the entry recording the stage-1 boundary result, the stage-2 verdict (or that stage 2 was moot on
a NO-GO), and the branch routed to — matching how Phase 02.4's entry carries its sealed Outcome
paragraph. Use scoped `Edit` calls for each.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; python -c "
s=open('.planning/ROADMAP.md').read()
assert 'DEPENDENCY RETARGETED' in s, 'D-13 amendment note absent'
assert 'dead pointer' in s.lower()
assert '02.5' in s
# additive check: every pre-existing phase heading survives
for h in ['### Phase 1','### Phase 2','### Phase 02.1','### Phase 02.2','### Phase 02.4','### Phase 02.5','### Phase 3','### Phase 4']:
    assert h in s, f'phase heading lost: {h}'
assert 'Not yet defined' not in s.split('### Phase 02.5')[1].split('### Phase 3')[0], 'Phase 02.5 Requirements line still a placeholder'
assert '13 plans' in s
print('roadmap amendment OK')
" &amp;&amp; git diff --stat .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains a dated `**DEPENDENCY RETARGETED**` note under Phase 3, below the existing 2026-08-07 re-scope note
    - Every pre-existing phase heading survives: `### Phase 1`, `### Phase 2`, `### Phase 02.1`, `### Phase 02.2`, `### Phase 02.3`, `### Phase 02.4`, `### Phase 02.5`, `### Phase 3`, `### Phase 4`
    - `git diff .planning/ROADMAP.md` shows additions and the two placeholder-line replacements in Phase 02.5's own entry, and no deletion of any existing amendment, note, or success criterion
    - Phase 02.5's entry no longer reads "Not yet defined" for Requirements, records `13 plans`, and lists all thirteen plans with one-line objectives
    - The note names the branch actually reached and, on a charts-win branch, names `curvature_handoff_43cf438bc944c509.json` as the contract Phase 3 consumes
  </acceptance_criteria>
  <reversibility rating="costly">Amending a dependency changes what Phase 3 is allowed to start on. CONTEXT.md rates D-13 costly. The amendment is additive and reversible in principle by a further dated note, but any downstream phase that started against the retargeted dependency would need re-examining.</reversibility>
  <done>Phase 3's dead-pointer dependency is retargeted by a dated additive note that names what it now points at and why, nothing above it is deleted, and Phase 02.5's own ROADMAP entry records its requirements, status, and thirteen plans.</done>
</task>

<task type="auto">
  <name>Task 2: Revisit 02.1's sealed graph-native answer by dated amendment (D-14)</name>
  <files>.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-AMENDMENT-01.md</files>
  <read_first>
    - .planning/phases/02.1-geometry-representation-research-inserted/02.1-AMENDMENT-02.md in full — especially the symmetric falsifier's exact wording and §6.4's own record that DISTORTION is "nearly decoupled" from held-out reconstruction
    - .planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-PREREGISTRATION-AMENDMENT-02.md (the template for recording a withdrawn or re-examined justification without changing a constant)
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md and 02.5-12-SUMMARY.md (the measured result the argument turns on)
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-CONTEXT.md D-14 and its Deferred Ideas section (the extrinsic-versus-intrinsic distinction, in the user's own framing)
  </read_first>
  <action>
**Branch check first.** D-14 fires on **any** PASS, including the raw-points branch. If plan
02.5-12 sealed a non-PASS verdict, or if stage 1 returned NO-GO, D-14 does not fire: write
`02.5-AMENDMENT-01.md` anyway as a short record stating that the obligation was checked, that its
trigger condition (any PASS) was not met, and what the actual outcome was — so a later reader sees
the obligation was considered and correctly not discharged, rather than silently skipped. Then
stop this task.

**On a PASS of either substrate**, write
`.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-AMENDMENT-01.md`, following
`02.1-AMENDMENT-02.md`'s and `02.4-PREREGISTRATION-AMENDMENT-02.md`'s shape — a dated document
committed alone, arguing openly, retracting nothing.

Header: `status: amendment`, `phase: 02.5-...`, `created: <today>`,
`amends: .planning/phases/02.1-geometry-representation-research-inserted/02.1-AMENDMENT-02.md`,
`changes_no_constant: true`, and a `trigger:` citing D-14.

Sections:

**§1 The obligation.** Quote D-14: any 02.5 PASS obliges a revisit of 02.1's sealed graph-native
answer by dated amendment, including the raw-points branch, on the user's stated reasoning that a
stale sealed decision should not be left standing unexamined. State that nothing sealed is reopened
by the revisit itself.

**§2 What 02.1's symmetric falsifier actually says.** Quote it verbatim from
`02.1-AMENDMENT-02.md`: graph-native is overturned when "a coordinate candidate's `DISTORTION`
drops materially below the classical failure at a tractable working dimension."

**§3 Why 02.5's result is not literally that condition — the argument D-14 requires be made in the
open.** Three distinct reasons, each stated rather than gestured at:
  (a) **Different statistic.** The falsifier is written on `DISTORTION`, a *global*
      distance-preservation statistic. 02.5 measured Spearman rank agreement between an estimated
      *local* mean-curvature field and an analytic one. These are not the same quantity and one
      does not bound the other.
  (b) **Different scope.** Every FAIL in this milestone is a global statistic and every
      local-scoped gate measured has passed — `02.4-FINDINGS.md`'s cross-phase argument, which is
      why this phase exists. A local PASS is therefore *expected* under that reading and is weak
      evidence about any global statistic, including `DISTORTION`.
  (c) **02.1's own recorded doubt.** `02.1-AMENDMENT-02.md` §6.4 already records that `DISTORTION`
      is "nearly decoupled" from held-out reconstruction — classical `(40,0)`, worst distortion of
      the three ladder rungs at `0.179641`, reconstructed *best* of all four arms. So the falsifier
      is written on a statistic 02.1 itself flagged as possibly measuring the wrong thing, and
      §6.5's seed-sensitivity study that would settle it was never run. A revisit that leaned on
      `DISTORTION` in either direction would inherit that doubt.

**§4 What 02.5's result does bear on.** State the branch honestly.
  - *Charts win:* a coordinate-producing, chart-based representation supports a locally-scoped
    curvature field better than raw ambient points do at the PU regime. That is a point in favour
    of coordinate-producing representations on a *local* axis, and it does not clear 02.1's
    global-`DISTORTION` bar.
  - *Raw points win:* no learned representation was needed at all, so this result is silent on
    02.1's coordinate-versus-graph-native question, and it opens the Ollivier-Ricci fork that
    finally exercises 02.1's own sealed `GEOM-04` recommendation — unused since 2026-08-05. Note
    that the fork is not a third estimator of the same quantity: `H` is extrinsic, Ollivier-Ricci
    is intrinsic, defined on the k-NN graph via optimal transport with no ambient space involved,
    and 02.1's own record states this ("CURV-03's extrinsic mean-curvature vector becomes an
    intrinsic Ricci scalar — a different mathematical object, not a renaming"). Phase 4's
    requirement text, which refers to `|H|` quantiles, would need amending if the intrinsic route
    is adopted.

**§5 Conclusion.** State plainly whether the falsifier's condition is met — it is not, on the
argument in §3 — and that `GEOM-04 = Ollivier-Ricci graph-native` therefore stands, unmodified and
unreopened. State what *would* meet it, so the falsifier remains live rather than becoming
unfalsifiable: a coordinate candidate measured on `DISTORTION` itself, at a tractable working
dimension, dropping materially below the classical failure — and note that 02.1 §6.5's unrun
seed-sensitivity study is the missing evidence that would make such a measurement interpretable.

**§6 What this amendment changes.** Nothing. No constant, no sealed verdict, no recommendation.
It records an argument that D-14 required be made rather than assumed, in either direction.

Commit this document alone.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; python -c "
import re
p='.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-AMENDMENT-01.md'
s=open(p).read()
assert 'D-14' in s
assert '02.1-AMENDMENT-02' in s
assert 'GEOM-04' in s
import os
# on a PASS branch the full argument is required; on a non-PASS branch the short record is
if 'not met' in s or 'did not fire' in s:
    print('D-14 not-fired record OK')
else:
    for sec in ['## 1','## 2','## 3','## 4','## 5','## 6']:
        assert re.search(r'^'+re.escape(sec), s, re.M), f'missing section {sec}'
    assert 'DISTORTION' in s and 'Ollivier' in s
    print('D-14 amendment OK')
" &amp;&amp; SHA=$(git log --format=%H -1 --diff-filter=A -- .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-AMENDMENT-01.md) &amp;&amp; test -z "$(git show --name-only --format= "$SHA" | grep -v '02.5-AMENDMENT-01.md' | grep -v '^$' || true)" &amp;&amp; echo "committed alone"</automated>
  </verify>
  <acceptance_criteria>
    - `02.5-AMENDMENT-01.md` exists, cites D-14, `02.1-AMENDMENT-02.md`, and `GEOM-04`
    - On a PASS branch: sections 1 through 6 present, `DISTORTION` and `Ollivier` both named, and §3 gives all three reasons the falsifier's condition is not literally met
    - On a non-PASS or NO-GO branch: the document records that the obligation was checked and its trigger not met, with the actual outcome named
    - The commit adding it contains only that file
    - `git status --porcelain .planning/phases/02.1-geometry-representation-research-inserted/` produces no output — 02.1's sealed documents are argued about, never edited
  </acceptance_criteria>
  <reversibility rating="reversible">A dated argument that changes no constant and reopens nothing; a later phase may argue the other way in its own dated amendment.</reversibility>
  <done>02.1's sealed graph-native answer is revisited by a dated amendment that argues the DISTORTION-versus-local-curvature difference explicitly in either direction, states what would meet the falsifier so it stays live, changes no constant, and leaves 02.1's own documents untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the phase record — findings, coverage declaration, and the requirements ledger</name>
  <files>.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md, .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/COVERAGE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md (the stage-1 sections 1-8 plan 02.5-07 wrote; this task appends, it does not rewrite)
    - .planning/REQUIREMENTS.md lines 87-94 and 206-213 (the CURV-01..08 entries and their Phase 3 traceability rows, which must stay Pending)
    - .planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-08-SUMMARY.md (how 02.4 minted requirement IDs into REQUIREMENTS.md and recorded the ledger-sync note)
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-12-SUMMARY.md, or 02.5-07-SUMMARY.md on the no-go branch
  </read_first>
  <action>
**Append** stage-2 sections to `02.5-FINDINGS.md` — do not rewrite sections 1-8. Add:

**§9 Stage 2, as measured.** Gate A's arms and margin against `THRESH_MARGIN`; Gate B's three
pairwise Spearman values and `seed_spread` against `THRESH_SEED_SPREAD`; the sealed verdict; and
the per-seed reconstruction statistics reported **separately** from the curvature numbers, with
one sentence naming why they are kept apart (D-09's counter-risk: a smooth reconstruction
guarantees nothing about its second derivative). On a stage-1 NO-GO branch, replace this section
with a short statement that stage 2 was moot rather than deferred, and why.

**§10 The routing taken and its consequence.** Which of D-12's branches applied, under the rule
ratified at plan 02.5-10's checkpoint, and what it means for Phase 3 and for the Ollivier-Ricci
fork.

**§11 Stage-2 limitations.** Carry forward `02.5-PREREGISTRATION-STAGE2.md` §9's four, plus
anything the runs revealed. At minimum, state whether D-11's retrain trigger fired and whether the
fallback was used; if it was, state that its hyperparameters were ratified with the failing result
already visible, which is the residual risk D-11 required be stated plainly.

**§12 What this phase does not claim.** Three things worth writing down: the fixtures are synthetic
proxies whose curvature scale was chosen rather than measured, so the boundary locates where the
*estimator* breaks and not where the *data* is; the analytic ground truth and the estimator were
both written by this phase, mitigated but not eliminated by the finite-difference cross-checks;
and nothing here revises any sealed verdict — `GATE_VERDICT`, `CAE_VERDICT`, and `TOPOAE_VERDICT`
all remain FAIL as measured, and this phase added a differently-scoped measurement rather than
reinterpreting an old one.

Write `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/COVERAGE.md` containing
exactly the reasoned declaration line and nothing that would read as a fabricated capability
matrix:

`No external API integration: this phase performs local, offline numerical computation on Phase 1's already-frozen cached subsample and Phase 02.2's already-sealed CAE fits — no network surface, no SDK, no service, and no new package (02.5-RESEARCH.md § Package Legitimacy Audit records four already-pinned dependencies and zero new ones).`

Then update `.planning/REQUIREMENTS.md` additively, following 02.4-08's ledger pattern:
- Add a short Phase 02.5 record noting that the phase's de-facto requirement set is
  `02.5-CONTEXT.md`'s D-00..D-15 and that no milestone-level REQ-IDs were minted for it, mirroring
  the note 02.4-02 recorded when it found no R4/R5/R6 entries.
- **Leave CURV-01..08 marked Pending and mapped to Phase 3.** Add one line stating explicitly that
  Phase 02.5 built parallel, phase-scoped machinery in `curvature_probe.py` and
  `chart_curvature.py` and deliberately did **not** implement `curvature.py`'s stubs, so no CURV
  requirement is completed by this phase. This is the ledger-visible form of the OQ-1 resolution.
- Record the D-15 handoff artifact's path if one was written, or state that none was.

Commit the three documents together with a message naming them as the phase record.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; python -c "
import re
f=open('.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md').read()
for sec in ['## 1','## 8','## 9','## 10','## 11','## 12']:
    assert re.search(r'^'+re.escape(sec), f, re.M), f'missing section {sec}'
c=open('.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/COVERAGE.md').read()
assert c.strip().startswith('No external API integration:'), 'coverage declaration must lead with the reasoned no-API line'
r=open('.planning/REQUIREMENTS.md').read()
assert '02.5' in r
for cid in ['CURV-01','CURV-02','CURV-03','CURV-04','CURV-05','CURV-06','CURV-07','CURV-08']:
    assert cid in r
assert r.count('| Phase 3 | Pending |') >= 8, 'CURV rows must remain Pending against Phase 3'
print('phase record OK')
" &amp;&amp; python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import curvature; 
try:
    curvature.mean_curvature_vector(None, None); print('ERROR: stub was implemented'); sys.exit(1)
except NotImplementedError: print('Phase 3 stubs still unimplemented')"</automated>
  </verify>
  <acceptance_criteria>
    - `02.5-FINDINGS.md` has sections 1 through 12; sections 1-8 are unchanged from plan 02.5-07 (verify with `git diff` showing additions only)
    - `COVERAGE.md` exists and its first non-empty line begins `No external API integration:` followed by a reason
    - `REQUIREMENTS.md` records the Phase 02.5 note and still shows all eight CURV rows as `Phase 3 | Pending`
    - `curvature.mean_curvature_vector` still raises `NotImplementedError`
    - `git status --porcelain notebooks/ src/effdim pyproject.toml` produces no output — this plan is documentation only
  </acceptance_criteria>
  <done>The phase record is complete: findings carry both stages plus the routing and the limitations, the coverage gate is closed with a reasoned declaration rather than a fabricated matrix, and the requirements ledger records the outcome while leaving every CURV requirement Pending against Phase 3.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm the amendments are additive and nothing sealed was reopened</name>
  <what-built>
The phase's outward-facing obligations are discharged: a dated `**DEPENDENCY RETARGETED**` note
under Phase 3 in `ROADMAP.md` replacing a dead pointer to a PASS that Phase 02.4 never produced;
`02.5-AMENDMENT-01.md` arguing D-14's revisit of 02.1's sealed graph-native answer against a
falsifier written on a different statistic; `02.5-FINDINGS.md` completed with stage 2, the routing,
and the limitations; `COVERAGE.md` closing the coverage gate with a reasoned no-API declaration;
and `REQUIREMENTS.md` recording the outcome with every CURV requirement left Pending against
Phase 3.
  </what-built>
  <how-to-verify>
1. `git diff .planning/ROADMAP.md` — confirm it is **additions only** plus the two placeholder-line
   replacements inside Phase 02.5's own entry. No existing amendment, note, success criterion, or
   phase heading may be deleted. This is the check that matters most: a whole-file rewrite of
   ROADMAP.md would silently destroy phase entries outside the diff window.
2. Read the new Phase 3 note. Confirm it names the dead pointer explicitly, names what the
   dependency now points at for the branch actually reached, and — on a charts-win branch — names
   the handoff artifact rather than only the verdict, with the reason.
3. Read `02.5-AMENDMENT-01.md` §3. D-14's requirement is that the difference between a
   locally-scoped curvature result and a falsifier written on global `DISTORTION` be argued **in
   the open**. Confirm all three reasons are given, including 02.1's own §6.4 record that
   `DISTORTION` is nearly decoupled from held-out reconstruction. Confirm §5 states what *would*
   meet the falsifier, so it stays live rather than becoming unfalsifiable.
4. Confirm `git status --porcelain .planning/phases/02.1-*/` is empty — 02.1's sealed documents are
   argued about, never edited. Same for the 02.2 and 02.4 phase directories.
5. Confirm every sealed verdict is untouched: `GATE_VERDICT`, `CAE_VERDICT`, `TOPOAE_VERDICT` all
   still FAIL as measured, and the stage-2 verdict sealed in plan 02.5-12 is unchanged.
6. Confirm `curvature.mean_curvature_vector` still raises `NotImplementedError` and that
   `REQUIREMENTS.md` shows all eight CURV rows as Pending against Phase 3 — this phase built
   parallel machinery and did not deliver Phase 3's requirements ahead of schedule.
7. Confirm `git status --porcelain notebooks/ src/effdim pyproject.toml` is empty — this plan
   touches documentation only.
  </how-to-verify>
  <resume-signal>Type "approved" to seal phase 02.5, or name what in the amendments reads wrong or is not additive.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New files (none exist before this phase; every symbol below is created by phase 02.5):

- `notebooks/pu_manifold/curvature_probe.py` — `CURVATURE_CONVENTION`, `swiss_roll_analytic_H`,
  `swiss_roll_analytic_H_scaled`, `make_swiss_roll_fixture`, `local_tangent_basis`,
  `centroid_mean_curvature`, `mean_curvature_norm`, `spearman_gate_statistic`,
  `median_relative_error`, `local_density_weights`, `gaussian_bump_values`,
  `graph_mean_curvature`, `make_graph_of_function_fixture`, `make_flat_fixture`,
  `quadric_fit_curvature`, `quadric_mean_curvature`, `estimator_agreement`, `permutation_null`,
  `measure_cell`, `STAGE1_GATING_METRICS`, `STAGE2_GATING_METRICS`, `GATE_DIRECTIONS`,
  `STAGE1_VERDICT_RULE`, `STAGE2_VERDICT_RULE`, `_apply_gates`, `verdict_from_stage1_metrics`,
  `verdict_from_stage2_metrics`, `write_curvature_verdict`, `write_curvature_handoff`,
  `clear_stale_curvature_handoff`
- `notebooks/pu_manifold/chart_curvature.py` — `ZERO_SECOND_DERIVATIVE_ACTIVATIONS`,
  `assert_c2_activation`, `chart_decoder_map`, `chart_mean_curvature`,
  `chart_mean_curvature_norm`, `chart_curvature_field`
- `notebooks/pu_manifold/tests/test_curvature_probe.py` — all `test_*` functions therein
- `notebooks/diagnostics/curvature_feasibility_sweep_run.py` — `SWEEP_CFG`, `BASE_CELL`,
  `SWEEP_AXES`, `NULL_QUANTILE`, `N_PERMUTATIONS`, `SPEARMAN_ABSOLUTE_FLOOR`, `K_GATING`,
  `K_DENSITY`, `D_PRIMARY`, `LADDER`, `WALL_CLOCK_BUDGET_S`, `PREREG_SHA`, `main`
- `notebooks/diagnostics/cae_local_regate_run.py` — `FIT_KEY`, `SEALED_SEEDS`,
  `FIXTURE_CAE_ARCH`, `FIXTURE_CAE_TRAIN_CFG`, `FIXTURE_SEEDS`, `GATE_A_CELL`, `THRESH_MARGIN`,
  `THRESH_SEED_SPREAD`, `RETRAIN_TRIGGER`, `STAGE2_PREREG_SHA`, `SUBSAMPLE_STEM`, `main`
- `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb`,
  `notebooks/02.5_swiss_roll_chart_curvature_check.ipynb`
- `.planning/phases/02.5-.../02.5-PREREGISTRATION.md`,
  `.planning/phases/02.5-.../02.5-PREREGISTRATION-STAGE2.md`,
  `.planning/phases/02.5-.../02.5-FINDINGS.md`,
  `.planning/phases/02.5-.../02.5-AMENDMENT-01.md`,
  `.planning/phases/02.5-.../COVERAGE.md`
- Cache artifacts (gitignored): `curvature_feasibility_{stage1_key}.json`,
  `curvature_verdict_stage1_{stage1_key}.json`, `curvature_gate_a_{gate_a_key}.json`,
  `curvature_gate_b_{fit_key}.json`, `curvature_fixture_cae_{gate_a_key}_seed{seed}.npz`,
  `curvature_verdict_stage2_{fit_key}.json`, `curvature_handoff_{fit_key}.json`
</artifacts_this_phase_produces>

<assumption_delta_decision>
The `assumption-delta scan` detector fired `detected: true` with two signals — a `pluralization`
hit on "second" and a `chosen` hit on "chosen". Both literal hits are false positives ("second
fundamental form" is a mathematical term; "gates chosen on local criteria" is gate design, not a
derived-to-chosen transition). But a genuine assumption delta of the same family does exist in
this phase and is recorded here rather than dismissed with the literals.

- **The noun that is now primary: `substrate`.** Until this phase, the milestone's identity model
  had one slot for "the representation Phase 3 decodes from" and each phase competed to fill it
  (Isomap coordinates, then Krein retention, then the CAE, then TopoAE). Phase 02.5 introduces a
  **second** candidate that is not a learned representation at all — raw ambient points — and asks
  which *substrate* supports a trustworthy curvature field.
- **Decision: `promote`.** D-09 already made this call and this plan set implements it: D-09
  states that it "supersedes the ROADMAP re-scope's conception of stage 2 as 'a locally-scoped gate
  on the CAE'", because if the estimator works on raw 768-d points no learned model is needed at
  all. The generalized noun `substrate` is therefore primary, and "the CAE" is demoted to one
  variant of it. The handoff schema follows: `write_curvature_handoff`'s payload leads with
  `substrate` (`"cae_charts"` or `"raw_points"`) rather than assuming a model, and D-13's ROADMAP
  amendment retargets Phase 3's dependency to "02.5's substrate verdict" rather than to a named
  model. This is a promote, not an add-alongside — there is no residual slot that still requires a
  learned representation.
- **Companion invariant (accepted, implemented):** the suggested contract test for a generalized
  identity is that every confirmed default round-trips through the primary use-path for every
  supported variant. Its concrete form here is
  `test_pass_only_handoff_and_stale_deletion` (plan 02.5-04), which exercises the handoff writer's
  full contract, combined with `write_curvature_handoff` raising on any absent contract field —
  so a future substrate that cannot name a working dimension, neighbourhood rule, density
  correction, and estimator variant cannot be handed off at all. A later phase reintroducing the
  singular "the representation is a trained model" assumption would have to drop the `substrate`
  field to do so, and the writer refuses.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| existing ROADMAP content -> this plan's edits | Scoped `Edit` only; a whole-file `Write` destroys phase entries outside the diff window, which is why the verify asserts every phase heading survives |
| 02.1's sealed documents -> this phase's argument | `02.5-AMENDMENT-01.md` argues about them in a separate dated file; `git status --porcelain` on the 02.1 phase directory must be empty |
| this phase's machinery -> Phase 3's CURV requirements | The ledger boundary; REQUIREMENTS.md must still show all eight CURV rows Pending, and `curvature.py`'s stubs must still raise |
| detector output -> recorded decision | The assumption-delta detector fired on two literal false positives; the genuine delta is recorded explicitly rather than dismissed along with them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.5-27 | Tampering | ROADMAP.md phase entries destroyed by a whole-file write | high | mitigate | Scoped `Edit` calls only, stated as a prohibition in the action; the verify asserts all nine pre-existing phase headings survive and `git diff --stat` is reviewed at the human checkpoint. |
| T-02.5-05 | Tampering | 02.1's, 02.2's, or 02.4's sealed documents edited rather than argued about | high | mitigate | `02.5-AMENDMENT-01.md` is a separate dated file committed alone; acceptance criteria and checkpoint step 4 assert `git status --porcelain` on those phase directories is empty. |
| T-02.5-21 | Elevation of Privilege | Phase 3's CURV-01..08 marked complete by this phase | medium | mitigate | REQUIREMENTS.md keeps all eight rows `Phase 3 | Pending` with an explicit line recording why; the verify re-asserts `curvature.mean_curvature_vector` raises `NotImplementedError`. |
| T-02.5-28 | Repudiation | D-14's obligation silently skipped on a non-PASS branch | medium | mitigate | On a non-PASS or NO-GO branch the amendment document is still written, recording that the obligation was checked and its trigger not met — visible non-firing rather than absence. |
| T-02.5-29 | Repudiation | the falsifier becoming unfalsifiable by a revisit that argues only one way | medium | mitigate | §5 is required to state what *would* meet the falsifier, and §3 is required to give all three reasons including 02.1's own recorded doubt about the statistic the falsifier is written on. |
| T-02.5-30 | Information Disclosure | a fabricated API capability matrix in COVERAGE.md | low | mitigate | The detector returned `detected: false` for this phase; the reasoned `No external API integration:` declaration is used instead of inventing rows, which the seal-time gate accepts. |
| T-02.5-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs anywhere in this phase; documentation only in this plan. |
</threat_model>

<verification>
- `git diff .planning/ROADMAP.md` shows additions plus Phase 02.5's two placeholder replacements, and no deletions; all nine phase headings survive
- `02.5-AMENDMENT-01.md` exists, cites D-14, `02.1-AMENDMENT-02.md`, and `GEOM-04`, and is committed alone
- `02.5-FINDINGS.md` has sections 1-12 with 1-8 unchanged
- `COVERAGE.md` leads with the reasoned `No external API integration:` line
- `REQUIREMENTS.md` shows all eight CURV rows Pending against Phase 3
- `curvature.mean_curvature_vector` raises `NotImplementedError`
- `git status --porcelain notebooks/ src/effdim pyproject.toml .planning/phases/02.1-* .planning/phases/02.2-* .planning/phases/02.4-*` produces no output
- The Task 4 blocking checkpoint is resolved
</verification>

<success_criteria>
Phase 3's dead-pointer dependency is retargeted additively and dated; 02.1's sealed falsifier is
revisited by an argument made in the open that changes no constant and leaves 02.1's documents
untouched; the phase record carries both stages, the routing, and the limitations; the coverage
gate is closed with a reasoned declaration; and no sealed verdict, no CURV requirement, and no
line of code was touched.
</success_criteria>

<output>
Create `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-13-SUMMARY.md` when done
</output>