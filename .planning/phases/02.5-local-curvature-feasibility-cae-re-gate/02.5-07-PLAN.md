---
phase: 02.5-local-curvature-feasibility-cae-re-gate
plan: 07
type: execute
wave: 6
depends_on: ["02.5-06"]
files_modified:
  - notebooks/diagnostics/curvature_feasibility_sweep_run.py
  - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "A runner executes the pre-registered one-axis-at-a-time sweep from the base cell, scores every cell's Spearman against its own permutation null, and persists the whole result through cache.json_cache under a key hashed from SWEEP_CFG (D-01, D-02, D-04)."
    - "Every constant in the runner is copied verbatim from the ratified 02.5-PREREGISTRATION.md, and the runner refuses to start unless that document's commit is proved an ancestor of HEAD (D-01..D-08)."
    - "The output is a feasibility boundary — for each swept axis, where Spearman crosses the null-calibrated threshold — and an explicit statement of where the PU regime sits relative to that frontier and which axis is responsible, not a bare verdict (D-04)."
    - "Both the density-corrected and the uncorrected estimator run at every cell, so the correction's effect is measured everywhere rather than assumed (D-06)."
    - "The Swiss roll d=2 anchor and the graph-of-function family at n_bumps 1 and 8 are all swept, and their frontiers are reconciled into one report that names any disagreement between them (D-03)."
    - "A stage-1 verdict artifact is written on either outcome; no Phase 3 handoff is written by stage 1 under any circumstance."
    - statement: "A human read the feasibility boundary report and made the stage-1 go/no-go decision; on no-go the phase halts with no auto-fallback and plans 02.5-08 through 02.5-13 do not execute as planned."
      verification: backstop
  artifacts:
    - notebooks/diagnostics/curvature_feasibility_sweep_run.py
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md
  key_links:
    - "ratified pre-registration -> verbatim constants in the runner -> cache.config_key(SWEEP_CFG) -> the artifact key: if a constant in the runner drifts from the document, the artifact key changes and the mismatch is visible rather than silent."
    - "per-cell Spearman -> per-cell permutation null -> per-axis crossing point -> the PU regime's position: the boundary is only meaningful if each cell carries its own null, because the null's tightness depends on n, which is one of the swept axes."
  prohibitions:
    - statement: "No constant is changed between the ratified pre-registration and the runner; a needed change requires a fresh amendment and a full re-run, never a silent edit"
      status: planned
      verification: manual
    - statement: "Stage 1 writes no Phase 3 handoff under any circumstance; the handoff belongs to stage 2's substrate verdict"
      status: planned
      verification: manual
    - statement: "On the wrong side of the boundary the phase halts for a user decision; no fallback is selected automatically"
      status: planned
      verification: manual
    - statement: "The report emits no recommended k for downstream use"
      status: planned
      verification: manual
    - statement: "notebooks/pu_manifold/cae.py, topoae.py, curvature.py, cache.py are never edited; no existing runner script is deleted or rewritten"
      status: planned
      verification: manual
    - statement: "No sealed verdict is reopened, softened, or recomputed"
      status: planned
      verification: manual
---

<objective>
Run stage 1 and emit the feasibility boundary — then stop, and let a human decide whether stage 2
runs at all.

Purpose: this is the phase's structural gate. The ROADMAP states it plainly: **if local curvature
is not estimable in the PU regime, the CAE re-gate is moot and must not be run** — that negative
result is itself a complete and reportable outcome. D-04 makes the output a boundary rather than a
binary, because a boundary says how far off the regime is and which axis is responsible, and a
binary is unactionable.

Output: `notebooks/diagnostics/curvature_feasibility_sweep_run.py`, the cached sweep and stage-1
verdict artifacts, `02.5-FINDINGS.md` with the boundary, and a resolved go/no-go decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-CONTEXT.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-RESEARCH.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PATTERNS.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PREREGISTRATION.md
@.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the stage-1 feasibility sweep runner</name>
  <files>notebooks/diagnostics/curvature_feasibility_sweep_run.py</files>
  <read_first>
    - notebooks/diagnostics/topoae_evaluate_run.py lines 1-90 (module-header shape, the precondition-halts-rather-than-regenerates paragraph, the verbatim-copied-constants block with its explanatory comment, the `_banner`/`_load_npz`/`_load_json` helpers)
    - notebooks/diagnostics/topoae_lambda_sweep_run.py (the sweep-over-a-grid shape and how it persists per-cell results)
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PREREGISTRATION.md (every constant, copied verbatim — this is the source of truth)
    - notebooks/pu_manifold/curvature_probe.py (`measure_cell`, `verdict_from_stage1_metrics`, `write_curvature_verdict`, the fixture makers)
    - notebooks/pu_manifold/cache.py (all 122 lines — `json_cache`, `config_key`, `_manifest_matches`)
  </read_first>
  <action>
Create `notebooks/diagnostics/curvature_feasibility_sweep_run.py`, mirroring
`topoae_evaluate_run.py`'s shape.

**Module docstring** — copy `topoae_evaluate_run.py` lines 1-30's structure. State: this is Phase
02.5's stage-1 feasibility sweep runner; it computes exactly and only the gating metric named in
`curvature_probe.STAGE1_GATING_METRICS` plus the non-gating context metrics; it applies the tested
strict-comparison rule `curvature_probe.verdict_from_stage1_metrics`, never re-derived inline; it
writes `curvature_verdict_stage1_{stage1_key}.json` on PASS and on FAIL alike; **it writes no
Phase 3 handoff under any circumstance** — the handoff belongs to stage 2's substrate verdict
(D-15), and stage 1 answers a different question. State that
`notebooks/pu_manifold/cae.py`, `topoae.py`, and `curvature.py` are never edited by this script.
Give the invocation: `PYTHONPATH=notebooks python notebooks/diagnostics/curvature_feasibility_sweep_run.py`.

**Pre-registered constants, copied verbatim** — with the same explanatory comment
`topoae_evaluate_run.py:45-49` carries: duplicated here rather than imported so the precondition
assertions below run before anything else. Copy from `02.5-PREREGISTRATION.md` §3, §4, §6, §7, §8,
§9: `NULL_QUANTILE`, `N_PERMUTATIONS`, `SPEARMAN_ABSOLUTE_FLOOR`, `K_GATING`, `K_DENSITY`,
`D_PRIMARY`, `LADDER`, `BASE_CELL` (a dict), `SWEEP_AXES` (a dict mapping axis name to the list of
values), `WALL_CLOCK_BUDGET_S`, the fixture parameters (`SIGMA`, `AMPLITUDE`, `DOMAIN_RADIUS`,
`DENSITY_SKEW_VALUES`, `N_BUMPS_VALUES`), and `SEEDS`. Add `PREREG_PATH` and `PREREG_SHA` (the
40-character SHA recorded in §15).

**STEP 0 — preconditions, before any computation.** In this order, each halting with a message
naming what is wrong:
- `02.5-PREREGISTRATION.md` exists at `PREREG_PATH`.
- `git merge-base --is-ancestor {PREREG_SHA} HEAD` exits 0. Use `subprocess.run` and check the
  return code; halt with a message stating that the pre-registration must precede the measurement
  and that this ordering is proved, not asserted. Follow `topoae_evaluate_run.py`'s use of
  `subprocess` for git.
- Every constant in this file matches the value recorded in `02.5-PREREGISTRATION.md`. Implement
  by reading the document and asserting each `NAME = value` appears in it; halt naming any
  constant that drifted. A runner whose constants have drifted from its own pre-registration is
  measuring a different rule than the one that was ratified.
- The module imports succeed and `curvature_probe.CURVATURE_CONVENTION == "trace"`.

**STEP 1 — build the cell list.** `SWEEP_CFG` is a dict containing every constant above plus
`{"phase": "02.5", "stage": 1, "prereg_sha": PREREG_SHA}`. `stage1_key = cache.config_key(SWEEP_CFG)`.
Build the cell list as one axis at a time from `BASE_CELL`: for each axis in `SWEEP_AXES`, for each
value, a cell equal to `BASE_CELL` with that one key replaced. Deduplicate — the base cell appears
once per axis otherwise. Add the `LADDER` rungs and the multi-seed base-cell repeats. Print the
total cell count and the per-cell and total wall-clock estimate before running anything, so an
over-budget sweep is visible before it starts rather than after.

**STEP 2 — run the cells.** For each cell: build the fixture (`make_swiss_roll_fixture` when
`d == 2` and the cell is the anchor, `make_flat_fixture` when the cell's `amplitude` is zero,
`make_graph_of_function_fixture` otherwise), then call `curvature_probe.measure_cell` **twice** —
once with `density_correct=False` and once with `density_correct=True` — so the correction's effect
is measured at every cell rather than assumed (D-06). Wrap each cell in a per-cell time budget; on
exceeding it, record the cell with `{"status": "timed_out", "elapsed_s": ...}` and continue. Never
silently drop a cell.

Compute each cell's threshold as `max(null_threshold, SPEARMAN_ABSOLUTE_FLOOR)` where
`null_threshold` comes from that cell's own `permutation_null` at `NULL_QUANTILE` — per cell,
because the null's tightness depends on `n`, which is a swept axis. Record `clears` per cell.

**STEP 3 — locate the boundary.** For each swept axis, walk its values in order and record the
crossing point: the last value at which `clears` is True and the first at which it is False, or a
statement that the axis clears throughout or fails throughout. Record separately for the
`n_bumps=1` and `n_bumps=8` fixture variants and for the two density regimes. Record where the
base cell — the PU-matched regime — sits: `clears` or not, and by what margin in `rho`.

**STEP 4 — persist.** Write the whole result through
`cache.json_cache(f"curvature_feasibility_{stage1_key}", SWEEP_CFG, _compute)`: the full per-cell
table, the per-axis crossing points, the base-cell result, the timing, and the environment record
(`platform`, `numpy.__version__`, `scipy.__version__`, UTC timestamp) the way
`topoae_evaluate_run.py` does. Then call
`curvature_probe.write_curvature_verdict(key=stage1_key, stage=1, metrics={"spearman_rho": <base cell's rho>},
thresholds={"spearman_rho": <base cell's threshold>}, verdict=<from verdict_from_stage1_metrics>, extra=<the non-gating context>)`.
Do **not** call `write_curvature_handoff` anywhere in this file.

**STEP 5 — print the read-out.** A banner-delimited summary: the base cell's `rho`, its threshold,
its verdict; the per-axis crossing table; the two fixture variants' agreement or disagreement; and
the density-corrected-versus-uncorrected contrast at the base cell. End with the D-04 statement in
prose: if the base cell does not clear, the phase halts for a user decision with no auto-fallback,
and Ollivier-Ricci / intrinsic curvature is the live remaining option because it is defined on the
k-NN graph via optimal transport, never touches the ambient space, and so is not subject to the
sampling constraint that just failed. Print no "recommended k".
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; python -c "
import ast,sys
src=open('notebooks/diagnostics/curvature_feasibility_sweep_run.py').read()
ast.parse(src)
code='\n'.join(l for l in src.splitlines() if not l.strip().startswith('#'))
for name in ['NULL_QUANTILE','N_PERMUTATIONS','SPEARMAN_ABSOLUTE_FLOOR','K_GATING','K_DENSITY','D_PRIMARY','BASE_CELL','SWEEP_AXES','SWEEP_CFG','PREREG_SHA','WALL_CLOCK_BUDGET_S']:
    assert name in code, f'missing constant {name}'
for fn in ['measure_cell','verdict_from_stage1_metrics','write_curvature_verdict','config_key','merge-base']:
    assert fn in code, f'missing call {fn}'
assert 'write_curvature_handoff' not in code, 'stage 1 must never write a Phase 3 handoff'
assert 'timed_out' in code, 'per-cell timeout handling absent'
print('runner structure OK')
" &amp;&amp; python -m pyflakes notebooks/diagnostics/curvature_feasibility_sweep_run.py 2>/dev/null || python -c "import py_compile; py_compile.compile('notebooks/diagnostics/curvature_feasibility_sweep_run.py', doraise=True); print('compiles')"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/diagnostics/curvature_feasibility_sweep_run.py` parses and compiles
    - It names every pre-registered constant and calls `measure_cell`, `verdict_from_stage1_metrics`, `write_curvature_verdict`, `cache.config_key`, and `git merge-base`
    - `grep -c 'write_curvature_handoff' notebooks/diagnostics/curvature_feasibility_sweep_run.py` returns 0
    - Behavior: running the script with a deliberately mutated constant (temporarily change `K_GATING`) halts at STEP 0 with a message naming `K_GATING`, and does not compute any cell — demonstrate, then revert
    - Behavior: running the script with `PREREG_SHA` set to a non-ancestor commit halts at STEP 0 with an ordering message — demonstrate with a throwaway SHA, then revert
    - `git status --porcelain notebooks/diagnostics/topoae_evaluate_run.py notebooks/diagnostics/topoae_lambda_sweep_run.py notebooks/diagnostics/cae_evaluate_run.py notebooks/diagnostics/cae_train_run.py` produces no output
  </acceptance_criteria>
  <done>A runner exists that refuses to start unless the pre-registration precedes it and its own constants match that document, then sweeps one axis at a time from the base cell, scores each cell against its own permutation null, and writes a verdict but never a handoff.</done>
</task>

<task type="auto">
  <name>Task 2: Run the sweep and write the feasibility boundary report</name>
  <files>notebooks/diagnostics/curvature_feasibility_sweep_run.py, .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md</files>
  <read_first>
    - notebooks/diagnostics/curvature_feasibility_sweep_run.py (as written by Task 1)
    - .planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-PREREGISTRATION.md §9 and §10 (the sweep design and the wrong-side consequence)
    - .planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-FINDINGS.md (the FINDINGS.md document shape this phase's report should follow — a cross-phase argument with numbered sections, not a bare table)
  </read_first>
  <action>
Run `PYTHONPATH=notebooks python notebooks/diagnostics/curvature_feasibility_sweep_run.py`.
Record the total wall-clock time. If any cell timed out, record which and why, and state in the
report that the boundary is reported with that cell absent and the absence named — never silently
dropped.

Re-run the script a second time and confirm it is a pure cache hit that reproduces the same
`stage1_key` and the same artifact byte-for-byte, following the cache-hit verification 02.4-06
performed. Record both invocations in the SUMMARY.

Then write `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md`,
following `02.4-FINDINGS.md`'s document shape:

**§1 What was measured and under what rule.** Name the ratified pre-registration, its SHA, and the
ancestry proof. State the gating statistic and threshold rule in one paragraph.

**§2 The feasibility boundary.** A table per swept axis: axis, values, `rho` at each, that cell's
null threshold, whether it cleared, and the crossing point. Then the base-cell row called out
separately, because it is the PU-matched regime and the only cell whose result routes the phase.

**§3 Where the PU regime sits, and which axis is responsible.** D-04's actual deliverable. State
the base cell's `rho` and margin. If it does not clear, name the axis whose crossing point the PU
regime sits on the wrong side of, and by how much — is the problem `d`, `D`, `n`, or `k`? If more
than one axis binds, say so; if the regime clears, state which axis it is closest to failing on,
because that is what a later phase would need to widen.

**§4 The two fixture variants.** Whether the `n_bumps=1` (codimension 1) and `n_bumps=8`
(codimension 8) frontiers agree. This is the empirical answer to `02.5-RESEARCH.md` Open Question
2. If they disagree, that disagreement is the finding: the low-codimension fixture's boundary does
not transfer, and any read of the `D=768` regime from a codimension-1 fixture is unsupported. Say
which way it went, plainly.

**§5 The density correction (D-06).** The corrected-versus-uncorrected contrast at every cell, and
specifically on the deliberately skewed flat fixture where true `||H||` is exactly zero. State how
much apparent curvature the uncorrected estimator reported there — that number is the size of the
risk D-06 exists to close.

**§6 The two estimators (D-05).** The centroid-versus-quadric agreement across the `d` ladder, and
where the quadric fit became underdetermined (`d(d+1)/2 > k`). This is the empirical content of
D-00's reframing: the gating estimator should keep working past the point where the quadric fit
stops being determined. If it does not, say so — that would be evidence against D-00's correction
and it must not be buried.

**§7 Limitations.** Copy §12 of the pre-registration and add anything the run itself revealed.

**§8 Routing.** State the outcome and the consequence, in the language D-04 requires. On a clear:
stage 2 proceeds to plan 02.5-08. On a wrong-side result: the phase halts for a user decision, no
fallback is selected automatically, stage 2 is **moot rather than deferred**, and Ollivier-Ricci /
intrinsic curvature is named as the live remaining option with the reason it is not subject to the
sampling constraint that failed. State explicitly that no "recommended `k`" is emitted, and why.

Commit the runner and the findings document.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; ls notebooks/.cache | grep -c 'curvature_feasibility_' &amp;&amp; ls notebooks/.cache | grep -c 'curvature_verdict_stage1_' &amp;&amp; test "$(ls notebooks/.cache | grep -c 'curvature_handoff_')" = "0" &amp;&amp; echo "no handoff written by stage 1" &amp;&amp; python -c "
import re
s=open('.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-FINDINGS.md').read()
for sec in ['## 1','## 2','## 3','## 4','## 5','## 6','## 7','## 8']:
    assert re.search(r'^'+re.escape(sec), s, re.M), f'missing section {sec}'
assert 'Ollivier' in s, 'D-04 requires the live remaining option be named'
assert 'recommended' in s.lower(), 'the no-recommended-k statement is required'
print('findings structure OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/.cache/curvature_feasibility_{stage1_key}.json` and `curvature_verdict_stage1_{stage1_key}.json` exist
    - No `curvature_handoff_*` artifact exists after stage 1
    - The second invocation is a pure cache hit producing a byte-identical artifact — verified by comparing a hash of the file before and after
    - `02.5-FINDINGS.md` exists with sections 1 through 8, names `Ollivier`, and carries the no-recommended-k statement
    - §3 names the base cell's `rho`, its threshold, and which axis binds
    - §4 states whether the codimension-1 and codimension-8 frontiers agree
    - Total wall-clock time recorded in the SUMMARY; any timed-out cell named in §2 and §7
    - `git status --porcelain notebooks/pu_manifold/ src/effdim pyproject.toml` produces no output
  </acceptance_criteria>
  <done>The pre-registered sweep has run, the feasibility boundary is located per axis, the PU regime's position and the responsible axis are named, the two fixture variants and the two estimators are reconciled, and the stage-1 verdict artifact is written with no handoff.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: STAGE-1 GO / NO-GO — the gate that decides whether stage 2 runs at all</name>
  <what-built>
The stage-1 feasibility probe is complete. `notebooks/diagnostics/curvature_feasibility_sweep_run.py`
ran the pre-registered one-axis-at-a-time sweep from the PU-matched base cell (`d = 20`,
`D = 768`, `n = 10,000`, `k = 30`, `n_bumps = 8`, density-corrected), scoring each cell's Spearman
rank correlation between the estimated local mean-curvature field and the fixture's closed-form
analytic answer, against that cell's own permutation null. `02.5-FINDINGS.md` records the boundary,
where the PU regime sits relative to it, which axis is responsible, whether the codimension-1 and
codimension-8 fixtures agree, how much fake curvature the uncorrected estimator reported on a flat
skewed fixture, and where the quadric cross-check stopped being determined.
  </what-built>
  <how-to-verify>
1. Read `02.5-FINDINGS.md` §3 first. It answers the only question this checkpoint turns on: does
   the PU-matched base cell clear its null-calibrated threshold, and by what margin?
2. Read §2's per-axis table. Confirm the crossing points are where §3 says they are, and that no
   cell is missing without being named as `timed_out` in §7.
3. Read §4. If the codimension-1 and codimension-8 frontiers disagree, the base cell's result at
   `n_bumps = 8` is the one that counts and the disagreement is a limitation on how far any of
   this transfers to the real codimension-748 regime. Confirm §7 says so.
4. Read §5. The uncorrected estimator's reported `||H||` on the flat, skewed fixture is the size of
   the risk D-06 exists to close. If it is large and the correction removed it, the correction
   earned its place; if it is large and the correction did not, stage 2's numbers would be
   uninterpretable regardless of the base cell's Spearman.
5. Read §6. D-00's reframing predicts the gating centroid estimator keeps working past the `d`
   where the quadric fit becomes underdetermined. Confirm whether it did. If it did not, that is
   evidence against the correction the whole phase is built on and it should change the decision
   even if the base cell technically cleared.
6. Confirm no `curvature_handoff_*` artifact exists — stage 1 must write none.
7. Decide.

**GO** — the base cell clears, and §5 and §6 do not undercut it. Stage 2 proceeds: plan 02.5-08
builds the CAE chart-decoder curvature module, and plans 02.5-09 through 02.5-13 follow.

**NO-GO** — the base cell does not clear, or §5/§6 undercut a nominal clear. Per D-04 and the
ROADMAP, the phase **halts for a user decision with no auto-fallback**, and this negative result
is a complete and reportable outcome for the phase, not a failure of it. Concretely, on NO-GO:
  - Plans 02.5-08 through 02.5-12 are **cancelled, not deferred** — stage 2 is moot, because
    comparing two substrates on a statistic neither can recover answers nothing.
  - Plan 02.5-13 still executes, on its no-go branch: D-13's dated ROADMAP amendment retargets
    Phase 3's dead-pointer dependency to this phase's stage-1 negative finding and the
    Ollivier-Ricci fork rather than to a substrate verdict, and D-14's obligation does not fire
    because it is conditioned on a PASS. Before executing it, amend `02.5-13-PLAN.md`'s
    `depends_on` from `["02.5-12"]` to `["02.5-07"]` — an explicit, human-gated re-pointing
    recorded here rather than a silent dependency skip.
  - `02.5-FINDINGS.md` §8 already names Ollivier-Ricci / intrinsic curvature as the live remaining
    option, with the reason: it is defined on the k-NN graph via optimal transport, never touches
    the ambient space, and so is not subject to the sampling constraint that just failed. Confirm
    that section reads correctly before approving the halt.

**Either way:** no sealed verdict is reopened, softened, or recomputed. `GATE_VERDICT`,
`CAE_VERDICT`, and `TOPOAE_VERDICT` all remain FAIL exactly as measured. And no "recommended `k`"
is emitted — D-04 forbids it because it would read as post-hoc licence to change `k* = 15`.
  </how-to-verify>
  <resume-signal>Type "go" to proceed to plan 02.5-08, or "no-go" to halt the phase at the stage-1 negative result (and confirm the plan 02.5-13 re-pointing). If neither, describe what in the boundary report is unclear.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

New files (none exist before this phase; every symbol below is created by phase 02.5):

- `notebooks/pu_manifold/curvature_probe.py` — `CURVATURE_CONVENTION`, `swiss_roll_analytic_H`,
  `swiss_roll_analytic_H_scaled`, `make_swiss_roll_fixture`, `local_tangent_basis`,
  `centroid_mean_curvature`, `mean_curvature_norm`, `spearman_gate_statistic`,
  `median_relative_error`, `local_density_weights`, `gaussian_bump_values`,
  `graph_mean_curvature`, `make_graph_of_function_fixture`, `make_flat_fixture`,
  `quadric_fit_curvature`, `quadric_mean_curvature`, `estimator_agreement`, `permutation_null`,
  `measure_cell`, `STAGE1_GATING_METRICS`, `STAGE2_GATING_METRICS`, `GATE_DIRECTIONS`,
  `STAGE1_VERDICT_RULE`, `STAGE2_VERDICT_RULE`, `_apply_gates`, `verdict_from_stage1_metrics`,
  `verdict_from_stage2_metrics`, `write_curvature_verdict`, `write_curvature_handoff`,
  `clear_stale_curvature_handoff`
- `notebooks/pu_manifold/chart_curvature.py` — `assert_c2_activation`, `chart_decoder_map`,
  `chart_mean_curvature`, `chart_mean_curvature_norm`, `chart_curvature_field`
- `notebooks/pu_manifold/tests/test_curvature_probe.py` — all `test_*` functions therein
- `notebooks/diagnostics/curvature_feasibility_sweep_run.py` — `SWEEP_CFG`, `BASE_CELL`,
  `SWEEP_AXES`, `NULL_QUANTILE`, `N_PERMUTATIONS`, `SPEARMAN_ABSOLUTE_FLOOR`, `K_GATING`,
  `K_DENSITY`, `D_PRIMARY`, `LADDER`, `WALL_CLOCK_BUDGET_S`, `PREREG_SHA`, `main`
- `notebooks/diagnostics/cae_local_regate_run.py` — `FIT_KEY`, `SEALED_SEEDS`, `main`
- `notebooks/02.5_swiss_roll_curvature_probe_check.ipynb`,
  `notebooks/02.5_swiss_roll_chart_curvature_check.ipynb`
- `.planning/phases/02.5-.../02.5-PREREGISTRATION.md`,
  `.planning/phases/02.5-.../02.5-PREREGISTRATION-STAGE2.md`,
  `.planning/phases/02.5-.../02.5-FINDINGS.md`, `.planning/phases/02.5-.../COVERAGE.md`
- Cache artifacts (gitignored): `curvature_feasibility_{stage1_key}.json`,
  `curvature_verdict_stage1_{stage1_key}.json`, `curvature_verdict_stage2_{fit_key}.json`,
  `curvature_handoff_{fit_key}.json`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ratified pre-registration -> runner constants | The STEP 0 constant-match assertion is the boundary; a drifted constant means the runner measures a rule nobody ratified |
| pre-registration commit -> measurement commit | The STEP 0 `git merge-base --is-ancestor` check is the boundary, enforced in code rather than by convention |
| stage-1 result -> stage-2 execution | The blocking go/no-go checkpoint is the boundary; without it a wrong-side result would flow into a stage-2 comparison that answers nothing |
| stage-1 verdict -> Phase 3 handoff | Stage 1 must not write a handoff at all; the negative grep in Task 1's verify is the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.5-01 | Tampering | a non-finite Spearman from a degenerate cell reaching the verdict | high | mitigate | `permutation_null` raises on constant or non-finite input; `_apply_gates` raises naming the gate before comparing. A degenerate cell halts rather than producing a spurious FAIL. |
| T-02.5-02 | Tampering | a stale sweep artifact reused after a constant changed | high | mitigate | `SWEEP_CFG` carries every constant and is hashed by `cache.config_key` into the artifact stem, and `cache._manifest_matches` raises on any cfg mismatch. Task 2 verifies the second invocation is a byte-identical cache hit. |
| T-02.5-09 | Repudiation | measurement running before or without the ratified pre-registration | high | mitigate | STEP 0 halts unless `git merge-base --is-ancestor PREREG_SHA HEAD` exits 0, and unless every runner constant matches the document. Both demonstrated to halt by Task 1's acceptance criteria. |
| T-02.5-12 | Information Disclosure | a Phase 3 handoff written from a stage-1 result | high | mitigate | `grep -c write_curvature_handoff` on the runner returns 0; Task 2 asserts no `curvature_handoff_*` artifact exists after stage 1. |
| T-02.5-17 | Denial of Service | a cell exceeding the wall-clock budget silently truncating the sweep | medium | mitigate | Per-cell timeout records `status: timed_out` and continues; the cell is named in FINDINGS §2 and §7 rather than dropped. The cell count and time estimate print before the sweep starts. |
| T-02.5-18 | Elevation of Privilege | a wrong-side stage-1 result flowing into stage 2 anyway | high | mitigate | Task 3 is a blocking human-verify checkpoint that names the cancellation of plans 02.5-08..12 and the explicit re-pointing of plan 02.5-13 on NO-GO. `autonomous: false` on this plan. |
| T-02.5-05 | Tampering | sealed modules or existing runner scripts | high | mitigate | Additive-only; acceptance criteria assert `git status --porcelain` is empty for `notebooks/pu_manifold/`, the four existing diagnostics runners, `src/effdim`, and `pyproject.toml`. |
| T-02.5-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; the runner imports only already-pinned `numpy`, `scipy`, `scikit-learn` and the local package. |
</threat_model>

<verification>
- `PYTHONPATH=notebooks python notebooks/diagnostics/curvature_feasibility_sweep_run.py` completes and prints the base-cell verdict and the per-axis crossing table
- A second invocation is a pure cache hit with a byte-identical artifact
- `ls notebooks/.cache | grep -c 'curvature_handoff_'` returns 0
- `02.5-FINDINGS.md` sections 1-8 present; §3 names the base cell's rho, threshold, and the binding axis; §8 names Ollivier-Ricci and the no-recommended-k statement
- The Task 3 blocking go/no-go checkpoint is resolved with a recorded decision
- `git status --porcelain notebooks/pu_manifold/ src/effdim pyproject.toml` produces no output
</verification>

<success_criteria>
Stage 1 has run under its ratified rule with the ordering proved in code, emitted a feasibility
boundary rather than a bare verdict, named where the PU regime sits and which axis is responsible,
and a human has made the go/no-go decision that determines whether stage 2 runs at all.
</success_criteria>

<output>
Create `.planning/phases/02.5-local-curvature-feasibility-cae-re-gate/02.5-07-SUMMARY.md` when done
</output>
