---
phase: 03.1-decoder-metric-regularization-inserted
plan: 05
subsystem: research-diagnostics
tags: [pytorch, decoder-priors, curvature, chart-autoencoder, jsonl-diagnostics, requirements-closure]

# Dependency graph
requires:
  - phase: 03.1-04
    provides: "the ratified 15-cell ladder (3 baseline + 6 scale + 6 christoffel), scale's MECHANISM DEMONSTRATED verdict and christoffel's MECHANISM NOT DEMONSTRATED verdict, and the D-08 two-tier read-out machinery the combination cell and this plan's findings both reuse"
provides:
  - "The post-hoc best-of-each combination cell -- two nested decoder_prior_active blocks, scale outer / christoffel inner, zero new machinery (D-12), labelled POST-HOC on every table that carries it (3/3, audited from the rendered text itself)"
  - "03.1-FINDINGS.md -- the phase's full record: the faithfulness anchor, the compute picture, Tier 1/Tier 2 per mode with full three-seed spreads, the combination cell, the Swiss roll discharge, CURV-04's closure, all ten carried limitations, and the outcome (Phase 4 stays blocked, no route out proposed)"
  - "CURV-04 closed in REQUIREMENTS.md against shipped chart_curvature.py fields and the tests that pin them, with the standing limitation that Phase 3's grids were not re-run"
  - "ROADMAP.md's Phase 03.1 entry, detail section and Phase 4 dependency note updated to record the phase's actual (non-null, non-sufficient) outcome"
affects: [Phase-4-Region-Partitioning, any-future-geometry-teacher-spike]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Post-render audit of a post-hoc label: count derived from the rendered table text itself (grep for config_id and label co-occurrence), never from a flag set by hand at each print call site"
    - "Selection rule read into the combination cell's own JSONL fields (scale_weight, christoffel_weight, selection_rule) so the choice is reconstructable from the record alone, without re-reading the plan"

key-files:
  created:
    - .planning/phases/03.1-decoder-metric-regularization-inserted/03.1-FINDINGS.md
  modified:
    - notebooks/diagnostics/decoder_prior_ladder_run.py
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md

key-decisions:
  - "The metric was fixed and the curvature estimate was not: scale drives log10_det_g from -83.9 to +0.037 (metric fixed, negative reconstruction cost) while rank rho moves only -0.122 -> +0.116 -- necessary but not sufficient, and the reason Tier 1/Tier 2 must be scored separately (D-08) is this phase's own headline."
  - "christoffel's Tier-1 verdict is MECHANISM NOT DEMONSTRATED under this ladder's rung resolution (F1's relief cut removed the middle rung, hiding where cond(g) actually bottoms out), but the combination cell's cond(g) halving relative to scale alone (5.7e2 -> 2.8e2) is independent evidence christoffel contributed something the ladder alone could not see."
  - "The combination cell's rank-rho spread is 18x tighter than scale alone's, reported as an observation worth a follow-up and explicitly NOT as an established low-variance property -- three draws cannot separate a real low-variance estimator from three draws that landed close (D-03)."
  - "F4 (the D-07 epoch-cut override) did not fire on any of the 18 ladder/combination records -- verified directly against relief_applied and epochs_run/early_stopped on every record, stated in one line per D-07/D-11 rather than treated as a limitation slot."
  - "Phase 4 stays blocked with no proposed route out (D-11); the developer-directed local-polynomial geometry-teacher spike is recorded as a decision this phase's result motivates, not as a phase conclusion, and this document does not design it."

requirements-completed: [CURV-04]

# Metrics
duration: ~1.3h of measured background compute for the 3 combination cells (logged in /tmp/03_1_combo.log), plus well under an hour of read-out/findings/requirements work around it
completed: 2026-08-21
status: complete
---

# Phase 03.1 Plan 05: Combination Cell, Phase Findings, and CURV-04 Closure Summary

**The metric was fixed and the curvature estimate was not: `scale` drove its own target field from `log10_det_g = -83.9` to `+0.037` at negative reconstruction cost, while rank `rho` moved only `-0.122 → +0.116` -- necessary but not sufficient, closing CURV-04 and leaving Phase 4 blocked with no route out proposed.**

## Performance

- **Duration:** ~1.3h measured background compute for the three combination cells (`/tmp/03_1_combo.log`), plus the read-out/findings/closure work around it
- **Tasks:** 3/3 (post-hoc combination cell, `03.1-FINDINGS.md`, CURV-04 closure + roadmap update)
- **Files modified:** 4 (`notebooks/diagnostics/decoder_prior_ladder_run.py`, `03.1-FINDINGS.md` created, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`)

## Accomplishments

- **Task 1 (combination cell).** `select_best_rung` and `run_combination` implemented per D-12: two nested `with decoder_priors.decoder_prior_active(...)` blocks, `scale` outer / `christoffel` inner, zero extension to `decoder_priors.py`'s signature, no joined `PRIOR_MODES` entry. Ran 3 cells at the pre-declared best-of-each weights (`scale=1.177422e-01`, `christoffel=2.164570e-02`, the latter selected on Tier-1 evidence alone since christoffel's own Tier-2 was ruled unreadable by D-08). Every table carrying the combination cell prints `POST-HOC (weights selected after seeing the ladders; not an independent measurement)`, verified by the summary's own post-render audit: `POST-HOC LABEL RENDERED ON EVERY TABLE CONTAINING THE COMBINATION CELL (3/3)`. This task's code and run were committed and launched in the prior session (`34d72ff`, `eba072c`); this session verified completion and consumed its output.
- **Task 2 (`03.1-FINDINGS.md`).** Ten sections written in the order the plan specifies: what was measured and not; the faithfulness anchor (`rank_spearman_rho == -0.015106571347065712` exact); the compute picture (per-mode training multipliers up to 4.23x at the combination cell, A2 confirmed linear evaluation cost, F1/F2 relief applied, F4 confirmed not fired on any of the 18 records); Tier 1 per mode (`scale`: MECHANISM DEMONSTRATED, monotone both rungs; `christoffel`: MECHANISM NOT DEMONSTRATED, non-monotone `cond(g)`); Tier 2 per mode (median + full three-seed spread on all four axes, `NOT SEED-CONSISTENT` flagged where sign flips across seeds); the combination cell (weights, selection rule, `cond(g)` halving as independent evidence, variance reported as observation not finding); the Swiss roll discharge (`03.1_swiss_roll_decoder_prior_check.ipynb`'s four-line read-out); CURV-04's closure; all ten carried limitations; and the outcome section naming Phase 4's continued block explicitly with the developer-directed geometry-teacher spike recorded as a decision, not a conclusion.
- **Task 3 (CURV-04 closure + roadmap).** `.planning/REQUIREMENTS.md`'s status table entry replaced (Reopened -> Closed, naming the four shipped fields and three pinning tests), one interpretation-table row added recording the absolute-scale-primary / `cond(g)`-demoted assumption delta, and a closure paragraph added before the pre-existing "Outcome recorded honestly" line (which itself still correctly states CURV-07's unchanged negative answer). `.planning/ROADMAP.md`'s Phase 03.1 checklist entry, detail section (outcome paragraph + wave-5 checkbox) and Phase 4's dependency note all updated in bounded, scoped edits -- all three named pre-existing headings (`Phase 1`, `Phase 03.1`, `Phase 4`) still present, `git diff --stat` on both files shows single-digit line deltas.

## Task Commits

Each task was committed atomically:

1. **Task 1: The post-hoc combination cell** -- `34d72ff` (feat), launch recorded at `eba072c` (docs) -- committed and launched in the prior session; this session verified the completed record
2. **Task 2: `03.1-FINDINGS.md`** -- `f11fb73` (docs)
3. **Task 3: CURV-04 closure and roadmap update** -- `7fac631` (docs)

**Plan metadata:** this summary's own commit (docs: complete plan)

## Files Created/Modified

- `.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-FINDINGS.md` -- the phase's full record (created this session)
- `notebooks/diagnostics/decoder_prior_ladder_run.py` -- `select_best_rung`, `run_combination`, `--combination` flag, and the post-render POST-HOC audit line (code from the prior session, verified against its completed output this session)
- `.planning/REQUIREMENTS.md` -- CURV-04's status-table entry, one interpretation-table row, and a closure paragraph
- `.planning/ROADMAP.md` -- Phase 03.1's checklist line, outcome paragraph, wave-5 checkbox, and Phase 4's dependency note

## Decisions Made

- **The headline is read as "necessary but not sufficient," not as a null.** `scale` completely repairs the metric at negative reconstruction cost; Tier 2 (the estimand) moves only partially and not seed-consistently. Both statements are true simultaneously because D-08 scores them separately, and stating that separation clearly is the phase's main deliverable.
- **`christoffel`'s Tier-1 null is attributed to F1's rung cut, not to an absence of effect** -- the combination cell's independent `cond(g)` halving (`5.7e2 -> 2.8e2` relative to `scale` alone) is cited as evidence for that attribution, while still reporting christoffel's own ladder verdict as `MECHANISM NOT DEMONSTRATED` exactly as measured.
- **The combination cell's tight rank-rho spread is reported as an observation, explicitly not a finding** -- three seeds cannot certify a low-variance estimator (D-03), and this is flagged in the findings document itself rather than left for a reader to notice.
- **F4's non-firing is stated in one line rather than padded into a seventh limitation** -- verified directly against `relief_applied` and `epochs_run`/`early_stopped` on all 18 ladder + combination records before writing the claim.
- **CURV-04's closure text is additive beside the existing "Reopened" and "Amendment" paragraphs**, not a rewrite of them -- the amendment's own historical account (why it was reopened) is left intact, with the closure recorded as a new, dated paragraph immediately before the pre-existing "Outcome recorded honestly" line, whose count (eleven/twelve of thirteen) is corrected in place.

## Deviations from Plan

None -- plan executed exactly as written. Task 1's combination run had already been launched and its code committed in the prior session (per the input state); this session's work was to verify its completed output and proceed with Tasks 2 and 3 exactly as specified, including citing every quoted figure against a `config_id` present in `notebooks/.cache/03.1_decoder_prior_ladder.jsonl`.

## Issues Encountered

None. Every acceptance criterion in `03.1-05-PLAN.md` for Tasks 2 and 3 was checked directly against file content before this summary was written: the ten-section order, the `POST-HOC` label's presence, the exact anchor value, the ten-limitation list, the `config_id`-traceability of every figure, the absence of any `PREREGISTRATION`/`VERDICT.json` artifact under this phase's directory, the CURV-04 checkbox and status-table wording, and the three named ROADMAP headings surviving the edit.

## User Setup Required

None -- no external service configuration required.

## Next Phase Readiness

Phase 03.1 is sealed. Phase 4 (Region Partitioning & Regional Alignment / MKNN) **remains blocked** -- Phase 03.1 tested whether decoder-parameterization regularization would recover the curvature ordering Phase 4 needs, and at `d=20` it does not: rank `rho` reached at most `+0.116` (single-mode) or `+0.013` (combination), far short of the `rho = 0.989` this fixture yields at `d=4` and of any value Phase 4 could safely quantile-partition on. Per D-11, no route out is proposed here. The developer-directed next step -- a local-polynomial geometry-teacher spike `(P̂, ÎI)` scored on the same four axes at `d=20`, to test geometry-supervised feasibility before any architecture change, weighing the `r/R = 0.906` locality-breakdown constraint from `02.5-NOTE-high-d-curvature-approaches.md` §1 -- is recorded in `03.1-FINDINGS.md` §10 as a decision this phase's result motivates, not designed here and not itself a phase or requirement.

---
*Phase: 03.1-decoder-metric-regularization-inserted*
*Completed: 2026-08-21*

## Self-Check: PASSED

- FOUND: `.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-FINDINGS.md`
- FOUND: `.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-05-SUMMARY.md`
- FOUND commit `34d72ff` (Task 1 code)
- FOUND commit `eba072c` (Task 1 background-launch record)
- FOUND commit `f11fb73` (Task 2 `03.1-FINDINGS.md`)
- FOUND commit `7fac631` (Task 3 CURV-04 closure + roadmap update)
