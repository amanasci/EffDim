---
phase: 03.1-decoder-metric-regularization-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/pu_manifold/chart_curvature.py
  - notebooks/pu_manifold/tests/test_curvature_probe.py
  - notebooks/diagnostics/decoder_prior_ladder_run.py
autonomous: true
requirements: [CURV-04]

must_haves:
  truths:
    - "chart_curvature.chart_mean_curvature returns lambda_min, lambda_max, det_g and log10_det_g alongside the existing metric_condition_number (CURV-04, D-15)"
    - "chart_curvature.chart_curvature_field returns the same four fields as (n,) float64 tensors in original row order (CURV-04, D-15)"
    - "Adding the four fields leaves H_vec, H_norm and metric_condition_number unchanged on the reverse path (D-15's bit-identity requirement)"
    - "A uniform rescaling of the decoder leaves metric_condition_number unchanged while moving lambda_min, lambda_max and det_g — the invariant that makes cond(g) alone provably unable to detect uniform collapse"
    - "notebooks/diagnostics/decoder_prior_ladder_run.py runs one prior-active cell end to end at smoke scale: train under decoder_prior_active -> curvature field on the reduced evaluation set -> four fidelity axes -> Tier-1 absolute-scale fields -> one JSONL record -> printed read-out"
    - "--dry-run prints the pre-declared ladder sizing and the D-08 Tier-1/Tier-2 reading rules and writes nothing (D-10, D-16)"
    - "D-04's per-mode weight calibration derives absolute weights from declared fractions of the base reconstruction loss measured at initialization"
    - "notebooks/pu_manifold/cae.py is unmodified"
    - statement: "The four new fields carry the same meaning downstream in synthetic_control_run.py and curvature_field_pu_run.py, which read the same return dict without their own edits"
      verification: backstop
  artifacts:
    - notebooks/diagnostics/decoder_prior_ladder_run.py
    - notebooks/pu_manifold/chart_curvature.py
    - notebooks/pu_manifold/tests/test_curvature_probe.py
  key_links:
    - "chart_mean_curvature's per-chunk loop -> chart_curvature_field's aggregation loop -> the runner's per-cell Tier-1 distribution summaries -> the JSONL record"
    - "decoder_priors.decoder_prior_active(model, weight, mode) wrapping synthetic_control_run.train_blocked -> cae.train_cae's chart_loss call site"
    - "pu._split(n, pu.PU_SPLIT_SEED, pu.PU_HOLDOUT_FRACTION) holdout indices -> both the reconstruction guard and the curvature evaluation set, so eval rows and truth rows are the same rows in the same order"
---

<objective>
Land CURV-04's absolute-scale instrumentation in `chart_curvature.py`, and prove the whole
phase's measurement path end to end at smoke scale with a new ladder runner.

Purpose: Phase 3 measured `cond(g)` ranking two collapsed seeds (1.01e+03, 1.76e+03) *ahead of*
the only healthy one (3.30e+07). Every downstream number this phase reports depends on a
diagnostic that can see absolute metric scale, so the instrumentation must land before any cell
is trained, and the runner that consumes it must be proven to work before eight hours of CPU
are spent on it.

Output: four new fields in both `chart_curvature` return dicts, three new tests pinning them,
and a working `decoder_prior_ladder_run.py` whose `--smoke` path exercises train → curvature →
four axes → Tier-1 fields → JSONL → read-out with a prior active.

**Requirement note:** CURV-04 is Reopened as of 2026-08-18 (`.planning/REQUIREMENTS.md`, the
amendment at the end of the interpretation table). This plan carries its instrumentation.

**Compute note:** every task in this plan runs in seconds-to-minutes. Nothing here is
timing-sensitive.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-CONTEXT.md
@.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-PATTERNS.md
</context>

<pre_declared_constants_and_reading_rules>

**This entire section is declared before any number exists.** It is the plan-text half of D-10
("state the reading rule before anything runs") and D-16 ("the chosen evaluation sample size and
the projection that justified it are declared in the plan before any cell runs"). Task 3 of this
plan encodes it as module constants and prints it from `--dry-run`, in the same idiom as
`swiss_roll_isometry_prior_sweep_run.py`'s "the ladder is declared in source, before any number
exists" block.

### A. Architecture and fixture — inherited from `synthetic_control_run.py`, not restated

| constant | value | source |
|---|---|---|
| fixture | `saddle` only | D-01 |
| `n` | 10000 | `synthetic_control_run.CONTROL_N` |
| `chart_dim` | 20 | `pu.PU_CHART_DIM` |
| `ambient` | 768 | `pu.AMBIENT_DIM` |
| `embed_dim` | 40 | `pu.PU_EMBED_DIM` |
| `n_charts` | 4 | `pu.CONVERGE_N_CHARTS` |
| `domain_radius` | 2.0 | `synthetic_control_run.SADDLE_DOMAIN_RADIUS` |
| `LADDER_FIXTURE_SEED` | 20260816 | `synthetic_control_run.CONTROL_FIXTURE_SEED` |
| `LADDER_EPOCH_BLOCK` | 25 | `synthetic_control_run.CONTROL_EPOCH_BLOCK` |
| early stopping | **disabled for every cell** | inherited via `pu._converge_cfg`, which sets `early_stop_patience = max_epochs + 1` and `wallclock_ceiling_s = inf` |

**The fixture seed is held FIXED at 20260816 for every cell and only the model/training seed
varies.** `synthetic_control_run.run_one_control` uses one `seed` for both the fixture draw and
`torch.manual_seed`; varying it would confound "seed spread" with "fixture spread". Phase 3's
own three-seed PU spread varied model initialization only, against fixed data, and this plan
matches that. The mandatory anchor cell is the one exception: it calls `run_one_control`
unchanged with `seed=CONTROL_FIXTURE_SEED`, because it must reproduce the sealed row bit for bit.

**Early stopping disabled, decided here and not revisited.** `03-08` measured that removing
total-loss early stopping cut held-out `mse_per_dim` 62.2% and left `cond(g)` unmoved, so the
converged protocol is the better one; and the anchor cell requires it, so using it everywhere
removes a protocol difference between the anchor and the ladder rather than creating one. The
consequence is that `epochs_run == max_epochs` for every cell, which is directly checkable.

### B. Ladder sizing — pre-declared, probe-selected

| constant | value | rationale |
|---|---|---|
| `LADDER_SEEDS` | `(20260813, 20260814, 20260815)` | D-03; Phase 3's own three-seed reporting unit |
| `LADDER_MODES` | `("scale", "christoffel")` | D-02; `conformal` and `isometry` deferred |
| `LADDER_FRACTIONS` | `(0.01, 0.1, 1.0)` | D-04's declared fractions of base reconstruction loss; 3 rungs per mode |
| `LADDER_EVAL_N` | `2000` | D-16; the `pu._split` holdout rows, exactly `curvature_field_pu_run.py`'s own selection-grid evaluation set |
| `LADDER_EPOCH_CANDIDATES` | `(100, 75, 50, 40, 30, 25, 20)` | probe picks the largest that fits |
| `LADDER_BUDGET_S` | `28800` (8.0 h) | D-06's envelope, less the anchor cell |
| `ANCHOR_CONFIG_ID` | `"control_saddle_nc4_d20_ep300"` | D-13 |
| `ANCHOR_RHO_SPEARMAN` | `-0.015106571347065712` | D-13; matched with `==`, never a tolerance |

**Cell count:** 3 baseline (`weight=0`, shared across modes by the `cell_key` `"any"` sentinel)
+ 3 fractions × 3 seeds × 2 modes (18) + 3 combination = **24 cells**.

**The evaluation sample size, and why it is a permitted lever (D-16).** The sealed row
`control_saddle_nc4_d20_ep300` measured `curv_wallclock_s = 2993.912286339997` for ONE
full-cloud (`n=10000`) `chart_curvature_field` call at `chart_dim=20` / `ambient=768` /
`n_charts_used=4` — mode- and weight-independent, because curvature evaluation runs after
training. At 24 cells that is `24 × 2993.91 ≈ 71,854 s ≈ 19.96 h` of evaluation alone, over
D-06's 12 h upper bound before a single epoch is trained. D-16 rules that D-07's sample-count
fence covers the *training* sample count only, so ladder cells evaluate on the 2,000-row
`pu._split` holdout — precedent: `curvature_field_pu_run.py` evaluates every cell of its
nine-cell selection grid on `x_holdout64` (lines 730, 1372) and reserves full-cloud evaluation
(line 1627) for the single deliverable field. **Projection at `LADDER_EVAL_N = 2000`, assuming
evaluation cost is linear in row count: `2993.91 × 2000/10000 ≈ 598.8 s/cell`, `24 × 598.8 ≈
14,371 s ≈ 3.99 h`.** That linearity is research assumption A2 and is **unverified** — plan
`03.1-03`'s probe measures evaluation cost at both `n=10000` and `n=2000` directly and the
projection is recomputed from the measurement, never from this number.

**Consequence to carry into the write-up:** per-cell four-axis scores carry more sampling noise
than the sealed full-cloud rows, so ladder cells are not cell-for-cell comparable to Phase 3's
sealed `d=20` row on evaluation-set size — which D-05 already rules out as a comparison point
for a different reason. Every record carries `eval_sample_size` and `eval_row_source` so a
full-cloud re-evaluation of any cell is well defined.

**Training projection.** The same sealed row measured `train_wallclock_s = 6253.836718825012` at
`max_epochs=300` → `~20.85 s/epoch`, corroborated by the Phase 3 nine-cell PU grid
(`nc4_seed20260813`: `epochs_run=30`, `train_wallclock_s=646.6931414349965` → `~21.56 s/epoch`,
same architecture). The per-mode multiplier at `chart_dim=20` is **not known** — the halted
spike's `3.685 s → 10.699 s` (`~2.9×`) was measured at `chart_dim=2` and `torch.func.jacfwd`'s
cost scales with the number of tangent directions, so the real multiplier is very likely larger
(research assumption A1). It is measured by the probe, never extrapolated.

### C. Pre-declared relief ladder, applied in order if the probe overruns

Declared here so that a fallback is a stated cost on a sanctioned lever, not a redesign under
time pressure — which is what killed the predecessor (`03-NOTE-isometry-prior-spike.md` §3
recorded `BUDGET NOT MET` with zero cells trained).

1. **F1 — rungs 3 → 2**, keeping the extremes `(0.01, 1.0)` and dropping the middle. 24 → 18
   cells. Cost: each mode's curve has 3 points (`weight=0` plus 2 rungs) instead of 4; monotone
   movement is still readable, resolution is coarser.
2. **F2 — `LADDER_EVAL_N` 2000 → 1000.** D-16's own permitted lever. Cost: more sampling noise
   per cell; `eval_sample_size` recorded per cell so a re-evaluation is well defined.
3. **F3 — `CHRISTOFFEL_MAX_ROWS_PER_CHART` 8 → 4.** D-07's named valve. **`christoffel` arm
   only** — `isometry_penalty` (which `scale` reuses) has no row subsampling and loops over every
   batch row assigned to each chart, so this valve does nothing for the `scale` arm (research
   Pitfall 3). Cost: wider error bars on the `christoffel` arm specifically; recorded per cell as
   `christoffel_max_rows_per_chart`.
4. **F4 — `scale`-arm-only epoch reduction** to the next candidate down. Reached only if the
   `scale` arm alone overruns, where F3 cannot help; epoch count is Claude's discretion and
   sample count is fenced by D-07. Cost: a protocol asymmetry between arms, recorded per cell as
   `max_epochs` and reported beside the numbers; the two arms are then not comparable to each
   other on epoch count and the write-up says so.

If F1–F4 are exhausted and the projection still exceeds `LADDER_BUDGET_S`, the runner prints the
full projection table, prints `BUDGET NOT MET`, and exits 1. That halts to an explicit user
decision (D-11) rather than silently cutting scope.

### D. D-08 Tier 1 — mechanism. The exact wording of "moves toward healthy".

Read per mode over the rungs ordered by ascending weight, on the **3-seed median** of each
per-cell distribution statistic, with the **full 3-seed spread always printed beside it** (D-03).

- **`λ_min` moves toward healthy** = median `λ_min` is non-decreasing across ascending weight
  rungs **and** is strictly greater at the top rung than at `weight=0`.
- **`λ_max` moves toward healthy** = median `λ_max` does not fall toward the collapse band.
  A `λ_max` that *drops* while `cond(g)` improves is the collapse signature and is read as the
  prior making the metric worse, never better.
- **`det(g)` moves toward healthy** = median `log10_det_g` is non-decreasing across ascending
  rungs and strictly greater at the top rung than at `weight=0` — i.e. the geometric-mean metric
  eigenvalue moves toward 1, which is exactly the quantity `scale`'s penalty `(log det g / d)²`
  minimizes.
- **`cond(g)` moves toward healthy** = median `cond(g)` is non-increasing across ascending rungs
  and strictly lower at the top rung than at `weight=0`. **`cond(g)` alone is never sufficient.**

**Per-mode verdict, printed as one of exactly three strings:**
- `MECHANISM DEMONSTRATED` — the mode's own target field moved as above. `scale`'s target fields
  are `log10_det_g` and `λ_min`; `christoffel`'s target field is `cond(g)`, with `λ_max` not
  falling.
- `COLLAPSE` — median `cond(g)` improved while median `λ_max` fell by at least one decade from
  the `weight=0` cell. Scored as the prior making the metric worse.
- `MECHANISM NOT DEMONSTRATED` — neither of the above.

**Refusal rule.** If a mode's Tier 1 is `MECHANISM NOT DEMONSTRATED` or `COLLAPSE`, its Tier 2
table is still printed in full (D-10 forbids hiding numbers) but is labelled
`not read as evidence about the estimand` — a prior that did not move its own target quantity
was never actually tested.

### E. D-08 Tier 2 — estimand. The exact wording of "moves toward 1".

Four axes, scored separately, **never collapsed into one score, and no arithmetic anywhere
combines them.** Per axis, over the rungs ordered by ascending weight, on the 3-seed median with
the full spread printed. The `weight=0` reference values on record for this fixture at the sealed
300-epoch protocol are cosine `-0.000478`, magnitude ratio `9955`, `R²` `0.000002`, `rho`
`-0.0151` — quoted as context only; the actual comparison is against **this phase's own
re-fitted `weight=0` cells at the same epoch budget** (D-05).

- **direction** — `direction_median_cosine` moves toward **+1**: `|cosine − 1|` strictly smaller
  at the mode's best rung than at `weight=0`.
- **magnitude** — `magnitude_median_ratio` moves toward **1**: `|log10(ratio)|` strictly smaller
  at the best rung than at `weight=0`. Log scale because the baseline sits four decades off; a
  linear distance would be dominated by that one number.
- **calibration** — `calibration_r2` moves toward **1**: strictly larger than at `weight=0`,
  **and** `calibration_slope` moves toward 1. Both reported; neither alone.
- **rank** — `rank_spearman_rho` moves toward **+1**: strictly larger than at `weight=0`.

**No bar, no verdict artifact (D-10).** These are reading rules for a diagnostics table, not a
gate. A null — every axis unmoved within the 3-seed spread — is a complete answer and is
reported as plainly as a success (D-11).

**Seed consistency (D-03).** Three draws cannot separate a small real effect from seed noise.
Any Tier-1 or Tier-2 movement whose direction is not consistent across all three seeds is
labelled `NOT SEED-CONSISTENT` and is not claimed as an effect.

**Bias guard (D-09).** Held-out `reconstruction.mse_per_dim` is printed beside every cell.
Stated rule: **an improvement bought with materially worse reconstruction is not an improvement.**
No numeric threshold — the last reconstruction threshold proposed in this milestone was deferred
as circular and that objection is unchanged. The comparison is against this phase's own
`weight=0` cells at the same three seeds, reported as a ratio, and the reader makes the call.
The bias guard is a **reading rule, never a selector**: no rule below selects on reconstruction.

### F. Combination-arm weight selection (D-02), declared before the ladder runs

The combination cell is **best-of-each**, and "best" is fixed here so it cannot drift after the
numbers land:

- `best_scale` = the rung whose median `log10_det_g` is closest to `0.0` — the quantity `scale`'s
  own penalty minimizes.
- `best_christoffel` = the rung with the lowest median `cond(g)` among rungs whose median
  `λ_max` did not fall by at least one decade from the `weight=0` cell (the same `COLLAPSE` guard
  as Tier 1).
- If a mode's Tier 1 is `MECHANISM NOT DEMONSTRATED` at every rung, its best is the largest
  weight rung and the combination cell carries the extra label
  `no mechanism in the {mode} arm`.

**Every table carrying the combination cell labels it**
`POST-HOC (weights selected after seeing the ladders; not an independent measurement)` — D-02
requires this and it is not a footnote.

### G. Baseline-pathology precondition, declared before the ladder runs

A prior can only be measured against a pathology that is present. After the three `weight=0`
cells complete at the chosen epoch budget, the runner prints `BASELINE PATHOLOGY CHECK` with
median `cond(g)`, `λ_min`, `λ_max` and `log10_det_g` per seed, against the sealed reference
bands in `03-FINDINGS.md` §5: healthy-metric seed 20260813 at `λ_max = 3.352`,
`cond = 3.30e+07`; collapsed seeds at `λ_max ≈ 5.4e-07`, `det(g) ≈ 1e-162`; and the healthy
`d=4` saddle control at `cond = 4.098`. **If all three baseline seeds land in the `d=4`-healthy
band — median `cond(g)` within one decade of 4.098 and median `λ_max` within one decade of 1 —
the runner prints `NO PATHOLOGY AT THIS EPOCH BUDGET` and exits 2 without training a single
prior-active cell.** That is itself a finding (the pathology is a long-training artifact) and
escalation is an explicit user decision per D-11.

### H. Non-finite guard (research assumption A3)

Continuous training was measured to diverge past ~epoch 220 on the flat fixture; the saddle's own
divergence point was never measured. After each cell's training the runner asserts every model
parameter is finite and that the cell's `H_est` and `cond` arrays are finite. On failure the
record is written with `nonfinite_guard.tripped = true` and the cell prints as
`DIVERGED — not read as evidence`, **never** as "the prior made it worse".

### I. Presentation and record locations (Claude's discretion, fixed here)

- **No presentation notebook accompanies the runner.** The runner's `--summary` prints the
  tables; CLAUDE.md's keep-it-simple rule and the fact that notebooks in this repo are reserved
  for Swiss roll checks and presentation both point the same way. D-14's Swiss roll notebook is
  the only notebook this phase adds.
- **Findings land at**
  `.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-FINDINGS.md`, matching
  Phase 3's `03-FINDINGS.md` precedent.
- **The JSONL record is** `notebooks/.cache/03.1_decoder_prior_ladder.jsonl` (gitignored, via
  `pu_manifold.cache.cache_path`).

</pre_declared_constants_and_reading_rules>

<assumption_delta_decision>

**Checkpoint fired on:** D-15 adds `λ_min` / `λ_max` / `det(g)` **alongside** `cond(g)`.

**Question:** does the current primary still name the right noun?

**The noun that is now the primary metric-health diagnostic:** **the metric's absolute spectrum
— `λ_min`, `λ_max`, `det(g)` (read as `log10_det_g`).** Not the ratio.

**Decision: `promote`.** The absolute-scale fields become the primary metric-health diagnostic
and `cond(g)` is demoted to one reported detail among four.

**Rationale (one line):** `cond(g)` is scale-invariant and was measured ranking the two
uniformly-collapsed seeds (1.01e+03, 1.76e+03) *ahead of* the only fit whose metric had a real
absolute scale (3.30e+07) — a diagnostic that inverts the ordering on the exact failure it is
being used to detect cannot remain primary.

**Enacted in this plan by:**
- Tier 1's wording (§D above) makes `cond(g)` explicitly insufficient on its own and introduces
  the `COLLAPSE` verdict, in which a `cond(g)` improvement is scored as the metric getting worse.
- Task 2 adds `test_cond_is_blind_to_uniform_metric_collapse_but_det_g_is_not`, the invariant
  test encoding the generalized intent: under a uniform rescaling of the decoder,
  `metric_condition_number` is unchanged while `lambda_min`, `lambda_max` and `det_g` all move.
- `cond(g)` is **not removed** — CURV-04's original flagging clause (within-config percentile,
  flagged points excluded from the reported `‖H‖` summary) still depends on it, and Phase 3's
  sealed records store only it. It is retained and reported, no longer relied on alone.

**Accepted debt, named:** Phase 3's recorded grids are not re-run with the new fields (D-15
states this explicitly), so no existing record can be re-audited for collapse. That limitation
is carried into `03.1-FINDINGS.md`, not bought by this phase.

</assumption_delta_decision>

<artifacts_this_phase_produces>

Newly created by this plan — excluded from drift verification because they do not yet exist:

**New file:** `notebooks/diagnostics/decoder_prior_ladder_run.py`

**New dict keys in `chart_curvature.chart_mean_curvature`'s return:** `"lambda_min"`,
`"lambda_max"`, `"det_g"`, `"log10_det_g"`

**New dict keys in `chart_curvature.chart_curvature_field`'s return:** `"lambda_min"`,
`"lambda_max"`, `"det_g"`, `"log10_det_g"`

**New module-level symbols in `decoder_prior_ladder_run.py`:** `LADDER_MODES`, `LADDER_SEEDS`,
`LADDER_FIXTURE_SEED`, `LADDER_FRACTIONS`, `LADDER_EVAL_N`, `LADDER_EPOCH_CANDIDATES`,
`LADDER_BUDGET_S`, `LADDER_EPOCH_BLOCK`, `ANCHOR_CONFIG_ID`, `ANCHOR_RHO_SPEARMAN`,
`RECORD_STEM`, `SMOKE_N`, `SMOKE_D`, `SMOKE_AMBIENT`, `SMOKE_N_CHARTS`, `SMOKE_EPOCHS`,
`READING_RULES_TEXT`, `cell_key`, `load_completed`, `append_record`, `calibrate_weights`,
`run_cell`, `print_cell_row`, `print_dry_run_report`, `run_smoke`, `build_arg_parser`, `main`

**New CLI flags on `decoder_prior_ladder_run.py`:** `--dry-run`, `--smoke`, `--record-path`,
`--device`, `--eval-n`, `--max-epochs`, `--mode`, `--seeds`

**New JSONL record fields** (beyond `synthetic_control_run.py`'s `rec` shape, which is copied):
`mode`, `weight`, `weight_fraction`, `base_recon_at_init`, `base_penalty_at_init`, `model_seed`,
`eval_sample_size`, `eval_row_source`, `christoffel_max_rows_per_chart`, `lambda_min` (dist
summary), `lambda_max` (dist summary), `det_g` (dist summary), `log10_det_g` (dist summary),
`nonfinite_guard`, `arm_label`

**New test function names in `notebooks/pu_manifold/tests/test_curvature_probe.py`:**
`test_chart_mean_curvature_reports_lambda_min_max_and_det_g`,
`test_chart_curvature_field_reports_lambda_min_max_and_det_g`,
`test_cond_is_blind_to_uniform_metric_collapse_but_det_g_is_not`

**New record file:** `notebooks/.cache/03.1_decoder_prior_ladder.jsonl` (gitignored)

</artifacts_this_phase_produces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "one prior-active cell measured and recorded" — one path only</name>

  <read_first>
    - notebooks/pu_manifold/chart_curvature.py (lines 400-560: `chart_mean_curvature`'s per-chunk loop and return dict, `chart_curvature_field`'s aggregation loop and return dict — this is the file being modified)
    - notebooks/diagnostics/synthetic_control_run.py (read in full: `CONTROL_N`, `CONTROL_FIXTURE_SEED`, `SADDLE_DOMAIN_RADIUS`, `CONTROL_EPOCH_BLOCK`, `SMOKE_*`, `build_fixture`, `build_matched_cae`, `train_blocked`, `_fidelity_axes`, `run_one_control`, `load_completed`, `append_record`, `print_control_row`, `build_arg_parser`)
    - notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py (lines 1-130 for the declared-in-source ladder block, `_to_jsonable` and `cell_key`; lines 200-230 for the prior-wrapped training idiom)
    - notebooks/pu_manifold/decoder_priors.py (module docstring, `PRIOR_MODES` docstring, `CHRISTOFFEL_MAX_ROWS_PER_CHART`, `christoffel_penalty`, `isometry_penalty`, `decoder_prior_active`)
    - notebooks/diagnostics/curvature_field_pu_run.py (targeted: `_split` around line 471, `_converge_cfg` around line 542, `_dist_summary` around line 1533, `resolve_device`, and the constants `PU_CHART_DIM`, `PU_EMBED_DIM`, `PU_HIDDEN_WIDTH`, `PU_ACTIVATION`, `PU_SPLIT_SEED`, `PU_HOLDOUT_FRACTION`, `AMBIENT_DIM`, `CONVERGE_N_CHARTS`, `COND_FLAG_PERCENTILE`, `FIELD_HIST_BINS`)
  </read_first>

  <files>notebooks/pu_manifold/chart_curvature.py, notebooks/diagnostics/decoder_prior_ladder_run.py</files>

  <action>
Wire ONE prior-active cell through every layer this phase touches, at smoke scale, end to end.
No ladder loop, no probe, no anchor, no summary — those are later tasks.

**Layer 1 — `notebooks/pu_manifold/chart_curvature.py` (D-15, CURV-04).**

In `chart_mean_curvature`'s per-chunk loop, immediately after `g` is formed by
`torch.einsum("boi,boj->bij", J, J)`, compute `eigs = torch.linalg.eigvalsh(g)` (ascending, `g`
is symmetric PSD) and accumulate four new per-chunk lists alongside the existing `cond_parts`:
`lambda_min_parts` from `eigs[..., 0]`, `lambda_max_parts` from `eigs[..., -1]`, `det_g_parts`
from `torch.linalg.det(g)`, and `log10_det_g_parts` from `eigs.log10().sum(dim=-1)`. Slice each
with `[:n_real]` and `.detach()` exactly as `cond_parts` already does. Concatenate them the same
way `metric_cond` is concatenated, and add `"lambda_min"`, `"lambda_max"`, `"det_g"` and
`"log10_det_g"` to the return dict beside the existing `"metric_condition_number"`.

**Do not replace the existing `torch.linalg.cond(g)` call with an eigenvalue-derived ratio.**
D-15 requires the reverse path to be shown still bit-identical, and an `eigvalsh`-derived cond
differs from `torch.linalg.cond` at ~1e-13 relative. The extra decomposition is a 20x20 solve per
32-row chunk, negligible against the Hessian computation that dominates the ~2994 s/cell cost.
Keep the existing key's value byte-for-byte unchanged.

`log10_det_g` exists because `det(g)` was measured at `2.24e-166` on a collapsed seed; the
summed log is underflow-proof and is the exact quantity `scale`'s penalty `(log det g / d)²`
minimizes, so it is the field Tier 1 reads.

In `chart_curvature_field`'s aggregation loop, pre-allocate four `(n,)` float64 tensors on
`x.device` alongside the existing `H_norm` and `metric_cond`, populate them per chart-chunk with
the same `[target] = out[...]` idiom already used for `metric_condition_number`, and add all four
to that function's return dict. Update both functions' docstring return-shape lines to name the
new keys. Do not touch `chart_mean_curvature_norm` or the codimension rationale it documents.

**Layer 2 — `notebooks/diagnostics/decoder_prior_ladder_run.py` (new file, D-13).**

Copy `synthetic_control_run.py`'s import block shape verbatim, adding `sys.path` inserts for both
`parents[1]` and `parent`, then `import curvature_field_pu_run as pu` and
`import synthetic_control_run as sc`, and `from pu_manifold import cae, cache, chart_curvature,
decoder_curvature, decoder_priors`. **Import the fixture, matched-architecture builder, blocked
trainer and fidelity axes from `sc` — never re-type them.** `synthetic_control_run.py` and
`swiss_roll_isometry_prior_sweep_run.py` are not edited by this phase.

Declare the module constants listed in this plan's §A/§B table as module-level names with
docstrings, in the same declared-in-source idiom as the sweep runner's ladder block. Set
`RECORD_STEM = "03.1_decoder_prior_ladder"` and resolve the record path through
`cache.cache_path(RECORD_STEM, "jsonl")`.

Copy `_to_jsonable` / `append_record` / `load_completed` from the sweep runner, and `cell_key`
verbatim including its `"any"` sentinel at `weight == 0.0` — `decoder_prior_active` installs
nothing at weight 0, so a `weight=0.0` cell recorded under one mode is valid evidence for the
other, and neither arm re-runs its own baseline.

Write `run_cell(mode, weight, weight_fraction, model_seed, max_epochs, n, chart_dim, ambient,
n_charts, embed_dim, eval_n, epoch_block, device, christoffel_max_rows)`:
  1. `fx = sc.build_fixture("saddle", n=n, d=chart_dim, D=ambient, seed=LADDER_FIXTURE_SEED)` —
     fixture seed FIXED, never `model_seed`.
  2. `X`, `H_true` as float64 numpy, `x32`/`x64` tensors on device, exactly as `run_one_control`.
  3. `train_idx, holdout_idx = pu._split(X.shape[0], pu.PU_SPLIT_SEED, pu.PU_HOLDOUT_FRACTION)`.
     `x_train32` from `train_idx`. Build the evaluation set as `x_eval64 = x64[eval_idx]` where
     `eval_idx = holdout_idx[:eval_n]`, and **`H_true_eval = H_true[eval_idx]` taken with the
     same index array in the same order.** A mis-aligned truth vector destroys all four axes
     silently, so index both from one variable.
  4. `torch.manual_seed(model_seed)`, then `sc.build_matched_cae(n_charts, chart_dim, embed_dim,
     ambient, device)` — constructed then moved to device, never `device=` in the constructor.
  5. Train inside the prior scope: `with decoder_priors.decoder_prior_active(model, weight,
     mode): fit = sc.train_blocked(model, x_train32, model_seed, n_charts, max_epochs,
     epoch_block)`. Time it with `time.monotonic()`. At `weight == 0.0` this is structurally the
     untouched `cae.train_cae` code path (D-05).
  6. `model.eval().double()`, then `decoder_curvature.assert_c2_decoder(model)`.
  7. Non-finite guard (§H): check every `model.parameters()` tensor with `torch.isfinite(...).
     all()`. Record the result in a `nonfinite_guard` dict with keys `tripped`, `where`.
  8. Held-out reconstruction on the full `holdout_idx` rows via `model.reconstruct` under
     `torch.no_grad()` and `cae.reconstruction_stats` (D-09's bias guard) — this stays on the
     full 2,000-row holdout regardless of `eval_n`, so the guard is not itself subsampled.
  9. `field = chart_curvature.chart_curvature_field(model, x_eval64, mode="reverse")`, timed
     separately into `curv_wallclock_s`.
 10. Pull `H_vec`, `H_norm`, `metric_condition_number`, `lambda_min`, `lambda_max`, `det_g`,
     `log10_det_g`, `chart_assignment` off the field dict as float64 numpy.
 11. `axes = sc._fidelity_axes(H_est, H_true_eval)` — imported, not re-typed.
 12. Build the record as `synthetic_control_run.run_one_control`'s `rec` dict shape (same
     `config_id`, `fixture`, `n`, `chart_dim`, `ambient_dim`, `embed_dim`, `n_charts`,
     `max_epochs`, `fixture_seed`, `epochs_run`, `early_stopped`, `epoch_block`, `n_blocks`,
     `c2_activation`, `fidelity`, `h_norm_est`, `h_norm_true`, `cond`, `flagged`,
     `n_charts_used`, `per_chart_counts`, `reconstruction`, `train_wallclock_s`,
     `curv_wallclock_s`, `device`, `torch_version` — all via `pu._dist_summary(...,
     pu.FIELD_HIST_BINS)` and `pu.COND_FLAG_PERCENTILE` as that file already does), plus this
     phase's new fields: `kind = "ladder_cell"`, `mode`, `weight`, `weight_fraction`,
     `base_recon_at_init`, `base_penalty_at_init`, `model_seed`, `eval_sample_size`,
     `eval_row_source = "pu._split holdout, first eval_n rows"`,
     `christoffel_max_rows_per_chart`, `lambda_min`/`lambda_max`/`det_g`/`log10_det_g` each as a
     `pu._dist_summary`, `nonfinite_guard`, and `arm_label`.
     `config_id` is `f"ladder_{mode}_w{weight:.6e}_s{model_seed}_e{max_epochs}_n{eval_n}"`, and
     at `weight == 0.0` the mode segment is the literal `any` so the baseline cell has one
     canonical id across both arms.

Write `print_cell_row(rec)` modelled on `sc.print_control_row`: the four fidelity axes as four
separate lines with no arithmetic combining them, then a Tier-1 line printing `lambda_min`,
`lambda_max`, `log10_det_g` and `cond` medians, then held-out `mse_per_dim`, charts used, and
the two wall-clock numbers. If `nonfinite_guard["tripped"]` is true, print
`DIVERGED — not read as evidence` as the first line of the row.

Add `--dry-run` (prints the constants and exits writing nothing — its full content is Task 3's
job; a stub that prints the §A/§B constants is enough here), `--smoke`, `--record-path`,
`--device` and `main()` with `pu.resolve_device`. `run_smoke` runs exactly two cells at
`SMOKE_N=400`, `SMOKE_D=4`, `SMOKE_AMBIENT=24`, `SMOKE_N_CHARTS=2`, `SMOKE_EPOCHS=2`,
`eval_n=80`: one at `weight=0.0, mode="scale"` and one at `weight=1e-3, mode="christoffel"`,
appending both to the record with `kind = "smoke_ladder_cell"` and printing both rows.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_curvature_probe.py -q -x -k "forward_mode_matches_reverse or reassembles_in_row_order or golden or analytic" && .venv/bin/python notebooks/diagnostics/decoder_prior_ladder_run.py --smoke --record-path /tmp/03_1_smoke.jsonl && .venv/bin/python -c "import json;rs=[json.loads(l) for l in open('/tmp/03_1_smoke.jsonl')];assert len(rs)==2;[print(r['config_id'],r['mode'],r['weight'],r['eval_sample_size'],r['lambda_min']['median'],r['lambda_max']['median'],r['det_g']['median'],r['log10_det_g']['median'],r['fidelity']['rank_spearman_rho'],r['reconstruction']['mse_per_dim']) for r in rs]" && git diff --quiet notebooks/pu_manifold/cae.py && echo TRACER_OK</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -c "from pu_manifold import chart_curvature as cc, cae; import torch; torch.manual_seed(3); m=cae.ChartAutoEncoder(in_dim=12,embed_dim=6,chart_dim=2,n_charts=3,hidden=[8],activation='silu').double(); o=cc.chart_mean_curvature(m, torch.rand(6,2,dtype=torch.float64), 1, mode='reverse'); print(sorted(o.keys()))"` output contains `lambda_min`, `lambda_max`, `det_g`, `log10_det_g` and still contains `metric_condition_number`
    - The same four keys are present in `chart_curvature_field`'s return dict, each a `(n,)` `torch.float64` tensor
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_curvature_probe.py -q` exits 0 with no test failures — in particular `test_chart_curvature_forward_mode_matches_reverse_to_float64_round_off` and `test_chart_curvature_field_reassembles_in_row_order` still pass, which is D-15's bit-identity proof that `H_vec`, `H_norm` and `metric_condition_number` did not move
    - `grep -n "torch.linalg.cond(g)" notebooks/pu_manifold/chart_curvature.py` still matches exactly one line — the existing cond call is retained, not replaced
    - `.venv/bin/python notebooks/diagnostics/decoder_prior_ladder_run.py --smoke --record-path /tmp/03_1_smoke.jsonl` exits 0 and appends exactly 2 JSON lines
    - Each smoke record carries non-null `mode`, `weight`, `weight_fraction`, `model_seed`, `eval_sample_size`, `eval_row_source`, `christoffel_max_rows_per_chart`, `nonfinite_guard`, and `lambda_min`/`lambda_max`/`det_g`/`log10_det_g` distribution summaries each with a `median` key
    - The record whose `weight` is `0.0` has a `config_id` whose mode segment is the literal `any`
    - `grep -cE "^def (build_fixture|build_matched_cae|train_blocked|_fidelity_axes)" notebooks/diagnostics/decoder_prior_ladder_run.py` returns 0 — those four are imported from `synthetic_control_run`, never re-typed (anchored to column 0 so a mention in a comment or docstring cannot trip it)
    - `grep -cE "^import synthetic_control_run as sc|^import curvature_field_pu_run as pu" notebooks/diagnostics/decoder_prior_ladder_run.py` returns 2 — both modules are imported rather than re-derived
    - `git diff --quiet notebooks/diagnostics/synthetic_control_run.py notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py notebooks/pu_manifold/cae.py` exits 0
  </acceptance_criteria>

  <reversibility rating="costly">The four new fields become the phase's primary Tier-1 diagnostic and three existing runners inherit them from this one return dict; reverting after the ladder has run would invalidate every Tier-1 column already recorded.</reversibility>

  <done>One prior-active cell runs end to end at smoke scale — trained under `decoder_prior_active`, curvature evaluated on the reduced set, four fidelity axes scored, the four CURV-04 absolute-scale fields recorded, one JSONL line appended, and the read-out printed — and the existing `chart_curvature` regression tests still pass unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the four new fields, and pin that cond(g) alone cannot see collapse</name>

  <read_first>
    - notebooks/pu_manifold/tests/test_curvature_probe.py (lines 1340-1360 for `_small_cae`; lines 1520-1660 for the golden-vector pin and `test_chart_curvature_forward_mode_matches_reverse_to_float64_round_off`, whose shape these tests mirror — this is the file being modified)
    - notebooks/pu_manifold/chart_curvature.py (the two return dicts as edited by Task 1)
    - notebooks/pu_manifold/cae.py (`EmbeddingDecoder` around line 125 and `mlp_stack` — the decoder's output layer is a plain `nn.Linear` with `out_activation=None`, which is what makes a uniform output rescaling expressible as scaling one weight and one bias)
  </read_first>

  <files>notebooks/pu_manifold/tests/test_curvature_probe.py</files>

  <behavior>
    - `test_chart_mean_curvature_reports_lambda_min_max_and_det_g`: on `_small_cae("silu", seed=3)`, `chart_idx=1`, `z_chart = torch.rand(6, model.chart_dim, dtype=torch.float64)`, the returned `lambda_min`/`lambda_max`/`det_g`/`log10_det_g` agree with `torch.linalg.eigvalsh(g)[..., 0]`, `[..., -1]`, `torch.linalg.det(g)` and `eigvalsh(g).log10().sum(-1)` computed independently in the test from `J = torch.func.jacrev`-derived `g`, to `rtol=1e-9, atol=1e-12`; and `lambda_max / lambda_min` agrees with the returned `metric_condition_number` to the same tolerance
    - `test_chart_curvature_field_reports_lambda_min_max_and_det_g`: the field-level call returns all four as `(n,)` float64 tensors, and each row's value equals the per-chart `chart_mean_curvature` value for that row's assigned chart — reassembled in original row order, mirroring `test_chart_curvature_field_reassembles_in_row_order`'s structure
    - `test_cond_is_blind_to_uniform_metric_collapse_but_det_g_is_not`: scaling the final `nn.Linear` of `model.embedding_decoder.net` (weight and bias) by `c = 1e-3` sends `J -> cJ` and `g -> c**2 g`; `metric_condition_number` is unchanged to `rtol=1e-9, atol=1e-12`, while `lambda_min` and `lambda_max` each scale by `c**2` and `det_g` scales by `c**(2 * model.chart_dim)`, to the same tolerance
  </behavior>

  <action>
Add three tests to `notebooks/pu_manifold/tests/test_curvature_probe.py`, beside the existing
`chart_curvature` block, using the existing `_small_cae` helper (`in_dim=12`, `embed_dim=6`,
`chart_dim=2`, `n_charts=3`, `hidden=[8]`, doubled) — do not redefine it and do not add a new
fixture helper.

Name them `test_chart_mean_curvature_reports_lambda_min_max_and_det_g`,
`test_chart_curvature_field_reports_lambda_min_max_and_det_g`, and
`test_cond_is_blind_to_uniform_metric_collapse_but_det_g_is_not`. Follow the file's own
convention of importing `torch` and `from pu_manifold import chart_curvature as cc` inside each
test body.

For the first test, recompute `g` independently inside the test rather than trusting the module:
build `decode_one = cc.chart_decoder_map(model, chart_idx)`, take
`J = torch.func.vmap(torch.func.jacrev(decode_one))(z_chart)`, form `g` by
`torch.einsum("boi,boj->bij", J, J)`, then compare against the module's four returned fields with
`torch.testing.assert_close(..., rtol=1e-9, atol=1e-12)`. Research verified this substitution
agrees with `torch.linalg.cond` and `torch.linalg.det` at ~9.4e-14 and ~4e-14 relative on a
realistically-shaped PSD matrix, so 1e-9 is a generous but honest tolerance.

For the third test — the invariant encoding the promotion recorded in this plan's
`<assumption_delta_decision>` — capture the baseline four fields first, then locate the final
`nn.Linear` in `model.embedding_decoder.net` by taking the last module of the stack that has a
`weight` attribute, and under `torch.no_grad()` multiply its `weight` and `bias` in place by
`c = 1e-3`. Because `EmbeddingDecoder` is built with `out_activation=None`, that scales the
decoder's output exactly, hence `J -> cJ` and `g -> c**2 g`. At `chart_dim = 2` the determinant
ratio is `c**4 = 1e-12`, four orders inside float64's normal range, so the exact ratio assertion
is safe at this scale. Assert `metric_condition_number` unchanged, `lambda_min` and `lambda_max`
scaled by `c**2`, and `det_g` scaled by `c**(2 * model.chart_dim)`.

Give the third test a docstring stating what it encodes: `cond(g)` is exactly invariant under a
uniform rescaling, so it cannot detect a uniformly collapsed metric — the failure Phase 3
measured on seeds 20260814 and 20260815, where the entire spectrum sat at ~1e-07 and `cond(g)`
nevertheless read better than the one healthy fit. Reference `CURV-04` and `03-FINDINGS.md` §5 in
that docstring.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_curvature_probe.py -q -x -k "lambda or det_g or collapse" && .venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_curvature_probe.py -q -k "lambda or det_g or collapse" -x` collects and passes exactly 3 tests
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0 with at least 316 tests collected (313 before this plan, plus the 3 added here) and zero failures
    - `grep -c "def test_chart_mean_curvature_reports_lambda_min_max_and_det_g\|def test_chart_curvature_field_reports_lambda_min_max_and_det_g\|def test_cond_is_blind_to_uniform_metric_collapse_but_det_g_is_not" notebooks/pu_manifold/tests/test_curvature_probe.py` returns 3
    - `grep -c "def _small_cae" notebooks/pu_manifold/tests/test_curvature_probe.py` returns 1 — the existing helper was reused, not duplicated
    - `git diff --quiet notebooks/pu_manifold/cae.py` exits 0
  </acceptance_criteria>

  <reversibility rating="reversible">Test-only addition; deleting the three tests restores the prior state exactly.</reversibility>

  <done>All three tests pass, the full 316-test suite is green, and the collapse-invariance test independently proves the claim that motivated CURV-04's reopening.</done>
</task>

<task type="auto">
  <name>Task 3: D-04 weight calibration, and the dry-run that declares the sizing and reading rules</name>

  <read_first>
    - notebooks/diagnostics/decoder_prior_ladder_run.py (the file being modified, as written by Task 1)
    - notebooks/pu_manifold/decoder_priors.py (`isometry_penalty` signature around line 310, `christoffel_penalty` signature around line 272 — both already multiply by `weight` internally, so calling with `weight=1.0` returns the raw unweighted batch mean)
    - notebooks/pu_manifold/cae.py (`chart_loss` around line 224 — `recon` is the per-row min chart error, batch mean; `ChartAutoEncoder.forward` returns `y_charts` and `p`)
    - notebooks/diagnostics/synthetic_control_run.py (`print_dry_run_report` around line 543 — the shape this dry-run copies, including printing the protocol deviation where the protocol is stated)
  </read_first>

  <files>notebooks/diagnostics/decoder_prior_ladder_run.py</files>

  <action>
Add `calibrate_weights(model, x_batch, fractions, christoffel_max_rows)` returning a dict keyed
by `(mode, fraction)` with the derived absolute weight, plus the `base_recon` and per-mode
`base_penalty` it was derived from.

The measurement is one forward pass, no training: call `out = model(x_batch)` then
`base_recon = float(cae.chart_loss(x_batch, out["y_charts"], out["p"])["recon"])`; then
`base_scale = float(decoder_priors.isometry_penalty(model, x_batch, weight=1.0, mode="scale"))`
and `base_christoffel = float(decoder_priors.christoffel_penalty(model, x_batch, weight=1.0,
max_rows_per_chart=christoffel_max_rows))`. The derived weight is
`fraction * base_recon / base_penalty[mode]`.

Two hard rules on this function, both stated in its docstring:
  - **The calibration is computed ONCE**, on a freshly-constructed untrained model seeded with
    `torch.manual_seed(LADDER_SEEDS[0])` at the full ladder architecture, using the first
    `pu.BATCH` (64) rows of `x_train32`, and the resulting absolute weights are reused for every
    seed. If each seed calibrated its own weights, a "rung" would be a different absolute weight
    per seed and would not be a rung at all.
  - If `base_penalty[mode]` is `0.0` or non-finite, raise `ValueError` naming the mode and the
    measured value rather than dividing. D-04's ladder is meaningless if the penalty is
    identically zero at init, and a silent `inf` weight would present as a diverged cell.
Record `weight_fraction`, `base_recon_at_init` and `base_penalty_at_init` on every cell record
so the derivation is reconstructable from the JSONL alone.

Then flesh out `print_dry_run_report()` so it prints, and exits writing nothing:
  1. The §A architecture-and-fixture table of this plan, with each value read from its imported
     source constant rather than re-typed, plus the two statements that the fixture seed is held
     fixed while only the model seed varies, and that early stopping is disabled for every cell
     by inheriting `pu._converge_cfg`.
  2. The §B sizing table, the 24-cell arithmetic, the measured `curv_wallclock_s =
     2993.912286339997` and `train_wallclock_s = 6253.836718825012` from the sealed row
     `control_saddle_nc4_d20_ep300`, the `~598.8 s/cell` projection at `LADDER_EVAL_N = 2000`,
     the statement that the linearity behind that projection is unverified and is measured by
     `--probe`, and the D-16 consequence that ladder cells are not comparable to the sealed
     full-cloud row on evaluation-set size.
  3. The §C relief ladder F1 through F4 in order, each with its stated cost, and the note that
     F3 is `christoffel`-only because `isometry_penalty` has no row subsampling.
  4. `READING_RULES_TEXT` — the §D Tier-1 and §E Tier-2 wording of this plan, verbatim, as a
     module-level string constant so it is declared in source before any number exists. Include
     the three Tier-1 verdict strings, the refusal rule, the four Tier-2 axes with their
     toward-1 definitions, the D-09 bias-guard sentence, the D-03 seed-consistency rule, and the
     statement that a null is a complete outcome and that no arithmetic anywhere combines the
     four axes.
  5. The §F combination-arm selection rule and its mandatory post-hoc label string.
  6. The §G baseline-pathology precondition, including the sealed reference bands and the
     `NO PATHOLOGY AT THIS EPOCH BUDGET` exit.

Add `--eval-n`, `--max-epochs`, `--mode` and `--seeds` to the argument parser with the §B
constants as defaults, so the later plans have the switches they need without editing the parser
again.
  </action>

  <verify>
    <automated>.venv/bin/python notebooks/diagnostics/decoder_prior_ladder_run.py --dry-run | tee /tmp/03_1_dryrun.txt && grep -q "MECHANISM DEMONSTRATED" /tmp/03_1_dryrun.txt && grep -q "MECHANISM NOT DEMONSTRATED" /tmp/03_1_dryrun.txt && grep -q "COLLAPSE" /tmp/03_1_dryrun.txt && grep -q "NOT SEED-CONSISTENT" /tmp/03_1_dryrun.txt && grep -q "POST-HOC" /tmp/03_1_dryrun.txt && grep -q "NO PATHOLOGY AT THIS EPOCH BUDGET" /tmp/03_1_dryrun.txt && grep -q "2993.912286339997" /tmp/03_1_dryrun.txt && grep -q -- "-0.015106571347065712" /tmp/03_1_dryrun.txt && echo DRYRUN_OK</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python notebooks/diagnostics/decoder_prior_ladder_run.py --dry-run` exits 0 and creates no file (`notebooks/.cache/03.1_decoder_prior_ladder.jsonl` still absent or unchanged in size before and after)
    - The dry-run output contains all three Tier-1 verdict strings `MECHANISM DEMONSTRATED`, `MECHANISM NOT DEMONSTRATED`, `COLLAPSE`
    - The dry-run output contains the four Tier-2 axis names `direction`, `magnitude`, `calibration`, `rank` on separate lines and the phrase `never collapsed`
    - The dry-run output contains `NOT SEED-CONSISTENT`, `POST-HOC`, `NO PATHOLOGY AT THIS EPOCH BUDGET`, and the four relief-valve labels `F1`, `F2`, `F3`, `F4`
    - The dry-run output contains the literal measured constants `2993.912286339997`, `6253.836718825012` and `-0.015106571347065712`
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'notebooks/diagnostics');sys.path.insert(0,'notebooks');import decoder_prior_ladder_run as r;print(r.LADDER_SEEDS, r.LADDER_MODES, r.LADDER_FRACTIONS, r.LADDER_EVAL_N, r.LADDER_BUDGET_S, r.LADDER_FIXTURE_SEED, r.ANCHOR_RHO_SPEARMAN)"` prints `(20260813, 20260814, 20260815) ('scale', 'christoffel') (0.01, 0.1, 1.0) 2000 28800 20260816 -0.015106571347065712`
    - `calibrate_weights` raises `ValueError` naming the mode when a base penalty is `0.0` — checkable by calling it with a stub whose penalty returns zero
    - `git diff --quiet notebooks/pu_manifold/cae.py notebooks/diagnostics/synthetic_control_run.py` exits 0
  </acceptance_criteria>

  <reversibility rating="reversible">Pure addition to a file this phase created; the fractions and candidate epoch list can be changed before any cell runs at no cost.</reversibility>

  <done>`--dry-run` prints the complete pre-declared sizing, relief ladder and D-08 reading rules and writes nothing, and `calibrate_weights` derives per-mode absolute weights from the declared fractions with a guard against a zero or non-finite base penalty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → local filesystem | The only boundary in scope: append-only JSONL under the gitignored `notebooks/.cache/`. No network, no auth surface, no multi-user access control, no untrusted input path anywhere in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-01 | Tampering | `decoder_priors.decoder_prior_active` mode validation | low | accept | The `ValueError` naming an unrecognized `mode` string is the only V5 Input Validation surface in scope. This phase adds no new mode string, does not extend the signature, and does not weaken the guard. Non-regression is covered by the existing `test_metric_deviation_unknown_mode_raises_naming_the_string`, re-run by the full-suite command in Task 2's verify. |
| T-03.1-02 | Information Disclosure | `notebooks/.cache/03.1_decoder_prior_ladder.jsonl` | low | accept | Gitignored, local, non-secret. Contains only synthetic-fixture-derived numbers, timings and torch version — the same pattern every prior phase's record already uses. No credential, no user data, no external identifier. |
| T-03.1-03 | Tampering | `notebooks/pu_manifold/cae.py` (sealed 02.2 architecture with RNG-order-dependent anchors) | medium | mitigate | `git diff --quiet notebooks/pu_manifold/cae.py` exits 0 is an acceptance criterion on every task in this plan. The prior is installed by a scoped contextmanager that rebinds `cae.chart_loss` and restores it in a `finally`, never by editing the module. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | high | accept | **No package-manager install task exists in this phase.** Every import (`torch` 2.13.0+cpu, `numpy` 2.5.1, `scikit-learn` 1.9.0, `pytest`) is an existing, already-vetted project dependency verified installed in `.venv`. `03.1-RESEARCH.md`'s Package Legitimacy Audit records this as not applicable for exactly this reason, so no `[ASSUMED]`/`[SUS]` package exists and no legitimacy checkpoint fires. |

**Honest assessment, recorded rather than padded:** no STRIDE-relevant mitigation is needed for
this phase beyond T-03.1-03's sealed-file check. This is local Python computing curvature on a
synthetic fixture — the same finding already recorded for this codebase in `02.6-REVIEW.md`
("no network surface, no auth, no user-input path and no persistence layer"). Inventing threats
here would be worse than recording their absence.
</threat_model>

<verification>
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0, at least 316 tests, zero failures
- `.venv/bin/python notebooks/diagnostics/decoder_prior_ladder_run.py --dry-run` exits 0 and writes nothing
- `.venv/bin/python notebooks/diagnostics/decoder_prior_ladder_run.py --smoke --record-path /tmp/03_1_smoke.jsonl` exits 0 and appends 2 records carrying all four CURV-04 fields
- `git diff --quiet notebooks/pu_manifold/cae.py notebooks/diagnostics/synthetic_control_run.py notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py` exits 0
- `git status --porcelain src/effdim/` is empty — `src/effdim/` is frozen this milestone
</verification>

<success_criteria>
- `chart_mean_curvature` and `chart_curvature_field` both return `lambda_min`, `lambda_max`, `det_g` and `log10_det_g` alongside an unchanged `metric_condition_number`
- The existing bit-identity regressions pass unchanged, proving `H_vec`, `H_norm` and `metric_condition_number` did not move (D-15)
- Three new tests pin the new fields against independent computation and pin that `cond(g)` alone is blind to uniform collapse
- One prior-active cell runs end to end at smoke scale and produces one complete JSONL record
- The full ladder sizing, the relief ladder and the D-08 Tier-1/Tier-2 reading rules are declared in source and printed by `--dry-run` before any real cell has run
- `cae.py`, `synthetic_control_run.py` and `swiss_roll_isometry_prior_sweep_run.py` are unmodified
</success_criteria>

<output>
Create `.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-01-SUMMARY.md` when done
</output>
