---
phase: 03.1-decoder-metric-regularization-inserted
plan: 04
type: execute
wave: 4
depends_on: ["03.1-03"]
files_modified:
  - notebooks/diagnostics/decoder_prior_ladder_run.py
autonomous: false
requirements: [CURV-04]

must_haves:
  truths:
    - "--summary prints Tier 1 and Tier 2 as two separate blocks that are never merged, with the three Tier-1 verdict strings and the four Tier-2 axes as four separate columns and no arithmetic anywhere combining them (D-08)"
    - "Every cell prints median AND the full three-seed spread; a movement not consistent across all three seeds is labelled NOT SEED-CONSISTENT and is not claimed as an effect (D-03)"
    - "Held-out reconstruction mse_per_dim is printed beside every cell as the bias guard, and no rule anywhere selects on it (D-09)"
    - "The three weight=0 baseline cells are re-fit fresh inside this phase under this phase's own protocol; Phase 3's sealed d=20 saddle row is not used as a comparison point (D-05)"
    - "The baseline-pathology precondition runs after the baseline cells and halts the ladder if all three baselines land in the d=4-healthy band, because a prior cannot be measured against a pathology that is absent"
    - "Every cell records eval_sample_size so a later full-cloud re-evaluation is well defined (D-16)"
    - "Every cell records the ratified relief_applied list, and if it carries the F4 entry --summary prints a D-07 OVERRIDE FIRED banner ahead of any Tier-1 or Tier-2 number, so cells trained on an overridden epoch budget cannot be read without the override (D-07, D-11)"
    - "A diverged cell is recorded with nonfinite_guard.tripped true and printed as DIVERGED, never as the prior making things worse (research assumption A3)"
    - "A null result — every axis unmoved within the three-seed spread — is reported as plainly as a success (D-10, D-11)"
    - statement: "The Tier-1 and Tier-2 tables read as the developer expects and the read-out states what the data shows rather than what the phase hoped for"
      verification: backstop
  artifacts:
    - notebooks/diagnostics/decoder_prior_ladder_run.py
    - notebooks/.cache/03.1_decoder_prior_ladder.jsonl
  key_links:
    - "chart_curvature's lambda_min / lambda_max / log10_det_g / metric_condition_number -> the per-cell distribution summaries -> Tier 1's per-mode verdict"
    - "sc._fidelity_axes' four columns -> the per-cell record's fidelity dict -> Tier 2's four separate axis tables"
    - "cell_key's 'any' sentinel at weight 0.0 -> one baseline cell set shared by both arms, never re-run per mode"
---

<objective>
Implement the D-08 read-out, then run the ladder and produce the phase's numbers.

Purpose: this is the measurement. `03-NOTE-isometry-prior-spike.md` delivered "tested, working
infrastructure and zero measurement"; everything in plans `03.1-01` through `03.1-03` exists so
that this plan trains cells and reports what happened. Tier 1 exists so that a flat Tier 2 can be
told apart from a prior that was never really active — without evidence the penalty moved its own
target quantity, a null is uninterpretable.

Output: a `--summary` read-out implemented and proven **before any real cell runs**, then the
baseline and both single-mode arms trained, recorded and read out.

**Requirement note:** every ladder cell records CURV-04's `λ_min` / `λ_max` / `det(g)` /
`log10 det(g)` alongside `cond(g)`, and Tier 1 reads the absolute-scale fields as primary with
`cond(g)` demoted to one reported detail — the promotion recorded in `03.1-01-PLAN.md`'s
`<assumption_delta_decision>`.

**Compute note — this plan is the overnight run.** It costs whatever `03.1-03`'s ratified
configuration projected, up to `LADDER_BUDGET_S = 28800` (8.0 h). **Run it with no other
CPU-heavy job on the machine.** Launch in the background with output tee'd to a log file and
poll; a foreground call will exceed the tool timeout many times over. `--resume` skips any cell
already in the record file, so an interrupted run is restartable without refitting.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-CONTEXT.md
@.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-PATTERNS.md
@.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-01-PLAN.md
@.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-03-SUMMARY.md
</context>

<the_reading_rules_this_plan_implements>

**Declared in `03.1-01-PLAN.md` §D/§E before anything ran, encoded in source as
`READING_RULES_TEXT` and printed by `--dry-run` as of `03.1-01` Task 3. Restated here verbatim
because this plan implements the printers. Not one word of it may change now that cells are
about to produce numbers.**

### Tier 1 — mechanism. The exact wording of "moves toward healthy".

Read per mode over the rungs ordered by ascending weight, on the **3-seed median** of each
per-cell distribution statistic, with the **full 3-seed spread always printed beside it**.

- **`λ_min` moves toward healthy** = median `λ_min` is non-decreasing across ascending weight
  rungs **and** is strictly greater at the top rung than at `weight=0`.
- **`λ_max` moves toward healthy** = median `λ_max` does not fall toward the collapse band. A
  `λ_max` that *drops* while `cond(g)` improves is the collapse signature and is read as the
  prior making the metric worse, never better.
- **`det(g)` moves toward healthy** = median `log10_det_g` is non-decreasing across ascending
  rungs and strictly greater at the top rung than at `weight=0` — i.e. the geometric-mean metric
  eigenvalue moves toward 1, exactly the quantity `scale`'s penalty `(log det g / d)²` minimizes.
- **`cond(g)` moves toward healthy** = median `cond(g)` is non-increasing across ascending rungs
  and strictly lower at the top rung than at `weight=0`. **`cond(g)` alone is never sufficient.**

**Per-mode verdict, exactly one of three strings:**
- `MECHANISM DEMONSTRATED` — the mode's own target field moved as above. `scale`'s target fields
  are `log10_det_g` and `λ_min`; `christoffel`'s is `cond(g)`, with `λ_max` not falling.
- `COLLAPSE` — median `cond(g)` improved while median `λ_max` fell by at least one decade from
  the `weight=0` cell. Scored as the prior making the metric worse.
- `MECHANISM NOT DEMONSTRATED` — neither of the above.

**Refusal rule.** If a mode's Tier 1 is `MECHANISM NOT DEMONSTRATED` or `COLLAPSE`, its Tier 2
table is still printed in full — D-10 forbids hiding numbers — but is labelled
`not read as evidence about the estimand`. A prior that did not move its own target quantity was
never actually tested.

### Tier 2 — estimand. The exact wording of "moves toward 1".

Four axes, scored separately, **never collapsed into one score, and no arithmetic anywhere
combines them.** Per axis, over the rungs ordered by ascending weight, on the 3-seed median with
the full spread printed. The comparison is against **this phase's own re-fitted `weight=0` cells
at the same epoch budget** (D-05), never against Phase 3's sealed row.

- **direction** — `direction_median_cosine` moves toward **+1**: `|cosine − 1|` strictly smaller
  at the mode's best rung than at `weight=0`.
- **magnitude** — `magnitude_median_ratio` moves toward **1**: `|log10(ratio)|` strictly smaller
  at the best rung than at `weight=0`. Log scale because the baseline sits four decades off; a
  linear distance would be dominated by that one number.
- **calibration** — `calibration_r2` moves toward **1**: strictly larger than at `weight=0`,
  **and** `calibration_slope` moves toward 1. Both reported; neither alone.
- **rank** — `rank_spearman_rho` moves toward **+1**: strictly larger than at `weight=0`.

**No bar, no verdict artifact (D-10).** These are reading rules for a diagnostics table, not a
gate. No `PREREGISTRATION.md`, no ratification commit, no git-ancestry proof, no verdict JSON, no
threshold table. **A null — every axis unmoved within the 3-seed spread — is a complete answer
and is reported as plainly as a success** (D-11).

**Seed consistency (D-03).** Three draws cannot separate a small real effect from seed noise. Any
Tier-1 or Tier-2 movement whose direction is not consistent across all three seeds is labelled
`NOT SEED-CONSISTENT` and is not claimed as an effect.

**Bias guard (D-09).** Held-out `reconstruction.mse_per_dim` is printed beside every cell.
Stated rule: **an improvement bought with materially worse reconstruction is not an
improvement.** No numeric threshold — the last reconstruction threshold proposed in this
milestone was deferred as circular and that objection is unchanged. The comparison is against
this phase's own `weight=0` cells at the same three seeds, reported as a ratio, and the reader
makes the call. **The bias guard is a reading rule, never a selector**: no rule selects on
reconstruction.

### Baseline-pathology precondition (`03.1-01-PLAN.md` §G)

After the three `weight=0` cells complete, print `BASELINE PATHOLOGY CHECK` with median
`cond(g)`, `λ_min`, `λ_max` and `log10_det_g` per seed against the sealed reference bands in
`03-FINDINGS.md` §5 — healthy-metric seed 20260813 at `λ_max = 3.352`, `cond = 3.30e+07`;
collapsed seeds at `λ_max ≈ 5.4e-07`, `det(g) ≈ 1e-162`; healthy `d=4` saddle control at
`cond = 4.098`. **If all three baseline seeds land in the `d=4`-healthy band — median `cond(g)`
within one decade of 4.098 and median `λ_max` within one decade of 1 — print
`NO PATHOLOGY AT THIS EPOCH BUDGET` and exit 2 without training a single prior-active cell.**
That is itself a finding: the pathology is a long-training artifact. Escalation is an explicit
user decision (D-11).

</the_reading_rules_this_plan_implements>

<artifacts_this_phase_produces>

Newly created by this plan — excluded from drift verification because they do not yet exist:

**New CLI flags on `notebooks/diagnostics/decoder_prior_ladder_run.py`:** `--summary`,
`--run-ladder`, `--resume`, `--arm`

**New module-level symbols in `decoder_prior_ladder_run.py`:** `HEALTHY_D4_COND`,
`HEALTHY_LAMBDA_MAX`, `COLLAPSE_LAMBDA_MAX`, `SEALED_HEALTHY_SEED_COND`,
`TIER1_VERDICTS`, `rows_for_mode`, `median_and_spread`, `seed_consistent`,
`baseline_pathology_check`, `tier1_readout`, `tier2_readout`, `print_summary`, `run_ladder`

**New JSONL record `kind` value:** `"ladder_cell"` (already emitted by `run_cell` from
`03.1-01`; this plan writes the real ones)

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Implement the two-tier read-out, and prove it on a synthetic record before any cell runs</name>

  <read_first>
    - notebooks/diagnostics/decoder_prior_ladder_run.py (the file being modified, including `run_cell`, `print_cell_row`, `READING_RULES_TEXT`, `cell_key`, `load_completed` from `03.1-01` and `probe`/`anchor_check` from `03.1-03`)
    - notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py (lines 460-540: `mechanism_check` and `bias_check` — the exact precedent for the Tier-1/Tier-2 split including the refusal line "the penalty is not doing what it claims; no other column below is read as evidence about anything", and `FULL_TABLE_COLUMNS` around line 555 for the summary table shape; lines 380-460 for the epochs-uniformity guard)
    - notebooks/diagnostics/synthetic_control_run.py (`print_control_row` lines 500-540 — the four-axes-as-four-lines print shape with no arithmetic combining them, and the `rank_calibration_applicable` branch)
    - .planning/phases/03-decoder-curvature-field/03-FINDINGS.md (§5's metric-eigenvalue table — the sealed reference bands the baseline-pathology check compares against; §6's four-axis table for the `d=20` saddle and `d=4` diagnostic rows)
  </read_first>

  <files>notebooks/diagnostics/decoder_prior_ladder_run.py</files>

  <action>
Implement the read-out **before any real cell runs**, and prove it against a synthetic record
file so a bug in the printer cannot be discovered after eight hours of compute.

Add module constants for the sealed reference bands, each with a docstring citing
`03-FINDINGS.md` §5 or §6: `HEALTHY_D4_COND = 4.098` (the `d=4` saddle diagnostic's median
`cond(g)`), `HEALTHY_LAMBDA_MAX = 3.352` and `SEALED_HEALTHY_SEED_COND = 3.30e+07` (seed
20260813, the one PU fit whose metric had a real absolute scale), `COLLAPSE_LAMBDA_MAX = 5.4e-07`
(seeds 20260814/20260815). `TIER1_VERDICTS` holds the three verdict strings exactly as this
plan's §Tier 1 spells them.

Add `rows_for_mode(records, mode)` returning that mode's cells plus the shared `weight=0`
baseline cells (matched by `cell_key`'s `"any"` sentinel, never re-run per mode), grouped by
weight and sorted ascending. Add `median_and_spread(values)` returning the median and the full
sorted list — every print shows both, never the median alone (D-03). Add
`seed_consistent(per_seed_deltas)` returning whether all three seeds moved in the same direction.

Add `baseline_pathology_check(baseline_rows)` implementing §G exactly: print
`BASELINE PATHOLOGY CHECK` with the four medians per seed against the three sealed bands, and
return a boolean. When it returns false, `run_ladder` prints `NO PATHOLOGY AT THIS EPOCH BUDGET`
and exits 2 before training any prior-active cell.

Add `tier1_readout(rows, mode)` printing, per weight rung ascending: median and full spread of
`lambda_min`, `lambda_max`, `log10_det_g` and `cond`, then the per-mode verdict as exactly one of
the three `TIER1_VERDICTS` strings, evaluated by the rules in this plan's §Tier 1. `scale`'s
target fields are `log10_det_g` and `lambda_min`; `christoffel`'s is `cond` with `lambda_max` not
falling. The `COLLAPSE` verdict fires when median `cond` improved while median `lambda_max` fell
by at least one decade from the `weight=0` cell. On `MECHANISM NOT DEMONSTRATED` or `COLLAPSE`,
print the refusal line stating that no Tier-2 column below is read as evidence about the
estimand for this mode — copy the shape of `mechanism_check`'s own refusal line.

Add `tier2_readout(rows, mode, tier1_verdict)` printing the four axes as **four separate
labelled blocks** — `direction`, `magnitude`, `calibration`, `rank` — each with median and full
spread per rung and its toward-1 movement judgement per this plan's §Tier 2, plus a
`NOT SEED-CONSISTENT` label wherever the three seeds disagree in direction. Print held-out
`mse_per_dim` median and spread beside every rung as D-09's bias guard, with the ratio against
the `weight=0` median, and print the stated rule that an improvement bought with materially worse
reconstruction is not an improvement. **No function in this runner may take two of the four axes
and return one number from them** — the four stay four, matching `bias_check`'s and
`_fidelity_axes`' own discipline. When `tier1_verdict` is not `MECHANISM DEMONSTRATED`, prefix
the whole Tier-2 block with `not read as evidence about the estimand`.

Add `print_summary(record_path)` and a `--summary` flag: print `READING_RULES_TEXT` first (so
every read-out carries its own rules), then the epochs-uniformity guard from the sweep runner
(every cell has `epochs_run == max_epochs` and `early_stopped == False`, since early stopping is
structurally inert under `pu._converge_cfg`), then any `DIVERGED` cells from
`nonfinite_guard.tripped`, **then the relief banner described next**, then
`baseline_pathology_check`, then Tier 1 and Tier 2 per mode in that order, then the full per-cell
table. Any cell with `nonfinite_guard.tripped` is excluded from every median and spread and is
listed separately as `DIVERGED — not read as evidence`, never folded in as a bad result.

**The relief banner, and why it is not optional.** Immediately after the diverged-cell list, scan
every cell's `relief_applied` list and print the distinct relief valves in effect across the run.
If any cell's list carries the F4 override entry, print — on its own line, before any Tier-1 or
Tier-2 number — `D-07 OVERRIDE FIRED`, followed by the affected `config_id`s and the sentence that
those cells were trained on a cut epoch budget as a **confirmed override of D-07's fence against
cutting epochs**, that under-training is the confound D-07 fenced against (`03-08`: training to
the full budget instead of stopping early cut held-out `mse_per_dim` 62.2% and left `cond(g)`
unmoved), and that no reading of those cells may omit it. F4 can reach a record only through the
explicit override confirmed at `03.1-03` Task 3, so this banner is what makes the override travel
with the numbers instead of being lost between the checkpoint and the write-up. If no cell carries
it, print one line saying the fence held.

Prove the printer before it matters: write a small synthetic JSONL by hand covering both modes,
three rungs, three seeds, one deliberately diverged cell, one deliberately seed-inconsistent
movement, and **one cell whose `relief_applied` carries the F4 override entry** so the banner path
is exercised before any real cell exists. Then run `--summary --record-path` against it. That
synthetic file is a throwaway under `/tmp`, not a committed fixture.
  </action>

  <verify>
    <automated>.venv/bin/python notebooks/diagnostics/decoder_prior_ladder_run.py --summary --record-path /tmp/03_1_synthetic.jsonl 2>&1 | tee /tmp/03_1_summary_synth.log; grep -q "MECHANISM" /tmp/03_1_summary_synth.log && grep -q "direction" /tmp/03_1_summary_synth.log && grep -q "magnitude" /tmp/03_1_summary_synth.log && grep -q "calibration" /tmp/03_1_summary_synth.log && grep -q "rank" /tmp/03_1_summary_synth.log && grep -q "NOT SEED-CONSISTENT" /tmp/03_1_summary_synth.log && grep -q "DIVERGED" /tmp/03_1_summary_synth.log && grep -q "BASELINE PATHOLOGY CHECK" /tmp/03_1_summary_synth.log && grep -q "D-07 OVERRIDE FIRED" /tmp/03_1_summary_synth.log && echo SUMMARY_OK</automated>
  </verify>

  <acceptance_criteria>
    - `--summary` against the synthetic record prints `READING_RULES_TEXT` in full, including all three of `MECHANISM DEMONSTRATED`, `MECHANISM NOT DEMONSTRATED` and `COLLAPSE`
    - Tier 1 and Tier 2 appear as two separately headed blocks and Tier 1 is printed before Tier 2 for each mode
    - The four Tier-2 axes `direction`, `magnitude`, `calibration`, `rank` each appear as their own labelled block, and every rung line shows both a median and the full three-seed spread
    - The deliberately seed-inconsistent movement in the synthetic record is labelled `NOT SEED-CONSISTENT`
    - The deliberately diverged cell is listed as `DIVERGED` and is absent from every median and spread
    - The synthetic record's one cell carrying the F4 override entry in `relief_applied` produces a `D-07 OVERRIDE FIRED` banner naming that cell's `config_id`, printed before the first Tier-1 number and before the first Tier-2 number
    - `BASELINE PATHOLOGY CHECK` is printed with the three sealed reference bands `4.098`, `3.352` and `3.30e+07` visible in the output
    - `grep -c "composite\|combined_score\|overall_score\|agreement_score" notebooks/diagnostics/decoder_prior_ladder_run.py` returns 0 — no code path collapses the four axes into one number
    - The Tier-2 output shows each of the four axis names exactly once as a block heading, and the number of distinct numeric columns under them is four — no fifth summary column exists
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` still exits 0
    - `git diff --quiet notebooks/pu_manifold/cae.py notebooks/diagnostics/synthetic_control_run.py notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py` exits 0
  </acceptance_criteria>

  <reversibility rating="reversible">Print-path only; changing a printer before any cell runs costs nothing.</reversibility>

  <done>`--summary` prints the pre-declared reading rules, the baseline-pathology check, Tier 1 then Tier 2 per mode with four separate axis blocks, medians beside full spreads, and correct `NOT SEED-CONSISTENT` and `DIVERGED` handling — all proven on a synthetic record before a single real cell has run.</done>
</task>

<task type="auto">
  <name>Task 2: Run the ladder — baseline, scale arm, christoffel arm</name>

  <read_first>
    - notebooks/diagnostics/decoder_prior_ladder_run.py (the file being modified, complete as of Task 1)
    - .planning/phases/03.1-decoder-metric-regularization-inserted/03.1-03-SUMMARY.md (the ratified configuration from the blocking checkpoint: `max_epochs`, `fractions`, `eval_n`, `christoffel_max_rows`, which fallbacks fired, and the projected total)
    - notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py (lines 82-129 `load_completed` / `append_record` and lines 98-106 `cell_key` — the resumability idiom; lines 607-731 for the CLI skeleton)
    - notebooks/pu_manifold/decoder_priors.py (`decoder_prior_active`: at `weight == 0.0` it yields immediately without touching `cae.chart_loss`, which is why the baseline cells are structurally the untouched `cae.train_cae` path)
  </read_first>

  <files>notebooks/diagnostics/decoder_prior_ladder_run.py</files>

  <action>
Add `run_ladder(record_path, resume, arm, max_epochs, fractions, eval_n, christoffel_max_rows,
seeds, device)` plus `--run-ladder`, `--resume` and `--arm` flags, and execute it at the
configuration ratified at `03.1-03` Task 3.

Order of execution, and it matters:

1. **Calibrate once.** Call `calibrate_weights` on a freshly-constructed untrained model seeded
   with `torch.manual_seed(LADDER_SEEDS[0])` at the full ladder architecture, using the first
   `pu.BATCH` (64) rows of `x_train32`. Print the derived absolute weight per `(mode, fraction)`
   beside its `base_recon_at_init` and `base_penalty_at_init`. **These absolute weights are then
   reused for every seed** — a rung that differed in absolute weight per seed would not be a rung.
2. **The three `weight=0` baseline cells** (D-05), one per seed, re-fit fresh inside this phase
   under this phase's own protocol. Keyed by `cell_key`'s `"any"` sentinel so they are recorded
   once and shared by both arms — neither arm re-runs its own baseline.
3. **`baseline_pathology_check`.** If it returns false, print `NO PATHOLOGY AT THIS EPOCH BUDGET`
   and `sys.exit(2)` **before training a single prior-active cell**. The ladder has nothing to fix
   and that is itself the finding.
4. **The `scale` arm**: each declared fraction × each seed.
5. **The `christoffel` arm**: each declared fraction × each seed, at the ratified
   `christoffel_max_rows`.

**Stamp the ratified relief configuration onto every cell.** `run_ladder` takes the
`relief_applied` list from the ratified configuration — the `03.1-03` probe summary record, the one
carrying `relief_applied` and `chosen_max_epochs` — and writes it onto every cell record it
appends, extending the field `run_cell` already emits rather than adding a parallel one. If that
list carries the F4 override entry, it lands on every cell the override affected, so a cell trained
on a cut epoch budget cannot be read anywhere without the override travelling with it. Do not
synthesize this list locally, and do not add F4 to it here: F4 reaches a record only from a probe
run that carried `--confirm-f4-override`.

Append each cell's record as it completes and honour `--resume` by skipping any `cell_key`
already present — the run must survive interruption without refitting, since it is an overnight
job. Print each cell's row with `print_cell_row` as it lands so progress is visible in the log,
and print a running elapsed total against the ratified projection so an overrun is visible early
rather than at the end.

The non-finite guard runs per cell as written in `03.1-01`: on a tripped guard, write the record
with `nonfinite_guard.tripped` true, print the row prefixed `DIVERGED — not read as evidence`,
and **continue to the next cell** rather than aborting the run. A diverged cell is never read as
"the prior made it worse" — the saddle's own continuous-training divergence point was never
measured (research assumption A3) and blocking at `epoch_block=25` is the mitigation, not a
guarantee.

Do not change any pre-declared constant, reading rule or selection rule at this point. If the run
overruns the ratified projection, let it finish or interrupt and resume — do not silently reduce
the cell set, and do not add rungs. Widening scope to avoid a negative result is exactly what
D-11 forbids.

Finish by running `--summary` against the real record and capturing the output to a log file, so
the Tier-1 and Tier-2 tables exist as text for the findings document in `03.1-05`.

**Runtime note for the executor:** this is the overnight run, up to 8 hours. Launch it in the
background with output tee'd to a log file and poll the log. Run nothing else CPU-heavy
concurrently. If the session ends mid-run, re-launch with `--resume`.
  </action>

  <verify>
    <automated>.venv/bin/python notebooks/diagnostics/decoder_prior_ladder_run.py --summary 2>&1 | tee /tmp/03_1_summary.log; .venv/bin/python -c "
import json
rs=[json.loads(l) for l in open('notebooks/.cache/03.1_decoder_prior_ladder.jsonl')]
cells=[r for r in rs if r.get('kind')=='ladder_cell']
base=[r for r in cells if r['weight']==0.0]
scale=[r for r in cells if r['mode']=='scale' and r['weight']>0.0]
chris=[r for r in cells if r['mode']=='christoffel' and r['weight']>0.0]
assert len(base)==3, ('baseline cells', len(base))
assert len(scale)==len(chris) and len(scale)>=6, ('arm cells', len(scale), len(chris))
for r in cells:
    for k in ('eval_sample_size','weight_fraction','model_seed','nonfinite_guard','lambda_min','lambda_max','det_g','log10_det_g'):
        assert k in r, (r['config_id'], k)
print('cells', len(cells), 'baseline', len(base), 'scale', len(scale), 'christoffel', len(chris))
" && grep -q "MECHANISM" /tmp/03_1_summary.log && grep -q "BASELINE PATHOLOGY CHECK" /tmp/03_1_summary.log && echo LADDER_OK</automated>
  </verify>

  <acceptance_criteria>
    - `notebooks/.cache/03.1_decoder_prior_ladder.jsonl` contains exactly 3 `ladder_cell` records at `weight == 0.0`, and equal non-zero counts for `mode == "scale"` and `mode == "christoffel"`, each at least 6 (2 rungs × 3 seeds after F1, or 9 at the full 3 rungs)
    - Every `ladder_cell` record carries non-null `eval_sample_size`, `weight_fraction`, `base_recon_at_init`, `base_penalty_at_init`, `model_seed`, `christoffel_max_rows_per_chart`, `nonfinite_guard`, and `lambda_min`/`lambda_max`/`det_g`/`log10_det_g` distribution summaries
    - Every `ladder_cell` record has `fixture_seed == 20260816` — the fixture is held fixed and only `model_seed` varies
    - Every `ladder_cell` record carries a `relief_applied` list identical to the ratified configuration's, and an F4 entry appears on those cells if and only if the D-07 override was confirmed at `03.1-03` Task 3 and the probe was re-run with `--confirm-f4-override`
    - If any cell carries the F4 entry, `/tmp/03_1_summary.log` prints `D-07 OVERRIDE FIRED` before its first Tier-1 number; if none does, it prints the one-line statement that the fence held
    - Every non-diverged `ladder_cell` record has `epochs_run == max_epochs` and `early_stopped == false` — early stopping is structurally inert under `pu._converge_cfg`
    - For each `(mode, fraction)` pair, the `weight` field is identical across all three seeds — the calibration was computed once and reused
    - `/tmp/03_1_summary.log` prints `BASELINE PATHOLOGY CHECK`, one Tier-1 verdict per mode from `TIER1_VERDICTS`, and the four Tier-2 axis blocks with medians beside full three-seed spreads
    - `git diff --quiet notebooks/pu_manifold/cae.py notebooks/pu_manifold/decoder_priors.py notebooks/pu_manifold/chart_curvature.py notebooks/diagnostics/synthetic_control_run.py notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py` exits 0
  </acceptance_criteria>

  <reversibility rating="costly">Up to eight hours of CPU. Re-running the ladder at a different epoch budget or rung set costs the whole run again, which is why the configuration was ratified at a blocking checkpoint in `03.1-03` and why no constant may be changed here.</reversibility>

  <done>The baseline and both single-mode arms are trained, recorded and read out; every cell carries its evaluation sample size and CURV-04's absolute-scale fields; and the Tier-1 and Tier-2 tables exist as text.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Read the Tier-1 and Tier-2 tables and decide whether the phase continues</name>

  <what-built>
The ladder ran: three fresh `weight=0` baseline cells and both single-mode arms at the ratified
epoch budget, every cell carrying `λ_min`, `λ_max`, `det(g)`, `log10 det(g)`, `cond(g)`, the four
fidelity axes and held-out reconstruction. `--summary` printed the pre-declared reading rules,
the baseline-pathology check, a Tier-1 verdict per mode and the four Tier-2 axis blocks with
medians beside full three-seed spreads.
  </what-built>

  <how-to-verify>
Run `.venv/bin/python notebooks/diagnostics/decoder_prior_ladder_run.py --summary` and read the
output top to bottom, in this order:

1. **Reading rules** — confirm they are the ones declared before the run, unchanged.
2. **Epochs uniformity and diverged cells** — confirm every cell reached `max_epochs`, and that
   any `DIVERGED` cell is listed separately and excluded from every median rather than folded in.
3. **`BASELINE PATHOLOGY CHECK`** — confirm the three fresh `weight=0` baselines are pathological
   at this epoch budget. If they are healthy, the ladder had nothing to fix and the run should
   have exited 2; that is a finding about the pathology being a long-training artifact, not a
   null about the priors.
4. **Tier 1, per mode** — did the penalty move its own target quantity? `scale`'s targets are
   `log10_det_g` and `λ_min`; `christoffel`'s is `cond(g)` with `λ_max` not falling. Note
   specifically whether any mode fired `COLLAPSE`, where `cond(g)` improved while `λ_max` fell —
   the failure mode `cond(g)` alone is provably blind to and the reason CURV-04 was reopened.
5. **Tier 2, per mode** — the four axes separately. Confirm nothing anywhere combines them, and
   that a mode whose Tier 1 did not fire carries the `not read as evidence about the estimand`
   label over its Tier-2 block.
6. **Bias guard** — held-out `mse_per_dim` beside every rung, with its ratio against the
   `weight=0` median. An improvement bought with materially worse reconstruction is not an
   improvement.
7. **Seed consistency** — anything labelled `NOT SEED-CONSISTENT` is not an effect. Three draws
   cannot separate a small real effect from seed noise, and the phase says so rather than
   implying otherwise.

**A null is a complete outcome.** If both arms are null, the correct next step is to report
"regularizing the parameterization does not rescue the `d=20` CAE metric" plainly and stop
(D-11). The phase does not pre-commit to a conclusion the data has not produced, does not widen
its own scope — more weights, a `d` sweep, the sphere fixture — to avoid reporting a negative
result, and does not propose a route out for Phase 4 that the data has not supplied. Phase 4
stays blocked and the write-up says so.
  </how-to-verify>

  <resume-signal>Type "approved" to proceed to the combination cell and the findings document, "null" to skip the combination cell and write the findings as a plain negative result, or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → local filesystem | Append-only JSONL under the gitignored `notebooks/.cache/`. No network, no auth, no untrusted input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-07 | Tampering | the pre-declared reading rules, once numbers exist | high | mitigate | The single highest-value integrity control in this phase: a rule edited after seeing the data would turn a diagnostics table into an unfalsifiable claim. Mitigated by encoding the rules as `READING_RULES_TEXT` in source in `03.1-01` before any cell ran, by printing them at the head of every `--summary`, by restating them verbatim in this plan, and by Task 1 proving the printers on a synthetic record before Task 2 runs a real cell. |
| T-03.1-08 | Repudiation | a diverged cell read as a measurement | medium | mitigate | A NaN/Inf cell folded into a median would read as "the prior made it worse" and would be indistinguishable from a real negative. Mitigated by the per-cell non-finite guard, by `DIVERGED` cells being excluded from every median and listed separately, and by an acceptance criterion checking that behaviour on a synthetic record. |
| T-03.1-02 | Information Disclosure | `notebooks/.cache/03.1_decoder_prior_ladder.jsonl` | low | accept | Gitignored, local, non-secret; synthetic-fixture-derived numbers and timings only. |
| T-03.1-03 | Tampering | `cae.py`, `decoder_priors.py`, `chart_curvature.py`, and the two additive-only runners | medium | mitigate | `git diff --quiet` on all five exits 0 is an acceptance criterion on both code tasks in this plan. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase; every import is an existing, verified project dependency. `03.1-RESEARCH.md`'s Package Legitimacy Audit records the gate as not applicable, so no `[ASSUMED]`/`[SUS]` package exists and no legitimacy checkpoint fires. |

**Honest assessment:** as in the rest of this phase, the threats worth recording are integrity
threats to the measurement rather than security threats. This is local Python fitting a synthetic
manifold — no network surface, no auth, no user-input path, no persistence beyond a gitignored
local file, matching the finding already recorded in `02.6-REVIEW.md`.
</threat_model>

<verification>
- `.venv/bin/python notebooks/diagnostics/decoder_prior_ladder_run.py --summary` exits 0 and prints the reading rules, the baseline-pathology check, Tier 1 then Tier 2 per mode
- The record file holds 3 baseline cells and equal, non-zero `scale` and `christoffel` arm counts, every cell carrying `eval_sample_size` and the four CURV-04 fields
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
- `git diff --quiet notebooks/pu_manifold/cae.py notebooks/pu_manifold/decoder_priors.py notebooks/pu_manifold/chart_curvature.py notebooks/diagnostics/synthetic_control_run.py notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py` exits 0
- `git status --porcelain src/effdim/` is empty
</verification>

<success_criteria>
- The two-tier read-out is implemented and proven on a synthetic record before any real cell runs
- The `weight=0` baseline is re-fit fresh inside this phase, three seeds, under this phase's own protocol — Phase 3's sealed row is never used as the comparison point
- The baseline-pathology precondition ran and either passed or halted the ladder with `NO PATHOLOGY AT THIS EPOCH BUDGET`
- Both single-mode arms are trained, recorded and read out, with Tier 1 before Tier 2 and the four axes never collapsed
- Every cell records its evaluation sample size, so a full-cloud re-evaluation of any cell is well defined
- A diverged cell is recorded and printed as diverged, never as evidence
- A human has read the tables and decided whether the phase continues to the combination cell or reports a plain null
</success_criteria>

<output>
Create `.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-04-SUMMARY.md` when done
</output>
