---
phase: 03.1-decoder-metric-regularization-inserted
plan: 03
type: execute
wave: 3
depends_on: ["03.1-01", "03.1-02"]
files_modified:
  - notebooks/diagnostics/decoder_prior_ladder_run.py
autonomous: false
requirements: [CURV-04]

must_haves:
  truths:
    - "decoder_prior_ladder_run.py --anchor-check reproduces the sealed row's rank_spearman_rho with == and not a tolerance, proving the copied fixture, protocol and metric call sequence is a faithful copy rather than a re-derivation (D-13)"
    - "decoder_prior_ladder_run.py --probe measures train seconds-per-epoch separately at weight=0, at a calibrated scale weight and at a calibrated christoffel weight, at the real n_charts=4 / chart_dim=20 / ambient=768 architecture (research assumption A1)"
    - "--probe measures curvature-evaluation seconds-per-cell at more than one evaluation sample size on one model and reports the measured per-row cost, so the linearity behind the 598.8 s/cell projection is verified rather than assumed (research assumption A2)"
    - "--probe projects the full 24-cell total from measured numbers only, applies the pre-declared relief valves F1 through F3 in order until a configuration fits LADDER_BUDGET_S, and exits 1 with BUDGET NOT MET if none does"
    - "F4 is never applied automatically because it overrides D-07's explicit fence against cutting epochs: the probe prints it as F4 AVAILABLE BUT NOT APPLIED, it fires only under the explicit --confirm-f4-override flag, and confirming it is its own separately-worded option at the Task 3 checkpoint with BUDGET NOT MET remaining co-equal (D-07, D-11)"
    - "The selected sizing is ratified at a blocking checkpoint before the overnight run launches, with the measured numbers and any fired fallback shown"
    - "notebooks/pu_manifold/cae.py, synthetic_control_run.py and swiss_roll_isometry_prior_sweep_run.py are unmodified"
    - statement: "The anchor cell and the probe ran on an otherwise-idle machine, so the measured seconds-per-epoch are not contaminated by a co-scheduled job"
      verification: backstop
  artifacts:
    - notebooks/diagnostics/decoder_prior_ladder_run.py
    - notebooks/.cache/03.1_decoder_prior_ladder.jsonl
  key_links:
    - "synthetic_control_run.run_one_control called unchanged -> the sealed row control_saddle_nc4_d20_ep300 -> rank_spearman_rho == -0.015106571347065712"
    - "calibrate_weights' declared fractions -> the probe's per-mode training cost -> the projection -> the relief ladder -> the ratified epoch budget the ladder in 03.1-04 runs at"
---

<objective>
Prove the new runner is a faithful copy, measure what the ladder actually costs, and ratify the
sizing before eight hours of CPU are committed.

Purpose: **this is the plan that exists so the phase produces numbers.**
`03-NOTE-isometry-prior-spike.md` §3 recorded `BUDGET NOT MET` with **zero cells trained** — the
predecessor delivered tested, working infrastructure and no measurement. Its probe measured
training cost only, at `chart_dim=2` on the Swiss roll, and never reached curvature evaluation at
the real scale. Research found the real dominant cost is curvature evaluation
(`curv_wallclock_s = 2993.912286339997` for one full-cloud call at `chart_dim=20` / `ambient=768`)
and that a naive copy of Phase 3's protocol across 24 cells costs on the order of 62 h against
D-06's 8–12 h envelope. Both halves of that arithmetic are measured here before anything is
committed to.

Output: a passing exact-equality anchor, a two-sided cost probe, a projection built only from
measured numbers, and a ratified run configuration.

**Requirement note:** the anchor cell is the one full-cloud (`n=10000`) evaluation this phase
runs, so it is also the single full-cloud record carrying CURV-04's new absolute-scale fields at
the sealed protocol — the one point where the new instrumentation and the sealed record meet.

**Compute note — this plan is timing-sensitive.** The anchor cell costs roughly 2.6 h
(`train_wallclock_s = 6253.836718825012` plus `curv_wallclock_s = 2993.912286339997` on the
sealed row) and the probe roughly another hour. **Both must run with no other CPU-heavy job on
the machine**, or the measured seconds-per-epoch are contaminated and the whole projection is
wrong. Do not co-schedule any other plan's work with this one. Every long command is launched in
the background with output tee'd to a log file and polled — a foreground call will exceed the
tool timeout.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-CONTEXT.md
@.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-PATTERNS.md
@.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-01-PLAN.md
</context>

<sizing_declared_before_anything_runs>

Restated here operatively; the full text with rationale is `03.1-01-PLAN.md`
§`pre_declared_constants_and_reading_rules`, and it is encoded in source and printed by
`--dry-run` as of `03.1-01` Task 3. **Nothing below may be changed after the probe reports.**

- **Evaluation sample size:** `LADDER_EVAL_N = 2000` — the `pu._split` holdout rows, exactly
  `curvature_field_pu_run.py`'s own selection-grid evaluation set (lines 730, 1372). Full-cloud
  `n=10000` is reserved for the anchor cell alone (D-16).
- **Rung count:** 3 per mode, at declared fractions `(0.01, 0.1, 1.0)` of the base reconstruction
  loss at initialization (D-04).
- **Seeds:** `(20260813, 20260814, 20260815)`, model seed only; fixture seed fixed at `20260816`
  (D-03).
- **Cells:** 3 baseline + 18 single-mode + 3 combination = **24**.
- **Epoch budget:** the largest of `(100, 75, 50, 40, 30, 25, 20)` whose projection fits
  `LADDER_BUDGET_S = 28800` (8.0 h). D-06's 8–12 h envelope, less the anchor cell's measured
  ~2.6 h; worst-case phase total ≈ 10.6 h.
- **Early stopping:** disabled for every cell, inherited via `pu._converge_cfg`.
- **Projected total to beat, from sealed numbers:** curvature evaluation `24 × 598.8 ≈ 3.99 h`
  at `LADDER_EVAL_N = 2000`, plus training at a per-mode multiplier this plan measures.
- **Relief valves, applied automatically in order:** F1 rungs 3→2 · F2 `LADDER_EVAL_N` 2000→1000
  · F3 `CHRISTOFFEL_MAX_ROWS_PER_CHART` 8→4 (`christoffel` arm only). **F1–F3 exhausted without
  fitting → `BUDGET NOT MET`, exit 1, halt to Task 3's checkpoint.**
- **F4 is not a fourth fallback. It is an override of D-07**, which names cutting epochs
  explicitly among the things that are *not* the valve, on a measured reason (`03-08`: training to
  the full budget instead of stopping early cut held-out `mse_per_dim` 62.2% and left `cond(g)`
  unmoved). It is never applied automatically, is presented at Task 3's checkpoint as its own
  separately-worded option beside a co-equal halt, and fires only under the explicit
  `--confirm-f4-override` flag. Full declaration, scope and stated cost: `03.1-01-PLAN.md` §C
  item 4. The **initial** epoch-count choice remains ordinary discretion; only the
  overrun-response cut is the override.
- **The training projection is not closed in plan text** (`03.1-01-PLAN.md` §B): at the smallest
  epoch candidate the full 24-cell set already projects over `LADDER_BUDGET_S` on training alone at
  every plausible per-mode multiplier, before the ~4 h evaluation floor. **A multi-step relief
  cascade is therefore the expected probe outcome, not an edge case.**

</sizing_declared_before_anything_runs>

<artifacts_this_phase_produces>

Newly created by this plan — excluded from drift verification because they do not yet exist:

**New CLI flags on `notebooks/diagnostics/decoder_prior_ladder_run.py`:** `--anchor-check`,
`--probe`, `--probe-epochs`, `--probe-eval-sizes`, `--confirm-f4-override`

**New module-level symbols in `decoder_prior_ladder_run.py`:** `PROBE_EPOCHS`,
`PROBE_EVAL_SIZES`, `ANCHOR_TRAIN_WALLCLOCK_S`, `ANCHOR_CURV_WALLCLOCK_S`, `RELIEF_LADDER`,
`F4_OVERRIDE_LABEL`, `anchor_check`, `probe`, `project_total_s`, `apply_relief_ladder`,
`print_projection_table`

**New JSONL record fields written by `--probe`:** `kind` value `"probe"`, `probe_mode`,
`probe_max_epochs`, `s_per_epoch`, `eval_size`, `curv_s_at_eval_size`, `curv_s_per_row`,
`n_charts_used`, `projected_total_s`, `relief_applied`, `chosen_max_epochs`

**New JSONL record `kind` value written by `--anchor-check`:** `"anchor_cell"`

</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The faithfulness gate — reproduce the sealed row with == , not a tolerance</name>

  <read_first>
    - notebooks/diagnostics/decoder_prior_ladder_run.py (the file being modified, as written by `03.1-01`)
    - notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py (lines 275-310: `anchor_check`, the exact shape to mirror — it raises `ValueError` naming the measured and expected values and calls the mismatch a faithfulness failure rather than a tolerance question)
    - notebooks/diagnostics/synthetic_control_run.py (`run_one_control` lines 358-501 and `run_controls` lines 620-690: the call the anchor must make unchanged, including `seed=CONTROL_FIXTURE_SEED`, `epoch_block=CONTROL_EPOCH_BLOCK`, full-cloud `chart_curvature_field(model, x64, ...)`, and the `config_id` format `control_{name}_nc{n_charts}_d{chart_dim}_ep{max_epochs}`)
    - notebooks/diagnostics/curvature_field_pu_run.py (targeted: `CONVERGE_MAX_EPOCHS = 300` at line 228, `CONVERGE_N_CHARTS = 4` at line 233, `PU_CHART_DIM`, `PU_EMBED_DIM`, `AMBIENT_DIM`)
  </read_first>

  <files>notebooks/diagnostics/decoder_prior_ladder_run.py</files>

  <action>
Add `anchor_check(device, record_path)` and an `--anchor-check` flag.

The anchor cell **calls `synthetic_control_run.run_one_control` unchanged** — it does not go
through this runner's own `run_cell`. That is the point: `run_cell` splits the fixture seed from
the model seed and evaluates on a subsample, so it is deliberately not the sealed code path. The
anchor's job is to prove that this runner's *imports* of `synthetic_control_run`'s fixture,
matched-architecture builder, blocked trainer and metric call sequence resolve to exactly the
same computation the sealed row was produced by. Call it with `"saddle"`, `n=CONTROL_N` (10000),
`chart_dim=pu.PU_CHART_DIM` (20), `ambient=pu.AMBIENT_DIM` (768), `n_charts=pu.CONVERGE_N_CHARTS`
(4), `embed_dim=pu.PU_EMBED_DIM` (40), `max_epochs=300`, `seed=sc.CONTROL_FIXTURE_SEED` (20260816),
`epoch_block=sc.CONTROL_EPOCH_BLOCK` (25).

Wrap the call in `with decoder_priors.decoder_prior_active(model, 0.0, mode)` **only if** doing
so requires no change to `run_one_control` — it does not, because `run_one_control` builds its own
model internally. So do not wrap it at all; record instead, in the function's docstring, the
module-docstring guarantee that `decoder_prior_active` installs nothing at `weight == 0.0`, which
is why the ladder's own `weight=0` cells are structurally the untouched `cae.train_cae` path
(D-05) even though the anchor does not exercise the wrapper.

Compare `rec["fidelity"]["rank_spearman_rho"]` against the module constant
`ANCHOR_RHO_SPEARMAN = -0.015106571347065712` with `!=`, and on mismatch raise `ValueError`
naming both values and stating that this is a faithfulness failure — this runner's fixture,
protocol or metric call sequence has drifted from `synthetic_control_run.py` — **not** a
tolerance question. The comparison is raw float inequality against the module constant and
nothing else; do not substitute any approximate-equality helper from `numpy`, `math` or
`torch.testing`, and do not introduce a tolerance parameter. On success print
`ANCHOR OK: rank_spearman_rho == -0.015106571347065712 exactly.`

Also assert `rec["config_id"] == ANCHOR_CONFIG_ID`, `rec["epochs_run"] == 300`,
`rec["n_charts_used"] == 4`, and `rec["n"] == 10000`, each with its own message — a run that
matched rho but differed on any of these would mean the sealed row is not the row that was
reproduced.

Append the record to the ladder JSONL with `kind = "anchor_cell"` and additionally record
`ANCHOR_TRAIN_WALLCLOCK_S = 6253.836718825012` and `ANCHOR_CURV_WALLCLOCK_S = 2993.912286339997`
as module constants beside the fresh `train_wallclock_s` and `curv_wallclock_s` this run measured,
plus their ratios. Those two ratios are the machine-speed correction the probe's projection
applies to the sealed reference numbers, so they must be captured here rather than assumed to be
1.0. Print both ratios explicitly.

This is also the one full-cloud (`n=10000`) evaluation this phase runs, so its record is the
single full-cloud row carrying CURV-04's `lambda_min` / `lambda_max` / `det_g` / `log10_det_g`
fields at the sealed protocol. Print those four medians on the anchor row so the sealed fit's
absolute metric scale is on record — Phase 3's own records store only the ratio and cannot be
re-audited for collapse, which is exactly the limitation D-15 exists to stop repeating.

**Runtime note for the executor:** this task costs roughly 2.6 hours. Launch it in the
background with output tee'd to a log file, poll the log, and do not run anything else
CPU-heavy while it runs.
  </action>

  <verify>
    <automated>.venv/bin/python notebooks/diagnostics/decoder_prior_ladder_run.py --anchor-check 2>&1 | tee /tmp/03_1_anchor.log; grep -q "ANCHOR OK: rank_spearman_rho == -0.015106571347065712 exactly." /tmp/03_1_anchor.log && grep -q "lambda_min" /tmp/03_1_anchor.log && git diff --quiet notebooks/diagnostics/synthetic_control_run.py notebooks/pu_manifold/cae.py && echo ANCHOR_GATE_OK</automated>
  </verify>

  <acceptance_criteria>
    - `/tmp/03_1_anchor.log` contains the exact line `ANCHOR OK: rank_spearman_rho == -0.015106571347065712 exactly.`
    - `grep -c "!= ANCHOR_RHO_SPEARMAN" notebooks/diagnostics/decoder_prior_ladder_run.py` returns at least 1 — the gate is raw float inequality against the module constant
    - `.venv/bin/python -c "import inspect,sys;sys.path.insert(0,'notebooks/diagnostics');sys.path.insert(0,'notebooks');import decoder_prior_ladder_run as r;s=inspect.getsource(r.anchor_check);print(any(tok in s for tok in ('isclose','assert_close','allclose','rtol','atol','tolerance')))"` prints `False` — no approximate-equality helper or tolerance parameter appears anywhere in `anchor_check`
    - The anchor record appended to `notebooks/.cache/03.1_decoder_prior_ladder.jsonl` has `kind == "anchor_cell"`, `config_id == "control_saddle_nc4_d20_ep300"`, `epochs_run == 300`, `n == 10000`, `n_charts_used == 4`
    - The same record carries non-null `lambda_min`, `lambda_max`, `det_g` and `log10_det_g` distribution summaries, each with a `median` key — the one full-cloud CURV-04 row this phase produces
    - The log prints both machine-speed ratios: measured `train_wallclock_s` over `6253.836718825012`, and measured `curv_wallclock_s` over `2993.912286339997`
    - `git diff --quiet notebooks/diagnostics/synthetic_control_run.py notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py notebooks/pu_manifold/cae.py` exits 0
  </acceptance_criteria>

  <reversibility rating="reversible">The anchor is a read-only proof against a sealed record; it changes no decision and can be re-run at its own cost.</reversibility>

  <done>The new runner reproduces the sealed row's `rank_spearman_rho` exactly with `==`, the sealed fit's absolute metric scale is on record for the first time, and the machine-speed correction ratios are measured rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 2: Two-sided cost probe — training per mode, and curvature evaluation per row</name>

  <read_first>
    - notebooks/diagnostics/decoder_prior_ladder_run.py (the file being modified, including `calibrate_weights` and `run_cell` from `03.1-01`)
    - notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py (lines 314-359: `probe`, the shape to mirror — two small-epoch cells, seconds-per-epoch, projection across candidate epoch counts, `sys.exit(1)` with `BUDGET NOT MET` if none fits. This probe measured training cost only, which is the exact gap this task closes.)
    - notebooks/pu_manifold/decoder_priors.py (`christoffel_penalty` lines 272-305: it subsamples to `max_rows_per_chart` rows per chart BEFORE computing the Jacobian and Hessian, so its cost is bounded by `n_charts_used × CHRISTOFFEL_MAX_ROWS_PER_CHART` and is independent of batch size; `isometry_penalty` lines 310-360: it loops over EVERY batch row assigned to each chart with no truncation, so its cost scales with `pu.BATCH` × `chart_dim` and `CHRISTOFFEL_MAX_ROWS_PER_CHART` does nothing for it)
    - notebooks/pu_manifold/chart_curvature.py (`chart_curvature_field`'s per-chart, per-`batch_size` loop and `VMAP_CHUNK = 32`, which is what makes evaluation cost a function of row count and `n_charts_used`)
  </read_first>

  <files>notebooks/diagnostics/decoder_prior_ladder_run.py</files>

  <action>
Add `probe(device, record_path)` and a `--probe` flag measuring **both** halves of the cost, at
the real ladder architecture (`n=10000`, `chart_dim=20`, `ambient=768`, `n_charts=4`,
`embed_dim=40`, `epoch_block=25`) and never on the Swiss roll.

**Half A — training cost per mode.** Run three short cells at `PROBE_EPOCHS = 3` and
`model_seed = LADDER_SEEDS[0]`: one at `weight=0.0`, one at the `scale` weight
`calibrate_weights` derives for the largest declared fraction `1.0`, one at the `christoffel`
weight for the same fraction. Report `s_per_epoch` for each as `train_wallclock_s / epochs_run`,
and print the two per-mode multipliers against the `weight=0` figure. **Every number here is
measured fresh at this architecture. Do not hard-code, import or restate the halted spike's own
per-epoch figures** (recorded in `03-NOTE-isometry-prior-spike.md` §3) — they were measured at
`chart_dim=2` on the Swiss roll, and `torch.func.jacfwd`'s cost scales with the number of tangent
directions it differentiates, so the real multiplier at `chart_dim=20` is very likely larger
(research assumption A1). The probe exists to replace that extrapolation with a measurement.

Use the largest fraction because it is the worst case: the projection must hold for the most
expensive rung, and a probe at the cheapest rung would under-count.

**Half B — curvature evaluation cost per row.** On the `weight=0` probe model, and on that one
model only so `n_charts_used` is held constant, call `chart_curvature.chart_curvature_field`
three times at `PROBE_EVAL_SIZES = (1000, 2000, 4000)` rows taken from the head of the
`pu._split` holdout index array, timing each into `curv_s_at_eval_size`. Print
`curv_s_per_row = curv_s_at_eval_size / eval_size` for each and the implied `n=10000`
extrapolation. **That extrapolation is the test of research assumption A2** — evaluation cost
being roughly linear in row count is unverified, and the 598.8 s/cell figure the plan text
projects depends entirely on it. Compare the extrapolated `n=10000` figure against two
independent full-cloud measurements: the sealed `2993.912286339997`, and the fresh full-cloud
number the anchor cell measured in Task 1 (read from the JSONL record with
`kind == "anchor_cell"`). Print all three side by side and the relative disagreement. If
`curv_s_per_row` varies by more than a factor of two across the three sample sizes, print
`EVAL COST IS NOT LINEAR IN ROW COUNT` with the three measurements and **use the largest measured
per-row cost** for the projection rather than the fitted one — being wrong in the conservative
direction is the only acceptable direction here.

**The projection.** Write `project_total_s(max_epochs, fractions, eval_n, s_per_epoch_by_mode,
curv_s_per_row, christoffel_max_rows)` returning the total for the full cell set: 3 baseline
cells at the `weight=0` rate, `len(fractions) × 3` cells per mode at that mode's rate, 3
combination cells at a rate of `s_scale + s_christoffel − s_baseline` (the two penalties are
computed additively inside one patched `chart_loss`, so their costs add and the shared base is
counted once), plus `n_cells × eval_n × curv_s_per_row` for evaluation. Print the full
`(max_epochs, projected_total_s)` table across `LADDER_EPOCH_CANDIDATES` in the same shape the
sweep runner's probe prints it.

**The relief ladder — F1 to F3 automatic, F4 only under a named override.** Write
`apply_relief_ladder(..., confirm_f4_override=False)` walking `RELIEF_LADDER` in the pre-declared
order — F1 rungs 3→2 keeping `(0.01, 1.0)`; F2 `eval_n` 2000→1000; F3 `christoffel_max_rows` 8→4
applied to the `christoffel` arm's rate only. After each step re-run the projection over
`LADDER_EPOCH_CANDIDATES` and stop at the first configuration whose projection is at or under
`LADDER_BUDGET_S = 28800`. Print which valves fired, in order, each with its pre-declared
consequence sentence: F1 costs ladder resolution; F2 costs per-cell sampling noise; F3 costs
error-bar width on the `christoffel` arm specifically **and does nothing for the `scale` arm
because `isometry_penalty` has no row subsampling**.

Expect at least one of these to fire. `03.1-01-PLAN.md` §B's back-of-envelope puts the full
24-cell set over budget on training alone even at the smallest epoch candidate, so a multi-step
cascade is the base case here rather than a surprise; print the projection after every step so the
cascade is legible in the log.

**F4 is an override of D-07, not a fourth fallback, and this function must treat it that way**
(declared in `03.1-01-PLAN.md` §C item 4). D-07 names cutting epochs explicitly among the things
that are not the valve, and its reason is measured: `03-08` found training to the full budget
instead of stopping early cut held-out `mse_per_dim` 62.2% while leaving `cond(g)` unmoved, so
under-training costs reconstruction — the very axis D-09's bias guard reads — and buys nothing on
the metric. Concretely:
  - When `confirm_f4_override` is false, which is the default, `apply_relief_ladder` **stops after
    F3** and never reduces any epoch count.
  - If F1–F3 did not fit, compute the F4 configuration anyway and print its projection under the
    heading `F4 AVAILABLE BUT NOT APPLIED — OVERRIDES D-07`, naming the epoch candidate F4 would
    drop the `scale` arm to and both halves of its cost: the D-07 under-training confound on the
    `scale` arm, and arm-to-arm epoch incomparability. Then print `BUDGET NOT MET` and
    `sys.exit(1)`.
  - Only a re-invocation carrying the explicit `--confirm-f4-override` flag applies F4. When it
    does, append `F4_OVERRIDE_LABEL` — the module constant holding the literal
    `F4 (D-07 OVERRIDE: epoch cut)` — to `relief_applied` on the probe summary record, so the
    override travels into `03.1-04`'s per-cell records and `03.1-05`'s write-up rather than being
    lost between the checkpoint and the report.
  - Do not default the flag to true, do not infer the override from any other flag or from the
    projection being close, and do not add a second boolean beside `relief_applied` to carry it.

Exiting 1 with `BUDGET NOT MET` after F1–F3 is the predecessor's own outcome and it is a
legitimate one — it halts to an explicit user decision (D-11) rather than silently cutting scope,
and, because F4 is an override rather than a fallback, rather than silently cutting epochs either.

Append one `kind = "probe"` record per measurement to the JSONL, plus one summary record
carrying `projected_total_s`, `relief_applied` and `chosen_max_epochs`, so the sizing decision is
reconstructable from the record file alone.

**Runtime note for the executor:** Half A costs roughly `3 × 3 epochs` of training at the real
architecture and Half B roughly 35 minutes of curvature evaluation. Launch in the background with
output tee'd to a log, poll the log, and run nothing else CPU-heavy concurrently — a co-scheduled
job contaminates every seconds-per-epoch number this task produces.
  </action>

  <verify>
    <automated>.venv/bin/python notebooks/diagnostics/decoder_prior_ladder_run.py --probe 2>&1 | tee /tmp/03_1_probe.log; grep -q "s_per_epoch" /tmp/03_1_probe.log && grep -q "curv_s_per_row" /tmp/03_1_probe.log && grep -qE "chosen_max_epochs|BUDGET NOT MET" /tmp/03_1_probe.log && grep -q "2993.912286339997" /tmp/03_1_probe.log && .venv/bin/python -c "import json;rs=[json.loads(l) for l in open('notebooks/.cache/03.1_decoder_prior_ladder.jsonl') if json.loads(l).get('kind')=='probe'];assert len(rs)>=6, len(rs);print(len(rs),'probe records')" && echo PROBE_OK</automated>
  </verify>

  <acceptance_criteria>
    - `/tmp/03_1_probe.log` contains three distinct `s_per_epoch` lines labelled with the modes `weight=0`, `scale`, `christoffel`, each with a numeric value, and the two multipliers against the `weight=0` rate
    - `/tmp/03_1_probe.log` contains three `curv_s_per_row` lines for eval sizes `1000`, `2000` and `4000`, plus the extrapolated `n=10000` figure, plus the sealed `2993.912286339997` and the anchor cell's own fresh full-cloud number, and the relative disagreement between them
    - `/tmp/03_1_probe.log` contains a projection table with one line per candidate in `(100, 75, 50, 40, 30, 25, 20)` showing `projected_total_s`
    - `/tmp/03_1_probe.log` ends with either a `chosen_max_epochs` line naming the selected epoch budget and any fired fallbacks in order, or `BUDGET NOT MET` followed by exit code 1
    - `notebooks/.cache/03.1_decoder_prior_ladder.jsonl` contains at least 6 records with `kind == "probe"` (3 training, 3 evaluation) plus one summary record carrying `projected_total_s`, `relief_applied` and `chosen_max_epochs`
    - `grep -c "3\.685\|10\.699" notebooks/diagnostics/decoder_prior_ladder_run.py` returns 0 — the halted spike's `chart_dim=2` roll per-epoch figures appear nowhere in the runner, so no `d=20` projection is built on them
    - Each of the three `s_per_epoch` values in `/tmp/03_1_probe.log` differs from every other, and the `scale` and `christoffel` values are each strictly greater than the `weight=0` value — a probe that reported identical rates would mean the prior was not installed
    - `.venv/bin/python -c "import inspect,sys;sys.path.insert(0,'notebooks/diagnostics');sys.path.insert(0,'notebooks');import decoder_prior_ladder_run as r;p=inspect.signature(r.apply_relief_ladder).parameters;print('confirm_f4_override' in p and p['confirm_f4_override'].default is False)"` prints `True` — F4 is gated behind a parameter that defaults to off
    - If `/tmp/03_1_probe.log` contains `BUDGET NOT MET`, it also contains the heading `F4 AVAILABLE BUT NOT APPLIED — OVERRIDES D-07` with the epoch candidate the `scale` arm would drop to and both halves of the stated cost, and no `chosen_max_epochs` line appears in that same run
    - `.venv/bin/python -c "import json;rs=[json.loads(l) for l in open('notebooks/.cache/03.1_decoder_prior_ladder.jsonl')];print(sorted({s.split()[0] for r in rs for s in r.get('relief_applied',[])}))"` prints a list drawn only from `['F1', 'F2', 'F3']` for any run not carrying `--confirm-f4-override` — the override cannot reach a record by any other path
    - `git diff --quiet notebooks/diagnostics/synthetic_control_run.py notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py notebooks/pu_manifold/cae.py notebooks/pu_manifold/decoder_priors.py` exits 0
  </acceptance_criteria>

  <reversibility rating="reversible">The probe measures and projects; it commits no compute to the ladder and its record can be discarded and re-measured.</reversibility>

  <done>Training cost is measured per mode at the real architecture, curvature-evaluation cost is measured at three sample sizes on one model and cross-checked against two independent full-cloud numbers, the 24-cell projection is built from measurements only, and either a configuration fitting the 8-hour budget is selected or the run halts with `BUDGET NOT MET`.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Ratify the run configuration before the overnight ladder launches</name>

  <decision>
Launch the ladder at the probe-selected configuration, apply a further pre-declared fallback
(F1/F2/F3), confirm the F4 override of D-07, or halt?
  </decision>

  <context>
CONTEXT.md's Claude's-Discretion list asks explicitly whether the timing probe is its own
blocking checkpoint before the overnight run launches. It is — because D-06 is rated **costly**
(the ladder, seed count and epoch budget all size off it) and because the predecessor spent its
entire spike and produced zero measurements by discovering its budget problem too late.

Present, from `/tmp/03_1_probe.log` and the JSONL probe records, in this order:

1. **Anchor result** — `ANCHOR OK` line, and the two machine-speed correction ratios (measured
   train and curvature wall clock over the sealed `6253.836718825012` and `2993.912286339997`).
2. **Measured training cost** — `s_per_epoch` at `weight=0`, at the calibrated `scale` weight and
   at the calibrated `christoffel` weight, and the two multipliers. State plainly whether the
   `chart_dim=20` multiplier came out larger than the halted spike's `~2.9×` at `chart_dim=2`,
   which is what research assumption A1 predicted.
3. **Measured evaluation cost** — `curv_s_per_row` at 1000, 2000 and 4000 rows, the `n=10000`
   extrapolation, and its disagreement with both full-cloud measurements. State plainly whether
   assumption A2's linearity held.
4. **The projection table** across all seven epoch candidates.
5. **Which relief valves fired**, in order, each with its pre-declared consequence: F1 costs
   ladder resolution; F2 costs per-cell sampling noise; F3 costs error-bar width on the
   `christoffel` arm only and does nothing for `scale`. **State plainly that a multi-step cascade
   is the expected outcome rather than an edge case** — `03.1-01-PLAN.md` §B's back-of-envelope
   puts the full 24-cell set over `LADDER_BUDGET_S` on training alone even at the smallest epoch
   candidate, at every plausible per-mode multiplier, so F1 and F2 firing is the base case. The
   human reading this checkpoint should not be surprised to see two or three valves already spent.
6. **Whether F4 is on the table, and what firing it would mean.** F4 is **not** a fourth
   fallback: it is an **override of D-07**, which names cutting epochs explicitly among the things
   that are not the valve. If F1–F3 did not fit, present the probe log's
   `F4 AVAILABLE BUT NOT APPLIED — OVERRIDES D-07` block in full: the epoch candidate the `scale`
   arm would drop to, and **both halves of the cost** — the D-07 under-training confound (`03-08`
   found training to the full budget instead of stopping early cut held-out `mse_per_dim` 62.2%
   and left `cond(g)` unmoved, so the cut costs reconstruction, the axis D-09's bias guard reads,
   and buys nothing on the metric) and arm-to-arm epoch incomparability. Say explicitly that
   confirming it crosses a locked decision and that `BUDGET NOT MET` is a co-equal choice here,
   not a lesser one. If F1–F3 did fit, say so and state that F4 is not on the table.
7. **The selected configuration**: `max_epochs`, `fractions`, `eval_n`, `christoffel_max_rows`,
   cell count, and `projected_total_s` against `LADDER_BUDGET_S = 28800` and against D-06's 8–12 h
   envelope with the anchor's ~2.6 h added.
8. **The interpretive consequence at the selected epoch budget**: ladder cells are trained far
   short of the sealed 300-epoch protocol, so per D-05 they are compared only against this
   phase's own freshly re-fit `weight=0` cells at the same budget and may never be quoted against
   Phase 3's sealed `d=20` row. Say whether the selected budget is low enough that the
   baseline-pathology precondition (plan `03.1-01` §G) is a live risk — if the `weight=0`
   baseline at this budget turns out healthy, the ladder has nothing to fix and the run halts.
  </context>

  <options>
    <option id="launch-as-selected">
      <name>Launch the ladder at the probe-selected configuration</name>
      <pros>The projection fits the pre-declared budget using only measured numbers and only pre-declared levers. The phase produces measurements, which is the outcome D-06 and D-07 exist to secure.</pros>
      <cons>Whatever the selected epoch budget is, it is far below the sealed 300-epoch protocol; the interpretive limitation in item 7 travels with every number the phase reports.</cons>
    </option>
    <option id="apply-further-fallback">
      <name>Apply the next pre-declared relief valve — F1, F2 or F3 only — and re-project</name>
      <pros>Buys a larger epoch budget, which reduces the risk that the baseline is not yet pathological. Every step is pre-declared and none of them touches a fenced lever, so there is no redesign under time pressure and no locked decision is crossed.</pros>
      <cons>Each step costs what it was declared to cost: ladder resolution (F1), per-cell sampling noise (F2), or error-bar width on the `christoffel` arm alone (F3, which does nothing for `scale` because `isometry_penalty` has no row subsampling). **This option never includes F4** — F4 is an override and is its own option below.</cons>
    </option>
    <option id="confirm-f4-d07-override">
      <name>F4 overrides D-07's fence against cutting epochs — confirm the override</name>
      <pros>The only lever left when the `scale` arm alone overruns, where F3 structurally cannot help. Choosing it is what stops this phase repeating its predecessor's outcome of tested infrastructure and zero measurement. D-11 already establishes that escalation past a locked decision is a legitimate move when it is an explicit user decision — which selecting this option is.</pros>
      <cons>**This crosses a locked decision, and the cost is measured rather than hypothetical.** D-07 names cutting epochs explicitly as not the valve because `03-08` found training to the full budget instead of stopping early cut held-out `mse_per_dim` 62.2% while leaving `cond(g)` unmoved — so the cut costs reconstruction, the axis D-09's bias guard reads, and buys nothing on the metric. It also makes the two arms incomparable on epoch count. Confirming it re-runs the probe with `--confirm-f4-override`, stamps `F4 (D-07 OVERRIDE: epoch cut)` into `relief_applied` on every affected cell, makes `--summary` print a `D-07 OVERRIDE FIRED` banner ahead of any Tier-1 or Tier-2 number, and obliges `03.1-FINDINGS.md` to state the override and its confound beside every number those cells produced.</cons>
    </option>
    <option id="raise-envelope">
      <name>Raise D-06's envelope</name>
      <pros>Keeps the ladder at a defensible epoch budget without spending any relief valve.</pros>
      <cons>D-06 is a locked decision rated costly; changing it is a user decision, not a planning one, and it is the decision the predecessor's failure is attributed to.</cons>
    </option>
    <option id="halt">
      <name>Halt and report `BUDGET NOT MET`</name>
      <pros>Honest. `03-NOTE-isometry-prior-spike.md` established the precedent that halting at a pre-declared budget gate is a legitimate outcome. This is a **co-equal** choice beside the F4 override, not a lesser one: declining to cross D-07 is always available and needs no justification.</pros>
      <cons>Repeats the predecessor's outcome — infrastructure and no measurement — and leaves Phase 4 blocked with no route out (D-11).</cons>
    </option>
  </options>

  <resume-signal>Select: launch-as-selected, apply-further-fallback, confirm-f4-d07-override, raise-envelope, or halt</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process → local filesystem | Append-only JSONL under the gitignored `notebooks/.cache/`. No network, no auth, no untrusted input path. This plan reads one sealed record file and appends to one new one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-05 | Tampering | the exact-equality anchor comparison in `anchor_check` | high | mitigate | The single load-bearing correctness gate in this plan. Softening `!=` to `np.isclose` would let a drifted fixture, protocol or metric call sequence pass as faithful and every ladder number downstream would be uninterpretable. Mitigated by an acceptance criterion asserting no `isclose`/`assert_close` appears inside `anchor_check`, and by the literal `ANCHOR OK` line being grep-matched in the automated verify. |
| T-03.1-06 | Repudiation | probe measurements contaminated by a co-scheduled CPU job | medium | mitigate | An unrecorded contaminated measurement would silently size the entire 8-hour run wrong. Mitigated by the objective's explicit no-co-scheduling instruction, by the plan's wave ordering placing nothing else in wave 3, and by the two machine-speed correction ratios in Task 1 which would show an implausible value if the machine were loaded. |
| T-03.1-12 | Elevation of Privilege | `apply_relief_ladder`'s F4 step | high | mitigate | F4 cuts epochs, which D-07 fences off explicitly; applying it automatically would let a planning-time function cross a locked user decision under time pressure. Mitigated by `confirm_f4_override` defaulting to false, by F4 being unreachable except through the `--confirm-f4-override` flag, by an acceptance criterion asserting that default, by a second asserting no record carries an F4 entry in `relief_applied` on a run without the flag, and by the Task 3 checkpoint presenting the override as its own separately-worded option beside a co-equal halt. |
| T-03.1-02 | Information Disclosure | `notebooks/.cache/03.1_decoder_prior_ladder.jsonl` | low | accept | Gitignored, local, non-secret; synthetic-fixture-derived numbers and timings only. Same pattern as every prior phase's record. |
| T-03.1-03 | Tampering | `cae.py`, `synthetic_control_run.py`, `swiss_roll_isometry_prior_sweep_run.py` (sealed / additive-only) | medium | mitigate | `git diff --quiet` on all three exits 0 is an acceptance criterion on both code tasks. |
| T-03.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase; every import is an existing, verified project dependency. `03.1-RESEARCH.md`'s Package Legitimacy Audit records the gate as not applicable, so no `[ASSUMED]`/`[SUS]` package exists and no legitimacy checkpoint fires. |

**Honest assessment:** the two threats worth writing down here are not security threats in the
usual sense — they are integrity threats to the measurement (a softened anchor, a contaminated
timing). This code has no network surface, no auth, no user-input path and no persistence beyond
a gitignored local file, which is the finding already recorded for this codebase in
`02.6-REVIEW.md`.
</threat_model>

<verification>
- `/tmp/03_1_anchor.log` contains `ANCHOR OK: rank_spearman_rho == -0.015106571347065712 exactly.`
- `/tmp/03_1_probe.log` contains the per-mode `s_per_epoch` lines, the three `curv_s_per_row` lines, the projection table, and either `chosen_max_epochs` or `BUDGET NOT MET`
- `notebooks/.cache/03.1_decoder_prior_ladder.jsonl` contains one `kind == "anchor_cell"` record and at least 7 `kind == "probe"` records
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` still exits 0
- `git diff --quiet notebooks/pu_manifold/cae.py notebooks/pu_manifold/decoder_priors.py notebooks/diagnostics/synthetic_control_run.py notebooks/diagnostics/swiss_roll_isometry_prior_sweep_run.py` exits 0
- `git status --porcelain src/effdim/` is empty
</verification>

<success_criteria>
- The new runner reproduces the sealed row exactly with `==`, and the comparison is never softened to a tolerance
- The sealed `d=20` saddle fit's absolute metric scale (`λ_min`, `λ_max`, `det(g)`, `log10 det(g)`) is on record for the first time
- Training cost is measured per mode at `chart_dim=20`, replacing the halted spike's `chart_dim=2` extrapolation
- Curvature-evaluation cost is measured at three sample sizes and cross-checked against two independent full-cloud numbers, so research assumption A2 is verified rather than trusted
- The 24-cell projection uses measured numbers only, and any relief applied automatically is one of the three pre-declared valves F1–F3 in the pre-declared order with its stated consequence printed
- F4 is never applied automatically: it is printed as an unapplied override of D-07 when F1–F3 do not fit, and it fires only after a named user override confirmed at the Task 3 checkpoint, recorded in `relief_applied`
- A human ratifies the run configuration before the overnight ladder launches
</success_criteria>

<output>
Create `.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-03-SUMMARY.md` when done
</output>
