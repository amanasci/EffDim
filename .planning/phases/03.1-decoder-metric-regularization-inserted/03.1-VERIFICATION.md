---
phase: 03.1-decoder-metric-regularization-inserted
verified: 2026-08-22T00:40:06Z
status: human_needed
score: 8/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 03.1: Decoder Metric Regularization (INSERTED) Verification Report

**Phase Goal:** Make the CAE produce a decoder parameterization whose pullback metric is usable
for curvature — well-scaled and well-conditioned — and measure the effect on a fixture with known
analytic curvature, so the answer is not another unfalsifiable field.

**Verified:** 2026-08-22
**Status:** human_needed
**Re-verification:** No — initial verification

## Note on what "goal achieved" means here

This is a measurement phase. "Achieved" does not mean the priors fixed the curvature estimate —
it means the phase produced an interpretable, falsifiable measurement under rules it declared
before the numbers existed, and reported the result (positive, negative, or partial) honestly.
Judged on that standard, this phase clears the bar. All quantitative claims in `03.1-FINDINGS.md`
were independently re-derived from the raw `notebooks/.cache/03.1_decoder_prior_ladder.jsonl`
records below — they are not narration, they trace to real per-cell data.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Effect measured on the fixture with known analytic curvature (D-01, `d=20` saddle only) | ✓ VERIFIED | `notebooks/.cache/03.1_decoder_prior_ladder.jsonl` has 1 `anchor_cell` + 14 `probe` + 18 `ladder_cell` (15 single-mode + 3 combination) records, all `fixture: "saddle"`, `chart_dim: 20`. No sphere record exists — matches D-01's declared scope, not the predecessor's zero-measurement outcome. |
| 2 | `scale`'s Tier-1 (mechanism) claim is supported by real data, scored against a rule declared before cells ran | ✓ VERIFIED | Independently recomputed from JSONL: baseline `log10_det_g` medians `[-84.17, -83.90, -82.51]`, top-rung `scale` medians `[0.0064, 0.0373, 0.0822]` — matches FINDINGS §4 exactly. `cond(g)` `1.716e8 → 5.726e2` also matches. `03.1-01-PLAN.md` §D/§E and `03.1-03-SUMMARY.md`'s reading rule predate Task 2's real cells (Tier-1/Tier-2 read-out code was proven against a *synthetic* record in `03.1-04` Task 1, before any real cell trained — see `03.1-04-SUMMARY.md`). |
| 3 | Answer is falsifiable, not an unfalsifiable field (D-13 exact-`==` anchor) | ✓ VERIFIED | Anchor record's `fidelity.rank_spearman_rho == -0.015106571347065712` exactly (read directly from JSONL) — matches `ANCHOR_RHO_SPEARMAN` module constant and CONTEXT D-13/D-01's cited value bit-for-bit. Code compares with `!=`, never `isclose` (`decoder_prior_ladder_run.py` Decisions Made, confirmed by grep — no `isclose`/`allclose` near the anchor check). |
| 4 | Reporting-unit rules honoured: D-03 (3 seeds, median+spread), D-09 (held-out reconstruction beside every cell), D-02 (POST-HOC label on every combination table) | ✓ VERIFIED | Every group in the JSONL has exactly 3 seed records; FINDINGS prints median and full spread throughout. `reconstruction.mse_per_dim` present on every ladder/combination record. Combination records carry a `selection_rule` field stating the post-hoc justification verbatim; `03.1-05-SUMMARY.md` states the post-render audit found the label on 3/3 tables. |
| 5 | D-11 compliance: null/partial reported plainly, no scope-widening, no pre-committed conclusion; Phase 4 left blocked with no route out proposed as the phase's own conclusion | ✓ VERIFIED | FINDINGS §10 states "Phase 4 stays blocked, with no proposed route out" and explicitly separates the developer-directed spike ("recorded as a decision rather than a phase conclusion... This document does not design the spike") from the phase's own findings. ROADMAP's Phase 4 dependency note mirrors this. No sphere fixture, no `d` sweep, no new weight was added mid-phase to soften the result. |
| 6 | D-14 Swiss roll check exists, executed with outputs, covers all three modes, honours CLAUDE.md's "must not" list | ✓ VERIFIED | `notebooks/03.1_swiss_roll_decoder_prior_check.ipynb` exists: 15 cells (8 code, all with outputs), imports `cae`/`decoder_priors`/`chart_curvature` unchanged, uses `make_swiss_roll`, trains baseline/scale/christoffel/combo + `PlainAutoEncoder` fresh, plots 3-D + x-z scatter, prints exactly the 4 pass/fail lines + read-out quoted in FINDINGS §7 (re-read from the notebook's own cell 14 output — verbatim match). Zero occurrences of `.cache`, `json.dump`, `PREREGISTRATION` in its source. |
| 7 | CURV-04 closure is real: four fields shipped, pinned by tests, reverse path bit-identical, standing limitation recorded | ✓ VERIFIED | `chart_curvature.py` lines 336-489 show `lambda_min`/`lambda_max`/`det_g`/`log10_det_g` added alongside an untouched `metric_condition_number`, from one reused `eigvalsh`. Ran the three new tests plus the two pre-existing bit-identity regressions directly: `6 passed`. `REQUIREMENTS.md` line 66/215 marks CURV-04 Closed with the standing limitation stated (Phase 3 grids not re-run). |
| 8 | Limitations honestly carried, in particular the 20-vs-300-epoch confound | ✓ VERIFIED | FINDINGS §9 limitation 1 states this as "the single biggest threat to the headline, first-class, not a footnote," with the exact numbers (`-83.9` vs sealed `-22.78`) reproduced from the anchor record. Nine further limitations listed, cross-checked against JSONL (F1 rung cut, F2 eval_n=1000, F4 non-firing across all 18 records verified directly: 0 records carry F4 in `relief_applied`, all have `epochs_run==20` and `early_stopped==False`). |
| 9 | Plan-level `verification: backstop` truths — the read-out "reads as the developer expects" / findings state what the data shows | ⚠️ Cannot be certified by static evidence alone | Both `03.1-04-PLAN.md` and `03.1-05-PLAN.md` declare one must-have truth each with `verification: backstop` — explicitly non-inferable from grep/code inspection per the honest-verifier contract. Routed to human verification below rather than marked VERIFIED on inference. |

**Score:** 8/9 truths verified (1 routed to human verification per its own declared `backstop` tier — not a failure, an abstention).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `notebooks/pu_manifold/chart_curvature.py` | D-15 fields added, reverse path unchanged | ✓ VERIFIED | Fields present (lines 336-489); 3 new + 2 pre-existing bit-identity tests pass |
| `notebooks/diagnostics/decoder_prior_ladder_run.py` | D-13 runner: anchor, probe, ladder, combination, CLI surface | ✓ VERIFIED | All CLI flags present and wired (`--dry-run`, `--smoke`, `--anchor-check`, `--probe`, `--confirm-f4-override`, `--summary`, `--run-ladder`, `--combination`); F4 gate confirmed unreachable by default |
| `notebooks/.cache/03.1_decoder_prior_ladder.jsonl` | 33 records (1 anchor + 14 probe + 18 ladder/combination) | ✓ VERIFIED | Read directly; record count and structure match FINDINGS' cited numbers exactly, cell-by-cell |
| `notebooks/03.1_swiss_roll_decoder_prior_check.ipynb` | D-14 mandatory Swiss roll check, executed | ✓ VERIFIED | 15 cells, 8 code with outputs, matches CLAUDE.md's shape and D-14's scope |
| `notebooks/diagnostics/check_swiss_roll_contract.py` | Contract checker referenced by plan 02's verify command | ✓ VERIFIED | File exists |
| `notebooks/pu_manifold/tests/test_curvature_probe.py` | 3 new tests pinning D-15 fields + collapse-blindness | ✓ VERIFIED | Ran targeted: 6 passed (3 new + 2 pre-existing regression + the reverse-mode bit-identity test) |
| `notebooks/pu_manifold/tests/test_decoder_priors.py` | 2 new nesting tests (D-12) | ✓ VERIFIED | Ran targeted: 2 passed |
| `.planning/phases/.../03.1-FINDINGS.md` | 10-section phase record | ✓ VERIFIED | All 10 sections present; every quoted number cross-checked against raw JSONL and found to match |
| `.planning/REQUIREMENTS.md` | CURV-04 closure, additive edit | ✓ VERIFIED | Closure paragraph present, prior "Reopened"/"Amendment" text left intact |
| `.planning/ROADMAP.md` | Phase 03.1 outcome recorded, Phase 4 gating note updated | ✓ VERIFIED | Outcome paragraph and Phase 4 dependency note both present and consistent with FINDINGS |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `chart_curvature`'s `lambda_min`/`lambda_max`/`log10_det_g`/`metric_condition_number` | Ladder's per-cell distribution summaries | direct field read | ✓ WIRED | Confirmed present on every ladder/anchor record read from JSONL |
| Anchor cell's `rank_spearman_rho` | `ANCHOR_RHO_SPEARMAN` module constant | exact `==` comparison | ✓ WIRED | Constant `-0.015106571347065712` matches JSONL record's value bit-for-bit |
| Ladder's Tier-1 verdicts | Combination cell's two weights | `select_best_rung` per pre-declared rule | ✓ WIRED | Combination records' `selection_rule` field states the exact pre-declared rule text, including christoffel's Tier-1-only justification |
| `03.1-FINDINGS.md`'s outcome | `ROADMAP.md` Phase 03.1 checkbox / Phase 4 dependency note | manual doc edit | ✓ WIRED | Numbers and framing (`rho -0.122 → +0.116`, "necessary but not sufficient") consistent across both documents |
| Swiss roll notebook | `pu_manifold.decoder_priors`/`cae`/`chart_curvature` | `import ... unchanged` | ✓ WIRED | Cell 1 imports the real modules and prints their `__file__` paths (no reimplementation) |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CURV-04 | 03.1-01..05 | Absolute-scale metric fields shipped, closed | ✓ SATISFIED | Fields shipped and tested; `REQUIREMENTS.md` closure paragraph accurate against code |

No orphaned requirements found — only CURV-04 maps to Phase 03.1 in `REQUIREMENTS.md`.

### Anti-Patterns Found

None. Grepped `notebooks/diagnostics/decoder_prior_ladder_run.py`, `notebooks/pu_manifold/chart_curvature.py`, and both modified test files for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER|not yet implemented|coming soon` — zero matches.

### Behavioral Spot-Checks / Data-Flow Trace

Re-derived, from the raw JSONL cache (not from FINDINGS' own prose), the full set of headline numbers: anchor `rank_spearman_rho`, anchor's `lambda_min`/`lambda_max`/`det_g`/`log10_det_g`, baseline and both `scale` rungs' `log10_det_g`/`cond`/`rho`/`lambda_min`/`lambda_max`, both `christoffel` rungs' same fields, and the combination cell's same fields. Every one matched `03.1-FINDINGS.md`'s tables to the reported precision. This is strong evidence the findings document is a faithful transcription of real measured data, not a plausible-sounding narrative.

Ran targeted test subsets (not the full suite, per the orchestrator's already-completed full-suite pass): `test_curvature_probe.py -k "lambda or det_g or blind or reassembles_in_row_order or forward_mode_matches_reverse"` → 6 passed; `test_decoder_priors.py -k nest` → 2 passed.

Did not re-run `--anchor-check` (measured 8562.7s / ~2.4h in the sealed record) or the full ladder — re-running is disproportionate to what a fresh JSONL read already proves, and the no-co-scheduling requirement (D-06 wave-parallelism constraint) makes an unplanned re-run itself a risk to any future timing-sensitive work.

### Human Verification Required

1. **`03.1-04-PLAN.md`'s backstop truth — the Tier-1/Tier-2 read-out reads as intended**
   **Test:** Read `--summary`'s printed output (or `03.1-FINDINGS.md` §4-§5, which transcribes it) and confirm the two tiers are visually and substantively separate, with no arithmetic anywhere combining a Tier-1 and Tier-2 number into one score.
   **Expected:** Two clearly separated blocks; the read-out states what the data shows (`scale` mechanism win, Tier-2 partial and non-seed-consistent) without editorializing toward a predetermined conclusion.
   **Why human:** Declared `verification: backstop` in the plan itself — this is a qualitative judgment about whether prose framing is honest, which static inspection cannot certify per the honest-verifier contract.

2. **`03.1-05-PLAN.md`'s backstop truth — a skeptical reader can reach every number in `03.1-FINDINGS.md` from the document alone**
   **Test:** Pick 3-4 headline numbers from FINDINGS (e.g. the `rho` progression, the `cond(g)` halving in combination) and confirm each is traceable to a named `config_id` and JSONL field without needing to re-derive anything.
   **Expected:** Every number is either printed with its source `config_id` or trivially locatable in the JSONL by the labelled quantities.
   **Why human:** Also declared `verification: backstop`. (Note: this verifier independently re-derived the majority of FINDINGS' headline numbers directly from the JSONL and found them to match exactly — see Behavioral Spot-Checks above — which is strong supporting evidence, but the plan's own truth asks about *reader* traceability as a documentation-quality property, which is a judgment call left to the human per its declared tier.)

3. **Swiss roll notebook's visual read (D-14 / CLAUDE.md's own reserved check)**
   **Test:** Open `notebooks/03.1_swiss_roll_decoder_prior_check.ipynb`'s executed x-z scatter cells and confirm the roll's colour bands (by arc-length `t`) stay in order across all four CAE arms.
   **Expected:** Colour bands visibly ordered, not scrambled, for baseline/scale/christoffel/combo.
   **Why human:** `03.1-02-SUMMARY.md`'s own coverage table (`D4`) marks this `human_judgment: true` by design — CLAUDE.md reserves this specific check for a human reading the executed notebook.

## Gaps Summary

No blocking gaps found. The phase delivered exactly what a measurement phase (not an implementation phase) is supposed to deliver: a real 18+3-cell ladder run on the one fixture the locked decisions specified, an exact-equality faithfulness anchor that traces to the sealed Phase 3 protocol, a Tier-1/Tier-2 separation that lets a real mechanism win (`scale`) coexist honestly with a small, non-seed-consistent estimand movement, a mandatory Swiss roll check that discharges a standing obligation from the halted predecessor spike, and a CURV-04 closure that is backed by passing tests rather than by prose alone. Every headline number in `03.1-FINDINGS.md` was independently re-derived from the raw JSONL cache in this verification pass and matched.

The three items above are routed to human verification rather than certified programmatically: two are plan-declared `backstop`-tier truths (qualitative judgments about read-out honesty and reader-traceability, explicitly outside what static inspection can certify), and one is CLAUDE.md's own reserved visual check on the Swiss roll notebook. None of the three represents a missing artifact, broken wiring, or an unresolved locked-decision violation — they are the phase's own declared abstention points, surfaced rather than silently passed.

No locked decision (D-01 through D-16) was found violated. The one place a deviation exists — `chosen_max_epochs = 20` against the anchor's `300`-epoch protocol — is explicitly Claude's-Discretion sizing, not F4's forbidden override, and is carried forward as the phase's own first-class limitation rather than hidden.

---
*Verified: 2026-08-22*
*Verifier: Claude (gsd-verifier)*
