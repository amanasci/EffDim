---
phase: 03.1
slug: decoder-metric-regularization-inserted
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-18
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `03.1-RESEARCH.md` § "Validation Architecture".

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (installed via this project's `dev` extras) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` — `testpaths = ["tests"]` targets the `src/effdim` convention; `notebooks/pu_manifold/tests/` is invoked by explicit path |
| **Quick run command** | `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_decoder_priors.py -q` (23 tests currently collect) |
| **Full suite command** | `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` (313 tests currently collect) |
| **Estimated runtime** | quick ~seconds; full suite ~minutes (CPU, float64) |

---

## Sampling Rate

- **After every task commit:** Run the targeted quick command for the module touched
  (`test_decoder_priors.py` for D-12 work, `test_curvature_probe.py` for D-15 work)
- **After every plan wave:** Run `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q`
- **Before `/gsd-verify-work`:** Full suite green **and** the new runner's `--anchor-check`
  passing — per D-13 the exact-equality anchor is itself a gate, not merely a test
- **Max feedback latency:** quick command < 60s

---

## Per-Task Verification Map

> Task IDs are filled in by the planner; the rows below are the behaviors that must map to at
> least one task each.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | CURV-04 | — | N/A | unit | `pytest notebooks/pu_manifold/tests/test_curvature_probe.py -k "lambda or det_g" -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | CURV-04 (bit-identity) | — | N/A | regression | `pytest notebooks/pu_manifold/tests/test_curvature_probe.py -k "reassembles_in_row_order or forward_mode_matches_reverse" -x` | ✅ | ⬜ pending |
| TBD | TBD | TBD | D-12 nesting | — | N/A | unit | `pytest notebooks/pu_manifold/tests/test_decoder_priors.py -k nest -x` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-13 anchor | — | N/A | integration | `.venv/bin/python notebooks/diagnostics/<new_runner>.py --anchor-check` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | D-14 Swiss roll | — | N/A | manual (executed notebook) | `notebooks/03.1_swiss_roll_decoder_prior_check.ipynb` executed end to end, pass/fail lines printed | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `notebooks/pu_manifold/tests/test_decoder_priors.py` — new test for D-12's nested
      `decoder_prior_active` composition: additive penalties, exact LIFO restore through both
      levels, including exit by exception
- [ ] `notebooks/pu_manifold/tests/test_curvature_probe.py` — new test(s) for D-15's `λ_min`,
      `λ_max`, `det(g)` fields, verified against independent `torch.linalg.eigvalsh` /
      `torch.linalg.cond` / `torch.linalg.det` computation to float64 round-off
- [ ] `notebooks/diagnostics/<new_runner>.py` (D-13) — needs `--dry-run`, `--anchor-check`,
      `--probe` (train-cost **and** curvature-eval-cost, per RESEARCH Pitfall 1), `--resume`,
      `--summary`
- [ ] `notebooks/03.1_swiss_roll_decoder_prior_check.ipynb` (D-14)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Swiss roll prior check reads as a pass | D-14 / CLAUDE.md | Per CLAUDE.md the check is a short, readable notebook — no verdict JSON, no thresholds table | Execute `notebooks/03.1_swiss_roll_decoder_prior_check.ipynb` end to end on CPU; read its three or four printed pass/fail lines and the one-sentence read-out; confirm the x-z scatter shows the roll's colour bands still in order |
| Tier 1 / Tier 2 read-out is reported per D-08 | D-08 / D-10 | The reading rule is prose fixed in the plan before any cell runs; a null is a complete outcome | Confirm the findings document prints both tiers separately, prints median **and** full 3-seed spread (D-03), prints held-out reconstruction beside every cell (D-09), and labels the combination cell post-hoc (D-02) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
