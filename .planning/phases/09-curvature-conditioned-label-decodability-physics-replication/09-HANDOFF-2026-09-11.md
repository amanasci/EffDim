# Handoff, 2026-09-11 evening (supersedes 09-HANDOFF-2026-09-07.md for the manuscript; that file still holds the phase/pod rules)

Read first: `CLAUDE.md`, the pod guide (`docs/remote-compute/…`, digest verified unchanged 2026-09-11),
memory entries `ml4ps-manuscript-framing`, `manuscript-writing-mistakes-to-avoid`,
`physics-probe-facing-instruments-agree`, `probe-facing-curvature-carries-the-effect`.

## What exists now

- **Manuscript** `docs/latex/ml4ps/main.tex` (ML4PS 2026, NeurIPS style, anonymous). Framing: the
  Platonic Representation Hypothesis and the Platonic Universe result; question: where on the
  manifold does cross-survey convergence live. Instrument: pointwise decoder curvature and full II.
  Sections: intro; instrument + validation; curvature vs cross-survey convergence (MKNN/CKA,
  density-explained); what a linear map sees (Eq. 1 Hessian identity, Eq. 2 residual expansion,
  relative II for two embeddings); probe-facing curvature on known surface + galaxies (decoder-only
  Figure 1 and Table 2; quadratic-chart estimator is ONE sentence); discussion tied to PRH.
  No "colleague"/"his" anywhere. `check.sh` enforces language, build, spill.
- **Records** (all sha256 in the supplements): fixture probe-facing `09_fixture_probe_facing.jsonl`;
  Physics probe-facing `09_physics_probe_facing.jsonl` (d=16, 20; both estimators); fixture γ sweeps.
- **Supplements 03–06** in this directory, uncommitted. Runners committed and pushed (`71914dd`).
- **Pod**: `/mnt/ssd-cluster/effdim/probe-facing-out/` holds the record and the per-d geometry npz
  (J, D²F, image at the 512 anchors, float32, ~400 MB each). Nothing of ours in `/root`.

## Before submission (deadline 2026-09-12 23:59 AoE)

1. Overleaf (Times) compile: confirm References starts on page 5. CM fallback spills ~25 lines;
   cuts in order are in `COMPLIANCE.md`.
2. Nine bibliography entries carry a `note` asking for author lists from arXiv. Verify and fix.
3. Track choice on OpenReview (assumed Research).
4. Commit `docs/latex/`, Supplements 03–06, notebooks 09.1/09.2 if the developer wants them in.

## Do not

- Re-add personal language or re-pivot the framing (memory `ml4ps-manuscript-framing`).
- Quote any ‖H_tan‖ or K_H partial as a curvature finding; quote probe-facing results as
  label-dependent, never as "curvature hurts probes".
- Claim the fixture "fills R^768" (it spans 18 dims) or that H_rad = −d validates the tangential
  computation (the finite-difference check does).
- Touch sealed modules or `src/effdim/`; run on the pod without the guide.

## Open experiments, not started

- Relative second fundamental form `II_G − A·II_F` between HSC and Legacy embeddings at shared
  points, vs MKNN/CKA: the curvature-conditioned PRH test the theory names. Needs two decoders on
  the 10k paired rows (fast), II at anchors, a per-point tangent-frame map A.
- Probe-facing at d = 25, 32; ablations listed in the 2026-09-11 conversation (probe weight
  source, contraction variant, metric, k, control set, sphere term, shuffled-label null).
