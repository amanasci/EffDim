---
phase: 09-curvature-conditioned-label-decodability-physics-replication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-curvature-conditioned-label-decodability-physics-replication/09-FIXTURE-FIDELITY-D16.md
  - .planning/phases/09-curvature-conditioned-label-decodability-physics-replication/COVERAGE.md
autonomous: true
requirements: [D9-12, D9-18]
user_setup: []

must_haves:
  truths:
    - "`notebooks/diagnostics/07_instrument_fixture_sweep_run.py --d 16 --out notebooks/.cache/09_fixture_fidelity_d16.jsonl` exits 0 and its final line is `DONE`, producing four cells (cubic and ridge fixtures at ambient D=28 and D=768)."
    - "`09-FIXTURE-FIDELITY-D16.md` records the measured rank Spearman, median cosine and median magnitude ratio of the plain-autoencoder decoder against analytic truth in all four cells, and states the resulting `d=16` instrument-fidelity range as an ordered pair of the minimum and maximum rank Spearman across cells."
    - "`09-FIXTURE-FIDELITY-D16.md` states plainly that `d=32` fixture fidelity is NOT measured and cannot be: the small-ambient fixture arm's literal ambient width is 28, graph fixtures need local width `d+1 = 33`, and `rotate_and_pad` raises by construction — a limitation already ratified in `HANDOFF-v1.1.md` §5.3 and named in `09-CONTEXT.md`'s Deferred section."
    - "`09-FIXTURE-FIDELITY-D16.md` quotes the already-measured ranges `(0.53, 0.99)` at `d=20` and `(0.17, 0.97)` at `d=25` beside the new `d=16` numbers, and states that split-half reliability cannot see a shared bias — `06-FINDINGS.md` measured `R_H = 0.990` beside `rho = 0.469` against known truth on the Swiss roll — so this fixture range, not a reliability score, is the phase's known-answer evidence about the instrument."
    - "`09-FIXTURE-FIDELITY-D16.md` reports the four cells' `var_explained` and median `cond(g)` beside their fidelity axes, and compares the fixtures spread-for-spread by quoting each cell's `ii_cv` rather than comparing them by fixture name alone."
    - "`COVERAGE.md` contains a single reasoned declaration line beginning `No external API integration:` and contains no capability table."
    - "No file under `notebooks/` and no file under `src/effdim/` is created, modified or deleted by this plan; the only tracked files it writes are the two documents under `.planning/phases/09-.../`."
    - statement: "The `d=16` fidelity numbers are quoted in `09-FINDINGS.md` beside the `d=16` verdict cell, so the one cell that matches the colleague's chart rank directly is read with its instrument's known-answer performance attached."
      verification: backstop
  artifacts:
    - .planning/phases/09-curvature-conditioned-label-decodability-physics-replication/09-FIXTURE-FIDELITY-D16.md
    - .planning/phases/09-curvature-conditioned-label-decodability-physics-replication/COVERAGE.md
  key_links:
    - "`09-FIXTURE-FIDELITY-D16.md`'s measured range is the value 09-05 freezes into `physics_curvature_probe.INSTRUMENT_FIDELITY_RANGE_D16`, and 09-10 quotes in the verdict sentence."
    - "`COVERAGE.md` is read by the seal-time `api-coverage.verify-pre` gate; the reasoned declaration form is what that gate accepts in place of a matrix."
  prohibitions:
    - statement: "The `d=32` fixture-fidelity gap is NOT patched, worked around, or estimated by substitution — it is out of scope by explicit developer decision and is reported as unmeasured."
      status: required
      verification: "No invocation of the fixture sweep runner with `--d 32` appears in any Phase 9 artifact, and `09-FIXTURE-FIDELITY-D16.md` states the limitation with its cause."
    - statement: "No sealed runner or module is edited to make this measurement — `07_instrument_fixture_sweep_run.py` is invoked with its existing `--d` and `--out` flags exactly as Phase 8 plan 08-07 added them."
      status: required
      verification: "`git diff --name-only <plan-base-sha>..HEAD -- notebooks/` prints nothing."
    - statement: "A fixture-fidelity number is a statement about the instrument on analytic fixtures, never about the Physics data, and must never be quoted as evidence that a Physics result is correct."
      status: required
      verification: "`09-FIXTURE-FIDELITY-D16.md` carries an explicit scope sentence saying so."
---

<objective>
Measure the plain-autoencoder decoder curvature instrument's known-answer fidelity at `d=16` — the
one `d` in Phase 9's sweep that matches the colleague's chart rank directly — and write the
phase's two standing declaration documents.

Purpose: `d=16` is the cell the whole replication turns on, and it is the one `d` where this
milestone has never measured the instrument against an analytic answer. `07-CONTEXT.md`'s
`INSTRUMENT_FIDELITY_RANGE = (0.53, 0.99)` was measured at `d=20` only. Without the `d=16`
measurement, a `d=16` verdict is uninterpretable in exactly the way `09-RESEARCH.md` Pitfall 2
warns about — and the colleague's own instrument is unvalidated at that `d` too, so neither side
would have a known-answer number.

Output: `09-FIXTURE-FIDELITY-D16.md`, `COVERAGE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/09-curvature-conditioned-label-decodability-physics-replication/09-CONTEXT.md
@.planning/phases/09-curvature-conditioned-label-decodability-physics-replication/09-RESEARCH.md
@HANDOFF-v1.1.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan creates two documents. The full phase-wide symbol and artifact inventory is in
`09-01-PLAN.md` § "Artifacts this phase produces"; the entries owned by this plan are:

| Path | Kind |
|------|------|
| `.planning/phases/09-.../09-FIXTURE-FIDELITY-D16.md` | new doc |
| `.planning/phases/09-.../COVERAGE.md` | new doc |
| `notebooks/.cache/09_fixture_fidelity_d16.jsonl` | new record (gitignored) |

No new code symbol, no new module, no new package. `src/effdim/` gains nothing and `notebooks/`
gains no tracked file.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Measure instrument fidelity at d=16 on the analytic fixtures and record it</name>

  <precondition>`.venv/bin/python` exists and `notebooks/diagnostics/07_instrument_fixture_sweep_run.py` resolves its hard-coded `NB` path — this runner only works on the developer's own machine and is deliberately not part of the execution-host hand-off.</precondition>

  <read_first>
    - `notebooks/diagnostics/07_instrument_fixture_sweep_run.py` — the full file: its module docstring, the `--d` / `--out` flags plan 08-07 added, the four-cell loop over fixture and ambient width, the `axes()` function's three returned quantities, and the printed summary table's column meanings
    - `notebooks/pu_manifold/varying_ii_controls.py` — `FAMILIES`, and the `rotate_and_pad` width requirement that makes `d=32` unmeasurable and `d=16` measurable
    - `HANDOFF-v1.1.md` §5 — the post-freeze diagnostics summary, including the `d=32` fixture limitation and the `d=20`/`d=25` fidelity ranges this document quotes beside the new numbers
    - `.planning/phases/09-.../09-RESEARCH.md` § Common Pitfalls 2 and 6 — the reliability-gate blind spot and the explicit instruction not to attempt `d=32`
    - `.claude/skills/spike-findings-effdim/SKILL.md` — the requirements "anchor at low `d` before interpreting a failure at high `d`", "compare fixtures spread-for-spread, not name-for-name", and "never report a rank statistic from this teacher without the direction axis beside it"
  </read_first>

  <files>.planning/phases/09-curvature-conditioned-label-decodability-physics-replication/09-FIXTURE-FIDELITY-D16.md</files>

  <action>
Run `.venv/bin/python notebooks/diagnostics/07_instrument_fixture_sweep_run.py --d 16 --out
notebooks/.cache/09_fixture_fidelity_d16.jsonl` and let it finish all four cells. Do not modify
the runner, the fixture module, or any sealed file: `--d` and `--out` are the existing flags plan
08-07 added additively, and invoking them is the whole intervention.

Expect four rows: fixture in {cubic, ridge} crossed with ambient D in {28, 768}, each carrying
`var_explained`, `cond_g_median`, `ii_cv`, and both the point-cloud and plain-decoder arms' `rho`,
`median_cosine` and `median_ratio`. The two D=768 cells train a 5,000-row autoencoder each, so
budget tens of minutes of CPU; cap threads with the environment if the machine is otherwise busy.

Then write `.planning/phases/09-.../09-FIXTURE-FIDELITY-D16.md` with these sections:

1. **What was measured and why** — one paragraph: `d=16` is the one cell in Phase 9's
   `D_SWEEP = (16, 20, 25, 32)` that matches the colleague's chart rank directly, and it is the
   one `d` at which this milestone has never scored the plain-autoencoder decoder curvature
   against an analytic answer. State that this is a statement about the instrument on analytic
   fixtures and never about the Physics data, and that no Physics result may be defended by
   quoting it.
2. **Measured table** — one row per cell with `fixture`, `D`, `n`, `var_explained`,
   `cond_g_median`, `ii_cv`, and the decoder arm's `rho`, `median_cosine`, `median_ratio`, with
   the point-cloud arm's three axes beside them as the matched baseline the runner already
   computes. Report every number to the precision the runner prints; do not round.
3. **The `d=16` fidelity range** — an ordered pair of the minimum and maximum decoder-arm rank
   Spearman across the four cells, stated in the same form as `07-CONTEXT.md`'s
   `INSTRUMENT_FIDELITY_RANGE`. Name it as the value plan 09-05 freezes into
   `physics_curvature_probe.INSTRUMENT_FIDELITY_RANGE_D16`.
4. **Spread-for-spread comparison** — quote each cell's `ii_cv` beside its `rho` and state
   whether the fixtures differ in dynamic range, per the project skill's requirement that
   fixtures be compared spread-for-spread rather than name-for-name. Report the direction axis
   (`median_cosine`) beside every rank number, per the same skill's standing requirement.
5. **Beside the other `d` values** — quote `(0.53, 0.99)` at `d=20` and `(0.17, 0.97)` at `d=25`
   from `HANDOFF-v1.1.md` §5, and state that `d=32` is unmeasured and unmeasurable here: the
   small-ambient fixture arm's literal ambient width is 28, a `d=32` graph fixture needs local
   width 33, and `rotate_and_pad` raises `ValueError` by construction. Say that Phase 9 does not
   fix the fixture and does not attempt the run.
6. **Why a reliability score is not a substitute** — restate `06-FINDINGS.md`'s measured
   `R_H = 0.990` beside `rho = 0.469` against known truth on the Swiss roll, and the colleague's
   own `d=16` `R_H` median of 0.514 with 42% of anchors below 0.5 from
   `09-COLLEAGUE-REANALYSIS.md`. Conclude that split-half reliability cannot see a shared bias,
   so a fixture range is the only known-answer evidence either side has, and neither instrument
   has one at `D=768, k=2048, d=16`.

Do not add a verdict, a threshold, or a pass/fail line to this document. It is a measurement
record read beside the phase verdict, not a gate.
  </action>

  <verify>
    <automated>test -s notebooks/.cache/09_fixture_fidelity_d16.jsonl && .venv/bin/python -c "import json,sys; rows=json.load(open('notebooks/.cache/09_fixture_fidelity_d16.jsonl')); assert len(rows)==4, rows; assert all(r['d']==16 for r in rows); assert {(r['fixture'], r['D']) for r in rows} == {('cubic',28),('cubic',768),('ridge',28),('ridge',768)}; print('OK', [round(r['plain_decoder']['rho'],4) for r in rows])"</automated>
  </verify>

  <acceptance_criteria>
    - `notebooks/.cache/09_fixture_fidelity_d16.jsonl` parses as a JSON list of exactly four objects, every one with `d == 16`, covering both fixtures at both ambient widths.
    - `grep -c 'INSTRUMENT_FIDELITY_RANGE_D16' .planning/phases/09-curvature-conditioned-label-decodability-physics-replication/09-FIXTURE-FIDELITY-D16.md` is at least 1.
    - `grep -c 'median_cosine\|median cosine' .planning/phases/09-curvature-conditioned-label-decodability-physics-replication/09-FIXTURE-FIDELITY-D16.md` is at least 1 — the direction axis is reported beside every rank number.
    - `grep -c 'ii_cv' .planning/phases/09-curvature-conditioned-label-decodability-physics-replication/09-FIXTURE-FIDELITY-D16.md` is at least 1 — fixtures are compared spread-for-spread.
    - `grep -c '0.53' .planning/phases/09-curvature-conditioned-label-decodability-physics-replication/09-FIXTURE-FIDELITY-D16.md` is at least 1 and `grep -c '0.17'` is at least 1 — the `d=20` and `d=25` ranges are quoted beside the new numbers.
    - `git status --porcelain notebooks/` prints nothing — no tracked file under `notebooks/` was created or modified.
  </acceptance_criteria>

  <done>Four analytic-fixture cells are measured at `d=16`, the resulting fidelity range is recorded in the form 09-05 will freeze and 09-10 will quote, the `d=32` limitation is stated with its cause, and the document says in its own words why a reliability score is not a substitute for a known answer.</done>
</task>

<task type="auto">
  <name>Task 2: Write the phase's API-coverage declaration</name>

  <read_first>
    - `.planning/phases/07-curvature-conditioned-crossmodal-alignment/COVERAGE.md` — the reasoned one-line declaration Phase 7 wrote for the same situation; copy its form
    - `.planning/phases/09-.../09-RESEARCH.md` § Architectural Responsibility Map — the confirmation that this phase has no browser, server or CDN tier and every capability is a single-process CPU-bound analysis reading cached parquet
  </read_first>

  <files>.planning/phases/09-curvature-conditioned-label-decodability-physics-replication/COVERAGE.md</files>

  <action>
Write `.planning/phases/09-.../COVERAGE.md` as a reasoned declaration, not a capability matrix.
The detector returned `detected: false` on the Phase 9 ROADMAP section; this document exists so
the seal-time `api-coverage.verify-pre` gate cannot misfire on plan text that mentions two
HuggingFace dataset repositories.

The file must contain a single declaration line beginning exactly with the token
`No external API integration:` followed by a one-line reason naming what the phase touches
instead: two public HuggingFace dataset repositories read as static column-projected parquet, with
no request/response surface, no authentication, no write path, and no versioned API contract to
enumerate capabilities against. Add one short paragraph beneath it recording that this is data
acquisition rather than an API-surface integration, that both reads are anonymous and read-only,
and that the revision pin (`Smith42/galaxies` at `v2.0`) is a data-provenance constant frozen in
`physics_labels.py`, not an API version negotiation.

Add no capability table and no `OPT-OUT` rows — a fabricated row for a capability that does not
exist is exactly what the gate's declaration branch exists to avoid.
  </action>

  <verify>
    <automated>grep -q '^No external API integration:' .planning/phases/09-curvature-conditioned-label-decodability-physics-replication/COVERAGE.md && echo OK</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/09-curvature-conditioned-label-decodability-physics-replication/COVERAGE.md` exists and its first non-heading line begins with `No external API integration:` followed by a reason of at least ten words.
    - `grep -cE '^\|' .planning/phases/09-curvature-conditioned-label-decodability-physics-replication/COVERAGE.md` is `0` — no capability table row was fabricated.
    - `grep -c 'OPT-OUT' .planning/phases/09-curvature-conditioned-label-decodability-physics-replication/COVERAGE.md` is `0`.
    - `grep -c 'v2.0' .planning/phases/09-curvature-conditioned-label-decodability-physics-replication/COVERAGE.md` is at least 1 — the revision pin is named as a data-provenance constant.
  </acceptance_criteria>

  <done>The phase carries a reasoned no-external-API declaration in the form the seal-time gate accepts, with no fabricated capability rows.</done>
</task>

</tasks>

<discretion_decisions>
- **Fixture fidelity at `d=16` IS measured** (`09-CONTEXT.md` §"Claude's Discretion": "Whether to
  measure fixture fidelity at `d=16` before the Physics run"). `d=16` is the only cell that
  matches the colleague's chart rank directly, so it carries the phase's most-read comparison, and
  it is the only `d` in this milestone's sweep whose instrument has never been scored against an
  analytic answer. The measurement costs tens of minutes of local CPU on an existing runner with
  no code change, and without it a `d=16` verdict has no known-answer context on either side —
  the exact gap `09-RESEARCH.md` Pitfall 2 names. Measured before the Physics run so the number is
  available to freeze at 09-05 and to quote in the verdict sentence.
- **This plan runs locally, not on the execution host.**
  `07_instrument_fixture_sweep_run.py` carries a hard-coded absolute `NB` path from Phase 7 and is
  sealed, so it works only on the developer's machine. It touches no Physics data, so running it
  locally violates nothing: the execution-host rule constrains where real Physics numbers are
  produced, not where analytic-fixture diagnostics run. This runner is deliberately excluded from
  the 09-06 hand-off bundle for the same reason.
- **This plan carries no `<threat_model>` STRIDE additions of its own.** It writes two markdown
  documents and invokes one existing runner read-only; the phase-wide register is in
  `09-01-PLAN.md` and every threat there applies unchanged. `09-RESEARCH.md` § Security Domain's
  V5-narrow assessment is the governing scope.
</discretion_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process -> `notebooks/.cache/` | The only filesystem write, via the existing runner's `--out` flag |
| repo working tree -> `.planning/` docs | Two new markdown documents; no code path reads them at runtime |

No network, no authentication, no user input, no external service, no persistence layer beyond the
gitignored local cache. ASVS L1 applies at its narrowest: `09-RESEARCH.md` § Security Domain
assessed V2/V3/V4/V6 as not applicable and V5 as narrowly applicable to malformed astronomical
data, which this plan does not touch at all.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10 | Tampering | the sealed `07_instrument_fixture_sweep_run.py` and `varying_ii_controls.py` | high | mitigate | Invoked only through its existing `--d`/`--out` flags; `git status --porcelain notebooks/` acceptance criterion proves no tracked file under `notebooks/` changed |
| T-09-11 | Tampering | `--out` path escaping the cache directory | low | mitigate | The `--out` value is a literal inside the cache directory written by this plan, not caller-supplied at runtime; the runner writes exactly where it is told and nothing composes a path from untrusted input |
| T-09-12 | Repudiation | a fidelity number quoted later without its fixture, ambient width and direction axis | medium | mitigate | The document's table carries `fixture`, `D`, `ii_cv` and `median_cosine` on every row, and the project skill's standing requirement to report the direction axis beside every rank statistic is an acceptance criterion |
| T-09-13 | Information Disclosure | none — synthetic analytic fixtures only, no external data | low | accept | Nothing to disclose |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed. `09-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable |
</threat_model>

<verification>
- `.venv/bin/python notebooks/diagnostics/07_instrument_fixture_sweep_run.py --d 16 --out notebooks/.cache/09_fixture_fidelity_d16.jsonl` exits 0 with a final `DONE` line.
- `notebooks/.cache/09_fixture_fidelity_d16.jsonl` parses as four cells at `d == 16`.
- `.planning/phases/09-.../09-FIXTURE-FIDELITY-D16.md` and `COVERAGE.md` both exist and satisfy their acceptance criteria.
- `git status --porcelain notebooks/ src/effdim/` prints nothing.
</verification>

<success_criteria>
- The instrument's known-answer fidelity at `d=16` is measured on both fixtures at both ambient
  widths, reported with its direction axis and its spread, and expressed as the ordered range
  09-05 will freeze.
- The `d=32` gap is stated as unmeasurable with its cause, not patched or estimated.
- The phase carries a reasoned no-external-API declaration the seal-time gate accepts.
- No code file anywhere in the repository changed.
</success_criteria>

<output>
Create `.planning/phases/09-curvature-conditioned-label-decodability-physics-replication/09-02-SUMMARY.md`
when done. Record in it: the four measured cells' decoder-arm `rho`, `median_cosine` and
`median_ratio`; the resulting `d=16` range as an ordered pair; each cell's `var_explained`,
`cond_g_median` and `ii_cv`; and the wallclock of the run so 09-06's cost model can cite a
measured per-thread figure for a 5,000-row fit.
</output>
