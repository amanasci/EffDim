---
phase: 02.4-topological-auto-encoder-validity-test-inserted
plan: 03
type: execute
wave: 2
depends_on: ["02.4-01"]
files_modified:
  - notebooks/diagnostics/topoae_lambda_sweep_run.py
  - notebooks/02.4_swiss_roll_topoae_check.ipynb
autonomous: false
requirements: [R8]
user_setup: []

must_haves:
  truths:
    - "notebooks/02.4_swiss_roll_topoae_check.ipynb runs end to end on CPU in under two minutes with zero error cells, committed with outputs"
    - "A grep of the Swiss roll notebook's source for the gitignored cache directory name returns nothing"
    - "The notebook imports topoae unchanged from notebooks/pu_manifold/ and never reimplements, simplifies, or inlines a variant of the model code"
    - "The notebook generates ~3,000 noiseless Swiss roll points in-notebook with a fixed random_state, centres them and divides by one global standard deviation"
    - "The notebook trains from scratch at latent dimension 2 and plots the original and the reconstruction side by side as a 3-D scatter and as an x-z scatter, every plot coloured by the arc-length parameter t"
    - "The notebook compares against a matched cae.PlainAutoEncoder baseline at the same 2-D bottleneck, same width, depth, and training protocol"
    - "The notebook ends with three or four printed pass/fail lines and a one-sentence read-out"
    - "The notebook is at most ~15 cells and contains no caching, no pre-registration, no cfg-hash cache keys, no verdict JSON, and no threshold table"
    - "The lambda value is selected on the Swiss roll fixture by a rule written down before the sweep runs, and the sweep runner records that rule in its own module docstring"
    - "The sweep records, for each lambda, the ratio of the topological loss term to the reconstruction term at the end of warm-up, so a failure to transfer from a 2-d sheet in 3-d to a ~20-d manifold in 768-d is visible rather than silent"
  artifacts:
    - notebooks/diagnostics/topoae_lambda_sweep_run.py
    - notebooks/02.4_swiss_roll_topoae_check.ipynb
  key_links:
    - "The sweep runner and the sanity-check notebook use the same Swiss roll generator call and the same constants (sklearn.datasets.make_swiss_roll, n_samples=3000, noise=0.0, random_state=20260806); the notebook consumes the frozen lambda the sweep selected"
    - "The lambda sweep lives in notebooks/diagnostics/ and not inside the notebook, because CLAUDE.md caps the sanity-check notebook at ~15 cells with no threshold tables and no pre-registration apparatus"
    - "The sweep runner carries no git-ancestry precondition — it runs on the Swiss roll fixture before the pre-registration exists, which D-11 permits because the fixture is not the gate data"
  prohibitions:
    - statement: "MUST NOT read from or write to the gitignored cache directory in the Swiss roll notebook"
      status: resolved
      verification: "test — grep the notebook source for the cache directory name returns nothing"
    - statement: "MUST NOT delete or rewrite any existing notebook or runner script — additive only"
      status: resolved
      verification: "git status --porcelain notebooks/ shows the two new files as additions and no deletions"
---

<objective>
Deliver the Swiss roll half of this phase: a lambda selection sweep under
`notebooks/diagnostics/`, and `notebooks/02.4_swiss_roll_topoae_check.ipynb` — the known-answer
sanity check `CLAUDE.md` makes mandatory for every new manifold model and names topological
auto-encoders explicitly for.

Purpose: two things at once, deliberately kept in separate files. The sweep freezes lambda on a
fixture that is not the gate data (D-11), which is what makes freezing it legitimate at all. The
notebook is the gate on the model itself — until a manifold whose answer is known in advance
shows the implementation recovers it, a FAIL on the PU data is uninterpretable and every
downstream conclusion drawn from it is unsupported. Both must land before a single PU fit runs.

Output: `notebooks/diagnostics/topoae_lambda_sweep_run.py`, the frozen lambda value, and
`notebooks/02.4_swiss_roll_topoae_check.ipynb` committed with executed outputs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-SPEC.md
@.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-CONTEXT.md
@.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-RESEARCH.md
@.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-PATTERNS.md
@.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-01-SUMMARY.md
@notebooks/02.2_swiss_roll_cae_check.ipynb
@notebooks/pu_manifold/cae.py
</context>

<artifacts_this_phase_produces>
Created by this plan (see `02.4-01-PLAN.md` for the whole-phase list):

- `notebooks/diagnostics/topoae_lambda_sweep_run.py` — module constants `SWISS_N`, `SWISS_SEED`,
  `SWISS_NOISE`, `LAMBDA_GRID`, `LAMBDA_SELECT_RULE`, `SWEEP_MAX_EPOCHS`, `SWEEP_BATCH`,
  `SWEEP_HIDDEN`, `SWEEP_LATENT`, `SWEEP_SEED`, `SPLIT_SEED`, `HOLDOUT_FRACTION`; cache stem
  `topoae_lambda_sweep_{cfg_key}`
- `notebooks/02.4_swiss_roll_topoae_check.ipynb`
- The frozen lambda value, carried forward into `02.4-PREREGISTRATION.md` by plan 02.4-04
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Lambda sweep runner on the Swiss roll fixture</name>
  <files>notebooks/diagnostics/topoae_lambda_sweep_run.py</files>
  <read_first>
    - `notebooks/pu_manifold/topoae.py` (as written by plan 02.4-01 — `train_topoae`, `topological_loss`, `lambda_schedule`, `latent_unit_scale`, `pairwise_distances_f64`)
    - `notebooks/diagnostics/cae_train_run.py` lines 1-72 (module docstring shape, the invocation line, the named-module-level-constants convention) and the `_banner` step convention
    - `notebooks/pu_manifold/cae.py` lines 344-368 (`reconstruction_stats`), 576-605 (`PlainAutoEncoder`), 716-733 (`train_plain_ae`)
    - `notebooks/pu_manifold/cache.py` (`json_cache`, `config_key`)
    - `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-CONTEXT.md` D-11, D-12, D-13, D-14, D-15
    - `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-RESEARCH.md` Pitfall 2, Open Question 3, and the Sources note recording the paper's own best-run lambda range of 0.43-0.50
  </read_first>
  <action>
Create `notebooks/diagnostics/topoae_lambda_sweep_run.py`, following `cae_train_run.py`'s shape:
a module docstring stating what it does and the exact invocation
`PYTHONPATH=notebooks python notebooks/diagnostics/topoae_lambda_sweep_run.py`, then every
constant as a named module-level value, then banner-separated steps.

The docstring must state, before the sweep is ever run, the selection rule that will pick lambda,
because the whole legitimacy of freezing lambda rests on the rule preceding the numbers. The
rule, which is binding and is repeated verbatim in `LAMBDA_SELECT_RULE`:

> The selected lambda is the largest value in `LAMBDA_GRID` whose Swiss-roll held-out
> `mse_per_dim` is strictly below 1.10 times the `mse_per_dim` of the zero-lambda fit. Maximise
> topological regularisation subject to not degrading reconstruction by more than ten percent
> against the unregularised fit at matched capacity. If no positive value satisfies it, the
> selection is the smallest positive grid value and the sweep reports that the constraint bound
> at the grid floor.

The docstring must also state, per D-11, that lambda is tuned here on a 2-d sheet in 3-d and
applied to a ~20-d manifold in 768-d, that this transfer is a stated risk rather than a
demonstrated property, and that the sweep therefore also records a transfer diagnostic (below).
And it must state, per A4 and D-13, that this runner deliberately carries no git-ancestry
precondition: it runs before `02.4-PREREGISTRATION.md` exists, which is legitimate precisely
because the Swiss roll fixture is not the gate data.

Constants: `SWISS_N = 3000`, `SWISS_SEED = 20260806`, `SWISS_NOISE = 0.0`,
`LAMBDA_GRID = (0.0, 0.1, 0.25, 0.5, 1.0, 2.0)` (spanning the paper's own best-found range with
a decade either side), `SWEEP_LATENT = 2`, `SWEEP_HIDDEN = (64, 64, 64)`,
`SWEEP_ACTIVATION = "silu"`, `SWEEP_BATCH = 64`, `SWEEP_MAX_EPOCHS = 300`, `SWEEP_LR = 3e-4`,
`SWEEP_WEIGHT_DECAY = 1e-4`, `WARMUP_FRAC = 0.25`, `RAMP_FRAC = 0.25`,
`HOLDOUT_FRACTION = 0.2`, `SPLIT_SEED = 20260806`, `SWEEP_SEED = 20260806`.

Steps:

1. Generate the fixture with `sklearn.datasets.make_swiss_roll(n_samples=SWISS_N,
   noise=SWISS_NOISE, random_state=SWISS_SEED)`, then centre and divide by one global scalar
   standard deviation — a single number, so the shape is preserved. Split into train and holdout
   with `np.random.default_rng(SPLIT_SEED).permutation`. These exact lines and these exact
   constants are what the sanity-check notebook repeats; they are the shared generator D-12
   requires, and the notebook must not diverge from them.
2. For each value in `LAMBDA_GRID`: instantiate `cae.PlainAutoEncoder(3, SWEEP_LATENT,
   hidden=SWEEP_HIDDEN, activation=SWEEP_ACTIVATION)` under `torch.manual_seed(SWEEP_SEED)` and
   train with `topoae.train_topoae` at that value. Record held-out `cae.reconstruction_stats`,
   the two directional `topological_loss` values on the held-out set with `latent_unit_scale`
   applied to the latent, and the transfer diagnostic: the ratio of the weighted topological
   term to the reconstruction term, averaged over the first epoch after the ramp completes. That
   ratio is the concrete, independently-tunable quantity `02.4-RESEARCH.md` Pitfall 2 warns
   transfers badly; recording it here lets the PU run report the same ratio, so a transfer
   failure shows up as an order-of-magnitude gap rather than as an unexplained FAIL. The
   zero-lambda arm is the unregularised reference the rule compares against and is equivalent to
   `cae.train_plain_ae` — run it through `train_topoae` at zero anyway, so the protocol is
   identical in every respect except the loss term.
3. Apply the selection rule verbatim, print the full table (lambda, held-out `mse_per_dim`, its
   ratio against the zero-lambda arm, both directional topological losses, the transfer
   diagnostic, elapsed wall-clock), print the selection and which branch of the rule produced it,
   and persist the whole table through `cache.json_cache` under the stem
   `topoae_lambda_sweep_{cfg_key}` where `cfg_key = cache.config_key(...)` over the constants
   dict. Never write with a raw file handle or a bare `np.savez` — the containment check and the
   manifest guard have to apply.

Keep the whole sweep under about twenty minutes on CPU: six arms at 3,000 points, 300 epochs,
hidden width 64 is small, but measure it and cut `SWEEP_MAX_EPOCHS` if it overruns.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; python -c "import ast; src=open('notebooks/diagnostics/topoae_lambda_sweep_run.py').read(); ast.parse(src); assert 'LAMBDA_GRID' in src and 'LAMBDA_SELECT_RULE' in src and 'make_swiss_roll' in src; assert 'merge-base' not in src; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/diagnostics/topoae_lambda_sweep_run.py` parses under `ast.parse` and defines module-level `SWISS_N = 3000`, `SWISS_SEED = 20260806`, `LAMBDA_GRID`, `LAMBDA_SELECT_RULE`, `SWEEP_LATENT = 2`, `SWEEP_BATCH = 64`
    - `grep -c "merge-base" notebooks/diagnostics/topoae_lambda_sweep_run.py` returns 0 — the sweep legitimately precedes the pre-registration
    - `grep -c "json_cache" notebooks/diagnostics/topoae_lambda_sweep_run.py` returns at least 1, and `grep -cE "np\.savez\(" notebooks/diagnostics/topoae_lambda_sweep_run.py` returns 0
    - The selection rule text appears inside the module docstring, above any code that computes a number
    - `git diff --quiet -- notebooks/pu_manifold/cae.py` exits 0
  </acceptance_criteria>
  <done>A runnable sweep exists whose selection rule is written down before any lambda has been measured, whose fixture generation is the exact call the notebook will repeat, and which records the topological-to-reconstruction term ratio that makes a transfer failure visible.</done>
  <reversibility rating="reversible">The runner is new code with no consumer yet; lambda is not frozen until plan 02.4-04 writes it into the pre-registration.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Run the sweep and record the selected lambda</name>
  <files>notebooks/diagnostics/topoae_lambda_sweep_run.py</files>
  <read_first>
    - `notebooks/diagnostics/topoae_lambda_sweep_run.py` (as written by Task 1)
    - `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-CONTEXT.md` D-11
  </read_first>
  <precondition>The project virtualenv is active with `torch==2.13.0+cpu`, `scikit-learn==1.9.0` and `numpy==2.5.1` importable — `python -c "import torch, sklearn, numpy"` exits 0.</precondition>
  <action>
Execute `PYTHONPATH=notebooks python notebooks/diagnostics/topoae_lambda_sweep_run.py` end to
end. Capture the printed table verbatim into the plan summary: for every arm, the held-out
`mse_per_dim`, its ratio against the zero-lambda arm, both directional topological losses, the
transfer diagnostic, and the elapsed wall-clock. State the selected value and whether the rule
bound at an interior grid point or at the grid floor.

Do not adjust the grid, the rule, or any constant to move the selection — if the rule binds at
the floor, that is the measured result and it is reported as such. If the sweep reveals a genuine
implementation problem (a loss that never decreases at any arm, a latent that collapses), stop
and report it rather than tuning around it: a model that only works after the sweep is rewritten
for it is itself the finding.

Re-invoke the runner a second time and confirm the cache returns a hit rather than recomputing,
proving the manifest key is stable.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; PYTHONPATH=notebooks timeout 3600 python notebooks/diagnostics/topoae_lambda_sweep_run.py 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The runner exits 0 and prints one row for every arm in `LAMBDA_GRID`
    - The printed output names a single selected value and states which branch of the selection rule produced it
    - A second invocation completes in seconds and reports a cache hit rather than retraining
    - The plan summary records the transfer diagnostic for the selected value, which the PU run will be compared against
    - `git diff --quiet -- notebooks/pu_manifold/cae.py notebooks/pu_manifold/topoae.py` exits 0 — running the sweep changed no source
  </acceptance_criteria>
  <done>The lambda is measured, printed, cached, and recorded in the plan summary together with the transfer diagnostic, ready for plan 02.4-04 to freeze into the pre-registration.</done>
</task>

<task type="auto">
  <name>Task 3: The mandatory Swiss roll sanity-check notebook</name>
  <files>notebooks/02.4_swiss_roll_topoae_check.ipynb</files>
  <read_first>
    - `CLAUDE.md` § "Swiss roll sanity check — required for every new manifold model" (the whole section, including "What the notebook must not do" — it is binding and unusually specific)
    - `notebooks/02.2_swiss_roll_cae_check.ipynb` (the reference implementation named by `CLAUDE.md`; copy its shape cell by cell)
    - `notebooks/02.1_swiss_roll_geometry_probes_check.ipynb` (the second reference)
    - `notebooks/diagnostics/topoae_lambda_sweep_run.py` (for the shared fixture constants and the selected lambda)
    - `notebooks/pu_manifold/topoae.py` (the model code, to be imported unchanged)
    - `notebooks/pu_manifold/cae.py` lines 344-368, 576-605, 716-733 (`reconstruction_stats`, `PlainAutoEncoder`, `train_plain_ae` — the matched baseline `CLAUDE.md` mandates)
    - `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-RESEARCH.md` § Architecture Patterns Pattern 5
    - `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-PATTERNS.md` § `notebooks/02.4_swiss_roll_topoae_check.ipynb`
  </read_first>
  <action>
Create `notebooks/02.4_swiss_roll_topoae_check.ipynb`, at most about fifteen cells, following
`02.2_swiss_roll_cae_check.ipynb` section for section:

1. Markdown — title, and why this notebook exists: a model that fails on real data has two
   possible causes, and only a manifold whose answer is known in advance separates them.
2. Markdown, section one: setup.
3. Code — imports and the `sys.path` relative-import setup exactly as the 02.2 notebook does it,
   then `from pu_manifold import topoae` and `from pu_manifold import cae`. Import the model code
   unchanged. Never reimplement, simplify, or inline a variant of it — if the model only works
   after it is rewritten for the notebook, that is itself the finding and must be reported rather
   than worked around.
4. Markdown, section two: the Swiss roll.
5. Code — `make_swiss_roll(n_samples=3000, noise=0.0, random_state=20260806)`, then centre and
   divide by one global standard deviation (a single scalar, so the shape is preserved), then a
   train/holdout split under the same split seed the sweep runner used. Same generator call, same
   constants as `topoae_lambda_sweep_run.py`; do not diverge.
6. Markdown, section three: fit the topological auto-encoder — same class and training function
   the PU run will use, only smaller. Ambient dimension 3, latent dimension 2 (the roll's true
   intrinsic dimension), hidden `(64, 64, 64)`, a few hundred epochs, the lambda the sweep froze.
7. Code — `torch.manual_seed(SEED)` first, then `cae.PlainAutoEncoder(3, 2, hidden=(64, 64, 64),
   activation="silu")` trained by `topoae.train_topoae`, and a second identically-constructed
   model trained by `cae.train_plain_ae` at the same width, depth, epochs, batch size, learning
   rate and weight decay — the matched baseline at the same 2-D bottleneck. Train from scratch
   inside the notebook, targeting under two minutes total on CPU.
8. Markdown, section four: the reconstruction, side by side.
9. Code — the original and the reconstruction, both as a 3-D scatter and as an x-z scatter. The
   default 3-D view hides the spiral; the x-z plane shows it unambiguously. Colour every plot by
   the roll's arc-length parameter `t`, so colour bands staying in order means the surface stayed
   in order.
10. Markdown, section five: how faithful is it, in numbers.
11. Code — `cae.reconstruction_stats` on held-out data for both models, the relative
    reconstruction error, and the beats-the-baseline comparison. If the topological auto-encoder
    is not clearly better than the plain baseline on a manifold it was designed for, say so
    plainly in the read-out; that is a real result about the model.
12. Markdown, section six: did the topology survive. The chart-survival plot the 02.2 notebook
    carries at this position has no analogue here, since this model has no charts.
13. Code — the replacement diagnostic: a scatter of persistence-pair edge lengths in the ambient
    space against the same pairs' lengths in the latent, plus the two directional
    `topoae.topological_loss` values printed. This is the model's own distinguishing internal
    state — the mechanism being tested, shown.
14. Markdown, section seven: read-out.
15. Code — three or four printed pass/fail lines and a one-sentence conclusion from their boolean
    AND: relative reconstruction error below a stated bound, beats the matched baseline on
    held-out `mse_per_dim`, and the structural check — the latent persistence-pair lengths
    correlate with the ambient ones above a stated bound, i.e. the spiral unrolled with its
    colour ordering intact.

Prohibitions, all from `CLAUDE.md`: no caching, no pre-registration, no git-ancestry proofs, no
cfg-hash cache keys, no verdict JSON artifacts, no threshold tables. Those belong to the gated
milestone runs on real data. The notebook must never read from or write to the gitignored cache
directory, and the literal directory name the acceptance grep searches for must not appear
anywhere in the notebook source, including markdown cells. It must never import anything from
`notebooks/diagnostics/`. It is a sanity check: short enough to read in one sitting and cheap
enough to re-run on a whim. If it grows past about fifteen cells it has drifted from its purpose.

Execute it end to end and commit it with its outputs, as every notebook in this repository is.
Do not delete or rewrite `02.1_swiss_roll_geometry_probes_check.ipynb` or
`02.2_swiss_roll_cae_check.ipynb` — this work is additive only.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; time jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=180 notebooks/02.4_swiss_roll_topoae_check.ipynb &amp;&amp; python -c "import json,sys; nb=json.load(open('notebooks/02.4_swiss_roll_topoae_check.ipynb')); errs=[o for c in nb['cells'] for o in c.get('outputs',[]) if o.get('output_type')=='error']; print('cells',len(nb['cells']),'errors',len(errs)); sys.exit(1 if errs or len(nb['cells'])>16 else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - `jupyter nbconvert --to notebook --execute --inplace notebooks/02.4_swiss_roll_topoae_check.ipynb` exits 0 and completes in under 120 seconds on CPU
    - The executed notebook contains zero cells with an `error` output and at most 16 cells
    - `grep -c "\.cache" notebooks/02.4_swiss_roll_topoae_check.ipynb` returns 0
    - `grep -c "diagnostics" notebooks/02.4_swiss_roll_topoae_check.ipynb` returns 0
    - `grep -c "make_swiss_roll" notebooks/02.4_swiss_roll_topoae_check.ipynb` returns at least 1, and the notebook references `topoae.train_topoae`, `cae.PlainAutoEncoder` and `cae.train_plain_ae`
    - The notebook is committed with outputs — `python -c "import json;nb=json.load(open('notebooks/02.4_swiss_roll_topoae_check.ipynb'));assert any(c.get('outputs') for c in nb['cells'])"` exits 0
    - `git status --porcelain notebooks/` shows no deletions and no modifications to `02.1_swiss_roll_geometry_probes_check.ipynb`, `02.2_swiss_roll_cae_check.ipynb`, or `02_k_sensitivity_refit.ipynb`
    - `git diff --quiet -- notebooks/pu_manifold/cae.py notebooks/pu_manifold/topoae.py` exits 0 — the notebook imports the model unchanged
  </acceptance_criteria>
  <done>The mandatory known-answer check exists, runs end to end on CPU in under two minutes with zero error cells, imports the model unchanged, beats or fails against a matched baseline on the record, and is committed with its outputs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Read the Swiss roll — does the spiral unroll?</name>
  <what-built>
`notebooks/02.4_swiss_roll_topoae_check.ipynb`, executed end to end: a 3,000-point noiseless
Swiss roll, a topological auto-encoder trained from scratch at latent dimension 2, a matched
`cae.PlainAutoEncoder` baseline at the same bottleneck, side-by-side 3-D and x-z scatters
coloured by the arc-length parameter, a persistence-pair edge-length agreement plot, and three or
four printed pass/fail lines. Plus `notebooks/diagnostics/topoae_lambda_sweep_run.py` and its
executed sweep table with the selected lambda.
  </what-built>
  <how-to-verify>
1. Open `notebooks/02.4_swiss_roll_topoae_check.ipynb` and look at the x-z scatter pair. In the
   reconstruction, do the colour bands stay in order along the spiral? Out-of-order colour means
   the surface tore or folded — the model did not recover a manifold whose answer is known.
2. Check the 3-D scatter pair for the same thing, remembering the default view hides the spiral.
3. Read the three or four printed pass/fail lines and the one-sentence read-out. Does the
   topological auto-encoder beat the matched plain baseline, or does it merely tie it? A tie is a
   real result about the model and must be stated plainly, not glossed.
4. Confirm the notebook ran in under two minutes with no error cells, and that it is at most
   about fifteen cells with no threshold table and no cache access.
5. Look at the sweep table in the plan summary. Is the selected lambda an interior grid point, or
   did the rule bind at the floor? Does the transfer diagnostic look plausible for carrying to
   768-d data?

**This is the gate on the model.** If the spiral does not unroll here, a FAIL on the PU data
would be uninterpretable, and the ~10 hours of fits that plans 02.4-05 and 02.4-06 spend would be
spent on an implementation nobody has shown to work.
  </how-to-verify>
  <resume-signal>Type "approved" to freeze this lambda and proceed to the pre-registration, or describe what the plots show.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notebook → gitignored cache | The sanity check must be reproducible from nothing; any cache read would make it depend on irreproducible provenance-bearing state |
| sweep result → pre-registration | The selected lambda becomes a frozen pre-registered constant; if the rule that picked it were written after the numbers, the freeze would be meaningless |
| existing notebooks and runners | `CLAUDE.md` forbids deleting or rewriting them; a phase that clobbers a sealed notebook destroys prior evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.4-12 | Tampering | lambda selected after its effect on the gate is visible | high | mitigate | The rule is written into the sweep runner's module docstring and `LAMBDA_SELECT_RULE` before the runner is executed, and the sweep runs only on the Swiss roll fixture, never on PU data — D-11's exemption. Task 2 forbids adjusting the grid or the rule to move the selection |
| T-02.4-13 | Tampering | the sanity-check notebook reading provenance-bearing cached state | medium | mitigate | Acceptance grep of the notebook source for the cache directory name returns zero; the notebook generates its own fixture in-notebook |
| T-02.4-14 | Tampering / Destruction | an existing notebook or runner overwritten | medium | mitigate | `git status --porcelain notebooks/` asserted to show additions only, and `git diff --quiet` asserted on the two prior Swiss roll notebooks |
| T-02.4-04 | Tampering / Information Disclosure | sweep cache stem composition | medium | mitigate | The sweep persists through `cache.json_cache` with a `cache.config_key` stem, so `cache._assert_inside_cache` and the manifest guard both apply; no raw file writes |
| T-02.4-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package — `make_swiss_roll` and `matplotlib` are already pinned in `notebooks/requirements-notebooks.txt`. Any install would first need a blocking `checkpoint:decision`; `[ASSUMED]`/`[SUS]` packages require slopcheck plus a blocking human checkpoint |
</threat_model>

<verification>
- `jupyter nbconvert --to notebook --execute notebooks/02.4_swiss_roll_topoae_check.ipynb` exits 0 in under 120 seconds with zero error cells
- `grep -c "\.cache" notebooks/02.4_swiss_roll_topoae_check.ipynb` returns 0
- The notebook has at most 16 cells and is committed with outputs
- `PYTHONPATH=notebooks python notebooks/diagnostics/topoae_lambda_sweep_run.py` exits 0 and a second invocation is a cache hit
- `git status --porcelain notebooks/` shows only the two new files, no deletions
- `git diff --quiet -- notebooks/pu_manifold/cae.py pyproject.toml src/effdim/` exits 0
- The blocking human-verify checkpoint is approved
</verification>

<success_criteria>
The model has passed a manifold whose answer is known in advance, on the record, in a notebook
short enough to read in one sitting and cheap enough to re-run — or it has failed it, and that
failure is stated plainly before any compute is spent on the PU data. Lambda is measured under a
rule that predates the numbers and is ready to be frozen.
</success_criteria>

<output>
Create `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-03-SUMMARY.md` when done
</output>
