---
phase: 02.4-topological-auto-encoder-validity-test-inserted
plan: 04
subsystem: ml-training
tags: [pre-registration, git-ancestry, gate-thresholds, lambda-freeze]

# Dependency graph
requires:
  - phase: 02.4-topological-auto-encoder-validity-test-inserted (plans 01-03)
    provides: "topoae.py's T1/T2/T3 gate engine (plan 02), LAMBDA_TOPO=0.1 frozen by the Swiss roll sweep and the three named Known Limitations (plan 03)"
provides:
  - "02.4-PREREGISTRATION.md: T1/T2/T3 metric definitions and thresholds (THRESH_T1=0.90, THRESH_T2=1.00, THRESH_T3=0.90), LADDER=(8,16,20,24,32,40) with D_PRIMARY=20, LAMBDA_TOPO=0.1 frozen, the three planner-resolved constants (AMBIENT_DIST_NORM=none, one PlainAutoEncoder baseline per TopoAE fit, quarter-warmup/quarter-ramp/half-constant schedule), every architecture/compute constant, the gate-engine key mapping, all eight SPEC prohibitions, a sixteen-fit expected-cost budget, and the three named Swiss roll limitations carried forward verbatim"
  - "The pre-registration commit SHA (744c1c1d73a9e788a67768e2b397ad453045062a), resolved by the exact command topoae_train_run.py will re-run, and proved an ancestor of HEAD"
affects: [02.4-05, 02.4-06, 02.4-07, 02.4-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pre-register-then-prove-ancestry: the document is committed alone (no notebooks/ file in the same commit), then its adding SHA is re-resolved by `git log --diff-filter=A ... | tail -1` and proved with `git merge-base --is-ancestor <sha> HEAD`, never hard-coded, mirroring 02.2-PREREGISTRATION.md / 02-REFIT-PREREGISTRATION.md"
    - "A blocking checkpoint:decision returned to the user rather than auto-selected, even with a strongly-implied recommended option, when the plan is marked autonomous:false and auto mode is confirmed inactive"

key-files:
  created:
    - .planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-PREREGISTRATION.md
  modified: []

key-decisions:
  - "Task 1's blocking checkpoint:decision was returned to the user (auto mode confirmed inactive: workflow._auto_chain_active=false, workflow.auto_advance=false; plan marked autonomous:false) rather than auto-selected, per this plan's own <checkpoint_note>"
  - "Resolved: option-a -- freeze LAMBDA_TOPO=0.1 and the three planner-resolved constants (AMBIENT_DIST_NORM=none, one matched PlainAutoEncoder baseline per TopoAE fit = 8 baselines, quarter-warmup/quarter-ramp/half-constant with a linear ramp) exactly as proposed. option-b (widen the grid and re-sweep) and option-c (change a resolved constant) both declined."
  - "Coordinator-supplied reframing recorded in the pre-registration's Known Limitation 2: the lambda sweep grid (0.0, 0.1, 0.3, 1.0, 3.0) already spans the paper's own log-uniform-[0.1,3] searched range, so LAMBDA_TOPO=0.1 is the smallest lambda the authors themselves considered, not an arbitrary grid floor -- recorded alongside, not in place of, the fact that the reconstruction-degradation selection rule is itself mis-specified for this method"
  - "The pre-registration is committed in one commit containing no file under notebooks/ (verified via git show --stat), so the adding SHA is unambiguous for the ancestry proof"
  - "Ancestry SHA 744c1c1d73a9e788a67768e2b397ad453045062a resolved via git log --diff-filter=A --format=%H -- .planning/phases/02.4-.../02.4-PREREGISTRATION.md | tail -1, and git merge-base --is-ancestor 744c1c1d73a9e788a67768e2b397ad453045062a HEAD exits 0 -- proved, not asserted"

requirements-completed: [R3]

coverage:
  - id: D1
    description: "02.4-PREREGISTRATION.md fixes the three metric definitions, their numeric PASS thresholds, the latent-dimension ladder, the primary d, all architecture and compute constants, and the prohibited moves"
    requirement: R3
    verification:
      - kind: other
        ref: "python3 verify script checking all 19 required constant names present in 02.4-PREREGISTRATION.md (t1_topo_fidelity, t2_recon_margin, t3_rank_structure, THRESH_T1, THRESH_T2, THRESH_T3, LADDER, D_PRIMARY, LAMBDA_TOPO, WARMUP_FRAC, RAMP_FRAC, AMBIENT_DIST_NORM, LATENT_DIST_NORM, MAX_EPOCHS_CANDIDATES, WALLCLOCK_CEILING_S, SEEDS_PRIMARY, HOLDOUT_FRACTION, BATCH, 43cf438bc944c509) -- exits 0, missing: []"
        status: pass
      - kind: other
        ref: "grep -c \"THRESH_T1 = 0.90\" / \"THRESH_T2 = 1.00\" / \"THRESH_T3 = 0.90\" / \"D_PRIMARY = 20\" .planning/phases/02.4-*/02.4-PREREGISTRATION.md -- each returns 1"
        status: pass
    human_judgment: false
  - id: D2
    description: "The pre-registration is committed before any TopoAE fit commit, and git merge-base --is-ancestor <preregistration-commit> <first-fit-commit> succeeds"
    requirement: R3
    verification:
      - kind: other
        ref: "git log --diff-filter=A --format=%H -- .planning/phases/02.4-.../02.4-PREREGISTRATION.md | tail -1 -> 744c1c1d73a9e788a67768e2b397ad453045062a; git merge-base --is-ancestor 744c1c1... HEAD exits 0; git show --stat 744c1c1... lists only 02.4-PREREGISTRATION.md, no file under notebooks/"
        status: pass
    human_judgment: false
  - id: D3
    description: "Task 1's blocking checkpoint:decision resolved by the user (option-a, freeze as measured and as resolved) rather than assumed by the executor"
    requirement: R3
    verification: []
    human_judgment: true
    rationale: "A one-way decision (D-11/D-16 rated one-way; freezing LAMBDA_TOPO and three planner-resolved constants) that this plan's own checkpoint_note requires be returned to a human rather than auto-selected, even where the executor could identify a consistent-with-precedent option. The coordinator's response and decisive context are the human sign-off recorded in Section 0/10 of 02.4-PREREGISTRATION.md."

# Metrics
duration: "~1h (checkpoint round-trip: file reads, context gathering, checkpoint return, coordinator response, write, verify, commit, ancestry proof)"
completed: 2026-08-07
status: complete
---

# Phase 02.4 Plan 04: Pre-Registration Summary

**`02.4-PREREGISTRATION.md` fixes T1/T2/T3's definitions and thresholds (0.90/1.00/0.90), the {8,16,20,24,32,40} ladder at primary d=20, `LAMBDA_TOPO=0.1` frozen at a blocking checkpoint the user resolved (option-a), the three planner-resolved constants, and all eight SPEC prohibitions -- committed alone at `744c1c1` and proved a git ancestor of HEAD, so nothing after this commit is a choice.**

## Performance

- **Duration:** ~1h (checkpoint round-trip dominates: return the decision, receive the coordinator's response and decisive reframing, then write/verify/commit)
- **Started:** 2026-08-07T13:58:25Z
- **Completed:** 2026-08-07T14:05:40Z
- **Tasks:** 3 of 3 completed (Task 1's blocking checkpoint resolved by the coordinator's response)
- **Files modified:** 1 (new)

## Accomplishments

- Returned Task 1's blocking `checkpoint:decision` (freeze `LAMBDA_TOPO` + three constants) to the user rather than guessing, per this plan's `autonomous: false` frontmatter and its explicit `<checkpoint_note>` -- confirmed auto mode inactive first (`workflow._auto_chain_active: false`, `workflow.auto_advance: false`) via `gsd_run query config-get`
- Resolved: **option-a**, freezing `LAMBDA_TOPO = 0.1` and the three planner-resolved constants (`AMBIENT_DIST_NORM = "none"`, one matched `PlainAutoEncoder` baseline per TopoAE fit, quarter-warmup/quarter-ramp/half-constant schedule) exactly as proposed
- Wrote `02.4-PREREGISTRATION.md` (399 lines, 10 numbered sections) covering: T1/T2/T3 metric definitions and their worse-of-two-directions / baseline-relative construction; `THRESH_T1=0.90`, `THRESH_T2=1.00`, `THRESH_T3=0.90` with derivation arguments for each, including the explicit reason 02.2's discretionary 10% T2 margin is **not** inherited; `LADDER=(8,16,20,24,32,40)`, `D_PRIMARY=20`, `SEEDS_PRIMARY=(20260806,20260807,20260808)`; every architecture/compute constant (`FIT_KEY`, `HIDDEN`, `ACTIVATION`, `BATCH`, `WALLCLOCK_CEILING_S=3600`, `MAX_EPOCHS_CANDIDATES` as a rule not a number, `HOLDOUT_FRACTION`, `SPLIT_SEED`, etc.); the gate-engine positional-slot-remap key mapping; all eight `02.4-SPEC.md` prohibitions verbatim with verification; a sixteen-fit (8 TopoAE + 8 matched baselines) expected-cost budget with fit ordering; D-11's transfer risk, D-05's non-comparability, and the three named Swiss roll limitations carried forward from `02.4-03-SUMMARY.md`
- Recorded the coordinator's decisive reframing verbatim in Known Limitation 2: the lambda grid `(0.0, 0.1, 0.3, 1.0, 3.0)` already spans the paper's own log-uniform-`[0.1, 3]` searched range, so `LAMBDA_TOPO=0.1` is the smallest lambda the authors themselves considered -- stated alongside, not instead of, the fact that the reconstruction-degradation selection rule remains mis-specified for a method that trades reconstruction for topology by design
- Committed the pre-registration alone (`744c1c1`, no file under `notebooks/` in the same commit -- verified via `git show --stat`), then resolved its adding SHA with the exact command `topoae_train_run.py` will re-run and proved `git merge-base --is-ancestor 744c1c1d73a9e788a67768e2b397ad453045062a HEAD` exits 0
- Verified all acceptance criteria mechanically: the Task 2 verify script (19 required constant names) exits 0 with `missing: []`; `THRESH_T1 = 0.90`/`THRESH_T2 = 1.00`/`THRESH_T3 = 0.90`/`D_PRIMARY = 20` each grep-count exactly 1; `distortion` appears 7 times, every occurrence in a non-gating or comparison context; `git diff --quiet -- notebooks/pu_manifold/cae.py pyproject.toml src/effdim/` is clean

## Task Commits

Each task was committed atomically:

1. **Task 1: Freeze lambda and the three planner-resolved constants** - no code commit; resolved via a blocking `checkpoint:decision` returned to the coordinator, who selected option-a. Recorded in `02.4-PREREGISTRATION.md` Sections 0 and 10 rather than as a separate commit, since the decision itself produced no artifact until Task 2 wrote the document.
2. **Task 2 + Task 3: Write and commit `02.4-PREREGISTRATION.md`, resolve and prove its ancestry SHA** - `744c1c1` (docs) -- the plan's Task 2 action writes the file with no commit of its own, and Task 3's action is "commit the pre-registration and record its ancestry SHA," so both land in this single commit, exactly as the plan's own task split specifies.

**Plan metadata:** (this SUMMARY's own commit)

## Files Created/Modified

- `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-PREREGISTRATION.md` - the phase's pre-registration: T1/T2/T3 definitions and thresholds, ladder/primary-d/seeds, architecture/compute constants, gate-engine key mapping, all eight prohibited moves, expected-cost budget, stated risks and the three carried-forward Swiss roll limitations, ordering/ancestry commands, and the Task 1 ratification record

## Decisions Made

See `key-decisions` in frontmatter for the full record. In short: the blocking checkpoint was returned rather than guessed (per this plan's `autonomous: false` and explicit `<checkpoint_note>`, confirmed against the live auto-mode config rather than assumed); the user selected option-a, freezing `LAMBDA_TOPO=0.1` and the three planner-resolved constants unchanged; the coordinator's reframing of the lambda-selection-rule limitation (the grid floor is the paper's own minimum searched value) was recorded as an addition to, not a replacement of, the existing mis-specification finding.

## Deviations from Plan

None - plan executed exactly as written. Task 1's checkpoint was correctly returned rather than resolved unilaterally; Tasks 2-3's write/commit/ancestry-proof sequence matches the plan's action text section-for-section, including the constant names, threshold values, and derivation arguments it specifies verbatim.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

`02.4-PREREGISTRATION.md` is committed and its ancestry is proved: `git merge-base --is-ancestor 744c1c1d73a9e788a67768e2b397ad453045062a HEAD` exits 0, and no file under `notebooks/` is part of the adding commit. Plan `02.4-05`'s gated PU train runner (`topoae_train_run.py`) can now re-resolve this SHA at every invocation and raise on a nonzero ancestry check, and copy every named constant in Sections 1-4 verbatim into its own module-level constants block -- none of them may move without a fresh, separately-ratified pre-registration and a full re-run of all sixteen fits. No blockers.

---
*Phase: 02.4-topological-auto-encoder-validity-test-inserted*
*Completed: 2026-08-07*

## Self-Check: PASSED

- FOUND: .planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-PREREGISTRATION.md
- FOUND: 744c1c1 (Task 2+3 commit)
- FOUND: .planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-04-SUMMARY.md
