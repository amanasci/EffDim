---
phase: 02.4-topological-auto-encoder-validity-test-inserted
plan: 08
type: execute
wave: 7
depends_on: ["02.4-07"]
files_modified:
  - notebooks/diagnostics/topoae_reconcile_run.py
  - notebooks/pu_manifold/tests/test_topoae.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [R7]
user_setup: []

must_haves:
  truths:
    - "Running the reconciliation step twice produces one amendment file and one REQUIREMENTS.md change, with the second run reporting that it was already written"
    - "The amendment and the REQUIREMENTS.md traceability update are both existence-guarded, so a second run is a reported no-op"
    - "git diff --quiet on Phase 02.1's sealed artifacts is clean after the reconciliation step"
    - "On PASS the runner writes a NEW dated 02.1-AMENDMENT-03.md revisiting the falsifier reading only, and applies a marker-guarded REQUIREMENTS.md traceability update for the reinstated DEC and CURV requirements"
    - "On FAIL the reconciliation is a reported no-op: no amendment is written, no DEC or CURV requirement is reinstated, and 02.1's recommendation stands untouched"
    - "TOPO-01 through TOPO-08 exist in REQUIREMENTS.md with a traceability row each, regardless of the verdict"
    - "The idempotency test exercises tmp_path-scoped fixtures and never the real repository files"
  artifacts:
    - notebooks/diagnostics/topoae_reconcile_run.py
    - .planning/REQUIREMENTS.md
    - ".planning/phases/02.1-geometry-representation-research/02.1-AMENDMENT-03.md (PASS only)"
  key_links:
    - "The reconciliation runner reads topoae_verdict_{fit_key}.json and refuses to act unless its recorded verdict is PASS — it never recomputes a verdict of its own"
    - "The REQUIREMENTS.md edit is bounded by a marker comment pair, so the guard is a string search rather than a diff heuristic"
    - "The amendment is a new dated file; no sealed 02.1 measurement, artifact, or prior amendment is opened for writing"
  prohibitions:
    - statement: "MUST NOT retract, edit, or overwrite any sealed Phase 02.1 measurement or artifact when writing the PASS amendment"
      status: resolved
      verification: "test — git diff --quiet on 02.1's sealed artifacts after the amendment step; the amendment is a new dated file"
    - statement: "MUST NOT treat a FAIL as an error to retry, downgrade, or suppress"
      status: resolved
      verification: "judgment — the runner's FAIL branch is a reported no-op that leaves 02.1's recommendation standing; reviewed at phase seal"
---

<!-- planner-discipline-allow: .cache -->

<objective>
Build `notebooks/diagnostics/topoae_reconcile_run.py` — the R7 reconciliation step — add the
`TOPO-01` .. `TOPO-08` requirement family to `.planning/REQUIREMENTS.md`, and prove the
reconciliation is idempotent with a `tmp_path`-scoped test.

Purpose: `02.4-RESEARCH.md` Pitfall 9 and `02.4-PATTERNS.md` both flag this as the one piece of
this phase with **no precedent in the codebase** — every prior runner wrote only to the gitignored
cache, and this one writes tracked `.planning/` files. That makes it genuinely new engineering,
not a copy, and its idempotence has to be a tested acceptance criterion rather than an intention.

The step is conditional by design. On PASS it writes a new dated amendment revisiting 02.1's
falsifier *reading* and reinstates the DEC and CURV traceability. On FAIL it is a reported no-op
and 02.1's graph-native recommendation stands entirely untouched. Both are correct outcomes.

Output: `notebooks/diagnostics/topoae_reconcile_run.py`, the TOPO requirement family, the
idempotency test, and — on PASS only — `02.1-AMENDMENT-03.md` and the traceability update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-SPEC.md
@.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-RESEARCH.md
@.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-PATTERNS.md
@.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-07-SUMMARY.md
@.planning/phases/02.1-geometry-representation-research/02.1-AMENDMENT-02.md
@notebooks/pu_manifold/cache.py
</context>

<artifacts_this_phase_produces>
Created by this plan (see `02.4-01-PLAN.md` for the whole-phase list):

- `notebooks/diagnostics/topoae_reconcile_run.py` — `load_verdict`,
  `write_amendment_if_absent`, `apply_traceability_update_if_absent`, `reconcile`
- `.planning/REQUIREMENTS.md` § "Topological Autoencoder Validity (TOPO)" with `TOPO-01` ..
  `TOPO-08`, plus eight traceability rows
- `.planning/phases/02.1-geometry-representation-research/02.1-AMENDMENT-03.md` (PASS only)
- Marker comment pair `<!-- topoae-reconciliation:start -->` / `<!-- topoae-reconciliation:end -->`
  bounding the PASS-only traceability update inside `REQUIREMENTS.md`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Mint the TOPO-01..08 requirement family</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` in full — in particular § "Chart Autoencoder Validity (CAE)" lines 50-61 (the section shape, the dated introductory paragraph, the `- [x] **CAE-0n**:` bullet form) and § Traceability lines 145-192 (the three-column table and the CAE rows' formatting)
    - `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-SPEC.md` § Requirements (the eight requirements these IDs mirror) and § Boundaries (TOPO-xx IDs are in scope)
    - `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-RESEARCH.md` § Phase Requirements (the instruction to mint `TOPO-01..08` in R1..R8 order)
    - `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-07-SUMMARY.md` (the verdict)
  </read_first>
  <action>
Add a `### Topological Autoencoder Validity (TOPO)` section to `.planning/REQUIREMENTS.md`,
immediately after the existing CAE section and before `### Decoder (DEC)`, following the CAE
section's shape exactly: a dated introductory sentence saying the family was added with Phase 02.4
(INSERTED) to empirically test whether the Topological Auto-Encoder of arXiv:1906.00722 — which
optimises topological signature matching rather than distance preservation — yields a
representation Phase 3 can validly decode from, then eight `- [x] **TOPO-0n**:` bullets.

Mint `TOPO-01` through `TOPO-08` in the order of `02.4-SPEC.md`'s own eight requirements, each
bullet stating the requirement in the same declarative register the CAE bullets use:

- `TOPO-01` — 0-dimensional persistence pairings computed from the minimum spanning tree of each
  batch's pairwise distance matrix in both input and latent space, ties broken lexicographically
  on distance then row index then column index, combined into the paper's topological loss.
- `TOPO-02` — a Topological Auto-Encoder trained on the frozen Phase 1 10,000-row normalized
  subsample, reproducible from recorded seeds, C2-smooth throughout encoder and decoder, across a
  pre-registered latent-dimension ladder, reusing `cae.py` by import and never editing it.
- `TOPO-03` — gate metric definitions and numeric thresholds pre-registered and committed before
  any fit runs, with the ordering proved by git ancestry rather than asserted.
- `TOPO-04` — exactly three gating metrics: topological fidelity, reconstruction margin against a
  matched-capacity plain autoencoder, and a rank-structure measure; geodesic distortion and the
  paper's unfaithfulness and coverage measures reported for comparability and never gating.
- `TOPO-05` — `topoae_verdict_{fit_key}.json` written on PASS and FAIL alike, self-contained and
  reproducible from its own recorded metrics and thresholds, with an absent or non-finite gating
  metric halting the run and writing nothing.
- `TOPO-06` — a Phase 3 handoff written on PASS only, with any existing handoff at that fit key
  actively deleted on FAIL rather than merely not written.
- `TOPO-07` — on PASS, a new dated amendment revisiting Phase 02.1's falsifier reading and a
  marker-guarded traceability update for the reinstated DEC and CURV requirements, both
  existence-guarded so a second run is a reported no-op and no sealed 02.1 artifact is edited.
- `TOPO-08` — the mandatory Swiss roll sanity check, `notebooks/02.4_swiss_roll_topoae_check.ipynb`,
  importing the model unchanged and compared against a matched `cae.PlainAutoEncoder` baseline at
  the same 2-D bottleneck.

Then add eight rows to the § Traceability table, immediately after the `CAE-07` row and before the
`DEC-01` row, each formatted `| TOPO-0n | Phase 02.4 | Complete |` in the table's existing
three-column form. Do not renumber, reorder, or otherwise touch any existing row — this edit is
additive and scoped. Use `Edit` with a narrow anchor, never a whole-file rewrite.

This section is added regardless of the verdict: `02.4-SPEC.md` § Boundaries puts the TOPO IDs in
scope unconditionally, and the DEC/CURV reinstatement in Task 3 is the only part that is
PASS-only.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; python -c "import re; s=open('.planning/REQUIREMENTS.md').read(); assert '### Topological Autoencoder Validity (TOPO)' in s; ids=sorted(re.findall(r'\*\*TOPO-0(\d)\*\*', s)); assert ids==[str(i) for i in range(1,9)], ids; rows=sorted(re.findall(r'\|\s*TOPO-0(\d)\s*\|\s*Phase 02.4\s*\|', s)); assert rows==[str(i) for i in range(1,9)], rows; assert all(k in s for k in ['CAE-07','DEC-01','CURV-08','GEOM-01']); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `### Topological Autoencoder Validity (TOPO)` with eight `**TOPO-0n**` bullets numbered 1 through 8
    - The § Traceability table contains eight `| TOPO-0n | Phase 02.4 | ... |` rows
    - Every pre-existing requirement ID still appears — `git diff .planning/REQUIREMENTS.md` shows only additions and zero deleted lines
    - The TOPO section sits between the CAE section and the DEC section, and the TOPO rows sit between the `CAE-07` and `DEC-01` rows
    - `git diff --numstat .planning/REQUIREMENTS.md` reports 0 deletions
  </acceptance_criteria>
  <done>Eight TOPO requirement IDs exist with matching traceability rows, added additively between the CAE and DEC families with no existing line deleted or renumbered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The conditional reconciliation runner and its idempotency test</name>
  <files>notebooks/diagnostics/topoae_reconcile_run.py, notebooks/pu_manifold/tests/test_topoae.py</files>
  <read_first>
    - `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-SPEC.md` § Requirements R7, § Prohibitions (the sealed-02.1 prohibition), § Acceptance Criteria
    - `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-RESEARCH.md` Pitfall 9 — no existing runner writes tracked `.planning/` files
    - `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-PATTERNS.md` § "Reconciliation runner (R7) — no direct precedent" and § "No Analog Found"
    - `.planning/phases/02.1-geometry-representation-research/02.1-AMENDMENT-02.md` in full — the prose and structure template for the amendment's *content* (read manually; the write mechanics are new), including §6.4's decoupling finding and §6.5's unrun seed-sensitivity study
    - `notebooks/diagnostics/cae_evaluate_run.py` — the nearest structural cousin, its STEP 8 PASS/FAIL branch, which writes only to the gitignored cache
    - `notebooks/pu_manifold/cache.py` (`cache_path`, `json_cache`)
    - `notebooks/pu_manifold/tests/test_cae.py` — the `tmp_path` / `monkeypatch` discipline every cache-touching test already follows
  </read_first>
  <behavior>
    - Test 1 `test_reconciliation_idempotent`: against a `tmp_path`-scoped fake planning tree holding a stub `REQUIREMENTS.md` and a PASS verdict JSON, `reconcile` run twice produces exactly one amendment file and exactly one `REQUIREMENTS.md` change, and the second run's returned result reports both steps as already written.
    - Test 2 `test_reconciliation_noop_on_fail`: with a FAIL verdict, `reconcile` writes no amendment, makes no `REQUIREMENTS.md` change, and returns a result naming the verdict as the reason.
    - Test 3 `test_reconciliation_refuses_without_verdict_artifact`: with no verdict JSON present, `reconcile` raises `FileNotFoundError` rather than assuming either outcome.
    - Test 4 `test_reconciliation_never_opens_sealed_artifacts_for_writing`: the fake tree's sealed 02.1 files are byte-identical before and after a PASS reconcile, compared by content hash.
    - Test 5 `test_traceability_marker_guard_is_a_string_search`: a `REQUIREMENTS.md` already containing the end marker is left untouched even when its body differs, proving the guard is the marker and not a diff heuristic.
  </behavior>
  <action>
Create `notebooks/diagnostics/topoae_reconcile_run.py`. This is the one piece of the phase with no
codebase analog — every prior runner wrote only to the gitignored cache, and this one writes
tracked `.planning/` files. Treat it as new, careful code, and parameterise every path by a
`planning_root` argument defaulting to the repository's `.planning/`, so the tests can drive it
against a `tmp_path` tree without ever touching the real files.

Module docstring: what it does, why it is conditional, the invocation
`PYTHONPATH=notebooks python notebooks/diagnostics/topoae_reconcile_run.py`, and the explicit
statement that it never edits, retracts, or overwrites any sealed Phase 02.1 measurement,
artifact, or prior amendment — the amendment it may write is a new dated file, and the
`REQUIREMENTS.md` change is bounded by markers.

`load_verdict(fit_key)` — read `topoae_verdict_{fit_key}.json` through `cache.cache_path`. Raise
`FileNotFoundError` naming the path when absent: the runner never assumes an outcome and never
recomputes a verdict of its own.

`write_amendment_if_absent(planning_root, verdict_payload, today)` — when the recorded verdict is
not `"PASS"`, return a result reporting the no-op and its reason without touching the filesystem.
When it is `"PASS"` and `02.1-AMENDMENT-03.md` already exists under
`planning_root/phases/02.1-geometry-representation-research/`, return a result reporting
"already written" and touch nothing. Otherwise write that new dated file, opened in an
exclusive-create mode so an existing file could never be clobbered even if the guard were
bypassed. Its content revisits the **falsifier reading only**, using `02.1-AMENDMENT-02.md` as the
prose template: the date; that Phase 02.4's gate returned PASS, with its three gate values and
thresholds; that 02.1's pre-registration carried a symmetric falsifier overturning the
graph-native answer when a coordinate candidate's distortion drops materially below the classical
failure at a tractable working dimension; and then, plainly, that this phase's gate is **not**
that statistic — it measures topological signature agreement, while 02.1's ~0.0796 wall was
measured on distance preservation — so the amendment argues about the reading rather than claiming
the falsifier's literal condition was met, and names that difference as the thing a reader must
weigh. Then the DEC and CURV requirement IDs a coordinate-producing win reinstates, and a closing
statement that no sealed 02.1 measurement, artifact, or prior amendment is retracted, edited, or
overwritten by this file. Open no other path under that directory for writing.

`apply_traceability_update_if_absent(planning_root, verdict_payload)` — PASS-only, guarded by a
string search for `<!-- topoae-reconciliation:end -->` in `REQUIREMENTS.md`. When the marker is
present, return "already written" and touch nothing. Otherwise append, inside a fresh marker pair
`<!-- topoae-reconciliation:start -->` / `<!-- topoae-reconciliation:end -->`, a dated note
recording that Phase 02.4 PASSed and that the DEC and CURV requirements a coordinate-producing
representation reinstates are back in scope for Phase 3, listing the IDs. Read, append, write —
never rewrite unrelated content, never renumber a row, never delete a line. A marker-string guard
is deliberately chosen over any diff or content heuristic so the idempotency check is exact and
testable.

`reconcile(fit_key, planning_root=None, today=None)` — compose the three, print one clear line per
step (written, already written, or skipped, and why), and return a structured result carrying the
verdict, both step outcomes, and the paths involved. On a non-PASS verdict, print that the
reconciliation is a no-op **by design**, that 02.1's graph-native recommendation stands untouched,
and that a FAIL is a complete, reportable outcome of this phase rather than an error to retry,
downgrade, or suppress.

Add the five tests named in `<behavior>` to `test_topoae.py`. Each builds a `tmp_path`-scoped fake
planning tree — a stub `REQUIREMENTS.md`, and a stub sealed 02.1 directory holding a couple of
files whose content hashes are recorded before and after — and passes it as `planning_root`. None
may exercise the real repository files, and every cache read goes through
`monkeypatch.setattr(cache, "CACHE_DIR", tmp_path)`.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; python -m pytest notebooks/pu_manifold/tests/test_topoae.py -q -k "reconcil or traceability" &amp;&amp; python -m pytest notebooks/pu_manifold/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest notebooks/pu_manifold/tests/test_topoae.py::test_reconciliation_idempotent -x` exits 0
    - `python -m pytest notebooks/pu_manifold/tests/test_topoae.py::test_reconciliation_noop_on_fail -x` exits 0
    - `python -m pytest notebooks/pu_manifold/tests/test_topoae.py::test_reconciliation_refuses_without_verdict_artifact -x` exits 0
    - `python -m pytest notebooks/pu_manifold/tests/test_topoae.py::test_reconciliation_never_opens_sealed_artifacts_for_writing -x` exits 0
    - `python -m pytest notebooks/pu_manifold/tests/test_topoae.py::test_traceability_marker_guard_is_a_string_search -x` exits 0
    - `notebooks/diagnostics/topoae_reconcile_run.py` defines `load_verdict`, `write_amendment_if_absent`, `apply_traceability_update_if_absent` and `reconcile`, and each of the last three accepts a `planning_root` argument
    - `grep -c "planning_root" notebooks/pu_manifold/tests/test_topoae.py` returns at least 5 — every reconciliation test drives a temporary tree
    - `git status --porcelain .planning/phases/02.1-geometry-representation-research/` is empty after the test run — the tests never touched the sealed directory
    - `python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
  </acceptance_criteria>
  <done>A conditional reconciliation runner exists whose idempotence, no-op-on-FAIL behaviour, refusal without a verdict, and hands-off treatment of sealed artifacts are each proved by a test running against a temporary planning tree rather than the real one.</done>
  <reversibility rating="costly">The runner writes tracked files. A wrong amendment would have to be corrected by a further dated amendment rather than an edit, since retracting a written amendment is exactly what the sealed-artifact prohibition forbids — which is why the existence guard and these tests come before the first real invocation.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Run the reconciliation for real, twice, and record the phase read-out</name>
  <files>notebooks/diagnostics/topoae_reconcile_run.py, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `notebooks/diagnostics/topoae_reconcile_run.py` (as written by Task 2)
    - `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-07-SUMMARY.md` — the verdict, the three gate values against their thresholds, the per-seed spread, and the T1/T2 frontier table
    - `notebooks/.cache/topoae_verdict_43cf438bc944c509.json` — the artifact the runner reads
    - `.planning/REQUIREMENTS.md` (as edited by Task 1)
    - `.planning/ROADMAP.md` § Phase 02.4 — the two obligations a PASS triggers, and the statement that a FAIL is a complete, reportable outcome rather than an error to work around
  </read_first>
  <precondition>`notebooks/.cache/topoae_verdict_43cf438bc944c509.json` exists and its recorded verdict has been read and approved at plan 02.4-07's blocking human-verify checkpoint.</precondition>
  <action>
Run `PYTHONPATH=notebooks python notebooks/diagnostics/topoae_reconcile_run.py` against the real
planning tree, then run it a **second** time and confirm both steps report "already written" or
"skipped" and that nothing changed on the second pass. The second invocation is the acceptance
criterion itself, not a formality — R7's own criterion is that two runs yield one amendment file
and one `REQUIREMENTS.md` change.

Then verify the sealed-artifact prohibition mechanically. `git status --porcelain` over
`.planning/phases/02.1-geometry-representation-research/` must show, on the PASS path, exactly one
added file — `02.1-AMENDMENT-03.md` — and nothing else; and on the FAIL path, nothing at all. No
prior amendment, recommendation, survey, or pre-registration in that directory may show a
modification.

Record the phase read-out in the plan summary, honestly and in whichever direction the verdict
went:

- **On PASS** — the amendment path, the reinstated DEC and CURV requirement IDs, and a plain
  statement that Phase 3 is unblocked and now has two candidate representations on the record
  (this phase's TopoAE latent and 02.1's graph-native recommendation), with the head-to-head
  between them explicitly out of this phase's scope and belonging to the amendment.
- **On FAIL** — that the reconciliation was a no-op by design, that 02.1's graph-native
  recommendation stands untouched, that Phase 3 remains blocked, and what the phase measured
  anyway: the three gate values, the per-seed spread at the primary rung, and the T1/T2 frontier
  across the ladder, which is informative about where this method sits on the
  reconstruction-versus-topology tradeoff on this data regardless of how the gate went. A FAIL is
  a complete, reportable outcome — the third this milestone has produced, and like the other two
  it is a result rather than an error to retry, downgrade, or suppress.

Also record the phase-level static guarantees, re-checked one last time: `cae.py` byte-identical
to its pre-phase state, `pyproject.toml` and `src/effdim/` byte-identical, `02.4-PREREGISTRATION.md`
byte-identical since its commit and still a proven git ancestor of HEAD, the full
`notebooks/pu_manifold/tests/` suite green, and `notebooks/02.4_swiss_roll_topoae_check.ipynb`
committed with outputs and zero error cells.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim &amp;&amp; PYTHONPATH=notebooks python notebooks/diagnostics/topoae_reconcile_run.py 2>&amp;1 | tail -20 &amp;&amp; echo "--- second run ---" &amp;&amp; PYTHONPATH=notebooks python notebooks/diagnostics/topoae_reconcile_run.py 2>&amp;1 | tail -20 &amp;&amp; git status --porcelain .planning/phases/02.1-geometry-representation-research/ &amp;&amp; git diff --quiet -- notebooks/pu_manifold/cae.py pyproject.toml src/effdim/ .planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-PREREGISTRATION.md &amp;&amp; python -m pytest notebooks/pu_manifold/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Both invocations exit 0, and the second prints "already written" or "skipped" for each of the two steps
    - Exactly one `02.1-AMENDMENT-03.md` exists after two runs on the PASS path, and none exists on the FAIL path
    - `.planning/REQUIREMENTS.md` contains exactly one `<!-- topoae-reconciliation:start -->` and exactly one `<!-- topoae-reconciliation:end -->` after two runs on the PASS path, and neither marker on the FAIL path
    - `git status --porcelain .planning/phases/02.1-geometry-representation-research/` shows at most one added file and zero modified files
    - `git diff --quiet -- notebooks/pu_manifold/cae.py pyproject.toml src/effdim/ .planning/phases/02.4-*/02.4-PREREGISTRATION.md` exits 0
    - `git merge-base --is-ancestor <prereg SHA> HEAD` still exits 0
    - `python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
    - The plan summary records the verdict, the three gate values against their thresholds, the per-seed spread, the frontier table, and which reconciliation branch ran
  </acceptance_criteria>
  <done>The reconciliation has run twice with the second pass a reported no-op, the sealed 02.1 directory shows at most one added file and no modifications, and the phase read-out is recorded in whichever direction the verdict went.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verdict artifact → tracked `.planning/` writes | This is the only runner in the codebase that writes tracked files; a wrong read of the verdict turns into a committed claim about a sealed phase |
| runner → sealed Phase 02.1 directory | Every file in that directory is sealed evidence; a single overwrite destroys a measurement no later phase can reproduce |
| repeated invocation → duplicated record | Without an exact guard, a second run silently duplicates an amendment or a traceability block, and the register stops being a register |
| `planning_root` argument → real repository | A test that forgot to pass a temporary tree would exercise the live `.planning/` files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.4-07 | Tampering | a sealed Phase 02.1 measurement, artifact, or prior amendment overwritten by the amendment step | high | mitigate | `02.1-AMENDMENT-03.md` is a new dated file opened in exclusive-create mode, and no other path under that directory is opened for writing; `git status --porcelain` on the sealed directory is asserted to show at most one added file and zero modified files; `test_reconciliation_never_opens_sealed_artifacts_for_writing` compares content hashes before and after |
| T-02.4-21 | Tampering | a second run duplicating the amendment or the traceability block | high | mitigate | The amendment step is guarded by a file-existence check and the traceability step by an exact search for `<!-- topoae-reconciliation:end -->`; both report "already written" and touch nothing. Task 3 runs the real reconciliation twice, and `test_reconciliation_idempotent` proves it on a temporary tree first |
| T-02.4-22 | Spoofing | reconciliation acting on an assumed rather than a recorded verdict | high | mitigate | `load_verdict` raises `FileNotFoundError` when the artifact is absent and the runner never recomputes a verdict of its own; the FAIL branch is a reported no-op, proved by `test_reconciliation_noop_on_fail` and `test_reconciliation_refuses_without_verdict_artifact` |
| T-02.4-23 | Tampering | the `REQUIREMENTS.md` edit clobbering unrelated rows | medium | mitigate | Both the Task 1 section addition and the runner's marker block are append-only against a narrow anchor; `git diff --numstat .planning/REQUIREMENTS.md` is asserted to report zero deletions |
| T-02.4-09 | Tampering | a test writing into the real planning tree or the real cache | medium | mitigate | Every reconciliation test passes a `tmp_path`-derived `planning_root` and monkeypatches the cache directory; `git status --porcelain` on the sealed directory is asserted empty after the test run |
| T-02.4-04 | Tampering / Information Disclosure | cache stem composition when reading the verdict | medium | mitigate | `load_verdict` resolves its path through `cache.cache_path`, so `cache._assert_inside_cache` applies; no raw path is composed |
| T-02.4-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package — the runner uses only the standard library plus the already-pinned `pu_manifold.cache`. Any install would first need a blocking `checkpoint:decision`; slopcheck plus a blocking human checkpoint applies to any `[ASSUMED]`/`[SUS]` package |
</threat_model>

<verification>
- `python -m pytest notebooks/pu_manifold/tests/ -q` exits 0, including all five reconciliation tests
- `PYTHONPATH=notebooks python notebooks/diagnostics/topoae_reconcile_run.py` run twice exits 0 both times, and the second run reports each step as already written or skipped
- Exactly one `02.1-AMENDMENT-03.md` exists after two runs on the PASS path; none on the FAIL path
- `.planning/REQUIREMENTS.md` carries `TOPO-01` .. `TOPO-08` with eight traceability rows, and at most one `<!-- topoae-reconciliation:end -->` marker
- `git diff --numstat .planning/REQUIREMENTS.md` reports zero deletions
- `git status --porcelain .planning/phases/02.1-geometry-representation-research/` shows at most one added file and zero modified files
- `git diff --quiet -- notebooks/pu_manifold/cae.py pyproject.toml src/effdim/ .planning/phases/02.4-*/02.4-PREREGISTRATION.md` exits 0
- `git merge-base --is-ancestor <prereg SHA> HEAD` exits 0
</verification>

<success_criteria>
The requirement register carries the eight TOPO IDs and their traceability rows regardless of the
verdict. The reconciliation obligation is discharged exactly once and is provably a no-op on a
second run — on PASS as a new dated amendment revisiting 02.1's falsifier *reading* plus a
marker-guarded DEC/CURV reinstatement, on FAIL as a reported no-op that leaves 02.1's
recommendation entirely untouched. Not one sealed Phase 02.1 file was opened for writing, and the
phase's static guarantees — `cae.py`, `pyproject.toml`, `src/effdim/` and the pre-registration all
byte-identical — hold at the end as they did at the start.
</success_criteria>

<output>
Create `.planning/phases/02.4-topological-auto-encoder-validity-test-inserted/02.4-08-SUMMARY.md` when done
</output>
