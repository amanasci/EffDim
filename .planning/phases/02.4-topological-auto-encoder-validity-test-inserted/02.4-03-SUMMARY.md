---
phase: 02.4-topological-auto-encoder-validity-test-inserted
plan: 03
subsystem: ml-training
tags: [pytorch, topological-data-analysis, swiss-roll, sanity-check, fidelity-correction]

# Dependency graph
requires:
  - phase: 02.4-topological-auto-encoder-validity-test-inserted (plans 01-02)
    provides: "notebooks/pu_manifold/topoae.py's persistence_pairs, topological_loss, train_topoae, the T1/T2/T3 gate engine"
  - phase: 02.2-chart-autoencoder-validity-test-inserted
    provides: "cae.PlainAutoEncoder, cae.train_plain_ae, cae.reconstruction_stats (imported unmodified, cae.py never edited)"
provides:
  - "notebooks/diagnostics/topoae_lambda_sweep_run.py: a lambda-selection sweep on the Swiss roll fixture, its binding selection rule written before any lambda is measured, and a transfer diagnostic recorded per arm"
  - "notebooks/02.4_swiss_roll_topoae_check.ipynb: the CLAUDE.md-mandated Swiss roll sanity check for the Topological Auto-Encoder, including a baseline-relative topological structural check"
  - "A fidelity correction to notebooks/pu_manifold/topoae.py's train_topoae/topological_loss, bringing the training loss into agreement with the paper (arXiv:1906.00722) and its reference implementation, discovered by this plan's own checkpoint"
affects: [02.4-04, 02.4-05, 02.4-06, 02.4-07, 02.4-08]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A jointly-trained scalar latent-distance-scale parameter (torch.nn.Parameter, float64, added to the model's own AdamW parameter group), matching the paper's self.latent_norm rather than the module's pre-existing latent_unit_scale (a separate, deliberately non-fitted statistic reserved for the T1 gate)"
    - "Per-batch ambient-distance normalization (d_x / d_x.max()) inside the training loop only -- the T1 gate deliberately keeps the whole-held-out-set ambient distance unnormalized, since a per-batch maximum has no single value over a whole set and would break cross-model comparability of the gate"
    - "Baseline-relative structural comparison: the same ambient MST pairing (computed once from held-out data alone) is applied to both the model latent and the matched-baseline latent, so a scale-free correlation statistic is directly comparable between the two rather than gated on an arbitrary absolute bound"

key-files:
  created:
    - notebooks/diagnostics/topoae_lambda_sweep_run.py
    - notebooks/02.4_swiss_roll_topoae_check.ipynb
  modified:
    - notebooks/pu_manifold/topoae.py
    - notebooks/pu_manifold/tests/test_topoae.py

key-decisions:
  - "Fidelity correction (2026-08-07, mid-plan, following an unapproved Swiss roll checkpoint): notebooks/pu_manifold/topoae.py's train_topoae did not faithfully translate the paper (Moor, Horn, Rieck, Borgwardt, ICML 2020, arXiv:1906.00722) or its reference implementation (github.com/BorgwardtLab/topological-autoencoders). Four gaps fixed: (1) missing jointly-trained latent_norm scale, now a float64 torch.nn.Parameter in the model's own AdamW group, final value returned as latent_norm_final; (2) missing per-batch d_x / d_x.max() ambient normalization in training (the T1 gate deliberately keeps this unnormalized, documented as intentional, not an oversight); (3) a spurious /batch_size division on the topo term, removed -- the reference's sig_error has no such division and the prior docstring's claim that this was 'the paper's own convention' was false; (4) a missing 1/2 factor on each directional term in topological_loss, added per the paper's L_{X->Z} := (1/2)||...||^2 definition"
  - "Float64 (vs. the reference's float32) is unchanged and reconfirmed as a deliberate, retained determinism divergence -- not touched by the fidelity correction"
  - "The 1/2 factor is a uniform multiplicative constant on both directional terms and therefore cancels in T1's baseline-relative gate ratio -- proved with a dedicated test (test_t1_gate_value_is_invariant_to_the_one_half_factor) rather than assumed"
  - "Gaps 3+4 compounded to roughly a 32x lambda-scale error at batch 64: the original sweep's grid {0.1 .. 2.0} was actually probing an effective lambda of roughly {0.003 .. 0.06}, never entering the paper's own log-uniform-[0.1, 3] searched range. The grid was redrawn to {0.0, 0.1, 0.3, 1.0, 3.0} -- log-uniform over [0.1, 3] with a 0.0 control arm -- and re-swept under the corrected loss, applying the SAME selection rule (not retuned)"
  - "Re-measured selection is again the grid floor, lambda = 0.1 -- no positive lambda satisfies the rule's <=10% reconstruction-degradation bound even under the corrected loss. This is now the measured result under a faithful implementation, not an artifact of the four gaps"
  - "Second checkpoint round: the notebook's topological structural check (persistence-pair edge-length correlation) was gated on an absolute bound (r > 0.8) with no baseline, while CLAUDE.md mandates a matched-baseline comparison for every check. Fixed: the same ambient MST pairing (computed once from held-out data) is now applied to both the TopoAE latent and the already-trained matched plain-AE latent, both correlations are printed and plotted side by side, and the gate is now baseline-relative (TopoAE's r must beat the plain-AE's r), with the absolute bound retained as a secondary reported line"
  - "With the baseline in hand: TopoAE r=0.680 vs plain-AE r=0.471 -- TopoAE clearly beats the baseline on the topological structural check (the thing the method exists to do) while losing to it on plain MSE reconstruction (ratio 1.382, TopoAE worse). Read as the trade the method makes on purpose -- the paper itself evaluates with KL divergence of density estimates and trustworthiness/continuity, never MSE -- reported plainly, not tuned toward this reading; the numbers were measured, not selected"
  - "Task 4's blocking human-verify checkpoint remains OPEN as of this SUMMARY. Two rounds of checkpoint feedback have been addressed (a training-loss fidelity correction, then a missing-baseline correction); the current, most complete measurement shows a genuine topology win alongside a reconstruction loss, which is one of three legitimate outcomes named by the coordinator's own dispatch. This SUMMARY documents both correction rounds under explicit coordinator directives and returns the honest result to the still-open checkpoint -- it does not itself resolve Task 4"

requirements-completed: []

coverage: []

# Metrics
duration: "~10h25m wall-clock across two sessions (2026-08-06 23:18 -> 2026-08-07 09:43), most of it two unattended ~20-25min background lambda-sweep runs (300 epochs x 5-6 arms each) plus two interactive round-trips with the orchestrator (a training-loss fidelity correction, then a missing-baseline correction); active execution time is a small fraction of the wall-clock span"
completed: 2026-08-07
status: blocked-checkpoint
---

# Phase 02.4 Plan 03: Swiss Roll Sanity Check + Two Mid-Plan Corrections Summary

**Built the lambda-selection sweep and the CLAUDE.md-mandated Swiss roll sanity check for the Topological Auto-Encoder; two rounds of checkpoint feedback then surfaced (1) a training-loss fidelity gap against the paper/reference implementation and (2) a missing baseline on the notebook's topological structural check -- with both fixed, TopoAE clearly beats the matched plain-AE baseline on the topological check (r=0.680 vs 0.471) while losing to it on plain MSE reconstruction, and Task 4's blocking checkpoint is still open pending a human decision on that reading.**

## Performance

- **Duration:** ~10h25m wall-clock (2026-08-06T23:18 -> 2026-08-07T09:43), dominated by two unattended ~20-25min background lambda-sweep runs and two interactive orchestrator round-trips; see frontmatter for the breakdown
- **Started:** 2026-08-06T23:18:06-04:00
- **Latest commit:** 2026-08-07T09:43:18-04:00
- **Tasks:** 3 of 4 plan tasks completed (Task 4, the blocking checkpoint, remains open); 4 additional deviation commits across two correction rounds
- **Files modified:** 4 (2 created by Tasks 1/3, 2 modified by the fidelity correction)

## Accomplishments

- Implemented `notebooks/diagnostics/topoae_lambda_sweep_run.py`: a lambda-selection sweep on the Swiss roll fixture with its binding selection rule written into the module docstring and `LAMBDA_SELECT_RULE` before any lambda was measured, a transfer diagnostic (topological-term/reconstruction-term ratio) recorded per arm, and no git-ancestry precondition (D-11/D-13/A4 permit this, since the Swiss roll fixture is not the gate data)
- Ran the sweep three times across two grid versions (cache-hit confirmed on every re-invocation) and recorded the selected lambda and the full per-arm table in this SUMMARY
- Built `notebooks/02.4_swiss_roll_topoae_check.ipynb` following `02.2_swiss_roll_cae_check.ipynb`'s 15-cell shape: imports `topoae`/`cae` unchanged, generates the fixture in-notebook with the sweep's exact constants, trains a matched `cae.PlainAutoEncoder`/`cae.train_plain_ae` baseline at the same 2-D bottleneck, plots 3-D and x-z reconstructions coloured by arc-length `t`, reports held-out reconstruction stats and a persistence-pair edge-length agreement diagnostic (now baseline-relative), and ends with printed pass/fail lines plus a read-out -- executed end to end in under 110s with zero error cells, across three runs
- **Correction round 1, following the first (unapproved) Swiss roll checkpoint:** the orchestrator independently re-read the paper (arXiv:1906.00722) and its reference implementation (github.com/BorgwardtLab/topological-autoencoders) against `topoae.py`'s `train_topoae`/`topological_loss` and found four fidelity gaps (see Key Decisions). All four were fixed in `topoae.py`, covered by 4 new tests (31 tests in this file, 106 in the full `notebooks/pu_manifold/tests/` suite, all green), the sweep was re-run over the paper's actual log-uniform-[0.1, 3] search range, and the notebook was re-executed against the corrected implementation. A printed-label/value mismatch found independently by the orchestrator was fixed at the same time
- **Correction round 2, following the second (still unapproved) Swiss roll checkpoint:** the orchestrator identified that the notebook's topological structural check had no matched baseline (an absolute `r > 0.8` bound only), unlike the two reconstruction checks. Fixed: the same ambient MST pairing is now applied to both the TopoAE latent and the already-trained plain-AE latent, both correlations are printed and plotted side by side, and the gate is now baseline-relative
- With both corrections in place: **TopoAE beats the matched plain-AE baseline on the topological structural check (r=0.680 vs 0.471) while losing to it on reconstruction (ratio 1.382)** -- visually confirmed via the side-by-side scatter plots, not an artifact of the new measurement code

## Task Commits

Each task/deviation step was committed atomically:

1. **Task 1: Lambda sweep runner on the Swiss roll fixture** - `3f1709e` (feat)
2. **Task 2: Run the sweep and record the selected lambda** - no new commit (script unchanged; execution results superseded by the round-1 re-sweep below)
3. **Task 3: The mandatory Swiss roll sanity-check notebook** - `2765c92` (feat) -- superseded by the round-1 and round-2 re-runs below
4. **Correction round 1a: fidelity correction to `topoae.py`'s training loss** - `4b9b6c9` (fix)
5. **Correction round 1b: re-sweep lambda over the paper's actual searched range** - `0789063` (fix)
6. **Correction round 1c: re-run the Swiss roll notebook against the corrected implementation, fix ratio label** - `72e8794` (fix)
7. **Correction round 1 SUMMARY (superseded by this one)** - `c3d41e8` (docs)
8. **Correction round 1 STATE.md/ROADMAP.md update (superseded by this round's update)** - `0014ce4` (docs)
9. **Correction round 2: add the missing baseline for the topological structural check** - `eedadb5` (fix)

_Note: Task 4 (`type="checkpoint:human-verify"`, `gate="blocking"`) is not yet resolved. Two Swiss roll reads have now reached this checkpoint and neither was approved outright -- the first triggered the round-1 fidelity-correction directive, the second triggered the round-2 missing-baseline directive. This SUMMARY is written under the round-2 directive, ahead of Task 4's resolution, to record both corrections before returning to the still-open checkpoint._

## Files Created/Modified

- `notebooks/diagnostics/topoae_lambda_sweep_run.py` - lambda-selection sweep on the Swiss roll fixture; redrawn to `{0.0, 0.1, 0.3, 1.0, 3.0}` (log-uniform over the paper's own [0.1, 3] search range) after the round-1 fidelity correction
- `notebooks/02.4_swiss_roll_topoae_check.ipynb` - the mandatory Swiss roll sanity check; re-executed against the round-1 corrected implementation, then again with round-2's baseline-relative topological check
- `notebooks/pu_manifold/topoae.py` - `topological_loss` now carries the paper's `1/2` factor on each directional term; `train_topoae` now normalizes ambient distances by their own per-batch max, normalizes latent distances by a newly-added jointly-trained `latent_norm` scalar parameter (returned as `latent_norm_final`), and no longer divides the topo term by batch size; `topological_fidelity`'s docstring corrected to state its ambient-normalization divergence from training explicitly rather than implying training already matched it
- `notebooks/pu_manifold/tests/test_topoae.py` - updated the tracer test's expected `fit.keys()` set (adds `latent_norm_final`); added 4 tests covering the learnable `latent_norm`'s gradient/movement, the ambient normalization's scale-invariance, and the `1/2` factor's cancellation in the T1 gate ratio

## Decisions Made

See `key-decisions` in frontmatter for the full, precise record. In short: round 1 fixed `train_topoae`'s training loss (missing jointly-trained `latent_norm`, missing per-batch ambient normalization, a spurious batch-size division, and a missing `1/2` factor) and re-swept lambda over the paper's actual search range, landing again at the grid floor (0.1) but now as a faithful measurement. Round 2 fixed the notebook's topological structural check, which had no matched baseline, unlike the two reconstruction checks CLAUDE.md also mandates a baseline for. With the baseline in hand, TopoAE clearly beats the plain-AE baseline on topology while losing to it on reconstruction -- read as the trade the method is designed to make, not tuned toward that reading.

## Deviations from Plan

### Auto-fixed / directive-driven issues

**1. [Rule 1 - Bug, escalated via a blocking checkpoint] `train_topoae` did not faithfully implement the paper's training loss**
- **Found during:** Task 4's blocking Swiss roll checkpoint, round 1 (the first read: TopoAE did not beat the matched plain-AE baseline, and the two directional loss terms diverged in opposite directions as lambda increased -- an asymmetry that turned out to be the decisive clue)
- **Issue:** Missing jointly-trained `latent_norm` scale; missing per-batch `d_x / d_x.max()` ambient normalization in training; a spurious `/batch_size` division on the topo term (the prior docstring's claim that this was "the paper's own convention" was false); a missing `1/2` factor on each directional term in `topological_loss`
- **Fix:** All four gaps closed in `notebooks/pu_manifold/topoae.py`; `cae.py` untouched throughout (verified via `git diff --quiet`)
- **Files modified:** `notebooks/pu_manifold/topoae.py`, `notebooks/pu_manifold/tests/test_topoae.py`
- **Verification:** 4 new tests added (31 in this file, 106 in the full suite), all passing; the `1/2` factor's cancellation in the T1 gate ratio and the ambient normalization's scale-invariance are each proved by a dedicated test rather than assumed
- **Committed in:** `4b9b6c9`

**2. [Rule 1 - Bug] The lambda sweep's grid was scaled against the pre-correction loss**
- **Found during:** the same round-1 checkpoint review, as a consequence of fix #1
- **Issue:** Gaps 3+4 above compounded to roughly a 32x lambda-scale error at batch 64; the original grid `{0.1 .. 2.0}` was actually probing an effective lambda of roughly `{0.003 .. 0.06}`, never entering the paper's own log-uniform-[0.1, 3] search range
- **Fix:** Redrew the grid to `{0.0, 0.1, 0.3, 1.0, 3.0}` (log-uniform over [0.1, 3], 0.0 control arm) and re-ran the sweep under the corrected loss, applying the identical (unretuned) selection rule
- **Files modified:** `notebooks/diagnostics/topoae_lambda_sweep_run.py`
- **Verification:** Re-run exits 0, prints one row per arm, a second invocation is a cache hit; `git diff --quiet` on `cae.py`/`topoae.py` confirms running the sweep changed no source
- **Committed in:** `0789063`

**3. [Rule 1 - Bug] Notebook printed the inverse ratio under its own label**
- **Found during:** the round-1 checkpoint review (found by the orchestrator independently)
- **Issue:** The printed `"TopoAE / plain-AE mse_per_dim ratio"` line was computing `plain_mse / topoae_mse` -- the inverse of its own label. The `beats baseline: False` verdict itself was already correct; only the printed ratio number was mislabelled
- **Fix:** Ratio now computed as `topoae_mse / plain_mse`, matching its label exactly; values below 1.0 mean TopoAE beats the baseline
- **Files modified:** `notebooks/02.4_swiss_roll_topoae_check.ipynb`
- **Verification:** Re-executed end to end; printed ratio (1.382) is now internally consistent with its own label and with the underlying `mse_per_dim` values shown two lines above it
- **Committed in:** `72e8794`

**4. [Rule 2 - Missing Critical] The topological structural check had no matched baseline**
- **Found during:** Task 4's blocking Swiss roll checkpoint, round 2 (after round 1's correction, the second read)
- **Issue:** Cell 12 gated the persistence-pair edge-length correlation on an absolute `r > 0.8` bound, with no comparison against the already-trained matched plain-AE model -- unlike the two reconstruction checks, which already had a baseline. Without a baseline, `r=0.680` alone could not distinguish "TopoAE preserves topology and merely misses an arbitrary bar" from "TopoAE is actively worse at its own objective"
- **Fix:** The same ambient MST pairing (computed once from `x_holdout` alone) is now applied to both the TopoAE latent and the plain-AE latent; both correlations are printed and plotted side by side; the gate is now `topoae_r > plain_ae_r` (baseline-relative), with the absolute bound kept as a secondary reported line
- **Files modified:** `notebooks/02.4_swiss_roll_topoae_check.ipynb`
- **Verification:** Re-executed end to end (65s, zero error cells); visually confirmed via the side-by-side scatter plots that the TopoAE points track the diagonal tighter than the plain-AE points, consistent with the correlation numbers (0.680 vs 0.471) rather than an artifact of the new code
- **Committed in:** `eedadb5`

---

**Total deviations:** 4 (1 bug fix escalated via the first blocking checkpoint, 2 direct consequences of it, 1 missing-critical-comparison fix escalated via the second blocking checkpoint)
**Impact on plan:** All four were necessary corrections to measurement fidelity and completeness, not scope creep. The plan's original Tasks 1-3 deliverables (the sweep runner, the notebook) are unchanged in shape and purpose; only their underlying computation and the notebook's own internal comparisons are now faithful to the paper and to CLAUDE.md's baseline-comparison mandate. Task 4's checkpoint decision was correctly deferred twice rather than approved on an incomplete or flawed measurement.

## Issues Encountered

None beyond the fidelity and completeness gaps documented above as deviations.

## Lambda Sweep Results (final, post-round-1-correction; unchanged by round 2)

`PYTHONPATH=notebooks python notebooks/diagnostics/topoae_lambda_sweep_run.py`, cache stem `topoae_lambda_sweep_1b8c6aa02dbf82a5`, re-invocation confirmed a cache hit (3.3s):

| lambda | mse_per_dim | mse_ratio_v0 | loss_x_to_z | loss_z_to_x | transfer_ratio | wallclock_s |
|--------|-------------|--------------|-------------|-------------|-----------------|-------------|
| 0.0 | 0.013669 | 1.0000 | 5.394058 | 32.389850 | 0.000000 | 68.3 |
| 0.1 | 0.025962 | 1.8993 | 1.455657 | 35.914894 | 0.422840 | 55.1 |
| 0.3 | 0.029536 | 2.1607 | 1.210372 | 40.710968 | 1.174374 | 54.9 |
| 1.0 | 0.035277 | 2.5807 | 1.013201 | 55.218575 | 3.400329 | 67.2 |
| 3.0 | 0.042192 | 3.0866 | 1.032286 | 51.692311 | 8.676973 | 80.3 |

**Selected lambda = 0.1 -- the grid floor.** No positive lambda satisfies the rule's <=10% reconstruction-degradation bound; the smallest tested positive value already costs 89.9% relative to the zero-lambda arm, rising to 208.7% at lambda=3.0. `loss_x_to_z` falls monotonically with lambda (the latent increasingly preserves the ambient MST) while `loss_z_to_x` rises (the latent increasingly invents extra structure) -- the same qualitative asymmetry the pre-correction sweep showed, now on a faithful loss. Round 2's finding (below) reads this asymmetry differently: `loss_z_to_x` is scale-sensitive and read under a normalization the model was never trained against (raw `d_x` vs. `z*latent_unit_scale(z)`, not the training-time `d_x/d_x.max()` vs. `d_z/latent_norm`), so it is not clean evidence on its own -- the scale-free correlation check below is.

## Swiss Roll Notebook Result (final, post-round-2-correction)

`notebooks/02.4_swiss_roll_topoae_check.ipynb`, 15 cells, executed end to end in 65s, zero error cells:

- TopoAE held-out `mse_per_dim` = 0.035089; matched plain-AE baseline = 0.025395 -- **ratio 1.382 (TopoAE / plain-AE); does not beat the baseline on reconstruction**
- TopoAE mean relative reconstruction error = 22.6% (bound: <15%)
- **Topological structural check, now baseline-relative:** TopoAE persistence-pair edge-length correlation = 0.680; matched plain-AE baseline = 0.471 -- **TopoAE clearly beats the baseline on this check** (absolute `r > 0.8` bound: not met, reported as a secondary line)
- Printed checks: reconstruction-error bound **False**, reconstruction-vs-baseline **False**, topology-vs-baseline **True**
- Printed read-out (the "beats on topology, loses on reconstruction" branch): *"TopoAE beats the matched plain-AE baseline on the topological structural check (r=0.680 vs plain-AE r=0.471) -- the thing the method exists to do -- while it does not beat the baseline on plain MSE reconstruction. That is the trade the method makes on purpose: the paper itself evaluates with KL divergence of density estimates and trustworthiness/continuity, never MSE, so an MSE penalty here does not contradict a topology win. On its own objective, this is a genuine recovery."*
- Visual inspection: the TopoAE reconstruction scatter (3-D and x-z) shows the spiral shape and colour ordering qualitatively present but noisy; the new side-by-side persistence-pair scatter plots show the TopoAE points tracking the diagonal noticeably tighter than the plain-AE points, consistent with (not contradicted by) the correlation gap

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Task 4's blocking human-verify checkpoint is still open. With both corrections in place -- a faithful training loss and a baseline-relative topological check -- the Swiss roll result is: TopoAE clearly beats the matched plain-AE baseline on the check that is its own stated objective (persistence-pair edge-length agreement, r=0.680 vs 0.471), while losing to that same baseline on plain MSE reconstruction (ratio 1.382). This is one of three legitimate outcomes named by the coordinator's own dispatch, and per that framing it reads as TopoAE "behaving exactly as designed" -- the paper's own evaluation protocol never uses MSE, so an MSE loss does not contradict a topology win. This is now reported as the faithful, complete measurement and awaits a human decision: approve this reading and proceed to freeze `lambda=0.1` for `02.4-PREREGISTRATION.md` (plan 02.4-04), request a different or additional check (e.g. the paper's own KL-divergence-of-density-estimates or trustworthiness/continuity measures, rather than relying on MSE-based reconstruction checks CLAUDE.md's template happens to emphasize), or direct some other path. Plans 02.4-04 through 02.4-08 remain blocked on this decision.

---
*Phase: 02.4-topological-auto-encoder-validity-test-inserted*
*Completed: 2026-08-07 (Task 4 checkpoint still open — see above)*
