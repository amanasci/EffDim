---
phase: 07-curvature-conditioned-crossmodal-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/pu_manifold/crossmodal_curvature.py
  - notebooks/pu_manifold/tests/test_crossmodal_curvature.py
autonomous: false
requirements: [D7-01, D7-02, D7-03, D7-04, D7-05, D7-06, D7-07]
user_setup: []

must_haves:
  truths:
    - "crossmodal_curvature.assert_preregistered() returns None when every constant in _REQUIRED_CONSTANTS is set, and raises RuntimeError naming the offending constant when any one of them is absent, None, an empty string, or an empty tuple (D7-06 boundary)."
    - "The freeze proof requires the pre-registration commit to be a STRICT ancestor of the commit that first produces a PU number: `git merge-base --is-ancestor <freeze> HEAD` exits 0 AND `git rev-list --count <freeze>..HEAD` is at least 1. A commit is its own ancestor, so the --is-ancestor check alone would pass if a number were produced in the freeze commit itself (D7-06 precision)."
    - "D_SWEEP is the literal tuple (20, 25, 32) and nothing else; the runner refuses a latent dimension outside D_SWEEP rather than silently fitting it (D7-01 boundary)."
    - "TIE_HANDLING_RULE freezes scipy.stats.spearmanr's average-rank tie handling as the convention for exactly-equal values in either paired array, and freezes the permutation route (not spearmanr's own asymptotic p-value) as the significance route, because the per-point MKNN array takes at most HEADLINE_K + 1 distinct values across 10,000 points (D7-01 adjacency)."
    - "POSITIVE_CONTROL_TARGET_RHOS is strictly increasing, asserted by assert_preregistered, so 'the smallest planted effect the test recovers' is well defined by tuple order rather than by whatever order results happen to arrive in (D7-02 ordering)."
    - "crossmodal_curvature.py re-declares every constant it needs as a plain literal in its own file; its import block resolves to numpy, scipy.stats, typing, and the pu_manifold modules mknn / curvature_probe / cross_split_curvature only — no sealed Phase 5 or Phase 6 module supplies a constant by import, so a same-named constant in a sealed module cannot collide with or shadow this phase's own (D7-05 adjacency)."
    - "`git diff --stat <phase-7-base>..HEAD -- notebooks/pu_manifold/mknn.py notebooks/pu_manifold/linear_probe.py notebooks/pu_manifold/pointcloud_probe.py notebooks/pu_manifold/cae.py notebooks/pu_manifold/decoder_curvature.py notebooks/pu_manifold/curvature_probe.py notebooks/pu_manifold/cross_split_curvature.py src/effdim/` produces empty output for the whole phase (D7-05 empty)."
    - statement: "Importing crossmodal_curvature never mutates module-level state in any sealed module (no monkeypatching, no attribute assignment onto mknn / cae / decoder_curvature / curvature_probe / cross_split_curvature), regardless of import order."
      verification: backstop
  artifacts:
    - notebooks/pu_manifold/crossmodal_curvature.py
    - notebooks/pu_manifold/tests/test_crossmodal_curvature.py
  key_links:
    - "assert_preregistered() is the single gate every number-producing code path calls first; if it is not called by the runner before compute, the freeze is decorative."
    - "The git commit created by Task 2 IS the freeze commit; its SHA is what every later ancestry proof is taken against."
  prohibitions:
    - "MUST NOT edit notebooks/pu_manifold/linear_probe.py, pointcloud_probe.py, mknn.py, cae.py, decoder_curvature.py, curvature_probe.py, cross_split_curvature.py, or anything under src/effdim/."
    - "MUST NOT delete or rewrite any existing notebook or runner script, including the notebooks/diagnostics/07_*_run.py spike scripts."
    - "MUST NOT edit a pre-registered constant after the freeze commit without recording the edit as a pre-registration BREACH in the phase record; a silent fix is the failure mode the whole freeze exists to prevent."
    - "MUST NOT quote the pre-existing notebooks/diagnostics/07_*_run.py spike scripts' numbers as this phase's own pre-registered output; they predate the freeze and carry no assert_preregistered guard."
---

<objective>
Freeze Phase 7 before any PU number can exist. Ratify the six open design decisions blind at a
`checkpoint:decision`, then commit the pre-registration constants block, `VERDICT_RULE`,
`VERDICT_VALUES` and `assert_preregistered()` into a new module `crossmodal_curvature.py` — a
commit that contains constants only and becomes the strict git ancestor every later number is
proven against (D7-06).

Purpose: D7-06's freeze-before-compute is a task-ordering constraint, not a note. This plan's
Task 2 commit is the freeze. Nothing downstream may produce a PU number until it exists.
Output: `crossmodal_curvature.py` (constants + guard only) and its test file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-CONTEXT.md
@.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-RESEARCH.md
@.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-PATTERNS.md
@CLAUDE.md
</context>

<assumption_delta_decision>
**Noun:** the unit of observation for a curvature-vs-alignment comparison.
**Decision:** `promote`.
**Rationale:** Phases 4, 5 and 6 all modeled the analysis unit as a *region* or *bucket* (2–3 of
them). D7-04 promotes the *point* to the primary unit — 10,000 paired observations. This is a
promote, not an add-alongside: Phase 7 runs no bucketed arm at all, and `N_BUCKETS` /
`BUCKET_RULE` (Phase 5/6 constants) deliberately have no counterpart in this phase's freeze. The
detector's own lexical hit (the word "also" in the ROADMAP prose) is a false positive and is
recorded as `no-change`.
</assumption_delta_decision>

<flagged_planner_assumptions>
Two edge-probe rows came back `unclassified` and are surfaced here rather than silently resolved.
The proposed resolutions below are **planner assumptions**, open for the developer to overturn at
Task 1's checkpoint:

1. **D7-03 (`unclassified`).** Assumption: the substantive edge is the sign convention.
   `curvature_probe.local_density_weights` returns the per-point INVERSE density `w`,
   mean-normalized to 1 — Phase 4's own `region_partition_mknn_run.py` prints "REGN-01:
   local_density_weights' w is mean-normalized to 1, so rho = 1/w is a RELATIVE density, not an
   absolute one." Reporting `spearman(w, ||H||)` instead of `spearman(1/w, ||H||)` silently flips
   the sign of D7-03's headline diagnostic and would make it non-comparable to Phase 4's measured
   `spearman(density, ||H||) = -0.0273`. Proposed resolution: the reported density statistic is
   taken on `1.0 / w`, and the record row names the convention.
2. **D7-07 (`unclassified`).** Assumption: the edge is how absence of scope is *proven* rather
   than asserted. Proposed resolution: freeze `ALIGNMENT_METRIC = "mknn"` as a pre-registered
   constant and carry `"alignment_metric": "mknn"` on every record row, so the exclusion is a
   positive, checkable fact instead of prose.
</flagged_planner_assumptions>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Ratify the six open pre-registration decisions, blind</name>
  <decision>
    Six decisions must be fixed before the freeze commit. All are being put to you BEFORE any PU
    number exists, which is the whole point — after Task 2 commits, changing any of them is a
    recorded pre-registration BREACH, not a fix.
  </decision>
  <context>
    `07-RESEARCH.md` § Open Questions left three of these open; `07-PATTERNS.md` § Cache/containment
    discipline left a fourth open; two more were found while planning by reading source. Each is
    listed with the planner's recommendation and the evidence behind it.

    **(a) Headline MKNN `k`.** Recommend `HEADLINE_K = 20`, inheriting `region_partition.HEADLINE_K`,
    with `MKNN_K_GRID = (5, 10, 20, 50)` reported as non-gating sensitivity exactly as Phase 4 did.
    Rationale: cross-phase comparability with Phase 4's measured ratio-over-chance table.
    Consequence to accept: at `k=20` the per-point MKNN array takes at most 21 distinct values
    across 10,000 points — a massive-ties regime, which is why (d) matters.

    **(b) The significance route is one-sided in the direction this phase does NOT expect.**
    Found by reading source, not in RESEARCH.md: `curvature_probe.permutation_null` calls
    `scipy.stats.permutation_test(..., alternative="greater")` and returns
    `clears_null = observed_rho > null_threshold` — strictly the upper tail. The research question
    predicts a NEGATIVE association (more curvature, worse alignment). As written, the phase's
    headline test is structurally incapable of detecting the effect it was built to look for.
    Recommend: leave `curvature_probe.py` unedited (D7-05 spirit) and call it TWICE — once on
    `(H, MKNN)` and once on `(-H, MKNN)` — at `NULL_QUANTILE_PER_TAIL = 0.975` each, declaring an
    association if either tail clears. Two looks at 0.975 is the Bonferroni equivalent of a 0.95
    two-sided test. Spearman on a negated array is exactly the negated Spearman (average-rank
    reversal is exact, ties included), so the second call is a mirror, not a new statistic.

    **(c) Verdict vocabulary, including the underpowered branch.** Recommend
    `VERDICT_VALUES = ("ASSOCIATION DETECTED", "NO DETECTABLE RELATIONSHIP", "SPLIT ACROSS d",
    "UNDERPOWERED -- NO CLAIM")`. `SPLIT ACROSS d` is a complete terminal outcome, mirroring Phase
    5's `SPLIT ACROSS SEEDS`: D7-01 says "same answer at each `d`" is what makes the truncation
    question moot, so disagreement across `d` is a real result, not a stall. `UNDERPOWERED -- NO
    CLAIM` makes D7-02's "not optional" mechanical: if the positive control recovers NOTHING at
    the pre-registered effect-size grid, the phase may not report a null at all.

    **(d) Where the frozen record lives.** `07-RESEARCH.md` says `notebooks/diagnostics/*.jsonl`
    (matching the spike scripts); every sealed Phase 4/5/6 precedent writes into
    `notebooks/.cache/` via `cache.py`. Recommend `notebooks/.cache/07_crossmodal_curvature.jsonl`
    via `cache.cache_path`. Rationale: it keeps the frozen record visually and physically distinct
    from the nine spike `.jsonl` files already sitting in `notebooks/diagnostics/`; it routes
    through `cache.py`'s `_assert_inside_cache` containment guard, which is this phase's one real
    security mitigation; and CLAUDE.md designates `notebooks/.cache/` as the gitignored
    milestone-artifact store.

    **(e) Seeds across the `d`-sweep.** Recommend one seed
    (`SEED_HANDLING_RULE = "single_seed_across_d_sweep"`), named as an accepted limitation
    inherited from Phase 5's measured seed-instability of decoder curvature fields, not as a
    silent assumption of stability. Three seeds × three `d` would be ~6 hours of field computation.

    **(f) Positive control shares the freeze commit.** Recommend yes: freeze
    `POSITIVE_CONTROL_TARGET_RHOS`, `POSITIVE_CONTROL_SEED` and `POSITIVE_CONTROL_RULE` in the same
    commit as everything else. A power analysis that licenses a null must itself be pre-registered,
    or it can be tuned after the fact to make an observed null look either well- or under-powered.
  </context>
  <options>
    <option id="ratify-all">
      <name>Ratify all six recommendations as stated</name>
      <pros>Consistent with Phase 4/5/6 precedent on every point where precedent exists; closes the one-sided-test defect before it can silently null the phase; keeps the sweep inside the ~2h budget.</pros>
      <cons>Single seed leaves Phase 5's seed-instability finding unaddressed for this field too.</cons>
    </option>
    <option id="ratify-with-amendments">
      <name>Ratify with amendments</name>
      <pros>Any of (a)-(f) can be changed now at zero cost; after Task 2 commits, the same change is a recorded BREACH.</pros>
      <cons>Amendments to (b) or (c) change what the phase can conclude and should be reasoned through, not swapped.</cons>
    </option>
    <option id="defer-freeze">
      <name>Do not freeze yet — send a question back</name>
      <pros>Appropriate if any recommendation rests on evidence you dispute.</pros>
      <cons>Blocks the entire phase; nothing downstream may run.</cons>
    </option>
  </options>
  <reversibility rating="one-way">Once the freeze commit exists and a PU number is produced against it, any later change to these constants is a recorded pre-registration BREACH — the record cannot absorb a silent revision, which is exactly the failure `02.6-FINDINGS.md` §4 documented once already.</reversibility>
  <resume-signal>Select: ratify-all, ratify-with-amendments (state which of a-f and how), or defer-freeze</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Commit the pre-registration constants block — THE FREEZE COMMIT</name>
  <files>notebooks/pu_manifold/crossmodal_curvature.py</files>
  <read_first>
    - notebooks/pu_manifold/pointcloud_probe.py (the structural template — docstring, flat constants block, VERDICT_RULE triple-quoted literal, _REQUIRED_CONSTANTS tuple, assert_preregistered loop, verdict_is_terminal, describe_inheritance)
    - notebooks/pu_manifold/region_partition.py lines 55-115 (origin of HEADLINE_K, MKNN_K_GRID, NULL_QUANTILE, N_PERMUTATIONS, N_BOOTSTRAP, FIELD_D, K_DENSITY, CONFIDENCE_LEVEL — the Phase 4 values re-declared here)
    - notebooks/diagnostics/07_pu_plain_ae_fit_run.py (the measured training cfg this phase re-declares: lr 1e-3, weight_decay 1e-4, batch 128, lip_weight 0.0, fps_pretrain_epochs 0, early stopping disabled by patience > max_epochs; torch.manual_seed(0) before model construction; cfg seed 20260825; 600 epochs to a converged 98.207%)
    - notebooks/diagnostics/curvature_field_pu_run.py lines 175-185 and 468-480 (PU_SPLIT_SEED = 20260813, PU_HOLDOUT_FRACTION = 0.2, and _split's exact permutation algorithm)
    - .planning/phases/07-curvature-conditioned-crossmodal-alignment/07-CONTEXT.md §3, §4, §5, §7, §8
  </read_first>
  <action>
Create `notebooks/pu_manifold/crossmodal_curvature.py` containing ONLY: the module docstring, the
constants block, `VERDICT_RULE`, `VERDICT_VALUES`, `_REQUIRED_CONSTANTS`, `assert_preregistered()`,
`verdict_is_terminal()`, and `describe_inheritance()`. No compute functions, no imports of numpy or
scipy yet — plan 07-02 adds those. A freeze commit that contains constants and their guard and
nothing else is the cleanest possible ancestry proof.

Docstring, following `pointcloud_probe.py` lines 1-64: open with "This module adds; it does not
edit." Name every module Phase 7 imports and never modifies (mknn, cae, decoder_curvature,
curvature_probe, cross_split_curvature) and every sealed module it does not even import for
constants (linear_probe, pointcloud_probe), citing D7-05. State that the constants below are
FROZEN, committed before any PU number exists, and that a later edit is a recorded pre-registration
BREACH, not a silent fix (D7-06). Cite the decision IDs inline in prose: D7-01 for the `d`-sweep,
D7-02 for the positive control, D7-03 for density and hubness, D7-04 for the per-point statistic,
D7-05 for additive-only, D7-06 for the freeze, D7-07 for the alignment-metric scope.

Constants block — flat `UPPER_CASE` literals, no computed defaults, no function call producing a
convenient default. Every name below carries a one-line comment naming the decision ID or the
source it is re-declared from. Use exactly these names and values (as ratified in Task 1):

Field and instrument (D7-01): `D_SWEEP = (20, 25, 32)`; `AE_IN_DIM = 768`;
`AE_HIDDEN = (250, 250, 250)`; `AE_ACTIVATION = "silu"`;
`CURVATURE_SOURCE_FUNCTION = "decoder_curvature.plain_decoder_curvature"`;
`CURVATURE_DIFFERENTIATES = "model.decode alone, never the encoder-composed round trip"`;
`CURVATURE_CONVENTION = "trace"`; `PU_FIELD_COLUMN = "legacysurvey"`;
`FIELD_EVALUATED_ON = "all_10000_rows_including_the_8000_training_rows"`;
`INSTRUMENT_FIDELITY_RANGE = (0.53, 0.99)`.

Fit protocol, re-declared from the measured spike run (D7-01): `MAX_EPOCHS = 600`;
`TORCH_INIT_SEED = 0`; `FIT_SEED = 20260825`; `SPLIT_SEED = 20260813`;
`HOLDOUT_FRACTION = 0.2`; and `TRAIN_CFG` as a dict literal carrying lr 1e-3, weight_decay 1e-4,
batch 128, lip_weight 0.0, fps_pretrain_epochs 0, early_stop_patience `MAX_EPOCHS + 1`,
early_stop_min_delta 1e-9, wallclock_ceiling_s `float("inf")`. Add a comment stating that
`early_stop_patience > MAX_EPOCHS` disables total-loss early stopping deliberately, per
`03-08-DEFECTS-01.md` defect 2.

Alignment statistic (D7-04, D7-07): `ALIGNMENT_METRIC = "mknn"`; `HEADLINE_K = 20`;
`MKNN_K_GRID = (5, 10, 20, 50)`; `PU_COLUMN_A = "hsc"`; `PU_COLUMN_B = "legacysurvey"`;
and `SENSITIVITY_GRID_RULE`, a string stating that only `HEADLINE_K` gets a permutation null and
that the other `k` values report a point estimate which cannot overturn or escalate the verdict.

Significance (D7-04): `N_PERMUTATIONS = 1000`; `PERMUTATION_SEED = 20260825`;
`NULL_QUANTILE_PER_TAIL = 0.975`; `SIGNIFICANCE_TAIL_RULE`, a string stating that
`curvature_probe.permutation_null` is one-sided (`alternative="greater"`,
`clears_null = observed_rho > null_threshold`), that the research hypothesis predicts a negative
association, that the test is therefore run twice — the positive tail on the field as given and the
negative tail on the negated field — each at `NULL_QUANTILE_PER_TAIL`, and that an association is
declared if either tail clears; and `TIE_HANDLING_RULE`, a string stating that
`scipy.stats.spearmanr`'s average-rank tie handling is the convention for exactly-equal values,
that the per-point MKNN array takes at most `HEADLINE_K + 1` distinct values across 10,000 points,
and that the permutation route rather than `spearmanr`'s own asymptotic p-value is the significance
route for exactly that reason.

Density and hubness (D7-03): `DENSITY_K = 30`; `DENSITY_FIELD_D = 20`;
`DENSITY_INPUT = "legacysurvey_ambient_768"`; `DENSITY_SIGN_CONVENTION`, a string stating that
`curvature_probe.local_density_weights` returns the per-point INVERSE density `w` mean-normalized
to 1, that the reported density statistic is taken on `1.0 / w`, and that `DENSITY_FIELD_D` is held
at 20 across the whole `d`-sweep rather than tracking the sweep's `d`, because density is a
property of the ambient cloud and varying it would make the three density numbers non-comparable
to each other and to Phase 4's; and `DIAGNOSTICS_ARE_NON_GATING = True`.

Positive control (D7-02): `POSITIVE_CONTROL_TARGET_RHOS = (0.02, 0.05, 0.10, 0.20)`;
`POSITIVE_CONTROL_SEED = 20260825`; `POSITIVE_CONTROL_RULE`, a string stating that the planted
relationship is built on PU's own realized `d=20` `||H||` field so the planting happens at PU's
actual dynamic range, that Phase 6's `rng.random(n)` selfcheck (a ~20x-spread field against PU's
order-2x) explicitly does not serve, that the same permutation machinery and the same `n` as the
headline test are used, that the reported quantity is the SMALLEST entry of
`POSITIVE_CONTROL_TARGET_RHOS` at which either tail clears, and — because a planting mechanism
chosen after seeing the real number is exactly the tuning this freeze exists to prevent — the
mechanism itself, spelled out in full: rank-transform the real field to `u = (rankdata(h) - 0.5) / n`;
set `p = clip(0.5 + slope * (u - 0.5), 0.0, 1.0)`; draw `j ~ Binomial(k, p)` from
`np.random.default_rng(POSITIVE_CONTROL_SEED)` and take the planted per-point value as `j / k`, so
it carries the same `j/k` discretization the real statistic has; and find `slope` by 40 iterations of
bisection on the bracket `[0.0, 2.0]` against the achieved Spearman, re-seeding the generator to
`POSITIVE_CONTROL_SEED` at every trial so the search is deterministic. The achieved Spearman is
recorded beside every target, never silently substituted for it.

Provenance and record (D7-05, D7-06): `SEED_HANDLING_RULE = "single_seed_across_d_sweep"`;
`RECORD_STEM = "07_crossmodal_curvature"`;
`RECORD_LOCATION_RULE`, a string stating the frozen record is written under `cache.CACHE_DIR` via
`cache.cache_path` and is distinct from the nine pre-existing `notebooks/diagnostics/07_*.jsonl`
spike outputs, which are informational inputs only and satisfy nothing;
`PREREGISTRATION_FREEZE_RULE`, a string stating that the freeze commit must be a STRICT ancestor of
the commit producing any PU number — `git merge-base --is-ancestor` alone is insufficient because a
commit is its own ancestor, so `git rev-list --count <freeze>..HEAD` must also be at least 1.

`VERDICT_RULE` — one triple-quoted string literal, prose, mirroring `pointcloud_probe.py` lines
138-176. It must name: the headline statistic as the Spearman rank correlation between the
per-point curvature magnitude and the per-point MKNN score over all 10,000 rows at `HEADLINE_K`;
the two-tailed significance procedure from `SIGNIFICANCE_TAIL_RULE`; a lettered decision list
mapping the three `d` outcomes onto `VERDICT_VALUES`, where all three `d` agreeing on an
association gives `ASSOCIATION DETECTED`, all three agreeing on no clearance gives `NO DETECTABLE
RELATIONSHIP`, and any disagreement gives `SPLIT ACROSS d` as a complete terminal outcome, not a
stall; and the D7-02 override, that `NO DETECTABLE RELATIONSHIP` may only be reported if the
positive control cleared at some entry of `POSITIVE_CONTROL_TARGET_RHOS`, and otherwise the verdict
is `UNDERPOWERED -- NO CLAIM`. It must also restate, inside the rule's own text and not only in
surrounding prose, every caveat that travels with any verdict produced under it: that no ground
truth for PU curvature exists and the instrument's validated fidelity is the RANGE in
`INSTRUMENT_FIDELITY_RANGE`, never a point estimate; that the density and hubness numbers are
reported and gate nothing; that Phase 4's `HOLDS` is not evidence of a curvature-alignment
association and must not be cited as one; that this milestone runs at n=10,000 where the k/n chance
floor is roughly ten times the source paper's; that the field is evaluated on all 10,000 rows
including the 8,000 the decoder trained on; and that the single seed is an accepted limitation
inherited from Phase 5's measured seed-instability.

`VERDICT_VALUES = ("ASSOCIATION DETECTED", "NO DETECTABLE RELATIONSHIP", "SPLIT ACROSS d",
"UNDERPOWERED -- NO CLAIM")`.

`_REQUIRED_CONSTANTS` — a tuple naming every constant above, including `VERDICT_RULE` and
`VERDICT_VALUES`.

`assert_preregistered()` — copy `pointcloud_probe.assert_preregistered` verbatim except the module
name in the error string and one addition: after the absent/None/empty-string/empty-sequence loop,
assert that `POSITIVE_CONTROL_TARGET_RHOS` is strictly increasing and that every `d` in `D_SWEEP` is
a positive int, raising `RuntimeError` naming the offending constant. The error message must open
with `crossmodal_curvature.assert_preregistered: Phase 7 is not frozen` and cite D7-06.

`verdict_is_terminal(verdict)` — membership in `VERDICT_VALUES`, mirroring
`pointcloud_probe.verdict_is_terminal`.

`describe_inheritance()` — a pure dict-returning audit surface with two keys: the Phase 4 values
re-declared here as literals (`HEADLINE_K`, `MKNN_K_GRID`, `N_PERMUTATIONS`, `DENSITY_K`,
`DENSITY_FIELD_D`) and what Phase 7 changes (`ALIGNMENT_METRIC`, `CURVATURE_SOURCE_FUNCTION`,
`D_SWEEP`, `SIGNIFICANCE_TAIL_RULE`, `SEED_HANDLING_RULE`). No I/O.

Commit this file alone with message `feat(07): freeze Phase 7 pre-registration constants (D7-06)`.
Record the resulting SHA in the plan SUMMARY as the freeze commit — every later ancestry proof is
taken against it.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import sys; sys.path.insert(0, 'notebooks'); from pu_manifold import crossmodal_curvature as cc; cc.assert_preregistered(); assert cc.D_SWEEP == (20, 25, 32); assert cc.HEADLINE_K == 20; assert cc.ALIGNMENT_METRIC == 'mknn'; assert len(cc.VERDICT_VALUES) == 4; print('frozen')"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/pu_manifold/crossmodal_curvature.py` exists and defines `assert_preregistered`, `verdict_is_terminal`, `describe_inheritance`, `_REQUIRED_CONSTANTS`, `VERDICT_RULE`, `VERDICT_VALUES`.
    - `crossmodal_curvature.assert_preregistered()` returns None without raising.
    - `crossmodal_curvature.D_SWEEP == (20, 25, 32)`, `HEADLINE_K == 20`, `MKNN_K_GRID == (5, 10, 20, 50)`, `ALIGNMENT_METRIC == "mknn"`, `NULL_QUANTILE_PER_TAIL == 0.975`, `N_PERMUTATIONS == 1000`, `DENSITY_K == 30`, `DENSITY_FIELD_D == 20`, `SEED_HANDLING_RULE == "single_seed_across_d_sweep"`, `INSTRUMENT_FIDELITY_RANGE == (0.53, 0.99)`.
    - `crossmodal_curvature.VERDICT_VALUES == ("ASSOCIATION DETECTED", "NO DETECTABLE RELATIONSHIP", "SPLIT ACROSS d", "UNDERPOWERED -- NO CLAIM")`.
    - `list(cc._REQUIRED_CONSTANTS)` contains every module-level `UPPER_CASE` name defined in the file — verified by comparing `set(cc._REQUIRED_CONSTANTS)` against `{n for n in vars(cc) if n.isupper()}` and asserting equality.
    - `grep -E '^(from|import)' notebooks/pu_manifold/crossmodal_curvature.py` lists only `typing` imports — this file has no numpy, scipy, torch or `pu_manifold` imports yet.
    - `git log --oneline -1 -- notebooks/pu_manifold/crossmodal_curvature.py` shows exactly the freeze commit, and `git show --stat HEAD` lists `notebooks/pu_manifold/crossmodal_curvature.py` as the only changed file.
    - `git diff --stat HEAD~1..HEAD -- notebooks/pu_manifold/mknn.py notebooks/pu_manifold/linear_probe.py notebooks/pu_manifold/pointcloud_probe.py notebooks/pu_manifold/cae.py notebooks/pu_manifold/decoder_curvature.py notebooks/pu_manifold/curvature_probe.py notebooks/pu_manifold/cross_split_curvature.py src/effdim/` produces empty output.
  </acceptance_criteria>
  <done>`crossmodal_curvature.py` exists carrying only frozen constants and their guard, `assert_preregistered()` passes, and the freeze commit SHA is recorded in the SUMMARY. No PU number exists anywhere in the tree (D7-06).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin the freeze guard with tests, including the strict-ancestor boundary</name>
  <files>notebooks/pu_manifold/tests/test_crossmodal_curvature.py</files>
  <read_first>
    - notebooks/pu_manifold/tests/test_pointcloud_probe.py (structural template: sys.path bootstrap, assert_preregistered pass test, monkeypatch-one-constant failure test)
    - notebooks/pu_manifold/crossmodal_curvature.py (written in Task 2)
  </read_first>
  <behavior>
    - `assert_preregistered()` returns None on the frozen module.
    - Setting any single `_REQUIRED_CONSTANTS` entry to `None` raises `RuntimeError` whose message contains that constant's name — parameterized over the whole tuple, so a constant added later without a guard entry fails the test.
    - Setting a string constant to `"   "` raises and names it.
    - Setting a tuple constant to `()` raises and names it.
    - Deleting a constant entirely raises and names it as absent.
    - `POSITIVE_CONTROL_TARGET_RHOS` set to a non-increasing tuple raises and names it.
    - `D_SWEEP` containing a non-positive or non-int entry raises and names it.
    - `verdict_is_terminal` is True for each of the four `VERDICT_VALUES` and False for a near-miss string.
    - `set(_REQUIRED_CONSTANTS)` equals the set of module-level `UPPER_CASE` names, so no constant can be frozen in the file but omitted from the guard.
  </behavior>
  <action>
Create `notebooks/pu_manifold/tests/test_crossmodal_curvature.py`, mirroring
`test_pointcloud_probe.py`'s structure: `sys.path.insert(0, str(Path(__file__).resolve().parents[2]))`
then `from pu_manifold import crossmodal_curvature as cc`.

Write one test per behavior above. Use `pytest.mark.parametrize` over `cc._REQUIRED_CONSTANTS` for
the None case and `monkeypatch.setattr(cc, name, None)` / `monkeypatch.delattr(cc, name)` so the
module is restored after each test. Assert both that `RuntimeError` is raised and that the
constant's name appears in `str(excinfo.value)`.

Add `test_required_constants_covers_every_frozen_constant`, comparing
`set(cc._REQUIRED_CONSTANTS)` against `{n for n in vars(cc) if n.isupper() and not n.startswith("_")}`
and asserting equality in both directions, so neither a missing guard entry nor a stale one can pass.

Add `test_freeze_commit_is_a_strict_ancestor_of_head`: read the freeze commit SHA from a
module-level test constant set to the SHA recorded in Task 2's SUMMARY, then assert via
`subprocess.run` that `git merge-base --is-ancestor <freeze> HEAD` exits 0 AND that
`git rev-list --count <freeze>..HEAD` returns an integer of at least 1. Mark it
`@pytest.mark.skipif` when the repository has no such commit, so a fresh clone at the freeze commit
itself does not fail the suite spuriously — but note in the test docstring that plan 07-04's own
acceptance criteria re-run the same check unconditionally at the moment a PU number is produced,
which is where the check actually bites.

Do not add any test that loads real PU data, trains a decoder, or reads `notebooks/.cache/`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_crossmodal_curvature.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_crossmodal_curvature.py -q` exits 0 with at least 8 passing tests.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0 — the full notebook suite is green and no pre-existing test regressed.
    - The parameterized None-case test reports one case per entry of `_REQUIRED_CONSTANTS` (visible with `-v`), so the count scales with the tuple.
    - `test_required_constants_covers_every_frozen_constant` passes, proving no frozen constant escapes the guard.
    - Test file completes in under 10 seconds — it loads no PU data and trains nothing.
  </acceptance_criteria>
  <done>The freeze guard is pinned: every constant is covered, every malformed-constant boundary raises with the constant named, and the strict-ancestor relation (not merely `--is-ancestor`) is the recorded proof shape.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols created by this plan (none of these exist in the codebase before it):

- Module `notebooks/pu_manifold/crossmodal_curvature.py`
- Constants: `D_SWEEP`, `AE_IN_DIM`, `AE_HIDDEN`, `AE_ACTIVATION`, `CURVATURE_SOURCE_FUNCTION`,
  `CURVATURE_DIFFERENTIATES`, `CURVATURE_CONVENTION`, `PU_FIELD_COLUMN`, `FIELD_EVALUATED_ON`,
  `INSTRUMENT_FIDELITY_RANGE`, `MAX_EPOCHS`, `TORCH_INIT_SEED`, `FIT_SEED`, `SPLIT_SEED`,
  `HOLDOUT_FRACTION`, `TRAIN_CFG`, `ALIGNMENT_METRIC`, `HEADLINE_K`, `MKNN_K_GRID`, `PU_COLUMN_A`,
  `PU_COLUMN_B`, `SENSITIVITY_GRID_RULE`, `N_PERMUTATIONS`, `PERMUTATION_SEED`,
  `NULL_QUANTILE_PER_TAIL`, `SIGNIFICANCE_TAIL_RULE`, `TIE_HANDLING_RULE`, `DENSITY_K`,
  `DENSITY_FIELD_D`, `DENSITY_INPUT`, `DENSITY_SIGN_CONVENTION`, `DIAGNOSTICS_ARE_NON_GATING`,
  `POSITIVE_CONTROL_TARGET_RHOS`, `POSITIVE_CONTROL_SEED`, `POSITIVE_CONTROL_RULE`,
  `SEED_HANDLING_RULE`, `RECORD_STEM`, `RECORD_LOCATION_RULE`, `PREREGISTRATION_FREEZE_RULE`,
  `VERDICT_RULE`, `VERDICT_VALUES`, `_REQUIRED_CONSTANTS`
- Functions: `assert_preregistered()`, `verdict_is_terminal(verdict)`, `describe_inheritance()`
- Test file `notebooks/pu_manifold/tests/test_crossmodal_curvature.py`

Symbols imported unchanged and NOT created here: `mknn.mknn_score`, `mknn._membership_matrix`,
`mknn.hubness_skewness`, `mknn.chance_floor`, `cae.PlainAutoEncoder`, `cae.train_plain_ae`,
`cae.reconstruction_stats`, `decoder_curvature.plain_decoder_curvature`,
`curvature_probe.permutation_null`, `curvature_probe.local_density_weights`,
`cross_split_curvature.partial_spearman`, `cache.cache_path`, `cache._assert_inside_cache`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → runner CLI | The only input surface in the phase: flag values typed by the single local user |
| frozen source → produced number | The integrity boundary this phase actually cares about: a number must be traceable to the code that was frozen before it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | `--record-path` CLI argument on `07_crossmodal_curvature_run.py` | low | mitigate | Every cached artifact path is composed by `cache.cache_path`, which calls `cache._assert_inside_cache`; the runner never opens a caller-supplied raw path. Wired in plan 07-02, asserted in 07-04. |
| T-07-02 | Tampering | `crossmodal_curvature.py` pre-registered constants | high | mitigate | `assert_preregistered()` is called first by every number-producing path (this plan); the freeze commit must be a STRICT ancestor of the number-producing commit, proven by `git merge-base --is-ancestor` AND `git rev-list --count <freeze>..HEAD >= 1` (plan 07-04). |
| T-07-03 | Repudiation | the frozen record rows | medium | mitigate | Every record row carries `preregistration_commit` and `run_commit`, so no number can exist without naming the code that produced it (plan 07-02 writes the fields, 07-04 asserts them). |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs in this phase; `07-RESEARCH.md` § Package Legitimacy Audit independently records N/A. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package exists to gate. |

ASVS level 1; block on high. T-07-02 is the only `high` and is mitigated, not accepted.
V2/V3/V4/V6 do not apply: no auth surface, no sessions, no multi-user access model, no
cryptography — this is a local, single-user, offline research pipeline with no network I/O and no
persisted secrets.
</threat_model>

<verification>
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0.
- `crossmodal_curvature.assert_preregistered()` passes.
- The freeze commit changed exactly one file.
- No sealed module and nothing under `src/effdim/` was touched.
</verification>

<success_criteria>
The six open decisions are ratified on the record, the pre-registration is committed as a
constants-only commit whose SHA is captured, and the guard is pinned by tests that cover every
constant and both freeze-proof boundaries. No PU number exists in the tree.
</success_criteria>

<output>
Create `.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-01-SUMMARY.md` when done.
It MUST record the freeze commit SHA verbatim — plans 07-02 and 07-04 read it from there.
</output>
