---
phase: 07-curvature-conditioned-crossmodal-alignment
plan: 05
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - notebooks/07_crossmodal_curvature_check.ipynb
  - .planning/phases/07-curvature-conditioned-crossmodal-alignment/07-FINDINGS.md
autonomous: false
requirements: [D7-01, D7-02, D7-03, D7-05, D7-07]
user_setup: []

must_haves:
  truths:
    - "The notebook reads the frozen record produced by plan 07-04 and recomputes nothing; it is a report over a sealed artifact, not a second computation with its own conventions."
    - "The notebook is committed with its outputs, executed end to end, per CLAUDE.md."
    - "07-FINDINGS.md states which existing notebook satisfies CLAUDE.md's Swiss roll gate for this phase's instrument and why no new Swiss roll notebook is owed: Phase 7 introduces no new manifold-learning model, reusing cae.PlainAutoEncoder and decoder_curvature.plain_decoder_curvature verbatim, and notebooks/02.6_swiss_roll_plainae_curvature_check.ipynb already tests that exact combination."
    - "Every quotation of the instrument's fidelity is the range +0.53 to +0.99 from INSTRUMENT_FIDELITY_RANGE; no single figure appears anywhere as the instrument's fidelity."
    - "07-FINDINGS.md carries an accepted-limitations section naming, at minimum: the single seed across the d-sweep against Phase 5's measured seed-instability; that the field is evaluated on all 10,000 rows including the 8,000 the decoder trained on; that PU reconstruction showed no plateau through d=48 so every d in the sweep truncates PU; and that the density partial and hubness numbers gate nothing."
    - statement: "No estimated ||H|| spread value is quoted anywhere in the notebook or findings as a precise quantity; the instrument's est/true ratio was measured swinging 0.665 to 1.626 non-monotonically across a 17-fold range of true spread, so spread is reported only as an order of magnitude such as 'low, order 2'."
      verification: backstop
  artifacts:
    - notebooks/07_crossmodal_curvature_check.ipynb
    - .planning/phases/07-curvature-conditioned-crossmodal-alignment/07-FINDINGS.md
  key_links:
    - "The notebook's numbers must come from the JSONL record, not from a fresh computation — a second computation with drifted conventions is how two documents end up quoting different values for the same quantity."
  prohibitions:
    - "MUST NOT claim the curvature field measures true PU curvature. No ground truth for PU exists; the analytic validation gives a RANGE, +0.53 to +0.99, not a point estimate, and the range must be quoted rather than its best cell."
    - "MUST NOT claim a null result means no curvature-alignment relationship exists, absent the D7-02 positive-control evidence that the test could have found one."
    - "MUST NOT make any claim about CKA; it is not implemented anywhere in the codebase and adding it is a separate decision, not a Phase 7 task."
    - "MUST NOT extrapolate any MKNN number to the source paper's n=101,725; this milestone runs at n=10,000, where the k/n chance floor is roughly ten times higher."
    - "MUST NOT reinterpret, soften, reopen or recompute any sealed verdict from Phases 2, 02.x, 3, 03.1, 4, 5 or 6."
    - "MUST NOT cite Phase 4's HOLDS as evidence of a curvature-alignment association; its split axis was measured +0.8208 correlated with density and its own findings attribute nearly the entire raw-score gap to region-size imbalance."
    - "MUST NOT report a rank statistic from this curvature field without the instrument's validated fidelity range stated beside it."
    - "MUST NOT delete or rewrite any existing notebook or runner script, including the nine notebooks/diagnostics/07_*_run.py spike scripts."
    - "MUST NOT quote those spike scripts' numbers as this phase's own pre-registered output; they are informational inputs that predate the freeze."
    - "MUST NOT edit notebooks/pu_manifold/linear_probe.py, pointcloud_probe.py, mknn.py, cae.py, decoder_curvature.py, curvature_probe.py, cross_split_curvature.py, or anything under src/effdim/."
---

<objective>
Report the phase. One notebook that reads the frozen record and shows the picture, and one findings
document that states the answer to the research question along with everything the phase declines
to claim.

Purpose: the milestone's actual research question — does the curvature of the PU embedding manifold
explain the weak crossmodal convergence reported by the Platonic Universe paper — gets an
interpretable answer here, or a recorded reason why the evidence does not support one.
Output: `07_crossmodal_curvature_check.ipynb` and `07-FINDINGS.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-CONTEXT.md
@.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The reporting notebook, executed end to end with outputs</name>
  <files>notebooks/07_crossmodal_curvature_check.ipynb</files>
  <read_first>
    - notebooks/.cache/07_crossmodal_curvature.jsonl (the frozen record — the notebook's only numeric source)
    - notebooks/.cache/07_crossmodal_curvature_fields.npz (the per-d ||H|| and cond(g) arrays, for distribution plots)
    - notebooks/02.6_swiss_roll_plainae_curvature_check.ipynb (structural reference for cell shape and the printed pass/fail read-out convention; also the notebook that already satisfies CLAUDE.md's Swiss roll gate for this phase's instrument)
    - notebooks/pu_manifold/crossmodal_curvature.py (VERDICT_RULE, VERDICT_VALUES, INSTRUMENT_FIDELITY_RANGE — quoted, not restated from memory)
  </read_first>
  <action>
Create `notebooks/07_crossmodal_curvature_check.ipynb`. It reads the frozen JSONL record and the
fields npz and recomputes nothing — no decoder fit, no MKNN recomputation, no permutation test. A
second computation with drifted conventions is how two documents end up quoting different values
for the same quantity.

Cells, in order:

A markdown cell stating the research question in the developer's own framing — does the curvature of
the PU embedding manifold explain the weak crossmodal convergence reported by the Platonic Universe
paper (arXiv:2509.19453) — and stating that a No is a result, readable only with the D7-02 power
evidence shown further down.

A cell loading the record and the npz, printing the freeze commit, the run commit, `n`,
`HEADLINE_K` and the distinct-value count of the per-point MKNN array beside the arithmetic reason
for it: the statistic is `j/k` for integer `j`, so at `k=20` there are at most 21 distinct values
across 10,000 points.

A table cell: one row per `d` in `D_SWEEP`, columns for holdout variance explained, `cond(g)`
median, `||H||` median, the observed Spearman rho, both tails' thresholds, `clears_either`, and
`direction`.

A scatter cell: for each `d`, `||H||` on one axis against per-point MKNN on the other, with the
massive horizontal banding visible rather than hidden — that banding IS the tie structure and a
reader should see it. Overlay the rank trend. Three panels, one per `d`.

A distribution cell: the three `||H||` distributions overlaid, with p05/p50/p95 marked. State the
spread only as an order of magnitude, with a one-line note that estimated spread is a noisy readout
because the instrument's est/true ratio was measured swinging 0.665 to 1.626 non-monotonically
across a 17-fold range of true spread.

A positive-control cell: target rho against achieved rho against cleared, for the four entries of
`POSITIVE_CONTROL_TARGET_RHOS`, with the smallest cleared target called out. A markdown line
stating what this licenses: if nothing cleared, the phase reports no null.

A diagnostics cell: the density-versus-curvature Spearman, the density-versus-MKNN Spearman, the raw
and density-controlled partials, the density p95/p05 ratio, and both columns' hubness skewness —
each labelled as reported and gating nothing, with the reason in one line: MKNN is a k-NN statistic
and therefore mechanically density-sensitive, which is how Phase 4's result became uninterpretable.

A final markdown cell with three or four printed pass/fail read-out lines and a one-sentence answer:
the verdict verbatim from the record, whether all three `d` agreed, whether the positive control
licensed the reading, and the instrument's fidelity as the RANGE `+0.53` to `+0.99`.

Keep the notebook short enough to read in one sitting. Execute it end to end and commit it with its
outputs, per CLAUDE.md. This is not a Swiss roll sanity check and must not be given that shape — no
model training, no toy fixture, no baseline comparison. Phase 7 introduces no new model, so it owes
no new Swiss roll notebook; Task 2 records why.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json; nb=json.load(open('notebooks/07_crossmodal_curvature_check.ipynb')); code=[c for c in nb['cells'] if c['cell_type']=='code' and ''.join(c['source']).strip()]; assert code, 'no code cells'; bad=[i for i,c in enumerate(code) if not c.get('outputs')]; assert not bad, f'unexecuted code cells: {bad}'; print(len(nb['cells']), 'cells, every code cell carries outputs')"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/07_crossmodal_curvature_check.ipynb` exists and every non-empty code cell carries a non-empty `outputs` list — the notebook was executed end to end before commit.
    - The notebook has at most 15 cells.
    - The notebook loads `notebooks/.cache/07_crossmodal_curvature.jsonl`; a grep of its source shows no call to `train_plain_ae`, `plain_decoder_curvature`, `per_point_mknn` or `permutation_null` — it recomputes nothing.
    - The printed table has exactly three data rows, one per entry of `D_SWEEP`, in that order.
    - The final read-out prints the verdict string verbatim from the record's verdict row.
    - The final read-out prints the instrument fidelity as two numbers, 0.53 and 0.99, drawn from `crossmodal_curvature.INSTRUMENT_FIDELITY_RANGE`.
    - `git status --porcelain notebooks/pu_manifold/ notebooks/diagnostics/ src/effdim/` is empty after this task — the notebook touched no module.
  </acceptance_criteria>
  <done>A committed, executed notebook shows the three-`d` table, the banded scatter, the `||H||` distributions, the positive control and the non-gating diagnostics, and prints the record's verdict with the instrument's fidelity range beside it.</done>
</task>

<task type="auto">
  <name>Task 2: 07-FINDINGS.md — the answer, the limitations, and what the phase does not claim</name>
  <files>.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-FINDINGS.md</files>
  <read_first>
    - .planning/phases/07-curvature-conditioned-crossmodal-alignment/07-04-SUMMARY.md (every measured number, quoted from here rather than recomputed)
    - .planning/phases/07-curvature-conditioned-crossmodal-alignment/07-CONTEXT.md §6 and §8 (the Phase 4 cautionary record, and the explicit must-not-claim list)
    - notebooks/pu_manifold/crossmodal_curvature.py (VERDICT_RULE — the caveats it embeds travel with the verdict and must be restated here)
    - .planning/phases/06-*/06-FINDINGS.md and .planning/phases/05-*/05-FINDINGS.md (the house shape for a findings document: measured tables, accepted gaps stated rather than buried, terminal outcome named plainly)
    - CLAUDE.md § Swiss roll sanity check (the gate whose satisfaction this document records)
  </read_first>
  <action>
Write `.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-FINDINGS.md`.

Open with the research question and the answer in one paragraph: does the curvature of the PU
embedding manifold explain the weak crossmodal convergence reported by arXiv:2509.19453, and what
the frozen record says. State the verdict verbatim from the record's verdict row. If it is
`SPLIT ACROSS d` or `UNDERPOWERED -- NO CLAIM`, state that as a complete terminal outcome of the
phase, in the same register as a positive result — Phase 5's `SPLIT ACROSS SEEDS` and Phase 6's
`NO DETECTABLE RELATIONSHIP` are the precedents for reporting a non-supportive outcome plainly.

A measured-results section: the three-`d` table, the sensitivity grid across `MKNN_K_GRID`, the
positive control's target-versus-achieved table with which entries cleared, and the density and
hubness diagnostics. Every number quoted from `07-04-SUMMARY.md`, not recomputed.

A section on what licensed the reading: the D7-02 positive control. State the smallest planted
effect the test recovered at PU's realized dynamic range, at `n=10,000`, through the identical
permutation machinery. If nothing cleared, state that the phase reports no null and why that is the
honest outcome rather than a failure.

A section on the instrument: it is validated against analytic ground truth at `d=20` and `D=768`,
with fidelity in the RANGE `+0.53` to `+0.99` — quote the range, and name the `cubic@768` cell at
`+0.5253` explicitly, because a reviewer will find it. State that reconstruction quality does not
predict fidelity: `cubic@768` reconstructs at 99.70% and scores `+0.5253` while `ridge@768`
reconstructs at 99.88% and scores `+0.9745`; what separates them is the surface's own curvature
variation, which for PU is unknown.

A CLAUDE.md Swiss roll gate section, stated rather than skipped: Phase 7 introduces no new
manifold-learning or representation-learning model. It reuses `cae.PlainAutoEncoder` and
`decoder_curvature.plain_decoder_curvature` verbatim, and
`notebooks/02.6_swiss_roll_plainae_curvature_check.ipynb` already tests that exact combination. Name
that file as the satisfying artifact. Add that the 2026-08-25 analytic-fixture sweep is a stronger
check than a fresh Swiss roll run would be, because it has a closed-form answer at the same `d` and
`D` PU actually uses, whereas the roll is a 2-D sheet in 3-D. State plainly that this is a
declaration of an already-satisfied gate, not a waiver.

An accepted-limitations section, each one named rather than buried: one seed across the whole
`d`-sweep, accepted against Phase 5's measured seed-instability of decoder curvature fields, so
agreement across `d` is not evidence of seed stability; the field evaluated on all 10,000 rows
including the 8,000 the decoder trained on; PU reconstruction showing no plateau anywhere through
`d=48`, so every `d` in the sweep describes a truncated approximation of PU and the sweep tests
whether that truncation matters rather than removing it; the density partial and hubness numbers
reported and gating nothing; and the per-point MKNN array's at-most-21 distinct values across
10,000 points, which is why the significance route is permutation-based and not `spearmanr`'s
asymptotic p-value.

A what-this-phase-does-not-claim section, reproducing `07-CONTEXT.md` §8 and §6 in full: that the
field measures true curvature; that a null means no relationship exists absent the power evidence;
anything about CKA; anything about MKNN at the source paper's `n=101,725`, where this milestone's
`k/n` chance floor is roughly ten times higher; any reinterpretation of Phases 2, 02.x, 3, 03.1, 4,
5 or 6; and specifically that Phase 4's `HOLDS` is not evidence of a curvature-alignment
association and is not cited as one anywhere in this phase.

A provenance section: the freeze commit SHA, the run commit SHA, the strict-ancestor proof
(`git merge-base --is-ancestor` plus `git rev-list --count <freeze>..HEAD` at least 1, with the
reason the second check is not redundant), the record and npz paths, and a line stating that the
nine pre-existing `notebooks/diagnostics/07_*_run.py` spike scripts are informational inputs that
predate the freeze, satisfy nothing, and were not re-run for any number in this document.

A section naming the two edge-probe rows this phase resolved on planner assumption rather than
developer instruction — the D7-03 density sign convention (`1/w`, not `w`) and the D7-07 scope
proof (`ALIGNMENT_METRIC` as a frozen constant) — so a later reader can see they were choices.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import re,sys; t=open('.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-FINDINGS.md').read(); req=['02.6_swiss_roll_plainae_curvature_check.ipynb','0.53','0.99','n=101,725','merge-base','Phase 4']; missing=[r for r in req if r not in t]; assert not missing, missing; assert '+0.97' not in t.replace('+0.9745',''), 'bare point-estimate fidelity figure present'; print('findings ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-FINDINGS.md` exists.
    - It names `notebooks/02.6_swiss_roll_plainae_curvature_check.ipynb` as the artifact satisfying CLAUDE.md's Swiss roll gate for this phase's instrument.
    - It contains both `0.53` and `0.99` as the instrument's fidelity range, and contains no bare `+0.97` used as the instrument's fidelity.
    - It contains the verdict string verbatim, matching the record's verdict row exactly.
    - It contains an accepted-limitations section listing at least the four limitations named in the action, each as its own bullet or paragraph.
    - It contains a what-this-phase-does-not-claim section covering all five items of `07-CONTEXT.md` §8 plus the Phase 4 `HOLDS` clause from §6.
    - It contains both the freeze commit SHA and the run commit SHA, and both match the `preregistration_commit` and `run_commit` fields stamped on the record's verdict row.
    - The two planner-assumption resolutions (D7-03 density sign, D7-07 scope proof) are named as assumptions.
  </acceptance_criteria>
  <done>The phase's answer, its power evidence, its instrument's honest fidelity range, its accepted limitations, its explicit non-claims and its provenance are all in one document, with the Swiss roll gate declared satisfied by name rather than skipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Read and approve the notebook and findings</name>
  <what-built>
    `notebooks/07_crossmodal_curvature_check.ipynb`, executed end to end and committed with its
    outputs, reading only the frozen record. `07-FINDINGS.md`, stating the verdict, the positive
    control's power evidence, the instrument's fidelity range, the accepted limitations, the explicit
    non-claims, and the freeze provenance.
  </what-built>
  <how-to-verify>
    1. Open the notebook and read it top to bottom. It should take one sitting. Check that the
       three-`d` table matches `07-04-SUMMARY.md` and that the scatter's horizontal banding is
       visible rather than smoothed away — that banding is the tie structure and hiding it would
       misrepresent the statistic.
    2. Read `07-FINDINGS.md`'s answer paragraph. Is the verdict stated in the same register you would
       state it in? A `SPLIT ACROSS d` or `UNDERPOWERED -- NO CLAIM` outcome should read as a
       terminal result, not as an apology.
    3. Read the positive-control section. This is the section that decides whether a null in this
       phase is worth anything. If the control cleared at nothing, confirm the document reports no
       null.
    4. Read the what-this-phase-does-not-claim section against `07-CONTEXT.md` §8 and §6. Anything
       missing or softened is the thing to catch here.
    5. Check the two named planner assumptions — the density `1/w` sign convention and the
       `ALIGNMENT_METRIC` scope proof. Either can be overturned; overturning the density convention
       flips the sign of D7-03's headline diagnostic.
    6. Confirm nothing under `notebooks/pu_manifold/`, `notebooks/diagnostics/` or `src/effdim/`
       changed in this plan's commits, and that none of the nine `07_*_run.py` spike scripts was
       edited or deleted.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what to change</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
New artifacts created by this plan:

- `notebooks/07_crossmodal_curvature_check.ipynb` — the reporting notebook, committed with outputs
- `.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-FINDINGS.md`

No new code symbols. This plan creates no functions, classes, constants or CLI flags; everything it
reads was created by plans 07-01 through 07-04 and is listed in their own artifact sections.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen record → published claim | The only boundary this plan sits on: what the record measured versus what the write-up asserts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering | the notebook recomputing rather than reading | medium | mitigate | The notebook reads the JSONL record and the npz only; an acceptance criterion greps its source for `train_plain_ae`, `plain_decoder_curvature`, `per_point_mknn` and `permutation_null` and requires none present, so a second computation with drifted conventions cannot appear beside the frozen one. |
| T-07-07 | Repudiation | a claim in the findings with no traceable number behind it | medium | mitigate | Every number is quoted from `07-04-SUMMARY.md` and the record; the provenance section carries the freeze and run SHAs, and an acceptance criterion requires them to match the record's own stamped fields. |
| T-07-03 | Repudiation | the record's stamped SHAs | medium | mitigate | Re-asserted here against the findings document rather than trusted from the earlier plan. |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs; `07-RESEARCH.md` § Package Legitimacy Audit records N/A. |

ASVS level 1; block on high. No `high` threat is introduced by this plan. V2/V3/V4/V6 do not apply:
no auth, no sessions, no multi-user access model, no cryptography.
</threat_model>

<verification>
- The notebook is committed with outputs and has at most 15 cells.
- `07-FINDINGS.md` names the Swiss roll gate's satisfying artifact and quotes the fidelity range.
- The verdict string in the findings matches the record's verdict row exactly.
- `git status --porcelain notebooks/pu_manifold/ notebooks/diagnostics/ src/effdim/` is empty.
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0.
</verification>

<success_criteria>
The research question has an interpretable, recorded answer — or a recorded reason why the evidence
does not support one — with the power evidence that licenses reading it, the instrument's honest
fidelity range, the accepted limitations and the explicit non-claims all in the same document, and
the developer has read and approved both artifacts.
</success_criteria>

<output>
Create `.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-05-SUMMARY.md` when done.
</output>
