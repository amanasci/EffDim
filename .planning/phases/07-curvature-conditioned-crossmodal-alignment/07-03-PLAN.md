---
phase: 07-curvature-conditioned-crossmodal-alignment
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - notebooks/pu_manifold/crossmodal_curvature.py
  - notebooks/pu_manifold/tests/test_crossmodal_curvature.py
autonomous: true
requirements: [D7-02, D7-03, D7-05]
user_setup: []

must_haves:
  truths:
    - "plant_positive_control produces per-point values that are exactly j/k for integer j in [0, k], so the planted array carries the same massive-tie discretization the real per-point MKNN array has; equal planted values are expected, not a defect, and are handled by the same average-rank Spearman and permutation route as the real statistic (D7-02 adjacency)."
    - "plant_positive_control raises ValueError naming the offending array when h_real is constant, contains a non-finite value, or has fewer than k + 2 rows — it never returns an empty or degenerate control that would make an underpowered test look powered (D7-02 empty)."
    - "plant_positive_control iterates POSITIVE_CONTROL_TARGET_RHOS in frozen tuple order and returns results in that order; assert_preregistered has already guaranteed the tuple is strictly increasing, so 'the smallest planted effect the test recovers' is defined by that order and not by completion order (D7-02 ordering)."
    - "The planting mechanism is deterministic: two calls with the same h_real, k and POSITIVE_CONTROL_SEED return identical planted arrays and identical achieved Spearman values, because the generator is re-seeded at every bisection trial."
    - "The reported density statistic is taken on 1.0 / w where w = curvature_probe.local_density_weights(X, DENSITY_K, DENSITY_FIELD_D) returns the per-point INVERSE density mean-normalized to 1 — the sign convention Phase 4's REGN-01 print states — and DENSITY_FIELD_D is held at 20 across the whole d-sweep rather than tracking the sweep's d, so the three density numbers stay comparable to each other and to Phase 4's measured -0.0273."
    - "The density partial and the hubness skewness are computed and reported; neither enters apply_verdict, whose signature accepts only the per-d clearance booleans and the positive-control result (D7-03, DIAGNOSTICS_ARE_NON_GATING)."
  artifacts:
    - notebooks/pu_manifold/crossmodal_curvature.py
    - notebooks/pu_manifold/tests/test_crossmodal_curvature.py
  key_links:
    - "plant_positive_control must be fed PU's own realized d=20 ||H|| field, not a synthetic surrogate — planting at PU's actual dynamic range is the whole content of D7-02."
    - "density_diagnostics must call cross_split_curvature.partial_spearman rather than a locally written residualize-and-correlate routine; the in-repo function is already the rank-space partial correlation D7-03 asks for."
  prohibitions:
    - "MUST NOT let any density or hubness number gate, escalate, or overturn the verdict — D7-03 says reported and gating nothing, and Phase 4's uninterpretable result is what that clause is guarding against."
    - "MUST NOT cite Phase 4's HOLDS as evidence of a curvature-alignment association anywhere in this module's docstrings or the record."
    - "MUST NOT write a new partial-correlation, bootstrap-CI, or permutation-test routine from scratch; cross_split_curvature.partial_spearman, scipy.stats.bootstrap and curvature_probe.permutation_null already exist and are already reviewed."
    - "MUST NOT edit notebooks/pu_manifold/linear_probe.py, pointcloud_probe.py, mknn.py, cae.py, decoder_curvature.py, curvature_probe.py, cross_split_curvature.py, or anything under src/effdim/."
    - "MUST NOT edit any constant frozen by plan 07-01; if the planting mechanism as frozen cannot hit a target rho, record the achieved value beside the target rather than adjusting the mechanism."
---

<objective>
Expand the proven tracer path with the two arms it deliberately left out: D7-02's positive control,
which is what licenses the phase to report a null at all, and D7-03's density and hubness
diagnostics, which are reported and gate nothing.

Purpose: D7-02 is the one piece of genuinely new statistical engineering in this phase — no
function in this codebase plants a curvature-alignment relationship at a specified dynamic range,
and Phase 6's `rng.random(n)` selfcheck is explicitly rejected as evidence for PU's much narrower
regime. Without it a null is uninterpretable, and a null is the likely outcome.
Output: `plant_positive_control` and `density_diagnostics` in the frozen module, both pinned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-CONTEXT.md
@.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-RESEARCH.md
@.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-02-SUMMARY.md
</context>

<flagged_planner_assumptions>
The D7-03 edge-probe row came back `unclassified` and its resolution below is a **planner
assumption**, not a settled fact: that the substantive edge is the density SIGN convention.
`curvature_probe.local_density_weights` returns the per-point INVERSE density, mean-normalized to
1; Phase 4's runner prints "rho = 1/w is a RELATIVE density, not an absolute one". Reporting
`spearman(w, ||H||)` instead of `spearman(1/w, ||H||)` silently flips the sign of D7-03's headline
diagnostic and makes it non-comparable to Phase 4's measured `-0.0273`. This plan implements the
`1/w` reading. If the developer reads that edge differently, say so before 07-04 runs.
</flagged_planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the D7-02 positive control at PU's own realized dynamic range</name>
  <files>notebooks/pu_manifold/crossmodal_curvature.py</files>
  <read_first>
    - notebooks/pu_manifold/crossmodal_curvature.py (POSITIVE_CONTROL_TARGET_RHOS, POSITIVE_CONTROL_SEED and especially POSITIVE_CONTROL_RULE, which spells out the planting mechanism in full; plus two_tailed_permutation_null from plan 07-02, which this reuses unchanged)
    - notebooks/pu_manifold/pointcloud_probe.py (Phase 6's selfcheck, which planted rng.random(n) — the ~20x-spread field D7-02 explicitly rejects; read it to see exactly what is being rejected and why)
    - .planning/phases/07-curvature-conditioned-crossmodal-alignment/07-CONTEXT.md §4 and §5 (the measured PU ||H|| spread of 1.495, and the instrument sweep showing est/true swinging 0.665 to 1.626 non-monotonically)
    - notebooks/pu_manifold/curvature_probe.py lines 1100-1150 (permutation_null's guards: non-finite, unequal length, and constant-array via np.ptp == 0)
  </read_first>
  <behavior>
    - `plant_positive_control(h_real, k, target_rhos, seed)` returns one result per entry of `target_rhos`, in that order, each carrying `target_rho`, `achieved_rho`, `slope`, the planted array's distinct-value count, and the full `two_tailed_permutation_null` result.
    - Every planted value is exactly `j / k` for integer `j` in `[0, k]`; the distinct-value count is at most `k + 1`.
    - Two calls with identical arguments return identical planted arrays and identical `achieved_rho` values.
    - `achieved_rho` has the same sign as `target_rho` and lands within a stated distance of it, recorded rather than asserted away.
    - `ValueError` naming the offending array is raised when `h_real` is constant, contains a non-finite value, or has fewer than `k + 2` rows.
    - `smallest_cleared_target(results)` returns the smallest `target_rho` whose `clears_either` is True, or `None` when none cleared — the value `apply_verdict` consumes.
  </behavior>
  <action>
Add `plant_positive_control(h_real, k, target_rhos, seed)` and
`smallest_cleared_target(results)` to `notebooks/pu_manifold/crossmodal_curvature.py`. Both pure —
no file I/O, no defaults on any pre-registered parameter.

Implement the mechanism EXACTLY as `POSITIVE_CONTROL_RULE` already spells it out; the rule string
is the specification and the code is its implementation, not the other way round. Rank-transform
with `scipy.stats.rankdata` to `u = (rankdata(h_real) - 0.5) / n`. For a candidate slope, form
`p = np.clip(0.5 + slope * (u - 0.5), 0.0, 1.0)`, draw `j = np.random.default_rng(seed).binomial(k, p)`
— re-creating the generator from `seed` on every trial, so the whole search is deterministic — and
take the planted array as `j / k`. Search `slope` by 40 iterations of bisection on `[0.0, 2.0]`
against the achieved `scipy.stats.spearmanr(h_real, planted).statistic`, keeping the endpoint whose
achieved value brackets the target. Record both `target_rho` and `achieved_rho`; never substitute
one for the other and never widen the bracket to force a match — if a target is unreachable under
the frozen mechanism, the recorded `achieved_rho` says so, which is itself the honest result.

Guard first, before any search: raise `ValueError` naming `h_real` when it is constant (mirror
`curvature_probe.permutation_null`'s own `np.ptp(arr) == 0` guard and its message shape), when it
contains a non-finite value, or when `h_real.shape[0] < k + 2`. A degenerate control that silently
returns something is the single worst failure available here — it would make an underpowered test
look powered and license a null the evidence does not support.

For each target, run the planted pair through `two_tailed_permutation_null` with the SAME
`N_PERMUTATIONS`, `PERMUTATION_SEED` and `NULL_QUANTILE_PER_TAIL` the headline test uses, passed in
explicitly by the caller. Reusing the identical machinery is the point: a control run through
different machinery measures the power of a different test.

Add `smallest_cleared_target(results)` returning the smallest `target_rho` among results whose
`clears_either` is True, else `None`. Because `assert_preregistered` has already guaranteed
`POSITIVE_CONTROL_TARGET_RHOS` is strictly increasing, this is just the first clearing entry in
iteration order — implement it that way and say so in the docstring.

Docstring must state, citing D7-02: that the control plants at PU's realized `||H||` dynamic range
by taking PU's own `d=20` field as `h_real` rather than a synthetic surrogate; that Phase 6's
`rng.random(n)` selfcheck does not serve because it planted a ~20x-spread field against PU's
order-2x; and that a `None` return from `smallest_cleared_target` forces the
`UNDERPOWERED -- NO CLAIM` verdict rather than a null.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_crossmodal_curvature.py -q -k positive_control</automated>
  </verify>
  <acceptance_criteria>
    - `crossmodal_curvature.plant_positive_control` and `crossmodal_curvature.smallest_cleared_target` exist and are importable.
    - On a synthetic `h_real` of 2,000 points drawn to match PU's measured p95/p05 spread of roughly 1.5, `plant_positive_control(h_real, 20, (0.02, 0.05, 0.10, 0.20), 20260825)` returns 4 results in that order, each with `achieved_rho` within 0.02 of its `target_rho` and of the same sign.
    - Every returned planted array satisfies `np.allclose(planted * 20, np.round(planted * 20))` and has at most 21 distinct values.
    - Calling `plant_positive_control` twice with identical arguments returns bitwise-identical planted arrays.
    - `plant_positive_control(np.ones(500), 20, (0.05,), 1)` raises `ValueError` whose message names `h_real`.
    - `smallest_cleared_target` returns `None` for an all-`False` result list and the smallest clearing `target_rho` otherwise.
    - `git diff --stat <freeze-commit>..HEAD -- notebooks/pu_manifold/curvature_probe.py notebooks/pu_manifold/pointcloud_probe.py notebooks/pu_manifold/linear_probe.py notebooks/pu_manifold/mknn.py` produces empty output.
  </acceptance_criteria>
  <done>A curvature-MKNN relationship can be planted at PU's own dynamic range and run through the identical test machinery, deterministically, with the achieved effect recorded beside the target — so a null from the real data can be told apart from an underpowered test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the D7-03 density and hubness diagnostics, gating nothing</name>
  <files>notebooks/pu_manifold/crossmodal_curvature.py</files>
  <read_first>
    - notebooks/pu_manifold/curvature_probe.py lines 161-200 (`local_density_weights` — note it returns the INVERSE density `w`, mean-normalized to 1, and takes an explicit intrinsic-dimension argument `d`)
    - notebooks/diagnostics/region_partition_mknn_run.py lines 175-192 (Phase 4's REGN-01 block: `w = local_density_weights(X_ls, k_density=30, d=20)`, `rho = 1.0 / w`, and the p05/p50/p95 print — the number this phase must stay comparable to)
    - notebooks/pu_manifold/cross_split_curvature.py lines 232-290 (`partial_spearman`'s full contract: rank-transforms x, y and each control column, residualizes by least squares with intercept, returns Pearson on residuals)
    - notebooks/pu_manifold/mknn.py lines 170-185 (`hubness_skewness` and `chance_floor` signatures)
    - .planning/phases/07-curvature-conditioned-crossmodal-alignment/07-CONTEXT.md §6 (the Phase 4 cautionary record — the +0.8208 density/direction confound, the region-size artifact, and the instruction that HOLDS is not evidence of an association)
  </read_first>
  <behavior>
    - `density_diagnostics(X_ambient, h, m, z_a, z_b, k, density_k, density_field_d)` returns a flat dict of plain floats.
    - It reports `spearman_density_vs_h`, `spearman_density_vs_mknn`, `partial_rho_raw`, `partial_rho_density_controlled`, the density p05/p50/p95 and their p95/p05 ratio, `hubness_skewness_a`, `hubness_skewness_b`, and `chance_floor`.
    - The density array used throughout is `1.0 / w`, never `w`.
    - `partial_rho_raw` equals `cross_split_curvature.partial_spearman(h, m, controls=None)` and, on a continuous fixture with no ties, matches `scipy.stats.spearmanr(h, m).statistic` to within tolerance.
    - Passing the density as a control changes `partial_rho_density_controlled` away from `partial_rho_raw` on a fixture where density is deliberately correlated with both, and leaves it essentially unchanged on a fixture where density is independent of both.
    - No value returned by this function is accepted by `apply_verdict`, whose signature takes only the per-`d` clearance mapping and the positive-control result.
  </behavior>
  <action>
Add `density_diagnostics(X_ambient, h, m, z_a, z_b, k, density_k, density_field_d)` to
`notebooks/pu_manifold/crossmodal_curvature.py`. Pure — no file I/O, no defaults on any
pre-registered parameter.

Compute `w = curvature_probe.local_density_weights(X_ambient, density_k, density_field_d)` and then
`density = 1.0 / w`. Add a comment on that line naming the reason: `local_density_weights` returns
the per-point INVERSE local density mean-normalized to 1, so `1/w` is the relative density and using
`w` directly would flip the sign of every statistic below and break comparability with Phase 4's
measured `spearman(density, ||H||) = -0.0273`.

Report `spearman_density_vs_h` and `spearman_density_vs_mknn` via `scipy.stats.spearmanr`.
Report `partial_rho_raw` as `cross_split_curvature.partial_spearman(h, m, controls=None)` and
`partial_rho_density_controlled` as `cross_split_curvature.partial_spearman(h, m, controls=density)`.
Do not write a residualize-and-correlate routine — the in-repo function is already the rank-space
partial correlation D7-03 names, already reviewed, and reusing it keeps Phase 7's number defined the
same way the source branch defined it.

Report the density distribution as p05, p50, p95 and the p95/p05 ratio, mirroring Phase 4's REGN-01
print so the two phases' density pictures sit side by side. Report
`hubness_skewness_a = mknn.hubness_skewness(z_a, k)` and `hubness_skewness_b` for the other column,
and `chance_floor = mknn.chance_floor(n, k)`.

Coerce every returned value to a plain Python float — the record is JSONL and the Phase 6 amendment
`fix(06): serialize numpy arrays in the Phase 6 record` is the cautionary precedent.

The docstring must state, citing D7-03: these numbers are reported and gate nothing; MKNN is a k-NN
statistic and therefore mechanically density-sensitive; and this is precisely how Phase 4's result
became uninterpretable, which is why the diagnostic exists here as a disclosure rather than a gate.
It must also state plainly that Phase 4's `HOLDS` is not evidence of a curvature-alignment
association and is not cited as one anywhere in this phase.

Do not change `apply_verdict`'s signature. Its inability to accept a density number is the
mechanical expression of `DIAGNOSTICS_ARE_NON_GATING`.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_crossmodal_curvature.py -q -k density</automated>
  </verify>
  <acceptance_criteria>
    - `crossmodal_curvature.density_diagnostics` exists and returns a dict whose every value is an instance of `float` (checked with `all(isinstance(v, float) for v in result.values())`).
    - The returned dict's keys include `spearman_density_vs_h`, `spearman_density_vs_mknn`, `partial_rho_raw`, `partial_rho_density_controlled`, `density_p05`, `density_p50`, `density_p95`, `density_ratio_p95_p05`, `hubness_skewness_a`, `hubness_skewness_b`, `chance_floor`.
    - On a continuous, tie-free fixture, `partial_rho_raw` matches `scipy.stats.spearmanr(h, m).statistic` to `pytest.approx(rel=1e-6)`.
    - On a fixture where a common latent drives density, `h` and `m`, `abs(partial_rho_density_controlled) < abs(partial_rho_raw)`; on an independent fixture the two agree to within 0.05.
    - `inspect.signature(crossmodal_curvature.apply_verdict).parameters` has exactly two entries, neither of them naming density — the non-gating property is structural, not a promise.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0.
  </acceptance_criteria>
  <done>The density partial, the density-vs-curvature Spearman, the density distribution and both columns' hubness skewness are computed on the `1/w` convention Phase 4 used, and none of them can reach the verdict function.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin both arms with tests and extend the runner's selfcheck</name>
  <files>notebooks/pu_manifold/tests/test_crossmodal_curvature.py, notebooks/diagnostics/07_crossmodal_curvature_run.py</files>
  <read_first>
    - notebooks/pu_manifold/tests/test_crossmodal_curvature.py (extend; do not replace)
    - notebooks/pu_manifold/crossmodal_curvature.py (plant_positive_control, smallest_cleared_target, density_diagnostics as written in Tasks 1 and 2)
    - notebooks/diagnostics/07_crossmodal_curvature_run.py (the `--selfcheck` tally written in plan 07-02, and the `--mode positive-control` stub that currently raises)
  </read_first>
  <behavior>
    - Every behavior listed in Tasks 1 and 2 has a test.
    - `--mode positive-control` no longer raises `NotImplementedError`; it loads the PU pair, reads a previously computed `d=20` field from the record, runs `plant_positive_control`, and appends one row per target to the frozen record.
    - `--selfcheck` gains assertions for the planted array's `j/k` discretization, the planter's determinism, and `partial_spearman`'s agreement with raw Spearman on a tie-free fixture.
  </behavior>
  <action>
Extend `notebooks/pu_manifold/tests/test_crossmodal_curvature.py` with one test per behavior from
Tasks 1 and 2. Build the positive-control fixture at 2,000 points with a spread matched to PU's
measured 1.495 — draw from a lognormal and rescale so the p95/p05 ratio lands near 1.5, then assert
that ratio in the fixture itself so a future edit cannot quietly widen it and make the control easy.
Use `n_resamples=199` in tests to keep the file fast; pass it explicitly, since these functions take
no defaults on pre-registered parameters.

Add a test asserting that a control planted at a WIDE spread (ratio near 20, matching Phase 6's
rejected `rng.random(n)` selfcheck) clears at a smaller target rho than the same procedure at PU's
narrow spread — this is the measurable content of D7-02's objection to Phase 6's selfcheck, and
turning it into a passing test is stronger than restating it in prose. If the two do not separate,
record the measurement rather than adjusting the fixture; that is itself a finding about the
control.

Implement `--mode positive-control` in `notebooks/diagnostics/07_crossmodal_curvature_run.py`.
Call `crossmodal_curvature.assert_preregistered()` first. Read the `d=20` `||H||` field from the
frozen record written by plan 07-04's sweep — accept a `--field-npz` path resolved through
`cache.cache_path` / `cache._assert_inside_cache`, and raise a clear error naming plan 07-04 if the
field is not present rather than silently regenerating it. Run `plant_positive_control` with
`POSITIVE_CONTROL_TARGET_RHOS`, `POSITIVE_CONTROL_SEED`, `HEADLINE_K`, `N_PERMUTATIONS`,
`PERMUTATION_SEED` and `NULL_QUANTILE_PER_TAIL`, then append one flat record row per target
carrying `target_rho`, `achieved_rho`, `slope`, `clears_either`, `direction`, both tail thresholds,
and the planted array's distinct-value count. Print `smallest_cleared_target`'s value, or the string
naming that nothing cleared and that the verdict is therefore `UNDERPOWERED -- NO CLAIM`.

Extend `--selfcheck` with the three new assertions and keep its tally-sums-to-total invariant.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_crossmodal_curvature.py -q && .venv/bin/python notebooks/diagnostics/07_crossmodal_curvature_run.py --selfcheck</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_crossmodal_curvature.py -q` exits 0 in under 60 seconds.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0 — full notebook suite green.
    - `.venv/bin/python notebooks/diagnostics/07_crossmodal_curvature_run.py --selfcheck` exits 0 and its printed tally counts sum to its printed total.
    - `.venv/bin/python notebooks/diagnostics/07_crossmodal_curvature_run.py --mode positive-control` with no field available exits non-zero with a message naming plan 07-04 and the missing field, rather than regenerating anything.
    - The wide-spread-versus-narrow-spread separation test is present and its measured outcome is recorded in the plan SUMMARY whether or not the two separated.
    - `grep -c "notebooks/.cache" notebooks/pu_manifold/tests/test_crossmodal_curvature.py` returns 0.
  </acceptance_criteria>
  <done>Both expansion arms are pinned by tests, the positive-control mode is runnable and refuses to invent a field it does not have, and D7-02's objection to Phase 6's selfcheck is a measured comparison rather than an assertion.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
New symbols created by this plan:

- Functions added to `notebooks/pu_manifold/crossmodal_curvature.py`:
  `plant_positive_control(h_real, k, target_rhos, seed)`,
  `smallest_cleared_target(results)`,
  `density_diagnostics(X_ambient, h, m, z_a, z_b, k, density_k, density_field_d)`
- Runner mode `--mode positive-control` and flag `--field-npz` in
  `notebooks/diagnostics/07_crossmodal_curvature_run.py`
- New tests in `notebooks/pu_manifold/tests/test_crossmodal_curvature.py`

Symbols imported unchanged and NOT created here: `curvature_probe.local_density_weights`,
`curvature_probe.permutation_null`, `cross_split_curvature.partial_spearman`,
`mknn.hubness_skewness`, `mknn.chance_floor`, `scipy.stats.rankdata`, `scipy.stats.spearmanr`,
`cache.cache_path`, `cache._assert_inside_cache`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → runner CLI | `--field-npz` joins `--record-path` as a path-valued flag |
| frozen source → produced number | `assert_preregistered()` at every mode entry point |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | `--record-path` and `--field-npz` on `07_crossmodal_curvature_run.py` | low | mitigate | Both paths are resolved through `cache.cache_path` and passed to `cache._assert_inside_cache` before any open; a traversal path raises rather than reads or writes. Implemented in Task 3. |
| T-07-02 | Tampering | frozen constants, especially the planting mechanism in `POSITIVE_CONTROL_RULE` | high | mitigate | `assert_preregistered()` runs first in every mode; the mechanism is specified in the frozen rule string and implemented from it, so a post-hoc adjustment to make a null look powered would show as a diff against the freeze commit. |
| T-07-05 | Tampering | the positive control itself | high | mitigate | The control is the evidence that licenses a null. A degenerate or silently-adjusted control is the phase's worst failure mode: `plant_positive_control` guards constant/non-finite/too-short input by raising, records `achieved_rho` beside `target_rho` rather than substituting, and never widens its bisection bracket to force a match. |
| T-07-03 | Repudiation | record rows | medium | mitigate | Every appended row carries `preregistration_commit` and `run_commit`. |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package-manager installs; `07-RESEARCH.md` § Package Legitimacy Audit records N/A. |

ASVS level 1; block on high. T-07-02 and T-07-05 are both `high` and both mitigated, neither
accepted. V2/V3/V4/V6 do not apply: no auth, no sessions, no multi-user access model, no
cryptography.
</threat_model>

<verification>
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0.
- `--selfcheck` exits 0 with a self-consistent tally.
- No sealed module changed since the freeze commit.
- `apply_verdict`'s signature still cannot accept a density number.
</verification>

<success_criteria>
The positive control plants deterministically at PU's realized dynamic range and runs through the
identical test machinery; the density partial, density distribution and hubness skewness are
computed on the `1/w` convention and reported without gating anything; both arms are pinned by
fast tests.
</success_criteria>

<output>
Create `.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-03-SUMMARY.md` when done.
Record the measured outcome of the wide-spread-versus-narrow-spread separation test verbatim,
including the case where the two did not separate.
</output>
