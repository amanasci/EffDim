---
phase: 02.2-chart-autoencoder-validity-test-inserted
plan: 05
type: execute
wave: 4
depends_on: ["02.2-01", "02.2-03"]
files_modified:
  - notebooks/diagnostics/cae_train_run.py
autonomous: true
requirements: [CAE-02, CAE-03, CAE-05, CAE-06]

must_haves:
  truths:
    - "The runner halts rather than regenerating when any gitignored Phase 1 or Phase 2 cache is absent, because regenerating changes provenance."
    - "The runner halts before any fit unless the pre-registration commit is a provable git ancestor of HEAD, so CAE-01's ordering is enforced at run time and not merely asserted in prose."
    - "The runner re-draws the 200,000 geodesic pairs under the recorded pair seed and halts unless the redraw is bit-identical to the cached array, so every distortion number downstream rests on a verified pairing."
    - "Every protocol value used by every fit is a named module-level constant copied verbatim from the pre-registration, never a magic number inlined at a call site."
    - "Three independent Chart Auto-Encoder fits complete under distinct recorded seeds, each within the wall-clock ceiling or explicitly marked truncated, and each persists a fit artifact carrying the contracted key set (D-08, CAE-02)."
    - "A ReLU-activated control fit at the main seed completes under the identical protocol, so the C2-smooth activation substitution has a measured reconstruction cost rather than an asserted one (CAE-06)."
    - "Both plain single-chart autoencoder controls and both classical-MDS decoder baselines complete under the identical protocol at matched capacity (CAE-03)."
    - "A completed run is never recomputed: a second invocation finds every artifact through the sidecar-manifest cache and exits without re-fitting."
  prohibitions:
    - statement: "MUST NOT regenerate any absent Phase 1 or Phase 2 cache — the runner halts, because regenerating changes provenance and breaks comparability with the audited FAIL."
      status: unverified
    - statement: "MUST NOT evaluate any gating metric on rows the model trained on, or silently substitute a different subsample or column than the frozen legacysurvey array under fit_key 43cf438bc944c509."
      status: unverified
    - statement: "MUST NOT handicap the baseline controls with shorter training, smaller capacity, or an un-fitted decoder in order to manufacture a CAE win."
      status: unverified
    - statement: "MUST NOT re-run the protocol with fresh seeds and report only the run that passes — the three pre-registered seeds and their spread are reported whatever they show."
      status: unverified
  artifacts:
    - path: "notebooks/diagnostics/cae_train_run.py"
      provides: "Precondition assertions, the pre-registered constants block, the holdout split, the timing probe, and the full fit registry for three CAE seeds plus five controls"
      contains: "THRESH_DISTORTION_MAX"
      min_lines: 260
  key_links:
    - from: "notebooks/diagnostics/cae_train_run.py"
      to: ".planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md"
      via: "every constant is copied verbatim from Section 4 as a named module-level constant, with the section cited in a header comment"
      pattern: "PREREGISTRATION"
    - from: "notebooks/diagnostics/cae_train_run.py"
      to: "notebooks/pu_manifold/cae.py"
      via: "imports train_cae, train_plain_ae, train_mlp_decoder, fit_linear_decoder, timing_probe, state_dict_to_arrays"
      pattern: "from pu_manifold"
    - from: "notebooks/diagnostics/cae_train_run.py"
      to: "notebooks/pu_manifold/cache.py"
      via: "every fit artifact is written through npz_cache and json_cache, so a completed run is a cache hit on re-invocation"
      pattern: "npz_cache"
---

<objective>
Write the training runner and execute the full pre-registered fit protocol: three Chart
Auto-Encoder seeds, one ReLU-activated control, two plain single-chart autoencoder controls, and
two classical-MDS decoder baselines.

Purpose: this is the expensive, irreversible part of the phase. Everything it produces is cached
under the frozen `fit_key`, and the guards it runs before the first fit — cache presence, git
ancestry of the pre-registration, bit-identity of the geodesic pair sample — are what make the
resulting numbers admissible evidence rather than merely numbers.

Output: `notebooks/diagnostics/cae_train_run.py`, and the full set of gitignored fit artifacts
under `notebooks/.cache/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-CONTEXT.md
@.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-RESEARCH.md
@.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PATTERNS.md
@.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md
@.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-03-SUMMARY.md
@notebooks/diagnostics/geometry_probes_run.py
@notebooks/pu_manifold/cae.py
@notebooks/pu_manifold/cache.py
</context>

<interfaces>
Facts confirmed during planning; the executor does not need to re-derive them.

Frozen Phase 1/2 cache artifacts under `notebooks/.cache/` (all gitignored, all present):
- `subsample_20260729_a79b3460b838fd0a.npz` — `hsc (10000, 768) float64`,
  `legacysurvey (10000, 768) float64`, `hsc_norms`, `ls_norms`, `row_indices (10000,) int64`.
  Sidecar manifest: `{dataset: legacysurvey_dinov3_vitb16, datasets_version: 5.0.1, n_rows: 10000,
  normalize: true, numpy_version: 2.5.1, seed: 20260729}`.
- `mds_eigenspectrum_43cf438bc944c509.npz` — `eigvals_all (10000,)`, `eigvals_top (40,)`,
  `eigvecs_top (10000, 40)`, `mds_coords (10000, 40)`, `geo_pairs_r2 (200000,) float64`,
  `geo_pairs_r2_check (200000,)`, `n_positive`, `k_eff`, `spectrum_seconds`.
  Sidecar manifest: `{d_sweep_max: 40, fit_key: 43cf438bc944c509, numpy_version: 2.5.1,
  r2_pair_count: 200000, r2_pair_seed: 20260731, r2_pair_seed_check: 20260732,
  scipy_version: 1.18.0}`.
- `isomap_43cf438bc944c509.joblib` (1.55 GiB) and its meta json, which records
  `dataset: legacysurvey_dinov3_vitb16`, `n_neighbors: 15`, `n_components: 18`. **This phase does
  not need to load the joblib**: the geodesic pair values come from the cached `geo_pairs_r2`
  array, and the classical-MDS coordinates come from `mds_coords`. Loading a 1.55 GiB pickle is
  avoided entirely.

The training array is the `legacysurvey` column — confirmed against the Isomap fit's own
`dataset` field, not inferred.

`notebooks/diagnostics/geometry_probes_run.py` is the runner-shape precedent: docstring stating the
halt-don't-regenerate precondition and the `PYTHONPATH=notebooks python ...` invocation, a
pre-registered-constants block with the source document cited in a comment, banner-printed steps
with timing, `_to_native` before any json write, and a required-keys assertion at the end.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write cae_train_run.py — preconditions, constants, split, timing probe, fit registry</name>
  <precondition>notebooks/.cache/subsample_20260729_a79b3460b838fd0a.npz and notebooks/.cache/mds_eigenspectrum_43cf438bc944c509.npz both exist and are non-empty — Phase 1 and Phase 2 artifacts, gitignored and not reproducible here.</precondition>
  <files>notebooks/diagnostics/cae_train_run.py</files>
  <read_first>
    - `notebooks/diagnostics/geometry_probes_run.py` lines 1-135 — the docstring, constants block, `gate_stats`/`_to_native`/`_print_rss` helpers, and the banner-printed step structure to mirror
    - `notebooks/diagnostics/geometry_probes_run.py` lines 159-190 — the STEP 0 self-check and STEP 1 pair-identity check, which this runner reproduces for its own preconditions
    - `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md` — Section 3's split rule and Section 4's constants, copied verbatim
    - `notebooks/pu_manifold/cae.py` — `train_cae`, `train_plain_ae`, `train_mlp_decoder`, `fit_linear_decoder`, `timing_probe`, `state_dict_to_arrays`, and the documented fit-artifact key set
    - `notebooks/pu_manifold/cache.py` — `npz_cache`, `json_cache`, `cache_path`
  </read_first>
  <action>
Create `notebooks/diagnostics/cae_train_run.py`.

Module docstring in the shape of `geometry_probes_run.py`'s: what the script does (Phase 02.2's
Chart Auto-Encoder fit protocol over the frozen Phase 1 subsample at
`fit_key = 43cf438bc944c509`), the precondition sentence stating the gitignored caches must exist
and that the script halts rather than regenerating because regenerating would change provenance,
the statement that all constants are fixed in `02.2-PREREGISTRATION.md` before any of these numbers
existed, and the invocation line `PYTHONPATH=notebooks python notebooks/diagnostics/cae_train_run.py`.

A pre-registered-constants block with a header comment citing `02.2-PREREGISTRATION.md` Section 4,
declaring every constant named in that section as a module-level name: `FIT_KEY`, `D_CHART`,
`L_EMBED`, `N_CHARTS_INIT`, `HIDDEN_WIDTH`, `ACTIVATION`, `LIP_WEIGHT`, `LIP_EVERY_N_STEPS`,
`WEIGHT_DECAY`, `ADAM_LR`, `BATCH`, `MAX_EPOCHS`, `EARLY_STOP_PATIENCE`, `EARLY_STOP_MIN_DELTA`,
`WALLCLOCK_CEILING_S`, `FPS_PRETRAIN_EPOCHS`, `PRUNE_TOL`, `OVERLAP_P_MIN`, `OVERLAP_MIN_POINTS`,
`HOLDOUT_FRACTION`, `SPLIT_SEED`, `SEEDS`, `MAIN_SEED`, `UNFAITHFUL_SAMPLES`, `UNFAITHFUL_SEED`,
`PAIR_SEED`, `PAIR_COUNT`, `MDS_BASELINE_P`, `PLAIN_AE_LATENTS`, `THRESH_DISTORTION_MAX`,
`THRESH_RCYCLE_RATIO_MAX`, `THRESH_RECON_MARGIN`. No protocol value may appear as a literal at a
call site; every cfg dict is assembled from these names.

STEP 0, preconditions, all three of which halt on failure and none of which has a fallback:
first, assert both required cache files exist, raising a `FileNotFoundError` naming the missing path
and stating that regenerating would change provenance;
second, resolve the pre-registration's adding commit with
`git log --diff-filter=A --format=%H -- <prereg path> | tail -1` via `subprocess`, assert it is
non-empty, and assert `git merge-base --is-ancestor <sha> HEAD` exits zero — this is CAE-01's
ordering, enforced at run time rather than asserted in prose, and a failure here means a fit is
about to run ahead of its own pre-registration;
third, reproduce the geodesic pair sample: build a generator at `PAIR_SEED`, call
`geometry_probes.draw_geo_pairs` for `PAIR_COUNT` pairs over 10,000 rows, and — because this phase
deliberately does not load the 1.55 GiB Isomap pickle — verify the pairing by asserting that the
cached `geo_pairs_r2_check` array's relationship to `geo_pairs_r2` is the one Phase 2 recorded, and
that the redrawn row and column index arrays have the expected shape and contain no self-pairs.
Persist the redrawn `rows` and `cols` arrays into the run's own cache so the evaluation runner in
plan 02.2-06 consumes exactly the same pairing rather than redrawing it.

STEP 1, data and split: load the frozen subsample npz, take the `legacysurvey` array, assert its
shape is 10,000 by 768, and build the split with
`np.random.default_rng(SPLIT_SEED).permutation(10000)`, the first
`int(10000 * (1 - HOLDOUT_FRACTION))` indices as train and the remainder as holdout. Print both
sizes and the sha256 of each index array so a reader can confirm the split downstream.

STEP 2, timing probe: call `cae.timing_probe` for 50 steps at full scale and write
`cae_timing_probe_{FIT_KEY}.json` through `json_cache`. Apply the pre-registered contingency: if the
probe reports the projection exceeds the ceiling, set the effective `lip_every_n_steps` to 4 for
every fit in the protocol including the controls; otherwise keep 1. Print which branch was taken
and record it in the probe artifact. This is a pre-registered conditional rule, not a judgment made
at run time.

STEP 3, the fit registry. Define a list of run descriptors, each with an identifier, the cache
stems it writes, and a callable that performs the fit. The registry entries are: three Chart
Auto-Encoder fits at `SEEDS`, writing `cae_fit_{FIT_KEY}_seed{seed}` and
`cae_fit_meta_{FIT_KEY}_seed{seed}`; one ReLU-activated Chart Auto-Encoder control at `MAIN_SEED`,
identical in every other respect, writing `cae_ctrl_relu_{FIT_KEY}` and
`cae_ctrl_relu_meta_{FIT_KEY}`; two plain single-chart autoencoder controls at each latent in
`PLAIN_AE_LATENTS`, writing `cae_ctrl_plainae{latent}_{FIT_KEY}` and its meta; two classical-MDS
decoder baselines at each dimension in `MDS_BASELINE_P`, each fitted from `mds_coords[:, :p]` on the
training rows, writing `cae_ctrl_mdsdec{p}_{FIT_KEY}` and its meta, and each also recording the
least-squares linear decoder's held-out error as a floor.

Every fit writes its arrays through `npz_cache` and its metadata through `json_cache`, keyed by a
cfg dict containing every protocol constant that fit used. That gives three properties at once: a
completed run is a cache hit on re-invocation so the script is resumable across sessions; a cfg
change raises rather than silently reusing a stale artifact; and the artifact records the exact
protocol it was produced under.

The npz payload for each Chart Auto-Encoder fit is the contracted key set from `cae.py`'s
fit-artifact docstring: every `state_dict_to_arrays` key, plus `z_all`, `p_all`,
`chart_argmax_all`, `train_idx`, `holdout_idx`, `y_holdout`. The controls store their own state_dict
arrays plus their holdout reconstructions and the same index arrays.

Add an `argparse` interface with a `--runs` option accepting one or more run identifiers, defaulting
to all of them, so a long protocol can be executed in chunks across sessions. Print a banner per run
with its elapsed time, and a closing table of every run identifier with its epochs, wall-clock
seconds, and whether it early-stopped or was truncated.

Do not execute the full protocol in this task — Task 2 does that. Verify only that the script's
preconditions and timing probe run clean, by invoking it with an empty run selection.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim && PYTHONPATH=notebooks .venv/bin/python notebooks/diagnostics/cae_train_run.py --runs none && test -f notebooks/.cache/cae_timing_probe_43cf438bc944c509.json</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/diagnostics/cae_train_run.py` exists and its module docstring contains the invocation line `PYTHONPATH=notebooks python notebooks/diagnostics/cae_train_run.py`.
    - Every one of the 32 pre-registered constant names listed in the action appears as a module-level assignment: `grep -c '^[A-Z_]* = ' notebooks/diagnostics/cae_train_run.py` returns at least 32.
    - Running with a temporarily renamed `mds_eigenspectrum_43cf438bc944c509.npz` exits non-zero with a `FileNotFoundError` naming that path, and creates no new cache artifact.
    - Running from a state where the pre-registration commit is not an ancestor of HEAD exits non-zero before any fit begins.
    - `notebooks/.cache/cae_timing_probe_43cf438bc944c509.json` exists and contains `seconds_per_step`, `projected_wallclock_s`, `exceeds_ceiling`, and the resulting `lip_every_n_steps`.
    - The script prints the train and holdout sizes as 8000 and 2000 and the sha256 of each index array.
    - `--runs` accepts a subset and the default selects every registry entry.
    - The 1.55 GiB Isomap pickle is not loaded: `grep -v '^#' notebooks/diagnostics/cae_train_run.py | grep -c 'mmap_mode'` returns 0.
  </acceptance_criteria>
  <done>The runner exists, refuses to proceed on a missing cache or an unproven pre-registration ordering, fixes the split reproducibly, and has written the timing probe that sets the compute contingency for every fit.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the three Chart Auto-Encoder seed fits</name>
  <precondition>notebooks/.cache/cae_timing_probe_43cf438bc944c509.json exists and records a resolved lip_every_n_steps.</precondition>
  <files>notebooks/.cache/cae_fit_43cf438bc944c509_seed20260803.npz, notebooks/.cache/cae_fit_43cf438bc944c509_seed20260804.npz, notebooks/.cache/cae_fit_43cf438bc944c509_seed20260805.npz (gitignored fit artifacts)</files>
  <read_first>
    - `notebooks/diagnostics/cae_train_run.py` — the registry identifiers, so the right subset is selected
    - `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md` — Section 7's expected cost, so a run that overruns badly is recognised as a deviation rather than accepted silently
  </read_first>
  <action>
Execute the three Chart Auto-Encoder fits at the pre-registered seeds. These are long-running CPU
jobs — the pre-registered ceiling is two wall-clock hours per fit and the three together are
expected to occupy several hours — so launch the runner as a background process and poll for
completion rather than blocking a foreground call. Invoke it once per seed, or once with the three
seed run identifiers selected, so a failure in one does not discard the others; each completed fit
is a cache hit on any later invocation.

If a fit returns `wallclock_truncated` True, that is a recorded outcome and not an error to be
worked around: the ceiling is a pre-registered compute constant. Do not raise the ceiling, do not
lower the epoch cap, and do not retry with different constants. Record the truncation and carry it
forward — the evaluation runner reports it and the verdict artifact carries it.

When all three have completed, confirm each wrote both its npz and its meta json, and print the
closing table of epochs, wall-clock seconds, and stopping reason per seed. Commit the runner's
unchanged state along with a summary note; the cache artifacts themselves are gitignored and are
not committed.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim && for s in 20260803 20260804 20260805; do test -f "notebooks/.cache/cae_fit_43cf438bc944c509_seed${s}.npz" || exit 1; test -f "notebooks/.cache/cae_fit_meta_43cf438bc944c509_seed${s}.json" || exit 1; done && echo three-seed-fits-present</automated>
  </verify>
  <acceptance_criteria>
    - All six artifacts exist: `cae_fit_43cf438bc944c509_seed{20260803,20260804,20260805}.npz` and their `cae_fit_meta_...json` companions.
    - Each meta json contains `epochs_run`, `wallclock_s`, `wallclock_truncated`, `early_stopped`, `seed`, `activation`, `torch_version`, `numpy_version`, `timestamp`, and a `cfg` echoing every protocol constant.
    - Each meta json's `activation` field is the pre-registered C2-smooth activation, not the reference implementation's.
    - Each npz contains all of `z_all`, `p_all`, `chart_argmax_all`, `train_idx`, `holdout_idx`, `y_holdout`, with `z_all` of shape `(10000, 40)` and `p_all` of shape `(10000, 16)`.
    - Every fit's `train_idx` and `holdout_idx` are identical across the three seeds — the split is fixed by `SPLIT_SEED`, not by the fit seed.
    - Each fit's final-epoch total loss is strictly below its first main-stage epoch total loss.
    - Re-invoking the runner for the same three seeds completes without re-fitting, proving the cache-hit path.
  </acceptance_criteria>
  <done>Three independent Chart Auto-Encoder fits exist under distinct recorded seeds with the contracted artifact key set, each honouring the pre-registered stopping rule and recording which limit fired.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the ReLU control and the four CAE-03 baseline fits</name>
  <precondition>The three Chart Auto-Encoder seed fits from Task 2 are present in notebooks/.cache/ so the controls train under the same resolved lip_every_n_steps.</precondition>
  <files>notebooks/.cache/cae_ctrl_relu_43cf438bc944c509.npz, notebooks/.cache/cae_ctrl_plainae20_43cf438bc944c509.npz, notebooks/.cache/cae_ctrl_plainae40_43cf438bc944c509.npz, notebooks/.cache/cae_ctrl_mdsdec8_43cf438bc944c509.npz, notebooks/.cache/cae_ctrl_mdsdec20_43cf438bc944c509.npz (gitignored fit artifacts)</files>
  <read_first>
    - `notebooks/diagnostics/cae_train_run.py` — the control registry entries and their cfg assembly
    - `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md` — Section 4's T3 definition and Section 2's CAE-06 substitution-cost requirement
  </read_first>
  <action>
Execute the remaining five registry entries: the ReLU-activated Chart Auto-Encoder control at the
main seed, the two plain single-chart autoencoder controls at the pre-registered latents, and the
two classical-MDS decoder baselines at the pre-registered dimensions.

The ReLU control exists solely to give CAE-06's "any reconstruction cost" a measured value: it is
the same architecture, the same seed, the same protocol, and the same budget as the main fit, with
only the activation differing. Run it under the ceiling like any other fit.

The plain autoencoder controls and the MDS decoder baselines are the CAE-03 comparison. Each runs
under the identical protocol — same optimizer, learning rate, weight decay, batch size, stopping
rule — so the eventual comparison measures architecture rather than training budget. Each MDS
baseline also records the least-squares linear decoder's held-out error as a floor beneath the
fitted decoder.

These are smaller than the Chart Auto-Encoder fits and should complete well inside the envelope;
still, launch as a background process and poll. When everything is done, print the closing table
across all eight runs and confirm every artifact exists.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim && test -f notebooks/.cache/cae_ctrl_relu_43cf438bc944c509.npz && test -f notebooks/.cache/cae_ctrl_plainae20_43cf438bc944c509.npz && test -f notebooks/.cache/cae_ctrl_plainae40_43cf438bc944c509.npz && test -f notebooks/.cache/cae_ctrl_mdsdec8_43cf438bc944c509.npz && test -f notebooks/.cache/cae_ctrl_mdsdec20_43cf438bc944c509.npz && echo all-controls-present</automated>
  </verify>
  <acceptance_criteria>
    - All five control artifacts and their meta json companions exist: `cae_ctrl_relu_43cf438bc944c509`, `cae_ctrl_plainae20_43cf438bc944c509`, `cae_ctrl_plainae40_43cf438bc944c509`, `cae_ctrl_mdsdec8_43cf438bc944c509`, `cae_ctrl_mdsdec20_43cf438bc944c509`.
    - The ReLU control's meta json `cfg` differs from the main seed fit's `cfg` in the activation field and in no other field.
    - Every control's meta json `cfg` reports the same `lr`, `weight_decay`, `batch`, `max_epochs`, `early_stop_patience`, `early_stop_min_delta`, and `wallclock_ceiling_s` as the main Chart Auto-Encoder fit — the controls are not handicapped.
    - Each MDS baseline meta json contains both the fitted-decoder held-out error and the linear least-squares floor.
    - Every control npz contains `train_idx` and `holdout_idx` identical to the Chart Auto-Encoder fits'.
    - The closing table prints eight rows, one per registry entry, each with epochs, wall-clock seconds, and stopping reason.
    - `notebooks/.cache/` contains no artifact named for a seed outside `SEEDS` — no exploratory extra run was left behind.
  </acceptance_criteria>
  <done>Every control and baseline the CAE-03 and CAE-06 requirements compare against has been fitted under the identical protocol at matched capacity, and all eight runs are recorded with their stopping reasons.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol Phase 02.2 creates. Newly-created symbols are not drift.

**Files created**
- `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md`
- `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md`
- `notebooks/pu_manifold/cae.py`
- `notebooks/pu_manifold/tests/test_cae.py`
- `notebooks/diagnostics/cae_train_run.py`
- `notebooks/diagnostics/cae_evaluate_run.py`

**Classes in `notebooks/pu_manifold/cae.py`**
`InitialEncoder`, `ChartEncoder`, `ChartDecoder`, `EmbeddingDecoder`, `ChartPredictor`,
`ChartAutoEncoder`, `PlainAutoEncoder`, `MlpDecoder`

**Functions in `notebooks/pu_manifold/cae.py`**
`activation_module`, `mlp_stack`, `chart_loss`, `lipschitz_penalty`, `farthest_point_sample`,
`fps_pretrain_loss`, `train_cae`, `timing_probe`, `train_plain_ae`, `train_mlp_decoder`,
`fit_linear_decoder`, `reconstruction_stats`, `r_cycle`, `select_overlap_pairs`,
`unfaithfulness_coverage`, `chart_survival`, `fit_global_scale`, `embedding_distortion`,
`verdict_from_metrics`, `write_cae_verdict`, `write_cae_handoff`, `to_native`,
`state_dict_to_arrays`, `arrays_to_state_dict`

**Test functions in `notebooks/pu_manifold/tests/test_cae.py`**
`test_end_to_end_smoke_writes_verdict_artifact`, `test_loss_matches_hand_computation`,
`test_lipschitz_regularizer_matches_svd`, `test_fps_selects_farthest_points`,
`test_fps_reproducible_under_fixed_seed`, `test_chart_decoder_targets_embedding_space`,
`test_single_chart_fails_circle_topology`, `test_r_cycle_zero_for_identity_charts`,
`test_pruning_removes_dead_chart`, `test_pruning_boundary_is_strict_less_than`,
`test_pruning_statistic_no_underflow`, `test_unfaithfulness_coverage_hand_check`,
`test_cae_verdict_cache_roundtrip`, `test_verdict_rule_strict_inequality`,
`test_verdict_rejects_missing_or_nonfinite_metric`, `test_verdict_json_is_byte_stable`,
`test_verdict_metrics_are_native_float64_roundtrip`,
`test_distortion_uses_global_embedding_not_chart_coords`,
`test_overlap_selection_halts_below_min_points`, `test_state_dict_array_roundtrip`,
`test_train_cae_respects_wallclock_ceiling`

**New gitignored cache stems under `notebooks/.cache/`**
`cae_fit_{fit_key}_seed{seed}`, `cae_fit_meta_{fit_key}_seed{seed}`, `cae_ctrl_relu_{fit_key}`,
`cae_ctrl_relu_meta_{fit_key}`, `cae_ctrl_plainae{latent}_{fit_key}`,
`cae_ctrl_plainae{latent}_meta_{fit_key}`, `cae_ctrl_mdsdec{p}_{fit_key}`,
`cae_ctrl_mdsdec{p}_meta_{fit_key}`, `cae_timing_probe_{fit_key}`, `cae_pairs_{fit_key}`,
`cae_verdict_{fit_key}`, `cae_handoff_{fit_key}`

**JSON field names introduced**
In `cae_verdict_{fit_key}.json`: `CAE_VERDICT`, `fit_key`, `phase`, `metrics`, `thresholds`,
`verdict_rule`, `chart_count_surviving`, `chart_count_by_seed`, `unfaithfulness`, `coverage`,
`krein_comparison_reported_not_gating`, `activation_substitution`, `assumptions`, `remediation`,
`holdout`, `seeds`, `wallclock`, `global_scale_factor`, `versions`, `timestamp`.
In `cae_handoff_{fit_key}.json`: `fit_key`, `phase`, `verdict`, `consumes`,
`global_embedding_key`, `chart_coords_key`, `chart_probs_key`, `chart_assignments_key`,
`decoder_state_keys`, `surviving_charts`, `activation`, `timestamp`.

**Pre-registered constant names (module-level in both runners)**
`FIT_KEY`, `D_CHART`, `L_EMBED`, `N_CHARTS_INIT`, `HIDDEN_WIDTH`, `ACTIVATION`, `LIP_WEIGHT`,
`LIP_EVERY_N_STEPS`, `WEIGHT_DECAY`, `ADAM_LR`, `BATCH`, `MAX_EPOCHS`, `EARLY_STOP_PATIENCE`,
`EARLY_STOP_MIN_DELTA`, `WALLCLOCK_CEILING_S`, `FPS_PRETRAIN_EPOCHS`, `PRUNE_TOL`,
`OVERLAP_P_MIN`, `OVERLAP_MIN_POINTS`, `HOLDOUT_FRACTION`, `SPLIT_SEED`, `SEEDS`, `MAIN_SEED`,
`UNFAITHFUL_SAMPLES`, `UNFAITHFUL_SEED`, `PAIR_SEED`, `PAIR_COUNT`, `MDS_BASELINE_P`,
`PLAIN_AE_LATENTS`, `THRESH_DISTORTION_MAX`, `THRESH_RCYCLE_RATIO_MAX`, `THRESH_RECON_MARGIN`

**CLI invocations introduced**
- `PYTHONPATH=notebooks python notebooks/diagnostics/cae_train_run.py [--runs ...]`
- `PYTHONPATH=notebooks python notebooks/diagnostics/cae_evaluate_run.py`
- `python -m pytest notebooks/pu_manifold/tests/test_cae.py -q`
</artifacts_this_phase_produces>

<flagged_assumptions>
Unresolved edge-probe rows carried forward as explicit assumptions, not silently dropped.

- **CAE-02 — unclassified.** No shape category applied by the deterministic probe. Assumption:
  seeding torch's global generator, the numpy shuffle generator, and the farthest-point-sampling
  generator from one recorded integer satisfies "reproducible from recorded seeds" on a single
  machine; cross-machine bit-reproducibility of CPU reductions is not claimed.
- **CAE-03 — unclassified.** Assumption: "matched capacity" is operationalised as the same hidden
  width, the same layer count per side, and the identical training protocol, rather than an exact
  parameter-count match.
- **CAE-06 — unclassified.** Assumption: one ReLU-activated control fit at the main seed is an
  adequate measurement of the activation-substitution reconstruction cost; a per-seed ReLU control
  was not budgeted against the overnight envelope.

Planner-added, not probe-surfaced: the geodesic pair sample is verified here without loading the
1.55 GiB Isomap pickle, because the pair *values* live in the cached `geo_pairs_r2` array. The
full bit-identity check 02.1 ran against `dist_matrix_` is therefore not repeated; if a reviewer
wants that stronger check, it costs one 1.55 GiB pickle load.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> run-time ordering gate | The runner's CAE-01 assertion is only as strong as the history it queries |
| pre-registration document -> runner constants | A transcription error between the document and the constants block silently changes the protocol the gate was registered against |
| gitignored caches -> training input | Absent or substituted inputs would produce numbers incomparable with the audited FAIL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-15 | Tampering | A fit running ahead of its own pre-registration | high | mitigate | STEP 0 resolves the pre-registration's adding commit and asserts `git merge-base --is-ancestor` against HEAD, exiting non-zero before any fit begins |
| T-02.2-16 | Tampering | A missing cache silently regenerated, changing provenance | high | mitigate | STEP 0 raises `FileNotFoundError` naming the path, with no fallback recompute path in the script at all |
| T-02.2-17 | Tampering | A stale fit artifact reused under a changed protocol | high | mitigate | Every artifact is written through the sidecar-manifest cache keyed by a cfg containing every protocol constant; a cfg change raises rather than returning the stale artifact |
| T-02.2-18 | Tampering | Deserializing an attacker-controlled pickle path | medium | mitigate | The 1.55 GiB Isomap pickle is not loaded by this phase at all; the cache module never accepts a caller-supplied absolute path and composes every path it loads from its own cache directory |
| T-02.2-19 | Spoofing | An exploratory extra seed left in the cache and later mistaken for protocol evidence | medium | mitigate | Task 3's acceptance criteria assert no artifact exists for a seed outside the pre-registered set; the prohibitions block forbids reporting only a passing run |
| T-02.2-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; RESEARCH.md's Package Legitimacy Audit records zero new packages and `torch==2.13.0+cpu` was vetted in a prior phase's legitimacy gate |
</threat_model>

<verification>
- `notebooks/diagnostics/cae_train_run.py` exists and halts on a missing cache or an unproven pre-registration ordering.
- All eight fit artifacts and their meta json companions exist under `notebooks/.cache/`.
- Every meta json echoes a `cfg` whose protocol values match the pre-registered constants.
- The split index arrays are identical across all eight runs.
- Re-invoking the runner completes without re-fitting.
</verification>

<success_criteria>
The full pre-registered fit protocol has run: three Chart Auto-Encoder seeds, one ReLU control for
the CAE-06 substitution cost, and four CAE-03 baselines, every one of them under the identical
protocol, inside the wall-clock ceiling or explicitly marked truncated, with the ordering against
the pre-registration proved at run time rather than asserted.
</success_criteria>

<output>
Create `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-05-SUMMARY.md` when done.
</output>