---
phase: 02.2-chart-autoencoder-validity-test-inserted
plan: 01
subsystem: docs
tags: [pre-registration, gate-thresholds, ratification, git-ancestry, chart-autoencoder]

requires:
  - phase: 02.1-geometry-representation-research
    provides: "02.1's classical q=0 distortion reference numbers (median_abs_rel=0.179641 at p=40, 0.121024 at p=8) used to argue T1's threshold"
  - phase: 02-eigenspectrum-audit-validity-gate
    provides: "02-REFIT-PREREGISTRATION.md section skeleton, frontmatter fields, and prohibition phrasing mirrored here; verdict_rule strict-less-than prose style"
provides:
  - "A committed, git-ancestry-provable pre-registration fixing all three CAE gate metric definitions and numeric thresholds (T1 median_abs_rel<0.15, T2 mean(R_cycle)<2.0*mean(E_base), T3 per-dim MSE >=10% below both baselines)"
  - "Every architecture and compute constant the training runner will copy verbatim (D_CHART, L_EMBED, N_CHARTS_INIT, SEEDS, thresholds, etc.)"
  - "A signed human ratification record (date 2026-08-04, verbatim resume signal, proposed-vs-ratified table, resolved ancestry SHA) satisfying D-10 before any fit runs"
affects: ["02.2-02", "02.2-03", "02.2-04", "02.2-05", "02.2-06", "phase-3-decoder-curvature"]

tech-stack:
  added: []
  patterns:
    - "Pre-registration-before-fit discipline: doc committed and ratified before any measured number exists, ordering proved via `git merge-base --is-ancestor` rather than asserted"
    - "Ratification record embeds the resolved ancestry SHA in the document itself so a reader never has to re-derive it"

key-files:
  created:
    - .planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md
  modified: []

key-decisions:
  - "All three proposed gate thresholds ratified exactly as written on 2026-08-04 — no amendments requested (T1=0.15, T2 ratio=2.0, T3 margin=0.10)"
  - "Ancestry proof resolved: adding commit c2c4c93f4dcb8cd5bbafb66e6d5b59d78480af93 confirmed an ancestor of HEAD via git merge-base --is-ancestor, and the SHA recorded verbatim in Section 8"

patterns-established:
  - "Pattern: blocking checkpoint recorded on paper before proceeding, its resolution (ratified/amended) appended as a discrete section and commit, never merged silently into the original pre-registration commit"

requirements-completed: [CAE-01, CAE-06]

coverage:
  - id: D1
    description: "02.2-PREREGISTRATION.md committed with status: pre-registered, fixing T1/T2/T3 gate metric definitions and numeric thresholds, and every architecture/compute constant, before any CAE fit exists"
    requirement: "CAE-01"
    verification:
      - kind: other
        ref: "grep -c 'THRESH_DISTORTION_MAX|THRESH_RCYCLE_RATIO_MAX|THRESH_RECON_MARGIN' 02.2-PREREGISTRATION.md >= 3"
        status: pass
    human_judgment: false
  - id: D2
    description: "Git-ancestry proof that the pre-registration's adding commit precedes HEAD, resolvable without re-deriving"
    requirement: "CAE-01"
    verification:
      - kind: other
        ref: "git log --diff-filter=A --format=%H -- 02.2-PREREGISTRATION.md | tail -1 | xargs -I{} git merge-base --is-ancestor {} HEAD"
        status: pass
    human_judgment: false
  - id: D3
    description: "SiLU activation substitution (CAE-06) and Theorem 2 compactness/reach-as-unverified-assumption recorded in Section 2, with tau/C named as never estimated for the PU point cloud"
    requirement: "CAE-06"
    verification:
      - kind: other
        ref: "grep -i 'tau' and grep -i 'reach' in Section 2 of 02.2-PREREGISTRATION.md"
        status: pass
    human_judgment: false
  - id: D4
    description: "Human ratification of the proposed numeric thresholds recorded with date, verbatim resume signal, and proposed-vs-ratified table before any training task runs (D-10)"
    requirement: "CAE-01"
    verification:
      - kind: other
        ref: "grep -v '^#' 02.2-PREREGISTRATION.md | grep -qi 'ratifi' (Section 8 no longer contains TBD)"
        status: pass
    human_judgment: false

duration: ~7min (active task execution across two sessions, separated by a blocking human-verify checkpoint pause)
completed: 2026-08-04
status: complete
---

# Phase 02.2 Plan 01: Pre-registration and D-10 Ratification Summary

**Committed and ratified `02.2-PREREGISTRATION.md` fixing all three Chart Auto-Encoder gate thresholds (T1 distortion <0.15, T2 cycle-residual ratio <2.0, T3 reconstruction margin >=10%) with a git-ancestry-provable ordering and a signed human ratification record, before any CAE fit exists.**

## Performance

- **Duration:** ~7 min of active execution (Task 1 commit at 2026-08-04T13:48:51Z, Task 3 commit at 2026-08-04T13:54:47Z), spanning two agent sessions separated by Task 2's blocking `checkpoint:human-verify` gate awaiting user ratification
- **Started:** 2026-08-04T13:48:51Z (Task 1 commit)
- **Completed:** 2026-08-04T13:54:47Z (Task 3 commit)
- **Tasks:** 3 (Task 1 auto, Task 2 blocking checkpoint, Task 3 auto)
- **Files modified:** 1 (`02.2-PREREGISTRATION.md`, created then amended in place)

## Accomplishments

- Wrote and committed `02.2-PREREGISTRATION.md` with all nine required sections, every architecture/compute constant, and all three gating metric definitions with exact computation and numeric thresholds (T1 `THRESH_DISTORTION_MAX=0.15`, T2 `THRESH_RCYCLE_RATIO_MAX=2.0`, T3 `THRESH_RECON_MARGIN=0.10`)
- Held the D-10 blocking checkpoint: the user reviewed the document and the git-ancestry proof, then ratified all three thresholds exactly as proposed (resume signal `"ratified"`, no amendments)
- Appended the Section 8 ratification record: date 2026-08-04, verbatim resume signal, a proposed-vs-ratified table for T1/T2/T3, and the resolved ancestry commit SHA `c2c4c93f4dcb8cd5bbafb66e6d5b59d78480af93`
- Verified the ancestry-proof mechanism end to end — `git log --diff-filter=A --format=%H -- 02.2-PREREGISTRATION.md | tail -1` resolves to `c2c4c93`, and `git merge-base --is-ancestor c2c4c93 HEAD` exits 0 — the ordering pre-registration -> ratification -> training is proved, not asserted
- Confirmed `notebooks/.cache/cae_*` is empty — no fit artifact exists at the end of this plan, satisfying CAE-01's ordering requirement structurally

## Task Commits

Each task was committed atomically:

1. **Task 1: Write 02.2-PREREGISTRATION.md with every constant and all three numeric thresholds** - `c2c4c93` (docs)
2. **Task 2: D-10 ratification of the proposed thresholds** - blocking `checkpoint:human-verify`, no code change; resolved by user response `"ratified"` (2026-08-04)
3. **Task 3: Append the ratification record and any amendments, then commit** - `281d5be` (docs)

**Plan metadata:** committed separately below (this SUMMARY.md + STATE.md + ROADMAP.md + REQUIREMENTS.md)

## Files Created/Modified

- `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md` - created in Task 1 (297 lines, nine sections, all constants and thresholds); amended in Task 3 to replace the Section 8 `TBD` stub with the real ratification record

## Decisions Made

- All three proposed gate thresholds ratified exactly as written on 2026-08-04 — no amendments requested. T1 stays `median_abs_rel < 0.15` (between 02.1's matched-dimension classical baseline 0.179641 and its best rung 0.121024). T2 stays `mean(R_cycle) < 2.0 * mean(E_base)`. T3 stays a strict 10% relative margin below both the plain-AE20 and MDS-decoder-8 controls.
- Since no amendment occurred, the frontmatter `amends:` field remains `none` and no `### Amendment A1` subheading was added — per the plan's conditional instruction.
- The pre-registration's adding-commit SHA (`c2c4c93f4dcb8cd5bbafb66e6d5b59d78480af93`) is recorded verbatim inside the document itself (Section 8), so any later plan or reader can verify the CAE-01 ordering claim without re-deriving it from git log.

## Deviations from Plan

None - plan executed exactly as written. The user ratified all three thresholds without requesting any numeric change, so Task 3's conditional amendment path (Amendment A1 subheading, `amends:` field update) was correctly not exercised.

## Issues Encountered

None. The continuation agent verified Task 1's commit (`c2c4c93`) and the intact `02.2-PREREGISTRATION.md` content before resuming at Task 2's resolution, per the resume instructions.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- CAE-01's ordering requirement is structurally satisfied: gate metrics and thresholds are pre-registered, committed, and ratified, with the ordering proved by git ancestry rather than asserted, and no CAE fit has run.
- Plan 02.2-02 (and downstream plans through 02.2-06) can now proceed to build `notebooks/pu_manifold/cae.py` and the training/evaluation runners, copying every pre-registered constant verbatim as named module-level values — per Section 6's prohibition, the constants transcribed here are not revisable without a documented amendment plus a full re-run.
- No blockers. `notebooks/.cache/cae_*` remains empty, confirmed at the end of this plan.

---
*Phase: 02.2-chart-autoencoder-validity-test-inserted*
*Completed: 2026-08-04*

## Self-Check: PASSED

- FOUND: `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md`
- FOUND: `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-01-SUMMARY.md`
- FOUND commit: `c2c4c93` (Task 1 - pre-registration)
- FOUND commit: `281d5be` (Task 3 - ratification record)
