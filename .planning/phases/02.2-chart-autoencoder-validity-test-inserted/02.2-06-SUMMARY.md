---
phase: 02.2-chart-autoencoder-validity-test-inserted
plan: 06
subsystem: ml-evaluation
tags: [torch, chart-autoencoder, arxiv-1912.10094, pre-registered-gate, verdict, negative-result]

requires:
  - phase: 02.2-chart-autoencoder-validity-test-inserted
    provides: "02.2-04's metric functions (chart_survival, r_cycle, select_overlap_pairs, unfaithfulness_coverage, embedding_distortion, reconstruction_stats, verdict_from_metrics) and 02.2-05's eight cached fit artifacts plus the redrawn geodesic pair sample"
provides:
  - "notebooks/diagnostics/cae_evaluate_run.py: reloads all eight cached fits, computes T1/T2/T3 and every non-gating measure, applies the tested strict-less-than verdict rule, writes cae_verdict_43cf438bc944c509.json"
  - "02.2-FINDINGS.md: the phase's written finding — every measured number, the verdict, deviations/assumptions carried forward, and the resolved phase-gate decision"
  - "02.2-PREREGISTRATION.md Section 9: closed outcome section; Sections 4/5 unedited since ratification"
  - "ROADMAP.md: Phase 02.2 marked complete (6/6, CAE_VERDICT=FAIL); Phase 02.3 (Chart Auto-Encoder Iteration) proposed as an inserted, not-yet-planned phase; Phase 3's dependency moved from Phase 02.2 PASS to Phase 02.3 PASS"
affects: ["02.3 (proposed)", "3"]

tech-stack:
  added: []
  patterns:
    - "T3's compound two-control AND condition (mse_cae must clear a 10% margin over BOTH the plain-AE and MDS-decoder controls) collapsed into verdict_from_metrics' single-scalar strict-less-than contract by taking the worse-case ratio max(mse_cae/mse_control_A, mse_cae/mse_control_B) against threshold (1 - THRESH_RECON_MARGIN) -- algebraically identical to the pre-registration's own rule divided through by mse_control on each side, never a re-derivation of it"
    - "Importing the training runner module (cae_train_run) as the single source of pre-registered constants (avoiding any re-declaration) also re-runs its own preconditions and full fit registry as an import-time side effect -- a pure cache-hit read of the same eight already-trained artifacts, not a re-fit, matching 02.2-05's own established cache-hit contract"
    - "A negative gate outcome (FAIL) gets the same documentation discipline as a passing one: FINDINGS.md records the measured numbers first (Sections 1-7, verdict sealed), then a separately-delimited subsection (7a) records the human decision made after reading them -- the measured record and the forward-looking decision never share a paragraph, so a future reader can trust §1-7 was never touched by what came after"

key-files:
  created:
    - notebooks/diagnostics/cae_evaluate_run.py
    - .planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md
  modified:
    - .planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md
    - .planning/ROADMAP.md

key-decisions:
  - "T3's gate metric is max(mse_cae/mse_plain_ae20, mse_cae/mse_mds_dec8) compared via strict-less-than against 1-THRESH_RECON_MARGIN (0.90) -- the pre-registration's own 'mse_cae < (1-margin)*mse_control' rule for both controls, divided through by mse_control on each side so the comparison direction stays uniform with T1/T2, never a reinterpretation of the ratified 0.10 margin"
  - "At the blocking phase-gate checkpoint, the user reviewed CAE_VERDICT=FAIL (T1 distortion 0.296981 vs <0.15; T3 worst-case ratio 3.586350 vs <0.90; T2 passed 1.089366 vs <2.0) and chose to iterate rather than adopt 02.1's Krein representation or stop and report -- the FAIL is not reinterpreted, softened, or revisited by this choice; it is permanent history"
  - "Phase 02.3 (Chart Auto-Encoder Iteration) proposed in ROADMAP.md as an inserted, not-yet-planned phase (requirements undefined, needs a /gsd-discuss-phase or /gsd-phase session); Phase 3's dependency moved in ROADMAP.md from Phase 02.2's (sealed, permanently FAIL) verdict to Phase 02.3's own future PASS verdict, never an automatic fallback to Krein"

patterns-established:
  - "Negative-result gate outcomes documented with the same rigor as passing ones: a dedicated 'phase gate decision' subsection, added only after the checkpoint resolves, is kept structurally separate from the measured-numbers sections that preceded it"

requirements-completed: [CAE-03, CAE-04, CAE-05, CAE-06, CAE-07]

coverage:
  - id: D1
    description: "A machine-readable cae_verdict_43cf438bc944c509.json exists carrying CAE_VERDICT (PASS/FAIL) plus every pre-registered metric and threshold (CAE-07), computed by the tested verdict_from_metrics rule and never re-derived inline in the runner"
    requirement: "CAE-07"
    verification:
      - kind: other
        ref: "PYTHONPATH=notebooks .venv/bin/python notebooks/diagnostics/cae_evaluate_run.py -- required-keys assertion at script exit; independent json.load + key-presence check against all 19 contracted verdict keys"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every gate comparison in the written verdict is strict less-than -- a metric exactly equal to its threshold does not clear it (CAE-07 boundary edge), inherited from cae.py's already-tested verdict_from_metrics, never re-derived"
    requirement: "CAE-07"
    verification:
      - kind: unit
        ref: "notebooks/pu_manifold/tests/test_cae.py#test_verdict_rule_strict_inequality"
        status: pass
    human_judgment: false
  - id: D3
    description: "Held-out reconstruction error reported as both an aggregate metric and a per-output-dimension distribution (mean/median/p95/max), against the matched-capacity single-chart plain-AE control and the fitted classical-MDS decoder, for the CAE and every control (CAE-03)"
    requirement: "CAE-03"
    verification:
      - kind: other
        ref: "notebooks/.cache/cae_verdict_43cf438bc944c509.json metrics.t3.per_dim (six models: cae, relu_control, plain_ae20, plain_ae40, mds_dec8, mds_dec20)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Chart-transition cycle residual (T2) reported over held-out points using the pre-registered overlap rule, with the qualifying-point count stated (CAE-04)"
    requirement: "CAE-04"
    verification:
      - kind: other
        ref: "notebooks/.cache/cae_verdict_43cf438bc944c509.json metrics.t2 (n_qualify=2000, mean_r_cycle, mean_base, ratio)"
        status: pass
    human_judgment: false
  - id: D5
    description: "Over-specified initial chart count, surviving count after pruning at the pre-registered tolerance, and the surviving count for each of the three seeds all reported, together with unfaithfulness and coverage (CAE-05)"
    requirement: "CAE-05"
    verification:
      - kind: other
        ref: "notebooks/.cache/cae_verdict_43cf438bc944c509.json chart_count_surviving, chart_count_by_seed (16/16 on all three seeds), unfaithfulness=0.042333, coverage=0.024"
        status: pass
    human_judgment: false
  - id: D6
    description: "C2-smooth activation substitution reported with its measured reconstruction cost against the ReLU control (signed), and the compact-manifold-with-positive-reach assumption carried into the verdict artifact as an unverified assumption, not evidence of success (CAE-06)"
    requirement: "CAE-06"
    verification:
      - kind: other
        ref: "notebooks/.cache/cae_verdict_43cf438bc944c509.json activation_substitution (delta_mse_relu_minus_silu=-1.283e-06) and assumptions[0] (Theorem 2 reach/density-ratio never estimated)"
        status: pass
    human_judgment: false
  - id: D7
    description: "The 02.1 Krein comparison appears in the verdict under a field naming it as reported and not gating (D-01), read from the cached geometry_probes artifact rather than recomputed"
    requirement: "CAE-03"
    verification:
      - kind: other
        ref: "notebooks/.cache/cae_verdict_43cf438bc944c509.json krein_comparison_reported_not_gating.gates_pass_fail == false; source references geometry_probes_43cf438bc944c509.json"
        status: pass
    human_judgment: false
  - id: D8
    description: "Sections 4 and 5 of 02.2-PREREGISTRATION.md are unedited since ratification; Section 9 was appended only after the fits ran and the verdict was computed"
    requirement: "CAE-01"
    verification:
      - kind: other
        ref: "git diff -- 02.2-PREREGISTRATION.md across this plan's commits touches only the '## 9. Outcome' section (verified line-range diff)"
        status: pass
    human_judgment: false
  - id: D9
    description: "On the measured FAIL, the run halted, 02.2-FINDINGS.md recorded the finding as a complete milestone outcome, and no downstream artifact (cae_handoff_43cf438bc944c509.json) exists or was written (D-02)"
    requirement: "CAE-07"
    verification:
      - kind: other
        ref: "ls notebooks/.cache/cae_handoff_* -> no such file; notebooks/diagnostics/cae_evaluate_run.py grep -ci fallback == 0"
        status: pass
    human_judgment: false
  - id: D10
    description: "The blocking phase-gate checkpoint was resolved by an explicit human decision (iterate, not adopt-Krein or stop-and-report) after reading the measured FAIL, recorded in 02.2-FINDINGS.md 7a and ROADMAP.md without reinterpreting the verdict"
    verification: []
    human_judgment: true
    rationale: "Which of the three documented remediation paths to take after a FAIL is inherently a human decision under D-02 -- the plan's own checkpoint exists specifically because automation must not choose it."

duration: ~15min active execution across three atomic task commits (session interrupted once mid-run by an API spend-limit error between Task 2 and the checkpoint resume; resumed cleanly, no rework)
completed: 2026-08-04
status: complete
---

# Phase 02.2 Plan 06: Chart Auto-Encoder Evaluation — Verdict and Phase Gate Summary

**Wrote and ran `notebooks/diagnostics/cae_evaluate_run.py` against the eight cached fits: `CAE_VERDICT = FAIL` (T1 geodesic distortion 0.296981 vs `<0.15`; T3 held-out reconstruction margin 3.586350 vs `<0.90`; T2 passed 1.089366 vs `<2.0`). At the resulting blocking phase gate the user chose to iterate rather than adopt 02.1's Krein representation or stop, so Phase 02.2 does not hand off to Phase 3 and a follow-up Phase 02.3 (Chart Auto-Encoder Iteration) is proposed in ROADMAP.md.**

## Performance

- **Duration:** ~15 min active task execution across three commits (14:48:52 to 17:32:15 local time on 2026-08-04, with a spend-limit interruption between Task 2's completion and the checkpoint resume — no work was lost or redone)
- **Started:** 2026-08-04T14:48:52-04:00 (Task 1 commit)
- **Completed:** 2026-08-04T17:32:15-04:00 (Task 3 commit)
- **Tasks:** 3 (`type="auto"` Tasks 1-2, `type="checkpoint:human-verify"` Task 3)
- **Files modified:** 4 (`notebooks/diagnostics/cae_evaluate_run.py` created, `02.2-FINDINGS.md` created, `02.2-PREREGISTRATION.md` and `ROADMAP.md` modified), plus one gitignored cache artifact (`cae_verdict_43cf438bc944c509.json`)

## Accomplishments

- **`cae_evaluate_run.py` (638 lines):** reloads all eight cached fit artifacts (three CAE seeds, ReLU control, two plain-AE controls, two MDS-decoder baselines), asserts split-index identity across all eight before computing anything (T-02.2-23), and computes every pre-registered metric: T1 geodesic distortion on the main seed's global embedding (all-pairs and a holdout-only non-gating companion), T2 chart-transition cycle residual over the 2,000-row overlap set, T3 held-out reconstruction versus both gating controls plus two non-gating companions and each MDS baseline's linear floor. Applies the tested `verdict_from_metrics` rule — never re-derived inline — and writes `cae_verdict_43cf438bc944c509.json` via `write_cae_verdict`. Contains no fallback path to a different representation (verified by a zero-count grep) and calls `write_cae_handoff` only inside a PASS branch (never exercised this run).
- **Measured verdict: `CAE_VERDICT = FAIL`.** T1 (0.296981, threshold `<0.15`) and T3 (worst-case ratio 3.586350, threshold `<0.90`) both failed by wide margins — not near-misses. T2 passed comfortably (1.089366, threshold `<2.0`). The CAE's 16-chart atlas reconstructs held-out data roughly 3.6x worse than the matched-capacity single-chart plain autoencoder and 2.2x worse than the classical-MDS decoder baseline; its global embedding distorts geodesic distances more than either of 02.1's classical reference rungs. All 16 initial charts survived pruning on every one of the three seeds (16/16 x 3) — the atlas did not collapse to a smaller effective count, it simply reconstructed worse across the board.
- **`02.2-FINDINGS.md` (now 313 lines):** the full written finding — pre-registered design table, the three-gate results table, every non-gating measure (chart survival, unfaithfulness/coverage, CAE-06 activation substitution cost, the Krein comparison reported-not-gating), deviations/assumptions carried forward (Theorem 2's unverified compactness/reach hypothesis foremost), the wall-clock record, and the verdict's meaning. Written before the phase-gate decision was known; a later, separately-delimited §7a records the resolved decision without touching anything above it.
- **`02.2-PREREGISTRATION.md` §9 closed:** the outcome section now states the measured verdict and gate table; Sections 4 and 5 (the thresholds and interpretation rule) are byte-identical to their ratified form — verified by `git diff` touching only §9's line range.
- **Phase gate resolved (Task 3):** the user reviewed the verdict and chose **iterate** over adopt-Krein or stop-and-report. `02.2-FINDINGS.md` §7a and `ROADMAP.md` record this without reinterpreting the FAIL: Phase 02.2 is marked complete (6/6 plans) with its FAIL verdict standing as permanent record; a new, not-yet-planned **Phase 02.3: Chart Auto-Encoder Iteration** is proposed (candidate investigation axes: chart count, chart latent dimension `D_CHART=20`, training budget/epochs, loss weighting, the Lipschitz penalty schedule); Phase 3's dependency in `ROADMAP.md` moves from Phase 02.2's PASS to Phase 02.3's own future PASS. No `cae_handoff_43cf438bc944c509.json` exists or will be written for this fit_key.

## Task Commits

1. **Task 1: Write cae_evaluate_run.py — every metric, the verdict, and the handoff** - `0e7be30` (feat)
2. **Task 2: Run the evaluation, write 02.2-FINDINGS.md, and append the pre-registration outcome** - `5140470` (docs)
3. **Task 3: Phase gate — read the verdict and decide what happens next** - `485693b` (docs; checkpoint resolved with the user's "iterate" decision)

**Plan metadata:** committed separately below (this SUMMARY.md + STATE.md + ROADMAP.md + REQUIREMENTS.md)

## Files Created/Modified

- `notebooks/diagnostics/cae_evaluate_run.py` (638 lines, created) - the evaluation runner: reload, chart survival, T1/T2/T3, non-gating evidence, verdict, PASS-only handoff
- `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md` (313 lines, created) - the full written finding plus the resolved phase-gate decision
- `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md` (modified) - §9 Outcome closed; §4/§5 unedited
- `.planning/ROADMAP.md` (modified) - Phase 02.2 marked complete (6/6, FAIL); Phase 02.3 proposed; Phase 3's dependency retargeted
- `notebooks/.cache/cae_verdict_43cf438bc944c509.json` (gitignored) - the machine-readable verdict artifact

## Decisions Made

- T3's compound two-control AND condition collapsed into `verdict_from_metrics`' single-scalar strict-less-than contract via `max(ratio_A, ratio_B) < (1 - THRESH_RECON_MARGIN)` — algebraically identical to the pre-registration's own rule, disclosed explicitly in the verdict artifact's `metrics.t3.recon_margin_gate_note`.
- Importing `cae_train_run` for its pre-registered constants (rather than re-declaring any of them) also re-runs that module's own preconditions and full fit registry as an import-time side effect — verified safe as a pure cache-hit read (no fit performed) matching 02.2-05's established cache-hit contract.
- At the phase gate, the user's "iterate" decision was recorded as a separate, clearly-delimited addition (`02.2-FINDINGS.md` §7a) rather than edited into the existing verdict sections, so the measured record and the forward-looking decision can never be mistaken for one another.

## Deviations from Plan

None - plan executed exactly as written. All three tasks' `<action>` and `<acceptance_criteria>` were implemented and verified as specified. The measured verdict is a legitimate FAIL, reported honestly per the plan's `<important>` framing — no metric, threshold, or interpretation rule was tuned, re-fit, or reinterpreted toward PASS.

## Issues Encountered

- Session was interrupted once by an API spend-limit error, occurring after Task 2's commit had already landed and before this summary was written. No uncommitted work existed at the interruption point (working tree was clean at `cad9d97`, the last commit before this plan's Task 1); the resumed session verified all prior work via `git log` and continued from Task 2's completion without redoing anything.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 02.2 is complete: 6/6 plans executed, terminal artifact `cae_verdict_43cf438bc944c509.json` written and self-consistent, `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` passes 75/75.
- Phase 3 does **not** proceed against Phase 02.2's output. Per the resolved phase-gate decision, Phase 3 now depends on a not-yet-planned **Phase 02.3: Chart Auto-Encoder Iteration**, which must investigate the FAIL (candidate axes: chart count, `D_CHART`, training budget, loss weighting, Lipschitz penalty schedule) and produce a fresh, separately-ratified pre-registration before any new fit runs. Phase 02.3 needs a `/gsd-discuss-phase` or `/gsd-phase` session to scope before it can be planned — no requirements are defined for it yet.
- The milestone's `## Progress` table and `Execution Order` line in `ROADMAP.md` reflect the current state: 02.2 complete (FAIL, iterate), 02.3 proposed/not planned, Phase 3 blocked on 02.3.
- No automatic fallback to 02.1's Krein representation occurred or is pending (D-02 held throughout); that path remains available as a documented option should a future decision revisit it, but nothing in this plan or its follow-up adopts it.

---
*Phase: 02.2-chart-autoencoder-validity-test-inserted*
*Completed: 2026-08-04*

## Self-Check: PASSED

- FOUND: `notebooks/diagnostics/cae_evaluate_run.py`
- FOUND: `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md`
- FOUND: `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-06-SUMMARY.md`
- FOUND: `notebooks/.cache/cae_verdict_43cf438bc944c509.json`
- FOUND commit: `0e7be30` (Task 1 - cae_evaluate_run.py)
- FOUND commit: `5140470` (Task 2 - verdict/findings recorded)
- FOUND commit: `485693b` (Task 3 - phase gate resolved)
