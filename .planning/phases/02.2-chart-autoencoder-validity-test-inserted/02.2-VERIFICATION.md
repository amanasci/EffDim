---
phase: 02.2-chart-autoencoder-validity-test-inserted
verified: 2026-08-04T21:49:49Z
status: passed
score: 10/10 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 02.2: Chart Autoencoder Validity Test Verification Report

**Phase Goal:** The Chart Auto-Encoder method (arXiv:1912.10094) empirically
validity-tested on the PU data behind a pre-registered PASS/FAIL gate.
**Verified:** 2026-08-04T21:49:49Z
**Status:** passed
**Re-verification:** No — initial verification

## Critical framing acknowledged

This phase produced `CAE_VERDICT = FAIL` against its pre-registered thresholds. **The FAIL
is the phase succeeding, not failing.** The phase goal was to run a valid, honest,
pre-registered test, not to make the CAE pass — CAE-07 and roadmap decision D-02 both name a
documented FAIL as a complete, reportable milestone outcome. This report verifies the
*integrity of the test* (ordering, unedited thresholds, no prohibited fallback, numbers
matching source-of-record), not whether the CAE passed.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CAE-01 ordering: pre-registration commit is a proven git ancestor of HEAD, ratification precedes every fit | ✓ VERIFIED | `git merge-base --is-ancestor c2c4c93f4dcb8cd5bbafb66e6d5b59d78480af93 HEAD` exits 0. Ratification commit `281d5be` (2026-08-04 09:54:47) is also an ancestor of HEAD and precedes every fit-code commit (`9fb9750`, `06023a9`, ...) and every cached fit artifact's mtime (first fit-related cache `cae_pairs_*` 11:02, first CAE seed fit 11:56 — all after 09:54:47). Both `cae_train_run.py` and `cae_evaluate_run.py` also enforce this check *at run time* via `subprocess.run(["git","merge-base","--is-ancestor",...])`, not merely in prose. |
| 2 | Pre-registration Sections 4 and 5 (thresholds, metric definitions) unedited since ratification; only Section 9 appended | ✓ VERIFIED | `git diff 281d5be HEAD -- 02.2-PREREGISTRATION.md` shows the only change is an addition to the empty `## 9. Outcome` section (was `TBD — appended only after the fits run.`); Sections 1-8, including 4 (gate definitions/thresholds) and 5 (interpretation rule), are byte-identical. |
| 3 | CAE trained on the frozen Phase 1 subsample with the full pre-registered architecture, reproducible from recorded seeds | ✓ VERIFIED | `notebooks/pu_manifold/cae.py` exports `InitialEncoder, ChartEncoder, ChartDecoder, EmbeddingDecoder, ChartPredictor, ChartAutoEncoder, chart_loss, lipschitz_penalty, farthest_point_sample, fps_pretrain_loss, train_cae`. Verdict JSON `seeds` field records `all_seeds: [20260803, 20260804, 20260805]`, `main_seed: 20260803`, matching `SEEDS`/`MAIN_SEED` in the pre-registration. |
| 4 | T1/T2/T3 measured and gated per the pre-registered strict-less-than rule; a value exactly at threshold does not clear it | ✓ VERIFIED | `verdict_from_metrics` (cae.py:1064-1097) computes `passed = bool(value < threshold)` for every gate — strict `<`, never `<=`. Covered by `test_verdict_rule_strict_inequality` and `test_pruning_boundary_is_strict_less_than` (both pass). Verdict JSON: T1 distortion 0.296981 vs threshold 0.15 → FAIL; T2 rcycle_ratio 1.089366 vs 2.0 → PASS; T3 recon_margin 3.586350 vs 0.90 → FAIL. |
| 5 | No `cae_handoff_*` artifact exists; D-02 forbids handoff/Krein fallback on non-PASS | ✓ VERIFIED | `find . -iname "*cae_handoff*"` returns nothing. `write_cae_handoff` (cae.py:1158) raises `ValueError` unless `verdict == "PASS"`. `cae_evaluate_run.py`'s STEP 8 only calls `write_cae_handoff` inside the `if verdict == "PASS":` branch; the `else` branch prints an explicit halt message and performs no substitution — code-level enforcement, not just narrative. |
| 6 | Measured numbers in 02.2-FINDINGS.md match `cae_verdict_43cf438bc944c509.json` | ✓ VERIFIED | Verdict JSON: `distortion=0.29698133226319146`, `rcycle_ratio=1.0893662590388085`, `recon_margin=3.5863496159842887`; `chart_count_by_seed` shows 16/16 surviving on all three seeds; `unfaithfulness=0.04233336076140404`, `coverage=0.024`; `activation_substitution.delta_mse_relu_minus_silu=-1.2832995422618792e-06`; `krein_comparison_reported_not_gating` = `{p40: 0.179641028446001, p8: 0.1210239937261199}`. All match FINDINGS.md §3/§4 to the reported precision. |
| 7 | Requirements CAE-01..07 all satisfied by phase artifacts | ✓ VERIFIED | See Requirements Coverage table below. |
| 8 | Findings document written as a complete, self-contained milestone outcome (not an error to work around) | ✓ VERIFIED | `02.2-FINDINGS.md` (312 lines) states verdict, full gate table, non-gating evidence, deviations/assumptions, wall-clock record, reproduction instructions, and the resolved phase-gate decision (§7a). |
| 9 | User ratification of thresholds recorded before any fit, and phase-gate "iterate" decision recorded without reinterpreting the FAIL | ✓ VERIFIED | Pre-registration §8: `Ratified 2026-08-04... resume signal: "ratified"`. FINDINGS §7a: "The FAIL verdict above is not reinterpreted, softened, or revisited by this decision." Commit `485693b`: "resolve phase gate — verdict FAIL stands, user elects to iterate." |
| 10 | ROADMAP.md accurately reflects the FAIL outcome and downstream consequence (Phase 3 blocked, Phase 02.3 proposed) | ✓ VERIFIED | ROADMAP.md lines 21-22, 133, 243, 250-252: Phase 02.2 marked complete with FAIL outcome stated verbatim with matching numbers; Phase 3's dependency explicitly moved to a proposed Phase 02.3's future PASS, not an automatic Krein fallback. |

**Score:** 10/10 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.planning/phases/.../02.2-PREREGISTRATION.md` | Pre-registered gate metrics/thresholds, ratification, outcome | ✓ VERIFIED | 337 lines; committed at `c2c4c93` (ancestor of HEAD); §9 outcome appended at `5140470` without touching §4/§5 |
| `notebooks/pu_manifold/cae.py` | Full CAE architecture, training loop, metrics, verdict engine | ✓ VERIFIED | Exports all contracted symbols (`InitialEncoder`...`arrays_to_state_dict`); strict-less-than verdict rule; `write_cae_handoff` refuses on non-PASS |
| `notebooks/pu_manifold/tests/test_cae.py` | Known-answer tests incl. boundary/precision/underflow edges | ✓ VERIFIED | 43 tests, all pass (`test_verdict_rule_strict_inequality`, `test_pruning_boundary_is_strict_less_than`, `test_distortion_uses_global_embedding_not_chart_coords`, etc.) |
| `notebooks/diagnostics/cae_train_run.py` | Runner with runtime preconditions (cache presence, git ancestry, pair bit-identity) | ✓ VERIFIED | Runtime `git merge-base --is-ancestor` check present (STEP 0b); contains `THRESH_DISTORTION_MAX` and the other named constants verbatim |
| `notebooks/diagnostics/cae_evaluate_run.py` | Metrics computation, verdict + handoff writers | ✓ VERIFIED | Contains `CAE_VERDICT`; PASS-only handoff gate; runtime ancestry check present (STEP 0b) |
| `notebooks/.cache/cae_verdict_43cf438bc944c509.json` | Machine-readable PASS/FAIL verdict | ✓ VERIFIED (gitignored, expected) | `CAE_VERDICT: "FAIL"`; all required keys present (35 keys incl. `assumptions`, `remediation`, `krein_comparison_reported_not_gating`) |
| `.planning/phases/.../02.2-FINDINGS.md` | Full written finding | ✓ VERIFIED | 312 lines; numbers cross-checked against verdict JSON (§6 above) |
| `notebooks/.cache/cae_handoff_43cf438bc944c509.json` | **Must NOT exist** (D-02 prohibition on non-PASS) | ✓ VERIFIED ABSENT | Confirmed absent by filesystem search — correct for a FAIL verdict |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `02.2-PREREGISTRATION.md` | `notebooks/diagnostics/cae_train_run.py` | Constants copied verbatim | ✓ WIRED | `THRESH_DISTORTION_MAX`, `THRESH_RCYCLE_RATIO_MAX`, `THRESH_RECON_MARGIN` present as named module-level constants |
| `02.2-PREREGISTRATION.md` (commit `c2c4c93`) | git history | Ancestor of HEAD | ✓ WIRED | `git merge-base --is-ancestor` exits 0, confirmed independently by the verifier and enforced at runtime by both runners |
| `cae_evaluate_run.py` | `cae.py verdict_from_metrics` | Verdict computed by tested rule, not re-derived inline | ✓ WIRED | `verdict, gate_detail = cae_mod.verdict_from_metrics(...)` — no inline re-implementation found |
| `cae_evaluate_run.py` | `geometry_probes_43cf438bc944c509.json` | Krein comparison read, not recomputed | ✓ WIRED | Verdict JSON `krein_comparison_reported_not_gating.source` references the 02.1 cached artifact; values match 02.1's own numbers verbatim |
| `notebooks/.cache/cae_verdict_43cf438bc944c509.json` | `write_cae_handoff` | PASS-only gate | ✓ WIRED | `verdict != "PASS"` raises `ValueError`; evaluate_run's STEP 8 `else` branch never calls it |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Ancestry proof holds | `git merge-base --is-ancestor c2c4c93... HEAD` | exit 0 | ✓ PASS |
| Ratification precedes all fit code/artifacts | commit timestamps vs. cache mtimes | 281d5be @ 09:54:47 < first cache write @ 11:02 | ✓ PASS |
| Prereg §4/§5 unedited since ratification | `git diff 281d5be HEAD -- 02.2-PREREGISTRATION.md` | only §9 diff present | ✓ PASS |
| No handoff artifact on FAIL | `find . -iname "*cae_handoff*"` | no results | ✓ PASS |
| Verdict strict-less-than enforced | `verdict_from_metrics` source read + test run | `value < threshold`, tests pass | ✓ PASS |
| CAE test suite passes | `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` | 75 passed | ✓ PASS |
| Core test suite passes | `.venv/bin/python -m pytest tests/ -q` | 120 passed | ✓ PASS |
| FINDINGS numbers match verdict JSON | direct JSON read vs. FINDINGS.md §3/§4 | exact match to reported precision | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CAE-01 | 02.2-01 | Gate metrics/thresholds pre-registered and committed before any fit, ordering proved by git ancestry | ✓ SATISFIED | `c2c4c93` ancestor of HEAD; runtime enforcement in both runners |
| CAE-02 | 02.2-02/03/05 | CAE trained on frozen subsample, reproducible from seeds, full paper architecture with Lipschitz reg + FPS pre-training | ✓ SATISFIED | `cae.py` architecture + `train_cae`; verdict `seeds` field, three seed fits cached |
| CAE-03 | 02.2-03/04/06 | Held-out reconstruction vs. matched-capacity baselines (plain AE, MDS decoder), aggregate + per-dim | ✓ SATISFIED | Verdict `t3.per_dim` and `mse_plain_ae{20,40}`/`mse_mds_dec{8,20}` fields; both required baselines present |
| CAE-04 | 02.2-04/06 | Chart-transition cycle residual `R_cycle` over held-out points | ✓ SATISFIED | Verdict `t2` field: `n_qualify=2000`, `ratio=1.089366` |
| CAE-05 | 02.2-04/06 | A-posteriori chart pruning, surviving count + stability across seeds, unfaithfulness/coverage | ✓ SATISFIED | `chart_count_by_seed` shows 16/16 on all three seeds; `unfaithfulness`/`coverage` fields present |
| CAE-06 | 02.2-01/02/03/06 | C2-smooth activation substitution (SiLU for ReLU) with measured cost; Theorem 2 reach/compactness recorded as unverified assumption | ✓ SATISFIED | Verdict `activation_substitution` field (delta = -1.283e-06); `assumptions` array names Theorem 2's unverified hypothesis verbatim |
| CAE-07 | 02.2-02/06 | Machine-readable `cae_verdict_{fit_key}.json` with `CAE_VERDICT`, checked by downstream notebooks before expensive cells, documented FAIL is a complete milestone outcome | ✓ SATISFIED | Verdict JSON written with all 35 required keys; `verdict_from_metrics` hardened against missing/non-finite metrics (tested); FINDINGS.md documents FAIL as complete outcome |

No orphaned requirements found — REQUIREMENTS.md's CAE-01..07 all trace to plans that were executed, and all are marked `Complete` in REQUIREMENTS.md's own coverage table (cross-checked, consistent).

### Anti-Patterns Found

None. Scanned `notebooks/pu_manifold/cae.py`, `notebooks/pu_manifold/tests/test_cae.py`,
`notebooks/diagnostics/cae_train_run.py`, `notebooks/diagnostics/cae_evaluate_run.py`,
`02.2-PREREGISTRATION.md`, and `02.2-FINDINGS.md` for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` —
zero matches (the pre-registration's original `TBD` placeholder in the then-empty §9 was
correctly replaced, not left in place, per the git diff check above).

Code review (`02.2-REVIEW.md`, commit `f8f6456`) found 0 critical issues, 3 warnings (NaN
guard gap in `chart_survival`, an incomplete shape guard in `embedding_distortion`, and a
confusing `IndexError` instead of `ValueError` in `select_overlap_pairs` for <2 surviving
charts), and 7 info-level items. The reviewer explicitly traced whether any of these — or any
other code path — could have spuriously produced the FAIL and found no such defect: none of
the three warnings were triggered by the run that produced this verdict (16/16 charts
survived on every seed, so the NaN/underflow and <2-chart paths never executed; the
distortion computation was independently verified against a near-isometric fixture recovering
`median_abs_rel < 1e-12`). These are legitimate robustness gaps for a future iteration
(candidate work for the proposed Phase 02.3) but do not compromise the integrity of this
phase's measured FAIL.

### Human Verification Required

None. Every must-have for this phase is a git-ancestry fact, a file-diff fact, a JSON-field
fact, or a passing automated test — all mechanically checkable, and all checked above.

### Gaps Summary

No gaps. All 10 observable truths verified, all 7 requirements satisfied, all artifacts
present/substantive/wired, the pre-registration's ordering and immutability guarantees hold
under independent git inspection, and the FINDINGS.md numbers are byte-traceable to the
verdict JSON. The measured `CAE_VERDICT = FAIL` is the correct, honestly-obtained outcome of
a properly pre-registered and executed test — it is not a defect of this phase, per the
critical framing established by CAE-07 and roadmap decision D-02. The user's "iterate"
decision at the phase gate and the resulting proposed Phase 02.3 are both accurately recorded
in ROADMAP.md without reinterpreting the sealed FAIL.

---

_Verified: 2026-08-04T21:49:49Z_
_Verifier: Claude (gsd-verifier)_
