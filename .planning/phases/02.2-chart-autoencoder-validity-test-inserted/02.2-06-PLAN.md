---
phase: 02.2-chart-autoencoder-validity-test-inserted
plan: 06
type: execute
wave: 5
depends_on: ["02.2-04", "02.2-05"]
files_modified:
  - notebooks/diagnostics/cae_evaluate_run.py
  - .planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md
  - .planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md
autonomous: false
requirements: [CAE-03, CAE-04, CAE-05, CAE-06, CAE-07]

must_haves:
  truths:
    - "A machine-readable cae_verdict_43cf438bc944c509.json exists carrying a CAE_VERDICT field of PASS or FAIL plus every pre-registered metric and threshold (CAE-07)."
    - "A metric exactly equal to its pre-registered threshold does NOT clear it — every gate comparison in the written verdict is strict less-than (CAE-07 boundary edge)."
    - "Held-out reconstruction error is reported as both an aggregate metric and a per-output-dimension distribution, against the matched-capacity single-chart control and the fitted classical-MDS decoder (CAE-03)."
    - "The chart-transition cycle residual is reported over held-out points using the pre-registered overlap rule, with the qualifying-point count stated (CAE-04)."
    - "The over-specified initial chart count, the surviving count after pruning at the pre-registered tolerance, and the surviving count for each of the three seeds are all reported, together with unfaithfulness and coverage (CAE-05)."
    - "The C2-smooth activation substitution is reported with its measured reconstruction cost against the ReLU control, and the compact-manifold-with-positive-reach assumption is carried into the verdict artifact as an unverified assumption (CAE-06)."
    - "The 02.1 Krein comparison appears in the verdict under a field naming it as reported and not gating (D-01)."
    - "On FAIL the run halts, 02.2-FINDINGS.md records the finding as a complete milestone outcome, and no downstream artifact falls back to the 02.1 Krein handoff (D-02)."
    - "On PASS a cae_handoff_43cf438bc944c509.json names exactly what Phase 3 consumes: the global embedding, the chart coordinates, the chart probabilities, the chart assignments, and the trained decoders (D-03)."
    - "Section 9 of the pre-registration is appended with the outcome only after the fits ran, leaving Sections 4 and 5 untouched."
  prohibitions:
    - statement: "MUST NOT revise any pre-registered threshold, metric definition, overlap-pair selection rule, or pruning tolerance now that measured numbers exist — a change is a separate documented amendment plus a full re-run, never a quiet edit."
      status: unverified
    - statement: "MUST NOT auto-fall-back to the Krein/02.1 representation, or continue any downstream Phase-3 work, on a FAIL verdict — a FAIL halts for the user's decision (D-02)."
      status: unverified
    - statement: "MUST NOT re-run the protocol with fresh seeds and report only the run that passes — the three pre-registered seeds and their spread are reported whatever they show."
      status: unverified
    - statement: "MUST NOT present Theorem 2's epsilon-faithfulness guarantee as evidence that this fit succeeded — reach tau and the density ratio C were never estimated for the PU point cloud, so the theorem is cited as motivation only."
      status: unverified
  artifacts:
    - path: "notebooks/diagnostics/cae_evaluate_run.py"
      provides: "Metric computation across all eight fits, the strict-less-than verdict application, and the verdict and handoff writers"
      contains: "CAE_VERDICT"
      min_lines: 220
    - path: ".planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md"
      provides: "The phase's written finding: every measured number, the verdict, the deviations and assumptions carried forward, and the options a FAIL leaves open"
      min_lines: 120
  key_links:
    - from: "notebooks/diagnostics/cae_evaluate_run.py"
      to: "notebooks/pu_manifold/cae.py verdict_from_metrics"
      via: "the verdict is computed by the tested strict-less-than rule, never re-derived inline in the runner"
      pattern: "verdict_from_metrics"
    - from: "notebooks/diagnostics/cae_evaluate_run.py"
      to: "notebooks/.cache/geometry_probes_43cf438bc944c509.json"
      via: "the 02.1 Krein comparison numbers are read from the cached 02.1 artifact rather than recomputed"
      pattern: "geometry_probes_"
    - from: "notebooks/.cache/cae_verdict_43cf438bc944c509.json"
      to: "Phase 3"
      via: "downstream notebooks read CAE_VERDICT before running any expensive cell and halt on FAIL"
      pattern: "CAE_VERDICT"
---

<objective>
Compute every pre-registered metric from the completed fits, apply the interpretation rule, and
write the phase's terminal artifact: `cae_verdict_43cf438bc944c509.json`. Then write the finding,
append the pre-registration's outcome section, and hand the verdict to the user.

Purpose: this plan produces the object the whole phase exists for. CAE-07 makes it machine-readable
so Phase 3 can gate on it without a human in the loop; D-02 makes a FAIL a complete, reportable
milestone outcome rather than an error to work around; D-03 makes a PASS emit a handoff naming
exactly what Phase 3 consumes.

Output: the verdict artifact, `02.2-FINDINGS.md`, the pre-registration's completed outcome section,
and on PASS the Phase 3 handoff artifact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-CONTEXT.md
@.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-RESEARCH.md
@.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PATTERNS.md
@.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md
@.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-04-SUMMARY.md
@.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-05-SUMMARY.md
@.planning/phases/02.1-geometry-representation-research/02.1-RECOMMENDATION.md
@.planning/phases/02-eigenspectrum-audit-validity-gate/02-FINDINGS.md
@notebooks/pu_manifold/cae.py
@notebooks/diagnostics/cae_train_run.py
</context>

<interfaces>
Facts confirmed during planning.

`notebooks/.cache/geometry_probes_43cf438bc944c509.json` is 02.1's own measured artifact. Its
`krein_ladder` array holds one row per `(p, q)` rung with `median_abs_rel`, `median_signed_rel`,
`p95_abs_rel`, `mass_capture`, `n_negative_sq`; its `working_dimension` object holds `classical_p`,
`krein_p`, `krein_q`, `criterion`. The rows this phase reports as the non-gating comparison are the
classical `q=0` rungs at `p=40` (`median_abs_rel = 0.179641`) and `p=8` (`0.121024`), plus the
frontier elbow rung `(p, q) = (8, 0)` from 02.1's recommendation. Read them; do not recompute them.

`notebooks/.cache/gate_verdict_43cf438bc944c509.json` is the schema precedent. Its top-level keys
are `artifacts`, `d_frozen`, `d_provisional`, `d_sweep_max`, `elbow`, `elbow_check`,
`elbow_criterion`, `fit_key`, `flags`, `k_star`, `m`, `n_components`, `phase`, `r`, `remediation`,
`spectrum`, `thresholds`, `timestamp`, `verdict`, `verdict_rule`, `versions`. Note the precedent's
verdict field is named `verdict`; CAE-07 names this phase's field `CAE_VERDICT`, so use that name.
Its `remediation` array is a three-item list of documented next options — mirror that shape, while
noting that under D-02 the array documents the options for the user rather than triggering any of
them.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write cae_evaluate_run.py — every metric, the verdict, and the handoff</name>
  <precondition>All eight fit artifacts written by plan 02.2-05 exist under notebooks/.cache/ (three cae_fit_*_seed*.npz plus the five cae_ctrl_* npz files) and notebooks/.cache/geometry_probes_43cf438bc944c509.json is present.</precondition>
  <files>notebooks/diagnostics/cae_evaluate_run.py</files>
  <read_first>
    - `notebooks/diagnostics/cae_train_run.py` — the constants block and the fit-artifact key set, both of which this runner re-reads rather than re-declaring divergently
    - `notebooks/diagnostics/geometry_probes_run.py` lines 397-473 — the artifact-writing step, `_to_native` usage, and the required-keys assertion at the end
    - `notebooks/pu_manifold/cae.py` — `chart_survival`, `r_cycle`, `select_overlap_pairs`, `unfaithfulness_coverage`, `embedding_distortion`, `reconstruction_stats`, `verdict_from_metrics`, `write_cae_verdict`, `write_cae_handoff`, `arrays_to_state_dict`
    - `notebooks/.cache/gate_verdict_43cf438bc944c509.json` — the artifact shape and `verdict_rule` prose style to mirror
    - `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md` — Sections 4 and 5, which this runner implements without reinterpreting
  </read_first>
  <action>
Create `notebooks/diagnostics/cae_evaluate_run.py`, following `geometry_probes_run.py`'s shape:
docstring with the halt-don't-regenerate precondition and the
`PYTHONPATH=notebooks python notebooks/diagnostics/cae_evaluate_run.py` invocation, a constants
block importing the pre-registered names from `cae_train_run` so there is exactly one declaration
of each, banner-printed steps, and a required-keys assertion before exit.

STEP 0, preconditions: assert every artifact plan 02.2-05 produced exists, and assert the
pre-registration ancestry the same way the training runner does. Halt on any absence; there is no
regeneration path.

STEP 1, reload: rebuild each model from its persisted state_dict arrays via `arrays_to_state_dict`,
and load `z_all`, `p_all`, `chart_argmax_all`, `train_idx`, `holdout_idx`, `y_holdout` from each
npz. Assert the split index arrays are identical across all eight runs before computing anything.

STEP 2, chart survival: call `chart_survival` at the pre-registered tolerance for each of the three
seeds. Report the initial count, the surviving count per seed, and the spread across seeds. The main
seed's surviving set feeds every downstream metric; the replicates supply the stability evidence
CAE-05 asks for. Chart count is an outcome being read off, not a knob.

STEP 3, gate T1: call `embedding_distortion` on the main seed's `z_all` with the pair arrays the
training runner persisted, the training-split mask for the scale fit. Report `median_abs_rel`,
`median_signed_rel`, `p95_abs_rel`, `global_scale_factor`, `n_pairs`, plus the same statistic
restricted to holdout-only pairs as a non-gating companion.

STEP 4, gate T2: select the overlap set from the main seed's `p_all` restricted to holdout rows and
surviving charts, then compute `r_cycle` over it and the matched base quantity. Report the mean
residual, the mean base, their ratio, and the qualifying-point count. If `select_overlap_pairs`
raises for an under-populated set, let it propagate — an undefined T2 halts the run, and a halt is a
reportable outcome that goes into the findings.

STEP 5, gate T3: compute `reconstruction_stats` on the holdout rows for the main seed and for every
control, giving each an aggregate per-dimension MSE and the per-output-dimension distribution
summary. Report the CAE's number against the gating plain-autoencoder control and the gating
classical-MDS decoder, plus the two non-gating controls and each MDS baseline's linear
least-squares floor, and the relative margin against each gating control.

STEP 6, non-gating evidence: `unfaithfulness_coverage` at the pre-registered sample count and seed
over the main seed's surviving charts; the CAE-06 activation substitution cost as the difference in
held-out reconstruction between the main fit and the ReLU control, stated with its sign so a cost
and a benefit are distinguishable; the Krein comparison read from
`geometry_probes_43cf438bc944c509.json`'s `krein_ladder` and `working_dimension`, placed in the
verdict under the field naming it reported and not gating; and the wall-clock and truncation status
of every run.

STEP 7, verdict: assemble the metrics and thresholds dicts, call `verdict_from_metrics`, and write
`cae_verdict_{FIT_KEY}.json` with `write_cae_verdict`. The verdict is never re-derived inline here —
the tested strict-less-than rule in `cae.py` is the single implementation. Carry into the artifact:
`chart_count_surviving`, `chart_count_by_seed`, `unfaithfulness`, `coverage`,
`krein_comparison_reported_not_gating`, `activation_substitution`, `holdout`, `seeds`, `wallclock`,
`global_scale_factor`, `versions` recording the numpy, scipy, torch and python versions, and
`assumptions` — an array carrying, at minimum, that the compact-manifold-with-positive-reach
hypothesis behind the paper's Theorem 2 was never verified for this point cloud because neither
reach nor the density ratio was ever estimated, so the theorem motivates the method rather than
predicting this result. Add a `remediation` array of documented next options mirroring the Phase 2
precedent's three-item shape, with the explicit note that under D-02 the array documents options for
the user and triggers none of them.

STEP 8, handoff: on PASS only, call `write_cae_handoff`. On FAIL, write nothing further and print
the halt banner naming `02.2-FINDINGS.md` as where the finding is recorded. There is no automatic
fallback path in this script to the 02.1 Krein handoff or to any other representation.

Do not execute the runner in this task beyond whatever is needed to make it import cleanly — Task 2
runs it.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim && PYTHONPATH=notebooks .venv/bin/python -c "import importlib.util,sys; spec=importlib.util.spec_from_file_location('m','notebooks/diagnostics/cae_evaluate_run.py'); print('import-shape-ok' if spec else 'fail')" && grep -v '^#' notebooks/diagnostics/cae_evaluate_run.py | grep -c 'verdict_from_metrics'</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/diagnostics/cae_evaluate_run.py` exists and its docstring contains the invocation line `PYTHONPATH=notebooks python notebooks/diagnostics/cae_evaluate_run.py`.
    - The pre-registered constants are imported from `cae_train_run` rather than re-declared: `grep -v '^#' notebooks/diagnostics/cae_evaluate_run.py | grep -c 'THRESH_DISTORTION_MAX = '` returns 0.
    - `grep -v '^#' notebooks/diagnostics/cae_evaluate_run.py | grep -c 'verdict_from_metrics'` returns at least 1 — the verdict is computed by the tested rule.
    - The script contains no fallback path to a different representation on FAIL: `grep -v '^#' notebooks/diagnostics/cae_evaluate_run.py | grep -ci 'fallback'` returns 0.
    - The script reads the 02.1 comparison from the cached artifact rather than recomputing it: `grep -v '^#' notebooks/diagnostics/cae_evaluate_run.py | grep -c 'geometry_probes_'` returns at least 1.
    - The script asserts the split index arrays are identical across all eight loaded runs before computing any metric.
    - `write_cae_handoff` is called only inside a PASS branch.
  </acceptance_criteria>
  <done>The evaluation runner exists, computes every pre-registered metric from the cached fits, applies the tested verdict rule, and has exactly one PASS-only handoff branch and no fallback branch.</done>
</task>

<task type="auto">
  <name>Task 2: Run the evaluation, write 02.2-FINDINGS.md, and append the pre-registration outcome</name>
  <precondition>notebooks/diagnostics/cae_evaluate_run.py exists and its preconditions pass against the eight cached fit artifacts.</precondition>
  <files>.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md, .planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md, notebooks/.cache/cae_verdict_43cf438bc944c509.json</files>
  <read_first>
    - `.planning/phases/02-eigenspectrum-audit-validity-gate/02-FINDINGS.md` — the findings-document structure, tone, and the discipline of reporting a FAIL as a complete outcome rather than a problem
    - `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md` — Section 9's stub, which this task fills, and Sections 4 and 5, which it must not touch
    - `.planning/phases/02.1-geometry-representation-research/02.1-RECOMMENDATION.md` — the Krein numbers and the Phase 3 re-specification brief the comparison is reported against
  </read_first>
  <action>
Run `PYTHONPATH=notebooks .venv/bin/python notebooks/diagnostics/cae_evaluate_run.py`. It is not a
long job relative to the fits, but launch it in the background and poll if the distortion statistic
over 200,000 pairs takes longer than a foreground call comfortably allows.

Write `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md` from the
measured output. Structure it after `02-FINDINGS.md`: what was tested and why; the pre-registered
design in one table with each constant and its source decision; a results table with all three gates
showing the measured value, the pre-registered threshold, and the strict-less-than outcome; the
non-gating evidence — surviving chart count with its per-seed spread, unfaithfulness, coverage, the
per-output-dimension reconstruction distribution, the activation substitution cost with its sign,
the Krein comparison labelled as reported and not gating; the deviations and assumptions carried
forward for Phase 3, namely the C2-smooth activation substitution with its measured cost and the
compact-manifold-with-positive-reach hypothesis recorded as unverified because neither reach nor
the density ratio was ever estimated for this point cloud; the wall-clock record including any
truncated fit; and the verdict with what it means next.

If the verdict is FAIL, the findings document says so plainly and treats it as a complete, reportable
milestone outcome — the same discipline Phase 2's own FAIL was recorded under. It lists the options
the user now has, without adopting any of them: the 02.1 Krein handoff is one documented option and
not an automatic consequence. If the verdict is PASS, the findings document names the handoff
artifact and what Phase 3 consumes from it.

Then append the outcome to `## 9. Outcome` in `02.2-PREREGISTRATION.md`, replacing only that
section's TBD stub with the measured values and the verdict, and citing `02.2-FINDINGS.md` for the
full record. Sections 4 and 5 are not edited — the thresholds and the interpretation rule stand
exactly as ratified, whatever the numbers turned out to be.

Commit the findings and the pre-registration outcome together with message
`docs(02.2): record CAE validity test outcome and verdict`.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim && test -f notebooks/.cache/cae_verdict_43cf438bc944c509.json && .venv/bin/python -c "
import json,sys
d=json.load(open('notebooks/.cache/cae_verdict_43cf438bc944c509.json'))
assert d['CAE_VERDICT'] in ('PASS','FAIL'), d.get('CAE_VERDICT')
for k in ('fit_key','phase','metrics','thresholds','verdict_rule','chart_count_surviving','chart_count_by_seed','unfaithfulness','coverage','krein_comparison_reported_not_gating','activation_substitution','assumptions','remediation','holdout','seeds','wallclock','global_scale_factor','versions','timestamp'):
    assert k in d, k
print('verdict-artifact-ok', d['CAE_VERDICT'])
" && test -f .planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/.cache/cae_verdict_43cf438bc944c509.json` exists, parses, and carries `CAE_VERDICT` with value `PASS` or `FAIL` plus every key listed in the verify command.
    - The artifact's `thresholds` object holds exactly the three ratified numeric values from the pre-registration, and its `verdict_rule` string states that every comparison is strict less-than.
    - `metrics` carries the three gating values plus the per-output-dimension distribution summary for the CAE and each control.
    - `02.2-FINDINGS.md` exists, contains a gate results table with all three gates showing measured value, threshold, and outcome, and contains a section recording both CAE-06 deviations.
    - `## 9. Outcome` in `02.2-PREREGISTRATION.md` no longer contains `TBD`, and `git diff` for that file over this task touches no line inside Sections 4 or 5.
    - If `CAE_VERDICT` is `PASS`, `notebooks/.cache/cae_handoff_43cf438bc944c509.json` exists and names the global embedding, chart coordinates, chart probabilities, chart assignments, and decoder state keys. If `FAIL`, that file does not exist.
    - `02.2-FINDINGS.md` states, in the case of a FAIL, that the outcome is a complete and legitimate milestone result and lists the user's options without adopting one.
  </acceptance_criteria>
  <done>The verdict artifact exists and is self-consistent, the finding is written, and the pre-registration's outcome section is closed without any edit to its thresholds or interpretation rule.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase gate — read the verdict and decide what happens next</name>
  <what-built>
The Chart Auto-Encoder validity test has run end to end against the thresholds you ratified before
any fit existed. `cae_verdict_43cf438bc944c509.json` is written, `02.2-FINDINGS.md` records the
finding, and the pre-registration's outcome section is closed.

What this gate decides, per D-02 and D-03:

- **On PASS**, Phase 3 is unblocked and decodes a curvature field from the CAE representation.
  `cae_handoff_43cf438bc944c509.json` names exactly what it consumes.
- **On FAIL**, the milestone remains at the phase-2 stage. That is a complete and legitimate
  outcome, not an error to work around — the same standing Phase 2's own FAIL was given. Adopting
  02.1's Krein handoff for Phase 3 is **not** automatic; nothing downstream has fallen back to it,
  and the next move is yours: adopt Krein, iterate the CAE under a fresh pre-registration, or stop
  and report.

Two things worth your eye regardless of the verdict:

- The Krein comparison in the artifact is reported and **not** load-bearing (D-01). It is there as
  evidence, not as an adjudication.
- The compact-manifold-with-positive-reach hypothesis behind the paper's Theorem 2 was never
  verified for this point cloud — neither the reach nor the density ratio has ever been estimated,
  so the theorem motivated the method and predicts nothing about this result either way.
  </what-built>
  <how-to-verify>
1. Read the verdict:
   `.venv/bin/python -m json.tool notebooks/.cache/cae_verdict_43cf438bc944c509.json`
2. Read the finding:
   `cat .planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md`
3. Confirm the thresholds are the ones you ratified and were not edited after the fact:
   `git log -p -- .planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md`
   — Sections 4 and 5 should show no change after the ratification commit.
4. Confirm the ordering still proves out:
   `PREREG=.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md; SHA=$(git log --diff-filter=A --format=%H -- "$PREREG" | tail -1); git merge-base --is-ancestor "$SHA" HEAD && echo ordering-ok`
5. On a FAIL, confirm nothing fell back:
   `ls notebooks/.cache/cae_handoff_* 2>/dev/null` must print nothing.
  </how-to-verify>
  <resume-signal>On PASS, type "approved" to seal the phase and unblock Phase 3. On FAIL, state your decision: "adopt krein", "iterate cae", or "stop and report" — and the roadmap is updated to match rather than defaulting to any of them.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol Phase 02.2 creates. Newly-created symbols are not drift.

**Files created**
- `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-PREREGISTRATION.md`
- `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md`
- `notebooks/pu_manifold/cae.py`
- `notebooks/pu_manifold/tests/test_cae.py`
- `notebooks/diagnostics/cae_train_run.py`
- `notebooks/diagnostics/cae_evaluate_run.py`

**Classes in `notebooks/pu_manifold/cae.py`**
`InitialEncoder`, `ChartEncoder`, `ChartDecoder`, `EmbeddingDecoder`, `ChartPredictor`,
`ChartAutoEncoder`, `PlainAutoEncoder`, `MlpDecoder`

**Functions in `notebooks/pu_manifold/cae.py`**
`activation_module`, `mlp_stack`, `chart_loss`, `lipschitz_penalty`, `farthest_point_sample`,
`fps_pretrain_loss`, `train_cae`, `timing_probe`, `train_plain_ae`, `train_mlp_decoder`,
`fit_linear_decoder`, `reconstruction_stats`, `r_cycle`, `select_overlap_pairs`,
`unfaithfulness_coverage`, `chart_survival`, `fit_global_scale`, `embedding_distortion`,
`verdict_from_metrics`, `write_cae_verdict`, `write_cae_handoff`, `to_native`,
`state_dict_to_arrays`, `arrays_to_state_dict`

**Test functions in `notebooks/pu_manifold/tests/test_cae.py`**
`test_end_to_end_smoke_writes_verdict_artifact`, `test_loss_matches_hand_computation`,
`test_lipschitz_regularizer_matches_svd`, `test_fps_selects_farthest_points`,
`test_fps_reproducible_under_fixed_seed`, `test_chart_decoder_targets_embedding_space`,
`test_single_chart_fails_circle_topology`, `test_r_cycle_zero_for_identity_charts`,
`test_pruning_removes_dead_chart`, `test_pruning_boundary_is_strict_less_than`,
`test_pruning_statistic_no_underflow`, `test_unfaithfulness_coverage_hand_check`,
`test_cae_verdict_cache_roundtrip`, `test_verdict_rule_strict_inequality`,
`test_verdict_rejects_missing_or_nonfinite_metric`, `test_verdict_json_is_byte_stable`,
`test_verdict_metrics_are_native_float64_roundtrip`,
`test_distortion_uses_global_embedding_not_chart_coords`,
`test_overlap_selection_halts_below_min_points`, `test_state_dict_array_roundtrip`,
`test_train_cae_respects_wallclock_ceiling`

**New gitignored cache stems under `notebooks/.cache/`**
`cae_fit_{fit_key}_seed{seed}`, `cae_fit_meta_{fit_key}_seed{seed}`, `cae_ctrl_relu_{fit_key}`,
`cae_ctrl_relu_meta_{fit_key}`, `cae_ctrl_plainae{latent}_{fit_key}`,
`cae_ctrl_plainae{latent}_meta_{fit_key}`, `cae_ctrl_mdsdec{p}_{fit_key}`,
`cae_ctrl_mdsdec{p}_meta_{fit_key}`, `cae_timing_probe_{fit_key}`, `cae_pairs_{fit_key}`,
`cae_verdict_{fit_key}`, `cae_handoff_{fit_key}`

**JSON field names introduced**
In `cae_verdict_{fit_key}.json`: `CAE_VERDICT`, `fit_key`, `phase`, `metrics`, `thresholds`,
`verdict_rule`, `chart_count_surviving`, `chart_count_by_seed`, `unfaithfulness`, `coverage`,
`krein_comparison_reported_not_gating`, `activation_substitution`, `assumptions`, `remediation`,
`holdout`, `seeds`, `wallclock`, `global_scale_factor`, `versions`, `timestamp`.
In `cae_handoff_{fit_key}.json`: `fit_key`, `phase`, `verdict`, `consumes`,
`global_embedding_key`, `chart_coords_key`, `chart_probs_key`, `chart_assignments_key`,
`decoder_state_keys`, `surviving_charts`, `activation`, `timestamp`.

**Pre-registered constant names (module-level in both runners)**
`FIT_KEY`, `D_CHART`, `L_EMBED`, `N_CHARTS_INIT`, `HIDDEN_WIDTH`, `ACTIVATION`, `LIP_WEIGHT`,
`LIP_EVERY_N_STEPS`, `WEIGHT_DECAY`, `ADAM_LR`, `BATCH`, `MAX_EPOCHS`, `EARLY_STOP_PATIENCE`,
`EARLY_STOP_MIN_DELTA`, `WALLCLOCK_CEILING_S`, `FPS_PRETRAIN_EPOCHS`, `PRUNE_TOL`,
`OVERLAP_P_MIN`, `OVERLAP_MIN_POINTS`, `HOLDOUT_FRACTION`, `SPLIT_SEED`, `SEEDS`, `MAIN_SEED`,
`UNFAITHFUL_SAMPLES`, `UNFAITHFUL_SEED`, `PAIR_SEED`, `PAIR_COUNT`, `MDS_BASELINE_P`,
`PLAIN_AE_LATENTS`, `THRESH_DISTORTION_MAX`, `THRESH_RCYCLE_RATIO_MAX`, `THRESH_RECON_MARGIN`

**CLI invocations introduced**
- `PYTHONPATH=notebooks python notebooks/diagnostics/cae_train_run.py [--runs ...]`
- `PYTHONPATH=notebooks python notebooks/diagnostics/cae_evaluate_run.py`
- `python -m pytest notebooks/pu_manifold/tests/test_cae.py -q`
</artifacts_this_phase_produces>

<flagged_assumptions>
Unresolved edge-probe rows carried forward as explicit assumptions, not silently dropped.

- **CAE-03 — unclassified.** Assumption: "matched capacity" is operationalised as the same hidden
  width, the same layer count per side, and the identical training protocol.
- **CAE-04 — unclassified.** Assumption: pairing each qualifying holdout row with its two
  highest-probability surviving charts is the right reading of the paper's "if the point is in the
  intersection of two chart neighbourhoods"; the paper gives no rule when three or more charts
  clear the cutoff.
- **CAE-06 — unclassified.** Assumption: one ReLU-activated control fit at the main seed adequately
  measures the activation-substitution reconstruction cost.
- **CAE-01 — unclassified.** Assumption: `git merge-base --is-ancestor` is sufficient proof of
  ordering and the history will not be rewritten.
- **CAE-02 — unclassified.** Assumption: single-machine seed reproducibility satisfies
  "reproducible from recorded seeds"; cross-machine bit-reproducibility is not claimed.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached fits -> reported metric | A metric computed from the wrong fit, or from a mismatched split, produces a plausible number that means nothing |
| verdict artifact -> Phase 3 | Downstream phases gate on this file without a human in the loop; a wrong or missing field is a silent unblocking |
| measured result -> pre-registration document | The document must record the outcome without its thresholds moving to meet it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.2-20 | Tampering | A threshold quietly edited to match a measured value | high | mitigate | Task 2 appends only Section 9; Task 3's checkpoint has the user inspect `git log -p` on the pre-registration for any post-ratification change to Sections 4 or 5 |
| T-02.2-21 | Elevation of privilege | A FAIL silently unblocking Phase 3 via an automatic fallback | high | mitigate | The evaluation runner has no fallback branch (asserted by a zero-count grep), `write_cae_handoff` raises on a non-PASS verdict, and the checkpoint's FAIL path requires an explicit user decision |
| T-02.2-22 | Spoofing | A verdict computed by an inline re-derivation diverging from the tested rule | high | mitigate | The runner calls `verdict_from_metrics` and never re-derives the comparison inline; the tested rule raises on an absent or non-finite gating metric |
| T-02.2-23 | Tampering | Metrics computed across mismatched splits between the CAE and its controls | medium | mitigate | STEP 1 asserts the split index arrays are identical across all eight loaded runs before any metric is computed |
| T-02.2-24 | Repudiation | The finding overclaiming theoretical coverage | medium | mitigate | The `assumptions` array in the verdict and a dedicated findings section both record that reach and the density ratio were never estimated, so the theorem is cited as motivation only |
| T-02.2-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task in this phase; RESEARCH.md's Package Legitimacy Audit records zero new packages and `torch==2.13.0+cpu` was vetted in a prior phase's legitimacy gate |
</threat_model>

<verification>
- `notebooks/.cache/cae_verdict_43cf438bc944c509.json` exists, parses, and carries `CAE_VERDICT` plus every contracted field.
- `02.2-FINDINGS.md` exists with the three-gate results table and both CAE-06 deviations recorded.
- `## 9. Outcome` in the pre-registration is filled; Sections 4 and 5 are unchanged since ratification.
- On PASS, `cae_handoff_43cf438bc944c509.json` exists and names every Phase 3 consumable; on FAIL it does not exist.
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` still exits 0.
</verification>

<success_criteria>
The phase's terminal artifact exists and is machine-readable: a `CAE_VERDICT` of PASS or FAIL
carrying every pre-registered metric and threshold, computed by the tested strict-less-than rule
against thresholds that were ratified before any fit ran. The finding is written, the outcome is
recorded in the pre-registration without its thresholds moving, and the user has decided what
happens next.
</success_criteria>

<output>
Create `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-06-SUMMARY.md` when done.
</output>
