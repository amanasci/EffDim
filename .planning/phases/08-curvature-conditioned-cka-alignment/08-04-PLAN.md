---
phase: 08-curvature-conditioned-cka-alignment
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - notebooks/pu_manifold/cka.py
  - notebooks/pu_manifold/tests/test_cka.py
  - notebooks/diagnostics/08_cka_alignment_run.py
autonomous: false
requirements: [D8-03, D8-04, D8-08, D8-09, D8-15, D8-18, D8-19, D8-21, D8-22]
user_setup: []

must_haves:
  truths:
    - "`assert_preregistered()` returns without raising — every constant in `_REQUIRED_CONSTANTS` carries a filled value."
    - "`SIGMA_HSC` and `SIGMA_LEGACYSURVEY` equal, to full float precision, the two values 08-03's `--mode sigma` measured and recorded in `08-03-SUMMARY.md`."
    - "`SEED_HANDLING_RULE` equals its ratified string exactly; any other value makes `assert_preregistered()` raise."
    - "`VERDICT_RULE` names `S_GRID` and states that clearance is required at every grid point with no headline `S`."
    - "`REPORTING_BLOCK_ROWS` names exactly five rows: the `d=32` gap, the shuffled-`||H||` false-positive rate, the planted-effect detection floor, the realized `||H||` contrast per `S`, and all three `sigma` rungs."
    - "`VERDICT_SENTENCE_RULE` states that the verdict sentence cannot be written without the `d=32` gap and the shuffled-`||H||` false-positive rate in the same sentence."
    - "Exactly one commit fills the constants — `git log --oneline -- notebooks/pu_manifold/cka.py` shows the freeze commit as the most recent change to the constants block."
    - "After the freeze commit is wired in, `--mode sweep --freeze-commit <sha>` passes the strict-ancestor gate, and `--mode sweep --freeze-commit HEAD` still exits 1."
  truths_backstop:
    - statement: "The freeze commit is a STRICT git ancestor of HEAD and no constant in the frozen block changed after it — `git merge-base --is-ancestor <freeze-sha> HEAD` exits 0 and `git log -p <freeze-sha>..HEAD -- notebooks/pu_manifold/cka.py` shows no change to any name in `_REQUIRED_CONSTANTS`."
      verification: backstop
  artifacts:
    - notebooks/pu_manifold/cka.py
    - notebooks/pu_manifold/tests/test_cka.py
    - notebooks/diagnostics/08_cka_alignment_run.py
  key_links:
    - "`FREEZE_COMMIT_SHA` appears in exactly two places — the runner's `_strict_ancestor_or_exit` and `test_cka.py`'s ancestry test — and both hold the same literal."
    - "Every production mode calls `cka.assert_preregistered()` first and `_strict_ancestor_or_exit` second, so no number can be produced by a tree whose constants drifted or whose freeze proof is missing."
  prohibitions:
    - statement: "No measured PU verdict number exists at or before the freeze commit (D8-22)."
      status: required
      verification: "`notebooks/.cache/08_cka_alignment.jsonl` does not exist before 08-05 runs; only the two scratch records from 08-01 and 08-03 do."
    - statement: "Seeds are never pooled — `SEED_HANDLING_RULE` is frozen to its ratified value and guarded by exact string equality (`05-03-DECISION.md`, D8-15)."
      status: required
      verification: "`test_assert_preregistered_rejects_unset_constant` plus a dedicated case asserting a different rule string makes the guard raise."
    - statement: "No sealed module is edited (D8-23) and `src/effdim/` is untouched (D8-24)."
      status: required
      verification: "`git diff --name-only <plan-base-sha>..HEAD -- src/effdim/ notebooks/pu_manifold/` lists only `cka.py` and `tests/test_cka.py`."
    - statement: "The validation ladder and `d=32` gate nothing — no constant frozen here makes either a precondition of the verdict (D8-12, D8-20)."
      status: required
      verification: "`VALIDATION_LADDER_IS_NON_GATING` and `D32_IS_NON_GATING` are both frozen `True`, and `VERDICT_RULE`'s text reads only `S_GRID` clearance."
---

<objective>
Freeze Phase 8. One checkpoint puts every pre-registered value in front of the developer, then one
commit fills every constant in `cka.py` — kernels, HSIC form, `sigma` and its three multipliers, the
`S` grid, the tertile rule, the null construction, the verdict and seed-combination rules, and
D8-21's reporting block — and that commit is the strict git ancestor every Phase 8 number must be
proved against.

Purpose: D8-22 is the phase's central discipline and this record already carries a sealed FAIL
(`02.2`) and an amendment (`06-PREREGISTRATION-AMENDMENT-01`) from getting it wrong. Everything
before this plan measured nothing on PU except the two D8-03 bandwidths, which are pre-registration
inputs by design and are frozen here as literals.

Output: a passing `assert_preregistered()`, the freeze commit, and the ancestry proof wired into both
the runner and the test suite.
</objective>

<superseded_decision>
D7-07's CKA-out-of-scope scope decision is superseded by phase decision, recorded in full in
`08-01-PLAN.md`'s `<superseded_decision>` block. This plan freezes that supersession into
`cka.ALIGNMENT_METRIC`, `cka.SUPERSEDES` and `cka.SUPERSESSION_RULE` — a positive, checkable fact in
Phase 8's own module, exactly the idiom `crossmodal_curvature.ALIGNMENT_METRIC = "mknn"` uses for
D7-07 itself. `notebooks/pu_manifold/crossmodal_curvature.py` is **not** edited. No sealed verdict is
reopened, softened or recomputed.
</superseded_decision>

<artifacts_this_phase_produces>
## Artifacts this phase produces

The full phase-level artifact table is in `08-01-PLAN.md`. This plan adds **eight new constants that
are born already-frozen** in the freeze commit, rather than declared UNSET first, because nothing
before the freeze reads them and D8-22 only requires that they be committed in the single freeze
commit that precedes every number:

`N_REPEATS`, `NEGATIVE_CONTROL_FIELD`, `PLANTED_EFFECT_GRID`, `PLANTED_EFFECT_SEED`,
`REPORTING_BLOCK_ROWS`, `REPORTING_BLOCK_RULE`, `VERDICT_SENTENCE_RULE`, `RECORD_STEM`.

`_REQUIRED_CONSTANTS` therefore goes from 37 entries to 45, and every one of the 45 carries a filled
value after this plan's freeze commit.

Also added: `FREEZE_COMMIT_SHA` (real value) in `notebooks/diagnostics/08_cka_alignment_run.py` and
in `notebooks/pu_manifold/tests/test_cka.py`; test functions `test_assert_preregistered_passes`,
`test_freeze_commit_exists`, `test_freeze_commit_is_a_strict_ancestor_of_head`,
`test_seed_handling_rule_is_exact`, `test_reporting_block_rows_are_complete`.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-CONTEXT.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-RESEARCH.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-PATTERNS.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-02-SUMMARY.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Ratify every Phase 8 pre-registered constant — the point of no return</name>

  <read_first>
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-03-SUMMARY.md` — the two measured sigma values at full precision, which this checkpoint asks the developer to ratify as frozen literals
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-CONTEXT.md` §"Inherited, Non-Negotiable" (D8-22) and the four `**Reversibility:** one-way` markers on D8-03, D8-09, D8-15, D8-21
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-03-DECISION.md` — the ratified never-pool constraint D8-15 carries
    - `notebooks/pu_manifold/cka.py` — the current UNSET constants block and `_REQUIRED_CONSTANTS`
  </read_first>

  <decision>
Ratify the exact value of every Phase 8 pre-registered constant, and with it the freeze itself. After
the next commit, changing any of these costs a new pre-registration and a full re-run — the remedy
`02.2`'s sealed FAIL and `06-PREREGISTRATION-AMENDMENT-01` both establish, never a silent fix.

Five of these are marked one-way in `08-CONTEXT.md` and are presented individually below: **D8-03**
(frozen global sigma), **D8-09** (no headline `S`; clearance at every `S`), **D8-15** (seed
combination, carrying `05-03-DECISION.md`'s ratified never-pool constraint), **D8-21** (frozen
unconditional reporting block), **D8-22** (the freeze commit itself).
  </decision>

  <context>
Present each group with its value and its one-line reason, then ask for a single ratification or a
list of amendments. Read the measured sigma values out of `08-03-SUMMARY.md` — do not re-measure.

**D8-03 (one-way) — the two frozen global RBF bandwidths.** `SIGMA_HSC` and `SIGMA_LEGACYSURVEY`,
the median pairwise Euclidean distance over all 10,000 points, computed separately per modality by
08-03's `--mode sigma`, quoted at full precision. A per-subset median was rejected because the
high-`||H||` subset is measurably denser at `d=20/25` (`spearman(density, ||H||)` = +0.4281 and
+0.3150), so a per-subset bandwidth would make the RBF gap a density artifact by construction.

**D8-04 — `SIGMA_MULTIPLIERS = (0.5, 1.0, 2.0)`,** `sigma` carrying the headline and the other two
reported beside it as non-gating diagnostics. A sign flip across the 4x range means the RBF read is
worthless here.

**D8-01/D8-02 — `KERNELS = ("linear", "rbf")`,** linear carrying the headline verdict and RBF gating
nothing; the unbiased Song et al. (2012) HSIC form, stated in `HSIC_ESTIMATOR_RULE`.

**D8-08 — `S_GRID = (10, 20, 50)`.** 07.1's grid values re-declared as fresh Phase 8 literals, not
inherited as a gating constant and with no headline `S`. `S` does not buy sample size: pooled subset
size is ~3,333 per tertile at any `S`; `S` trades density-match tightness against realized `||H||`
contrast. Confirm the values, or name a different grid.

**D8-09 (one-way) — no headline `S`; the verdict fires only if the gap clears its null at EVERY grid
point.** Relaxing this after seeing an `S`-dependent result is the post-hoc retuning the `k*=15` and
`02.2` pre-registrations exist to prevent.

**D8-05/D8-10/D8-11 — three `||H||` tertiles; the statistic is CKA(tertile 3) minus CKA(tertile 1);
the middle tertile gates nothing; the null permutes tertile labels within density strata.**
`N_PERMUTATIONS = 1000`, `PERMUTATION_SEED = 20260827`, `NULL_QUANTILE_PER_TAIL = 0.975`
(two-tailed), `NULL_KERNELS = ("linear", "rbf_sigma")`.

**D8-13/D8-14 — `D_SWEEP = (20, 25, 32)`, `SEED_FIELD_D = 25`, `TORCH_INIT_SEEDS = (0, 1, 2)`,
per-`d` verdicts reported independently, six field cells at all three `S` values — 18 cells.** Flag
the departure from `08-RESEARCH.md` Open Question 1, which recommended running the seed axis at a
headline `S` only: D8-09 leaves no headline `S` to restrict to, so a seed verdict measured at one `S`
would be un-evaluable under the phase's own verdict rule.

**D8-15 (one-way) — unanimous 3-of-3 or nothing, seeds never pooled.** `SEED_HANDLING_RULE` carries
`05-03-DECISION.md`'s ratified constraint and is guarded by exact string equality. One or two of
three clearances is a terminal, non-supportive `SPLIT ACROSS SEEDS`, never upgraded by majority vote.

**D8-18/D8-19 — the control constants.** `PLANTED_EFFECT_GRID = (0.0, 0.02, 0.05, 0.10, 0.20, 0.35,
0.50)`, the fraction of the high-`||H||` tertile's rows in one modality whose crossmodal pairing is
destroyed, swept to read a detection floor at PU's realized ~1.5x dynamic range rather than at Phase
6's ~20x. `PLANTED_EFFECT_SEED = 20260827`. `N_REPEATS = 30` for the shuffled-`||H||` false-positive
rate, `NEGATIVE_CONTROL_FIELD = "h_norm_25"`, run at all three `S`. Note the runtime: 30 repeats at
three `S` values is 90 full null computations, on the order of one to two hours. Frozen rather than
left as a `--n-repeats` flag, because it determines the precision of a number D8-21 makes mandatory —
a post-hoc `--n-repeats 3` could shrink an inconvenient rate.

**D8-21 (one-way) — the frozen unconditional reporting block.** `REPORTING_BLOCK_ROWS` names exactly
five rows `08-FINDINGS.md` must print regardless of outcome, each beside the headline and not in an
appendix: the `d=32` gap, the shuffled-`||H||` false-positive rate, the planted-effect detection
floor, the realized `||H||` contrast per `S`, and all three `sigma` rungs. `VERDICT_SENTENCE_RULE`
makes it structurally impossible to quote a headline without its caveat: the verdict sentence cannot
be written without the `d=32` gap and the false-positive rate in the same sentence. This is the
developer's own answer to D8-12 and D8-20 both declining to gate — `08-CONTEXT.md` records the
consequence in its own words: *"Phase 8 has no structural mechanism preventing an artifact from being
written up as a result. Every safeguard is now a reporting obligation."*

**D8-22 (one-way) — the freeze itself.** The next commit contains every value above. After it, a
constant edit is a pre-registration breach.
  </context>

  <options>
    <option id="ratify">
      <name>Ratify as presented</name>
      <pros>The values follow the record and the phase proceeds to the freeze commit immediately.</pros>
      <cons>Each is one-way; a later change costs a new pre-registration and a full re-run.</cons>
    </option>
    <option id="ratify-with-amendments">
      <name>Ratify with named amendments</name>
      <pros>Any value can be changed now at the cost of an edit; nothing is frozen yet.</pros>
      <cons>Requires naming each amended constant and its new value before the freeze can proceed.</cons>
    </option>
    <option id="halt">
      <name>Halt — a value needs measurement or discussion first</name>
      <pros>Nothing is frozen on an unresolved question.</pros>
      <cons>Blocks the phase until resolved.</cons>
    </option>
  </options>

  <resume-signal>Select: ratify, ratify-with-amendments (naming each constant and value), or halt.</resume-signal>

  <reversibility rating="one-way">D8-03, D8-09, D8-15, D8-21 and D8-22 are all marked one-way in `08-CONTEXT.md`. This checkpoint is the last point at which any of them can be changed for the cost of an edit.</reversibility>

  <verify>
    <automated>test -f .planning/phases/08-curvature-conditioned-cka-alignment/08-04-DECISION.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-04-DECISION.md` exists and records the developer's selection verbatim, the date, and every amended value if any.
    - The decision file quotes `SIGMA_HSC` and `SIGMA_LEGACYSURVEY` at the same full precision `08-03-SUMMARY.md` recorded.
    - The decision file states explicitly that a standing authorization is not a user response — this checkpoint is closed only by the developer's own words.
  </acceptance_criteria>

  <done>Every pre-registered constant has an explicitly ratified value and the developer has acknowledged the freeze as the point of no return.</done>
</task>

<task type="auto">
  <name>Task 2: Fill every constant — the D8-22 freeze commit</name>

  <precondition>`08-04-DECISION.md` exists and records a `ratify` or `ratify-with-amendments` outcome from Task 1. Task 2 must not run on a `halt` outcome or on an absent decision file.</precondition>

  <read_first>
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-04-DECISION.md` — the ratified values, including any amendments, which override this plan's proposed literals
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-03-SUMMARY.md` — the two sigma values at full precision
    - `notebooks/pu_manifold/cka.py` — the full UNSET constants block and `_REQUIRED_CONSTANTS` from 08-01 and 08-02
    - `notebooks/pu_manifold/density_stratified_null.py` lines 61-95 — the constants-block idiom and the freeze-provenance docstring shape
    - `notebooks/pu_manifold/linear_probe.py` lines 249-383 — `assert_preregistered`'s exact-equality guard on a rule constant encoding a ratified decision
  </read_first>

  <files>notebooks/pu_manifold/cka.py</files>

  <action>
Fill every UNSET constant in `cka.py` with the value ratified in `08-04-DECISION.md`, and add the
eight control and reporting constants already filled. Update `_REQUIRED_CONSTANTS` to 45 entries and
add a guard branch in `assert_preregistered` for each of the eight new names. This is a single
commit: the freeze commit. Nothing else goes in it — no test change, no runner change, no `.planning`
file.

Scalars and tuples: `KERNELS = ("linear", "rbf")`; `SIGMA_MULTIPLIERS = (0.5, 1.0, 2.0)`;
`SIGMA_HSC` and `SIGMA_LEGACYSURVEY` set to the two full-precision literals from `08-03-SUMMARY.md`;
`GRAM_DTYPE = "float32"`; `S_GRID = (10, 20, 50)`; `N_TERTILES = 3`; `D_SWEEP = (20, 25, 32)`;
`SEED_FIELD_D = 25`; `TORCH_INIT_SEEDS = (0, 1, 2)`; `N_PERMUTATIONS = 1000`;
`PERMUTATION_SEED = 20260827`; `NULL_QUANTILE_PER_TAIL = 0.975`;
`NULL_KERNELS = ("linear", "rbf_sigma")`; `DENSITY_K = 30`; `DENSITY_FIELD_D = 20`;
`DENSITY_INPUT = "legacysurvey_ambient_768"`; `ALIGNMENT_METRIC = "cka"`;
`SUPERSEDES = ("crossmodal_curvature.ALIGNMENT_METRIC",)`; `N_REPEATS = 30`;
`NEGATIVE_CONTROL_FIELD = "h_norm_25"`;
`PLANTED_EFFECT_GRID = (0.0, 0.02, 0.05, 0.10, 0.20, 0.35, 0.50)`;
`PLANTED_EFFECT_SEED = 20260827`; `RECORD_STEM = "08_cka_alignment"`; and every
`*_IS_NON_GATING` boolean set to `True` — `RBF_IS_NON_GATING`, `SIGMA_LADDER_IS_NON_GATING`,
`MIDDLE_TERTILE_IS_NON_GATING`, `D32_IS_NON_GATING`, `VALIDATION_LADDER_IS_NON_GATING`,
`DIAGNOSTICS_ARE_NON_GATING`. `DENSITY_K`, `DENSITY_FIELD_D` and `DENSITY_INPUT` are Phase 8's own
fresh literals even though their values equal `crossmodal_curvature`'s — the freeze-boundary rule
that module's own docstring states, and D8-07's inherit-unchanged mandate applied to the value, not
to the import.

Rule strings, each a full prose sentence-set rather than a stub, in the "value plus prose-rule string
naming what it may and may not do" pairing: `HSIC_ESTIMATOR_RULE` (the Song et al. 2012 unbiased
form on raw zero-diagonal Gram matrices; never double-centered, because the correction terms already
debias and applying them to a pre-centered matrix silently reproduces the biased estimator under the
unbiased one's name); `SIGMA_FREEZE_RULE` (D8-03, including the measured density-curvature
correlations that make a per-subset median a confound by construction); `STRATIFICATION_RULE`
(including D8-06's density-residualized-curvature semantics); `SENSITIVITY_GRID_RULE` (thresholds not
point estimates; qualifies but never overturns or escalates); `TERTILE_STATISTIC_RULE` (D8-10);
`NULL_CONSTRUCTION_RULE` (D8-11, naming both rejected alternatives); `VERDICT_RULE` (D8-09, naming
`S_GRID` explicitly); `SEED_HANDLING_RULE` (the exact ratified never-pool string);
`SEED_VERDICT_COMBINATION_RULE` (D8-15's unanimous-or-nothing branching);
`DENSITY_SIGN_CONVENTION` (D8-07's `1.0 / w` relative density, matching Phase 4's REGN-01);
`SUPERSESSION_RULE` (Phase 8 supersedes D7-07's CKA-out-of-scope decision by phase decision, taken
2026-08-27, never by editing `crossmodal_curvature.py`, whose `ALIGNMENT_METRIC = "mknn"` remains
true of Phase 7's own record rows); `SWISS_ROLL_APPLICABILITY_RULE` (D8-17's deliberate
not-applicable declaration and its reason).

`REPORTING_BLOCK_ROWS` is a tuple of exactly five row identifiers —
`"d32_gap"`, `"shuffled_h_false_positive_rate"`, `"planted_effect_detection_floor"`,
`"realized_h_contrast_per_s"`, `"sigma_rungs"` — and `REPORTING_BLOCK_RULE` states that
`08-FINDINGS.md` prints all five regardless of outcome, each beside the headline and not in an
appendix, 07.1's D-15 being the precedent. `VERDICT_SENTENCE_RULE` states that the verdict sentence
cannot be written without the `d=32` gap and the shuffled-`||H||` false-positive rate in the same
sentence, so a headline cannot be quoted without its caveat — the failure mode by which Phase 4's
number escaped its confound.

Update the module docstring's freeze paragraph to state that this commit is the freeze commit and
that a later edit to any of these constants, once a Phase 8 number exists, is a pre-registration
breach whose only remedy is a fresh freeze and a fresh run.

Commit this file alone, with a message naming D8-22 and stating that it precedes every Phase 8
number. Record the resulting SHA — Task 3 needs it.
  </action>

  <verify>
    <automated>.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import cka; cka.assert_preregistered(); print(len(cka._REQUIRED_CONSTANTS))"</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import cka; cka.assert_preregistered(); print(len(cka._REQUIRED_CONSTANTS))"` exits 0 and prints 45.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import cka; assert len(cka.REPORTING_BLOCK_ROWS)==5; assert cka.NULL_KERNELS==('linear','rbf_sigma'); assert cka.S_GRID==(10,20,50); assert cka.N_TERTILES==3; assert cka.GRAM_DTYPE=='float32'"` exits 0.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import cka; assert 'S_GRID' in cka.VERDICT_RULE; assert all(k in cka.VERDICT_SENTENCE_RULE for k in ('d=32','false-positive'))"` exits 0.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import cka; assert all(v is True for v in (cka.RBF_IS_NON_GATING, cka.SIGMA_LADDER_IS_NON_GATING, cka.MIDDLE_TERTILE_IS_NON_GATING, cka.D32_IS_NON_GATING, cka.VALIDATION_LADDER_IS_NON_GATING))"` exits 0.
    - `git show --stat HEAD --name-only` lists exactly one file, `notebooks/pu_manifold/cka.py`.
    - `test ! -f notebooks/.cache/08_cka_alignment.jsonl` — no Phase 8 verdict record exists at the freeze commit.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0.
  </acceptance_criteria>

  <done>Every one of the 45 pre-registered constants carries its ratified value, `assert_preregistered()` passes, and exactly one commit — containing only `cka.py` — did it.</done>

  <reversibility rating="one-way">This commit is the freeze. After it, changing any constant costs a new pre-registration and a full re-run, per `02.2` and `06-PREREGISTRATION-AMENDMENT-01`.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Wire the freeze SHA into the runner and the ancestry proof into the test suite</name>

  <precondition>Task 2's freeze commit exists in the current branch's history and its SHA is recorded in the task-2 commit output.</precondition>

  <read_first>
    - `notebooks/diagnostics/08_cka_alignment_run.py` — the placeholder `FREEZE_COMMIT_SHA` and `_strict_ancestor_or_exit` from 08-03
    - `notebooks/pu_manifold/tests/test_density_stratified_null.py` lines 33-90 — `FREEZE_COMMIT_SHA`, `_freeze_commit_exists`, `_freeze_commit_is_strict_ancestor_of_head`, and the parametrized guard sweep to mirror
    - `notebooks/pu_manifold/cka.py` — the now-frozen constants, for the exact values the new tests assert
  </read_first>

  <files>notebooks/diagnostics/08_cka_alignment_run.py, notebooks/pu_manifold/tests/test_cka.py</files>

  <action>
Set `FREEZE_COMMIT_SHA` in `notebooks/diagnostics/08_cka_alignment_run.py` to the full 40-character
SHA of Task 2's commit, with a comment stating it is the commit that filled `cka.py`'s constants and
that every Phase 8 number must be a strict descendant of it. Point `resolve_record_path`'s default at
`cache.cache_path(cka.RECORD_STEM, "jsonl")`. Make every production mode — `sweep`,
`positive-control`, `negative-control` — call `cka.assert_preregistered()` first and
`_strict_ancestor_or_exit(args.freeze_commit)` second, before touching any data.

Add to `notebooks/pu_manifold/tests/test_cka.py` the same `FREEZE_COMMIT_SHA` literal and the
`_repo_root` / `_freeze_commit_exists` / `_freeze_commit_is_strict_ancestor_of_head` helper trio,
copied from `test_density_stratified_null.py` lines 33-73 in shape: `git cat-file -e <sha>^{commit}`
for existence, `git merge-base --is-ancestor <sha> HEAD` for ancestry, and
`git rev-list --count <sha>..HEAD` being at least 1 for strictness.

Add `test_assert_preregistered_passes` (the guard now returns without raising),
`test_freeze_commit_exists`, `test_freeze_commit_is_a_strict_ancestor_of_head`,
`test_seed_handling_rule_is_exact` (monkeypatch `SEED_HANDLING_RULE` to any other non-empty string
and assert `assert_preregistered` raises — truthiness is not enough), and
`test_reporting_block_rows_are_complete` (the five row identifiers, exactly, in order). Extend the
existing parametrized `_REQUIRED_CONSTANTS` rejection sweep so it covers all 45 names.

Update `.planning/phases/08-curvature-conditioned-cka-alignment/08-VALIDATION.md`: fill the Task ID,
Plan and Status columns for every row now green, re-key the D8-23 row to
`test_cka_import_purity.py::test_import_cka_does_not_mutate_sealed_modules`, and add the freeze SHA
to the D8-22 manual-only row's instructions so the ancestry check is runnable as written.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_cka.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_cka.py::test_freeze_commit_is_a_strict_ancestor_of_head -q` exits 0.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_cka.py::test_assert_preregistered_passes -q` exits 0.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_cka.py::test_assert_preregistered_rejects_unset_constant -q` reports 45 parametrized cases.
    - `git merge-base --is-ancestor $(grep -oE '[0-9a-f]{40}' notebooks/diagnostics/08_cka_alignment_run.py | head -1) HEAD` exits 0, and `git rev-list --count $(grep -oE '[0-9a-f]{40}' notebooks/diagnostics/08_cka_alignment_run.py | head -1)..HEAD` prints at least 1.
    - The 40-character SHA in `notebooks/diagnostics/08_cka_alignment_run.py` and in `notebooks/pu_manifold/tests/test_cka.py` are identical.
    - `.venv/bin/python notebooks/diagnostics/08_cka_alignment_run.py --mode sweep --freeze-commit HEAD` exits 1 — HEAD is not the freeze commit.
    - `git log -p <freeze-sha>..HEAD -- notebooks/pu_manifold/cka.py` shows no change to any name in `_REQUIRED_CONSTANTS`.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0.
  </acceptance_criteria>

  <done>The freeze commit's SHA is wired into both the runner's gate and the test suite's ancestry proof, every production mode refuses to run without it, and all 45 constants are covered by the guard sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer ratification → frozen constant | The checkpoint is the only place a value can still change cheaply |
| git history → every later Phase 8 number | The freeze commit's strict ancestry is what makes a number quotable |
| working tree → frozen constants block | A post-freeze edit is the phase's central integrity failure |

No network, no authentication, no user input, no external service. The risks are
scientific-integrity risks.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-18 | Tampering | a constant edited after the freeze commit | high | mitigate | `test_freeze_commit_is_a_strict_ancestor_of_head` plus the manual `git log -p <freeze-sha>..HEAD -- notebooks/pu_manifold/cka.py` backstop; the freeze commit contains only `cka.py`, so the diff is readable |
| T-08-19 | Repudiation | a run whose constants drifted producing a quotable number | high | mitigate | Every production mode calls `assert_preregistered()` then `_strict_ancestor_or_exit`; both are pinned by tests |
| T-08-15 | Spoofing | a production row stamped with a wrong `--freeze-commit` | high | mitigate | Exact-equality check against the hardcoded 40-char SHA, plus ancestry and strictness checks |
| T-08-09 | Tampering | seed pooling reintroduced by an edit to `SEED_HANDLING_RULE` | high | mitigate | Exact-string-equality guard, `test_seed_handling_rule_is_exact`, and `pooled_field_guard` raising |
| T-08-20 | Repudiation | a standing authorization recorded as developer approval of a one-way decision | high | mitigate | Task 1's decision file must record the developer's own words and state explicitly that a standing authorization is not a user response |
| T-08-21 | Tampering | D8-21's reporting block edited later to drop an inconvenient row | high | mitigate | `REPORTING_BLOCK_ROWS` is frozen with exactly five entries and pinned by `test_reporting_block_rows_are_complete`; it sits inside the freeze commit's diff |
| T-08-06 | Information Disclosure | none — no secrets, no network egress | low | accept | Local constants and git metadata only |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages. `08-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable — none proposed, none `[ASSUMED]`, none `[SUS]`, none `[SLOP]` |
</threat_model>

<verification>
- `.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import cka; cka.assert_preregistered()"` exits 0.
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0.
- `git show --name-only <freeze-sha>` lists exactly `notebooks/pu_manifold/cka.py`.
- `git merge-base --is-ancestor <freeze-sha> HEAD` exits 0 and `git rev-list --count <freeze-sha>..HEAD` is at least 1.
- `test ! -f notebooks/.cache/08_cka_alignment.jsonl`.
- `git diff --name-only <plan-base-sha>..HEAD -- src/effdim/` prints nothing.
</verification>

<success_criteria>
- All 45 pre-registered constants carry ratified values and `assert_preregistered()` passes.
- Exactly one commit, containing only `cka.py`, performed the freeze, and it is a strict ancestor of
  HEAD.
- Every production mode refuses to run without both the guard and the strict-ancestor proof.
- No Phase 8 verdict number exists anywhere in the tree.
- The five one-way decisions (D8-03, D8-09, D8-15, D8-21, D8-22) are recorded as explicitly ratified
  in `08-04-DECISION.md`, in the developer's own words.
</success_criteria>

<output>
Create `.planning/phases/08-curvature-conditioned-cka-alignment/08-04-SUMMARY.md` when done.
Record in it: the freeze commit SHA in full, the ratified value of every constant D8-22 enumerates,
any amendment the developer made at Task 1, and the confirmation that no Phase 8 number existed at
the freeze commit.
</output>
