---
phase: 08-curvature-conditioned-cka-alignment
plan: 06
type: execute
wave: 5
depends_on: ["08-05"]
files_modified:
  - notebooks/08_cka_alignment_check.ipynb
  - .planning/phases/08-curvature-conditioned-cka-alignment/08-FINDINGS.md
  - .planning/phases/08-curvature-conditioned-cka-alignment/08-VALIDATION.md
autonomous: false
requirements: [D8-01, D8-04, D8-06, D8-10, D8-12, D8-17, D8-18, D8-19, D8-20, D8-21, D8-22, D8-23, D8-24]
user_setup: []

must_haves:
  truths:
    - "`notebooks/08_cka_alignment_check.ipynb` is committed with its outputs, executed end to end, and reads only `notebooks/.cache/08_cka_alignment.jsonl` — it recomputes no statistic."
    - "The notebook prints all five rows named by `cka.REPORTING_BLOCK_ROWS`, regardless of the sweep's outcome."
    - "`08-FINDINGS.md` exists and its verdict sentence contains both the `d=32` gap and the shuffled-`||H||` false-positive rate."
    - "`08-FINDINGS.md` states, where the tertile definition is introduced, that the tertiles rank density-residualized curvature and not raw `||H||`."
    - "`08-FINDINGS.md` carries a per-`d` clearance table in the shape 07.1's own table uses, so the two sit beside each other row-for-row."
    - "`08-FINDINGS.md` records the freeze commit SHA and the ancestry proof command with its output."
    - "`08-FINDINGS.md` records that D8-12 and D8-20 both gate nothing and that this was the developer's explicit choice on 2026-08-27, with D8-21 as the answer to it."
    - "`08-VALIDATION.md` has `status: validated`, every row's Status filled, and the four manual-only verifications marked with their outcome."
  truths_backstop:
    - statement: "The freeze commit is a strict git ancestor of HEAD and no frozen constant changed after it — `git merge-base --is-ancestor <freeze-sha> HEAD` exits 0 and `git log -p <freeze-sha>..HEAD -- notebooks/pu_manifold/cka.py` shows no constant change."
      verification: backstop
    - statement: "`08-FINDINGS.md` states the density-residualized-tertile semantics where the tertile definition is introduced, not in a footnote — a judgement about prominence in prose."
      verification: backstop
    - statement: "The verdict sentence cannot be written without `d=32`'s gap and the shuffled-`||H||` false-positive rate in the same sentence — a structural property of prose."
      verification: backstop
    - statement: "The frozen unconditional reporting block prints every required row beside the headline and not in an appendix — a layout and prominence judgement."
      verification: backstop
  artifacts:
    - notebooks/08_cka_alignment_check.ipynb
    - .planning/phases/08-curvature-conditioned-cka-alignment/08-FINDINGS.md
    - .planning/phases/08-curvature-conditioned-cka-alignment/08-VALIDATION.md
  key_links:
    - "The notebook and `08-FINDINGS.md` both read their numbers from `notebooks/.cache/08_cka_alignment.jsonl`; neither recomputes a statistic, so the record and the write-up cannot diverge."
    - "`cka.REPORTING_BLOCK_ROWS` and `cka.VERDICT_SENTENCE_RULE` are read programmatically by the notebook, so a row dropped from the write-up is detectable rather than invisible."
  prohibitions:
    - statement: "No Swiss roll notebook is written — the CLAUDE.md standing rule is declared NOT APPLICABLE by D8-17, by decision and not by omission."
      status: required
      verification: "No file matching `notebooks/08*swiss*roll*.ipynb` exists; `08-FINDINGS.md` records the D8-17 declaration and its reason."
    - statement: "No sealed verdict is reopened, softened or recomputed — Phase 7's `ASSOCIATION DETECTED`, 07.1's `SURVIVES AT SUBSET OF d` / `SEED STABLE AT d=25`, Phase 5's `SPLIT ACROSS SEEDS`."
      status: required
      verification: "`08-FINDINGS.md` cites each as fixed context with its source document, and states no Phase 8 number reinterprets any of them."
    - statement: "No frozen constant is edited to match a written number (D8-22)."
      status: required
      verification: "`git log -p <freeze-sha>..HEAD -- notebooks/pu_manifold/cka.py` shows no change to any name in `_REQUIRED_CONSTANTS`."
    - statement: "No sealed module is edited (D8-23) and `src/effdim/` is untouched (D8-24)."
      status: required
      verification: "`git diff --name-only <phase-base-sha>..HEAD -- src/effdim/` prints nothing; `notebooks/pu_manifold/` shows only `cka.py`, `tests/test_cka.py` and `tests/test_cka_import_purity.py`."
    - statement: "No number is rounded so that a near-miss reads as a miss or a clearance (07.1 §2 house style)."
      status: required
      verification: "Every number in the reporting block is printed at the precision the JSONL carries."
---

<objective>
Turn the frozen record into a readable result that cannot be quoted without its caveats: a reporting
notebook committed with its outputs, and `08-FINDINGS.md` carrying D8-21's frozen unconditional
reporting block beside the headline and a verdict sentence that structurally cannot omit the `d=32`
gap or the false-positive rate.

Purpose: **this is not documentation polish.** D8-12 and D8-20 both declined to gate — a
hard-invalidator reading of `d=32` and a hard-gate ordering of the validation ladder were each
offered and explicitly declined by the developer on 2026-08-27. `08-CONTEXT.md` records the
consequence in its own words: *"Phase 8 has no structural mechanism preventing an artifact from being
written up as a result. Every safeguard is now a reporting obligation."* D8-21 is the developer's
answer to that, which makes this plan's deliverables load-bearing and not optional.

Output: `notebooks/08_cka_alignment_check.ipynb`, `08-FINDINGS.md`, and a validated
`08-VALIDATION.md`.
</objective>

<superseded_decision>
D7-07's CKA-out-of-scope scope decision is superseded by phase decision, taken by the developer
2026-08-27, recorded in full in `08-01-PLAN.md`'s `<superseded_decision>` block and frozen into
`cka.ALIGNMENT_METRIC = "cka"` / `cka.SUPERSEDES` / `cka.SUPERSESSION_RULE`.
`notebooks/pu_manifold/crossmodal_curvature.py` was never edited; its `ALIGNMENT_METRIC = "mknn"`
(line 109) remains true of Phase 7's own record rows. `08-FINDINGS.md` must state this supersession
explicitly, because a reader comparing the two modules' constants will otherwise see a contradiction
with no explanation.
</superseded_decision>

<artifacts_this_phase_produces>
## Artifacts this phase produces

The full phase-level artifact table is in `08-01-PLAN.md`. New artifacts from **this plan**:

- `notebooks/08_cka_alignment_check.ipynb` — reporting notebook, committed with outputs, executed end
  to end
- `.planning/phases/08-curvature-conditioned-cka-alignment/08-FINDINGS.md` — the verdict document
- Updated `.planning/phases/08-curvature-conditioned-cka-alignment/08-VALIDATION.md` — `status:
  validated`, all Status cells filled, four manual-only rows resolved
- `.planning/phases/08-curvature-conditioned-cka-alignment/08-06-DECISION.md` — the human-verify
  checkpoint's outcome

No new code symbol is created. `cka.py` and `08_cka_alignment_run.py` are read, not modified.
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-CONTEXT.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-RESEARCH.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-VALIDATION.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-04-SUMMARY.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reporting notebook — the frozen block printed unconditionally, committed with outputs</name>

  <precondition>`notebooks/.cache/08_cka_alignment.jsonl` exists and carries positive-control, negative-control and sweep rows from 08-05, every one stamped with the freeze commit SHA.</precondition>

  <read_first>
    - `notebooks/07.1_density_stratified_null.ipynb` — open it directly for cell structure, the per-`d` table layout and the verdict-sentence composition Phase 8's notebook sits beside. `08-PATTERNS.md` flags this analog as a role-match that was not itself read during pattern mapping, so this read is required rather than optional.
    - `notebooks/pu_manifold/cka.py` — `REPORTING_BLOCK_ROWS`, `REPORTING_BLOCK_RULE`, `VERDICT_SENTENCE_RULE`, and the non-gating constants, all read programmatically by the notebook
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-05-SUMMARY.md` — the numbers to expect, so a mismatch between notebook and record is caught
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-CONTEXT.md` D8-21 and §"Specific Ideas" — the requirement that the `d=32` reading be prominent and not a table row
    - `CLAUDE.md` — notebooks are committed with their outputs, executed end to end
  </read_first>

  <files>notebooks/08_cka_alignment_check.ipynb</files>

  <action>
Create `notebooks/08_cka_alignment_check.ipynb`, following `notebooks/07.1_density_stratified_null.ipynb`'s
cell structure. It loads `notebooks/.cache/08_cka_alignment.jsonl` and `pu_manifold.cka` and
**recomputes no statistic** — every number it prints comes from the frozen record, so the notebook and
the record cannot diverge.

Cell order, headline first and caveats beside it rather than after it:

1. Markdown header naming the phase, the freeze commit SHA and the fact that every number below was
   produced by a run whose `preregistration_commit` equals that SHA.
2. A cell that loads the record, asserts every row's `preregistration_commit` equals
   `cka`-era freeze SHA, and prints the row counts per mode.
3. **The frozen unconditional reporting block**, driven programmatically by iterating
   `cka.REPORTING_BLOCK_ROWS` and printing each of the five rows from the record: the `d=32` gap, the
   shuffled-`||H||` false-positive rate, the planted-effect detection floor, the realized `||H||`
   contrast per `S`, and the gap at all three `sigma` rungs. Iterating the frozen tuple rather than
   hand-listing five prints is what makes a dropped row detectable: if a row identifier has no
   corresponding number in the record, the cell raises rather than printing four rows quietly.
4. The per-`d` clearance table, in 07.1's own table shape, with one row per `d` and one column per `S`.
5. A cell printing `d=32`'s gap on its own, with the text explaining why it is prominent: `d=32` has
   nil density–curvature coupling (`spearman(density, ||H||) = +0.0118`, `p=0.238`) and no surviving
   07.1 association, so a gap appearing there is prima facie evidence the split machinery manufactures
   gaps — and D8-12 made it a reported diagnostic rather than a hard invalidator, so the judgement
   rests on the reader.
6. The `d=25` seed axis: three per-seed verdicts and the combined outcome, with a note that the three
   seed fields were never pooled (`05-03-DECISION.md`, D8-15).
7. The validation ladder: the invariance-ladder result from 08-01, the detection floor, and the
   false-positive rate beside its nominal rate — with a note that all three gate nothing (D8-20).
8. The verdict sentence, composed to satisfy `cka.VERDICT_SENTENCE_RULE`, followed by a cell that
   asserts the composed sentence string contains both the `d=32` gap value and the false-positive
   rate value. Make that assertion a real one: if the sentence is later edited to drop a number, the
   notebook fails on re-execution.
9. A short closing cell listing what Phase 8 does **not** claim.

Report every number at the precision the JSONL carries — never round a near-miss away (07.1 §2 house
style, inherited from `07-CONTEXT.md` §5's n=800 Betti draw-2 result). Keep the notebook short enough
to read in one sitting; if it passes roughly 15 cells it has drifted past its purpose.

Execute the notebook end to end and commit it with its outputs, per CLAUDE.md. Do not write to
`notebooks/.cache/` from the notebook.

**Do not create a Swiss roll notebook.** The CLAUDE.md standing rule is declared NOT APPLICABLE by
D8-17: CKA is a statistic over two representations that already exist, with no decoder and no
representation map, and the rule's stated purpose is served by D8-16's closed-form invariance ladder,
which ran in 08-01. The declaration is by decision, not omission.
  </action>

  <verify>
    <automated>.venv/bin/python -c "import json,sys; nb=json.load(open('notebooks/08_cka_alignment_check.ipynb')); cells=nb['cells']; assert len(cells)<=16, len(cells); out=[c for c in cells if c['cell_type']=='code' and c.get('outputs')]; assert len(out)>=6, len(out); print('cells', len(cells), 'with outputs', len(out))"</automated>
  </verify>

  <acceptance_criteria>
    - `notebooks/08_cka_alignment_check.ipynb` exists, has at most 16 cells, and at least 6 code cells carry non-empty `outputs` — it was executed end to end before commit.
    - Its rendered output contains one line for each of the five identifiers in `cka.REPORTING_BLOCK_ROWS`.
    - Its rendered output contains a `d=32` gap value printed outside the per-`d` table.
    - Its rendered output contains the composed verdict sentence, and that sentence contains both the `d=32` gap value and the shuffled-`||H||` false-positive rate value as substrings.
    - `grep -c "notebooks/.cache/08_cka_alignment.jsonl" notebooks/08_cka_alignment_check.ipynb` is at least 1, and `grep -c "stratified_tertile_label_null\|tertile_gap_panel" notebooks/08_cka_alignment_check.ipynb` is 0 — the notebook reads, it does not recompute.
    - `ls notebooks/08*swiss*roll*.ipynb 2>/dev/null` prints nothing.
    - `git status --porcelain notebooks/` lists only `08_cka_alignment_check.ipynb`.
  </acceptance_criteria>

  <done>The reporting notebook is committed with its outputs, prints all five frozen block rows programmatically, prints `d=32` prominently, and fails on re-execution if the verdict sentence loses either mandated number.</done>
</task>

<task type="auto">
  <name>Task 2: 08-FINDINGS.md — the frozen block beside the headline and a caveat-bearing verdict sentence</name>

  <precondition>Task 1's notebook is executed and committed, so every number `08-FINDINGS.md` quotes has a rendered source in the notebook and a row in the frozen record.</precondition>

  <read_first>
    - `.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md` — §1's independence rule (D-14), §2's per-`d` clearance table and its full-precision house style, and D-15's unconditional-reporting precedent that D8-21 cites directly
    - `.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-FINDINGS.md` — Phase 7's `ASSOCIATION DETECTED`, cited as fixed context and never reinterpreted
    - `notebooks/08_cka_alignment_check.ipynb` — the executed outputs, which are the source of every number quoted
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-CONTEXT.md` — the full decision set, especially D8-06's semantic consequence, D8-12, D8-20 and D8-21
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-05-SUMMARY.md` — the numbers at full precision
  </read_first>

  <files>.planning/phases/08-curvature-conditioned-cka-alignment/08-FINDINGS.md</files>

  <action>
Write `08-FINDINGS.md`, following `07.1-FINDINGS.md`'s section shape so the two documents read as one
series.

**Section 1 — the verdict sentence, first.** Compose it to satisfy `cka.VERDICT_SENTENCE_RULE`: the
sentence states the headline linear-CKA outcome AND the `d=32` gap AND the shuffled-`||H||`
false-positive rate, all three in the same sentence. This is structural, not stylistic — it is what
makes it impossible to quote the headline without its caveat, which is how Phase 4's number escaped
its confound. Immediately below it, print the frozen unconditional reporting block: all five rows
named by `cka.REPORTING_BLOCK_ROWS`, at full precision, beside the headline and **not** in an
appendix. If the phase's outcome is a null, all five rows still print — that is what "unconditional"
means and it is 07.1's D-15 precedent.

**Section 2 — the per-`d` clearance table**, in the same shape as 07.1's, so Phase 8's rows sit
beside 07.1's `per_d_results = {20: false, 25: true, 32: false}` row-for-row without translation. Each
`d` carries its own verdict; a null at one `d` does not void another and no pooled headline is
invented (D8-13).

**Section 3 — what the tertiles actually rank.** State explicitly, where the tertile definition is
introduced and not in a footnote, that the split is within density strata and therefore ranks
**density-residualized curvature, not raw `||H||`** (D8-06's semantic consequence). Give the reason
in one line: at `d=20/25` a curvature-magnitude split IS partly a density split
(`spearman(density, ||H||)` = +0.4281 and +0.3150), so density matching is load-bearing rather than a
robustness check.

**Section 4 — the seed axis.** The three per-seed verdicts at `d=25`, the combined outcome under the
unanimous-or-nothing rule, and the statement that the three fields were never pooled
(`05-03-DECISION.md`, ratified one-way).

**Section 5 — the validation ladder, and what it does not do.** The invariance-ladder result, the
detection floor at PU's realized ~1.5x dynamic range, and the false-positive rate beside its nominal
rate. Then state plainly that **the ladder gates nothing (D8-20) and `d=32` gates nothing (D8-12)** —
both hard-gate readings were offered and explicitly declined by the developer on 2026-08-27 — and
that Phase 8 therefore has no structural mechanism preventing an artifact from being written up as a
result. Every safeguard is a reporting obligation, and this document is where it is discharged.

**Section 6 — provenance.** The freeze commit SHA, the ancestry proof command and its output, the
resolved `subsample_*.npz` path, the frozen field files, and the confirmation that no Phase 8 number
predates the freeze commit.

**Section 7 — supersession and what is not reopened.** Record that Phase 8 supersedes D7-07's
CKA-out-of-scope decision by phase decision taken 2026-08-27, carried as `cka.SUPERSEDES` and never
by editing `crossmodal_curvature.py`, whose `ALIGNMENT_METRIC = "mknn"` remains true of Phase 7's own
rows. Record that D8-17 declared the CLAUDE.md Swiss roll standing rule not applicable, with its
reason, so the gate reads as satisfied by decision rather than by omission. Record that no sealed
verdict was reopened, softened or recomputed, citing Phase 7's, 07.1's and Phase 5's by name — and
note, as `07.1-FINDINGS.md` itself does, that neither Phase 7's nor 07.1's verdict has been ratified
by a human; that outstanding ratification is a UAT item, not a Phase 8 task.

**Section 8 — what Phase 8 does not claim.** At minimum: it does not claim a per-point local CKA
result, it does not claim an intramodal or model-size-ladder result, it does not reinterpret Phase
4's blocked status, and it does not claim that a gap surviving at `d=32` would be uninterpretable —
D8-12 left that judgement to the reader on purpose.

Report every number at the precision the record carries. Never round a near-miss away.
  </action>

  <verify>
    <automated>.venv/bin/python .planning/phases/08-curvature-conditioned-cka-alignment/check_findings.py</automated>
  </verify>

  <acceptance_criteria>
    - Write a small checker at `.planning/phases/08-curvature-conditioned-cka-alignment/check_findings.py` that imports `pu_manifold.cka`, reads `08-FINDINGS.md`, and exits non-zero unless every one of the five identifiers in `cka.REPORTING_BLOCK_ROWS` has a corresponding printed row. Running it exits 0.
    - The same checker asserts the file's first verdict sentence contains both the `d=32` gap value and the shuffled-`||H||` false-positive rate value, exactly as `cka.VERDICT_SENTENCE_RULE` requires, and exits non-zero if either is absent.
    - `grep -n "density-residualized" .planning/phases/08-curvature-conditioned-cka-alignment/08-FINDINGS.md` matches at least once, and the match is in the section that introduces the tertile definition, not in a trailing note.
    - `grep -c "05-03-DECISION" .planning/phases/08-curvature-conditioned-cka-alignment/08-FINDINGS.md` is at least 1.
    - `grep -c "D8-17" .planning/phases/08-curvature-conditioned-cka-alignment/08-FINDINGS.md` is at least 1 — the Swiss-roll not-applicable declaration is recorded.
    - `grep -c "D7-07" .planning/phases/08-curvature-conditioned-cka-alignment/08-FINDINGS.md` is at least 1 — the supersession is recorded.
    - The freeze SHA in `08-FINDINGS.md` matches the one in `notebooks/diagnostics/08_cka_alignment_run.py`, and `git merge-base --is-ancestor <that-sha> HEAD` exits 0.
    - The per-`d` table has one row for each of `d=20`, `d=25`, `d=32` and one column for each of `S` = 10, 20, 50.
  </acceptance_criteria>

  <done>`08-FINDINGS.md` leads with a verdict sentence that carries its own caveats, prints all five frozen block rows beside the headline, states the density-residualized-tertile semantics where the tertiles are introduced, and records the supersession, the D8-17 declaration and the four things Phase 8 does not claim.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — the four checks no test can run</name>

  <read_first>
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-VALIDATION.md` § Manual-Only Verifications — the four rows this checkpoint discharges
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-FINDINGS.md` — the document under review
    - `notebooks/08_cka_alignment_check.ipynb` — the rendered outputs the findings quote
  </read_first>

  <what-built>
`cka.py` (the unbiased-HSIC estimator, both kernels, the within-density-stratum tertile split, the
label-permutation null and every verdict rule, all 45 constants frozen in one commit),
`08_cka_alignment_run.py` (five modes: selfcheck, sigma, positive-control, negative-control, sweep),
two test modules including the repository's first cross-module import-purity test, the reporting
notebook committed with its outputs, and `08-FINDINGS.md`.

Automated verification already passed: the invariance ladder on both kernels, the freeze guard sweep
over all 45 constants, the import-purity test across four import orders, the strict-ancestor proof,
and the reporting-block completeness checker.
  </what-built>

  <how-to-verify>
Four checks, each a judgement no test can make. `08-VALIDATION.md` lists all four as manual-only.

1. **Freeze ancestry (D8-22).** Run `git merge-base --is-ancestor <freeze-sha> HEAD` — it must exit 0.
   Run `git log -p <freeze-sha>..HEAD -- notebooks/pu_manifold/cka.py` — it must show no change to any
   constant. Run `git show --name-only <freeze-sha>` — it must list `notebooks/pu_manifold/cka.py`
   alone. Confirm every JSONL row's `preregistration_commit` equals that SHA.

2. **Density-residualized-tertile semantics stated where the tertiles are introduced (D8-06).** Open
   `08-FINDINGS.md`. The statement that the tertiles rank density-residualized curvature and not raw
   `||H||` must appear where the tertile definition is introduced — not in a footnote, not in an
   appendix. Judge prominence, not presence.

3. **The verdict sentence carries its caveats (D8-21).** Read the first verdict sentence. It must be
   impossible to quote it without the `d=32` gap and the shuffled-`||H||` false-positive rate coming
   along, because both are in that sentence. A sentence that mentions them in the next paragraph does
   not satisfy this.

4. **The frozen reporting block sits beside the headline (D8-21).** All five rows — the `d=32` gap,
   the false-positive rate, the planted-effect detection floor, the realized `||H||` contrast per `S`,
   and all three `sigma` rungs — must print beside the headline, not in an appendix, and must be
   present regardless of the outcome.

Then read the result itself and judge whether it is a finding or an artifact. D8-12 and D8-20 both
gate nothing by your own explicit choice on 2026-08-27, so this judgement rests with you and no
mechanism makes it for you. The numbers that inform it are the `d=32` gap, the false-positive rate,
and the detection floor.
  </how-to-verify>

  <resume-signal>Type "approved", or describe which of the four checks fails and what must change.</resume-signal>

  <verify>
    <automated>test -f .planning/phases/08-curvature-conditioned-cka-alignment/08-06-DECISION.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-06-DECISION.md` exists and records the developer's own words, the date, and a per-check outcome for all four manual verifications.
    - The decision file states explicitly that a standing authorization is not a user response — this checkpoint is closed only by the developer's own words.
    - `08-VALIDATION.md` is updated to `status: validated`, every Status cell filled, and each manual-only row marked with its outcome and the date.
    - If any check failed, the decision file names the required change and the phase does not seal until it is made.
  </acceptance_criteria>

  <done>All four manual-only verifications are discharged with a recorded outcome in the developer's own words, and `08-VALIDATION.md` is validated.</done>

  <reversibility rating="reversible">The write-up can be amended freely; the frozen constants and the record cannot, and this checkpoint changes neither.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen record → write-up | The only place a number can be dropped, rounded or quoted without its caveat |
| write-up → reader | D8-12 and D8-20 both gate nothing, so the reader's judgement is the last line of defence and D8-21 is what equips it |
| git history → provenance claim | The freeze ancestry stated in `08-FINDINGS.md` must be the one the repository actually has |

No network, no authentication, no user input, no external service. The risks are
scientific-integrity risks.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Repudiation | a result written up without its D8-21 caveats | high | mitigate | The notebook iterates `REPORTING_BLOCK_ROWS` and raises on a missing row; the `check_findings.py` checker asserts all five rows and both mandated numbers in the verdict sentence; a blocking human-verify checkpoint judges prominence |
| T-08-21 | Tampering | a reporting-block row dropped from the write-up after the fact | high | mitigate | `REPORTING_BLOCK_ROWS` is frozen inside the freeze commit; both the notebook and the checker read it programmatically rather than hand-listing |
| T-08-25 | Repudiation | a near-miss rounded so it reads as a clearance or a miss | high | mitigate | Full-precision house style (07.1 §2) stated as a prohibition; every number quoted from the JSONL at record precision |
| T-08-26 | Spoofing | a provenance claim in the write-up that the repository does not support | high | mitigate | The freeze SHA in `08-FINDINGS.md` must match the runner's, and `git merge-base --is-ancestor` must exit 0 — both in the acceptance criteria and in the human checkpoint |
| T-08-20 | Repudiation | a standing authorization recorded as developer approval | high | mitigate | The decision file must record the developer's own words and state explicitly that a standing authorization is not a user response |
| T-08-27 | Tampering | a sealed verdict softened or reinterpreted in the write-up | high | mitigate | Section 7 cites Phase 7's, 07.1's and Phase 5's verdicts as fixed context by name, and records that neither Phase 7's nor 07.1's has been human-ratified |
| T-08-06 | Information Disclosure | none — no secrets, no network egress | low | accept | Local documents and a gitignored record |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages. `08-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable — none proposed, none `[ASSUMED]`, none `[SUS]`, none `[SLOP]` |
</threat_model>

<verification>
- `.venv/bin/python .planning/phases/08-curvature-conditioned-cka-alignment/check_findings.py` exits 0.
- The notebook has non-empty outputs on at least 6 code cells and at most 16 cells total.
- `git merge-base --is-ancestor <freeze-sha> HEAD` exits 0; `git log -p <freeze-sha>..HEAD -- notebooks/pu_manifold/cka.py` shows no constant change.
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0.
- `git diff --name-only <phase-base-sha>..HEAD -- src/effdim/` prints nothing.
- `08-06-DECISION.md` exists with a per-check outcome for all four manual verifications.
</verification>

<success_criteria>
- The reporting notebook is committed with its outputs and fails on re-execution if a frozen block
  row or a mandated verdict-sentence number goes missing.
- `08-FINDINGS.md` leads with a caveat-bearing verdict sentence and prints all five frozen block rows
  beside it, unconditionally.
- The D8-06 semantics, the D8-17 Swiss-roll declaration, the D7-07 supersession, the never-pool
  constraint and the four non-claims are all recorded.
- All four manual-only verifications are discharged in the developer's own words.
- `08-VALIDATION.md` reads `status: validated`.
</success_criteria>

<output>
Create `.planning/phases/08-curvature-conditioned-cka-alignment/08-06-SUMMARY.md` when done.
Record in it: the verdict sentence verbatim, the five frozen block rows at full precision, the
developer's per-check outcomes, and any amendment the checkpoint required.
</output>
