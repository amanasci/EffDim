---
phase: 08-curvature-conditioned-cka-alignment
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - notebooks/pu_manifold/cka.py
  - notebooks/pu_manifold/tests/test_cka.py
autonomous: true
requirements: [D8-01, D8-05, D8-06, D8-08, D8-09, D8-10, D8-11, D8-12, D8-13, D8-15, D8-20, D8-22, D8-23]
user_setup: []

must_haves:
  truths:
    - "`tertile_split_within_strata(h, strata)` returns exactly three index arrays whose union is every point and whose pairwise intersections are empty."
    - "The three subsets' density-stratum marginals are identical: each subset contains the same count from every stratum, up to the within-stratum remainder rule."
    - "The three subsets are equal-n up to the within-stratum remainder, at every `S` in the grid, at n=10,000."
    - "`stratified_tertile_label_null` preserves stratum membership counts and all three tertile sizes exactly on every resample."
    - "`stratified_tertile_label_null` returns one null array per kernel in `NULL_KERNELS`, each of length `n_resamples`."
    - "`tertile_gap_panel` reports CKA for all three tertiles and the tertile-3-minus-tertile-1 gap, for every kernel supplied."
    - "`per_d_verdict` fires a clearance only when the gap clears its two-tailed null at EVERY `S` in the grid — a single non-clearing `S` yields a non-clearance."
    - "`combine_seed_verdicts` raises unless given exactly three seed entries, and returns a terminal split outcome for one or two clearances without upgrading by majority vote."
    - "Any pooled-seed code path raises rather than averaging the three `||H||` fields."
  artifacts:
    - notebooks/pu_manifold/cka.py
    - notebooks/pu_manifold/tests/test_cka.py
  key_links:
    - "`tertile_split_within_strata` consumes the stratum id array produced by `density_stratified_null.density_strata(density, S)` — imported as a pure function, never for a gating value."
    - "`stratified_tertile_label_null` calls `cka_on_subset` against Gram matrices built once outside the function; no kernel value is recomputed inside the resample loop."
    - "`per_d_verdict` reads `NULL_QUANTILE_PER_TAIL` and `S_GRID` from `cka`'s own frozen constants, never from `density_stratified_null`."
  key_links_backstop:
    - statement: "The realized `||H||` contrast printed per `S` decreases as `S` grows, confirming `S` trades density-match tightness against curvature contrast rather than sample size — measurable only on the real PU field in 08-05."
      verification: backstop
  prohibitions:
    - statement: "Seeds are NEVER pooled into one averaged `||H||` field; each seed gets its own within-stratum tertile split and its own verdict (`05-03-DECISION.md`, ratified one-way, carried by D8-15)."
      status: required
      verification: "`SEED_HANDLING_RULE` is guarded by exact string equality in `assert_preregistered`, and `test_seed_pooling_raises` asserts a pooled-mode call raises."
    - statement: "`mknn.permutation_null`'s pairing-shuffle null is NOT used — it nulls alignment itself, a question Phase 7 already settled (D8-11)."
      status: required
      verification: "`grep -n 'permutation_type' notebooks/pu_manifold/cka.py` prints nothing."
    - statement: "No monotone-trend or compound multi-criterion verdict is introduced; the middle tertile is a shape diagnostic and gates nothing (D8-10)."
      status: required
      verification: "`MIDDLE_TERTILE_IS_NON_GATING` is a required constant and `test_middle_tertile_does_not_gate` asserts the verdict is unchanged when the middle tertile's CKA is perturbed."
    - statement: "No sealed module is edited (D8-23) and `src/effdim/` is untouched (D8-24)."
      status: required
      verification: "`git diff --name-only <plan-base-sha>..HEAD -- src/effdim/ notebooks/pu_manifold/` lists only `cka.py` and `tests/test_cka.py`."
    - statement: "No constant is filled with a value in this plan — every new constant is added UNSET and to `_REQUIRED_CONSTANTS` (D8-22)."
      status: required
      verification: "`.venv/bin/python -c \"import sys; sys.path.insert(0,'notebooks'); from pu_manifold import cka; cka.assert_preregistered()\"` still exits non-zero."
---

<objective>
Expand the proven estimator into Phase 8's phase-specific machinery: the within-density-stratum
tertile split (D8-05/06/08), the tertile-difference statistic and its within-stratum label-permutation
null (D8-10/11), and the verdict rules — clearance at every `S` (D8-09), independent per-`d` reporting
(D8-13), unanimous-or-nothing seed combination (D8-15), and the four non-gating declarations
(D8-01 RBF, D8-10 middle tertile, D8-12 `d=32`, D8-20 the validation ladder).

Purpose: every one of these is pure index bookkeeping plus a call into the already-verified
estimator, so all of it is unit-testable on synthetic fields with no PU data and no decoder.

Output: `cka.py` gains its split/null/verdict layer with every new constant still UNSET;
`test_cka.py` gains the split, null, verdict and seed tests.
</objective>

<superseded_decision>
D7-07's CKA-out-of-scope scope decision is superseded by phase decision, recorded in full in
`08-01-PLAN.md`'s `<superseded_decision>` block and carried in code as `cka.SUPERSEDES` /
`cka.SUPERSESSION_RULE`. `notebooks/pu_manifold/crossmodal_curvature.py` is **not** edited by this
plan or any other Phase 8 plan (D8-23). No sealed verdict is reopened, softened or recomputed.
</superseded_decision>

<artifacts_this_phase_produces>
## Artifacts this phase produces

The full phase-level artifact table is in `08-01-PLAN.md`. New symbols added by **this plan**, all
in `notebooks/pu_manifold/cka.py` and `notebooks/pu_manifold/tests/test_cka.py`:

- Functions: `tertile_split_within_strata`, `realized_h_contrast`, `tertile_gap_panel`,
  `stratified_tertile_label_null`, `null_threshold`, `per_d_verdict`, `combine_seed_verdicts`,
  `pooled_field_guard`
- Constants (declared UNSET here, filled at the 08-04 freeze): `S_GRID`, `N_TERTILES`, `D_SWEEP`,
  `SEED_FIELD_D`, `TORCH_INIT_SEEDS`, `N_PERMUTATIONS`, `PERMUTATION_SEED`,
  `NULL_QUANTILE_PER_TAIL`, `NULL_KERNELS`, `DENSITY_K`, `DENSITY_FIELD_D`, `DENSITY_INPUT`,
  `DENSITY_SIGN_CONVENTION`, `STRATIFICATION_RULE`, `SENSITIVITY_GRID_RULE`,
  `TERTILE_STATISTIC_RULE`, `NULL_CONSTRUCTION_RULE`, `VERDICT_RULE`, `SEED_HANDLING_RULE`,
  `SEED_VERDICT_COMBINATION_RULE`, `MIDDLE_TERTILE_IS_NON_GATING`, `D32_IS_NON_GATING`,
  `VALIDATION_LADDER_IS_NON_GATING`
- Test functions: `test_tertile_within_stratum_split`, `test_tertile_split_density_marginals_match`,
  `test_tertile_split_equal_n_at_every_s`, `test_stratified_tertile_null_preserves_sizes`,
  `test_null_panel_has_one_array_per_kernel`, `test_verdict_requires_clearance_at_every_s`,
  `test_middle_tertile_does_not_gate`, `test_per_d_verdicts_are_independent`,
  `test_combine_seed_verdicts_requires_exactly_three`, `test_seed_pooling_raises`
- Verdict string outcomes (new vocabulary): `"CLEARS AT EVERY S"`, `"DOES NOT CLEAR"`,
  `"CLEARS IN ALL THREE SEEDS"`, `"NO CLEARANCE IN ANY SEED"`, `"SPLIT ACROSS SEEDS"`
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-CONTEXT.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-RESEARCH.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-PATTERNS.md
@.planning/phases/08-curvature-conditioned-cka-alignment/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Within-density-stratum tertile split and the realized-contrast diagnostic</name>

  <read_first>
    - `notebooks/pu_manifold/cka.py` — the module from 08-01: existing constants block, `_REQUIRED_CONSTANTS`, `assert_preregistered`, `cka_on_subset`
    - `notebooks/pu_manifold/density_stratified_null.py` lines 60-100 and 460-520 — `STRATIFICATION_RULE`, `SENSITIVITY_GRID_RULE`, and `density_strata`'s exact signature, stable-sort rank binning, remainder-to-last-stratum rule and 3-point floor `ValueError`
    - `notebooks/pu_manifold/crossmodal_curvature.py` lines 145-170 — `DENSITY_K = 30`, `DENSITY_FIELD_D = 20`, `DENSITY_INPUT = "legacysurvey_ambient_768"`, `DENSITY_SIGN_CONVENTION`, `DIAGNOSTICS_ARE_NON_GATING`
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-CONTEXT.md` §"Split and Density Matching" (D8-05, D8-06, D8-07, D8-08) and §"Specific Ideas"
  </read_first>

  <files>notebooks/pu_manifold/cka.py, notebooks/pu_manifold/tests/test_cka.py</files>

  <behavior>
    - `tertile_split_within_strata(h, strata)` on n=900 with 3 strata returns three index arrays covering all 900 indices exactly once.
    - Each returned subset draws the same number of points from every stratum, so the three subsets' stratum-count vectors are identical up to each stratum's `n_s % 3` remainder.
    - Subset sizes differ by at most `len(np.unique(strata))` points from each other, at S = 10, 20 and 50 on n=10,000 synthetic input.
    - When `h` is perfectly rank-correlated with an artificial signal, tertile 3 holds the highest-`h` third within every stratum, not globally.
    - `realized_h_contrast(h, tertiles)` returns the tertile-3 median over the tertile-1 median and is strictly greater than 1.0 whenever `h` is non-constant.
    - `tertile_split_within_strata` raises `ValueError` when `h` and `strata` have different lengths, or when any stratum holds fewer than 3 points.
  </behavior>

  <action>
Add to `cka.py`, keeping every new constant UNSET and appending each new name to `_REQUIRED_CONSTANTS`
with a matching branch in `assert_preregistered`:

Constants (UNSET here): `S_GRID = ()`, `N_TERTILES = None`, `DENSITY_K = None`,
`DENSITY_FIELD_D = None`, `DENSITY_INPUT = ""`, `DENSITY_SIGN_CONVENTION = ""`,
`STRATIFICATION_RULE = ""`, `SENSITIVITY_GRID_RULE = ""`. Each gets the "value plus prose-rule
string naming what it may and may not do" pairing that `density_stratified_null.py` lines 61-95 use.
`SENSITIVITY_GRID_RULE`'s prose must state, per D8-08/D8-09, that `S_GRID` is a grid of THRESHOLDS
and that there is **no headline `S`** — clearance is required at every grid point, and an
`S`-dependent gap is self-reporting as an artifact rather than something a reader has to notice.
`DENSITY_SIGN_CONVENTION`'s prose restates D8-07: `curvature_probe.local_density_weights` returns the
per-point INVERSE density `w`, mean-normalized to 1, and the density used throughout is the relative
density `1.0 / w`, matching Phase 4's REGN-01 printed convention so Phase 4 / 7 / 07.1 / 8 numbers
stay comparable. `STRATIFICATION_RULE`'s prose must additionally record D8-06's semantic consequence:
the tertiles rank **density-residualized curvature**, not raw `||H||`.

`tertile_split_within_strata(h, strata) -> Tuple[np.ndarray, np.ndarray, np.ndarray]`: for each
unique stratum id, rank that stratum's points by `h` using a stable argsort, cut into three
contiguous rank blocks of size `n_s // 3` with the `n_s % 3` remainder going to the last block —
the same remainder-to-last convention `density_strata` itself uses, so the two binning rules agree
rather than each inventing their own. Pool the per-stratum blocks across strata into three global
index arrays and return them sorted ascending. Validate first: raise `ValueError` on length
mismatch, on non-finite `h`, and on any stratum holding fewer than 3 points, naming the offending
stratum and its size in the message.

`realized_h_contrast(h, tertiles) -> float`: the median of `h` over tertile 3 divided by the median
over tertile 1. This is D8-21's mandatory "realized `||H||` contrast per `S`" row and the number that
makes D8-18's planted effect calibratable against PU's measured ~1.5x spread. Return it, do not gate
on it.

Import `density_strata` from `density_stratified_null` as a pure function only. Do not import any
gating value from it: `S_GRID` is Phase 8's own fresh literal even where a value coincides with
07.1's, per the freeze-boundary rule `density_stratified_null.py`'s own docstring states.

Add the matching tests to `test_cka.py` — `test_tertile_within_stratum_split`,
`test_tertile_split_density_marginals_match`, `test_tertile_split_equal_n_at_every_s` — built on
synthetic `h` and synthetic density from a fixed RNG seed. No PU data, nothing read from
`notebooks/.cache/`.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_cka.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_cka.py::test_tertile_within_stratum_split notebooks/pu_manifold/tests/test_cka.py::test_tertile_split_density_marginals_match notebooks/pu_manifold/tests/test_cka.py::test_tertile_split_equal_n_at_every_s -q` exits 0.
    - `test_tertile_split_equal_n_at_every_s` runs at S values 10, 20 and 50 on n=10,000 and asserts the max-minus-min subset size is at most the number of strata.
    - `test_tertile_split_density_marginals_match` asserts the three subsets' per-stratum count vectors are equal up to each stratum's remainder, using `np.bincount` on the stratum ids of each subset.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import cka; print(len(cka._REQUIRED_CONSTANTS))"` prints 22.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import cka; cka.assert_preregistered()"` exits non-zero.
    - `grep -n "notebooks/.cache\|np.load" notebooks/pu_manifold/cka.py` prints nothing — the module still performs no file I/O.
  </acceptance_criteria>

  <done>The within-stratum tertile split produces three disjoint, density-marginal-matched, equal-n subsets at every `S` in the grid, and the realized-contrast diagnostic is available for D8-21's reporting block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tertile-difference panel and the within-stratum label-permutation null</name>

  <read_first>
    - `notebooks/pu_manifold/cka.py` — Task 1's split functions and the 08-01 estimator
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-RESEARCH.md` §"Code Examples" → "Within-stratum tertile-label permutation null (D8-11 shape, new to this phase)" and §"Common Pitfalls" → Pitfall 2 and Pitfall 3
    - `notebooks/pu_manifold/density_stratified_null.py` — `stratified_partial_null`'s per-stratum `rng.permutation(idx)` idiom and its precompute-outside-the-loop optimization
    - `notebooks/pu_manifold/mknn.py` — the pairing-shuffle null that D8-11 explicitly does NOT use, read so the wrong function is not reached for by habit
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-CONTEXT.md` §"Null and Verdict Rule" (D8-10, D8-11)
  </read_first>

  <files>notebooks/pu_manifold/cka.py, notebooks/pu_manifold/tests/test_cka.py</files>

  <behavior>
    - `tertile_gap_panel(K_full, L_full, tertiles)` returns, per kernel key, the CKA of all three tertiles plus the tertile-3-minus-tertile-1 gap.
    - The middle tertile's value is present in the panel but is not an input to any verdict function.
    - `stratified_tertile_label_null(h, strata, K_full, L_full, n_resamples, seed)` returns a dict keyed by the same kernel names, each holding an array of length `n_resamples`.
    - On every resample the permuted split's three tertile sizes equal the observed split's three tertile sizes exactly, and every point's stratum membership is unchanged.
    - Two calls with the same seed produce bit-identical null arrays; different seeds produce different ones.
    - The null loop performs no kernel-value recomputation: passing a Gram matrix pair that would raise if re-derived still succeeds.
  </behavior>

  <action>
Add to `cka.py`, keeping every new constant UNSET and registered in `_REQUIRED_CONSTANTS`:

Constants (UNSET here): `N_PERMUTATIONS = None`, `PERMUTATION_SEED = None`,
`NULL_QUANTILE_PER_TAIL = None`, `NULL_KERNELS = ()`, `TERTILE_STATISTIC_RULE = ""`,
`NULL_CONSTRUCTION_RULE = ""`, `MIDDLE_TERTILE_IS_NON_GATING = None`.
`TERTILE_STATISTIC_RULE`'s prose states D8-10: the statistic is CKA(tertile 3) minus CKA(tertile 1);
the middle tertile is printed beside it as a shape diagnostic and gates nothing; monotone-trend and
Phase-6-style compound criteria were considered and rejected, because Phase 6 died on a monotonicity
criterion while its other two criteria held, and at three buckets a trend statistic is near-powerless.
`NULL_CONSTRUCTION_RULE`'s prose states D8-11: the null permutes `||H||` tertile LABELS within density
strata and recomputes the entire three-subset panel, preserving density structure and subset sizes
exactly while breaking only the curvature link; it explicitly is **not** the crossmodal row-pairing
shuffle, which nulls alignment itself — a question Phase 7 already settled — and it is not a
bootstrap CI, whose bias on a nonlinear whole-subset statistic is uncharacterized and has no
precedent in this record.

`tertile_gap_panel(K_full, L_full, tertiles) -> Dict[str, Dict[str, float]]`: `K_full` and `L_full`
are dicts keyed by kernel name mapping to the full `(n, n)` Gram matrices built once outside this
function. For each kernel key present in both, compute `cka_on_subset` for tertile 1, tertile 2 and
tertile 3 and the gap `cka_3 - cka_1`. Return per kernel a dict with keys `cka_t1`, `cka_t2`,
`cka_t3`, `gap`. Raise `ValueError` when the two dicts' key sets differ.

`stratified_tertile_label_null(h, strata, K_full, L_full, n_resamples, seed) -> Dict[str, np.ndarray]`:
follow RESEARCH.md's code example shape. Build `strat_indices` once outside the loop. Precompute all
`n_resamples` within-stratum permutation index sets before entering the CKA-recomputation loop, so the
loop body is pure array indexing plus HSIC arithmetic with no further RNG calls — the equivalent of
the precompute-outside-the-loop optimization `stratified_partial_null` already applies at the rank
level (RESEARCH.md §Runtime). Per resample: permute `h` within each stratum, rebuild the tertile index
sets with `tertile_split_within_strata`, and record `cka_3 - cka_1` per kernel. Return one float array
per kernel. Never rebuild a Gram matrix inside this function — that is RESEARCH.md Pitfall 3 and it is
the difference between minutes and an intractable run.

`null_threshold(null_array, quantile_per_tail) -> Tuple[float, float]`: the two-tailed thresholds,
the `1 - quantile_per_tail` and `quantile_per_tail` empirical quantiles of the null array. Two-tailed
is inherited from Phase 7's own two-tailed permutation wrapper — see `<discretion_decisions>`.

Add tests `test_stratified_tertile_null_preserves_sizes`,
`test_null_panel_has_one_array_per_kernel`, `test_null_is_seed_reproducible` and
`test_null_does_not_rebuild_grams` to `test_cka.py`, all on synthetic Gram matrices from a fixed RNG
seed at n=600 with `n_resamples=25` so the suite stays under the 15-second feedback budget.
`test_null_does_not_rebuild_grams` passes Gram matrices whose backing feature matrices are not
available, proving the loop cannot be re-deriving them.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_cka.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_cka.py::test_stratified_tertile_null_preserves_sizes -q` exits 0 and the test asserts, for all 25 resamples, that the three permuted tertile sizes equal the observed tertile sizes elementwise.
    - `test_null_is_seed_reproducible` asserts two calls at the same seed give arrays equal under `np.array_equal`, and that a different seed gives a different array.
    - `grep -n "permutation_type" notebooks/pu_manifold/cka.py` prints nothing.
    - `grep -n "def linear_gram\|def rbf_gram" notebooks/pu_manifold/cka.py | wc -l` prints 2, and neither name appears inside the body of `stratified_tertile_label_null` — verified by `.venv/bin/python -c "import sys,inspect; sys.path.insert(0,'notebooks'); from pu_manifold import cka; src=inspect.getsource(cka.stratified_tertile_label_null); assert 'linear_gram' not in src and 'rbf_gram' not in src"` exiting 0.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_cka.py -q` completes in under 60 seconds.
  </acceptance_criteria>

  <done>The tertile-difference panel and the within-stratum label-permutation null run against precomputed Gram matrices, preserve stratum and tertile sizes exactly, and are seed-reproducible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verdict rules — clearance at every S, per-d independence, seed unanimity, and the four non-gating declarations</name>

  <read_first>
    - `notebooks/pu_manifold/cka.py` — Tasks 1 and 2's additions
    - `notebooks/pu_manifold/linear_probe.py` lines 249-383 and 831-887 — `assert_preregistered`'s exact-string-equality guard on a rule constant encoding a ratified decision, and `combine_seed_verdicts`'s exactly-three / count-clearances / one-or-two-is-terminal branching
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-03-DECISION.md` — the ratified one-way do-not-pool-seeds constraint D8-15 carries verbatim
    - `.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md` §1 and §2 — D-14's per-`d` independence rule and the per-`d` clearance table Phase 8's must sit beside row-for-row
    - `.planning/phases/08-curvature-conditioned-cka-alignment/08-CONTEXT.md` §"Null and Verdict Rule" (D8-09, D8-12, D8-13, D8-15) and §"Validation Ladder" (D8-20)
  </read_first>

  <files>notebooks/pu_manifold/cka.py, notebooks/pu_manifold/tests/test_cka.py</files>

  <behavior>
    - `per_d_verdict` given per-`S` gaps and per-`S` null thresholds returns a clearance only when every `S` clears; one non-clearing `S` yields the non-clearance outcome.
    - `per_d_verdict`'s return carries the per-`S` detail so a reader sees which `S` failed, not only the terminal string.
    - Perturbing the middle tertile's CKA in the panel leaves `per_d_verdict`'s output unchanged.
    - `combine_seed_verdicts` raises `ValueError` for two or four seed entries, returns the all-three outcome for three clearances, the none outcome for zero, and the terminal split outcome for one or two.
    - `combine_seed_verdicts` raises `RuntimeError` when its rule string is empty — it cannot run before the freeze.
    - `pooled_field_guard` raises `RuntimeError` naming `05-03-DECISION.md` whenever a caller supplies more than one seed field to be combined into a single field.
    - `assert_preregistered` rejects a `SEED_HANDLING_RULE` that is any string other than the exact ratified value.
  </behavior>

  <action>
Add to `cka.py`, keeping every new constant UNSET and registered in `_REQUIRED_CONSTANTS`:

Constants (UNSET here): `D_SWEEP = ()`, `SEED_FIELD_D = None`, `TORCH_INIT_SEEDS = ()`,
`VERDICT_RULE = ""`, `SEED_HANDLING_RULE = ""`, `SEED_VERDICT_COMBINATION_RULE = ""`,
`D32_IS_NON_GATING = None`, `VALIDATION_LADDER_IS_NON_GATING = None`.

`VERDICT_RULE`'s prose states D8-09 in full: there is NO headline `S`; the verdict fires only if the
gap clears its two-tailed null at EVERY point in `S_GRID`; relaxing this after seeing an `S`-dependent
result is exactly the post-hoc retuning the `k*=15` and `02.2` pre-registrations exist to prevent. It
must also name `S_GRID` by name, so `assert_preregistered` can check that the rule string mentions the
constant it governs — the pattern `linear_probe.assert_preregistered` uses on `VERDICT_RULE` /
`N_BUCKETS`. `SEED_HANDLING_RULE` must be guarded by **exact string equality**, not truthiness, to the
ratified value carrying the never-pool constraint, so a future edit that reintroduces pooling fails
loudly — `linear_probe.py`'s own `SEED_HANDLING_RULE != "no_pooling_per_seed_verdicts"` check is the
shape to copy. `D32_IS_NON_GATING`'s prose states D8-12: `d=32` is a REPORTED DIAGNOSTIC that gates
nothing and is NOT a hard invalidator; a hard-invalidator reading was offered and explicitly declined
by the developer on 2026-08-27; the consequence is that the artifact judgement rests on the reader,
which makes D8-21's reporting obligations load-bearing. `VALIDATION_LADDER_IS_NON_GATING`'s prose
states D8-20: all three rungs run and are reported beside the verdict, and none of them gates it; a
hard-gate ordering was offered and explicitly declined on 2026-08-27; Phase 8 therefore has no
structural mechanism preventing an artifact from being written up as a result, and every safeguard is
a reporting obligation discharged by D8-21.

`per_d_verdict(gaps_by_s, thresholds_by_s, rule) -> Dict[str, Any]`: raise `RuntimeError` when `rule`
is empty — it cannot run before the freeze, mirroring `combine_seed_verdicts`'s own guard. Raise
`ValueError` when the two mappings' `S` key sets differ or when either is missing a value in
`S_GRID`. Return a dict with `verdict` (the clears-at-every-S outcome string or the does-not-clear
one), `per_s` (a mapping from each `S` to its gap, its two thresholds and a boolean clearance), and
`n_s_cleared`. The middle tertile's CKA is never read here.

`combine_seed_verdicts(per_seed_verdicts, rule) -> Dict[str, Any]`: copy `linear_probe.py` lines
831-887's shape exactly — raise `RuntimeError` on an empty rule, `ValueError` unless exactly three
seed entries are supplied, then count clearances: three gives the all-three outcome, zero gives the
none outcome, one or two gives the terminal split outcome. The split outcome is terminal and
non-supportive and is never upgraded by majority vote (D8-15).

`pooled_field_guard(fields) -> None`: raise `RuntimeError` naming `05-03-DECISION.md` and D8-15 if
called with more than one seed field, so any future pooled-mode equivalent fails at the first call
rather than silently averaging. Each seed gets its own within-stratum tertile split and its own
verdict.

Add tests `test_verdict_requires_clearance_at_every_s`, `test_middle_tertile_does_not_gate`,
`test_per_d_verdicts_are_independent`, `test_combine_seed_verdicts_requires_exactly_three`,
`test_seed_pooling_raises`, and extend the parametrized `_REQUIRED_CONSTANTS` sweep to the new
constants. `test_per_d_verdicts_are_independent` asserts that computing a verdict for one `d` does not
read or mutate another `d`'s inputs, mirroring 07.1's D-14: a null at one `d` does not silently void
another, and no pooled headline is invented.
  </action>

  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_cka.py -x -q</automated>
  </verify>

  <acceptance_criteria>
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_cka.py -x -q` exits 0 with at least 20 collected test functions.
    - `test_verdict_requires_clearance_at_every_s` includes a case where two of three `S` values clear and asserts the returned verdict is the does-not-clear outcome, with `n_s_cleared` equal to 2.
    - `test_seed_pooling_raises` asserts `pytest.raises(RuntimeError, match="05-03-DECISION")`.
    - `test_combine_seed_verdicts_requires_exactly_three` covers dict sizes 2 and 4 raising `ValueError`, and clearance counts 0, 1, 2 and 3 returning the four distinct outcome strings.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import cka; print(len(cka._REQUIRED_CONSTANTS))"` prints 37.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import cka; cka.assert_preregistered()"` exits non-zero — every constant is still UNSET.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0.
  </acceptance_criteria>

  <done>Every verdict rule Phase 8 will read is implemented, guarded and tested: clearance at every `S`, independent per-`d` reporting, unanimous-or-nothing seed combination with a raising pooled-field guard, and the four non-gating declarations carried as checkable constants.</done>

  <reversibility rating="costly">These rule strings become pre-registration constants at 08-04. Changing a verdict rule after the freeze costs a new pre-registration and a full re-run; changing one now costs only an edit.</reversibility>
</task>

</tasks>

<discretion_decisions>
Decided here under `08-CONTEXT.md` §"Claude's Discretion", with reasons. Values are frozen at 08-04.

- **Two-tailed, `NULL_QUANTILE_PER_TAIL = 0.975`.** Inherits Phase 7's two-tailed permutation wrapper,
  which `08-CONTEXT.md` names as the default unless the record argues otherwise. It does not: a
  one-tailed test would make a **negative** gap — CKA higher in the LOW-`||H||` tertile — silently
  unreportable, and a negative gap is a real possible finding here, not a nuisance.
- **`N_PERMUTATIONS = 1000`, `PERMUTATION_SEED = 20260827`.** 1000 inherits Phase 7/07.1's own
  convention (RESEARCH.md A1); the seed is a fresh date-stamped literal in 07.1's
  `PERMUTATION_SEED = 20260825` idiom, re-declared rather than imported across the freeze boundary.
- **`NULL_KERNELS = ("linear", "rbf_sigma")`.** The permutation null runs on the headline linear
  kernel and on RBF at `sigma` only, not at the 0.5x and 2x rungs. Reason: D8-04 makes those rungs
  non-gating diagnostics, so a null on them would gate nothing, and RESEARCH.md A2 costs the
  difference at roughly 2x total runtime for no verdict content. The 0.5x and 2x gaps are still
  computed and still printed in D8-21's frozen block — as point statistics beside the headline, which
  is exactly what a non-gating diagnostic is.
- **`S_GRID = (10, 20, 50)`.** Inherits 07.1's grid VALUES as fresh Phase 8 literals — not
  `N_STRATA_HEADLINE`, which D8-08 explicitly declines to inherit as a gating constant, and not a
  headline `S`, which D8-09 forbids. Reason to reuse the values: this is the grid already exercised
  against this exact density field, so Phase 8's rows sit beside 07.1's without translation; and at
  n=10,000 all three clear `density_strata`'s 3-point floor with room (n//S = 1000, 500, 200). No
  justification here rests on sample size — pooled subset size is ~3,333 per tertile at any `S`;
  `S` trades density-match tightness against realized `||H||` contrast, which is why
  `realized_h_contrast` is reported per `S`.
- **`D_SWEEP = (20, 25, 32)`, `SEED_FIELD_D = 25`, `TORCH_INIT_SEEDS = (0, 1, 2)` — full cross
  product with `S_GRID`, a departure from RESEARCH.md Open Question 1's recommendation, stated with
  its reason.** RESEARCH recommended restricting the seed axis to a headline `S` to save runtime.
  **D8-09 leaves no headline `S` to restrict to**: a seed verdict measured at a single `S` cannot be
  evaluated under a rule that requires clearance at every `S`. So all six field cells
  (`h_norm_20`, `h_norm_25`, `h_norm_32`, `h_norm_25_seed0/1/2`) run at all three `S` values —
  18 cells. RESEARCH's own cost model puts the whole sweep well under an hour, so the saving was not
  worth breaking the verdict rule for.
</discretion_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → `cka.py` constants | A post-hoc edit to a verdict or null constant is this plan's central integrity risk (D8-22) |
| verdict function → reader | A verdict that silently reads a non-gating quantity (middle tertile, `d=32`, ladder) would smuggle a gate past the developer's explicit declining of one |
| import graph → sealed modules | `density_strata` is imported as a pure function; no gating value crosses the freeze boundary |

No network, no authentication, no user input, no external service. The integrity risks are
scientific-integrity risks.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-09 | Tampering | `SEED_HANDLING_RULE` — a future edit reintroducing seed pooling | high | mitigate | Exact-string-equality guard in `assert_preregistered` plus `pooled_field_guard` raising and `test_seed_pooling_raises` pinning it |
| T-08-10 | Tampering | `VERDICT_RULE` — relaxing clearance-at-every-`S` after seeing an `S`-dependent result | high | mitigate | `VERDICT_RULE` is frozen at 08-04 and must name `S_GRID`; `test_verdict_requires_clearance_at_every_s` fails if a partial-clearance case starts returning a clearance |
| T-08-11 | Elevation of Privilege | a non-gating quantity silently becoming a gate | high | mitigate | `MIDDLE_TERTILE_IS_NON_GATING`, `D32_IS_NON_GATING`, `VALIDATION_LADDER_IS_NON_GATING`, `RBF_IS_NON_GATING`, `SIGMA_LADDER_IS_NON_GATING` are required constants; `test_middle_tertile_does_not_gate` perturbs the middle tertile and asserts the verdict is unchanged |
| T-08-12 | Tampering | the wrong permutation null (crossmodal row-pairing shuffle) reused by habit | high | mitigate | `NULL_CONSTRUCTION_RULE` names the rejected null explicitly; negative grep for the pairing-shuffle keyword; `test_stratified_tertile_null_preserves_sizes` fails for a pairing shuffle, which does not preserve tertile membership |
| T-08-13 | Denial of Service | Gram rebuild inside the resample loop making the run intractable | medium | mitigate | `test_null_does_not_rebuild_grams` passes Gram matrices with no recoverable backing features; source assertion that the Gram builders are absent from the null's source |
| T-08-14 | Information Disclosure | none — no secrets, no network egress | low | accept | Pure in-memory numpy over synthetic arrays in this plan |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages. `08-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable — none proposed, none `[ASSUMED]`, none `[SUS]`, none `[SLOP]` |
</threat_model>

<verification>
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_cka.py -x -q` exits 0.
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0.
- `.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import cka; cka.assert_preregistered()"` exits non-zero.
- `.venv/bin/python notebooks/diagnostics/08_cka_alignment_run.py --mode selfcheck --record-path notebooks/.cache/08_scratch_tracer.jsonl` still exits 0 — the tracer path is not regressed.
- `git diff --name-only <plan-base-sha>..HEAD -- notebooks/ src/` lists only `notebooks/pu_manifold/cka.py` and `notebooks/pu_manifold/tests/test_cka.py`.
</verification>

<success_criteria>
- The within-stratum tertile split, the tertile-difference panel, the label-permutation null and all
  four verdict/seed functions exist, are guarded, and are covered by passing synthetic tests.
- Every new constant is UNSET and registered in `_REQUIRED_CONSTANTS`; `assert_preregistered()` still
  raises; no PU number exists.
- Nothing in the module performs file I/O, imports a gating value across the freeze boundary, or
  reads a non-gating quantity into a verdict.
</success_criteria>

<output>
Create `.planning/phases/08-curvature-conditioned-cka-alignment/08-02-SUMMARY.md` when done.
Record in it: the final `len(_REQUIRED_CONSTANTS)`, the four verdict outcome strings as written, and
the measured wallclock of one 25-resample null at n=600 so 08-05 can extrapolate the real run.
</output>
