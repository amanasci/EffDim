---
phase: 07.1-density-stratified-null-and-seed-stability
plan: 05
type: execute
wave: 4
depends_on: ["07.1-04"]
files_modified:
  - notebooks/diagnostics/07.1_density_stratified_null_run.py
  - notebooks/pu_manifold/tests/test_density_stratified_null.py
autonomous: false
requirements: [D7.1-02]
user_setup: []

must_haves:
  truths:
    - "D7.1-02 is answered: three `d=25` decoder fits at three `TORCH_INIT_SEED` values, each gated on D7.1-01's stratified partial, combined by the frozen unanimity rule."
    - "`crossmodal_curvature.split_indices` returns bit-identical train and holdout index arrays across all three runs, because `SPLIT_SEED` is held fixed while only `TORCH_INIT_SEED` varies (D-09) — asserted in the runner and pinned by a test, so the three fits are compared on identical training data."
    - "Across the three runs the ONLY quantity that changes is `h_norm`: MKNN is computed between the two ambient embeddings and density on the ambient `legacysurvey` array, neither of which passes through the autoencoder, so one stratification serves all three seeds and no per-seed edge array exists (D-10)."
    - "`PERMUTATION_SEED` is identical across the three nulls, so any difference between the three thresholds comes from the curvature field alone (D-13)."
    - "Each seed's gating decision is the stratified partial at `N_STRATA_HEADLINE`; the raw statistic is recorded beside it and gates nothing (D-12)."
    - "A 2-of-3 clearance pattern returns `SPLIT ACROSS SEEDS`, a complete terminal non-supportive outcome, and the supportive value is returned only on 3-of-3 (D-11)."
    - "`apply_seed_verdict` raises ValueError when the per-seed mapping's keys are not exactly the frozen `TORCH_INIT_SEEDS` — a verdict from one or two completed fits is not a verdict."
    - "The three seeds' `h_norm` fields are recorded with a relative-precision distinct count and their pairwise Spearman agreements; two seeds producing an identical field would make the three-seed axis a one-seed measurement, so identical fields are reported, never silently averaged or deduplicated."
    - "The three fits run as ONE sequential in-process loop — never as concurrent processes — with `OMP_NUM_THREADS` / `MKL_NUM_THREADS` / `NUMEXPR_NUM_THREADS` capped before numpy and torch are imported (07-CONTEXT.md §7 measured three concurrent torch jobs driving load to 44 for a ~10x slowdown)."
    - "`cc.TORCH_INIT_SEED` is asserted equal to Phase 7's frozen 0 on entry, set per fit from 07.1's own frozen `TORCH_INIT_SEEDS`, and restored in a `finally` block — the sealed module's attribute is never left mutated when the run ends, however it ends."
    - "Seed 0 reproduces Phase 7's realized `d=25` fit, so its agreement against the frozen `h_norm_25` is recorded as a reproduction check; a disagreement below the stated floor halts and is reported as a finding, not absorbed."
    - statement: "Re-running `--mode seeds` with `--resume` skips every seed already recorded under a matching `preregistration_commit` and appends no duplicate row."
      verification: backstop
    - statement: "A run interrupted mid-fit leaves the record consistent: every completed seed's row is already written as one whole line, and `--resume` picks up from the first unwritten seed."
      verification: backstop
  prohibitions:
    - statement: "A 2-of-3 seed outcome must not be reported as support, as 'mostly holds', as 'holds at two of three seeds', or with the dissenting seed set aside as an outlier. It is `SPLIT ACROSS SEEDS` — a terminal non-supportive outcome, not a weakened claim — and the phase declines to claim the effect at `d=25`. This is Phase 5's ratified framing (`05-03-DECISION.md`) inherited verbatim."
      requirement: D7.1-02
  artifacts:
    - path: "notebooks/diagnostics/07.1_density_stratified_null_run.py"
      provides: "--mode seeds: three serial d=25 fits, per-seed stratified partials, unanimity verdict"
      exports: ["run_seeds", "fit_field_at_seed"]
    - path: "notebooks/pu_manifold/tests/test_density_stratified_null.py"
      provides: "split-identity, seed-mutation-restore and unanimity coverage"
  key_links:
    - from: "notebooks/diagnostics/07.1_density_stratified_null_run.py"
      to: "notebooks/diagnostics/07_crossmodal_curvature_run.py"
      via: "the d=25 fit path reuses Phase 7's fit_and_field verbatim, loaded by file path, with only cc.TORCH_INIT_SEED varying around each call"
      pattern: "fit_and_field"
    - from: "notebooks/diagnostics/07.1_density_stratified_null_run.py"
      to: "notebooks/pu_manifold/density_stratified_null.py"
      via: "the per-seed clearance mapping is passed to the frozen apply_seed_verdict"
      pattern: "apply_seed_verdict"
---

<objective>
Answer D7.1-02. Fit the `d=25` decoder three times at three `TORCH_INIT_SEED` values with the
split held fixed, gate each on D7.1-01's stratified partial (D-12), and combine by the frozen
unanimity rule (D-11).

Purpose: Phase 7 ran `SEED_HANDLING_RULE = "single_seed_across_d_sweep"`, an accepted limitation
inherited from Phase 5's measured seed instability of decoder curvature fields. `d=25` is the one
cell whose density-controlled residual (-0.06583482510693942, ~3.2x the borrowed edge) clears with
room, so it is the only cell where a seed-stability question is worth ~1.9h of compute. This plan
replaces the accepted limitation at that one cell — and nowhere else.

Output: three `d=25` curvature fields, three stratified partials, three clearance booleans, the
seed-agreement table, and the D7.1-02 verdict.

Cost: `DSWEEP_COST_MODEL_MINUTES[25] = 38`, so three fits are ~114 min of serial compute — the
entire wall-clock budget of the phase. Everything else in 07.1 is seconds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-CONTEXT.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-RESEARCH.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-PATTERNS.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-04-SUMMARY.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-03-DECISION.md
@CLAUDE.md

<interfaces>
<!-- The fit path reused verbatim, and the module attribute the runner scopes a mutation around. -->

From notebooks/diagnostics/07_crossmodal_curvature_run.py (line 132) — reused UNCHANGED:
```
def fit_and_field(X, d, max_epochs, n_rows, smoke=False) -> Dict[str, Any]
```
Its first act is `torch.manual_seed(cc.TORCH_INIT_SEED)` — a module attribute read at CALL time,
which is what lets an outside caller vary the seed without editing sealed source. It then builds
`cae.PlainAutoEncoder(in_dim, latent_dim=d, hidden=cc.AE_HIDDEN, activation=cc.AE_ACTIVATION)`,
splits with `cc.split_indices(n, cc.SPLIT_SEED, cc.HOLDOUT_FRACTION)`, trains with
`cae.train_plain_ae` under `cc.TRAIN_CFG` plus `max_epochs`, calls `model.eval().double()`,
encodes the FULL float64 array, and returns:
```
{"h_norm": (n,) float64, "metric_condition_number": (n,) float64, "var_explained": float,
 "reconstruction_stats": dict, "wallclock_fit_s": float, "wallclock_field_s": float}
```
It raises `ValueError` for a `d` outside `cc.D_SWEEP` unless `smoke=True`; `25` is in `D_SWEEP`.

Also in that file:
```
DSWEEP_COST_MODEL_MINUTES = {20: 24, 25: 38, 32: 62}      # line 87
def load_pu_pair(column_a, column_b) -> Tuple[np.ndarray, np.ndarray, str]   # line 101
```
Loading this script as a module by file path uses the same
`importlib.util.spec_from_file_location` pattern
`notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py` already uses; module-level code
only sets thread env vars and imports, and `main()` is guarded by `if __name__ == "__main__":`.

From notebooks/pu_manifold/crossmodal_curvature.py:
```
TORCH_INIT_SEED = 0 ; SPLIT_SEED = 20260813 ; HOLDOUT_FRACTION = 0.2
MAX_EPOCHS = 600 ; AE_HIDDEN = (250, 250, 250) ; AE_ACTIVATION = "silu"
def split_indices(n, split_seed, holdout_fraction) -> Tuple[np.ndarray, np.ndarray]   # line 392
```

From notebooks/pu_manifold/density_stratified_null.py (frozen at 07.1-01):
```
TORCH_INIT_SEEDS, SPLIT_SEED, SEED_COMBINATION_RULE, SEED_GATING_STATISTIC,
RAW_STATISTIC_IS_NON_GATING, N_STRATA_HEADLINE, N_PERMUTATIONS, PERMUTATION_SEED,
NULL_QUANTILE_PER_TAIL, SEED_VERDICT_VALUES, RECORD_STEM
def apply_seed_verdict(per_seed_results: Dict[int, bool], positive_control_cleared_at: Any) -> str
def stratified_partial_null(h, m, density, n_strata, n_resamples, seed, quantile_per_tail) -> Dict
```

From notebooks/.cache/07_crossmodal_curvature_fields.npz — `h_norm_25`, Phase 7's realized d=25
field, fitted at `TORCH_INIT_SEED = 0` and `SPLIT_SEED = 20260813`. Its recorded
`partial_rho_density_controlled` is -0.06583482510693942 (reproduced exactly this session).

07-CONTEXT.md §7, verbatim: three concurrent torch jobs on this 20-core machine drove load to 44
and cost roughly a 10x slowdown; run heavy jobs serially with `OMP_NUM_THREADS` capped; and
`pgrep -f <script>.py` matches the invoking shell's own command line, so kill by real PID or leave
orphaned 53-thread processes running.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build --mode seeds — three serial d=25 fits behind the split-identity assertion</name>
  <reversibility rating="costly">D-12 gates each seed on D7.1-01's stratified partial; undoing that re-orders the phase's waves and changes which deliverable can start first (rated costly in 07.1-CONTEXT.md).</reversibility>
  <precondition>notebooks/.cache/07.1_density_stratified_null.jsonl contains a row_kind "verdict" row and a row_kind "positive_control_summary" row written by 07.1-04 under a preregistration_commit equal to 07.1's freeze SHA</precondition>
  <files>notebooks/diagnostics/07.1_density_stratified_null_run.py, notebooks/pu_manifold/tests/test_density_stratified_null.py</files>
  <read_first>
    - notebooks/diagnostics/07_crossmodal_curvature_run.py — `fit_and_field` in full (lines
      132-195), `run_dsweep` lines 468-700 (the resume pattern, the cost-model print, the record
      row keys, and the sequential-loop comment)
    - notebooks/diagnostics/07.1_density_stratified_null_run.py — everything written at 07.1-03
      and 07.1-04, so the new mode reuses `recompute_mknn_and_density`, `resolve_record_path`,
      `append_record_row`, `_strict_ancestor_or_exit` rather than adding parallel copies
    - notebooks/pu_manifold/density_stratified_null.py — the frozen seed constants and
      `apply_seed_verdict`
    - notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py — the
      `importlib.util.spec_from_file_location` fixture pattern for loading a runner by path
    - .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-CONTEXT.md — D-09,
      D-10, D-11, D-12, D-13
  </read_first>
  <behavior>
    - `test_split_indices_identical_across_seeds`: calling `cc.split_indices(10000, cc.SPLIT_SEED,
      cc.HOLDOUT_FRACTION)` three times, with `cc.TORCH_INIT_SEED` set to each of the frozen
      `TORCH_INIT_SEEDS` in between, returns arrays that are elementwise equal — the training data
      is identical across the three fits (D-09).
    - `test_torch_init_seed_is_restored_after_a_failed_fit`: when the fit callable raises, the
      runner's seed-scoping helper still restores `cc.TORCH_INIT_SEED` to its entry value.
    - `test_seed_unanimity_rule`: `apply_seed_verdict` returns the supportive value only on 3-of-3
      and `SPLIT ACROSS SEEDS` on every 2-of-3 pattern (already added at 07.1-01; assert it is
      exercised here and extend it with the all-false and `positive_control_cleared_at=None`
      branches if 07.1-01 did not cover them).
    - `test_apply_seed_verdict_raises_on_partial_seed_set`: a mapping with one or two of the three
      frozen seeds raises `ValueError` naming `TORCH_INIT_SEEDS`.
    - `test_seed_record_row_shape`: a fabricated three-seed record parses, its rows appear in the
      frozen `TORCH_INIT_SEEDS` order, and each row carries `torch_init_seed`,
      `split_checksum`, `h_norm_distinct`, `partial_rho_density_controlled`,
      `partial_rho_raw`, `clears_either` and `var_explained`.
  </behavior>
  <action>
    Add `fit_field_at_seed(runner7, X_ls, seed, n_rows)` and `run_seeds(args)` to
    `notebooks/diagnostics/07.1_density_stratified_null_run.py`, and add `seeds` to `--mode`'s
    choices.

    `fit_field_at_seed` loads the Phase 7 runner as a module by file path with
    `importlib.util.spec_from_file_location` (the same mechanism the existing runner tests use) and
    calls its `fit_and_field(X_ls, d=25, max_epochs=cc.MAX_EPOCHS, n_rows=n_rows)` UNCHANGED. It
    scopes the seed mutation: read `cc.TORCH_INIT_SEED` into a local, assert it equals `0` on the
    first entry (Phase 7's frozen value — if the sealed module has drifted, halt), set it to the
    requested seed, call the fit, and restore the original in a `finally` block so the sealed
    module's attribute is never left mutated however the call ends. Do not copy, wrap or re-derive
    any part of the fit path — a second fit implementation is exactly the class of duplication
    07.1-02 is removing elsewhere in the tree.

    `run_seeds(args)`: (1) the frozen module's `assert_preregistered()`; (2)
    `_strict_ancestor_or_exit(args.freeze_commit)`; (3) `resolve_record_path` with `--resume`
    skipping any seed already recorded under a matching `preregistration_commit`; (4)
    `recompute_mknn_and_density()` ONCE and `density_strata` at `N_STRATA_HEADLINE` ONCE — MKNN is
    computed between the two ambient embeddings and density on the ambient `legacysurvey` array,
    neither of which passes through the autoencoder, and density is `d`-independent, so one
    stratification serves all three seeds (D-10). There is no per-seed edge array in this phase;
    do not build machinery for one.

    Before the loop, compute `cc.split_indices(n_rows, cc.SPLIT_SEED, cc.HOLDOUT_FRACTION)` once
    and hash both index arrays into a `split_checksum` string. Inside the loop, recompute the
    split after each seed change and assert it is elementwise identical to the pre-loop arrays,
    halting with a message naming D-09 if it ever is not.

    Then ONE sequential in-process loop over the frozen `TORCH_INIT_SEEDS`, in that order — never
    concurrent processes, never a process pool, never a background job. Before each fit print the
    projected time from `DSWEEP_COST_MODEL_MINUTES[25]` (38 min) and the elapsed total, matching
    `run_dsweep`'s progress-print shape. After each fit: compute
    `partial_spearman(h_norm, m, controls=density)` (gating, D-12) and
    `partial_spearman(h_norm, m, controls=None)` (recorded, gates nothing, D-12); build a
    `stratified_partial_null` at `N_STRATA_HEADLINE`, `N_PERMUTATIONS` and the frozen
    `PERMUTATION_SEED` — identical across all three seeds so any difference between the three
    thresholds comes from the curvature field alone (D-13); take
    `_distinct_value_count(h_norm)`; and for seed 0 additionally compute the Spearman agreement
    between this fit's `h_norm` and the frozen `h_norm_25` from
    `07_crossmodal_curvature_fields.npz`, plus the recomputed partial's difference from
    -0.06583482510693942. Append one `row_kind: "seed"` row per seed carrying every one of those
    values as plain Python scalars, written immediately after each fit completes so an interrupted
    run leaves whole lines and `--resume` can pick up.

    Save the three `h_norm` arrays into `notebooks/.cache/07.1_seed_fields_d25.npz` through the
    cache module's path helper, keyed `h_norm_25_seed{value}`, so the fields are reusable without
    a refit.

    Seed 0's reproduction check: if its Spearman agreement against the frozen `h_norm_25` falls
    below `0.99`, HALT and report it as a finding — a `d=25` refit at Phase 7's own seed and split
    that does not reproduce Phase 7's field is a real result about training determinism, not a
    tolerance to widen. Record the realized agreement whichever way it lands.

    After the loop: print the three-seed table (seed, `var_explained`, `h_norm_distinct`, observed
    partial, band edges, margin, `clears_either`, raw statistic), print the three pairwise
    Spearman agreements between the seeds' `h_norm` fields, then call the frozen
    `apply_seed_verdict` with the per-seed clearance mapping and the negative-direction
    `positive_control_cleared_at` read from 07.1-04's `positive_control_summary` row. Append a
    `row_kind: "seed_verdict"` row.

    Extend `notebooks/pu_manifold/tests/test_density_stratified_null.py` with the tests named in
    `<behavior>`. None of them fits anything — they exercise the split identity, the seed-scoping
    restore, the unanimity rule and the record shape, all in under a second.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_density_stratified_null.py -k "split_indices or seed" -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_density_stratified_null.py -k "split_indices or seed" -q` exits 0 with 0 failed.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_density_stratified_null.py::test_split_indices_identical_across_seeds -x` exits 0.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_density_stratified_null.py::test_seed_unanimity_rule -x` exits 0.
    - `.venv/bin/python notebooks/diagnostics/07.1_density_stratified_null_run.py --selfcheck` exits 0 and reports `seeds` among the available modes.
    - `.venv/bin/python notebooks/diagnostics/07.1_density_stratified_null_run.py --mode seeds` with no `--freeze-commit` exits 1.
    - `awk '/^def run_seeds/,0' notebooks/diagnostics/07.1_density_stratified_null_run.py | grep -v '^\s*#' | grep -cE 'multiprocessing|ProcessPool|ThreadPool|subprocess.Popen'` prints `0` — the three fits are one sequential in-process loop.
    - `.venv/bin/python -c "import sys;sys.path.insert(0,'notebooks');from pu_manifold import crossmodal_curvature as cc;print(cc.TORCH_INIT_SEED)"` prints `0` after the test run — the sealed module's attribute is restored, not left mutated.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>`--mode seeds` exists, refuses to run without the strict-ancestor proof, asserts the split is identical across seeds before fitting anything, and scopes every mutation of the sealed module's seed attribute — but has not yet been run at full scale.</done>
</task>

<task type="auto">
  <name>Task 2: Run the three d=25 fits serially and produce the D7.1-02 verdict</name>
  <reversibility rating="reversible">A run of already-frozen code; re-running appends nothing new under `--resume` and changes no source.</reversibility>
  <precondition>No other torch job is running on this machine — confirm with a process check that identifies the invoking shell's own command line and excludes it, per 07-CONTEXT.md §7's warning that `pgrep -f <script>.py` matches the caller</precondition>
  <files>notebooks/diagnostics/07.1_density_stratified_null_run.py</files>
  <read_first>
    - notebooks/diagnostics/07.1_density_stratified_null_run.py — `run_seeds` as written at Task 1
    - .planning/phases/07-curvature-conditioned-crossmodal-alignment/07-CONTEXT.md §7 — the cost
      model and the concurrency warning in full
    - .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-04-SUMMARY.md — the
      D7.1-01 verdict and the positive control's negative-direction smallest cleared target, both
      of which this run consumes
  </read_first>
  <action>
    Run the deliverable, ONE process, serially, with the thread cap left at its default of 8:

    `.venv/bin/python notebooks/diagnostics/07.1_density_stratified_null_run.py --mode seeds
    --freeze-commit <07.1-freeze-sha>`

    Expect roughly 114 minutes total — three fits at the cost model's 38 min for `d=25`, plus
    seconds for the MKNN, density and permutation work. Do not launch a second heavy job
    alongside it: `07-CONTEXT.md` §7 measured three concurrent torch jobs driving this machine's
    load to 44 for a ~10x slowdown. If the run must be interrupted, kill it by its real PID — a
    `pgrep -f` on the script name matches the invoking shell's own command line and will leave
    orphaned multi-thread processes behind — then resume with `--resume`, which skips every seed
    already recorded under a matching `preregistration_commit`.

    Capture the full stdout to a file and record it in the plan SUMMARY, including every
    per-fit wallclock, every `var_explained`, the three-seed table, the three pairwise field
    agreements, seed 0's reproduction agreement against the frozen `h_norm_25`, and the verdict.

    If seed 0's reproduction agreement falls below the stated floor, or if the split-identity
    assertion fires, or if any per-seed `partial_spearman` raises, HALT and report. Each of those
    is a real finding about the fit path's determinism and belongs in FINDINGS, not in a widened
    tolerance.

    Report the verdict exactly as `apply_seed_verdict` returns it. If it is `SPLIT ACROSS SEEDS`,
    that is the phase's complete answer for D7.1-02 — record it as a terminal outcome, not as a
    partial result to be qualified.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json; rows=[json.loads(l) for l in open('notebooks/.cache/07.1_density_stratified_null.jsonl')]; s=[r for r in rows if r.get('row_kind')=='seed']; v=[r for r in rows if r.get('row_kind')=='seed_verdict']; assert len(s)==3, len(s); assert len(v)==1, len(v); assert len({r['torch_init_seed'] for r in s})==3; assert len({r['split_checksum'] for r in s})==1, 'split drifted across seeds'; print('OK', v[0]['verdict'])"</automated>
  </verify>
  <acceptance_criteria>
    - The `--mode seeds` command exits 0.
    - `notebooks/.cache/07.1_density_stratified_null.jsonl` contains exactly 3 `row_kind: "seed"` rows with three distinct `torch_init_seed` values and one shared `split_checksum` across all three.
    - It contains exactly 1 `row_kind: "seed_verdict"` row whose `verdict` is a member of the frozen `SEED_VERDICT_VALUES`.
    - `notebooks/.cache/07.1_seed_fields_d25.npz` exists and carries three `h_norm_25_seed*` keys, each of shape `(10000,)`.
    - Every `seed` row carries both `partial_rho_density_controlled` (the gating value) and `partial_rho_raw` (recorded, non-gating).
    - Seed 0's row carries a `reproduction_spearman_vs_frozen_h_norm_25` value, recorded whichever way it landed.
    - `grep -c 'NaN' notebooks/.cache/07.1_density_stratified_null.jsonl` prints `0`.
    - Re-running the identical command with `--resume` exits 0 and appends zero new `seed` rows.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>Three `d=25` fits at three seeds are recorded with identical training splits, each gated on its own stratified partial, and the frozen unanimity rule has returned the D7.1-02 verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Read the three-seed table and the D7.1-02 verdict</name>
  <what-built>
    Three `d=25` decoder fits at three `TORCH_INIT_SEED` values with `SPLIT_SEED` held fixed,
    three stratified partials against the same null construction and the same permutation seed,
    the three pairwise field-agreement numbers, seed 0's reproduction check against Phase 7's
    frozen `h_norm_25`, and the D7.1-02 verdict — all in
    `notebooks/.cache/07.1_density_stratified_null.jsonl` and in the captured stdout.
  </what-built>
  <how-to-verify>
    `.venv/bin/python -c "import json;[print(json.dumps(json.loads(l))) for l in open('notebooks/.cache/07.1_density_stratified_null.jsonl') if json.loads(l).get('row_kind','').startswith('seed')]"`

    Four things to confirm:

    1. **The split really was identical.** One `split_checksum` across all three rows. If it were
       not, the three fits would not be comparable and the run would have halted.

    2. **Seed 0's reproduction.** Its `h_norm` should agree closely with Phase 7's frozen
       `h_norm_25`, and its recomputed partial should land near -0.06583482510693942. A large
       disagreement is a finding about training determinism on this machine, not a nuisance.

    3. **Field spread across seeds.** How far apart are the three pairwise Spearman agreements?
       Phase 5 measured decoder curvature fields as seed-unstable; this is the direct measurement
       of that at `d=25`. Note that the three verdicts are three fits of the same architecture on
       identical data, so their spread measures fit-to-fit field instability, not sampling
       variability.

    4. **The verdict.** 3-of-3 is `SEED STABLE AT d=25`. Anything else is `SPLIT ACROSS SEEDS` — a
       complete, terminal, non-supportive outcome, not a weakened claim, and not something to
       report as "holds at two of three." That framing was ratified at 07.1-01 Task 2 before any
       of these three numbers existed, which is what makes it credible now.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to 07.1-06, or describe issues</resume-signal>
</task>

</tasks>

<planner_contributions>
Detector outcomes are recorded once, in `07.1-01-PLAN.md`'s `<planner_contributions>` block
(API-coverage: not detected; assumption-delta: not detected, `no-change`; schema-push: no ORM
files; security: threat models present in every plan).
</planner_contributions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 07.1 runner → sealed Phase 7 module attribute | `cc.TORCH_INIT_SEED` is mutated at runtime to vary the seed; leaving it mutated would silently change any later import's behaviour |
| committed source → the scientific record | A seed number produced before the freeze commit is not covered by the pre-registration |
| a long-running fit → the machine | Concurrent torch jobs measurably degrade throughput ~10x and leave orphaned processes when killed by pattern |
| an interrupted run → the record | A partially written row would make the record unparseable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-19 | Tampering | `cc.TORCH_INIT_SEED` left mutated after a run or a raise | high | mitigate | The seed-scoping helper asserts the entry value is Phase 7's frozen 0, sets the requested seed, and restores in a `finally`; pinned by `test_torch_init_seed_is_restored_after_a_failed_fit` and by an acceptance criterion reading the attribute back after the test run |
| T-07.1-20 | Tampering | a seed number produced before the freeze commit | high | mitigate | `run_seeds` calls `assert_preregistered()` then `_strict_ancestor_or_exit` before loading any data; running without `--freeze-commit` exits 1 |
| T-07.1-21 | Repudiation | three fits compared on different training data | high | mitigate | The split is computed once, checksummed, re-asserted after every seed change, and the checksum is recorded on every row; a drift halts the run naming D-09 |
| T-07.1-22 | Denial of service | concurrent torch jobs driving load to 44 for a ~10x slowdown | medium | mitigate | One sequential in-process loop, thread caps written before numpy/torch import, a negative grep barring process pools in `run_seeds`, and a `<precondition>` requiring a no-other-torch-job check that excludes the caller's own command line |
| T-07.1-23 | Tampering | an interrupted run leaving a partial record line | medium | mitigate | `append_record_row` writes one complete `json.dumps` plus newline per row after each fit completes; `--resume` skips seeds already recorded under a matching `preregistration_commit` |
| T-07.1-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed — `07.1-RESEARCH.md` §Package Legitimacy Audit records "Not applicable"; nothing to gate |
</threat_model>

<artifacts_this_phase_produces>
Symbols created by THIS plan:

**Added to `notebooks/diagnostics/07.1_density_stratified_null_run.py`**
- `fit_field_at_seed(runner7, X_ls, seed, n_rows) -> Dict[str, Any]`
- `run_seeds(args) -> str`
- `--mode` gains the choice `seeds`

**Added to `notebooks/pu_manifold/tests/test_density_stratified_null.py`**
- `test_split_indices_identical_across_seeds`,
  `test_torch_init_seed_is_restored_after_a_failed_fit`,
  `test_apply_seed_verdict_raises_on_partial_seed_set`, `test_seed_record_row_shape`
  (and extensions to `test_seed_unanimity_rule` where 07.1-01 did not already cover a branch)

**New record rows in `notebooks/.cache/07.1_density_stratified_null.jsonl`** (gitignored)
- `row_kind` values: `"seed"` (three), `"seed_verdict"` (one)

**New cache artifact (gitignored)**
- `notebooks/.cache/07.1_seed_fields_d25.npz` with keys `h_norm_25_seed{value}` for each frozen
  `TORCH_INIT_SEEDS` entry
</artifacts_this_phase_produces>

<verification>
- Three `seed` rows and one `seed_verdict` row exist, with one shared `split_checksum`.
- The three-seed table, the pairwise agreements and seed 0's reproduction number are all in the
  SUMMARY.
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` — 0 failures.
- `cc.TORCH_INIT_SEED` reads `0` after the run.
- The blocking checkpoint is resolved and its answer recorded.
</verification>

<success_criteria>
- D7.1-02 is answered by the frozen unanimity rule over three `d=25` fits, each gated on
  D7.1-01's stratified partial and each measured against a null drawn from the same permutation
  seed (D-11, D-12, D-13).
- The three fits are provably on identical training data (D-09) and differ only in `h_norm`
  (D-10).
- Phase 7's `SEED_HANDLING_RULE` accepted limitation is replaced at `d=25` and nowhere else.
</success_criteria>

<output>
Create `.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-05-SUMMARY.md`
when done. Record the full run stdout, the three-seed table, the three pairwise field agreements,
seed 0's reproduction agreement against the frozen `h_norm_25`, the D7.1-02 verdict, and the
checkpoint's answer.
</output>
