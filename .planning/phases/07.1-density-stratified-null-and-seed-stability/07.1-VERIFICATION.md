---
phase: 07.1-density-stratified-null-and-seed-stability
verified: 2026-08-27T08:22:15Z
status: human_needed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Read 07.1-FINDINGS.md and confirm the D7.1-01 verdict (SURVIVES AT SUBSET OF d) and D7.1-02 verdict (SEED STABLE AT d=25) are scientifically sound and ready to stand as the record."
    expected: "A human reads the two verdicts, the per-d clearance table, and the three-seed table, and either ratifies them or raises a scientific objection."
    why_human: "Every blocking checkpoint:human-verify in this phase (07.1-04 Task 3, 07.1-05 Task 3, 07.1-06 Task 3) was mechanically advanced by the executor under a standing overnight 'keep working' instruction while the user was asleep. This is disclosed accurately and consistently in all three SUMMARYs, in STATE.md, and in FINDINGS.md's opening section -- the disclosure itself checks out -- but the underlying fact remains: no human has read either verdict. This is a judgment call, not a code defect; it does not block the phase's technical correctness (all must-haves below are independently verified against the codebase and the record), but the phase should not be treated as fully closed until a human ratifies the two verdicts."
  - test: "Decide whether 07.1-REVIEW.md's three open Warning-level findings (WR-01/WR-02/WR-03 in that review, about the 07.1 runner's own --resume/dedupe robustness and non-atomic npz writes) need a follow-up plan before the phase is archived."
    expected: "A human or the next planning pass decides to fix, defer, or accept these; none affects the validity of the single clean run that produced the current record (verified: exactly one positive_control_summary row, no duplicate null_grid/seed rows in the 34-row record)."
    why_human: "Judgment call on debt disposition, not a technical gap -- the record on disk shows these robustness bugs never actually fired in this run."
---

# Phase 07.1: Density-Stratified Null and Seed Stability Verification Report

**Phase Goal:** Settle whether Phase 7's curvature-MKNN association survives its density confound,
and whether the one cell that does is seed-stable.
**Verified:** 2026-08-27T08:22:15Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | The freeze contract holds: `density_stratified_null.py`'s pre-registration block is a strict git ancestor of every later phase commit, edited nowhere after, and the freeze commit contains only that one file. | VERIFIED | `git merge-base --is-ancestor 6768666 e7a9e14` exits 0. `git show 6768666 --stat` shows exactly one file (`density_stratified_null.py`, +450). `git show <c> -- density_stratified_null.py` for every later commit touching the file (`eab02a5`, `af3a1a7`, `fe75aac`, `06c943c`) shows zero removal/reassignment lines for any of `N_STRATA_HEADLINE`, `STRATA_GRID`, `N_PERMUTATIONS`, `NULL_QUANTILE_PER_TAIL`, `PERMUTATION_SEED`, `POSITIVE_CONTROL_TARGET_RHOS`, `PARTIAL_VERDICT_VALUES`, `SEED_VERDICT_VALUES`, `TORCH_INIT_SEEDS`, `D_SWEEP` — only new functions appended below the freeze banner. `test_freeze_commit_is_a_strict_ancestor_of_head` PASSED (run directly). |
| 2 | D7.1-01 is answered with the specified verdict, per-d results, and a threshold built for the partial statistic rather than borrowed from the raw one. | VERIFIED | `notebooks/.cache/07.1_density_stratified_null.jsonl` `verdict` row: `verdict='SURVIVES AT SUBSET OF d'`, `per_d_results={'20': False, '25': True, '32': False}` — byte-identical to FINDINGS.md Section 2's table (observed/edge/margin values checked to full precision for all three `d`). `apply_partial_verdict` in `density_stratified_null.py` computes this from the partial statistic's own calibrated `stratified_partial_null` band, never the raw-statistic's borrowed edge; the borrowed-vs-own-edge comparison table in FINDINGS Section 2 is present and correctly labeled "for context, not reuse." |
| 3 | D7.1-02 is answered: 3-of-3 unanimous seed stability at d=25, split held fixed across seeds, three genuinely different h_norm fields. | VERIFIED | Record's `seed_verdict` row: `verdict='SEED STABLE AT d=25'`, `per_seed_results={'0': True, '1': True, '2': True}`, `split_checksum` identical across all three `seed` rows (`e062eaa2...`). Recomputed pairwise Spearman on `notebooks/.cache/07.1_seed_fields_d25.npz`'s three `h_norm_25_seed{0,1,2}` arrays directly: 0v1=0.76267, 0v2=0.72311, 1v2=0.84641 (exact match to FINDINGS Section 5), `np.array_equal(h0,h1)=False` — a real three-seed spread, not one fit counted three times. `apply_seed_verdict` unanimity rule confirmed by reading the function (all-3-clear -> supportive value; 2-of-3 -> `SPLIT ACROSS SEEDS`). `test_seed_unanimity_rule` PASSED. |
| 4 | The D-04 positive control licensed the phase's non-detections rather than being asserted in prose only. | VERIFIED | `apply_partial_verdict`/`apply_seed_verdict` mechanically route to `UNDERPOWERED -- NO CLAIM` only on the `not any(clears)` branch when `positive_control_cleared_at is None` — read directly in `density_stratified_null.py:401-411,436-446`. Record's `positive_control_summary` row: `smallest_cleared_target_negative=0.02` (not None), matching FINDINGS Section 4's table exactly (0.01/0.015 fail, 0.02 clears in both directions). Since d=25 cleared, the UNDERPOWERED branch never fires for the overall verdict, but the gating code path is real and exercised at the unit level by `test_apply_partial_verdict_underpowered_without_positive_control` PASSED. |
| 5 | Phase 7's `ASSOCIATION DETECTED` is carried unmodified, not reopened, softened, or reinterpreted. | VERIFIED | FINDINGS.md Section 1's block-quote of `07-FINDINGS.md` (`**Answer, verbatim from the frozen record's \`verdict\` row: \`ASSOCIATION DETECTED\`.** All three \`d\`...`) is a byte-exact match against `.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-FINDINGS.md` lines 7-9. `PARTIAL_VERDICT_VALUES` is confirmed structurally distinct from Phase 7's `VERDICT_VALUES` (`SPLIT ACROSS d`, not `SURVIVES AT SUBSET OF d`) by reading `crossmodal_curvature.py:272-277`. No "not real"/"no detectable relationship" language found anywhere in FINDINGS.md (grep, zero hits). |
| 6 | CLAUDE.md's Swiss roll gate is satisfied for this phase, declared with reason and named artifact, not waived. | VERIFIED | `notebooks/02.6_swiss_roll_plainae_curvature_check.ipynb` exists (1.2MB, dated before this phase — from Phase 02.6). FINDINGS.md Section 8 names it, states the reason (07.1 reuses `cae.PlainAutoEncoder` / `decoder_curvature.plain_decoder_curvature` unchanged, confirmed by grep in `crossmodal_curvature.py`), and explicitly states it is a declaration of an already-satisfied gate, "not a waiver, an exemption, a deferral." |
| 7 | The notebook is executed end to end, outputs committed, reading from the record rather than recomputing. | VERIFIED | `notebooks/07.1_density_stratified_null.ipynb`: 9 code cells, all with outputs, sequential execution_count 1-9, no gaps. Final cell's printed output block-matches FINDINGS.md's read-out verbatim (`PASS D7.1-01 verdict recorded: 'SURVIVES AT SUBSET OF d'` etc.), and reads `verdict_row`/`seed_verdict_row`/`positive_control_summary` from the loaded record rather than recomputing. |
| 8 | Phase 7 review debt WR-01, WR-02, WR-04 closed with demonstrated no-op proofs, not merely asserted. | VERIFIED | `density_diagnostics` in `crossmodal_curvature.py:697-714` has finite/constant/length-mismatch guards. `test_density_diagnostics_guard_does_not_fire_on_phase7_arrays` PASSED (run directly). `run_smoke`'s distinct-count call (`_distinct_value_count` -> `cc._relative_precision_distinct_count`) confirmed as the single implementation, no duplicate inline logic found. `resolve_record_path`/scratch-guard tests (`test_resolve_record_path_*`, `test_scratch_guard_*`) all PASSED (6/6, run directly). |
| 9 | `src/effdim/` untouched throughout the phase (CLAUDE.md's milestone constraint). | VERIFIED | `git diff 4d2f0db..HEAD --stat -- src/effdim/` empty (independently re-run, confirms orchestrator's claim). |

**Score:** 9/9 truths verified (0 present, behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `notebooks/pu_manifold/density_stratified_null.py` | Frozen pre-registration, verdict functions, null routine, positive control | VERIFIED | Exists, substantive, wired into runner and tests (`gsd-tools verify.artifacts` all_passed for plans 01/03/04) |
| `notebooks/pu_manifold/tests/test_density_stratified_null.py` | Freeze-guard, verdict-rule, null-routine, seed-fit tests | VERIFIED | 71 tests collected; all targeted behavioral tests re-run directly PASSED |
| `notebooks/diagnostics/07.1_density_stratified_null_run.py` | Runner: smoke/positive-control/null/seeds/selfcheck modes | VERIFIED | Exists, wired to `density_stratified_null.py` and the jsonl record |
| `notebooks/pu_manifold/crossmodal_curvature.py` | WR-01 guards on `density_diagnostics` | VERIFIED | Guards present, no-op proof test passes |
| `notebooks/diagnostics/07_crossmodal_curvature_run.py` | WR-02 dedupe fix | VERIFIED | Single distinct-count implementation confirmed |
| `notebooks/pu_manifold/tests/test_crossmodal_curvature.py` / `test_crossmodal_curvature_run.py` | WR-01/WR-04 coverage | VERIFIED | Relevant tests re-run directly, all PASSED |
| `notebooks/.cache/07.1_density_stratified_null.jsonl` (gitignored) | 34-row record | VERIFIED (present on disk) | 34 rows, row_kind counts match FINDINGS' claimed breakdown exactly; verdict/seed_verdict rows byte-match FINDINGS |
| `notebooks/.cache/07.1_seed_fields_d25.npz` (gitignored) | Three seeds' h_norm fields | VERIFIED (present on disk) | Recomputed pairwise Spearman from this file matches FINDINGS Section 5 exactly |
| `.planning/phases/07.1-.../07.1-FINDINGS.md` | Both verdicts, tables, gate declaration, limitations | VERIFIED | 373 lines, all numbers cross-checked against the record file directly, not transcribed on trust |
| `notebooks/07.1_density_stratified_null.ipynb` | Executed notebook, outputs committed | VERIFIED | 9/9 code cells have outputs, sequential execution |

### Key Link Verification

All key links declared across all 6 plans' frontmatter verified via `gsd-tools query verify.key-links` (12/12 links across plans 01-06, all `verified: true`, "Pattern found in source"). No orphaned or unwired artifacts found.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Freeze commit is a strict git ancestor of HEAD | `pytest ...::test_freeze_commit_is_a_strict_ancestor_of_head` | PASSED | PASS |
| `TORCH_INIT_SEED` restored after a failed fit | `pytest ...::test_torch_init_seed_is_restored_after_a_failed_fit` | PASSED | PASS |
| Fit halts if entry seed has drifted | `pytest ...::test_fit_field_at_seed_halts_if_entry_seed_has_drifted` | PASSED | PASS |
| Null calibrated on a true-null fixture with a real density confound | `pytest ...::test_null_calibration_on_true_null_fixture` | PASSED | PASS |
| Recomputed d=20 partial matches Phase 7's frozen record (D-07) | `pytest ...::test_recomputed_partial_matches_frozen_record` | PASSED | PASS |
| `split_indices` identical across the three seed fits | `pytest ...::test_split_indices_identical_across_seeds` | PASSED | PASS |
| WR-01 guard does not fire on Phase 7's real d=20/25/32 arrays | `pytest ...::test_density_diagnostics_guard_does_not_fire_on_phase7_arrays` | PASSED | PASS |
| WR-04 `resolve_record_path` / scratch-guard coverage | `pytest ...::test_resolve_record_path_* / test_scratch_guard_*` (6 tests) | 6/6 PASSED | PASS |
| Record row counts match FINDINGS' claimed breakdown | direct jsonl parse | Counter matches exactly (18/9/3/1/1/1/1) | PASS |
| Pairwise seed h_norm Spearman matches FINDINGS Section 5 | direct npz parse + scipy.spearmanr | Exact match (0.76267/0.72311/0.84641) | PASS |
| Notebook read-out matches FINDINGS' read-out sentence | direct ipynb parse | Byte-match | PASS |

Full workspace test suite not re-run in this verification session beyond the targeted named tests above (orchestrator already confirmed 671 passed / 1 skipped / 0 failed for `notebooks/pu_manifold/tests/`, and 120 passed for `tests/`; all behavior-critical named tests for this phase's own must-haves were independently re-run here and passed).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| D7.1-01 | 07.1-01, 07.1-02, 07.1-03, 07.1-04, 07.1-06 | Density-stratified null for the partial | SATISFIED | Verdict `SURVIVES AT SUBSET OF d` recorded, reported, code-backed |
| D7.1-02 | 07.1-01, 07.1-05, 07.1-06 | Seed stability at d=25 | SATISFIED | Verdict `SEED STABLE AT d=25` recorded, reported, code-backed |

**Finding — requirement IDs not present in REQUIREMENTS.md (process gap, not a goal-achievement gap).** `D7.1-01` and `D7.1-02` are defined and used consistently across `ROADMAP.md` (Phase 07.1's goal section), every plan's frontmatter `requirements:` field, and `07.1-FINDINGS.md`'s `requirements-completed` — but `.planning/REQUIREMENTS.md` contains no `D7.1-*` entries anywhere (grepped, zero hits), unlike the comparable Phase 02.1 (INSERTED, added `GEOM-01..05` to REQUIREMENTS.md) and the Phase 3/Phase 4 requirement re-mints (each documented with a "Requirement Re-Mint" section). This is a documentation-hygiene gap in the milestone's requirement traceability file, not a gap in the phase's own technical delivery — ROADMAP.md is unambiguous about what D7.1-01/D7.1-02 mean and both are demonstrably answered. Flagged for the developer's awareness; does not block this phase.

### Anti-Patterns Found

No debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER) found in any of the 7 phase-modified source/test files (grepped directly). One string match on "not available this run" in the runner is a legitimate defensive branch for a partial `--resume` state (verified in context, not a stub). No empty implementations, no hardcoded-empty stub returns found in the reviewed files.

**Carried from `07.1-REVIEW.md` (filed by this phase, status: `issues_found`, 3 warnings, 0 critical):** WR-01 (no `--resume`/dedupe logic on `run_positive_control`), WR-02 (inconsistent dedupe gating between per-cell and summary/verdict rows in `run_null`/`run_seeds`), WR-03 (non-atomic `np.savez` rewrite of the multi-seed npz on every iteration) — all about the 07.1 runner's own re-run/crash robustness, not about the correctness of the single run that actually produced the current record. Verified directly: the record file has exactly one `positive_control_summary` row and no duplicate `null_grid`/`seed` rows, confirming these bugs did not fire in this run. These remain open technical debt in the review file at time of this verification.

### Human Verification Required

1. **Read and ratify the two verdicts.**
   **Test:** A human reads `07.1-FINDINGS.md`'s D7.1-01 and D7.1-02 verdicts, the per-d table, and the three-seed table.
   **Expected:** Either ratification or a raised scientific objection.
   **Why human:** Every blocking `checkpoint:human-verify` in this phase (07.1-04, 07.1-05, 07.1-06) was mechanically advanced by the executor under a standing overnight "keep working" instruction. This disclosure is accurate, present, and consistent everywhere it should be (all three SUMMARYs, STATE.md, FINDINGS.md's own opening section) — that consistency is itself verified — but the underlying fact that no human has read either verdict remains. Every number checked in this verification is real, measured, and internally consistent; what is unverified is the human judgment call the checkpoints exist to gate.

2. **Decide disposition of `07.1-REVIEW.md`'s 3 open warnings.**
   **Test:** Fix, defer, or explicitly accept WR-01/WR-02/WR-03 (re-run robustness of the 07.1 runner).
   **Expected:** A decision recorded before the phase is archived, or a follow-up plan opened.
   **Why human:** Judgment call on debt priority; verified not to have affected the current record's validity.

### Gaps Summary

No technical gaps. All 9 derived observable truths are VERIFIED against the codebase and the record file directly (not from SUMMARY claims). The freeze contract, both verdicts, the positive-control gating mechanism, the Swiss roll gate declaration, the notebook execution, and the Phase 7 review-debt closures all check out on independent re-derivation — record numbers were parsed directly from the jsonl/npz files and cross-checked against FINDINGS.md's prose to full precision, not trusted on the SUMMARY's word. The phase does not resolve to `passed` only because two items require human judgment: (1) no human has yet ratified either verdict — a fact the phase's own record honestly discloses rather than hides, and (2) three open code-review warnings about the runner's own re-run robustness remain undisposed. Neither is a defect in the delivered science.

---

*Verified: 2026-08-27T08:22:15Z*
*Verifier: Claude (gsd-verifier)*
