---
status: testing
phase: 07.1-density-stratified-null-and-seed-stability
source: [07.1-VERIFICATION.md]
started: 2026-08-27T08:30:00Z
updated: 2026-08-27T08:30:00Z
---

## Current Test

number: 1
name: Read and ratify the two verdicts
expected: |
  A human reads 07.1-FINDINGS.md's D7.1-01 verdict (SURVIVES AT SUBSET OF d), its per-d
  clearance table, the D7.1-02 verdict (SEED STABLE AT d=25) and the three-seed table, and
  either ratifies them as the phase's record or raises a scientific objection.
awaiting: user response

## Tests

### 1. Read and ratify the two verdicts
expected: A human reads the two verdicts, the per-d clearance table and the three-seed table, and either ratifies them or raises a scientific objection.
result: [pending]

Why a human is required: every blocking `checkpoint:human-verify` in this phase (07.1-04
Task 3, 07.1-05 Task 3, 07.1-06 Task 3) was mechanically advanced by the orchestrator under a
standing overnight "keep working" instruction while the user was asleep. That disclosure is
accurate and consistent across all three SUMMARYs, STATE.md and FINDINGS.md's opening section
— the verifier checked the disclosure itself — but no human has read either verdict. Every
number is real, measured and independently re-derived; what is unverified is the human
judgment the checkpoints exist to gate.

What to look at:
- `07.1-FINDINGS.md` §2 — D7.1-01, per-d clearance table with exact margins
- `07.1-FINDINGS.md` §5 — D7.1-02, three-seed table
- `07.1-FINDINGS.md` §3 — the measured stratum-count mechanism, which contradicts both
  D-02's stated rationale and RESEARCH's synthetic-fixture prediction
- Note when reading §5: seed 0 (the seed Phase 7 actually realized) is the weakest of the
  three by ~2.5x (margin_fraction -2.34 vs -5.84 and -5.98). All three clear, so the
  unanimity verdict is unaffected.

Consequence of an objection: the pre-registration is hard-locked (frozen in commit 6768666,
a verified strict ancestor of every later phase commit). Re-tuning any gating constant now
voids the pre-registration contract; the only remedy is a fresh freeze and a fresh run of the
whole phase. So an objection is cheapest to raise before any downstream phase builds on
these verdicts.

### 2. Decide disposition of 07.1-REVIEW.md's 3 open warnings
expected: Fix, defer, or explicitly accept WR-01/WR-02/WR-03 (re-run robustness of the 07.1 runner), or open a follow-up plan.
result: [pending]

Why a human is required: judgment call on debt priority. All three concern the runner's
own re-run behavior, not the delivered science:
- WR-01 — `run_positive_control` has no `--resume`/dedupe, so a re-run silently duplicates
  its 19 rows
- WR-02 — `run_null`/`run_seeds` dedupe per-cell rows on `--resume` but dedupe summary rows
  unconditionally; inconsistent contract
- WR-03 — `run_seeds` rewrites the whole `.npz` non-atomically each iteration; an interrupt
  mid-write could corrupt already-saved seeds' fields

Verified not to have fired in the single clean run that produced the current record: the
jsonl holds exactly 34 rows with one `positive_control_summary` row and no duplicate
`null_grid` or `seed` rows.

## Summary

total: 2
passed: 0
issues: 0
pending: 2
skipped: 0
blocked: 0

## Gaps
