---
phase: 07.1-density-stratified-null-and-seed-stability
plan: 06
type: execute
wave: 5
depends_on: ["07.1-02", "07.1-04", "07.1-05"]
files_modified:
  - .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md
  - notebooks/07.1_density_stratified_null.ipynb
autonomous: false
requirements: [D7.1-01, D7.1-02]
user_setup: []

must_haves:
  truths:
    - "`07.1-FINDINGS.md` reports both verdicts independently — D7.1-01's over `d in {20, 25, 32}` and D7.1-02's over the three seeds at `d=25` — so a null in one does not silently void the other (D-14)."
    - "The per-`d` clearance table appears in FINDINGS unconditionally, whichever verdict fired, with each residual's exact observed value, its exact band edge and its exact margin (D-15)."
    - "FINDINGS states which mechanism the REAL PU data showed for the stratum-count grid — a null-MEAN shift, a band-width change, or neither — measured, not inherited from D-02's stated rationale; and where the measurement contradicts D-02's stated mechanism, FINDINGS says so plainly rather than re-asserting the decision's prose."
    - "FINDINGS records D-05's trade-off: `h` and `m` are each permuted independently within strata, so the band mixes both residualization sides and is not attributable to the MKNN link alone; the shuffle-`m`-only alternative was considered and not chosen."
    - "FINDINGS records the WR-01 finding as protecting 07.1's own future call sites, and states plainly that Phase 7's `run_dsweep` never called `density_diagnostics` — so the fix validates nothing retroactively about the frozen jsonl."
    - "FINDINGS declares CLAUDE.md's Swiss roll gate already satisfied, naming `notebooks/02.6_swiss_roll_plainae_curvature_check.ipynb` as the satisfying artifact and stating the reason: 07.1 introduces no new manifold model, reusing `cae.PlainAutoEncoder` and `decoder_curvature.plain_decoder_curvature` unchanged."
    - "FINDINGS lists the phase's accepted limitations explicitly, including that the three `d=25` fits share one architecture and one training split so their spread measures fit-to-fit field instability rather than sampling variability, and that the seed axis was replaced at `d=25` only."
    - "`notebooks/07.1_density_stratified_null.ipynb` is executed end to end with its outputs committed, reads its numbers from the 07.1 record rather than recomputing a second, possibly divergent copy, and ends with printed pass/fail read-out lines."
    - "Phase 7's `ASSOCIATION DETECTED` appears in FINDINGS unmodified, as the reading 07.1 builds on rather than one it revises."
  prohibitions:
    - statement: "The Swiss-roll gate declaration must not be written as a waiver, an exemption, a deferral or a 'not applicable here'. CLAUDE.md's gate is non-optional; 07.1 satisfies it by introducing no new manifold model, and FINDINGS must state that reason and name the satisfying artifact. A declaration that omits the reason is indistinguishable from skipping the gate."
      requirement: D7.1-01
  artifacts:
    - path: ".planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md"
      provides: "Both verdicts, the per-d clearance table, the S-grid threshold table with null means, the seed table, the accepted limitations, and the Swiss-roll gate declaration"
      min_lines: 120
    - path: "notebooks/07.1_density_stratified_null.ipynb"
      provides: "The executed notebook, outputs committed"
  key_links:
    - from: "notebooks/07.1_density_stratified_null.ipynb"
      to: "notebooks/.cache/07.1_density_stratified_null.jsonl"
      via: "the notebook reads the recorded rows rather than recomputing them"
      pattern: "07.1_density_stratified_null"
---

<objective>
Write the phase's record: `07.1-FINDINGS.md` and an executed notebook, reporting both verdicts,
every table the goal names, the measured mechanism behind the stratum-count grid, and the phase's
accepted limitations.

Purpose: the phase goal names individual residuals to adjudicate. This plan is where they are
adjudicated in prose, with their numbers, their margins and the trade-offs of the instrument that
produced them — including the places where the measurement contradicts the reasoning that
motivated the design.

Output: `07.1-FINDINGS.md` and `notebooks/07.1_density_stratified_null.ipynb`, committed with
outputs, per CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-CONTEXT.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-RESEARCH.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-02-SUMMARY.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-04-SUMMARY.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-05-SUMMARY.md
@.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-FINDINGS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 07.1-FINDINGS.md</name>
  <reversibility rating="reversible">A document; every claim in it is re-derivable from the committed record.</reversibility>
  <precondition>notebooks/.cache/07.1_density_stratified_null.jsonl contains one row_kind "verdict" row and one row_kind "seed_verdict" row</precondition>
  <files>.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md</files>
  <read_first>
    - .planning/phases/07-curvature-conditioned-crossmodal-alignment/07-FINDINGS.md — §1.4 (the
      three density partials and the borrowed-threshold problem this phase exists to fix), §4 (how
      the Swiss-roll gate declaration was worded), §5 (the accepted-limitations section shape to
      mirror)
    - .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-04-SUMMARY.md and
      07.1-05-SUMMARY.md — every realized number
    - .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-02-SUMMARY.md — the
      two D-18 no-op proofs and the WR-01 wording constraint
    - .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-RESEARCH.md —
      §Common Pitfalls 1 and 2, §Open Questions 1 and 2, §Assumptions Log A1/A2/A3
    - notebooks/.cache/07.1_density_stratified_null.jsonl — read every row directly; do not
      transcribe numbers from a SUMMARY without checking them against the record
  </read_first>
  <action>
    Write `.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md`,
    mirroring `07-FINDINGS.md`'s section shape. Sections, in order:

    **1. What this phase answers, and what it does not.** D7.1-01 and D7.1-02 stated as questions,
    each with its verdict, reported independently (D-14). Then, explicitly: Phase 7's
    `ASSOCIATION DETECTED` is not reopened, softened or recomputed — 07.1 adds a statistic Phase 7
    did not have. Quote Phase 7's verdict unmodified.

    **2. D7.1-01 — the stratified null.** The per-`d` clearance table, reported unconditionally
    whichever verdict fired (D-15): for each `d`, the observed partial to full precision, the
    stratified band's two edges, the margin on the residual's own side, and that margin as a
    fraction of the band half-width. Beside it, for comparison only, Phase 7's borrowed
    negative-tail thresholds (0.020623594394910027 at d=20, 0.019682955551122476 at d=25,
    0.01872752130596235 at d=32) and the note that every prior reading of these three residuals
    was judged against a threshold built for the RAW statistic. Name every marginal number with
    its exact value and margin — the house style `07-CONTEXT.md` §5 set for the n=800 Betti draw-2
    result, where a max life exactly equal to its band failed a strict `>` and was reported as
    such rather than rounded away.

    **3. The stratum-count grid, and what it actually measured.** The `S in {10, 20, 50}` threshold
    table with `null_mean`, `null_std` and both edges at every cell, plus the observed partial
    shown once per `d` with the explicit note that it is invariant to `S` — this is a grid of
    thresholds, not of point estimates, which is where 07.1's sensitivity reporting differs in
    kind from Phase 7's `MKNN_K_GRID`, and the write-up says so rather than letting a reader
    assume the analogy (D-03).

    Then state which mechanism the REAL data showed. `07.1-RESEARCH.md` Pitfall 1 measured, on a
    synthetic lognormal fixture, that band width is flat across S=10..500 (std 0.0098-0.0117) while
    the null MEAN is strongly positive at S=10 (+0.033) decaying to +0.007-0.012 by S=100-500 —
    and that a positive-biased null is LIBERAL on the negative tail every residual here sits in.
    Report what the measured PU null means did across the three `S` values at its measured
    7.8-order density spread, and say plainly whether it reproduced. **Where the measurement
    contradicts D-02's stated rationale — that finer strata narrow the band and therefore raise
    sensitivity — say so.** D-02's CHOICE of `S = 20` may well stand on its own; its stated
    justification is a separate claim and this is the phase that can check it. Do not re-assert
    D-02's mechanism as fact.

    **4. The positive control.** The smallest planted residual recovered in each direction, the
    achieved values verbatim, any `bracket_exhausted` cell, and what licence the result gives:
    without a recovery, no non-detection reading is reported anywhere in the phase (D-04). Note
    that Phase 7's own grid could not resolve the floor between 0.021 and 0.05, exactly where the
    d=20 and d=32 residuals sit, which is why 07.1's grid was chosen finer.

    **5. D7.1-02 — seed stability at d=25.** The three-seed table, the three pairwise field
    agreements, seed 0's reproduction agreement against Phase 7's frozen `h_norm_25`, and the
    verdict. If it is `SPLIT ACROSS SEEDS`, report it as a complete terminal non-supportive
    outcome and state that the phase declines to claim the effect at `d=25` — not as a weakened
    claim and not with the dissenting seed set aside.

    **6. What the null is and is not.** D-05's recorded trade-off, verbatim: `h` and `m` are each
    permuted independently within strata, mirroring `permutation_type="pairings"`, so `h`'s
    residualization also varies per permutation and the band mixes both residualization sides. It
    is NOT attributable to the MKNN link alone. Name the shuffle-`m`-only alternative and record
    that it was considered and not chosen. Carry RESEARCH's Assumptions Log A1 forward: whether
    restricted permutation transfers cleanly from its usual ANOVA/Mantel settings to a
    rank-residualized partial correlation is not established by citation, and the positive control
    is the arbiter, not the citation.

    **7. Phase 7 review debt.** WR-01, WR-02 and WR-04 closed, with both no-op proofs quoted: the
    captured before/after `n_distinct` pair from `--mode smoke`, and the guard-non-firing test's
    result on Phase 7's real d=20/25/32 arrays. State the WR-01 correction plainly:
    Phase 7's `run_dsweep` never called `density_diagnostics` — it inlines an equivalent
    computation — so the guards protect 07.1's own future call sites and validate nothing
    retroactively about the frozen jsonl. WR-03 was already closed by `11242a7` and is not
    reopened.

    **8. The Swiss roll gate.** Declare CLAUDE.md's gate already satisfied by
    `notebooks/02.6_swiss_roll_plainae_curvature_check.ipynb`, and give the reason: 07.1 introduces
    no new manifold model, reusing `cae.PlainAutoEncoder` and
    `decoder_curvature.plain_decoder_curvature` unchanged. This is a declaration of an
    already-satisfied gate with its reason stated, not a waiver — word it the way
    `07-FINDINGS.md` §4 did.

    **9. Accepted limitations.** At minimum: the three `d=25` fits share one architecture and one
    training split, so their spread measures fit-to-fit field instability, not sampling
    variability; the seed axis was replaced at `d=25` only and Phase 7's
    `SEED_HANDLING_RULE = "single_seed_across_d_sweep"` still stands everywhere else; the field is
    still evaluated on all 10,000 rows including the 8,000 the decoder trained on; no ground truth
    for PU curvature exists anywhere in this record; and the deferred items — the full 3x3 seed x
    `d` grid, a stratified null for the RAW statistic, and CKA — are named as deferred, not as
    answered.

    **10. Artifacts.** Every path this phase produced, with a one-line description each.

    Every number in the document must be read from
    `notebooks/.cache/07.1_density_stratified_null.jsonl`, not transcribed from a SUMMARY without
    checking. Where a number is quoted from Phase 7, quote it from
    `notebooks/.cache/07_crossmodal_curvature.jsonl` at full precision.
  </action>
  <verify>
    <automated>test -s .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md &amp;&amp; grep -qF '02.6_swiss_roll_plainae_curvature_check.ipynb' .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md &amp;&amp; grep -qF 'SEED_HANDLING_RULE' .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md` exists and is non-empty.
    - It contains all ten numbered sections listed in `<action>`, each with a heading.
    - It names `notebooks/02.6_swiss_roll_plainae_curvature_check.ipynb` and states the reason the gate is already satisfied (no new manifold model) in the same section.
    - It contains the substring `SEED_HANDLING_RULE` in its accepted-limitations section.
    - It reports both verdict strings, each a member of the corresponding frozen vocabulary in `notebooks/pu_manifold/density_stratified_null.py`.
    - Every `d` in {20, 25, 32} appears in the per-`d` clearance table with an observed value, a band edge and a margin — verified by reading the table against `notebooks/.cache/07.1_density_stratified_null.jsonl`'s nine `null_grid` rows.
    - The null-mean-vs-`S` table in the document matches the `null_mean_vs_strata` record row cell for cell.
    - The document states explicitly whether the measured PU null-mean-vs-`S` behaviour reproduced `07.1-RESEARCH.md` Pitfall 1's synthetic finding, and whether it contradicts D-02's stated mechanism.
    - The document states that Phase 7's `run_dsweep` never called `density_diagnostics`, in the review-debt section.
    - `grep -ciE 'waive|waiver|exempt|not applicable' .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md` prints `0` in the Swiss-roll section — the gate is declared satisfied with a reason, never waived.
  </acceptance_criteria>
  <done>Both verdicts, all four tables, the measured stratum-count mechanism, D-05's trade-off, the review-debt proofs, the Swiss-roll declaration and the accepted limitations are all written down with their numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Write and execute the 07.1 notebook, outputs committed</name>
  <reversibility rating="reversible">A notebook that reads a committed record; re-executing it changes nothing outside itself.</reversibility>
  <precondition>notebooks/.cache/07.1_density_stratified_null.jsonl contains the null_grid, null_mean_vs_strata, positive_control_summary, verdict, seed and seed_verdict rows written by 07.1-04 and 07.1-05</precondition>
  <files>notebooks/07.1_density_stratified_null.ipynb</files>
  <read_first>
    - .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md — as
      written at Task 1; the notebook shows the same numbers, it does not derive different ones
    - notebooks/.cache/07.1_density_stratified_null.jsonl — every row
    - CLAUDE.md — the notebook rules: committed with outputs, executed end to end; and the Swiss
      roll section, which this notebook is NOT (it introduces no model and makes no
      pass/fail claim about one)
  </read_first>
  <action>
    Create `notebooks/07.1_density_stratified_null.ipynb`. It READS the 07.1 record and renders it;
    it does not recompute the grid, refit anything, or build a second implementation of any
    statistic. A notebook that recomputes is a second, silently divergent copy of the answer — the
    record written under the freeze is the answer.

    Cells, in order: load the record; the per-`d` clearance table as a dataframe, with the observed
    value, both band edges, the margin, and Phase 7's borrowed threshold beside it for contrast;
    a plot of the null distribution's band against the observed value at each `d`, at the headline
    `S`; the `null_mean`-vs-`S` diagnostic as a small table and a line plot, with the synthetic
    fixture's measured trend from `07.1-RESEARCH.md` Pitfall 1 annotated for comparison; the
    positive control's recovery curve (achieved value against target, per direction) with the
    smallest cleared target marked; the three-seed table at `d=25` with the pairwise field
    agreements; and a final cell printing three or four pass/fail read-out lines and a
    one-sentence read-out — the D7.1-01 verdict, the D7.1-02 verdict, whether the positive control
    licensed a non-detection reading, and whether the measured stratum-count mechanism matched
    D-02's stated one.

    Keep it short. Execute it top to bottom and commit it WITH its outputs, per CLAUDE.md. Never
    write to `notebooks/.cache/` from the notebook — it reads only.
  </action>
  <verify>
    <automated>.venv/bin/python -c "import json,sys; nb=json.load(open('notebooks/07.1_density_stratified_null.ipynb')); cells=[c for c in nb['cells'] if c['cell_type']=='code']; assert cells, 'no code cells'; assert all(c.get('outputs') or c.get('execution_count') is not None for c in cells), 'unexecuted code cell'; src=''.join(''.join(c['source']) for c in cells); assert '07.1_density_stratified_null' in src; print('OK', len(cells), 'code cells executed')"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/07.1_density_stratified_null.ipynb` exists, every code cell has a non-null `execution_count`, and at least one cell carries outputs.
    - The notebook's source references `notebooks/.cache/07.1_density_stratified_null.jsonl` and contains no call that refits a model or recomputes the permutation null.
    - The final cell's output contains the D7.1-01 verdict string and the D7.1-02 verdict string, both matching the record's `verdict` and `seed_verdict` rows exactly.
    - The final cell prints at least three pass/fail read-out lines and a one-sentence read-out.
    - `git status --porcelain notebooks/07.1_density_stratified_null.ipynb` is empty after the commit — the executed notebook including outputs is committed.
    - The notebook writes nothing: `notebooks/.cache/` file count and mtimes are unchanged by running it.
  </acceptance_criteria>
  <done>An executed notebook renders every table and plot the phase produced, reads them from the frozen record rather than recomputing, ends with pass/fail read-out lines, and is committed with its outputs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final read of the phase record</name>
  <what-built>
    `07.1-FINDINGS.md` and `notebooks/07.1_density_stratified_null.ipynb`, reporting both verdicts,
    the per-`d` clearance table, the stratum-count threshold table with its null means, the
    positive control's recovery, the three-seed table at `d=25`, the review-debt proofs, the
    Swiss-roll gate declaration and the accepted limitations.
  </what-built>
  <how-to-verify>
    Read `.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md` and
    open `notebooks/07.1_density_stratified_null.ipynb`.

    Five things to confirm:

    1. **Both verdicts are reported independently.** D7.1-01's answer does not depend on D7.1-02's
       and vice versa (D-14). Each is one of its own frozen vocabulary's values.

    2. **The per-`d` table is there whichever verdict fired** (D-15), with every marginal number
       named rather than rounded to a clean line.

    3. **The stratum-count section reports what was measured, not what was expected.** If the real
       PU null means moved with `S` the way the synthetic fixture's did, D-02's stated mechanism
       is contradicted and the document says so. If they did not, the document says that instead.
       Either way the reasoning that motivated `S = 20` is checked against evidence rather than
       repeated.

    4. **The WR-01 finding is worded correctly.** It protects future call sites. It says nothing
       retroactive about the frozen jsonl, because `run_dsweep` never called that function.

    5. **Nothing sealed was reopened.** Phase 7's `ASSOCIATION DETECTED` appears unmodified, and no
       verdict from Phases 2, 02.x, 3, 03.1, 4, 5 or 6 is reinterpreted anywhere.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe issues</resume-signal>
</task>

</tasks>

<planner_contributions>
Detector outcomes are recorded once, in `07.1-01-PLAN.md`'s `<planner_contributions>` block
(API-coverage: not detected; assumption-delta: not detected, `no-change`; schema-push: no ORM
files; security: threat models present in every plan).
</planner_contributions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the frozen record → the written finding | A number transcribed rather than read, or a notebook that recomputes, produces a second answer that can silently diverge from the recorded one |
| this phase's prose → prior sealed phases | A findings document is where a sealed verdict most easily gets softened by paraphrase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-24 | Tampering | a number in FINDINGS diverging from the record | high | mitigate | Every number is read from `07.1_density_stratified_null.jsonl` (and Phase 7 numbers from `07_crossmodal_curvature.jsonl`); acceptance criteria check the FINDINGS tables against the record's rows cell for cell |
| T-07.1-25 | Tampering | the notebook recomputing and producing a second, divergent answer | medium | mitigate | The notebook reads the record only; an acceptance criterion bars any refit or permutation recompute in its source, and it writes nothing to the cache |
| T-07.1-26 | Repudiation | a sealed prior verdict softened by paraphrase | high | mitigate | Phase 7's verdict is quoted unmodified; the phase-wide prohibition against reinterpreting any sealed verdict is checked at the final blocking checkpoint |
| T-07.1-27 | Repudiation | the Swiss-roll gate declared satisfied without its reason, indistinguishable from a skip | medium | mitigate | The declaration must name `02.6_swiss_roll_plainae_curvature_check.ipynb` AND state the no-new-manifold-model reason, both checked by acceptance criteria |
| T-07.1-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed — `07.1-RESEARCH.md` §Package Legitimacy Audit records "Not applicable"; nothing to gate |
</threat_model>

<artifacts_this_phase_produces>
Symbols and files created by THIS plan:

**New file `.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-FINDINGS.md`** —
ten numbered sections: the two questions and their verdicts; the D7.1-01 per-`d` clearance table;
the stratum-count grid and its measured mechanism; the positive control; D7.1-02's three-seed
table; what the null is and is not (D-05's trade-off); the Phase 7 review debt and its two no-op
proofs; the Swiss-roll gate declaration; the accepted limitations; the artifact list.

**New file `notebooks/07.1_density_stratified_null.ipynb`** — executed, outputs committed, reading
the 07.1 record only. Final cell prints the two verdict strings, the positive-control licence line
and the stratum-count-mechanism line.

No new code symbols. This plan adds no function, constant or test.
</artifacts_this_phase_produces>

<verification>
- `07.1-FINDINGS.md` exists with all ten sections and both verdicts.
- The notebook is executed with outputs and committed clean.
- Every table in FINDINGS matches the record row for row.
- The final blocking checkpoint is resolved and its answer recorded.
</verification>

<success_criteria>
- Both deliverables are reported independently, with every table the phase goal names.
- The stratum-count section reports the measured mechanism, and contradicts D-02's stated
  rationale where the evidence does.
- The Swiss-roll gate is declared satisfied with its reason and its artifact named — not waived.
- No sealed verdict from any prior phase is reopened, softened or reinterpreted.
</success_criteria>

<output>
Create `.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-06-SUMMARY.md`
when done. Record both verdict strings, the FINDINGS path, the notebook path, and the final
checkpoint's answer.
</output>