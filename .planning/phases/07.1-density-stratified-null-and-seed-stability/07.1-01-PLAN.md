---
phase: 07.1-density-stratified-null-and-seed-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/pu_manifold/density_stratified_null.py
  - notebooks/pu_manifold/tests/test_density_stratified_null.py
autonomous: false
requirements: [D7.1-01, D7.1-02]
user_setup: []

must_haves:
  truths:
    - "`density_stratified_null.assert_preregistered()` raises RuntimeError naming every constant that is absent, None, blank-string or empty-sequence, and returns None when all are set."
    - "Every gating constant in the new module is a fresh literal declared at module top level — no gating value reaches it through the sealed Phase 7 module's namespace (D-08)."
    - "`apply_partial_verdict` raises ValueError when its per-`d` mapping's keys are not exactly `set(D_SWEEP)` — a verdict from a partial sweep is not a verdict."
    - "`apply_seed_verdict` raises ValueError when its per-seed mapping's keys are not exactly `set(TORCH_INIT_SEEDS)`."
    - "Both verdict functions' signatures accept only a clearance mapping and a positive-control result — neither can accept a density number, a raw statistic, or an `S` value (D-16, structural not promissory)."
    - "`PARTIAL_VERDICT_VALUES` contains a value distinct from Phase 7's `SPLIT ACROSS d` vocabulary for the subset-survival outcome (D-15)."
    - "`apply_seed_verdict` returns `SPLIT ACROSS SEEDS` for any 2-of-3 clearance pattern and the supportive value only for 3-of-3 (D-11)."
    - "The freeze commit contains only the new module and is a strict git ancestor of every later commit in this phase."
  prohibitions:
    - statement: "The phase must not re-tune `N_STRATA_HEADLINE`, `STRATA_GRID`, `N_PERMUTATIONS`, `NULL_QUANTILE_PER_TAIL`, `PERMUTATION_SEED`, `POSITIVE_CONTROL_TARGET_RHOS` or either verdict vocabulary after any 07.1 number exists. Post-hoc rule-fitting voids the pre-registration contract; the only remedy is a fresh freeze and a fresh run."
      requirement: D7.1-01
    - statement: "The phase must not reinterpret, soften, reopen or re-derive any sealed verdict from Phases 2, 02.x, 3, 03.1, 4, 5, 6 or 7 — including Phase 7's `ASSOCIATION DETECTED`. 07.1 adds a statistic Phase 7 did not have; it does not overturn Phase 7's reading."
      requirement: D7.1-02
  artifacts:
    - path: "notebooks/pu_manifold/density_stratified_null.py"
      provides: "07.1's own frozen pre-registration block, assert_preregistered(), and both verdict functions"
      exports: ["assert_preregistered", "verdict_is_terminal", "apply_partial_verdict", "apply_seed_verdict"]
      contains: "N_PERMUTATIONS = 1000"
    - path: "notebooks/pu_manifold/tests/test_density_stratified_null.py"
      provides: "Freeze-guard, verdict-rule and locally-declared-constant tests"
      min_lines: 120
  key_links:
    - from: "notebooks/pu_manifold/density_stratified_null.py"
      to: "notebooks/pu_manifold/tests/test_density_stratified_null.py"
      via: "the test module imports the new module and asserts its freeze commit is a strict git ancestor of HEAD"
      pattern: "merge-base --is-ancestor"
---

<objective>
Freeze 07.1's own pre-registration: its gating constants (D-08), its two verdict vocabularies
and verdict functions (D-11, D-14, D-15, D-16), in a commit that strictly precedes any number
this phase produces.

Purpose: D7-05 sealed `crossmodal_curvature.py`. A gating constant imported across that freeze
boundary is not covered by 07.1's own `assert_preregistered()` or its own git-ancestry proof, so
D-08 requires the values be re-declared fresh here. This plan produces **no numbers at all** —
that is its point. Every later plan in the phase runs downstream of this commit.

Output: `notebooks/pu_manifold/density_stratified_null.py` (the freeze commit) plus
`notebooks/pu_manifold/tests/test_density_stratified_null.py` seeded with the freeze-guard and
verdict tests VALIDATION.md's Wave 0 requires.

Why this plan runs before the tracer (07.1-03) rather than after: the freeze-before-any-number
contract is structural. A tracer that produces a stratified-null number before the gating
constants exist in committed source cannot be retro-fitted into the contract; the constants must
land first. The tracer is the very next plan and proves this module end to end.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-CONTEXT.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-PATTERNS.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Extracted from the sealed Phase 7 module this plan mirrors but never imports constants from. -->

From notebooks/pu_manifold/crossmodal_curvature.py — the shapes to copy (NOT the values to import):

- `_REQUIRED_CONSTANTS: Tuple[str, ...]` — flat tuple of constant NAMES (line 283)
- `assert_preregistered() -> None` — loops the names, appends `"{name} (absent)"` /
  `"{name} (None)"` / `"{name} (empty string)"` / `"{name} (empty sequence)"`, raises
  `RuntimeError` naming every offender (line 300)
- `verdict_is_terminal(verdict: str) -> bool` (line 343)
- `apply_verdict(per_d_results: Dict[int, bool], positive_control_cleared_at: Any) -> str` —
  raises `ValueError` unless `set(per_d_results.keys()) == set(D_SWEEP)`; returns one member of
  `VERDICT_VALUES`; ends with `assert verdict_is_terminal(verdict)` (line 475)
- `VERDICT_VALUES = ("ASSOCIATION DETECTED", "NO DETECTABLE RELATIONSHIP", "SPLIT ACROSS d",
  "UNDERPOWERED -- NO CLAIM")` (line 272)
- `_relative_precision_distinct_count(values: np.ndarray) -> int` (line 517) — import and call,
  never reimplement
- `split_indices(n, split_seed, holdout_fraction) -> Tuple[np.ndarray, np.ndarray]` (line 392)

From notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py — the freeze-ancestry test shape:
- module-level `FREEZE_COMMIT_SHA` string; `_repo_root()` returning `Path(__file__).resolve().parents[3]`;
  `subprocess.run(["git", "cat-file", "-e", f"{SHA}^{{commit}}"], cwd=_repo_root())` existence probe
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Ratify 07.1's gating constant set before it is frozen (D-08)</name>
  <decision>
    D-08 re-declares Phase 7's permutation constants fresh in a new 07.1 module and freezes them
    with a git-ancestry proof. Ratify the exact constant set, its values, and two planner
    discretion calls inside it, before the freeze commit lands.
  </decision>
  <context>
    D-08 is rated **one-way** in `07.1-CONTEXT.md`: the pre-registration contract is that gating
    constants are committed in source before any runner can produce a number, with
    `git merge-base --is-ancestor` as the proof. Changing any of them after the first number
    exists breaks that contract and voids the verdict; the only remedy is a fresh freeze and a
    fresh run. Two of the values below are Claude's-discretion calls, not decisions the developer
    has already made, so they are surfaced here rather than assumed:

    (a) **Positive-control target grid (D-04, Claude's discretion).** Phase 7 used
    `(0.02, 0.05, 0.10, 0.20)` and `07-FINDINGS.md` records that the grid could not resolve the
    detection floor between 0.021 and 0.05 — exactly where the d=20 (-0.02419) and d=32
    (-0.02172) residuals sit. The proposed 07.1 grid brackets both:
    `(0.010, 0.015, 0.020, 0.025, 0.030, 0.040, 0.050, 0.10, 0.20)`.

    (b) **Positive-control direction (planner assumption, surfaced not assumed).** Phase 7's
    control plants a POSITIVE association only. `07.1-RESEARCH.md` Pitfall 1 measured that
    coarse strata bias the null MEAN positive, which is *liberal* on the negative tail — and
    every residual this phase adjudicates is negative. The proposal runs the control in BOTH
    directions (the negative arm is the exact mirror `two_tailed_permutation_null` already uses:
    bisect against `-h_real`) and gates `apply_partial_verdict`'s power argument on the
    NEGATIVE-direction floor. Cost of the extra arm is a few seconds.

    Also confirm the two additional `TORCH_INIT_SEED` values (Claude's discretion, D7.1-02):
    proposed `TORCH_INIT_SEEDS = (0, 1, 2)`. Seed `0` is Phase 7's realized value, so one of the
    three fits doubles as a reproduction check against the frozen `h_norm_25`.
  </context>
  <options>
    <option id="ratify-as-proposed">
      <name>Freeze exactly as proposed</name>
      <pros>Brackets both observed residuals with grid points on either side; the power argument
      is measured in the same direction as the residuals it licenses a reading about; seed 0
      gives a free reproduction check.</pros>
      <cons>The two-direction control is a small extension beyond D-04's literal
      "extend the bisection" wording; three extra targets and one extra arm to report.</cons>
    </option>
    <option id="positive-arm-only">
      <name>Freeze the finer grid but keep the control positive-direction only</name>
      <pros>Closest to D-04's literal text and to Phase 7's construction; simplest
      (CLAUDE.md: keep things simple first).</pros>
      <cons>Leaves the phase's power argument measured in the opposite direction from every
      residual it adjudicates, with a measured mechanism (RESEARCH Pitfall 1) predicting the two
      directions are not symmetric.</cons>
    </option>
    <option id="amend-then-freeze">
      <name>Amend values, then freeze</name>
      <pros>Any constant can be changed now at zero cost; after the freeze it cannot.</pros>
      <cons>None — this is the last cheap moment to change any of them.</cons>
    </option>
  </options>
  <resume-signal>Select: ratify-as-proposed, positive-arm-only, or amend-then-freeze (naming the amendments)</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Ratify the unanimity rule and the new verdict vocabularies (D-11, D-15)</name>
  <decision>
    Freeze `SEED_COMBINATION_RULE` as unanimity — all three seeds must clear, 2-of-3 is
    `SPLIT ACROSS SEEDS`, a terminal non-supportive outcome — and freeze
    `PARTIAL_VERDICT_VALUES` with a distinct value for subset survival across `d`.
  </decision>
  <context>
    D-11 is rated **one-way**: it is the same ratification Phase 5 escalated to a blocking
    developer checkpoint (`05-03-DECISION.md`). Relaxing it after seeing the three numbers is
    post-hoc rule-fitting, and the only clean remedy is re-running the seeds under a rule frozen
    in advance. On Phase 5's measured evidence a split is a live possibility, so this is the
    consequential call of the phase and it is made in the direction that refuses to overclaim.

    D-15 mints a verdict value Phase 7 does not have. The phase's most likely outcome —
    `d=25` clears, `d=20` and `d=32` do not — is a genuine finding (most of Phase 7's association
    was density everywhere except `d=25`), and Phase 7's `SPLIT ACROSS d` vocabulary would label
    it identically to a failure. Proposed vocabularies:

    - `PARTIAL_VERDICT_VALUES = ("RESIDUAL SURVIVES AT ALL d", "SURVIVES AT SUBSET OF d",
      "NO SURVIVING RESIDUAL", "UNDERPOWERED -- NO CLAIM")`
    - `SEED_VERDICT_VALUES = ("SEED STABLE AT d=25", "SPLIT ACROSS SEEDS",
      "NO SURVIVING RESIDUAL AT ANY SEED", "UNDERPOWERED -- NO CLAIM")`

    The per-`d` clearance table is reported unconditionally regardless of which verdict fires,
    because the phase goal names individual residuals to adjudicate.
  </context>
  <options>
    <option id="ratify">
      <name>Freeze unanimity and both vocabularies as proposed</name>
      <pros>Inherits Phase 5's ratified don't-pool discipline verbatim; names the likely outcome
      honestly instead of mislabelling it a failure.</pros>
      <cons>A 2-of-3 seed result becomes a terminal non-claim even if two seeds agree strongly.</cons>
    </option>
    <option id="ratify-with-amendments">
      <name>Ratify with named amendments to the vocabulary or the rule</name>
      <pros>Changes are free right now and impossible after the freeze.</pros>
      <cons>Any relaxation of unanimity must be argued now, blind to the three numbers — which is
      the entire point of ratifying before rather than after.</cons>
    </option>
  </options>
  <resume-signal>Select: ratify, or ratify-with-amendments (naming each amendment)</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write and freeze 07.1's pre-registration module and its guard tests</name>
  <reversibility rating="one-way">Committing these constants is the pre-registration act itself (D-08); after any 07.1 number exists they cannot be changed without voiding the verdict and re-running.</reversibility>
  <files>notebooks/pu_manifold/density_stratified_null.py, notebooks/pu_manifold/tests/test_density_stratified_null.py</files>
  <read_first>
    - notebooks/pu_manifold/crossmodal_curvature.py — lines 60-129 (the constants block shape),
      194-297 (`VERDICT_VALUES` + `_REQUIRED_CONSTANTS`), 300-347 (`assert_preregistered`,
      `verdict_is_terminal`), 475-515 (`apply_verdict`'s exact-key check and terminal assert)
    - notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py — lines 1-50 (the
      `FREEZE_COMMIT_SHA` / `_repo_root()` / `git cat-file -e` freeze-probe shape)
    - .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-PATTERNS.md —
      "Pre-registration freeze block + `assert_preregistered()`" and "Gating vs non-gating
      structural separation"
    - .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-CONTEXT.md — D-08,
      D-11, D-13, D-14, D-15, D-16
  </read_first>
  <behavior>
    - `assert_preregistered()` returns None when every name in `_REQUIRED_CONSTANTS` is set.
    - Setting any one required constant to `None`, `""`, `()` or deleting it makes
      `assert_preregistered()` raise `RuntimeError` whose message contains that constant's name.
    - `apply_partial_verdict({20: True, 25: True, 32: True}, 0.02)` returns
      `"RESIDUAL SURVIVES AT ALL d"`.
    - `apply_partial_verdict({20: False, 25: True, 32: False}, 0.02)` returns
      `"SURVIVES AT SUBSET OF d"` (D-15).
    - `apply_partial_verdict({20: False, 25: False, 32: False}, 0.02)` returns
      `"NO SURVIVING RESIDUAL"`; with `positive_control_cleared_at=None` it returns
      `"UNDERPOWERED -- NO CLAIM"` (D-04's licence rule, made mechanical).
    - `apply_partial_verdict({20: True, 25: True}, 0.02)` raises `ValueError` naming `D_SWEEP`.
    - `apply_seed_verdict({0: True, 1: True, 2: True}, 0.02)` returns `"SEED STABLE AT d=25"`.
    - `apply_seed_verdict({0: True, 1: True, 2: False}, 0.02)` returns `"SPLIT ACROSS SEEDS"`,
      and the same mapping built in any key order returns the same string (D-11 order-invariance).
    - `apply_seed_verdict({0: True, 1: True}, 0.02)` raises `ValueError` naming `TORCH_INIT_SEEDS`.
    - `inspect.signature(apply_partial_verdict).parameters` has exactly two names, and neither is
      a density, raw-statistic or stratum-count parameter (D-16 structural non-gating).
    - The module source matched line-anchored contains `N_PERMUTATIONS = 1000`,
      `PERMUTATION_SEED = 20260825`, `NULL_QUANTILE_PER_TAIL = 0.975`, `N_STRATA_HEADLINE = 20`
      and `SPLIT_SEED = 20260813` as top-level literal assignments (D-08: declared fresh, not
      inherited).
    - `FREEZE_COMMIT_SHA` recorded in the test module resolves to a real commit and is a strict
      git ancestor of `HEAD` (`git merge-base --is-ancestor` exits 0 AND
      `git rev-list --count <freeze>..HEAD` is >= 1).
  </behavior>
  <action>
    Create `notebooks/pu_manifold/density_stratified_null.py` containing ONLY the frozen
    pre-registration block, `assert_preregistered()`, `verdict_is_terminal()` and the two verdict
    functions. No permutation routine, no strata routine, no positive control — those land in
    07.1-03 and 07.1-04, strictly below the freeze line and marked with a banner comment there.

    Declare every value below as a fresh top-level literal. Do NOT reference any gating value
    through the sealed Phase 7 module's namespace (D-08); importing that module for
    `_relative_precision_distinct_count` and `split_indices` is fine and expected, importing its
    pre-registered VALUES is what the decision forbids.

    Constants to declare, with the frozen values:
    `D_SWEEP = (20, 25, 32)`;
    `N_PERMUTATIONS = 1000`; `PERMUTATION_SEED = 20260825`; `NULL_QUANTILE_PER_TAIL = 0.975`
    (D-08, D-13);
    `N_STRATA_HEADLINE = 20` (D-02, the gating stratum count — 500 points per stratum at
    n = 10,000); `STRATA_GRID = (10, 20, 50)` (D-03, non-gating thresholds);
    `STRATIFICATION_RULE` — prose stating equal-count quantile bins on density RANK, assignment
    by `np.argsort(density)` position so exactly-tied densities are separated by index order and
    never merged, remainder rows going to the LAST stratum (D-01);
    `SENSITIVITY_GRID_RULE` — prose stating the `S` grid is a grid of THRESHOLDS, not of point
    estimates (the observed partial is computed with `controls=density` and never touches
    strata), that it may qualify a reading but never overturn or escalate it, and that
    `null_vals.mean()` is reported at every `S` alongside the band because
    `07.1-RESEARCH.md` Pitfall 1 measured the stratum-count artifact as a null-MEAN shift, not a
    band-width change (D-03);
    `PERMUTATION_SCHEME_RULE` — prose stating `h` and `m` are each permuted independently within
    strata, mirroring `permutation_type="pairings"`, AND recording D-05's trade-off verbatim:
    because `h`'s residualization also varies per permutation, the band mixes both
    residualization sides and is NOT attributable to the MKNN link alone; the shuffle-`m`-only
    alternative was considered and not chosen;
    `FROZEN_PARTIAL_REFERENCE = {20: -0.024188908104711526, 25: -0.06583482510693942,
    32: -0.02171865371304575}` and `PARTIAL_REFERENCE_RTOL = 1e-9`,
    `PARTIAL_REFERENCE_ATOL = 1e-12` (D-07; values read directly from
    `notebooks/.cache/07_crossmodal_curvature.jsonl`'s three `row_kind: "sweep"` rows);
    `POSITIVE_CONTROL_TARGET_RHOS`, `POSITIVE_CONTROL_DIRECTIONS`, `POSITIVE_CONTROL_SEED = 20260825`
    and `POSITIVE_CONTROL_RULE` exactly as ratified at Task 1 (D-04);
    `SPLIT_SEED = 20260813` and `HOLDOUT_FRACTION = 0.2` (D-09, held fixed across seeds);
    `TORCH_INIT_SEEDS` exactly as ratified at Task 1 (D-09);
    `SEED_COMBINATION_RULE` — prose stating unanimity, that 2-of-3 is `SPLIT ACROSS SEEDS`, a
    complete terminal non-supportive outcome and not a claim, and that it inherits Phase 5's
    ratified don't-pool decision (D-11);
    `SEED_GATING_STATISTIC = "partial_rho_density_controlled"` and
    `RAW_STATISTIC_IS_NON_GATING = True` (D-12);
    `DIAGNOSTICS_ARE_NON_GATING = True` (D-16);
    `RECORD_STEM = "07.1_density_stratified_null"` and `RECORD_LOCATION_RULE` (records land in
    the gitignored `notebooks/.cache/`, resolved through the cache module's containment guard);
    `PREREGISTRATION_FREEZE_RULE` — prose stating that no 07.1 number may be produced at or
    before the freeze commit, that the proof is equality against a hardcoded SHA plus
    `git merge-base --is-ancestor` plus `git rev-list --count >= 1`, and that Phase 7's constants
    are re-declared rather than imported because a gating constant crossing the D7-05 freeze
    boundary is not covered by this module's own guard (D-08);
    `PARTIAL_VERDICT_RULE`, `PARTIAL_VERDICT_VALUES`, `SEED_VERDICT_RULE`, `SEED_VERDICT_VALUES`
    exactly as ratified at Task 2 (D-14, D-15).

    Then `_REQUIRED_CONSTANTS` as a flat tuple naming every constant above, and
    `assert_preregistered()` copying `crossmodal_curvature.assert_preregistered`'s shape exactly:
    loop the names against `globals()`, collect `"{name} (absent)"`, `"{name} (None)"`,
    `"{name} (empty string)"`, `"{name} (empty sequence)"`, raise `RuntimeError` naming every
    offender in one message.

    `verdict_is_terminal(verdict, values)` takes the vocabulary tuple explicitly so one function
    serves both vocabularies (D-14: two independent verdicts).

    `apply_partial_verdict(per_d_results, positive_control_cleared_at)` and
    `apply_seed_verdict(per_seed_results, positive_control_cleared_at)` — two parameters each,
    exactly mirroring `apply_verdict`'s signature discipline so neither can accept a density
    number, the raw statistic, or a stratum count (D-16). Each raises `ValueError` when its
    mapping's keys are not exactly the frozen key set, ends with an `assert verdict_is_terminal`,
    and applies the all/none/mixed branch with the `UNDERPOWERED -- NO CLAIM` override when
    `positive_control_cleared_at is None` (D-04's licence made mechanical).

    Commit the module ALONE as the freeze commit, message
    `feat(07.1): freeze 07.1 pre-registration constants and verdict rules`. Record the resulting
    SHA — it is 07.1's `FREEZE_COMMIT_SHA` and every later plan hardcodes it.

    Then, in a SECOND commit, create
    `notebooks/pu_manifold/tests/test_density_stratified_null.py` with the tests named in
    `<behavior>`, matching `test_crossmodal_curvature_run.py`'s header conventions
    (`sys.path.insert` to `parents[2]`, `_repo_root()` at `parents[3]`, a module-level
    `FREEZE_COMMIT_SHA` holding the SHA from the first commit, and a `git cat-file -e` existence
    probe before the ancestry assertions).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_density_stratified_null.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_density_stratified_null.py -q` exits 0.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import density_stratified_null as d; d.assert_preregistered()"` exits 0 and prints nothing.
    - `.venv/bin/python -c "import sys; sys.path.insert(0,'notebooks'); from pu_manifold import density_stratified_null as d; d.N_PERMUTATIONS=None; d.assert_preregistered()"` exits non-zero with `RuntimeError` and the substring `N_PERMUTATIONS` in stderr.
    - `grep -Ec '^N_PERMUTATIONS = 1000$' notebooks/pu_manifold/density_stratified_null.py` prints `1`.
    - `grep -Ec '^PERMUTATION_SEED = 20260825$' notebooks/pu_manifold/density_stratified_null.py` prints `1`.
    - `grep -Ec '^NULL_QUANTILE_PER_TAIL = 0.975$' notebooks/pu_manifold/density_stratified_null.py` prints `1`.
    - `grep -Ec '^N_STRATA_HEADLINE = 20$' notebooks/pu_manifold/density_stratified_null.py` prints `1`.
    - `grep -Ec '^SPLIT_SEED = 20260813$' notebooks/pu_manifold/density_stratified_null.py` prints `1`.
    - `git merge-base --is-ancestor <freeze-sha> HEAD` exits 0 AND `git rev-list --count <freeze-sha>..HEAD` prints a value >= 1.
    - `git show --stat --name-only --format= <freeze-sha>` lists exactly one path: `notebooks/pu_manifold/density_stratified_null.py`.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` reports 0 failures (the pre-existing suite is unaffected — this plan adds only new files).
  </acceptance_criteria>
  <done>The module exists, `assert_preregistered()` passes and fails correctly, both verdict functions apply their frozen rules and refuse partial key sets, and the freeze commit is a strict ancestor of HEAD containing only that module.</done>
</task>

</tasks>

<planner_contributions>
Detector outcomes recorded per the orchestrator's injected contribution blocks:

- **API-coverage gate:** `node gsd-core/bin/lib/api-coverage.cjs --json` over this phase's ROADMAP
  section + CONTEXT.md returned `{"detected": false, "signals": []}`. This phase integrates no
  external API, SDK or service — it is pure numpy/scipy/torch compute over frozen local artifacts.
  No `COVERAGE.md` is owed; the seal-time detector will re-confirm the negative.
- **Assumption-delta:** `gsd-tools query assumption-delta scan 07.1 --json` returned
  `{"detected": false, "signals": []}`. Decision recorded: **`no-change`** — no singular→plural,
  required→optional or derived→chosen transition is introduced. The nouns that were primary in
  Phase 7 (`d`, the field, the record row) stay primary; 07.1 adds a second STATISTIC over the
  same identity model, not a second identity.
- **Schema-push gate:** scanned the phase scope and repository for Payload / Prisma / Drizzle /
  Supabase / TypeORM file patterns — zero matches (this is a Python research repository with no
  ORM and no database tier). No `[BLOCKING]` schema push task is injected.
- **Security:** `<threat_model>` present below at `security_asvs_level: 1`,
  `security_block_on: high`.
</planner_contributions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed source → the scientific record | A gating constant's value crossing into a published verdict; the only integrity boundary this plan touches |
| sealed Phase 7 module → new 07.1 module | D7-05 published `crossmodal_curvature.py` as import-never-edit; a value crossing that boundary is not covered by 07.1's own guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-01 | Tampering | 07.1 pre-registration constants | high | mitigate | Freeze commit contains only `density_stratified_null.py`; `assert_preregistered()` refuses absent/None/blank/empty-tuple; test asserts `git merge-base --is-ancestor` AND `git rev-list --count >= 1` |
| T-07.1-02 | Tampering | gating value inherited across the D7-05 freeze boundary | high | mitigate | Every gating value is a line-anchored top-level literal, asserted by `grep -Ec '^NAME = VALUE$'` acceptance criteria (D-08) |
| T-07.1-03 | Elevation of privilege | a verdict function accepting a quantity it may not gate on | medium | mitigate | Both verdict signatures take exactly two parameters; a signature-inspection test pins it (D-16) |
| T-07.1-04 | Repudiation | a verdict computed from a partial sweep or partial seed set | medium | mitigate | Both verdict functions raise `ValueError` unless their mapping's keys exactly match the frozen key set |
| T-07.1-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs no packages — `07.1-RESEARCH.md` §Package Legitimacy Audit records "Not applicable"; every function used is already in the pinned numpy 2.5.1 / scipy 1.18.0 / torch environment. No `[ASSUMED]`/`[SUS]` package exists to gate. |
</threat_model>

<artifacts_this_phase_produces>
Symbols created by THIS plan (the plan-review source-grounding pass must exclude these from drift
verification — they do not exist in the tree before this plan runs):

**New file `notebooks/pu_manifold/density_stratified_null.py`**
- Constants: `D_SWEEP`, `N_PERMUTATIONS`, `PERMUTATION_SEED`, `NULL_QUANTILE_PER_TAIL`,
  `N_STRATA_HEADLINE`, `STRATA_GRID`, `STRATIFICATION_RULE`, `SENSITIVITY_GRID_RULE`,
  `PERMUTATION_SCHEME_RULE`, `FROZEN_PARTIAL_REFERENCE`, `PARTIAL_REFERENCE_RTOL`,
  `PARTIAL_REFERENCE_ATOL`, `POSITIVE_CONTROL_TARGET_RHOS`, `POSITIVE_CONTROL_DIRECTIONS`,
  `POSITIVE_CONTROL_SEED`, `POSITIVE_CONTROL_RULE`, `SPLIT_SEED`, `HOLDOUT_FRACTION`,
  `TORCH_INIT_SEEDS`, `SEED_COMBINATION_RULE`, `SEED_GATING_STATISTIC`,
  `RAW_STATISTIC_IS_NON_GATING`, `DIAGNOSTICS_ARE_NON_GATING`, `RECORD_STEM`,
  `RECORD_LOCATION_RULE`, `PREREGISTRATION_FREEZE_RULE`, `PARTIAL_VERDICT_RULE`,
  `PARTIAL_VERDICT_VALUES`, `SEED_VERDICT_RULE`, `SEED_VERDICT_VALUES`, `_REQUIRED_CONSTANTS`
- Functions: `assert_preregistered()`, `verdict_is_terminal(verdict, values)`,
  `apply_partial_verdict(per_d_results, positive_control_cleared_at)`,
  `apply_seed_verdict(per_seed_results, positive_control_cleared_at)`
- Verdict string values: `"RESIDUAL SURVIVES AT ALL d"`, `"SURVIVES AT SUBSET OF d"`,
  `"NO SURVIVING RESIDUAL"`, `"UNDERPOWERED -- NO CLAIM"`, `"SEED STABLE AT d=25"`,
  `"SPLIT ACROSS SEEDS"`, `"NO SURVIVING RESIDUAL AT ANY SEED"`

**New file `notebooks/pu_manifold/tests/test_density_stratified_null.py`**
- `FREEZE_COMMIT_SHA` (module constant), `_repo_root()`, `_freeze_commit_exists()`
- `test_assert_preregistered_passes_when_all_constants_set`,
  `test_assert_preregistered_rejects_unset_constant` (parameterized over `_REQUIRED_CONSTANTS`),
  `test_gating_constants_are_declared_as_local_literals`,
  `test_freeze_commit_is_a_strict_ancestor_of_head`,
  `test_apply_partial_verdict_all_clear`, `test_apply_partial_verdict_mints_survives_at_subset`,
  `test_apply_partial_verdict_underpowered_without_positive_control`,
  `test_apply_partial_verdict_raises_on_partial_sweep`,
  `test_apply_partial_verdict_signature_cannot_accept_density`,
  `test_seed_unanimity_rule`, `test_apply_seed_verdict_raises_on_partial_seed_set`,
  `test_apply_seed_verdict_is_key_order_invariant`

**New git artifact:** 07.1's `FREEZE_COMMIT_SHA` (the commit containing only the module above).
</artifacts_this_phase_produces>

<verification>
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` — full suite green (589 tests
  collected before this plan; expect 589 + the new file's tests, 0 failures).
- `git show --stat --name-only --format= <freeze-sha>` lists exactly one file.
- `git merge-base --is-ancestor <freeze-sha> HEAD` exits 0.
- Both checkpoints resolved and their selections recorded in the plan SUMMARY.
</verification>

<success_criteria>
- 07.1's gating constants exist as fresh literals in committed source, with a git-ancestry proof
  and an `assert_preregistered()` that refuses None/absent/blank/empty-tuple (D-08).
- Two independent verdict functions exist, each structurally unable to accept a quantity it may
  not gate on, each refusing a partial key set (D-14, D-16).
- `SURVIVES AT SUBSET OF d` and `SPLIT ACROSS SEEDS` are distinct frozen values (D-15, D-11).
- No 07.1 number of any kind was produced by this plan.
</success_criteria>

<output>
Create `.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-01-SUMMARY.md`
when done. Record the freeze commit SHA prominently — 07.1-03, 07.1-04 and 07.1-05 all hardcode
it — and record both checkpoint selections verbatim.
</output>
