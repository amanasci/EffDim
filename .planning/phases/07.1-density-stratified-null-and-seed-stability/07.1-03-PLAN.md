---
phase: 07.1-density-stratified-null-and-seed-stability
plan: 03
type: execute
wave: 2
depends_on: ["07.1-01"]
files_modified:
  - notebooks/pu_manifold/density_stratified_null.py
  - notebooks/diagnostics/07.1_density_stratified_null_run.py
  - notebooks/pu_manifold/tests/test_density_stratified_null.py
autonomous: true
requirements: [D7.1-01]
user_setup: []

must_haves:
  truths:
    - "A single command runs the whole D7.1-01 path end to end on real PU data at d=20 — reload the frozen field, recompute MKNN and density, recompute the observed partial, assert it against Phase 7's record, build a within-stratum permutation null, read a two-tailed band off it, and append one row to a scratch record — and prints every intermediate number."
    - "The recomputed `partial_rho_density_controlled` at d=20 equals Phase 7's frozen -0.024188908104711526 within rtol 1e-9 / atol 1e-12; a mismatch halts the run and is reported as a finding, never rounded past (D-07)."
    - "Strata are equal-count quantile bins on density RANK: exactly-tied density values are separated by `np.argsort` index order into adjacent strata and never merged into one, and the `n % S` remainder rows go to the LAST stratum, so every stratum's size is `n // S` except the last (D-01)."
    - "`stratified_partial_null` raises ValueError naming the offending array when `h` or `m` is non-finite, constant, or of a different length from `density`, and raises when `n // n_strata` is below the 3-point floor `cross_split_curvature.partial_spearman` enforces."
    - "Any distinct-value count the new module or runner takes goes through `crossmodal_curvature._relative_precision_distinct_count`, never a locally written round-and-count, and every value reaching the JSONL writer is a plain Python float — a raw numpy value raises TypeError before it is written."
    - "`rankdata(x)[perm]` equals `rankdata(x[perm])`, pinned by a test, so the precompute-ranks-once optimization is proven correct rather than assumed."
    - "`partial_spearman(-h, m, controls=c)` equals `-partial_spearman(h, m, controls=c)` exactly, pinned by a test — this is what makes reading both tails off ONE null distribution equivalent to Phase 7's mirror-call two-tailed construction."
    - statement: "On a true-null fixture carrying a real density confound, the two-tailed band rejects at no more than roughly 2.5% per tail at the pre-registered quantile — the restricted permutation is calibrated, not merely implemented."
      verification: backstop
  prohibitions:
    - statement: "The stratified null band must not be described anywhere — in code docstrings, in the record, or in FINDINGS — as attributable to the MKNN link alone. D-05 permutes `h` and `m` independently within strata, so `h`'s residualization also varies per permutation and the band mixes both residualization sides. The shuffle-`m`-only alternative was considered and not chosen, and that trade-off is reported, not omitted."
      requirement: D7.1-01
  artifacts:
    - path: "notebooks/pu_manifold/density_stratified_null.py"
      provides: "density_strata and stratified_partial_null, below the freeze banner"
      exports: ["density_strata", "stratified_partial_null"]
    - path: "notebooks/diagnostics/07.1_density_stratified_null_run.py"
      provides: "The 07.1 runner skeleton and its end-to-end --mode smoke path"
      exports: ["run_smoke", "resolve_record_path", "append_record_row", "recompute_mknn_and_density", "load_frozen_field"]
    - path: "notebooks/pu_manifold/tests/test_density_stratified_null.py"
      provides: "Null-routine correctness, calibration, strata and guard tests"
  key_links:
    - from: "notebooks/diagnostics/07.1_density_stratified_null_run.py"
      to: "notebooks/pu_manifold/density_stratified_null.py"
      via: "the runner imports the frozen module and calls assert_preregistered() before any compute"
      pattern: "assert_preregistered"
    - from: "notebooks/pu_manifold/density_stratified_null.py"
      to: "notebooks/pu_manifold/cross_split_curvature.py"
      via: "the null's per-resample statistic reproduces partial_spearman's rank-space residualization against a FIXED density control"
      pattern: "partial_spearman"
---

<objective>
The tracer: one command that walks the entire D7.1-01 path end to end on real PU data at a single
`d`, through every layer this phase touches — frozen artifact reload, MKNN and density recompute,
the observed partial and its D-07 assertion against Phase 7's record, the new within-stratum
permutation null, the two-tailed band, and one appended record row.

Purpose: prove the architecture before it expands to a 3x3 grid, three seeds and two verdicts.
Every layer 07.1-04 and 07.1-05 build on exists and is exercised here on real data, at
production quality. The only things reduced are the resample count and the `d`/`S` coverage —
never the correctness of any layer.

Output: `density_strata` and `stratified_partial_null` in the frozen module (below its freeze
banner), the 07.1 runner with a working `--mode smoke`, and the null-routine test group.

Measured cost of the full path at n = 10,000 (measured directly this session, not estimated):
MKNN at k=20 takes 3.6s, `local_density_weights` takes 1.7s, one `partial_spearman` call takes
~0.01s, and `07.1-RESEARCH.md` measured ~1.5ms per stratified resample. The tracer is seconds,
not minutes — cost is not a constraint anywhere in D7.1-01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-CONTEXT.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-PATTERNS.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-RESEARCH.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Everything the runner and the null routine consume. Extracted from the tree at plan time. -->

From notebooks/pu_manifold/cross_split_curvature.py (line 232), the statistic being calibrated:
```
def partial_spearman(x, y, controls: Optional[Any] = None) -> float
```
Its algebra, which the per-resample statistic must reproduce with the control FIXED: rank-transform
`x` and `y`; rank-transform each control column; `design = column_stack([ones(n), RC])`;
`residual(v) = v - design @ lstsq(design, v, rcond=None)[0]`; return
`float(np.corrcoef(residual(rx), residual(ry))[0, 1])`. Raises below 3 points; raises when a
residual's std falls under `EPS`.

From notebooks/pu_manifold/crossmodal_curvature.py:
```
def per_point_mknn(z1, z2, k) -> np.ndarray                    # line 406, returns (n,) in [0, 1]
def _relative_precision_distinct_count(values) -> int          # line 517
def split_indices(n, split_seed, holdout_fraction) -> Tuple    # line 392
HEADLINE_K = 20 ; DENSITY_K = 30 ; DENSITY_FIELD_D = 20
PU_COLUMN_A = "hsc" ; PU_COLUMN_B = "legacysurvey"
DENSITY_INPUT = "legacysurvey_ambient_768"
```

From notebooks/pu_manifold/curvature_probe.py (line 161):
```
def local_density_weights(X, k_density, d) -> np.ndarray   # returns INVERSE density, mean-normalized to 1
```
`DENSITY_SIGN_CONVENTION`: report on `1.0 / w`, never on `w` — using `w` flips the sign of every
statistic and breaks comparability with Phase 4's REGN-01.

Its clearance-rule donor, notebooks/pu_manifold/curvature_probe.py `permutation_null` (line 1021),
ends with `"clears_null": bool(observed_rho > null_threshold)` — a STRICT inequality. The new
routine mirrors that strictness on both tails.

From notebooks/pu_manifold/cache.py:
```
def cache_path(stem: str, ext: str) -> Path                # containment-checked
def _assert_inside_cache(path: Path) -> None               # raises ValueError on escape
```

From notebooks/diagnostics/07_crossmodal_curvature_run.py — the runner skeleton to mirror:
- lines 14-46: `_flag_value_from_argv(flag, argv)` accepting BOTH `--flag value` and `--flag=value`,
  then `_THREADS = 8` and the `OMP_NUM_THREADS` / `MKL_NUM_THREADS` / `NUMEXPR_NUM_THREADS` env
  writes, ALL of it above the numpy/torch imports
- lines 55-62: `NOTEBOOK_ROOT = Path(__file__).resolve().parents[1]` inserted onto `sys.path`
- lines 101-129: `load_pu_pair(column_a, column_b)` — picks the `subsample_*.npz` carrying both
  columns with the most rows, lexicographically first on a tie, and returns `(Xa, Xb, path)`
- lines 198-231: `_distinct_value_count`, `resolve_record_path`, `append_record_row` (whose
  `isinstance(value, (np.ndarray, np.generic))` check raises TypeError before any write)
- lines 408-465: `_strict_ancestor_or_exit(freeze_commit)` — equality against the module's own
  hardcoded SHA, THEN `git merge-base --is-ancestor`, THEN `git rev-list --count >= 1`

Frozen artifacts (gitignored):
- `notebooks/.cache/07_crossmodal_curvature_fields.npz` — keys `h_norm_20`, `h_norm_25`,
  `h_norm_32`, `cond_g_20`, `cond_g_25`, `cond_g_32`, `h_norm`, `cond_g`; each `(10000,) float64`.
  It carries NO MKNN, NO embeddings and NO density — those are recomputed.
- `notebooks/.cache/07_crossmodal_curvature.jsonl` — three `row_kind: "sweep"` rows carrying
  `partial_rho_density_controlled` = -0.024188908104711526 (d=20), -0.06583482510693942 (d=25),
  -0.02171865371304575 (d=32).
- `notebooks/.cache/subsample_*.npz` — the ambient `hsc` / `legacysurvey` columns, `(10000, 768)`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end stratified null at d=20 — one path, real data, through every layer</name>
  <reversibility rating="reversible">Compute functions and a runner mode behind the already-frozen constant surface; changing either is a local edit with no migration and no published contract.</reversibility>
  <precondition>notebooks/.cache/07_crossmodal_curvature_fields.npz exists and carries key h_norm_20, notebooks/.cache/07_crossmodal_curvature.jsonl exists with three row_kind "sweep" rows, and at least one notebooks/.cache/subsample_*.npz carries both the hsc and legacysurvey columns</precondition>
  <files>notebooks/pu_manifold/density_stratified_null.py, notebooks/diagnostics/07.1_density_stratified_null_run.py</files>
  <read_first>
    - notebooks/pu_manifold/density_stratified_null.py — the whole module as frozen by 07.1-01,
      so the new functions land BELOW a banner comment and touch no frozen constant
    - notebooks/pu_manifold/cross_split_curvature.py — `partial_spearman`, lines 232-288 (the
      exact residualization algebra to reproduce)
    - notebooks/pu_manifold/curvature_probe.py — `local_density_weights` (line 161) for the
      density convention, and `permutation_null` lines 1097-1160 for the guard clauses and the
      strict `observed > threshold` clearance rule
    - notebooks/diagnostics/07_crossmodal_curvature_run.py — lines 1-130 (header, argv sniffing,
      thread caps, `load_pu_pair`), 198-235 (`_distinct_value_count`, `resolve_record_path`,
      `append_record_row`), 408-465 (`_strict_ancestor_or_exit`), 791-856 (`build_arg_parser`,
      `main`)
    - .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-RESEARCH.md —
      §Architecture Patterns Pattern 1 (the verified precompute-ranks-once implementation shape)
      and §Common Pitfalls 1 (why the null's MEAN is reported, not only its band)
    - .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-CONTEXT.md — D-01,
      D-05, D-06, D-07
  </read_first>
  <action>
    **Part A — the compute functions**, appended to `notebooks/pu_manifold/density_stratified_null.py`
    below a banner comment reading that everything above it is the frozen pre-registration from
    07.1-01 and is untouched by this addition (the same banner convention
    `crossmodal_curvature.py` uses at its own line 512).

    `density_strata(density, n_strata) -> np.ndarray` — returns an `(n,)` int array of stratum
    labels. Equal-count quantile bins on density RANK (D-01), assigned through
    `np.argsort(density)` so exactly-tied density values are separated by index order into
    adjacent strata rather than merged; bin size `n // n_strata`, with the `n % n_strata`
    remainder rows going to the LAST stratum. Raises `ValueError` if `n_strata` is below 1 or if
    `n // n_strata` is below 3 — the floor `partial_spearman` enforces, named in the message.

    `stratified_partial_null(h, m, density, n_strata, n_resamples, seed, quantile_per_tail) -> Dict`
    — the D-06 routine. Guard first, mirroring `curvature_probe.permutation_null`'s clause order
    and message style, every message beginning `stratified_partial_null: `: non-finite on `h`,
    non-finite on `m`, non-finite on `density`, length mismatch across the three, then a
    zero-peak-to-peak constant check on `h` and on `m`.

    Then precompute, ONCE, outside the resample loop: `rankdata(h)`, `rankdata(m)`,
    `rankdata(density)`, the design matrix `column_stack([ones(n), rank(density)])`, and the list
    of per-stratum index arrays from `density_strata`. The control is FIXED across every
    resample — only `h` and `m` move. Each resample permutes the precomputed RANK values within
    each stratum independently for `h` and for `m` (D-05, mirroring
    `permutation_type="pairings"`), then computes the residual-Pearson statistic directly from
    the permuted rank vectors. `rankdata(x)[perm] == rankdata(x[perm])` is what licenses skipping
    the rank transform inside the loop; Task 2 pins that identity with a test.

    Draw permutations from a single `np.random.default_rng(seed)`, so the same `seed` reproduces
    the same null exactly (D-13).

    Return a flat dict of plain Python scalars: `observed`, `null_mean`, `null_std`, `null_low`,
    `null_high`, `n_strata`, `stratum_size_min`, `stratum_size_max`, `n_resamples`, `seed`,
    `quantile_per_tail`, `clears_positive`, `clears_negative`, `clears_either`, `direction`.
    `null_high` is the `quantile_per_tail` quantile of the null values and `null_low` the
    `1 - quantile_per_tail` quantile. Clearance is STRICT on both tails, mirroring
    `curvature_probe.permutation_null`'s own `observed > threshold`: `clears_positive` is
    `observed > null_high`, `clears_negative` is `observed < null_low`. An observed value exactly
    equal to either edge does NOT clear. `direction` is `"positive"`, `"negative"` or `"neither"`,
    with `"positive"` winning a simultaneous tie deterministically, exactly as
    `two_tailed_permutation_null` resolves it.

    Reading both tails off ONE null distribution is equivalent to Phase 7's mirror-call
    construction because `partial_spearman(-h, m, controls=c) == -partial_spearman(h, m,
    controls=c)` exactly — a rank reversal is affine and the design carries an intercept. State
    that in the docstring; Task 2 pins it with a test.

    The docstring must also record D-05's trade-off verbatim from the frozen
    `PERMUTATION_SCHEME_RULE`: the band mixes both residualization sides and is not attributable
    to the MKNN link alone.

    **Part B — the runner.** Create `notebooks/diagnostics/07.1_density_stratified_null_run.py`,
    mirroring the Phase 7 runner's skeleton exactly. Module docstring naming each mode and giving
    a copy-pasteable usage line per mode. Then, in this order and all above the heavy imports:
    `_flag_value_from_argv` accepting both `--flag value` and `--flag=value`, `_THREADS = 8`
    overridable by `--threads`, and the three thread-cap env writes. Then the imports,
    `NOTEBOOK_ROOT` on `sys.path`, and a module-level `FREEZE_COMMIT_SHA` holding the SHA
    07.1-01-SUMMARY.md records.

    Functions: `_git_rev_parse`; `_strict_ancestor_or_exit(freeze_commit)` copying the Phase 7
    shape — equality against this module's own `FREEZE_COMMIT_SHA` first (a wrong-but-genuine
    ancestor must not pass), then `git merge-base --is-ancestor`, then
    `git rev-list --count <freeze>..HEAD >= 1`, exiting 1 with a message naming D-08;
    `_distinct_value_count(arr)` delegating to `crossmodal_curvature._relative_precision_distinct_count`
    — a thin delegation, never a second implementation, since duplicating it is precisely the
    defect 07.1-02 is removing elsewhere in the tree; `resolve_record_path(record_path_arg)`
    defaulting to the cache path for the frozen module's `RECORD_STEM` and routing any supplied
    path through the cache module's containment guard before it is opened;
    `append_record_row(row, record_path)` copied from the Phase 7 runner including its
    raw-numpy-value TypeError guard.

    `load_frozen_field(d) -> np.ndarray` — loads `h_norm_{d}` from
    `notebooks/.cache/07_crossmodal_curvature_fields.npz` through the cache module's path helper,
    raising a message naming the missing key if absent.

    `recompute_mknn_and_density() -> Tuple[np.ndarray, np.ndarray, np.ndarray, np.ndarray, str]` —
    `load_pu_pair` on the frozen column names, `per_point_mknn` at `HEADLINE_K`,
    `local_density_weights` on the `legacysurvey` array at `DENSITY_K` / `DENSITY_FIELD_D`, and
    `density = 1.0 / w` per `DENSITY_SIGN_CONVENTION`. Print the wallclock of each step and the
    density p05/p50/p95 percentiles. Computed once per run and reused across every `d` and every
    `S`, because neither MKNN nor density passes through the autoencoder and density is
    `d`-independent (D-10).

    `run_smoke(args) -> str` — the tracer path. Call the frozen module's `assert_preregistered()`
    first. Then: recompute MKNN and density; reload `h_norm_20`; compute
    `partial_spearman(h, m, controls=density)`; compare it against the frozen module's
    `FROZEN_PARTIAL_REFERENCE[20]` at the frozen rtol/atol and HALT with a non-zero exit naming
    D-07 if it does not match; build a `stratified_partial_null` at `N_STRATA_HEADLINE` with
    `--smoke-permutations` resamples and the frozen `PERMUTATION_SEED` and
    `NULL_QUANTILE_PER_TAIL`; print the observed value, the null mean, the null std, both band
    edges, both stratum-size extremes, `clears_either` and `direction`; and append exactly one
    row of plain Python scalars to the scratch record path. `--mode smoke` REQUIRES
    `--record-path` and refuses to default onto the frozen record — it is a reduced-resample
    exercise, not the deliverable, and it prints a banner saying so, mirroring `run_dsweep`'s own
    scratch banner. It does NOT call `_strict_ancestor_or_exit`; the production modes added in
    07.1-04 and 07.1-05 do.

    `selfcheck()` — imports clean, `assert_preregistered()` passes, both verdict functions raise
    on a partial key set, and every frozen artifact the runner needs exists. Prints one line per
    check and returns a bool.

    `build_arg_parser()` with `--mode` (choices `smoke`, `selfcheck` for now — later plans extend
    the list), `--record-path`, `--threads`, `--smoke-permutations` defaulting to 100,
    `--freeze-commit`, `--resume`, and `--selfcheck`. Then `main()` guarded by
    `if __name__ == "__main__":`.

    Run the tracer and record its full stdout in the plan SUMMARY:
    `.venv/bin/python notebooks/diagnostics/07.1_density_stratified_null_run.py --mode smoke
    --record-path notebooks/.cache/07.1_scratch_tracer.jsonl`
  </action>
  <verify>
    <automated>.venv/bin/python notebooks/diagnostics/07.1_density_stratified_null_run.py --mode smoke --record-path notebooks/.cache/07.1_scratch_tracer.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - The smoke command exits 0.
    - Its stdout contains the recomputed d=20 partial printed to at least 12 significant figures, and that value equals `-0.024188908104711526` within rtol 1e-9 / atol 1e-12 (measured this session: the recomputation returns `-0.02418890810471153`).
    - Its stdout contains a null mean, a null std, both band edges, `stratum_size_min`, `stratum_size_max`, `clears_either` and `direction`.
    - `stratum_size_min` and `stratum_size_max` both print `500` at `N_STRATA_HEADLINE = 20` and n = 10,000.
    - `notebooks/.cache/07.1_scratch_tracer.jsonl` contains exactly one line, and `.venv/bin/python -c "import json;print(sorted(json.loads(open('notebooks/.cache/07.1_scratch_tracer.jsonl').read().splitlines()[0]).keys()))"` exits 0 and lists at least `clears_either`, `null_high`, `null_low`, `null_mean`, `observed`.
    - `.venv/bin/python notebooks/diagnostics/07.1_density_stratified_null_run.py --mode smoke` WITHOUT `--record-path` exits non-zero and prints a message refusing to default onto the frozen record.
    - `.venv/bin/python notebooks/diagnostics/07.1_density_stratified_null_run.py --selfcheck` exits 0.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` reports 0 failures.
    - `git show --stat --name-only --format= HEAD` does NOT list `notebooks/pu_manifold/crossmodal_curvature.py` — the sealed Phase 7 module is imported by this plan and never edited by it.
  </acceptance_criteria>
  <done>One command reloads the frozen d=20 field, reproduces Phase 7's recorded partial, builds a within-stratum permutation null, reads a two-tailed band off it, and writes one scratch record row — every layer D7.1-01 needs, wired and exercised on real PU data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the null routine's correctness, calibration and guards</name>
  <reversibility rating="reversible">Test additions only.</reversibility>
  <files>notebooks/pu_manifold/tests/test_density_stratified_null.py</files>
  <read_first>
    - notebooks/pu_manifold/density_stratified_null.py — `density_strata` and
      `stratified_partial_null` as written in Task 1
    - notebooks/pu_manifold/tests/test_density_stratified_null.py — the file 07.1-01 created,
      for its header, `sys.path` and `FREEZE_COMMIT_SHA` conventions
    - .planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-VALIDATION.md — the
      Per-Task Verification Map rows this task satisfies, and their exact test names
  </read_first>
  <behavior>
    - `test_rank_permutation_equivariance`: for a seeded random array and a seeded permutation,
      `rankdata(x)[perm]` equals `rankdata(x[perm])` elementwise, including with deliberate ties.
    - `test_partial_spearman_is_exactly_odd_under_negation`: on a seeded fixture,
      `partial_spearman(-h, m, controls=c)` equals `-partial_spearman(h, m, controls=c)` to within
      1e-12 — the identity that makes a single null distribution serve both tails.
    - `test_recomputed_partial_matches_frozen_record`: reloading `h_norm_20`, `h_norm_25` and
      `h_norm_32` from the frozen npz and recomputing against the recomputed MKNN and density
      reproduces the three `partial_rho_density_controlled` values in
      `07_crossmodal_curvature.jsonl` at the frozen rtol/atol (D-07). Skips with an explicit
      reason if either gitignored artifact is absent.
    - `test_density_strata_are_equal_count`: at n = 10,000 and S = 20 every stratum holds exactly
      500 points; at n = 10,007 and S = 20 the first 19 hold 500 and the last holds 507.
    - `test_density_strata_separate_tied_densities`: an input where many density values are
      exactly equal still yields the declared per-stratum counts, and the tied points land in
      adjacent strata rather than collapsing into one.
    - `test_density_strata_raises_below_the_three_point_floor`: `n_strata` large enough that
      `n // n_strata` is under 3 raises `ValueError` mentioning the floor.
    - `test_stratified_null_rejects_nonfinite_input` /
      `test_stratified_null_rejects_constant_input` /
      `test_stratified_null_rejects_length_mismatch`: each raises `ValueError` whose message
      begins with the routine's name and identifies the offending array.
    - `test_stratified_null_is_reproducible_under_a_fixed_seed`: two calls with the same seed
      return byte-identical `null_mean`, `null_low` and `null_high`.
    - `test_clearance_is_strict_at_the_band_edge`: an observed value set exactly equal to
      `null_low` yields `clears_negative is False`, and exactly equal to `null_high` yields
      `clears_positive is False`.
    - `test_null_calibration_on_true_null_fixture`: on a seeded fixture where `h` and `m` are
      conditionally independent given a lognormal density (a genuine true null carrying a real
      density confound), repeated draws reject at no more than roughly 2.5% per tail at the
      frozen quantile. Keep the fixture small enough that the test runs in under 60s, and record
      the realized rejection rate in the assertion message so a near-miss is readable.
    - `test_record_row_rejects_raw_numpy`: the runner's record writer raises `TypeError` when any
      value in the row is a numpy array or numpy scalar.
  </behavior>
  <action>
    Extend `notebooks/pu_manifold/tests/test_density_stratified_null.py` with the tests named in
    `<behavior>`, using the exact function names VALIDATION.md's Per-Task Verification Map lists
    where it lists one. Load the 07.1 runner by file path with
    `importlib.util.spec_from_file_location` in a module-scoped fixture, matching
    `test_crossmodal_curvature_run.py`'s existing pattern rather than inventing a second one.

    Every test that touches a gitignored artifact under `notebooks/.cache/` must skip with an
    explicit reason when it is absent, never fail — the artifacts are not in the repository.

    Record the measured full-suite wallclock in the plan SUMMARY; VALIDATION.md's Test
    Infrastructure table lists it as unmeasured and this is the Wave-0 obligation to close it.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_density_stratified_null.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_density_stratified_null.py -q` exits 0, reports 0 failed and 0 errors, and completes in under 60s.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_density_stratified_null.py::test_rank_permutation_equivariance -x` exits 0.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_density_stratified_null.py::test_recomputed_partial_matches_frozen_record -x` exits 0 and does NOT report a skip (the frozen artifacts are present in this working tree).
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_density_stratified_null.py::test_null_calibration_on_true_null_fixture -x` exits 0.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` reports 0 failures, and its measured wallclock is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The precompute-ranks optimization, the single-null two-tail equivalence, the D-07 reproduction at all three `d`, the strata contract including tied densities and the 3-point floor, every input guard, seed reproducibility, strict clearance at the band edge and the null's calibration on a true-null fixture are each pinned by a named passing test.</done>
</task>

</tasks>

<planner_contributions>
Detector outcomes are recorded once, in `07.1-01-PLAN.md`'s `<planner_contributions>` block
(API-coverage: not detected; assumption-delta: not detected, `no-change`; schema-push: no ORM
files; security: threat models present in every plan).
</planner_contributions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied `--record-path` → the cache directory | A traversal segment could write outside `notebooks/.cache/` |
| computed arrays → the JSONL record | A raw numpy value reaching `json.dumps` emits a non-standard token or a `TypeError` at read time |
| caller arrays → `stratified_partial_null` | Non-finite or constant input silently produces `NaN`, which poisons an entire 1,000-resample band |
| committed source → the scientific record | A reduced-resample exercise run landing in the frozen record would be indistinguishable from the deliverable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-09 | Tampering | `--record-path` on the new 07.1 runner | high | mitigate | `resolve_record_path` routes any supplied path through `cache._assert_inside_cache` before it is opened, reusing the sealed T-07-01 guard rather than a bespoke path join |
| T-07.1-10 | Tampering | a `--mode smoke` reduced-resample run reaching the frozen record | high | mitigate | `--mode smoke` requires `--record-path`, exits non-zero without it, and prints a scratch banner; only the production modes call `_strict_ancestor_or_exit` |
| T-07.1-11 | Information disclosure | non-finite value poisoning a 1,000-resample band | medium | mitigate | Guard clauses on `h`, `m` and `density` raise `ValueError` naming the offending array before any resample, mirroring `curvature_probe.permutation_null`; pinned by three tests |
| T-07.1-12 | Tampering | raw numpy value reaching the JSONL writer | medium | mitigate | `append_record_row` copied verbatim including its `isinstance(value, (np.ndarray, np.generic))` `TypeError`; pinned by `test_record_row_rejects_raw_numpy` (the Phase 6 serialization amendment is the precedent) |
| T-07.1-13 | Repudiation | a distinct-value count taken at raw float precision | medium | mitigate | `_distinct_value_count` is a thin delegation to `crossmodal_curvature._relative_precision_distinct_count`; `05-02-SUMMARY.md`'s retracted 5,301/9,852-vs-4/3 miscount is the standing precedent |
| T-07.1-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed — `07.1-RESEARCH.md` §Package Legitimacy Audit records "Not applicable"; nothing to gate |
</threat_model>

<artifacts_this_phase_produces>
Symbols created by THIS plan:

**Added to `notebooks/pu_manifold/density_stratified_null.py`** (below the 07.1-01 freeze banner)
- `density_strata(density, n_strata) -> np.ndarray`
- `stratified_partial_null(h, m, density, n_strata, n_resamples, seed, quantile_per_tail) -> Dict[str, Any]`
  returning keys `observed`, `null_mean`, `null_std`, `null_low`, `null_high`, `n_strata`,
  `stratum_size_min`, `stratum_size_max`, `n_resamples`, `seed`, `quantile_per_tail`,
  `clears_positive`, `clears_negative`, `clears_either`, `direction`

**New file `notebooks/diagnostics/07.1_density_stratified_null_run.py`**
- Constants: `FREEZE_COMMIT_SHA`, `_THREADS`, `NOTEBOOK_ROOT`, `DIAGNOSTICS_ROOT`
- Functions: `_flag_value_from_argv`, `_git_rev_parse`, `_strict_ancestor_or_exit`,
  `_distinct_value_count`, `resolve_record_path`, `append_record_row`, `load_frozen_field`,
  `recompute_mknn_and_density`, `run_smoke`, `selfcheck`, `build_arg_parser`, `main`
- CLI flags: `--mode {smoke,selfcheck}`, `--record-path`, `--threads`, `--smoke-permutations`,
  `--freeze-commit`, `--resume`, `--selfcheck`

**Added to `notebooks/pu_manifold/tests/test_density_stratified_null.py`**
- `test_rank_permutation_equivariance`, `test_partial_spearman_is_exactly_odd_under_negation`,
  `test_recomputed_partial_matches_frozen_record`, `test_density_strata_are_equal_count`,
  `test_density_strata_separate_tied_densities`,
  `test_density_strata_raises_below_the_three_point_floor`,
  `test_stratified_null_rejects_nonfinite_input`, `test_stratified_null_rejects_constant_input`,
  `test_stratified_null_rejects_length_mismatch`,
  `test_stratified_null_is_reproducible_under_a_fixed_seed`,
  `test_clearance_is_strict_at_the_band_edge`, `test_null_calibration_on_true_null_fixture`,
  `test_record_row_rejects_raw_numpy`, plus a module-scoped `runner_071` fixture

**New record path (gitignored):** `notebooks/.cache/07.1_scratch_tracer.jsonl` (scratch only —
never the deliverable record)
</artifacts_this_phase_produces>

<verification>
- The tracer command runs end to end on real PU data and exits 0.
- The D-07 reproduction matches Phase 7's frozen record at all three `d` (one in the tracer, all
  three in the test).
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` — 0 failures, wallclock recorded.
- The sealed Phase 7 module and runner appear in no commit from this plan.
</verification>

<success_criteria>
- Every layer D7.1-01 needs exists and has been exercised on real data at production quality
  (D-06): frozen reload, MKNN/density recompute, observed partial, D-07 assertion, within-stratum
  null, two-tailed band, record write.
- The null's calibration, the strata contract and every input guard are pinned by named tests.
- Nothing gating has been produced: the tracer writes only to a scratch path and its resample
  count is reduced.
</success_criteria>

<output>
Create `.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-03-SUMMARY.md`
when done. Record the tracer's full stdout, the recomputed d=20 partial to full precision, the
measured wallclock of each stage, and the full-suite wallclock VALIDATION.md asks Wave 0 to
measure.
</output>
