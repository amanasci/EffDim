---
phase: 07.1-density-stratified-null-and-seed-stability
plan: 02
subsystem: testing
tags: [pytest, guard-clauses, code-review-debt, sealed-source-amendment]

# Dependency graph
requires:
  - phase: 07-curvature-conditioned-crossmodal-alignment
    provides: crossmodal_curvature.py (sealed), 07_crossmodal_curvature_run.py (sealed runner), 07-REVIEW.md (WR-01/WR-02/WR-04 findings)
provides:
  - "density_diagnostics finite/constant/length-mismatch guards (WR-01), demonstrated a no-op on Phase 7's real d=20/25/32 arrays"
  - "run_smoke's single distinct-value-count implementation (WR-02), demonstrated behaviour-preserving via a captured before/after --mode smoke run"
  - "test_crossmodal_curvature_run.py extended with resolve_record_path and is_scratch scratch-detection coverage (WR-04)"
affects: [07.1-04, 07.1-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Guard clauses mirroring a donor function's exact clause order and message style (curvature_probe.permutation_null -> density_diagnostics)"
    - "D-18 no-op proof: demonstrated via a real before/after capture or a real-data non-firing test, never asserted in prose"

key-files:
  created: []
  modified:
    - notebooks/pu_manifold/crossmodal_curvature.py
    - notebooks/diagnostics/07_crossmodal_curvature_run.py
    - notebooks/pu_manifold/tests/test_crossmodal_curvature.py
    - notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py

key-decisions:
  - "Task 1 checkpoint (D-18 ratification) auto-resolved to amend-both-with-proofs per standing session authorization -- see Checkpoints Auto-Resolved below"
  - "WR-01's no-op proof is worded as protecting future call sites only, never as validating the frozen 07_crossmodal_curvature.jsonl (run_dsweep never calls density_diagnostics)"

patterns-established:
  - "Sealed Phase 7 source may be amended in place only under a demonstrated, not asserted, no-op proof (D-18), each amendment its own reviewable commit"

requirements-completed: [D7.1-01, D7.1-02]

coverage:
  - id: D1
    description: "density_diagnostics raises a named ValueError on non-finite h/m, length mismatch, and constant h/m, mirroring permutation_null's guard order/style (WR-01)"
    requirement: D7.1-01
    verification:
      - kind: unit
        ref: "notebooks/pu_manifold/tests/test_crossmodal_curvature.py -k density_diagnostics_guard"
        status: pass
    human_judgment: false
  - id: D2
    description: "D-18 no-op proof: the new guards do not fire on Phase 7's real d=20/25/32 h_norm arrays reloaded from the frozen fields npz"
    requirement: D7.1-01
    verification:
      - kind: unit
        ref: "notebooks/pu_manifold/tests/test_crossmodal_curvature.py::test_density_diagnostics_guard_does_not_fire_on_phase7_arrays"
        status: pass
    human_judgment: false
  - id: D3
    description: "run_smoke's inline distinct-value count replaced with a call to the existing _distinct_value_count helper (WR-02), no second implementation remains"
    requirement: D7.1-01
    verification:
      - kind: unit
        ref: "grep -c _distinct_value_count in run_smoke's body (>=1); grep -c np.unique in run_smoke's body (0)"
        status: pass
    human_judgment: false
  - id: D4
    description: "D-18 no-op proof: --mode smoke's printed n_distinct is identical before and after the WR-02 edit, captured from two real runs"
    requirement: D7.1-01
    verification:
      - kind: manual_procedural
        ref: ".venv/bin/python notebooks/diagnostics/07_crossmodal_curvature_run.py --mode smoke (before/after capture, both n_distinct=16)"
        status: pass
    human_judgment: false
  - id: D5
    description: "resolve_record_path's default and supplied-path branches, and the is_scratch scratch-detection guard in both argv forms, gain test coverage (WR-04)"
    requirement: D7.1-02
    verification:
      - kind: unit
        ref: "notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py -k \"resolve_record_path or scratch\""
        status: pass
    human_judgment: false

duration: 35min
completed: 2026-08-27
status: complete
---

# Phase 07.1 Plan 02: Phase 7 Review Debt (WR-01, WR-02, WR-04) Summary

**Guard clauses on `density_diagnostics`, `run_smoke`'s distinct-count dedupe, and runner test coverage for `resolve_record_path`/`is_scratch` -- all three carrying a demonstrated (not asserted) no-op proof.**

## Performance

- **Duration:** ~35 min
- **Started:** 2026-08-26 (context load) / first commit 2026-08-27T00:11:56-04:00
- **Completed:** 2026-08-27T00:19:45-04:00
- **Tasks:** 3 (1 checkpoint, 2 auto)
- **Files modified:** 4

## Accomplishments

- `crossmodal_curvature.density_diagnostics` now raises a named `ValueError` on non-finite `h`/`m`, a length mismatch between them, or a constant `h`/`m` -- mirroring `curvature_probe.permutation_null`'s exact guard-clause order and message style. Additive only: no statistic, key, or docstring language changed.
- The WR-01 no-op proof (`test_density_diagnostics_guard_does_not_fire_on_phase7_arrays`) reloads Phase 7's **real** `h_norm_20`/`h_norm_25`/`h_norm_32` from the frozen `07_crossmodal_curvature_fields.npz`, recomputes MKNN on the real `hsc`/`legacysurvey` ambient embeddings, and asserts the new guards do not fire and every returned value is finite. One incidental confirmation: the recomputed `partial_rho_density_controlled` at d=20 came out `-0.024188908104711526`, matching Phase 7's frozen jsonl record to full precision -- the reload path is intact.
- `run_smoke`'s inline relative-precision distinct count (`scale = ...; n_distinct = int(np.unique(...))`) is replaced with a call to the file's own `_distinct_value_count` helper, which already delegates to `crossmodal_curvature._relative_precision_distinct_count` -- the single implementation `run_dsweep` already used. The dead `scale` local was removed with it.
- `test_crossmodal_curvature_run.py` (created by `c92260f` for CR-01/CR-02/CR-03, 15 tests) is extended, not recreated, with 6 new tests covering `resolve_record_path`'s two branches and the `is_scratch` scratch-detection guard in both `--flag value` and `--flag=value` argv forms.
- Full suite: 642 passed, 1 skipped, 0 failed (baseline before this plan: 630 passed, 1 skipped).

## Checkpoints Auto-Resolved

**Task 1 (`checkpoint:decision`, gate=`blocking`): Ratifying in-place amendment of sealed Phase 7 source (D-18).**

Per standing session authorization (user away, pre-approved autonomous execution for non-`blocking-human` checkpoints), auto-resolved to **`amend-both-with-proofs`** -- the plan's presented option and the one 07.1-CONTEXT.md's D-17/D-18 decisions already commit to. Rationale, as stated in the plan's Task 1 context: closing the debt here means 07.1's own new code copies enforced patterns rather than merely-documented ones; both no-op proofs (WR-01's guard-non-firing test, WR-02's before/after `n_distinct` capture) are cheap and real, matching the `c92260f` and Phase 6 amendment precedents. The alternative options (`wr02-only`, `defer-both`) were not selected -- `wr02-only` would leave `density_diagnostics` unguarded in a module 07.1's own code sits beside, and `defer-both` would reverse D-17 without a new decision superseding it.

The one correction the plan's Task 1 context flagged before ratification was honored throughout: `run_dsweep` (the function that produced the frozen jsonl) never calls `density_diagnostics` -- it inlines an equivalent computation. WR-01's no-op proof and every reference to it in this SUMMARY are worded as protecting **future call sites**, never as validating `07_crossmodal_curvature.jsonl`'s provenance.

## Task Commits

Each task was committed atomically, and Task 3's two steps (WR-02, WR-04) were committed separately per the plan's explicit instruction so the WR-02 amendment's diff is independently reviewable:

1. **Task 2: WR-01 -- finite/constant guards on `density_diagnostics`, with its no-op proof** - `fe229f8` (fix)
2. **Task 3, Step B: WR-02 dedupe `run_smoke`'s distinct-count implementation** - `dea4951` (fix)
3. **Task 3, Step D: WR-04 `resolve_record_path` and scratch-detection coverage** - `d2e8830` (test)

## Files Created/Modified

- `notebooks/pu_manifold/crossmodal_curvature.py` -- WR-01 guard clauses inserted into `density_diagnostics` (5 `raise ValueError` sites; 0 lines removed)
- `notebooks/diagnostics/07_crossmodal_curvature_run.py` -- WR-02: `run_smoke`'s inline distinct-count replaced with a call to `_distinct_value_count`
- `notebooks/pu_manifold/tests/test_crossmodal_curvature.py` -- 6 new tests: 5 named `*_guard_*` (nonfinite h, nonfinite m, constant h, constant m, length mismatch) plus the D-18 no-op-proof test against Phase 7's real arrays
- `notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py` -- 6 new tests: 3 for `resolve_record_path`, 3 for `is_scratch` scratch detection (extended, not recreated)

## Re-grepped current line numbers (WR-02 site, per 07.1-RESEARCH.md Pitfall 3)

At plan-execution time, the WR-02 site (`run_smoke`'s inline distinct count) was confirmed at lines **265-266** of `notebooks/diagnostics/07_crossmodal_curvature_run.py` (`scale = ...` at 265, `n_distinct = int(np.unique(...))` at 266) -- not the 246-247 CONTEXT.md cited (pre-`c92260f`) nor even RESEARCH.md's own 264-266 citation exactly (off by the intervening `t_mknn` assignment line), confirming the file anchors on function name / surrounding code, not raw line numbers, remain the right approach; both lines are now removed.

## D-18 No-Op Proofs (captured, not asserted)

**WR-02 -- `--mode smoke` before/after `n_distinct` capture (both real runs, `.venv/bin/python notebooks/diagnostics/07_crossmodal_curvature_run.py --mode smoke`):**

```
BEFORE (pre-edit): [per_point_mknn] wallclock=0.1s  n_distinct=16 (<= HEADLINE_K + 1 = 21)
AFTER  (post-edit): [per_point_mknn] wallclock=0.1s  n_distinct=16 (<= HEADLINE_K + 1 = 21)
```

Identical (`n_distinct=16` both times). Every other printed statistic in the two runs (`var_explained=0.7567`, `cond(g) median=2.6605e+01`, `observed_rho=-0.3282`, both tail thresholds, `VERDICT: ASSOCIATION DETECTED`) was also byte-identical between the before and after captures, confirming the edit is behaviour-preserving on the real smoke path, not merely on the one changed line.

**WR-01 -- guard-non-firing test's pass line, against Phase 7's real arrays:**

```
notebooks/pu_manifold/tests/test_crossmodal_curvature.py::test_density_diagnostics_guard_does_not_fire_on_phase7_arrays PASSED
```

Loops over `d in (20, 25, 32)`, reloading `h_norm_20`/`h_norm_25`/`h_norm_32` from the frozen `07_crossmodal_curvature_fields.npz` and recomputing `m` via `cc.per_point_mknn` on the real `hsc`/`legacysurvey` ambient columns from `subsample_20260729_a79b3460b838fd0a.npz` (the same file `load_pu_pair` resolves). Asserts every returned value is a finite plain `float` at all three `d`. Not skipped in this run -- both gitignored cache artifacts were present.

## Decisions Made

- **Checkpoint auto-resolution:** see "Checkpoints Auto-Resolved" above.
- **WR-01 test placement:** the 5 new guard tests plus the no-op-proof test were added to the existing `test_crossmodal_curvature.py` (matching the plan's `<artifacts_this_phase_produces>` naming), rather than a new file, consistent with WR-04's own finding that fragmenting coverage across files is the pattern this debt exists to close.
- **An extra test not named in the plan's artifact list** (`test_density_diagnostics_guard_rejects_length_mismatch`) was added because the plan's own `<behavior>` block requires the length-mismatch guard ("density_diagnostics with h and m of different lengths raises ValueError naming both lengths") but the artifact list names only 5 guard tests. Covering it directly, rather than relying on incidental coverage from another test, keeps the guard behaviour independently verified. This does not violate the "at least 11 passed" / "at least 21 passed" acceptance criteria (12 and 21+6=... both satisfied with room).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking, verification-command level] Two acceptance-criteria shell one-liners in the plan text are self-defeating and were verified by an equivalent correct method instead of literally**

- **Found during:** Task 2 acceptance-criteria check
- **Issue (a):** `awk '/^def density_diagnostics/,/^def [a-z_]+\(/' ... | grep -c 'raise ValueError'` prints `0`, not the expected `>= 4`. Root cause: `density_diagnostics` is the **last** function in the file, and its own `def` line matches BOTH the awk range's start pattern and its end pattern (`^def [a-z_]+\(` matches `def density_diagnostics(` too), so awk's range collapses to that single line and never captures the function body below it. This is a defect in the verification command, not in the implementation.
  **Issue (b):** `git diff <pre-task-sha> -- ... | grep -c '^-'` printed `1`, not the expected `0`. Root cause: `grep -c '^-'` matches the unified-diff file header line `--- a/notebooks/pu_manifold/crossmodal_curvature.py`, which also starts with `-` but is diff metadata, not a removed code line.
- **Fix:** Verified the underlying intent by an equivalent correct method instead of editing the plan text: a Python-based function-body slice (matching `density_diagnostics` up to the next `^def` or end-of-file) found **5** `raise ValueError` statements (satisfies `>= 4`); `git diff HEAD -- <file> | grep '^-'` shows the header line is the ONLY match, i.e. **0** actual code deletions (satisfies the intent of the `0` criterion).
- **Files modified:** None (verification-only; no plan or source text was edited to work around this)
- **Verification:** Manual re-derivation shown above; both underlying acceptance criteria are satisfied in substance.
- **Committed in:** N/A (documentation of a verification-command quirk, not a code change)

---

**Total deviations:** 1 auto-fixed (Rule 3, verification-command level; no code, plan, or scope change)
**Impact on plan:** None on delivered functionality -- both flagged commands are self-inflicted shell/awk quirks in the plan's own acceptance-criteria text, not defects in the implementation. The underlying requirements they were meant to check are satisfied and independently re-verified above.

## Issues Encountered

None beyond the verification-command quirk documented above.

## User Setup Required

None -- no external service configuration required. `user_setup: []` per plan frontmatter.

## Next Phase Readiness

- WR-01, WR-02 and WR-04 are closed (D-17); WR-03 was already closed by `11242a7` and not reopened.
- No number in `notebooks/.cache/07_crossmodal_curvature.jsonl` was touched, recomputed, or reinterpreted by this plan.
- This plan runs in Wave 1 alongside 07.1-01 (already complete) on a disjoint file set; it produces no 07.1 pre-registration number and neither depends on nor blocks 07.1-01's freeze. 07.1-04 (which builds the stratified null and reuses `density_diagnostics`-shaped logic at far higher call volume) and 07.1-06 (FINDINGS) both inherit the now-guarded `density_diagnostics` and the extended runner test coverage.

## Self-Check: PASSED

All 4 modified files found on disk; SUMMARY.md found on disk; all 3 task commits (`fe229f8`, `dea4951`, `d2e8830`) found in git log.

---
*Phase: 07.1-density-stratified-null-and-seed-stability*
*Completed: 2026-08-27*
