---
phase: 07.1-density-stratified-null-and-seed-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/pu_manifold/crossmodal_curvature.py
  - notebooks/diagnostics/07_crossmodal_curvature_run.py
  - notebooks/pu_manifold/tests/test_crossmodal_curvature.py
  - notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py
autonomous: false
requirements: [D7.1-01, D7.1-02]
user_setup: []

must_haves:
  truths:
    - "`crossmodal_curvature.density_diagnostics` raises ValueError naming the offending array when `h` or `m` contains a non-finite value or is constant, matching `curvature_probe.permutation_null`'s guard-clause shape and message style."
    - "The new guards do NOT fire on Phase 7's actual d=20, d=25 and d=32 arrays reloaded from the frozen `07_crossmodal_curvature_fields.npz` — proven by a test that calls `density_diagnostics` on them, not asserted in prose (D-18)."
    - "`run_smoke` computes its distinct-value count by calling the file's existing `_distinct_value_count` helper, with no second relative-precision implementation anywhere in that function (WR-02)."
    - "`--mode smoke` prints the same `n_distinct` value before and after the WR-02 amendment, captured from two real runs rather than asserted (D-18's no-op proof)."
    - "`resolve_record_path(None)` returns the cache-resolved path for `RECORD_STEM`, and a supplied path outside the cache directory raises ValueError before the path is ever opened (WR-04)."
    - "`run_dsweep` exits 1 when `--smoke-rows` or `--max-epochs` is present in argv without `--record-path`, in both the `--flag value` and `--flag=value` argv forms (WR-04's `is_scratch` detection, exercised through the guard it protects because `is_scratch` is a local variable inside `run_dsweep`, not an importable symbol)."
  prohibitions:
    - statement: "The WR-01 no-op proof must not be worded, in any task text, test docstring, commit message or FINDINGS entry, as retroactively validating the frozen `07_crossmodal_curvature.jsonl`. `run_dsweep` never calls `density_diagnostics` — it inlines an equivalent computation — so the fix protects future call sites, not the sealed record's provenance."
      requirement: D7.1-01
  artifacts:
    - path: "notebooks/pu_manifold/crossmodal_curvature.py"
      provides: "density_diagnostics with finite/constant guards (WR-01)"
      contains: "density_diagnostics:"
    - path: "notebooks/pu_manifold/tests/test_crossmodal_curvature.py"
      provides: "WR-01 guard no-op-proof tests against Phase 7's real arrays"
    - path: "notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py"
      provides: "WR-04 resolve_record_path and scratch-detection coverage"
  key_links:
    - from: "notebooks/diagnostics/07_crossmodal_curvature_run.py"
      to: "notebooks/pu_manifold/crossmodal_curvature.py"
      via: "_distinct_value_count delegates to _relative_precision_distinct_count — the single count implementation WR-02 restores"
      pattern: "_relative_precision_distinct_count"
---

<objective>
Close Phase 7's remaining review debt — WR-01, WR-02 and WR-04 (D-17) — with each in-place
amendment carrying a demonstrated, not asserted, no-op proof (D-18).

Purpose: 07.1 adds a second runner to a codebase where WR-04 recorded that no test imported the
first one, and it re-runs the guard-clause and relative-precision-counting disciplines that WR-01
and WR-02 record as missing. Closing the debt here means 07.1's own new code inherits patterns
that are enforced rather than merely described.

Output: guards on `density_diagnostics`, the duplicate distinct-count removed from `run_smoke`,
and three test groups covering all of it.

This plan runs in Wave 1 alongside 07.1-01 because it touches an entirely disjoint file set —
the sealed Phase 7 module, the Phase 7 runner, and their two test files — and produces no 07.1
number, so it neither depends on nor blocks the pre-registration freeze.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-CONTEXT.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-PATTERNS.md
@.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-RESEARCH.md
@.planning/phases/07-curvature-conditioned-crossmodal-alignment/07-REVIEW.md
@CLAUDE.md

<interfaces>
<!-- Guard-clause donor and target signatures. Extracted from the tree at plan time. -->

Guard-clause donor — notebooks/pu_manifold/curvature_probe.py, `permutation_null` (lines ~1097-1119):
raises `ValueError` with a message beginning `"permutation_null: "` and naming the offending
array, in this order: non-finite on the first array, non-finite on the second, length mismatch,
then `np.ptp(...) == 0` constant checks on each.

Target — notebooks/pu_manifold/crossmodal_curvature.py, `density_diagnostics` (line 649):
```
def density_diagnostics(X_ambient, h, m, z_a, z_b, k, density_k, density_field_d) -> Dict[str, float]
```
Its first three executable lines coerce `X`, `h_arr`, `m_arr` with `np.asarray(..., dtype=np.float64)`
(`h`/`m` also `.ravel()`), then it calls `curvature_probe.local_density_weights(X, density_k,
density_field_d)` and takes `density = 1.0 / w`. It returns a flat dict of 11 plain-`float` keys.

Target — notebooks/diagnostics/07_crossmodal_curvature_run.py:
```
def _distinct_value_count(arr: np.ndarray) -> int          # line ~198, delegates to cc._relative_precision_distinct_count
def resolve_record_path(record_path_arg: Optional[str]) -> Path   # line ~207
def run_smoke(args: argparse.Namespace) -> str             # line ~234, the WR-02 site is inside it
def run_dsweep(args: argparse.Namespace) -> str            # line ~468, the is_scratch guard is at ~494-505
```
`run_dsweep`'s order of operations before any heavy work: `cc.assert_preregistered()` →
`_strict_ancestor_or_exit(args.freeze_commit)` → the scratch-detection guard → `sys.exit(1)`.
The real freeze SHA the gate accepts is the runner's own module constant
`FREEZE_COMMIT_SHA = "f032745f6450068c63763993d39fa112fd36bb8c"`.

Frozen arrays for the WR-01 no-op proof — notebooks/.cache/07_crossmodal_curvature_fields.npz
keys: `h_norm_20`, `h_norm_25`, `h_norm_32`, `cond_g_20`, `cond_g_25`, `cond_g_32`, `h_norm`, `cond_g`.
Ambient embeddings live in `notebooks/.cache/subsample_*.npz` under columns `hsc` / `legacysurvey`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Ratify amending sealed Phase 7 source in place (D-18)</name>
  <decision>
    D-18 amends two files D7-05 published as sealed ("import, never edit") after the Phase 7
    record was produced. Ratify the in-place amendment, and ratify the no-op proof that is what
    makes it defensible.
  </decision>
  <context>
    D-18 is rated **one-way** in `07.1-CONTEXT.md`: editing sealed source after the record was
    produced means the committed record was not generated by the current source. The no-op proof
    is what keeps that defensible; without it the change cannot simply be reverted out of the
    record's provenance story. Precedent for post-seal amendment: `c92260f` hardened
    CR-01/CR-02/CR-03 in-phase, and Phase 6 amended its own record serializer.

    One correction the developer should see before ratifying, from `07.1-RESEARCH.md` Pitfall 2
    (code-grounded, confirmed by grep): **`run_dsweep` — the function that produced the frozen
    jsonl — never calls `density_diagnostics` at all.** It inlines an equivalent computation.
    `density_diagnostics` is exercised only by its own unit tests. So WR-01's guards protect a
    function the sealed runner never reached. That does not make the fix worthless — it makes the
    *wording* of the proof load-bearing: the proof shows the guards do not fire on Phase 7's real
    arrays, and it says nothing about the frozen jsonl's provenance, because there is nothing to
    say.

    WR-02 is genuinely on a reachable path (`run_smoke`), and its two implementations currently
    agree numerically, so its proof is a real before/after comparison of a real run.
  </context>
  <options>
    <option id="amend-both-with-proofs">
      <name>Amend WR-01 and WR-02 in place, each under its own demonstrated no-op proof</name>
      <pros>Closes the debt where 07.1's own new code will copy the same patterns; both proofs
      are cheap and real; matches the `c92260f` and Phase 6 precedents.</pros>
      <cons>Sealed Phase 7 source is edited after its record exists; the provenance story now
      requires the proofs to be readable and kept.</cons>
    </option>
    <option id="wr02-only">
      <name>Amend WR-02 only; leave WR-01's dead-code function untouched</name>
      <pros>Edits only the one site that is actually on a reachable path; smallest possible
      change to sealed source.</pros>
      <cons>Leaves an unguarded function in a module 07.1's own module sits beside, with the
      guard-clause discipline documented but not enforced anywhere in it.</cons>
    </option>
    <option id="defer-both">
      <name>Defer both amendments out of 07.1</name>
      <pros>Sealed Phase 7 source stays byte-identical to what produced its record.</pros>
      <cons>Reverses D-17, which folds all three items into this phase; the debt stays unclaimed
      with 07.1's second runner compounding WR-04.</cons>
    </option>
  </options>
  <resume-signal>Select: amend-both-with-proofs, wr02-only, or defer-both</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-01 — finite/constant guards on density_diagnostics, with its no-op proof</name>
  <reversibility rating="one-way">Amends source D7-05 sealed after the Phase 7 record was produced; the no-op proof is what keeps the provenance story defensible (D-18).</reversibility>
  <precondition>notebooks/.cache/07_crossmodal_curvature_fields.npz exists and carries the keys h_norm_20, h_norm_25 and h_norm_32 (the frozen Phase 7 fields the no-op proof reloads)</precondition>
  <files>notebooks/pu_manifold/crossmodal_curvature.py, notebooks/pu_manifold/tests/test_crossmodal_curvature.py</files>
  <read_first>
    - notebooks/pu_manifold/crossmodal_curvature.py — `density_diagnostics`, lines 649-733 (the
      full function, including its docstring, so the guards land above the first statistic and
      the docstring's non-gating language is preserved)
    - notebooks/pu_manifold/curvature_probe.py — `permutation_null`'s guard block, lines
      1097-1119 (the exact clause order and message style to mirror)
    - notebooks/pu_manifold/tests/test_crossmodal_curvature.py — the existing
      `test_density_diagnostics_*` tests (6 of them) for the fixture and import conventions to
      match
    - .planning/phases/07-curvature-conditioned-crossmodal-alignment/07-REVIEW.md §WR-01
  </read_first>
  <behavior>
    - `density_diagnostics` with an `h` containing `np.nan` or `np.inf` raises `ValueError` whose
      message contains `density_diagnostics` and identifies `h`.
    - The same for a non-finite `m`.
    - `density_diagnostics` with a constant `h` (`np.ptp(h) == 0`) raises `ValueError` whose
      message contains `density_diagnostics` and identifies `h`; likewise for a constant `m`.
    - `density_diagnostics` with `h` and `m` of different lengths raises `ValueError` naming both
      lengths.
    - `density_diagnostics` called on Phase 7's REAL arrays — `h_norm_20`, `h_norm_25`,
      `h_norm_32` from the frozen npz, `m` from `per_point_mknn` on the two ambient columns at
      `HEADLINE_K`, `X_ambient` the `legacysurvey` column — returns a dict of 11 finite plain
      Python floats and raises nothing. This is the D-18 no-op proof.
    - Every previously passing `test_density_diagnostics_*` test still passes unchanged.
  </behavior>
  <action>
    Insert guard clauses into `crossmodal_curvature.density_diagnostics` immediately after the
    three `np.asarray` coercions and BEFORE the `local_density_weights` call, mirroring
    `curvature_probe.permutation_null`'s clause order and message style: non-finite check on the
    curvature array, non-finite check on the MKNN array, length-mismatch check between them, then
    a zero-peak-to-peak constant check on each. Every message begins `density_diagnostics: ` and
    names the offending argument by its public parameter name. Raise `ValueError`, matching the
    donor — not `TypeError`, not an assert.

    Do not touch the docstring's non-gating language, the `DENSITY_SIGN_CONVENTION` comment, the
    `1.0 / w` inversion, the statistic order, or the returned dict's keys. This is an additive
    guard insert; every number the function returns must be byte-identical for any input that
    previously returned successfully.

    Then extend `notebooks/pu_manifold/tests/test_crossmodal_curvature.py` with the tests named in
    `<behavior>`, all containing the substring `density_diagnostics_guard` in their function names
    so `-k density_diagnostics_guard` selects exactly them. The no-op-proof test loads the frozen
    npz via the cache module and skips (`pytest.skip`) with an explicit reason if the artifact is
    absent, rather than failing — the artifact is gitignored and may not exist on a fresh clone.

    Word the no-op-proof test's docstring and the commit message per D-18 and
    `07.1-RESEARCH.md` Pitfall 2: the proof establishes that the guards do not fire on Phase 7's
    real arrays and therefore protects future call sites. It establishes nothing about the frozen
    record, because the sealed sweep path never reached this function.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_crossmodal_curvature.py -k density_diagnostics -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_crossmodal_curvature.py -k density_diagnostics -q` exits 0 and reports at least 11 passed (the 6 pre-existing plus the 5 new guard tests), 0 failed.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_crossmodal_curvature.py -k density_diagnostics_guard -q` exits 0 and reports 0 failed and 0 errors.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` reports 0 failures (589 tests were collected before this plan).
    - `awk '/^def density_diagnostics/,/^def [a-z_]+\(/' notebooks/pu_manifold/crossmodal_curvature.py | grep -c 'raise ValueError' prints a value >= 4`.
    - `git diff <pre-task-sha> -- notebooks/pu_manifold/crossmodal_curvature.py | grep -c '^-'` prints `0` — the amendment adds lines only, removing none.
  </acceptance_criteria>
  <done>`density_diagnostics` raises a named `ValueError` on non-finite, constant and length-mismatched input; a test proves the guards do not fire on Phase 7's real d=20/25/32 arrays; every pre-existing test still passes.</done>
</task>

<task type="auto">
  <name>Task 3: WR-02 dedupe with its before/after no-op proof, and WR-04 runner coverage</name>
  <reversibility rating="one-way">Same sealed-source amendment boundary as Task 2 (D-18); the before/after `--mode smoke` capture is the proof.</reversibility>
  <files>notebooks/diagnostics/07_crossmodal_curvature_run.py, notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py</files>
  <read_first>
    - notebooks/diagnostics/07_crossmodal_curvature_run.py — `_distinct_value_count` (~line 198),
      `resolve_record_path` (~207), `run_smoke` in full (~234-303, the WR-02 site is the two
      lines immediately after the `per_point_mknn` call and its `t_mknn` timing line — re-grep
      for the current line numbers, do NOT trust `07.1-CONTEXT.md`'s cited 246-247, which
      `c92260f` already shifted once), `run_dsweep`'s scratch guard (~486-505),
      `build_arg_parser` (~791)
    - notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py — the whole file (161 lines);
      its module-scoped `runner` fixture built with `importlib.util.spec_from_file_location` is
      the fixture the new tests reuse. Do NOT create a second fixture and do NOT recreate this
      file — it already exists from `c92260f` with 15 passing tests.
    - notebooks/pu_manifold/cache.py — `cache_path` and `_assert_inside_cache` (lines 39-56)
    - .planning/phases/07-curvature-conditioned-crossmodal-alignment/07-REVIEW.md §WR-02, §WR-04
  </read_first>
  <action>
    **Step A — capture the "before" number.** Run
    `.venv/bin/python notebooks/diagnostics/07_crossmodal_curvature_run.py --mode smoke` and
    record the printed `n_distinct=` value verbatim into the plan SUMMARY. Do this BEFORE editing
    anything. If the run fails for an environmental reason, halt and report — do not proceed to
    the edit without a captured baseline, because the baseline IS the proof.

    **Step B — WR-02.** In `run_smoke`, delete the two-line inline relative-precision distinct
    count and replace it with a call to the file's own `_distinct_value_count` helper, which is
    defined a few dozen lines above and already delegates to
    `crossmodal_curvature._relative_precision_distinct_count`. The local `scale` variable becomes
    dead and goes with it. Nothing else in `run_smoke` changes — not the print format, not the
    timing lines, not the `HEADLINE_K + 1` bound it prints alongside.

    **Step C — capture the "after" number.** Re-run the identical `--mode smoke` command and
    record the printed `n_distinct=` value. Assert it equals Step A's value. If it differs, that
    is a real finding under D-18 — halt and report it rather than adjusting either side.

    **Step D — WR-04.** Extend `notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py`
    (never recreate it) using its existing module-scoped `runner` fixture, adding two groups:

    `resolve_record_path` — three tests: (1) `resolve_record_path(None)` returns a path equal to
    `cache.cache_path(cc.RECORD_STEM, "jsonl")`, i.e. it ends with `07_crossmodal_curvature.jsonl`
    and lives under the cache directory; (2) a path constructed inside the cache directory is
    returned unchanged; (3) a traversal path pointing outside the cache directory raises
    `ValueError` and the file is never created (assert the path does not exist afterwards).

    Scratch detection — three tests exercising the guard `is_scratch` protects. `is_scratch` is a
    local variable inside `run_dsweep`, not an importable symbol, so test it through the behaviour
    it gates: monkeypatch `sys.argv` to include `--smoke-rows 100` (space form) with
    `args.record_path = None` and the runner's own `FREEZE_COMMIT_SHA` as `args.freeze_commit`,
    call `run_dsweep`, and assert it raises `SystemExit` with code 1 — reached before
    `load_pu_pair`, so no PU data is loaded and nothing is trained. Repeat for `--max-epochs=3`
    (equals form, which is exactly the CR-03 shape). Third test: with a production argv carrying
    neither flag, the same guard does not fire — assert the run gets past it by monkeypatching
    `load_pu_pair` to raise a sentinel exception and asserting that sentinel, not `SystemExit`,
    is what propagates.

    Commit Steps B and D separately so the WR-02 amendment's diff is reviewable on its own.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py -q` exits 0 and reports at least 21 passed (15 pre-existing + 6 new), 0 failed.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py -k "resolve_record_path or scratch" -q` exits 0 and reports exactly 6 passed.
    - The `n_distinct=` value printed by `.venv/bin/python notebooks/diagnostics/07_crossmodal_curvature_run.py --mode smoke` is identical before and after Step B, and both captured values appear verbatim in the plan SUMMARY.
    - `awk '/^def run_smoke/,/^def run_positive_control/' notebooks/diagnostics/07_crossmodal_curvature_run.py | grep -v '^\s*#' | grep -c 'np\.unique'` prints `0`.
    - `awk '/^def run_smoke/,/^def run_positive_control/' notebooks/diagnostics/07_crossmodal_curvature_run.py | grep -c '_distinct_value_count' prints a value >= 1`.
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` reports 0 failures.
  </acceptance_criteria>
  <done>`run_smoke` has one distinct-count implementation, proven behaviour-preserving by a captured before/after `--mode smoke` comparison; `resolve_record_path`'s two branches and the scratch-detection guard both have tests in the existing runner test file.</done>
</task>

</tasks>

<planner_contributions>
Detector outcomes for this plan are recorded once, in `07.1-01-PLAN.md`'s
`<planner_contributions>` block (API-coverage: not detected; assumption-delta: not detected,
`no-change`; schema-push: no ORM files; security: threat models present in every plan). They are
phase-wide results, not per-plan ones.
</planner_contributions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sealed Phase 7 source → the frozen Phase 7 record | Editing source after its record exists breaks the "the record was produced by this source" claim unless the edit is proven a no-op |
| user-supplied `--record-path` → the cache directory | A traversal segment could write outside `notebooks/.cache/` |
| caller arrays → `density_diagnostics` | Non-finite or constant input silently produces `NaN`, which `json.dumps` emits as the non-standard `NaN` token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-05 | Tampering | in-place amendment of sealed Phase 7 source (D-18) | high | mitigate | Both amendments carry a demonstrated no-op proof: a captured before/after `--mode smoke` `n_distinct` comparison (WR-02) and guard-non-firing tests on Phase 7's real d=20/25/32 arrays (WR-01); WR-01's proof is explicitly scoped to future call sites, never to the frozen jsonl |
| T-07.1-06 | Tampering | path traversal through `--record-path` on the Phase 7 runner | high | mitigate | `resolve_record_path` already routes a supplied path through `cache._assert_inside_cache`; this plan adds the missing test for both branches, including a traversal path that must raise before any write (WR-04). Canon path-traversal coverage otherwise belongs to `/gsd-secure-phase` — not minted as a bespoke prohibition |
| T-07.1-07 | Information disclosure | non-finite value reaching `json.dumps` and emitting the non-standard `NaN` token | medium | mitigate | WR-01's finite guards raise `ValueError` naming the offending array before any statistic is computed |
| T-07.1-08 | Tampering | a reduced-scale exercise run landing in the frozen record | medium | mitigate | `run_dsweep`'s scratch guard exits 1 without `--record-path`; this plan adds the tests proving it fires in both argv forms and does not fire on a production argv |
| T-07.1-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed — `07.1-RESEARCH.md` §Package Legitimacy Audit records "Not applicable"; nothing to gate |
</threat_model>

<artifacts_this_phase_produces>
Symbols created or changed by THIS plan:

**Extended `notebooks/pu_manifold/tests/test_crossmodal_curvature.py`** (existing file, additive)
- `test_density_diagnostics_guard_rejects_nonfinite_h`
- `test_density_diagnostics_guard_rejects_nonfinite_m`
- `test_density_diagnostics_guard_rejects_constant_h`
- `test_density_diagnostics_guard_rejects_constant_m`
- `test_density_diagnostics_guard_does_not_fire_on_phase7_arrays`

**Extended `notebooks/pu_manifold/tests/test_crossmodal_curvature_run.py`** (existing file from
`c92260f`, additive — 15 tests already present, reusing its module-scoped `runner` fixture)
- `test_resolve_record_path_defaults_to_the_cache_record_stem`
- `test_resolve_record_path_accepts_a_path_inside_the_cache`
- `test_resolve_record_path_rejects_a_traversal_path`
- `test_scratch_guard_exits_on_smoke_rows_without_record_path`
- `test_scratch_guard_exits_on_max_epochs_equals_form_without_record_path`
- `test_scratch_guard_does_not_fire_on_a_production_argv`

**Amended (no new symbols):** `crossmodal_curvature.density_diagnostics` gains guard clauses;
`07_crossmodal_curvature_run.run_smoke` loses its inline count in favour of the existing
`_distinct_value_count` helper. No public signature, return shape or key name changes.
</artifacts_this_phase_produces>

<verification>
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` — 0 failures.
- Both D-18 no-op proofs recorded in the SUMMARY: the captured before/after `n_distinct` pair,
  and the guard-non-firing test's pass line.
- `git log --oneline` shows WR-01, WR-02 and WR-04 as separate, individually reviewable commits.
</verification>

<success_criteria>
- WR-01, WR-02 and WR-04 are closed (D-17), each amendment demonstrated a no-op rather than
  asserted one (D-18).
- The WR-01 finding is worded as protecting future call sites, never as validating the frozen
  jsonl.
- No number in `notebooks/.cache/07_crossmodal_curvature.jsonl` is touched, recomputed or
  reinterpreted.
</success_criteria>

<output>
Create `.planning/phases/07.1-density-stratified-null-and-seed-stability/07.1-02-SUMMARY.md`
when done. Record the checkpoint selection, the captured before/after `n_distinct` values, and
the re-grepped current line numbers of the WR-02 site.
</output>
