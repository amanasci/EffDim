# Phase 07.1: Density-Stratified Null and Seed Stability - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-26
**Phase:** 07.1-density-stratified-null-and-seed-stability
**Areas discussed:** Density stratum design, What the null permutes, Seed axis at d=25, 07.1's verdict rule + WR debt

---

## Density stratum design

**Q1 — How should density strata be defined for the within-stratum permutation?**

| Option | Description | Selected |
|--------|-------------|----------|
| Equal-count quantile bins | Cut on density RANK; immune to the 7.8-order spread, uniform permutation entropy, same rank scale `partial_spearman` works in | ✓ |
| Equal-width bins in log-density | Respects the multiplicative nature of a k-NN density estimate, but bin populations come out wildly unequal | |
| You decide | Claude picks and records the reasoning | |

**Q2 — How many density strata for the headline (gating) null at n=10,000?**

| Option | Description | Selected |
|--------|-------------|----------|
| 20 strata, 500 points each | Middle of the defensible range; 500 pts far above `partial_spearman`'s 3-point floor | ✓ |
| 10 strata, 1000 points each | Conservative; wider band risks calling the d=20 / d=32 residuals noise | |
| 50 strata, 200 points each | Tightest control, narrowest band; risks clearing on stratification tightness | |

**Notes:** Claude flagged before the question that finer strata make the test MORE sensitive, not
more conservative — within-stratum permutation constrains movement, so the permuted partial varies
less and the band narrows.

**Q3 — Report a non-gating strata sensitivity grid alongside the headline S=20 null?**

| Option | Description | Selected |
|--------|-------------|----------|
| Thresholds at S = 10, 20, 50 | Exposes whether a residual cleared on tightness; 3x permutation cost; only S=20 gates | ✓ |
| Headline S=20 only | Cheapest, least ambiguous; no way to detect a stratification artifact | |
| Wider grid (S = 5..100) | Fuller curve, more cost, more cherry-pick surface | |

**Notes:** Claude flagged the structural difference from `MKNN_K_GRID` — the observed statistic is
invariant to `S`, so this is a grid of thresholds, not of point estimates.

**Q4 — Does the stratified null need its own positive control before it can license a "not real" reading?**

| Option | Description | Selected |
|--------|-------------|----------|
| Plant against the partial | Extend `plant_positive_control`'s bisection to target `partial_rho_density_controlled` | ✓ |
| Inherit D7-02's evidence | Cheaper; but the partial residualizes both arrays first, so an inherited floor is an assumption | |
| Negative control only | Confirms the null is unbiased, says nothing about detection floor | |

---

## What the null permutes

**Preamble:** Claude established that `curvature_probe.permutation_null` cannot be reused — it
delegates to `scipy.stats.permutation_test` with `permutation_type="pairings"`, which re-pairs
globally and takes no stratum argument.

**Q1 — Which array gets shuffled within strata to build the null?**

| Option | Description | Selected |
|--------|-------------|----------|
| MKNN only | Leaves `h`'s joint distribution with density intact; cleanest attribution to the MKNN link | |
| Both, independently | Mirrors `permutation_type="pairings"`; `h`'s residualization also varies per permutation | ✓ |
| Curvature only | Mirror of option 1; disturbs the stronger density association (0.4281 vs -0.2121) | |

**Notes:** This was the one question where the user did not take Claude's recommendation. The
trade-off — the band mixes both residualization sides and is not attributable to the MKNN link
alone — is recorded in CONTEXT.md D-05 as something FINDINGS must state.

**Q2 — Where does the OBSERVED partial come from — recomputed, or lifted from Phase 7's frozen record?**

| Option | Description | Selected |
|--------|-------------|----------|
| Recompute, then assert equality | Proves the frozen field and reload path are intact; a mismatch is a real finding | ✓ |
| Lift the frozen values verbatim | Zero drift risk, but no independent check of the reload path | |
| Recompute only, no assertion | Simplest; a disagreement with Phase 7 would go uncaught | |

**Q3 — Phase 7's permutation constants — inherit the values, or mint fresh ones for 07.1?**

| Option | Description | Selected |
|--------|-------------|----------|
| Re-declare same values in 07.1's module | Same numbers under 07.1's own `assert_preregistered` + git-ancestry freeze | ✓ |
| Import Phase 7's constants directly | No drift possible, but the freeze proof no longer spans 07.1's gating constants | |
| Mint different values | Tunable to the stratified null; any difference needs justifying in the write-up | |

---

## Seed axis at d=25

**Preamble:** Claude confirmed from the source that MKNN is computed between the `hsc` and
`legacysurvey` ambient embeddings and density on `legacysurvey_ambient_768` — neither passes
through the autoencoder — so across three seeds only `h_norm` changes.

**Q1 — Which seed varies across the three d=25 runs?**

| Option | Description | Selected |
|--------|-------------|----------|
| TORCH_INIT_SEED only | Line 157 drives weight init and minibatch order; split held fixed, isolates the fitted field | ✓ |
| TORCH_INIT_SEED and SPLIT_SEED together | Broader stability notion; two sources of variation, three observations | |
| SPLIT_SEED only | Isolates data-partition sensitivity; weaker fit for D7.1-02's stated question | |

**Q2 — Pre-registered combination rule — what makes d=25's result "seed-stable"?**

| Option | Description | Selected |
|--------|-------------|----------|
| All 3 clear, else SPLIT | Inherits Phase 5's `05-03-DECISION.md` ratification; 2-of-3 is terminal non-supportive | ✓ |
| Majority — 2 of 3 clear | More likely positive; contradicts the ratification the roadmap says 07.1 inherits | |
| Report spread, no verdict | Honest and cheap; leaves D7.1-02 with no answer and invites post-hoc reading | |

**Q3 — Which statistic gates each seed's clear/no-clear at d=25?**

| Option | Description | Selected |
|--------|-------------|----------|
| D7.1-01's stratified partial | The load-bearing quantity; creates a wave dependency on D7.1-01 | ✓ |
| Both raw and partial must clear | Strictest; doubles failure modes on a statistic the phase isn't adjudicating | |
| Raw statistic only | Lets both deliverables run in parallel; tests a number already known to be ~49% density | |

**Q4 — PERMUTATION_SEED across the three d=25 runs — fixed or varied?**

| Option | Description | Selected |
|--------|-------------|----------|
| Fixed at 20260825 | Identical permutation index sets; threshold differences come from the field alone | ✓ |
| Vary with the fit seed | Fuller picture of run-to-run variability; field instability and null noise become inseparable | |

---

## 07.1's verdict rule + WR debt

**Preamble:** Claude noted Phase 7's `apply_verdict` raises unless `per_d_results` keys are exactly
`set(D_SWEEP)`, and its signature structurally cannot accept a density number.

**Q1 — What verdict shape does 07.1 freeze?**

| Option | Description | Selected |
|--------|-------------|----------|
| Two independent verdicts, one per deliverable | Each answers its own question; a null in D7.1-01 must not silently void the seed work | ✓ |
| Per-d / per-seed table, no rolled-up verdict | The goal names individual residuals; but breaks the mechanical-verdict discipline | |
| One combined phase verdict | Simplest headline; mixes two axes into one word | |

**Q2 — D7.1-01's likely outcome is "d=25 clears, d=20 and d=32 don't". How should the frozen rule name that?**

| Option | Description | Selected |
|--------|-------------|----------|
| Its own verdict value, per-d table always reported | A genuine finding, not a failure to reach one; Phase 7's `SPLIT ACROSS d` would bury it | ✓ |
| Reuse Phase 7's vocabulary verbatim | Max consistency, zero new vocabulary; labels the likely result the same as a failure | |
| You decide | Claude picks and freezes it before compute | |

**Q3 — Phase 7's unclaimed review debt — fold into 07.1, or leave it? (multi-select)**

| Option | Description | Selected |
|--------|-------------|----------|
| WR-01 — `density_diagnostics` guards | No finite/constant guard; a NaN reaches `json.dumps`. 07.1 drives this path per permutation | ✓ |
| WR-02 — duplicate distinct-count in `run_smoke` | Two-line fix; the duplication class behind 05-02's retracted miscount | ✓ |
| WR-04 — runner test coverage | 157 tests, none touching the runner where CR-01..03 all lived; 07.1 adds a second runner | ✓ |
| None — leave all three | Smallest scope | |

**Notes:** WR-03 already closed by commit `11242a7`; not reopened.

**Q4 — How should WR-01 and WR-02 be applied given D7-05's seal on those files?**

| Option | Description | Selected |
|--------|-------------|----------|
| Amend in place, with a re-run proof | Demonstrate the no-op: re-run `--mode smoke`, unit-test the guards don't fire on Phase 7's arrays | ✓ |
| Re-implement guarded versions in 07.1's module | Honors the seal absolutely; leaves the defects open and duplicates `density_diagnostics` | |
| Amend in place, no re-run proof | Cheapest; asserts a no-op rather than demonstrating it | |

**Notes:** Claude surfaced the conflict with D7-05 ("import, never edit") unprompted, along with the
mitigating fact that both fixes are provably no-ops on Phase 7's realized path, and the existing
precedent (`c92260f`, and Phase 6's record-serializer amendment).

---

## Claude's Discretion

- Module and runner naming and layout for 07.1's new code.
- The positive control's target grid for D-04 — Phase 7's `(0.02, 0.05, 0.10, 0.20)` could not
  resolve the floor within 0.021-0.05, which argues for a finer low end.
- The two additional `TORCH_INIT_SEED` values.
- Reporting artifacts — default is that 07.1 writes its own FINDINGS.md and notebook, reopens no
  Phase 7 verdict, and declares CLAUDE.md's Swiss roll gate satisfied as `07-FINDINGS.md` Sec 4 did.
  Offered as its own gray area; user chose to close discussion and take the default.
- Serial-run discipline and thread caps.

## Deferred Ideas

- The full 3x3 seed x d grid (~7h vs ~2h) — explicitly scoped out by the ROADMAP.
- A stratified null for the raw statistic, for a like-for-like raw-vs-partial comparison.
- CKA as a second alignment probe — remains out of scope by D7-07.
- Whether PU's reconstruction is capacity-limited rather than dimension-limited.
