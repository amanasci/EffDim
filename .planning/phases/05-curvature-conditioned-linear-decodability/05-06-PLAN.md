---
phase: 05-curvature-conditioned-linear-decodability
plan: 06
type: execute
wave: 6
depends_on: [05-05]
files_modified:
  - notebooks/05_curvature_conditioned_linear_decodability.ipynb
  - .planning/phases/05-curvature-conditioned-linear-decodability/05-FINDINGS.md
  - .planning/phases/05-curvature-conditioned-linear-decodability/05-VERIFICATION.md
autonomous: false
requirements: [D5-04, D5-05, D5-07, D5-08, D5-09, D5-11, D5-12, D5-13]

flagged_assumptions:
  - "AMENDED PLAN. The phase reports THREE per-seed verdicts and their spread plus one combined phase verdict, not one pooled verdict. 05-CONTEXT.md D5-04's pooled field was superseded by 05-03-DECISION.md at the 05-03 Task 1 blocking checkpoint. The question the original plan asked of a pooled diagnostic -- `is the pooled field an artifact of averaging fields that share no signal` -- is answered by that decision record rather than by a measurement, because no pooled field was ever built. 05-FINDINGS.md must say so in those terms."
  - "The git-ancestry proof is the phase's only fully intact guarantee and it is proved mechanically, not asserted. If the ancestry check fails, or if the diff of the frozen constants file between the freeze commit and HEAD is non-empty, 05-VERIFICATION.md records the failure at full strength and 05-FINDINGS.md states that the pre-registration was breached. It is never repaired by amending a commit or by rewording the claim."
  - "notebooks/pu_manifold/linear_probe.py now has THREE commits in its history, not two: creation at 05-01, the per-seed structural repair at 05-03 (constants still unset), and the freeze at 05-04. The count changed because the amendment restructured the constants block BEFORE the freeze so the frozen file could carry the ratified three-verdict design. The ordering guarantee is unaffected -- it constrains what happens AFTER the freeze -- but 05-VERIFICATION.md must quote the three-commit log and explain the middle commit rather than let a reader find an unexplained third entry."
  - "05-FINDINGS.md must state D5-11's and D5-12's consequences in the phase's own words rather than by cross-reference. 04-FINDINGS.md set that standard and 05-VALIDATION.md lists prose adequacy as a manual-only verification, because no test can judge whether a paragraph is honest."
  - "The Swiss roll determination is recorded as a visible decision, not a silent skip: CLAUDE.md's rule targets models that map data to a lower-dimensional representation and back or claim to recover manifold structure. A ridge map from 768 dimensions to 768 dimensions has no bottleneck, no latent space and no decoder, and this phase writes no estimator. The per-seed amendment introduced no new model and does not change the determination. The estimator this phase reads from is already covered by notebooks/03_swiss_roll_chart_curvature_field_check.ipynb."
  - "Spec-less probe fallback, edge half. The phase has thirteen requirement IDs (D5-01..D5-13) and the deterministic edge probe surfaced 29 applicable items across them, ALL unresolved on entry. Across the four amended plans, 23 are authored into must_haves.truths (18 plain, 5 as backstop markers) and 6 unclassified rows are surfaced as flagged assumptions. 23 + 6 = 29: no silent drops. This plan authors 5 truths (3 of them backstop markers) and surfaces 1 unclassified row. NOTE: 05-06's earlier `spec-less probe fallback skipped -- phase has no requirement IDs to probe` claim is RETRACTED; the phase does have requirement IDs and the probe did run."
  - "EDGE PROBE UNRESOLVED (D5-11, category `unclassified`): `unclassified -- review manually`. Not auto-backstopped. Surfaced here as a flagged assumption. D5-11 (`Phase 5 runs with no known-answer anchor, and this is a deliberate choice`) is an accepted-gap declaration, not a data-shape constraint, so no edge category applies. Its content is verified as a prose-adequacy judgement at this plan's blocking human checkpoint, plus literal-presence assertions for the sealed rank value and the not-for-autonomous-action clause."

must_haves:
  truths:
    - "notebooks/05_curvature_conditioned_linear_decodability.ipynb exists, is executed end to end, and is committed with its outputs, per CLAUDE.md"
    - "The notebook imports the phase's code unchanged from notebooks/pu_manifold/ and notebooks/diagnostics/ and reimplements nothing; it fits no probe, extracts no curvature field, and constructs no field from more than one seed"
    - "05-FINDINGS.md reports all three per-seed verdicts, their spread, and the single combined phase verdict, and states that the phase verdict was produced by the module's combination function from a rule frozen in an ancestor commit (D5-04, D5-09)"
    - "05-FINDINGS.md states that no pooled field was ever built, names 05-03-DECISION.md as superseding 05-CONTEXT.md D5-04, quotes the measured evidence the decision was made on, and answers the pooled-artifact question from that decision record rather than from a diagnostic (D5-04, D5-05)"
    - "05-FINDINGS.md records the disposition of D5-05's pooled-versus-seed half -- no referent, not computed against a substitute -- rather than dropping it or reporting it as satisfied (D5-05)"
    - "05-FINDINGS.md states the corrected effective distinct-level counts of 4 for seed 20260814 and 3 for seed 20260815 at every relative tolerance from 1e-9 to 1e-3, and contains neither of 05-02-SUMMARY.md's retracted exact counts 5,301 and 9,852 (D5-05)"
    - "05-FINDINGS.md exists and states, in the phase's own words and without deferring to a cross-reference, that the curvature field has no demonstrated relationship to true curvature, quoting the sealed d=20 decoder row rank_spearman_rho = -0.015106571347065712, and that a detected effect therefore cannot be attributed to curvature by anything in this phase (D5-11)"
    - "05-FINDINGS.md quotes the sealed rank value at full precision with an ASCII hyphen-minus, never rounded to -0.0151 and never with a unicode minus, and a fixed-string grep finds it (D5-11)"
    - "05-FINDINGS.md states that the CAE underlying the decoder failed its own validity gate with CAE_VERDICT = FAIL at Phase 02.2, that Phase 3 runs on a deliberate override, and that Phase 03.1 found the ordering only partially and non-seed-consistently moved (D5-12)"
    - "05-FINDINGS.md reports all three seeds' realized test-split bucket counts beside their full-field counts and all three size-matched results, and names Phase 4's region-size artifact as the reason that check exists (D5-08)"
    - "05-FINDINGS.md reports the three re-measured per-seed spearman(density, curvature magnitude) values and states they are a disclosure and not a gate (D5-13)"
    - "05-VERIFICATION.md proves the ordering guarantee mechanically: the freeze commit is an ancestor of the first probe-number commit, and the diff of the frozen constants file between the freeze commit and HEAD is empty, with both commands and both outputs quoted (D5-09)"
    - "05-VERIFICATION.md quotes the full commit log of notebooks/pu_manifold/linear_probe.py -- exactly three commits -- and names what each one was, so the pre-freeze structural repair is explained rather than left as an unexplained entry (D5-09)"
    - "The phase verdict and all three per-seed verdicts reported in 05-FINDINGS.md are byte-identical to the strings in the probe_overall and probe_seed JSONL rows (D5-09)"
    - statement: "05-FINDINGS.md's accepted-gaps section reaches the standard 04-FINDINGS.md set: each gap is stated at full strength in the phase's own words, and none is softened by pointing at another document"
      verification: backstop
    - statement: "The frozen VERDICT_RULE and SEED_VERDICT_COMBINATION_RULE, read as text, make no detectable relationship and split-across-seeds each a complete terminal outcome rather than a near-miss of the positive branch"
      verification: backstop
    - statement: "The Phase 02.2 FAIL verdict and the Phase 3 deliberate override are reported as two distinct facts that stand together, and neither is presented as cancelling, superseding or excusing the other"
      verification: backstop
    - statement: "If any document in the inherited chain is missing or unreadable at write-up time, 05-FINDINGS.md records the absence and what could not be verified, rather than omitting the paragraph that would have cited it"
      verification: backstop
    - statement: "The inherited chain is stated in phase order -- Phase 02.2's FAIL, then Phase 3's override, then Phase 03.1's partial repair, then Phase 5 -- so a reader can see which decision came first and what each later one was made in spite of"
      verification: backstop
  prohibitions:
    - "MUST NOT modify notebooks/pu_manifold/linear_probe.py. If a defect in it is found here, record a pre-registration BREACH with the diff and the reason; do not patch it"
    - "MUST NOT re-run the probe, re-bucket any seed, pool the seeds, or re-word either verdict at write-up time; the findings report what the frozen rules produced"
    - "MUST NOT present a SPLIT ACROSS SEEDS outcome as partial support for the hypothesis, as a majority result, or as a reason to report the agreeing seeds and set the third aside. The frozen combination rule already states why one or two seeds out of three is a property of those decoder fits and not of the manifold"
    - "MUST NOT present D5-11's declined low-d anchor as resolved, mitigated or compensated. The saddle-fixture question is open and explicitly not for autonomous action"
    - "MUST NOT use the continuous Spearman, the inter-seed diagnostics, the density numbers or any non-headline bucket to soften or upgrade any verdict at either level"
    - "MUST NOT carry 05-02-SUMMARY.md's 5,301 / 9,852 exact-distinct-value claim into 05-FINDINGS.md; the corrected effective level counts are 4 and 3"
    - "MUST NOT delete or rewrite any existing notebook or runner script; this plan adds one notebook and two planning documents (CLAUDE.md: additive only)"
  artifacts:
    - path: "notebooks/05_curvature_conditioned_linear_decodability.ipynb"
      provides: "The executed, committed notebook reading the phase's cached artifacts"
      contains: "linear_probe"
    - path: ".planning/phases/05-curvature-conditioned-linear-decodability/05-FINDINGS.md"
      provides: "The phase record: what it claims, what it deliberately does not, the frozen configuration, three per-seed verdicts and their spread, and the accepted gaps"
      contains: "-0.015106571347065712"
    - path: ".planning/phases/05-curvature-conditioned-linear-decodability/05-VERIFICATION.md"
      provides: "The mechanical proof of the ordering guarantee from git ancestry, including the explained three-commit history of the frozen file"
      contains: "merge-base"
  key_links:
    - from: ".planning/phases/05-curvature-conditioned-linear-decodability/05-FINDINGS.md"
      to: "notebooks/.cache/05_curvature_probe_decodability.jsonl"
      via: "every number in the findings is quoted from a record row, not recomputed at write-up time"
      pattern: "probe_overall"
    - from: ".planning/phases/05-curvature-conditioned-linear-decodability/05-FINDINGS.md"
      to: ".planning/phases/05-curvature-conditioned-linear-decodability/05-03-DECISION.md"
      via: "the pooled-field question is answered from the ratified decision record, since no pooled diagnostic exists"
      pattern: "05-03-DECISION"
    - from: ".planning/phases/05-curvature-conditioned-linear-decodability/05-VERIFICATION.md"
      to: "notebooks/pu_manifold/linear_probe.py"
      via: "the empty-diff assertion and the three-commit log both name the frozen constants file explicitly"
      pattern: "linear_probe.py"
    - from: "notebooks/05_curvature_conditioned_linear_decodability.ipynb"
      to: "notebooks/pu_manifold/linear_probe.py"
      via: "the notebook imports the frozen module unchanged and quotes its constants and both frozen rules"
      pattern: "from pu_manifold import"
---

<objective>
Close the phase: an executed notebook that shows three per-seed results and their spread, a findings
document that states what those results are worth, and a verification document that proves the one
guarantee this phase can make.

Purpose: Phase 5's numbers sit on a chain of acknowledged weaknesses — a CAE that failed its own
validity gate, a curvature field with no demonstrated relationship to true curvature, three seeds
MEASURED to share no signal, and a density confound that had to be re-measured rather than inherited.
None of that is a reason not to report the result. It is a reason to report it with each weakness
named at full strength, in the phase's own words, so nobody downstream inherits a number without
inheriting what it rests on. `04-FINDINGS.md` set that standard and this document has to match it.

The amendment changes what "the result" is. There is no pooled verdict field: at the `05-03` Task 1
blocking checkpoint the developer ratified, one-way, that the three seeds are not pooled
(`05-03-DECISION.md`, superseding `05-CONTEXT.md` D5-04). The phase reports three per-seed verdicts,
their spread, and one combined phase verdict produced by a rule frozen before any of the three
existed. The question the original plan meant to answer with a pooled diagnostic — whether a pooled
field would have been an artifact of averaging fields that share no signal — is answered by that
decision record, on measured evidence, rather than by building the artifact in order to criticize it.

Output: `notebooks/05_curvature_conditioned_linear_decodability.ipynb` (executed, committed with
outputs), `05-FINDINGS.md`, `05-VERIFICATION.md`.

**Retraction carried into the phase record:** an earlier draft of this plan recorded "spec-less probe
fallback skipped — the phase has no requirement IDs to probe". That is wrong. The phase has thirteen
requirement IDs, D5-01 through D5-13, and the deterministic edge probe ran over them, surfacing 29
applicable items. All 29 are accounted for across the four amended plans: 23 authored into
`must_haves.truths` and 6 unclassified rows surfaced as flagged assumptions. `05-FINDINGS.md` records
the retraction.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.claude/skills/spike-findings-effdim/SKILL.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-03-DECISION.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-CONTEXT.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-PREREGISTRATION.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-05-SUMMARY.md
@.planning/phases/04-region-partitioning-regional-alignment-mknn/04-FINDINGS.md
@.planning/phases/04-region-partitioning-regional-alignment-mknn/04-VERIFICATION.md
</context>

<superseded_decision>
`05-CONTEXT.md` **D5-04** ("pool the three cached CAE seeds into one averaged `||H||` field; the pooled
field is the verdict field") is **superseded** by `05-03-DECISION.md`. No pooled field exists.
`05-CONTEXT.md` **D5-05**'s pooled-versus-seed half has no referent and is dispositioned in
`notebooks/.cache/05_density_diagnostics.json` and in `05-PREREGISTRATION.md`; `05-FINDINGS.md`
restates that disposition. Every other D5 decision stands.
</superseded_decision>

<artifacts_this_phase_produces>
Symbols and files Phase 5 CREATES (all new — exclude from drift/source-grounding checks).

**`notebooks/pu_manifold/linear_probe.py`** — NEW module, FROZEN at `05-04`, read-only here. 31
constants: `TRAIN_FRACTION`, `SPLIT_SEED`, `SPLIT_RULE`, `RIDGE_ALPHA_GRID`, `RIDGE_SELECTION_RULE`,
`ALPHA_PER_TARGET`, `FIT_INTERCEPT`, `EMBEDDING_PREPROCESSING`, `RESIDUAL_METRIC`, `R2_MULTIOUTPUT`,
`N_BUCKETS`, `BUCKET_RULE`, `BUCKET_EDGES_PER_SEED`, `SEED_HANDLING_RULE`,
`SEED_VERDICT_COMBINATION_RULE`, `PHASE_VERDICT_VALUES`, `SEED_STEMS`, `N_CHARTS`, `CURVATURE_MODE`,
`CURVATURE_CONVENTION`, `CURVATURE_SOURCE_FUNCTION`, `SIZE_MATCH_RULE`, `SIZE_MATCH_N_REPEATS`,
`SIZE_MATCH_SEED`, `N_BOOTSTRAP`, `BOOTSTRAP_SEED`, `CONFIDENCE_LEVEL`, `K_DENSITY`, `FIELD_D`,
`VERDICT_RULE`, `PREREGISTRATION_PATH`. Functions `train_test_split_indices`, `fit_probe`,
`predict_probe`, `per_point_residuals`, `aggregate_r2`, `pool_seed_fields` (retained, unused),
`bucket_edges_from_field`, `assign_buckets`, `bucket_by_field`, `bucket_counts`,
`bucket_residual_ci`, `size_matched_check`, `apply_verdict_rule`, `combine_seed_verdicts`,
`assert_preregistered`.

**Terminal outcome strings:** per-seed `HOLDS`, `NO DETECTABLE RELATIONSHIP`; phase-level
`HOLDS IN ALL THREE SEEDS`, `SPLIT ACROSS SEEDS`, `NO DETECTABLE RELATIONSHIP IN ANY SEED`.

**Retired names:** `POOLING_METHOD`, `BUCKET_EDGES` (removed at `05-03`, before the freeze).

**`notebooks/diagnostics/curvature_probe_decodability_run.py`** — NEW runner. Functions
`load_pu_pair`, `build_cae`, `load_converged_model`, `extract_seed_field`, `run_field_mode`,
`run_pool_mode` (a hard refusal), `run_perseed_mode`, `_effective_distinct_levels`,
`run_density_diagnostics`, `run_bucketed_mode`, `_score_one_seed`, `_spearman_report`,
`_direction_axis_report`, `_load_cached_seed_field`, `run_inter_seed_diagnostics`, `selfcheck`,
`build_arg_parser`, `main`. Flags `--mode {field,pool,perseed,bucketed}`, `--selfcheck`, `--smoke`,
`--smoke-n`, `--seeds`, `--n-charts`, `--batch-size`, `--record-path`, `--field-stem`,
`--bucket-stem`, `--pooling-method` (tripwire).

**`notebooks/pu_manifold/tests/test_linear_probe.py`** — NEW test file: ten tests from `05-01`, four
added at `05-03`.

**`notebooks/05_curvature_conditioned_linear_decodability.ipynb`** — NEW notebook, created here,
committed executed with outputs.

**Cache artifact keys** (gitignored): `05_curvature_field_seed2026081{3,4,5}`,
`05_curvature_buckets_seed2026081{3,4,5}`, `05_inter_seed_diagnostics.json`,
`05_density_diagnostics.json`, `05_curvature_probe_decodability.jsonl`, `05_probe_selfcheck.jsonl`.
**Retired:** `05_curvature_field` (the pooled npz) — never created.

**JSONL row kinds:** `probe_overall` (one), `probe_seed` (three), `probe_bucket` (`3 * N_BUCKETS`),
`probe_conditioning` (one). Key sets are listed in `05-05-PLAN.md`'s artifacts section.

**Planning artifacts:** `05-03-DECISION.md` (`05-03` checkpoint), `05-PREREGISTRATION.md` (`05-04`),
`05-FINDINGS.md` and `05-VERIFICATION.md` (this plan).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The executed notebook — three seeds side by side</name>
  <precondition>notebooks/.cache/05_curvature_probe_decodability.jsonl exists with one probe_overall row, three probe_seed rows, 3 * N_BUCKETS probe_bucket rows and one probe_conditioning row.</precondition>
  <files>notebooks/05_curvature_conditioned_linear_decodability.ipynb</files>
  <read_first>
    - `notebooks/04_region_partition_mknn.ipynb` — Phase 4's notebook, for shape: how it loads cached artifacts rather than recomputing, how it quotes the frozen constants, how it plots, and how it ends with the verdict and the caveats rather than with a plot.
    - `notebooks/pu_manifold/linear_probe.py` — the frozen module the notebook imports unchanged, including both frozen rule strings and `PHASE_VERDICT_VALUES`.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-03-DECISION.md` — the ratified refusal to pool, whose evidence table the notebook's inter-seed cell restates.
    - `notebooks/.cache/05_curvature_probe_decodability.jsonl`, `05_inter_seed_diagnostics.json`, `05_density_diagnostics.json` and the three `05_curvature_buckets_seed*.npz` — every number the notebook shows comes from these; it recomputes nothing.
    - `CLAUDE.md` § General — notebooks are committed with their outputs, executed end to end; additive only; keep things simple first.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-RESEARCH.md` § Swiss Roll Rule Applicability — the determination the notebook's closing markdown cell restates.
  </read_first>
  <action>
Create `notebooks/05_curvature_conditioned_linear_decodability.ipynb` and execute it end to end, then
commit it with its outputs.

Import the phase's code unchanged from `notebooks/pu_manifold/` and `notebooks/diagnostics/`.
Reimplement nothing. The notebook is a reader for artifacts the runners already produced; it must not
fit a probe, extract a curvature field, bucket a field, or construct any field from more than one
seed. Keep it short — roughly a dozen to fifteen cells. CLAUDE.md's keep-it-simple-first rule applies
here as much as anywhere.

Cells, in order:

1. Markdown: the phase question in one paragraph — does linear crossmodal decodability degrade where
   decoder-side curvature magnitude is higher — plus the note that the rules judging the answer were
   frozen before the answer existed, with the freeze commit SHA quoted from `05-04-SUMMARY.md`; and
   the note that the three CAE seeds were ratified as not poolable at `05-03`, so the answer is three
   per-seed verdicts and their spread rather than one pooled verdict.
2. Imports and the frozen constants: print every constant off `linear_probe`, and print `VERDICT_RULE`
   and `SEED_VERDICT_COMBINATION_RULE` in full along with `PHASE_VERDICT_VALUES`. A reader should be
   able to see both rules, and what a split means, before seeing any number.
3. Load the three per-seed fields and the three per-seed bucket artifacts from cache; print, per seed,
   the median, min, max, `n_charts_used`, and the effective distinct-level counts at the three
   relative tolerances. State in an adjacent markdown line that seeds 20260814 and 20260815 carry 4
   and 3 effective levels and that `05-02-SUMMARY.md`'s exact-float counts of 5,301 and 9,852 are
   last-ULP noise, retracted at `05-03`.
4. Plot: the three seeds' curvature-magnitude distributions on a log x-axis, one panel each. This is
   the picture of RESEARCH Pitfall 2 — two narrow, few-level fields and one continuous one — and it
   should be visible rather than described. There is no fourth panel; there is no pooled field.
5. Markdown and print: the inter-seed Spearman values with the median direction cosine and
   anti-alignment fraction beside each, read from the inter-seed diagnostics JSON. State in the
   markdown that this is inter-seed agreement and not agreement with true curvature, and that these
   are the numbers the one-way pooling decision was made on.
6. Markdown: the pooling decision itself, in three sentences — what `05-CONTEXT.md` D5-04 called for,
   what was ratified instead at the `05-03` blocking checkpoint, and the three rejected alternatives.
   State that the question of whether a pooled field would have been an artifact of averaging fields
   that share no signal is answered here from the decision record, because no pooled field was built.
7. Load the probe record rows; print the overall row's `n_train`, `n_test`, `selected_alpha`,
   `r2_overall` and `mean_residual_overall`, with a line noting there is one fit shared by all three
   seeds' comparisons.
8. Table: per seed and per bucket, the realized test-split `n`, mean residual, R-squared, and the
   bootstrap CI — with each seed's full-field counts printed in an adjacent column so the difference
   between the two is visible, per D5-08. Three blocks, one per seed, or one table with a seed column.
9. Plot: per-bucket mean residual with its CI as error bars against bucket index, three series, one
   per seed, so the spread between seeds is visible in the same axes.
10. Plot: the continuous view — per-point residual against curvature magnitude on the test split, on
    a log x-axis, one panel per seed, with that seed's Spearman value in each title. Sensitivity only;
    label it as such in the markdown.
11. Print: each seed's size-matched check — match size, median difference, sign stability and
    disjoint-CI fraction — on three adjacent lines.
12. Print: the per-seed density confound from the density diagnostics JSON, three values, with Phase
    4's point-cloud number beside them and the note that it does not transfer; and print the
    `pooled_field_disposition` string so D5-05's dispositioned half is visible in the notebook too.
13. Markdown, the closing read-out: the three per-seed verdicts quoted verbatim from their
    `probe_seed` rows, then the phase verdict quoted verbatim from the `probe_overall` row; the
    statement that the per-seed verdicts came from the module's verdict function and the phase verdict
    from the module's combination function, both from committed rules; and the three caveats in one
    short paragraph each — D5-11's no-anchor gap with the sealed `-0.015106571347065712` row, D5-12's
    `CAE_VERDICT = FAIL` inheritance, and the Swiss roll determination (the rule does not trigger for
    a linear probe, the per-seed amendment introduced no new model, and the curvature estimator this
    phase reads from is already covered by `notebooks/03_swiss_roll_chart_curvature_field_check.ipynb`).
    If the phase verdict is `SPLIT ACROSS SEEDS`, restate the frozen combination rule's own reason
    that a split is not partial support; do not add a new interpretation.

Do not add a threshold table, a verdict JSON artifact, a cfg-hash cache key or a pre-registration
re-derivation to the notebook. Those live in the runner and in the planning documents; the notebook
shows what happened.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, pathlib
nb = json.loads(pathlib.Path('notebooks/05_curvature_conditioned_linear_decodability.ipynb').read_text())
cells = nb['cells']
code = [c for c in cells if c['cell_type'] == 'code']
assert len(cells) <= 24, ('notebook too long', len(cells))
executed = [c for c in code if c.get('execution_count') is not None]
assert len(executed) == len(code), ('unexecuted code cells', len(code) - len(executed))
with_out = [c for c in code if c.get('outputs')]
assert len(with_out) >= max(1, len(code) - 2), ('code cells missing outputs', len(with_out), len(code))
src = chr(10).join(''.join(c['source']) for c in cells)
for tok in ('linear_probe','VERDICT_RULE','SEED_VERDICT_COMBINATION_RULE','PHASE_VERDICT_VALUES','05_curvature_probe_decodability','05-03-DECISION','pooled_field_disposition','-0.015106571347065712','CAE_VERDICT = FAIL','Swiss roll'):
    assert tok in src, tok
import re as _re
for stale in ('5,301','9,852'):
    for m in _re.finditer(_re.escape(stale), src):
        window = src[max(0, m.start()-400):m.end()+400].lower()
        assert any(w in window for w in ('retract','wrong','last-ulp','last ulp','float noise')), ('the retracted count ' + stale + ' appears without a retraction within 400 characters')
for banned in ('fit_probe(','chart_curvature_field(','pool_seed_fields(','bucket_by_field('):
    assert banned not in src, ('notebook recomputes instead of reading: ' + banned)
rows = [json.loads(l) for l in pathlib.Path('notebooks/.cache/05_curvature_probe_decodability.jsonl').read_text().splitlines() if l.strip()]
o = [r for r in rows if r['kind']=='probe_overall'][0]
outs = chr(10).join(str(o2) for c in code for o2 in c.get('outputs',[]))
assert o['phase_verdict'] in (src + outs), 'the phase verdict does not appear in the notebook'
for s in [r for r in rows if r['kind']=='probe_seed']:
    assert s['verdict'] in (src + outs), ('per-seed verdict missing from notebook: ' + str(s['seed_stem']))
print('notebook executed, outputs present, reads artifacts only; phase verdict:', o['phase_verdict'])"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/05_curvature_conditioned_linear_decodability.ipynb` exists with at most 24 cells
    - Every code cell has a non-null `execution_count`, and all but at most two carry outputs
    - The notebook source contains the literals `linear_probe`, `VERDICT_RULE`, `SEED_VERDICT_COMBINATION_RULE`, `PHASE_VERDICT_VALUES`, `05_curvature_probe_decodability`, `05-03-DECISION`, `pooled_field_disposition`, `-0.015106571347065712`, `CAE_VERDICT = FAIL` and `Swiss roll`
    - The notebook does not call the probe fit, the curvature-field extractor, the seed pooler or the bucketer; it reads cached artifacts only
    - The notebook prints both frozen rules in full before displaying any probe number
    - The per-bucket table shows realized test-split counts and full-field counts in adjacent columns, for all three seeds
    - Curvature-magnitude distributions are plotted for all three seeds and there is no pooled-field panel
    - The closing markdown cell quotes all three per-seed verdicts and the phase verdict verbatim from their record rows
    - If either retracted count `5,301` or `9,852` appears, it appears only inside a sentence that retracts it
    - `git status --porcelain notebooks/` shows the new notebook as added and no existing notebook as modified or deleted
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
  </acceptance_criteria>
  <done>The result is legible in one executed, committed notebook that shows both rules before any number, plots three seeds side by side with no pooled panel, reads every value from a cached artifact, and closes on three per-seed verdicts, the phase verdict, and the three caveats.</done>
</task>

<task type="auto">
  <name>Task 2: The phase record and the mechanical ordering proof</name>
  <files>.planning/phases/05-curvature-conditioned-linear-decodability/05-FINDINGS.md, .planning/phases/05-curvature-conditioned-linear-decodability/05-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/04-region-partitioning-regional-alignment-mknn/04-FINDINGS.md` — read in full. This document matches its structure: what the phase claims and what it deliberately does not; the frozen configuration; the field; the density number; the result; requirement outcomes; and what a follow-on phase would need. Its § 7 region-size artifact write-up is the honesty bar for the size-match reporting here.
    - `.planning/phases/04-region-partitioning-regional-alignment-mknn/04-VERIFICATION.md` § Critical Focus: The Ordering Guarantee — the exact shape of the mechanical proof, with the commands and their outputs quoted.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-03-DECISION.md` — read in full. Its Decision, Evidence, Correction, Rationale and Consequence sections are the source for this document's pooling narrative, its corrected level counts, and its answer to the pooled-artifact question.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-PREREGISTRATION.md` — the frozen constants, `VERDICT_RULE` and `SEED_VERDICT_COMBINATION_RULE`, quoted rather than paraphrased.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-02-SUMMARY.md`, `05-03-SUMMARY.md`, `05-04-SUMMARY.md`, `05-05-SUMMARY.md` — every measured number, and the `freeze_commit` and `preregistration_commit` SHAs.
    - `.claude/skills/spike-findings-effdim/SKILL.md` § Open and `.claude/skills/spike-findings-effdim/references/high-d-curvature-feasibility.md` — the sealed `d=20` row and the open saddle-fixture question, stated as open and not for autonomous action.
    - `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md` and `.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-FINDINGS.md` — the D5-12 chain, so the paragraph about it is accurate rather than approximate. If either file is missing or unreadable, record the absence and what could not be verified rather than omitting the paragraph.
  </read_first>
  <action>
Write two documents.

**`05-VERIFICATION.md`** first, because the findings' claims depend on it. Open with a Critical Focus
section on the ordering guarantee and run, quoting each command and its literal output:
- `git merge-base --is-ancestor` from the `freeze_commit` SHA recorded in `05-04-SUMMARY.md` to the
  first probe-number commit (the `05-05` Task 2 commit), with its exit status;
- `git diff` between that freeze commit and `HEAD`, restricted to
  `notebooks/pu_manifold/linear_probe.py`, which must print nothing;
- `git log --oneline` restricted to `notebooks/pu_manifold/linear_probe.py`, which lists exactly THREE
  commits. Name each one in the document so no reader has to guess: creation at `05-01`; the per-seed
  structural repair at `05-03`, which renamed `POOLING_METHOD` to `SEED_HANDLING_RULE` and
  `BUCKET_EDGES` to `BUCKET_EDGES_PER_SEED`, added `SEED_VERDICT_COMBINATION_RULE`,
  `PHASE_VERDICT_VALUES` and `combine_seed_verdicts`, and left every pre-registered constant unset;
  and the freeze at `05-04`. State explicitly that the middle commit precedes the freeze and assigned
  no value, so the ordering guarantee — which constrains what happens after the freeze — is intact,
  and show the freeze commit is the most recent commit touching the file;
- `git log --oneline` restricted to
  `.planning/phases/05-curvature-conditioned-linear-decodability/05-PREREGISTRATION.md`.

If any of these fails, record the failure at full strength and state that the pre-registration was
breached, with the offending diff quoted. Do not amend a commit, do not rebase, and do not reword the
claim to fit the evidence.

Then sections for: observable truths against the phase goal; required artifacts with their paths;
key-link verification; requirement coverage against D5-01 through D5-13, one row each with an honest
outcome; behavioral spot-checks (the full test suite, the `--selfcheck` run, `--mode pool` raising
with `05-03-DECISION.md` named, and `--mode bucketed` raising when the pre-registration is stubbed
empty); anti-patterns found; the CLAUDE.md Swiss roll determination; human verification required; and
a gaps summary. Include a spec-less-probe-coverage section recording that the edge probe surfaced 29
applicable items over D5-01..D5-13, that 23 were authored into `must_haves.truths` across the four
amended plans and 6 unclassified rows were surfaced as flagged assumptions, that 23 + 6 = 29, and that
the earlier "no requirement IDs to probe" skip note is retracted.

**`05-FINDINGS.md`** — the phase record. Sections:

1. What this phase claims and what it deliberately does not. The claim is narrow: under rules frozen
   before the numbers existed, held-out per-point residual from a single global ridge map between the
   two PU modalities either does or does not differ between the highest and lowest tertile of a
   decoder-side curvature magnitude field — asked independently of three CAE seeds, answered three
   times, and combined into one phase read-out by a frozen rule. Everything the phase does not claim
   goes here, at full strength, in the phase's own words: that the field measures true curvature; that
   a difference, if found, is caused by curvature; that the CAE the decoder comes from is valid; and
   that a per-seed difference, if the verdicts split, generalizes beyond that seed's decoder fit.

2. The frozen configuration. All 31 constants, quoted from `linear_probe.py`, with the freeze commit
   SHA and the pre-registration commit SHA. Note that `POOLING_METHOD` and `BUCKET_EDGES` were retired
   before the freeze and why.

3. The seed decision and the field. State plainly that `05-CONTEXT.md` D5-04 called for pooling the
   three seeds into one averaged field and naming it the verdict field; that the question was put to
   the developer at the `05-03` Task 1 blocking checkpoint with `05-02`'s measured numbers in front of
   them; and that the ratified one-way outcome was the opposite, recorded in `05-03-DECISION.md`.
   Quote the evidence: pairwise Spearman `-0.1402`, `+0.2019`, `-0.2725` with p-values; median
   direction cosines `0.0039`, `0.0014`, `0.0007` with anti-alignment fractions `46.1`, `48.1`, `46.4`
   percent; per-seed medians, chart counts and `median log10 det g`. Then the per-seed field summary:
   medians, min, max, and the corrected effective distinct-level counts — 4 for seed 20260814 and 3
   for seed 20260815, measured at relative precision and stable from rel 1e-9 through rel 1e-3 — with
   the explicit statement that `05-02-SUMMARY.md`'s exact-float counts of 5,301 and 9,852 are
   last-ULP noise, are retracted, and that `05-RESEARCH.md` Pitfall 2 and `03-09-SUMMARY.md` were
   correct.

   Then answer the pooled-artifact question directly, in the phase's own words: the original plan
   would have built a pooled field and then asked whether it was an artifact of averaging fields that
   share no signal. No pooled field was built, so that question is answered from the decision record
   rather than from a diagnostic — the three fields are mutually anti-correlated and directionally
   orthogonal, and two of three are 3-to-4-level step functions at a metric determinant about 100
   orders of magnitude away from the third, so a pooled field would have been seed 20260813's
   structure plus two step functions disagreeing with it and with each other. State that this is a
   reasoned answer from measurement, not a measurement of the pooled object itself.

   Finally, record D5-05 in two parts: the measured pairwise inter-seed agreement (part one, met), and
   the DISPOSITION of the pooled-versus-seed statistic (part two — no referent, not computed against a
   substitute, recorded as `pooled_field_disposition` in
   `notebooks/.cache/05_density_diagnostics.json`). Neither part is reported as dropped.

4. Density. The three re-measured per-seed Spearman values between local density and curvature
   magnitude, Phase 4's point-cloud `-0.0273` and direction `+0.8208` beside them, the statement that
   neither transfers, and that this is a disclosure and not a gate (D5-13). Note whether the three
   seeds agree with each other on the confound.

5. The result. `n_train`, `n_test`, the selected alpha with the frozen grid, overall R-squared and
   mean residual — stated once, because there is one fit shared by all three seeds' comparisons, which
   also means the three per-seed verdicts are not statistically independent and differ only in the
   field that buckets the same residuals. Then, per seed: that seed's edges, its per-bucket table with
   realized test-split counts and full-field counts side by side, its bootstrap CIs, its size-matched
   check, and its continuous Spearman labelled sensitivity only with the direction axis recorded as a
   visible non-application. Then the three per-seed verdicts and the phase verdict, each quoted
   byte-for-byte from its record row, with the note that the per-seed verdicts came from the module's
   verdict function and the phase verdict from the module's combination function.

   If the phase verdict is `NO DETECTABLE RELATIONSHIP IN ANY SEED`, report it as a complete result
   under the frozen rule and do not soften it. If it is `SPLIT ACROSS SEEDS`, report it as a complete
   result too and restate the frozen combination rule's own reason that a split is not partial
   support: an effect present in one or two of three decoder fits' curvature fields, when those fields
   were measured to share no signal, is a property of those fits and not of the manifold. Do not
   report the agreeing seeds as the headline and set the third aside (D5-07, D5-08). Name Phase 4's
   region-size artifact explicitly as the reason the size-matched check exists.

   Include the conditioning check from `05-05` Task 3: whether the measured singular spectrum
   confirmed RESEARCH A2's stated reason for ridge, and whether the selected alpha sat at a grid
   boundary. If either came out against the pre-registration's stated reasoning, say so here.

6. Accepted gaps, stated up front. One subsection each, in the phase's own words and never by
   cross-reference:
   - D5-11. No known-answer anchor, by explicit choice. The sealed `d=20` decoder row is
     `rank_spearman_rho = -0.015106571347065712` — essentially zero rank correlation with analytic
     curvature on the only control that tests it — with 52 to 75 percent of points anti-aligned in
     direction. A Swiss roll or low-`d` anchor stage was offered and declined. Any relationship this
     phase measures rests on a field with no demonstrated relationship to true curvature, so a
     detected effect cannot be attributed to curvature by anything in this phase. Then the unresolved
     mitigating context, reported and explicitly NOT used to upgrade the result: the sealed saddle
     control sets a constant analytic Hessian, so its curvature magnitude varies only through the
     pullback metric and it may be structurally unable to show ordering at all, which would make the
     sealed value a fact about the fixture rather than about the decoder. That question is open and is
     explicitly not for autonomous action.
   - D5-12. The CAE failed its own validity gate, `CAE_VERDICT = FAIL` at Phase 02.2; Phase 3 runs on
     a deliberate override of that gate; Phase 03.1 found the metric repaired by the `scale` prior but
     the ordering only partially and non-seed-consistently moved. State these in phase order — 02.2,
     then 3, then 03.1, then 5 — so a reader can see which decision came first and what each later one
     was made in spite of. The FAIL and the override are two facts that stand together; neither
     cancels or excuses the other. Every Phase 5 number inherits that chain, and this phase's own
     measured inter-seed disagreement is a direct continuation of Phase 03.1's non-seed-consistent
     ordering finding.
   - The `r/R` non-application: no neighbourhood and no `k` in this estimator, so the spike-findings
     disclosure has no value to report. A visible non-application, not an omission. The same for the
     density and residual Spearmans' direction axis: both operands are scalars, so no vector direction
     axis exists and the sign of rho is the direction.
   - The Swiss roll determination, restated: the rule does not trigger for a linear probe, the
     per-seed amendment introduced no new model, and the estimator this phase reads from is already
     covered by `notebooks/03_swiss_roll_chart_curvature_field_check.ipynb`.
   - The spec-less probe fallback record, with its retraction: an earlier draft recorded the fallback
     as skipped for want of requirement IDs. That is wrong — the phase has D5-01 through D5-13 and the
     edge probe surfaced 29 applicable items across them, 23 authored into plan `must_haves` and 6
     unclassified rows surfaced as flagged assumptions.
   - The shared-split dependence: one train/test split serves all three seeds' bucketings, so the
     three per-seed verdicts are not independent replicates. That is deliberate — it isolates the
     field as the only thing that differs between them — and it means the spread measures field
     disagreement, not sampling variability.

7. Requirement outcomes. One row per D5-01 through D5-13, each with an honest outcome — met, met with
   caveat, superseded, or not met — and one line of evidence naming the artifact or commit that shows
   it. D5-04's row reads SUPERSEDED, naming `05-03-DECISION.md` and the checkpoint that ratified it.
   D5-05's row reads met in part one and dispositioned in part two.

8. What a follow-on phase would need. Concretely, from what this phase learned: the declined low-`d`
   probe-methodology anchor; the saddle-fixture resolution; an external astrophysical label as the
   probe target; per-region independent fits at matched `n` as sensitivity-only; and — new from this
   phase — whatever would be required to obtain three CAE fits whose curvature fields agree, since
   without that a per-seed spread is the ceiling on what any curvature-conditioned claim about this
   manifold can be. Note that the first four are already on `05-CONTEXT.md`'s Deferred list and none
   was smuggled into this phase.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, pathlib, subprocess, sys
sys.path.insert(0,'notebooks')
base = pathlib.Path('.planning/phases/05-curvature-conditioned-linear-decodability')
f = (base / '05-FINDINGS.md').read_text()
v = (base / '05-VERIFICATION.md').read_text()
for tok in ['-0.015106571347065712','CAE_VERDICT = FAIL','Swiss roll','r/R','not for autonomous action','-0.0273','+0.8208','05-03-DECISION','SUPERSEDED','pooled_field_disposition','-0.1402','+0.2019','-0.2725']:
    assert tok in f, ('missing from FINDINGS: ' + tok)
for i in range(1, 14):
    did = 'D5-%02d' % i
    assert did in f, ('requirement outcome missing: ' + did)
for tok in ['merge-base','--is-ancestor','linear_probe.py','git diff','05-03']:
    assert tok in v, ('missing from VERIFICATION: ' + tok)
assert subprocess.run(['grep','-qF','--','-0.015106571347065712',str(base / '05-FINDINGS.md')]).returncode == 0
rows = [json.loads(l) for l in pathlib.Path('notebooks/.cache/05_curvature_probe_decodability.jsonl').read_text().splitlines() if l.strip()]
o = [r for r in rows if r['kind']=='probe_overall'][0]
assert o['phase_verdict'] in f, ('findings do not quote the phase verdict verbatim: ' + o['phase_verdict'])
for s in [r for r in rows if r['kind']=='probe_seed']:
    assert str(s['seed_stem']) in f, ('seed missing from findings: ' + str(s['seed_stem']))
    assert s['verdict'] in f, ('per-seed verdict missing from findings: ' + s['verdict'])
assert subprocess.run(['git','diff','--quiet','--','notebooks/pu_manifold/linear_probe.py']).returncode == 0
touched = subprocess.run(['git','log','--format=%H','--','notebooks/pu_manifold/linear_probe.py'],capture_output=True,text=True).stdout.strip().splitlines()
assert len(touched) == 3, ('frozen module commit count is not 3: ' + str(len(touched)))
freeze_head = subprocess.run(['git','log','--format=%H','-1','--','notebooks/pu_manifold/linear_probe.py'],capture_output=True,text=True).stdout.strip()
assert freeze_head in v, 'VERIFICATION does not quote the most recent commit touching the frozen file'
assert not pathlib.Path('notebooks/.cache/05_curvature_field.npz').exists(), 'a pooled field artifact exists'
print('findings and verification complete; phase verdict:', o['phase_verdict'], '| linear_probe.py commits:', len(touched))"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-FINDINGS.md` and `05-VERIFICATION.md` both exist
    - `05-VERIFICATION.md` quotes the ancestry command, the restricted diff command and their literal outputs, and names `notebooks/pu_manifold/linear_probe.py` explicitly
    - `git log --oneline -- notebooks/pu_manifold/linear_probe.py` returns exactly three commits, `05-VERIFICATION.md` quotes that output, names each of the three, and states that the middle one preceded the freeze and assigned no constant
    - The most recent commit touching `notebooks/pu_manifold/linear_probe.py` equals the `freeze_commit` recorded in `05-04-SUMMARY.md`, and that SHA appears in `05-VERIFICATION.md`
    - `git diff --quiet -- notebooks/pu_manifold/linear_probe.py` exits 0
    - `05-VERIFICATION.md` records the spec-less probe coverage arithmetic: 29 surfaced, 23 authored, 6 flagged, and the retraction of the earlier no-requirement-IDs skip note
    - `05-FINDINGS.md` contains the literals `-0.015106571347065712`, `CAE_VERDICT = FAIL`, `Swiss roll`, `r/R`, `not for autonomous action`, `-0.0273`, `+0.8208`, `05-03-DECISION`, `SUPERSEDED`, `pooled_field_disposition`, `-0.1402`, `+0.2019` and `-0.2725`
    - `grep -qF -- '-0.015106571347065712' 05-FINDINGS.md` exits 0 — the sealed value is at full precision with an ASCII hyphen-minus
    - `05-FINDINGS.md` states the corrected effective level counts 4 and 3 and retracts `05-02-SUMMARY.md`'s 5,301 / 9,852 exact counts by name
    - `05-FINDINGS.md` names every one of D5-01 through D5-13 in its requirement-outcomes section, each with an outcome and a line of evidence; D5-04's outcome is `SUPERSEDED` and D5-05's is split into a met half and a dispositioned half
    - All three per-seed verdict strings and all three seed stems appear in `05-FINDINGS.md`, and the phase verdict string in `05-FINDINGS.md` is byte-identical to the `probe_overall` row's `phase_verdict`
    - `05-FINDINGS.md` reports all three seeds' realized test-split bucket counts beside their full-field counts and names Phase 4's region-size artifact as the reason the size-matched check exists
    - `05-FINDINGS.md` reports the three per-seed density Spearman values and states they are a disclosure and not a gate
    - `05-FINDINGS.md` states D5-11's consequence in the phase's own words, with no sentence that defers the statement to another document
    - `05-FINDINGS.md` states the D5-12 chain in phase order and presents the Phase 02.2 FAIL and the Phase 3 override as two facts that stand together
    - `05-FINDINGS.md` answers the pooled-field-artifact question from `05-03-DECISION.md` and states explicitly that no pooled field was built, so the answer is reasoned from measurement rather than measured on the pooled object
    - `notebooks/.cache/05_curvature_field.npz` does not exist
    - `git status --porcelain src/effdim/` produces no output
  </acceptance_criteria>
  <done>The phase record exists at Phase 4's honesty standard, reports three verdicts and their spread rather than one pooled verdict, records the superseded decision and the dispositioned statistic rather than hiding either, and the one guarantee Phase 5 can make is proved mechanically from git with its three-commit history explained.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Close Phase 5 — read both rules, check the ancestry, judge the gaps</name>
  <read_first>
    - `notebooks/pu_manifold/linear_probe.py` — `VERDICT_RULE` and `SEED_VERDICT_COMBINATION_RULE` as frozen text, plus `PHASE_VERDICT_VALUES`. Read the rule text itself, not the code around it; steps 1 and 5 below are judgements about what that text permits.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-FINDINGS.md` — read § the seed decision and § Accepted gaps end to end; steps 3, 4 and 5 are prose-adequacy judgements no test can make.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-VERIFICATION.md` — the quoted git commands and outputs, to compare against the four commands re-run by hand in step 2.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-03-DECISION.md` — the ratified refusal to pool, so step 4's check on the findings' pooling narrative is against the record rather than against memory.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-04-SUMMARY.md` — the `freeze_commit` SHA step 2's ancestry and diff commands take as their argument.
    - `notebooks/.cache/05_curvature_probe_decodability.jsonl` — the `probe_overall` and `probe_seed` rows whose verdict strings step 7 compares against the notebook and the findings.
  </read_first>
  <what-built>
The whole phase. A frozen pre-registration in committed source and in `05-PREREGISTRATION.md`; three
per-seed decoder-side curvature fields and three independent per-seed bucketings under a ratified
one-way decision NOT to pool them; one global ridge probe from `hsc` to `legacysurvey` with held-out
per-point residuals bucketed three ways; three per-seed verdicts applied mechanically from the
committed verdict rule and one phase verdict combined mechanically from the committed combination
rule; an executed notebook; and a findings and verification pair.

Four things a machine cannot check are what this checkpoint is for. `05-VALIDATION.md` names the first
three as manual-only, and the amendment adds the fourth: whether the frozen `VERDICT_RULE` really
admits "no detectable relationship" as a complete outcome rather than a near-miss; whether
`05-FINDINGS.md`'s accepted-gaps section states each gap at full strength in the phase's own words
rather than deferring to a cross-reference; whether the ordering guarantee's evidence actually says
what the verification document claims it says; and whether the frozen
`SEED_VERDICT_COMBINATION_RULE` really makes a split a complete terminal outcome rather than partial
support — and whether the findings honoured that when they reported it.
  </what-built>
  <how-to-verify>
1. Read `VERDICT_RULE` and `SEED_VERDICT_COMBINATION_RULE` in `notebooks/pu_manifold/linear_probe.py`
   — the text, not the code around it. Confirm that `NO DETECTABLE RELATIONSHIP` appears as a named
   terminal per-seed outcome and that nothing in the text lets the continuous Spearman, a non-headline
   bucket, or a different `N_BUCKETS` convert it into anything else. Then confirm the combination rule
   names all three members of `PHASE_VERDICT_VALUES`, states that `SPLIT ACROSS SEEDS` is a complete
   terminal outcome and not partial support, gives its measured reason, and forbids upgrading a split
   by majority.

2. Run these four and confirm the first exits 0, the second prints nothing, the third lists exactly
   three commits, and the fourth exits non-zero:
   `git merge-base --is-ancestor <freeze_commit> <first-probe-commit>`
   `git diff <freeze_commit> HEAD -- notebooks/pu_manifold/linear_probe.py`
   `git log --oneline -- notebooks/pu_manifold/linear_probe.py`
   `.venv/bin/python notebooks/diagnostics/curvature_probe_decodability_run.py --mode pool`
   The `freeze_commit` SHA is recorded in `05-04-SUMMARY.md`; the first probe commit is `05-05`
   Task 2's. The third should list creation at `05-01`, the per-seed structural repair at `05-03`, and
   the freeze at `05-04`, in that order from oldest — confirm the freeze is the most recent and that
   `05-VERIFICATION.md` explains the middle commit rather than leaving it unexplained. Compare all
   four outputs against what `05-VERIFICATION.md` quotes.

3. Read `05-FINDINGS.md` § Accepted gaps end to end. Confirm the D5-11 paragraph states, in its own
   sentences, that the field has no demonstrated relationship to true curvature and that a detected
   effect cannot be attributed to curvature by anything in this phase — and that it does not do this
   by pointing at `05-CONTEXT.md` or at the spike findings. Confirm the D5-12 paragraph names
   `CAE_VERDICT = FAIL` and the deliberate override and presents them as two facts that stand
   together. Confirm the saddle-fixture question is stated as open and not for autonomous action, and
   is not used to soften the D5-11 gap.

4. Read `05-FINDINGS.md` § the seed decision. Confirm it says plainly that `05-CONTEXT.md` D5-04
   called for a pooled field, that the ratified outcome was the opposite, and that no pooled field was
   built — and that the pooled-artifact question is answered from the decision record with its
   measured evidence rather than from a diagnostic that does not exist. Confirm D5-05's second half is
   recorded as dispositioned, not as satisfied and not as dropped.

5. If the phase verdict is `SPLIT ACROSS SEEDS`, read how the findings report it. Confirm it is
   reported as a complete result, that the agreeing seeds are not presented as the headline with the
   third set aside, and that no new interpretation was added beyond the frozen rule's own reason.

6. Open `notebooks/05_curvature_conditioned_linear_decodability.ipynb` and confirm it is committed
   with its outputs, that both frozen rules are printed before any probe number appears, that all
   three seeds' distributions are plotted with no pooled panel, and that the per-bucket table shows
   realized test-split counts beside full-field counts for every seed.

7. Confirm the phase verdict and all three per-seed verdicts are the same strings in the notebook, in
   `05-FINDINGS.md`, and in the `probe_overall` and `probe_seed` rows of
   `notebooks/.cache/05_curvature_probe_decodability.jsonl`.

8. Run `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` and
   `.venv/bin/python notebooks/diagnostics/curvature_probe_decodability_run.py --selfcheck`; both
   should exit 0.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 5, or describe what is wrong — in particular, name any accepted gap that reads as softened, any claim in 05-VERIFICATION.md the git output does not support, any place a verdict was interpreted rather than quoted, or any place a split outcome is presented as partial support.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> evidential claim | The ordering guarantee is a repository-history property. `05-VERIFICATION.md` quotes the commands and their literal outputs rather than summarizing them, so the claim and its evidence are checkable side by side. The frozen file's three-commit history is named commit by commit so a pre-freeze structural change cannot be mistaken for a post-freeze edit or hidden as one. |
| cached artifact -> committed prose | Every number in `05-FINDINGS.md` is quoted from a JSONL row or a diagnostics JSON, never recomputed at write-up time. |
| ratified decision -> committed prose | The pooled-field question is answered from `05-03-DECISION.md`, which is itself a committed record of a blocking checkpoint, rather than from an artifact that was deliberately not built. |
| local filesystem -> `notebooks/.cache/` | Read-only in this plan. |

No network surface, no authentication, no session, no user-supplied input path. ASVS L1; block on
`high`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19 | Repudiation | an ordering claim asserted rather than proved | high | mitigate | `05-VERIFICATION.md` quotes each git command and its literal output; the verify re-runs the restricted diff and the commit-count check independently and asserts the newest commit touching the frozen file is the recorded freeze; the blocking human checkpoint re-runs all four by hand |
| T-05-31 | Repudiation | the pre-freeze structural repair at `05-03` misread as a post-freeze edit, or quietly omitted from the history so the count looks like the original two | high | mitigate | The verify asserts the commit count is exactly three; the acceptance criteria require `05-VERIFICATION.md` to name all three and state that the middle one preceded the freeze and assigned no constant; the checkpoint reads the log by hand |
| T-05-20 | Tampering (of the record) | a verdict softened or reinterpreted at write-up time | high | mitigate | The verify asserts the findings contain the phase verdict string and all three per-seed verdict strings byte-for-byte; the checkpoint compares notebook, findings and record rows |
| T-05-27 | Tampering (of the record) | a `SPLIT ACROSS SEEDS` outcome written up as partial support, or the agreeing seeds reported as the headline with the third set aside | high | mitigate | The non-support framing was frozen into `SEED_VERDICT_COMBINATION_RULE` before any verdict existed; this plan's prohibitions forbid re-framing it; the acceptance criteria require all three per-seed verdicts and all three seed stems to appear; the blocking checkpoint's step 5 reads the split write-up specifically |
| T-05-32 | Information disclosure (of an omission) | the superseded D5-04 and the dispositioned D5-05 half absent from the phase record, so a downstream reader sees a pooled design that never happened or a statistic that silently vanished | high | mitigate | The verify asserts `05-03-DECISION`, `SUPERSEDED` and `pooled_field_disposition` are literally present in `05-FINDINGS.md`; the requirement-outcomes table must carry D5-04 as SUPERSEDED and D5-05 as met-plus-dispositioned; the checkpoint's step 4 reads that section |
| T-05-23 | Information disclosure (of a misleading measurement) | the retracted 5,301 / 9,852 exact-distinct counts entering the findings as effective level counts | high | mitigate | The acceptance criteria require the corrected 4 and 3 counts and an explicit retraction naming the old numbers; the notebook check permits the old numbers only inside a retracting sentence |
| T-05-21 | Information disclosure (of a misleading result) | an accepted gap stated by cross-reference instead of at full strength, so a downstream reader inherits the number without the caveat | high | mitigate | The verify asserts the literal presence of the sealed rank value, `CAE_VERDICT = FAIL`, the `r/R` non-application and `not for autonomous action`; prose adequacy is a manual-only check and is the blocking checkpoint's first and third job |
| T-05-29 | Information disclosure (of a misleading literal) | the sealed rank value written with a unicode minus or rounded, so a literal check passes on a different number | medium | mitigate | `grep -qF` with an ASCII hyphen-minus is asserted against `05-FINDINGS.md` |
| T-05-11 | Tampering (of results) | the frozen constants file edited during write-up to make a claim true | high | mitigate | Every task's verify runs the restricted diff and asserts the frozen file's history holds exactly three commits with the freeze most recent |
| T-05-22 | Tampering | an existing notebook or runner overwritten by the new one | medium | mitigate | The acceptance criteria assert `git status --porcelain notebooks/` shows the new notebook as added and no existing notebook as modified or deleted, per CLAUDE.md's additive-only rule |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package |
</threat_model>

<verification>
- `notebooks/05_curvature_conditioned_linear_decodability.ipynb` exists, is executed, carries its outputs, plots three seeds with no pooled panel, and reads cached artifacts only
- `05-FINDINGS.md` and `05-VERIFICATION.md` exist and contain every literal the acceptance criteria name
- `git merge-base --is-ancestor <freeze_commit> <first-probe-commit>` exits 0
- `git diff <freeze_commit> HEAD -- notebooks/pu_manifold/linear_probe.py` prints nothing
- `git log --oneline -- notebooks/pu_manifold/linear_probe.py` lists exactly three commits, all three are named in `05-VERIFICATION.md`, and the most recent is the recorded freeze
- All three per-seed verdict strings and the phase verdict string are identical across the notebook, `05-FINDINGS.md` and the record rows
- `05-FINDINGS.md` records D5-04 as SUPERSEDED and D5-05's second half as dispositioned
- `notebooks/.cache/05_curvature_field.npz` does not exist
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
- `.venv/bin/python notebooks/diagnostics/curvature_probe_decodability_run.py --selfcheck` exits 0
- `.venv/bin/python notebooks/diagnostics/curvature_probe_decodability_run.py --mode pool` exits non-zero naming `05-03-DECISION.md`
- `git status --porcelain src/effdim/` produces no output
- No existing notebook or runner script was deleted or rewritten
- CLAUDE.md Swiss roll rule: **does not trigger.** This plan writes a reader notebook and two records; it introduces no model. The per-seed amendment introduced none either. The determination is restated in `05-FINDINGS.md` as a visible decision.
</verification>

<success_criteria>
Phase 5 closes with its result reported and its worth stated. D5-09's ordering guarantee is proved
mechanically from git ancestry, not asserted, and the frozen file's three-commit history is explained
rather than left to be discovered. D5-04's supersession by `05-03-DECISION.md` and D5-05's
dispositioned pooled half are both on the record, so no downstream reader can believe a pooled field
existed or that a statistic vanished. D5-05's measured half, D5-07's continuous companion, D5-08's
per-seed realized-count size match and D5-13's per-seed density numbers are all reported and none of
them moved any verdict. D5-11 and D5-12's accepted gaps are stated at full strength in the phase's own
words, matching the standard `04-FINDINGS.md` set, so no downstream phase can inherit a Phase 5 number
without inheriting what it rests on — including that the number is one of three, from fields measured
to share no signal.
</success_criteria>

<output>
Create `.planning/phases/05-curvature-conditioned-linear-decodability/05-06-SUMMARY.md` when done
</output>
