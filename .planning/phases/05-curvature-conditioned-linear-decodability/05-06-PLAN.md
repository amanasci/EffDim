---
phase: 05-curvature-conditioned-linear-decodability
plan: 06
type: execute
wave: 6
depends_on: [05-05]
files_modified:
  - notebooks/05_curvature_conditioned_linear_decodability.ipynb
  - .planning/phases/05-curvature-conditioned-linear-decodability/05-FINDINGS.md
  - .planning/phases/05-curvature-conditioned-linear-decodability/05-VERIFICATION.md
autonomous: false
requirements: [D5-05, D5-07, D5-08, D5-09, D5-11, D5-12, D5-13]

flagged_assumptions:
  - "The git-ancestry proof is the phase's only fully intact guarantee and it is proved mechanically, not asserted. If the ancestry check fails, or if the diff of the frozen constants file between the freeze commit and HEAD is non-empty, 05-VERIFICATION.md records the failure at full strength and 05-FINDINGS.md states that the pre-registration was breached. It is never repaired by amending a commit or by rewording the claim."
  - "05-FINDINGS.md must state D5-11's and D5-12's consequences in the phase's own words rather than by cross-reference. 04-FINDINGS.md set that standard and 05-VALIDATION.md lists prose adequacy as a manual-only verification, because no test can judge whether a paragraph is honest."
  - "The Swiss roll determination is recorded as a visible decision, not a silent skip: CLAUDE.md's rule targets models that map data to a lower-dimensional representation and back or claim to recover manifold structure. A ridge map from 768 dimensions to 768 dimensions has no bottleneck, no latent space and no decoder, and this phase writes no estimator. The estimator it reads from is already covered by notebooks/03_swiss_roll_chart_curvature_field_check.ipynb."
  - "Spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip). Recorded here as well as at 05-01 so the phase record carries it."

must_haves:
  truths:
    - "notebooks/05_curvature_conditioned_linear_decodability.ipynb exists, is executed end to end, and is committed with its outputs, per CLAUDE.md"
    - "The notebook imports the phase's code unchanged from notebooks/pu_manifold/ and notebooks/diagnostics/ and reimplements nothing"
    - "05-FINDINGS.md exists and states, in the phase's own words and without deferring to a cross-reference, that the curvature field has no demonstrated relationship to true curvature, quoting the sealed d=20 decoder row rank_spearman_rho = -0.015106571347065712, and that a detected effect therefore cannot be attributed to curvature by anything in this phase (D5-11)"
    - "05-FINDINGS.md states that the CAE underlying the decoder failed its own validity gate with CAE_VERDICT = FAIL at Phase 02.2, that Phase 3 runs on a deliberate override, and that Phase 03.1 found the ordering only partially and non-seed-consistently moved (D5-12)"
    - "05-FINDINGS.md reports the measured inter-seed agreement and states plainly whether the pooled field is an artifact of averaging fields that share no signal, while still reporting the verdict the frozen rule produced (D5-05)"
    - "05-FINDINGS.md reports the realized test-split bucket counts beside the full-field counts and the size-matched result, and names Phase 4's region-size artifact as the reason that check exists (D5-08)"
    - "05-FINDINGS.md reports the re-measured spearman(density, pooled curvature magnitude) and states it is a disclosure and not a gate (D5-13)"
    - "05-VERIFICATION.md proves the ordering guarantee mechanically: the freeze commit is an ancestor of the first probe-number commit, and the diff of the frozen constants file between the freeze commit and HEAD is empty, with both commands and both outputs quoted (D5-09)"
    - "The verdict reported in 05-FINDINGS.md is byte-identical to the verdict string in the probe_overall JSONL row, and the findings state it came from the module's verdict function rather than from a judgement made at write-up time (D5-09)"
    - statement: "05-FINDINGS.md's accepted-gaps section reaches the standard 04-FINDINGS.md set: each gap is stated at full strength in the phase's own words, and none is softened by pointing at another document"
      verification: backstop
    - statement: "The frozen VERDICT_RULE, read as text, makes no detectable relationship a complete terminal outcome rather than a near-miss of the positive branch"
      verification: backstop
  prohibitions:
    - "MUST NOT modify notebooks/pu_manifold/linear_probe.py. If a defect in it is found here, record a pre-registration BREACH with the diff and the reason; do not patch it"
    - "MUST NOT re-run the probe, re-pool the seeds, re-cut the buckets or re-word the verdict at write-up time; the findings report what the frozen rule produced"
    - "MUST NOT present D5-11's declined low-d anchor as resolved, mitigated or compensated. The saddle-fixture question is open and explicitly not for autonomous action"
    - "MUST NOT use the continuous Spearman, the inter-seed diagnostics, the density number or any non-headline bucket to soften or upgrade a NO DETECTABLE RELATIONSHIP verdict"
    - "MUST NOT delete or rewrite any existing notebook or runner script; this plan adds one notebook and two planning documents (CLAUDE.md: additive only)"
  artifacts:
    - path: "notebooks/05_curvature_conditioned_linear_decodability.ipynb"
      provides: "The executed, committed notebook reading the phase's cached artifacts"
      contains: "linear_probe"
    - path: ".planning/phases/05-curvature-conditioned-linear-decodability/05-FINDINGS.md"
      provides: "The phase record: what it claims, what it deliberately does not, the frozen configuration, the numbers, and the accepted gaps"
      contains: "-0.015106571347065712"
    - path: ".planning/phases/05-curvature-conditioned-linear-decodability/05-VERIFICATION.md"
      provides: "The mechanical proof of the ordering guarantee from git ancestry"
      contains: "merge-base"
  key_links:
    - from: ".planning/phases/05-curvature-conditioned-linear-decodability/05-FINDINGS.md"
      to: "notebooks/.cache/05_curvature_probe_decodability.jsonl"
      via: "every number in the findings is quoted from a record row, not recomputed at write-up time"
      pattern: "probe_overall"
    - from: ".planning/phases/05-curvature-conditioned-linear-decodability/05-VERIFICATION.md"
      to: "notebooks/pu_manifold/linear_probe.py"
      via: "the empty-diff assertion names the frozen constants file explicitly"
      pattern: "linear_probe.py"
    - from: "notebooks/05_curvature_conditioned_linear_decodability.ipynb"
      to: "notebooks/pu_manifold/linear_probe.py"
      via: "the notebook imports the frozen module unchanged and quotes its constants"
      pattern: "from pu_manifold import"
---

<objective>
Close the phase: an executed notebook that shows the result, a findings document that states what
the result is worth, and a verification document that proves the one guarantee this phase can make.

Purpose: Phase 5's numbers sit on a chain of acknowledged weaknesses — a CAE that failed its own
validity gate, a curvature field with no demonstrated relationship to true curvature, three seeds
that may share no signal, and a density confound that had to be re-measured rather than inherited.
None of that is a reason not to report the result. It is a reason to report it with each weakness
named at full strength, in the phase's own words, so nobody downstream inherits a number without
inheriting what it rests on. `04-FINDINGS.md` set that standard and this document has to match it.

Output: `notebooks/05_curvature_conditioned_linear_decodability.ipynb` (executed, committed with
outputs), `05-FINDINGS.md`, `05-VERIFICATION.md`.

**Recorded skip, carried into the phase record:** spec-less probe fallback skipped — the phase has
no requirement IDs to probe (visible skip). No phase `SPEC.md` exists, so `## Edge Coverage` and
`## Prohibitions` are absent and the nothing-to-probe guard fired. Coverage is traced against
`05-CONTEXT.md`'s D5-01..D5-13 instead.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.claude/skills/spike-findings-effdim/SKILL.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-CONTEXT.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-PREREGISTRATION.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-05-SUMMARY.md
@.planning/phases/04-region-partitioning-regional-alignment-mknn/04-FINDINGS.md
@.planning/phases/04-region-partitioning-regional-alignment-mknn/04-VERIFICATION.md
</context>

<artifacts_this_phase_produces>
Symbols and files Phase 5 CREATES (all new — exclude from drift/source-grounding checks).

**`notebooks/pu_manifold/linear_probe.py`** — NEW module, FROZEN at `05-04`. Constants
`TRAIN_FRACTION`, `SPLIT_SEED`, `SPLIT_RULE`, `RIDGE_ALPHA_GRID`, `RIDGE_SELECTION_RULE`,
`ALPHA_PER_TARGET`, `FIT_INTERCEPT`, `EMBEDDING_PREPROCESSING`, `RESIDUAL_METRIC`,
`R2_MULTIOUTPUT`, `N_BUCKETS`, `BUCKET_RULE`, `BUCKET_EDGES`, `POOLING_METHOD`, `SEED_STEMS`,
`N_CHARTS`, `CURVATURE_MODE`, `CURVATURE_CONVENTION`, `CURVATURE_SOURCE_FUNCTION`,
`SIZE_MATCH_RULE`, `SIZE_MATCH_N_REPEATS`, `SIZE_MATCH_SEED`, `N_BOOTSTRAP`, `BOOTSTRAP_SEED`,
`CONFIDENCE_LEVEL`, `K_DENSITY`, `FIELD_D`, `VERDICT_RULE`, `PREREGISTRATION_PATH`. Functions
`train_test_split_indices`, `fit_probe`, `predict_probe`, `per_point_residuals`, `aggregate_r2`,
`pool_seed_fields`, `bucket_edges_from_field`, `assign_buckets`, `bucket_by_field`,
`bucket_counts`, `bucket_residual_ci`, `size_matched_check`, `apply_verdict_rule`,
`assert_preregistered`.

**`notebooks/diagnostics/curvature_probe_decodability_run.py`** — NEW runner. Functions
`load_pu_pair`, `build_cae`, `load_converged_model`, `extract_seed_field`, `run_pool`,
`run_bucketed`, `_spearman_report`, `_direction_axis_report`, `selfcheck`, `build_arg_parser`,
`main`. Flags `--mode {field,pool,bucketed}`, `--selfcheck`, `--smoke`, `--smoke-n`, `--seeds`,
`--n-charts`, `--batch-size`, `--record-path`, `--field-stem`, `--pooling-method`.

**`notebooks/pu_manifold/tests/test_linear_probe.py`** — NEW test file (ten tests).

**`notebooks/05_curvature_conditioned_linear_decodability.ipynb`** — NEW notebook, created here,
committed executed with outputs.

**Cache artifact keys** (gitignored): `05_curvature_field_seed20260813` / `..._seed20260814` /
`..._seed20260815`, `05_curvature_field`, `05_inter_seed_diagnostics.json`,
`05_density_diagnostics.json`, `05_curvature_probe_decodability.jsonl`, `05_probe_selfcheck.jsonl`.

**JSONL record keys:** `kind`, `data_source`, `n`, `n_train`, `n_test`, `train_fraction`,
`split_seed`, `selected_alpha`, `alpha_grid`, `r2_overall`, `mean_residual_overall`, `n_buckets`,
`bucket_edges`, `bucket_index`, `bucket_n`, `bucket_mean_residual`, `bucket_r2`, `ci_low`,
`ci_high`, `degenerate`, `confidence_level`, `n_resamples`, `spearman_h_residual_rho`,
`spearman_h_residual_p`, `size_match_n`, `size_match_median_diff`, `size_match_sign_stable`,
`size_match_ci_disjoint_fraction`, `verdict`, `pooling_method`, `curvature_source_function`,
`curvature_convention`, `seed_stems`, `subsample_file`, `field_stem`, `wallclock_s`, plus the
conditioning row's `singular_values_head`, `condition_number`, `effective_rank_1pct`,
`effective_rank_0p1pct`, `effective_rank_0p01pct`, `cumvar_first_20`, `cumvar_first_25`,
`alpha_at_grid_boundary`.

**Planning artifacts:** `05-PREREGISTRATION.md` (`05-04`), `05-FINDINGS.md` and
`05-VERIFICATION.md` (this plan).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The executed notebook</name>
  <files>notebooks/05_curvature_conditioned_linear_decodability.ipynb</files>
  <read_first>
    - `notebooks/04_region_partition_mknn.ipynb` — Phase 4's notebook, for shape: how it loads cached artifacts rather than recomputing, how it quotes the frozen constants, how it plots, and how it ends with the verdict and the caveats rather than with a plot.
    - `notebooks/pu_manifold/linear_probe.py` — the frozen module the notebook imports unchanged.
    - `notebooks/.cache/05_curvature_probe_decodability.jsonl`, `05_inter_seed_diagnostics.json`, `05_density_diagnostics.json` and `05_curvature_field.npz` — every number the notebook shows comes from these; it recomputes nothing.
    - `CLAUDE.md` § General — notebooks are committed with their outputs, executed end to end; additive only; keep things simple first.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-RESEARCH.md` § Swiss Roll Rule Applicability — the determination the notebook's closing markdown cell restates.
  </read_first>
  <action>
Create `notebooks/05_curvature_conditioned_linear_decodability.ipynb` and execute it end to end,
then commit it with its outputs.

Import the phase's code unchanged from `notebooks/pu_manifold/` and `notebooks/diagnostics/`.
Reimplement nothing. The notebook is a reader for artifacts the runners already produced; it must
not fit a probe, extract a curvature field, or re-pool the seeds. Keep it short — roughly a dozen
cells. CLAUDE.md's keep-it-simple-first rule applies here as much as anywhere.

Cells, in order:

1. Markdown: the phase question in one paragraph — does linear crossmodal decodability degrade
   where decoder-side curvature magnitude is higher — and the note that the rule judging the answer
   was frozen before the answer existed, with the freeze commit SHA quoted from `05-04-SUMMARY.md`.
2. Imports and the frozen constants: print every constant off `linear_probe` and print
   `VERDICT_RULE` in full. A reader should be able to see the rule before seeing any number.
3. Load the three per-seed fields and the pooled field from cache; print each seed's median, min,
   max and distinct-value count.
4. Plot: the three seeds' curvature-magnitude distributions on a log x-axis, and the pooled field's
   distribution beside them. This is the picture of RESEARCH Pitfall 2 — two piecewise-constant
   fields and one continuous one — and it should be visible rather than described.
5. Markdown and print: the inter-seed Spearman values with the median direction cosine beside each,
   read from the inter-seed diagnostics JSON. State in the markdown that this is inter-seed
   agreement and not agreement with true curvature.
6. Load the probe record rows; print the overall row's `n_train`, `n_test`, `selected_alpha`,
   `r2_overall` and `mean_residual_overall`.
7. Table: per bucket, the realized test-split `n`, mean residual, R-squared, and the bootstrap CI —
   with the full-field counts printed in an adjacent column so the difference between the two is
   visible, per D5-08.
8. Plot: per-bucket mean residual with its CI as error bars, against bucket index.
9. Plot: the continuous view — per-point residual against pooled curvature magnitude on the test
   split, on a log x-axis, with the Spearman value in the title. Sensitivity only; label it as such.
10. Print: the size-matched check's match size, median difference, sign stability and disjoint-CI
    fraction.
11. Print: the density confound from the density diagnostics JSON, with Phase 4's point-cloud
    number beside it and the note that it does not transfer.
12. Markdown, the closing read-out: the verdict quoted verbatim from the `probe_overall` row; the
    statement that it was produced by the module's verdict function from the committed rule; and
    the three caveats in one short paragraph each — D5-11's no-anchor gap with the sealed
    `-0.015106571347065712` row, D5-12's `CAE_VERDICT = FAIL` inheritance, and the Swiss roll
    determination (the rule does not trigger for a linear probe, and the curvature estimator this
    phase reads from is already covered by
    `notebooks/03_swiss_roll_chart_curvature_field_check.ipynb`).

Do not add a threshold table, a verdict JSON artifact, a cfg-hash cache key or a pre-registration
re-derivation to the notebook. Those live in the runner and in the planning documents; the notebook
shows what happened.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, pathlib
nb = json.loads(pathlib.Path('notebooks/05_curvature_conditioned_linear_decodability.ipynb').read_text())
cells = nb['cells']
code = [c for c in cells if c['cell_type'] == 'code']
assert len(cells) <= 20, ('notebook too long', len(cells))
executed = [c for c in code if c.get('execution_count') is not None]
assert len(executed) == len(code), ('unexecuted code cells', len(code) - len(executed))
with_out = [c for c in code if c.get('outputs')]
assert len(with_out) >= max(1, len(code) - 2), ('code cells missing outputs', len(with_out), len(code))
src = chr(10).join(''.join(c['source']) for c in cells)
for tok in ('linear_probe','VERDICT_RULE','05_curvature_probe_decodability','-0.015106571347065712','CAE_VERDICT = FAIL','Swiss roll'):
    assert tok in src, tok
for banned in ('fit_probe(','chart_curvature_field(','pool_seed_fields('):
    assert banned not in src, ('notebook recomputes instead of reading: ' + banned)
print('notebook executed, outputs present, reads artifacts only')"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/05_curvature_conditioned_linear_decodability.ipynb` exists with at most 20 cells
    - Every code cell has a non-null `execution_count`, and all but at most two carry outputs
    - The notebook source contains the literals `linear_probe`, `VERDICT_RULE`, `05_curvature_probe_decodability`, `-0.015106571347065712`, `CAE_VERDICT = FAIL` and `Swiss roll`
    - The notebook source does not call the probe fit, the curvature-field extractor or the seed pooler; it reads cached artifacts only
    - The notebook prints the full frozen `VERDICT_RULE` before displaying any probe number
    - The per-bucket table shows realized test-split counts and full-field counts in adjacent columns
    - The closing markdown cell quotes the verdict verbatim from the `probe_overall` record row
    - `git status --porcelain notebooks/` shows the new notebook as added and no existing notebook as modified or deleted
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
  </acceptance_criteria>
  <done>The result is legible in one executed, committed notebook that shows the rule before the numbers, reads every value from a cached artifact, and closes on the verdict with its three caveats.</done>
</task>

<task type="auto">
  <name>Task 2: The phase record and the mechanical ordering proof</name>
  <files>.planning/phases/05-curvature-conditioned-linear-decodability/05-FINDINGS.md, .planning/phases/05-curvature-conditioned-linear-decodability/05-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/04-region-partitioning-regional-alignment-mknn/04-FINDINGS.md` — read in full. This document matches its structure: what the phase claims and what it deliberately does not; the frozen configuration; the field; the density number; the result; requirement outcomes; and what a follow-on phase would need. Its § 7 region-size artifact write-up is the honesty bar for the size-match reporting here.
    - `.planning/phases/04-region-partitioning-regional-alignment-mknn/04-VERIFICATION.md` § Critical Focus: The Ordering Guarantee — the exact shape of the mechanical proof, with the commands and their outputs quoted.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-PREREGISTRATION.md` — the frozen constants and the verdict rule, quoted rather than paraphrased.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-02-SUMMARY.md`, `05-03-SUMMARY.md`, `05-04-SUMMARY.md`, `05-05-SUMMARY.md` — every measured number, and the `freeze_commit` and `preregistration_commit` SHAs.
    - `.claude/skills/spike-findings-effdim/SKILL.md` § Open and `.claude/skills/spike-findings-effdim/references/high-d-curvature-feasibility.md` — the sealed `d=20` row and the open saddle-fixture question, stated as open and not for autonomous action.
    - `.planning/phases/02.2-chart-autoencoder-validity-test-inserted/02.2-FINDINGS.md` and `.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-FINDINGS.md` — the D5-12 chain, so the paragraph about it is accurate rather than approximate.
  </read_first>
  <action>
Write two documents.

**`05-VERIFICATION.md`** first, because the findings' claims depend on it. Open with a Critical
Focus section on the ordering guarantee and run, quoting each command and its literal output:
- `git merge-base --is-ancestor` from the `freeze_commit` SHA recorded in `05-04-SUMMARY.md` to the
  first probe-number commit (the `05-05` Task 2 commit), with its exit status;
- `git diff` between that freeze commit and `HEAD`, restricted to
  `notebooks/pu_manifold/linear_probe.py`, which must print nothing;
- `git log --oneline` restricted to `notebooks/pu_manifold/linear_probe.py`, showing every commit
  that ever touched the frozen file, so a reader can see there are exactly two — creation at `05-01`
  and the freeze at `05-04` — and no third;
- `git log --oneline` restricted to
  `.planning/phases/05-curvature-conditioned-linear-decodability/05-PREREGISTRATION.md`.

If any of these fails, record the failure at full strength and state that the pre-registration was
breached, with the offending diff quoted. Do not amend a commit, do not rebase, and do not reword
the claim to fit the evidence.

Then sections for: observable truths against the phase goal; required artifacts with their paths;
key-link verification; requirement coverage against D5-01 through D5-13, one row each with an honest
outcome; behavioral spot-checks (the full test suite, the `--selfcheck` run, and `--mode bucketed`
raising when the pre-registration is stubbed empty); anti-patterns found; the CLAUDE.md Swiss roll
determination; human verification required; and a gaps summary.

**`05-FINDINGS.md`** — the phase record. Sections:

1. What this phase claims and what it deliberately does not. The claim is narrow: under a rule
   frozen before the numbers existed, held-out per-point residual from a single global ridge map
   between the two PU modalities either does or does not differ between the highest and lowest
   tertile of a pooled decoder-side curvature magnitude. Everything the phase does not claim goes
   here, at full strength, in the phase's own words: that the field measures true curvature; that a
   difference, if found, is caused by curvature; that the CAE the decoder comes from is valid.

2. The frozen configuration. Every constant, quoted from `linear_probe.py`, with the freeze commit
   SHA and the pre-registration commit SHA.

3. The field. The three per-seed medians, min, max and distinct-value counts; the pairwise
   inter-seed Spearman values with their direction cosines beside them; the ratified pooling rule
   and why; the pooled-versus-seed Spearman values. Then state plainly whether the three seeds
   agree, and if they do not, that the pooled field may be an artifact of averaging fields that
   share no signal — while still reporting the verdict the frozen rule produced, because D5-05 makes
   this diagnostic non-gating by design.

4. Density. The re-measured Spearman between local density and pooled curvature magnitude, Phase
   4's point-cloud `-0.0273` and direction `+0.8208` beside it, the statement that neither
   transfers, and that this is a disclosure and not a gate (D5-13).

5. The result. `n_train`, `n_test`, the selected alpha with the frozen grid, overall R-squared and
   mean residual; the per-bucket table with realized test-split counts and full-field counts side by
   side; the bootstrap CIs; the size-matched check; the continuous Spearman labelled sensitivity
   only; and the verdict, quoted byte-for-byte from the `probe_overall` row with the note that it
   came from the module's verdict function. If the verdict is NO DETECTABLE RELATIONSHIP, report it
   as a complete result under the frozen rule and do not soften it (D5-07, D5-08). Name Phase 4's
   region-size artifact explicitly as the reason the size-matched check exists.

   Include the conditioning check from `05-05` Task 3: whether the measured singular spectrum
   confirmed RESEARCH A2's stated reason for ridge, and whether the selected alpha sat at a grid
   boundary. If either came out against the pre-registration's stated reasoning, say so here.

6. Accepted gaps, stated up front. One subsection each, in the phase's own words and never by
   cross-reference:
   - D5-11. No known-answer anchor, by explicit choice. The sealed `d=20` decoder row is
     `rank_spearman_rho = -0.015106571347065712` — essentially zero rank correlation with analytic
     curvature on the only control that tests it — with 52 to 75 percent of points anti-aligned in
     direction. A Swiss roll or low-`d` anchor stage was offered and declined. Any relationship this
     phase measures rests on a field with no demonstrated relationship to true curvature, so a
     detected effect cannot be attributed to curvature by anything in this phase. Then the
     unresolved mitigating context, reported and explicitly NOT used to upgrade the result: the
     sealed saddle control sets a constant analytic Hessian, so its curvature magnitude varies only
     through the pullback metric and it may be structurally unable to show ordering at all, which
     would make the sealed value a fact about the fixture rather than about the decoder. That
     question is open and is explicitly not for autonomous action.
   - D5-12. The CAE failed its own validity gate, `CAE_VERDICT = FAIL` at Phase 02.2; Phase 3 runs
     on a deliberate override of that gate; Phase 03.1 found the metric repaired by the `scale`
     prior but the ordering only partially and non-seed-consistently moved. Every Phase 5 number
     inherits that chain.
   - The `r/R` non-application: no neighbourhood and no `k` in this estimator, so the
     spike-findings disclosure has no value to report. A visible non-application, not an omission.
   - The Swiss roll determination, restated: the rule does not trigger for a linear probe, and the
     estimator this phase reads from is already covered by
     `notebooks/03_swiss_roll_chart_curvature_field_check.ipynb`.
   - The spec-less probe fallback skip: the phase has no requirement IDs to probe, so no
     probe-derived predicates were generated; coverage is traced against D5-01 through D5-13.

7. Requirement outcomes. One row per D5-01 through D5-13, each with an honest outcome — met, met
   with caveat, or not met — and one line of evidence naming the artifact or commit that shows it.

8. What a follow-on phase would need. Concretely, from what this phase learned: the declined low-`d`
   probe-methodology anchor; the saddle-fixture resolution; an external astrophysical label as the
   probe target; per-region independent fits at matched `n` as sensitivity-only. Note that all four
   are already on `05-CONTEXT.md`'s Deferred list and none was smuggled into this phase.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, pathlib, subprocess, sys
sys.path.insert(0,'notebooks')
base = pathlib.Path('.planning/phases/05-curvature-conditioned-linear-decodability')
f = (base / '05-FINDINGS.md').read_text()
v = (base / '05-VERIFICATION.md').read_text()
for tok in ['-0.015106571347065712','CAE_VERDICT = FAIL','Swiss roll','r/R','not for autonomous action','-0.0273','+0.8208']:
    assert tok in f, ('missing from FINDINGS: ' + tok)
for i in range(1, 14):
    did = 'D5-%02d' % i
    assert did in f, ('requirement outcome missing: ' + did)
for tok in ['merge-base','--is-ancestor','linear_probe.py','git diff']:
    assert tok in v, ('missing from VERIFICATION: ' + tok)
rows = [json.loads(l) for l in pathlib.Path('notebooks/.cache/05_curvature_probe_decodability.jsonl').read_text().splitlines() if l.strip()]
verdict = [r for r in rows if r['kind']=='probe_overall'][0]['verdict']
assert verdict in f, ('findings do not quote the record verdict verbatim: ' + verdict)
assert subprocess.run(['git','diff','--quiet','--','notebooks/pu_manifold/linear_probe.py']).returncode == 0
touched = subprocess.run(['git','log','--oneline','--','notebooks/pu_manifold/linear_probe.py'],capture_output=True,text=True).stdout.strip().splitlines()
assert len(touched) <= 2, ('frozen module touched more than twice: ' + str(len(touched)))
print('findings and verification complete; verdict:', verdict, '| linear_probe.py commits:', len(touched))"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-FINDINGS.md` and `05-VERIFICATION.md` both exist
    - `05-VERIFICATION.md` quotes the ancestry command, the restricted diff command and their literal outputs, and names `notebooks/pu_manifold/linear_probe.py` explicitly
    - `git log --oneline -- notebooks/pu_manifold/linear_probe.py` returns at most two commits, and `05-VERIFICATION.md` quotes that output
    - `git diff --quiet -- notebooks/pu_manifold/linear_probe.py` exits 0
    - `05-FINDINGS.md` contains the literals `-0.015106571347065712`, `CAE_VERDICT = FAIL`, `Swiss roll`, `r/R`, `not for autonomous action`, `-0.0273` and `+0.8208`
    - `05-FINDINGS.md` names every one of D5-01 through D5-13 in its requirement-outcomes section, each with an outcome and a line of evidence
    - The verdict string in `05-FINDINGS.md` is byte-identical to the `probe_overall` row's `verdict`
    - `05-FINDINGS.md` reports the realized test-split bucket counts beside the full-field counts and names Phase 4's region-size artifact as the reason the size-matched check exists
    - `05-FINDINGS.md` reports the measured density Spearman on the pooled field and states it is a disclosure and not a gate
    - `05-FINDINGS.md` states D5-11's consequence in the phase's own words, with no sentence that defers the statement to another document
    - `git status --porcelain src/effdim/` produces no output
  </acceptance_criteria>
  <done>The phase record exists at Phase 4's honesty standard, and the one guarantee Phase 5 can make is proved mechanically from git rather than asserted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Close Phase 5 — read the rule, check the ancestry, judge the gaps</name>
  <what-built>
The whole phase. A frozen pre-registration in committed source and in `05-PREREGISTRATION.md`; three
per-seed decoder-side curvature fields and a pooled field under a ratified pooling rule; one global
ridge probe from `hsc` to `legacysurvey` with held-out per-point residuals bucketed by curvature
magnitude; the verdict applied mechanically from the committed rule; an executed notebook; and a
findings and verification pair.

Three things a machine cannot check are what this checkpoint is for, and `05-VALIDATION.md` names
all three as manual-only: whether the frozen `VERDICT_RULE` really admits "no detectable
relationship" as a complete outcome rather than a near-miss; whether `05-FINDINGS.md`'s accepted-gaps
section states each gap at full strength in the phase's own words rather than deferring to a
cross-reference; and whether the ordering guarantee's evidence actually says what the verification
document claims it says.
  </what-built>
  <how-to-verify>
1. Read `VERDICT_RULE` in `notebooks/pu_manifold/linear_probe.py` — the text, not the code around
   it. Confirm that NO DETECTABLE RELATIONSHIP appears as a named terminal outcome, and that nothing
   in the text lets the continuous Spearman, a non-headline bucket, or a different `N_BUCKETS`
   convert it into anything else.

2. Run these three and confirm the first two exit 0 and the second prints nothing:
   `git merge-base --is-ancestor <freeze_commit> <first-probe-commit>`
   `git diff <freeze_commit> HEAD -- notebooks/pu_manifold/linear_probe.py`
   `git log --oneline -- notebooks/pu_manifold/linear_probe.py`
   The `freeze_commit` SHA is recorded in `05-04-SUMMARY.md`; the first probe commit is `05-05`
   Task 2's. The third should list exactly two commits. Compare all three outputs against what
   `05-VERIFICATION.md` quotes.

3. Read `05-FINDINGS.md` § Accepted gaps end to end. Confirm the D5-11 paragraph states, in its own
   sentences, that the field has no demonstrated relationship to true curvature and that a detected
   effect cannot be attributed to curvature by anything in this phase — and that it does not do this
   by pointing at `05-CONTEXT.md` or at the spike findings. Confirm the D5-12 paragraph names
   `CAE_VERDICT = FAIL` and the deliberate override. Confirm the saddle-fixture question is stated as
   open and not for autonomous action, and is not used to soften the D5-11 gap.

4. Open `notebooks/05_curvature_conditioned_linear_decodability.ipynb` and confirm it is committed
   with its outputs, that the frozen rule is printed before any probe number appears, and that the
   per-bucket table shows realized test-split counts beside full-field counts.

5. Confirm the verdict in the notebook, in `05-FINDINGS.md`, and in the `probe_overall` row of
   `notebooks/.cache/05_curvature_probe_decodability.jsonl` are the same string.

6. Run `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` and
   `.venv/bin/python notebooks/diagnostics/curvature_probe_decodability_run.py --selfcheck`; both
   should exit 0.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 5, or describe what is wrong — in particular, name any accepted gap that reads as softened, any claim in 05-VERIFICATION.md the git output does not support, or any place the verdict was interpreted rather than quoted.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git history -> evidential claim | The ordering guarantee is a repository-history property. `05-VERIFICATION.md` quotes the commands and their literal outputs rather than summarizing them, so the claim and its evidence are checkable side by side. |
| cached artifact -> committed prose | Every number in `05-FINDINGS.md` is quoted from a JSONL row or a diagnostics JSON, never recomputed at write-up time. |
| local filesystem -> `notebooks/.cache/` | Read-only in this plan. |

No network surface, no authentication, no session, no user-supplied input path. ASVS L1; block on
`high`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-19 | Repudiation | an ordering claim asserted rather than proved | high | mitigate | `05-VERIFICATION.md` quotes each git command and its literal output; the verify re-runs the restricted diff and the commit-count check independently; the blocking human checkpoint re-runs all three by hand |
| T-05-20 | Tampering (of the record) | a verdict softened or reinterpreted at write-up time | high | mitigate | The verify asserts the findings contain the record row's verdict string byte-for-byte; the checkpoint compares notebook, findings and record row |
| T-05-21 | Information disclosure (of a misleading result) | an accepted gap stated by cross-reference instead of at full strength, so a downstream reader inherits the number without the caveat | high | mitigate | The verify asserts the literal presence of the sealed rank value, `CAE_VERDICT = FAIL`, the `r/R` non-application and `not for autonomous action`; prose adequacy is a manual-only check and is the blocking checkpoint's first job |
| T-05-11 | Tampering (of results) | the frozen constants file edited during write-up to make a claim true | high | mitigate | Every task's verify runs the restricted diff and asserts the frozen file has at most two commits in its entire history |
| T-05-22 | Tampering | an existing notebook or runner overwritten by the new one | medium | mitigate | The acceptance criteria assert `git status --porcelain notebooks/` shows the new notebook as added and no existing notebook as modified or deleted, per CLAUDE.md's additive-only rule |
| T-05-SC | Tampering | package installs | low | accept | This plan installs no package |
</threat_model>

<verification>
- `notebooks/05_curvature_conditioned_linear_decodability.ipynb` exists, is executed, carries its outputs, and reads cached artifacts only
- `05-FINDINGS.md` and `05-VERIFICATION.md` exist and contain every literal the acceptance criteria name
- `git merge-base --is-ancestor <freeze_commit> <first-probe-commit>` exits 0
- `git diff <freeze_commit> HEAD -- notebooks/pu_manifold/linear_probe.py` prints nothing
- `git log --oneline -- notebooks/pu_manifold/linear_probe.py` lists at most two commits
- The verdict string is identical across the notebook, `05-FINDINGS.md` and the `probe_overall` record row
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
- `.venv/bin/python notebooks/diagnostics/curvature_probe_decodability_run.py --selfcheck` exits 0
- `git status --porcelain src/effdim/` produces no output
- No existing notebook or runner script was deleted or rewritten
</verification>

<success_criteria>
Phase 5 closes with its result reported and its worth stated. D5-09's ordering guarantee is proved
mechanically from git ancestry, not asserted. D5-05, D5-07, D5-08 and D5-13's diagnostics are all
reported and none of them moved the verdict. D5-11 and D5-12's accepted gaps are stated at full
strength in the phase's own words, matching the standard `04-FINDINGS.md` set, so no downstream
phase can inherit a Phase 5 number without inheriting what it rests on.
</success_criteria>

<output>
Create `.planning/phases/05-curvature-conditioned-linear-decodability/05-06-SUMMARY.md` when done
</output>
