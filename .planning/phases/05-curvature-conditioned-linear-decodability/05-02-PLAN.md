---
phase: 05-curvature-conditioned-linear-decodability
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - notebooks/diagnostics/curvature_probe_decodability_run.py
autonomous: true
requirements: [D5-03, D5-05, D5-06]

flagged_assumptions:
  - "The three seeds' fields are expected to disagree. 03-09-SUMMARY.md measured ||H|| medians of 1359.0 / 51437.9 / 70794.1 across seeds 20260813/14/15, with two of the three fields piecewise-constant (4 and 3 distinct values) on metrics whose spectrum collapsed to ~1e-07. This plan MEASURES that rather than assuming it, because the measurement is the input to 05-03's one-way pooling decision. If the re-measured spread contradicts 03-09's, the discrepancy is reported and 05-03's checkpoint is presented with the NEW numbers."
  - "The spike-findings rule forbids reporting a rank statistic from a curvature field without the direction axis beside it (52-75% of points carried an anti-aligned H at d=20). No analytic truth exists for the PU field, so a direction-vs-truth axis is impossible here. The strongest direction axis that IS constructible is inter-seed median cosine between unit H_vec fields, and this plan builds it. That substitution is a weaker check than the rule intends and this plan says so rather than presenting it as equivalent."
  - "The r/R disclosure the spike-findings rule requires does not apply to this estimator: chart_curvature_field is an autodiff path through the decoder with no neighbourhood and no k, so there is no neighbourhood-radius-over-cloud-radius ratio to disclose. Recorded as a visible non-application, not an omission."

must_haves:
  truths:
    - "Three per-seed decoder-side curvature fields exist as cached npz artifacts, one per sealed CAE checkpoint 03_converged_cae_pu_nc4_seed20260813 / 20260814 / 20260815, each carrying H_norm of length 10000 and H_vec of shape (10000, 768) (D5-03)"
    - "Each per-seed field was produced by chart_curvature.chart_curvature_field(model, x64, mode='reverse') on the resolved subsample's legacysurvey column, not by any other estimator and not by retraining (D5-03)"
    - "Per-seed extraction is cached through cache.npz_cache with a cfg manifest, so re-running --mode field recomputes no seed whose artifact already matches its configuration and raises rather than silently reusing a mismatched one"
    - "All three pairwise inter-seed Spearman correlations between the seeds' H_norm fields are computed and recorded in notebooks/.cache/05_inter_seed_diagnostics.json, with UNDEFINED reported explicitly when an input is constant (D5-05)"
    - "The direction axis is reported beside every rank statistic: pairwise median cosine between the seeds' unit H_vec fields is computed and recorded alongside the Spearman values"
    - "Per-seed distinct-value counts and medians of H_norm are recorded, so the piecewise-constant symptom RESEARCH Pitfall 2 names is measured rather than assumed (D5-04 input)"
    - "No pooled field, no bucket edges and no PU probe number exist at the end of this plan (D5-09)"
    - statement: "The measured inter-seed spread is the evidence the 05-03 pooling checkpoint is decided on, and it is presented to the developer as measured numbers rather than as a recommendation alone"
      verification: backstop
  prohibitions:
    - "MUST NOT compute, print, cache or commit any PU probe number in this plan (D5-09)"
    - "MUST NOT pool the seeds, compute bucket edges, or set any pre-registered constant here; pooling is one-way and is decided at 05-03's blocking checkpoint (D5-04)"
    - "MUST NOT retrain, fine-tune or re-initialize any CAE; a missing checkpoint is a raised FileNotFoundError, never a fallback to training"
    - "MUST NOT report a Spearman between seed fields without the inter-seed direction cosine beside it (spike-findings requirement)"
    - "MUST NOT modify notebooks/pu_manifold/linear_probe.py in this plan; it must reach the 05-04 freeze commit unchanged from 05-01"
  artifacts:
    - path: "notebooks/.cache/05_curvature_field_seed20260813.npz"
      provides: "Seed 20260813's decoder-side curvature field, 10000 rows"
      contains: "H_norm"
    - path: "notebooks/.cache/05_curvature_field_seed20260814.npz"
      provides: "Seed 20260814's decoder-side curvature field, 10000 rows"
      contains: "H_norm"
    - path: "notebooks/.cache/05_curvature_field_seed20260815.npz"
      provides: "Seed 20260815's decoder-side curvature field, 10000 rows"
      contains: "H_norm"
    - path: "notebooks/.cache/05_inter_seed_diagnostics.json"
      provides: "D5-05's pairwise inter-seed Spearman, the direction-cosine axis beside it, and per-seed medians and distinct-value counts"
      contains: "pairwise_spearman"
  key_links:
    - from: "notebooks/diagnostics/curvature_probe_decodability_run.py"
      to: "notebooks/pu_manifold/chart_curvature.py"
      via: "the per-seed field is produced by chart_curvature_field in reverse mode"
      pattern: "chart_curvature_field"
    - from: "notebooks/.cache/05_inter_seed_diagnostics.json"
      to: "notebooks/.cache/05_curvature_field_seed20260813.npz"
      via: "the diagnostics read the three cached per-seed fields and nothing else"
      pattern: "05_curvature_field_seed"
---

<objective>
Extract the three sealed CAE seeds' decoder-side curvature fields over all 10,000 PU points, cache
each one independently, and measure how much the three seeds actually agree.

Purpose: this is the phase's single largest runtime cost and the evidence the one-way pooling
decision at `05-03` is made on. `03-09-SUMMARY.md` measured one seed's full-10,000-row field at
3,129.5 seconds of CPU wall-clock, so all three is roughly 2.6 hours; per-seed caching means a
crash, an interrupt or a machine reboot never repeats a completed seed. And Phase 03.1 already
found the curvature ordering was not seed-consistent, so pooling may be averaging fields that
share no signal — D5-05 requires that be measured and reported, and `05-03` cannot honestly choose
a pooling rule without the number in front of it.

Output: `notebooks/.cache/05_curvature_field_seed2026081{3,4,5}.npz` and
`notebooks/.cache/05_inter_seed_diagnostics.json`.

No pooled field, no bucket edges, no probe. The pre-registration is still unfrozen and
`--mode bucketed` is still dead.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.claude/skills/spike-findings-effdim/SKILL.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-CONTEXT.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-RESEARCH.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols and files Phase 5 CREATES (all new — exclude from drift/source-grounding checks).

**`notebooks/pu_manifold/linear_probe.py`** — NEW module (created at `05-01`, frozen at `05-04`).
Constants `TRAIN_FRACTION`, `SPLIT_SEED`, `SPLIT_RULE`, `RIDGE_ALPHA_GRID`,
`RIDGE_SELECTION_RULE`, `ALPHA_PER_TARGET`, `FIT_INTERCEPT`, `EMBEDDING_PREPROCESSING`,
`RESIDUAL_METRIC`, `R2_MULTIOUTPUT`, `N_BUCKETS`, `BUCKET_RULE`, `BUCKET_EDGES`, `POOLING_METHOD`,
`SEED_STEMS`, `N_CHARTS`, `CURVATURE_MODE`, `CURVATURE_CONVENTION`, `CURVATURE_SOURCE_FUNCTION`,
`SIZE_MATCH_RULE`, `SIZE_MATCH_N_REPEATS`, `SIZE_MATCH_SEED`, `N_BOOTSTRAP`, `BOOTSTRAP_SEED`,
`CONFIDENCE_LEVEL`, `K_DENSITY`, `FIELD_D`, `VERDICT_RULE`, `PREREGISTRATION_PATH`. Functions
`train_test_split_indices`, `fit_probe`, `predict_probe`, `per_point_residuals`, `aggregate_r2`,
`pool_seed_fields`, `bucket_edges_from_field`, `assign_buckets`, `bucket_by_field`,
`bucket_counts`, `bucket_residual_ci`, `size_matched_check`, `apply_verdict_rule`,
`assert_preregistered`.

**`notebooks/diagnostics/curvature_probe_decodability_run.py`** — NEW runner. Functions
`load_pu_pair`, `build_cae`, `load_converged_model`, `extract_seed_field`, `run_pool`,
`run_bucketed`, `_spearman_report`, `_direction_axis_report`, `selfcheck`, `build_arg_parser`,
`main`. Flags `--mode {field,pool,bucketed}`, `--selfcheck`, `--smoke`, `--smoke-n`, `--seeds`,
`--n-charts`, `--batch-size`, `--record-path`, `--field-stem`, `--pooling-method`.

**`notebooks/pu_manifold/tests/test_linear_probe.py`** — NEW test file (ten tests, `05-01`).

**`notebooks/05_curvature_conditioned_linear_decodability.ipynb`** — NEW notebook (`05-06`).

**Cache artifact keys** (gitignored): `05_curvature_field_seed20260813` /
`..._seed20260814` / `..._seed20260815` (npz; keys `H_norm`, `H_vec`, `chart_assignment`,
`metric_condition_number`, `lambda_min`, `lambda_max`, `det_g`, `log10_det_g`, `n_charts_used`),
`05_curvature_field` (npz; keys `pooled_H_norm`, `per_seed_H_norm`, `bucket_labels`,
`bucket_edges`, `seed_stems`, `pooling_method`), `05_inter_seed_diagnostics.json`,
`05_density_diagnostics.json`, `05_curvature_probe_decodability.jsonl`,
`05_probe_selfcheck.jsonl`.

**JSONL record keys:** `kind`, `data_source`, `n`, `n_train`, `n_test`, `train_fraction`,
`split_seed`, `selected_alpha`, `alpha_grid`, `r2_overall`, `mean_residual_overall`, `n_buckets`,
`bucket_edges`, `bucket_index`, `bucket_n`, `bucket_mean_residual`, `bucket_r2`, `ci_low`,
`ci_high`, `degenerate`, `confidence_level`, `n_resamples`, `spearman_h_residual_rho`,
`spearman_h_residual_p`, `size_match_n`, `size_match_median_diff`, `size_match_sign_stable`,
`size_match_ci_disjoint_fraction`, `verdict`, `pooling_method`, `curvature_source_function`,
`curvature_convention`, `seed_stems`, `subsample_file`, `field_stem`, `wallclock_s`.

**Planning artifacts:** `05-PREREGISTRATION.md`, `05-FINDINGS.md`, `05-VERIFICATION.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extract all three seeds' decoder-side curvature fields, one cached artifact each</name>
  <precondition>All three sealed checkpoints exist: notebooks/.cache/03_converged_cae_pu_nc4_seed20260813.pt, _seed20260814.pt and _seed20260815.pt are present and loadable. If any is missing, halt — this plan must never train a CAE.</precondition>
  <files>notebooks/diagnostics/curvature_probe_decodability_run.py</files>
  <read_first>
    - `notebooks/diagnostics/curvature_probe_decodability_run.py` — the runner as written at `05-01`, in particular `extract_seed_field` and the `--mode field` branch.
    - `.planning/phases/03-decoder-curvature-field/03-09-SUMMARY.md` — the measured per-seed `||H||` medians (1359.0 / 51437.9 / 70794.1), the per-seed distinct-value counts, the collapsed metric spectrum, and the 3,129.5 s single-seed wall-clock this task is budgeting against. Read this before running so the numbers you get are comparable to the numbers already on record.
    - `notebooks/pu_manifold/chart_curvature.py` lines 505-611 — `chart_curvature_field`'s return keys and its statement that `batch_size` is numerically inert and results are bit-identical across batch sizes.
    - `notebooks/pu_manifold/cache.py` — `npz_cache`'s load-or-compute contract and `_manifest_matches`'s raise-on-mismatch behaviour.
  </read_first>
  <action>
Run the extraction. Expect roughly 52 minutes per seed on CPU and roughly 2.6 hours total; run the
three seeds one at a time so a failure costs one seed, not three.

Confirm first, in one short command, that `--mode field --smoke --smoke-n 64` still exits 0 and
that all three checkpoint files are present. Then run `--mode field` once per seed, passing a
single value to `--seeds` each time, with `--n-charts 4` and `--batch-size 32`.

The field is computed on the `legacysurvey` column of the resolved subsample — the modality the CAE
was fit on and the modality whose geometry the probe's TARGET lives in, matching Phase 3's own call
site. `load_pu_pair`'s resolution rule (all files carrying both `hsc` and `legacysurvey`, most rows
wins, lexicographically first sorted-glob path on a tie) must resolve to the same file Phase 4
used; print the resolved filename and record it in the summary so both phases can be shown to
describe the same 10,000 points.

Because `extract_seed_field` goes through `cache.npz_cache`, a re-run of a completed seed is a load,
not a recompute, and a re-run under a changed configuration raises a manifest mismatch rather than
returning a stale array. If a run is interrupted, simply re-issue the same command: completed seeds
are skipped.

For each seed, record in the plan summary: wall-clock seconds, median `H_norm`, min and max
`H_norm`, the count of distinct `H_norm` values rounded to 12 significant figures, `n_charts_used`,
and the median `log10_det_g`. The distinct-value count is the direct measurement of RESEARCH
Pitfall 2's piecewise-constant symptom; `03-09-SUMMARY.md` recorded 4 and 3 distinct values for
seeds 20260814 and 20260815. If the numbers you measure disagree materially with `03-09`'s, say so
plainly in the summary rather than reconciling them — a discrepancy here is itself a finding and it
changes what `05-03`'s checkpoint is deciding.

Compute nothing else. No pooling, no bucketing, no probe.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import numpy as np, sys, pathlib
c = pathlib.Path('notebooks/.cache')
seeds = (20260813, 20260814, 20260815)
for s in seeds:
    p = c / f'05_curvature_field_seed{s}.npz'
    assert p.exists(), f'missing {p}'
    z = np.load(p)
    assert z['H_norm'].shape == (10000,), (s, z['H_norm'].shape)
    assert z['H_vec'].shape == (10000, 768), (s, z['H_vec'].shape)
    assert np.all(np.isfinite(z['H_norm'])), s
    assert (c / f'05_curvature_field_seed{s}.meta.json').exists(), s
    print(s, 'median', float(np.median(z['H_norm'])), 'distinct', len(np.unique(np.round(z['H_norm'], 6))))
print('three seed fields present and well-formed')"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/.cache/05_curvature_field_seed20260813.npz`, `..._seed20260814.npz` and `..._seed20260815.npz` all exist
    - Each npz carries `H_norm` of shape `(10000,)`, `H_vec` of shape `(10000, 768)`, and `chart_assignment`, `metric_condition_number`, `lambda_min`, `lambda_max`, `det_g`, `log10_det_g` each of length 10000
    - Every `H_norm` array is entirely finite
    - A sidecar `05_curvature_field_seed{seed}.meta.json` exists for each seed and its stored cfg names `seed`, `n_charts`, `mode`, `batch_size`, `n_rows`, `subsample_file`, `curvature_convention` and `source_function`
    - Re-running `--mode field --seeds 20260813` a second time completes in under 30 seconds (cache hit, not recompute)
    - The plan summary records, for each of the three seeds: wall-clock seconds, median `H_norm`, min and max `H_norm`, distinct-`H_norm`-value count, `n_charts_used`, and median `log10_det_g`
    - The plan summary names the resolved subsample file and states whether it is the same file `04-FINDINGS.md` names
    - `notebooks/.cache/05_curvature_probe_decodability.jsonl` still does not exist
    - `git status --porcelain notebooks/pu_manifold/linear_probe.py` produces no output
  </acceptance_criteria>
  <done>Three independently cached decoder-side curvature fields exist over all 10,000 points, produced by the corrected call on the three sealed checkpoints, with per-seed magnitude and distinct-value statistics on record.</done>
</task>

<task type="auto">
  <name>Task 2: Inter-seed agreement, with the direction axis beside every rank statistic</name>
  <files>notebooks/diagnostics/curvature_probe_decodability_run.py</files>
  <read_first>
    - `notebooks/diagnostics/curvature_probe_decodability_run.py` — the runner as written at `05-01`; `_spearman_report` and `_direction_axis_report` are extended here.
    - `notebooks/diagnostics/region_partition_mknn_run.py` lines 125-136 — `_spearman_report`'s exact shape, including the branch that reports UNDEFINED with an explicit marker when `scipy.stats.spearmanr` returns NaN on constant input. Two of the three seed fields are expected to be near-constant, so that branch is not hypothetical here.
    - `.claude/skills/spike-findings-effdim/SKILL.md` § Requirements — the rule that a rank statistic from this teacher is never reported without the direction axis beside it, and the measurement behind it (52-75% of points anti-aligned at `d=20`).
    - `.planning/phases/03.1-decoder-metric-regularization-inserted/03.1-FINDINGS.md` — the finding that the ordering moved only partially and non-seed-consistently under the `scale` prior, which is the prior expectation this task tests.
    - `notebooks/pu_manifold/cache.py` — `json_cache`, used for the diagnostics artifact.
  </read_first>
  <action>
Add a diagnostics step to `--mode field` that runs after all requested seeds have their artifacts,
loads the three cached per-seed npz files and writes
`notebooks/.cache/05_inter_seed_diagnostics.json` through `cache.json_cache`.

Rank axis (D5-05): all three pairwise `scipy.stats.spearmanr` correlations between the seeds'
`H_norm` fields, reported through the runner's `_spearman_report` so a NaN from constant input
becomes an explicit `undefined: true` marker rather than a number. Record `rho`, `p_value`, `n` and
`undefined` for each of the three pairs.

Direction axis, reported beside every one of those rank numbers and never separately: add
`_direction_axis_report(H_vec_a, H_vec_b, name)`, which normalizes both fields row-wise with the
codebase's `np.maximum(norm, 1e-12)` divide-by-zero floor, takes the per-row dot product, and
reports the median cosine, the 25th and 75th percentile cosine, and the fraction of rows whose
cosine is negative (the anti-alignment fraction). Run it for the same three pairs. State in the
function's own docstring, and in the plan summary, that this is inter-seed agreement and NOT
agreement with true curvature: no analytic truth exists for the PU field, so this is a weaker check
than the spike-findings rule intends, and it is being used because it is the strongest direction
axis this phase can construct — not because it is equivalent.

Also record, per seed: median, min, max, the count of distinct `H_norm` values, the fraction of
points assigned to each chart, and the median `log10_det_g`. These are RESEARCH Pitfall 2's
symptom measurements and they are the numbers `05-03`'s checkpoint is presented with.

Finally record, as an explicit key in the JSON, that `r_over_R` is `null` with reason
`"not defined for an autodiff decoder-side estimator: chart_curvature_field has no neighbourhood
and no k"`. The spike-findings `r/R` disclosure requirement is being visibly answered, not skipped.

Print a compact read-out of everything above to stdout so the summary can quote it directly.

Compute no pooled field. Do not choose a pooling method here — that is `05-03`'s blocking
checkpoint and it is one-way.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, pathlib
d = json.loads(pathlib.Path('notebooks/.cache/05_inter_seed_diagnostics.json').read_text())
assert 'pairwise_spearman' in d and len(d['pairwise_spearman']) == 3, d.keys()
assert 'pairwise_direction' in d and len(d['pairwise_direction']) == 3
for k, v in d['pairwise_direction'].items():
    assert 'median_cosine' in v and 'fraction_negative_cosine' in v, (k, v)
assert 'per_seed' in d and len(d['per_seed']) == 3
for s, v in d['per_seed'].items():
    for key in ('median_h_norm','min_h_norm','max_h_norm','n_distinct_h_norm','median_log10_det_g'):
        assert key in v, (s, key)
assert d.get('r_over_R', 'MISSING') is None, d.get('r_over_R')
assert 'not defined' in d.get('r_over_R_reason',''), d.get('r_over_R_reason')
print('inter-seed diagnostics complete: rank axis, direction axis, r/R disclosure')"</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/.cache/05_inter_seed_diagnostics.json` exists and parses as JSON
    - It contains `pairwise_spearman` with exactly three entries, each carrying `rho`, `p_value`, `n` and `undefined`
    - It contains `pairwise_direction` with exactly three entries, each carrying `median_cosine`, `q25_cosine`, `q75_cosine` and `fraction_negative_cosine`
    - It contains `per_seed` with exactly three entries, each carrying `median_h_norm`, `min_h_norm`, `max_h_norm`, `n_distinct_h_norm`, `chart_fractions` and `median_log10_det_g`
    - It contains `r_over_R` set to null and `r_over_R_reason` containing the phrase `not defined`
    - `grep -v '^#' notebooks/diagnostics/curvature_probe_decodability_run.py | grep -c 'def _direction_axis_report('` returns 1
    - The runner prints, for each pair, the Spearman value and the median cosine on adjacent lines
    - The plan summary quotes all three pairwise Spearman values and all three median cosines, and states in the phase's own words whether the three seeds agree
    - `notebooks/.cache/05_curvature_field.npz` does NOT exist (no pooled field yet)
    - `notebooks/.cache/05_curvature_probe_decodability.jsonl` still does not exist
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
  </acceptance_criteria>
  <done>D5-05's inter-seed agreement is measured and recorded with the direction axis beside every rank statistic and the r/R non-application disclosed, and the numbers 05-03's one-way pooling decision needs are on record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sealed `.pt` checkpoints -> in-process model | `torch.load(..., weights_only=False)` on this project's own Phase 3 output, living only in the gitignored local cache. |
| local filesystem -> `notebooks/.cache/` | The only persistence; every path composed by `cache.cache_path` / `cache.npz_cache` / `cache.json_cache` under `_assert_inside_cache`. |

No network surface, no authentication, no session, no user-supplied input path. ASVS L1; block on
`high`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | a 64-row smoke field or a differently-configured field silently reused as the real 10,000-row artifact | high | mitigate | Per-seed npz goes through `cache.npz_cache` with a cfg carrying `n_rows`, `seed`, `n_charts`, `mode`, `batch_size`, subsample file and convention; `_manifest_matches` raises on mismatch. `--smoke` bypasses the cache entirely. This plan's verify asserts every `H_norm` is length 10000 |
| T-05-06 | Spoofing (of provenance) | a field attributed to the wrong seed | medium | mitigate | `load_converged_model` validates the checkpoint's own recorded `n_charts` and `seed` against the request and raises `ValueError` on mismatch; the seed is part of the cache stem and of the manifest cfg |
| T-05-07 | Information disclosure (of a misleading result) | a rank statistic reported without its direction axis | high | mitigate | `_direction_axis_report` runs for the same three pairs as `_spearman_report`, and this plan's verify asserts three direction entries exist alongside three Spearman entries |
| T-05-03 | Tampering (of results) | a PU probe number computed before the pre-registration exists | high | mitigate | This plan computes curvature only; the verify asserts `05_curvature_probe_decodability.jsonl` does not exist and that `linear_probe.py` is unmodified |
| T-05-02 | Tampering | pickle deserialization via `torch.load` | low | accept | Identical call and identical checkpoints as the sealed `curvature_field_pu_run.load_converged_model`; inherited posture, not introduced |
| T-05-SC | Tampering | package installs | low | accept | This plan installs no package |
</threat_model>

<verification>
- Three `05_curvature_field_seed*.npz` artifacts exist, each with `H_norm` of length 10000 and `H_vec` of shape (10000, 768), all finite
- Each has a sidecar `.meta.json` whose cfg names seed, n_charts, mode, batch_size, n_rows, subsample file and convention
- `05_inter_seed_diagnostics.json` carries three pairwise Spearman entries, three pairwise direction entries, three per-seed summaries, and the `r_over_R` null disclosure
- `notebooks/.cache/05_curvature_field.npz` does not exist
- `notebooks/.cache/05_curvature_probe_decodability.jsonl` does not exist
- `git status --porcelain notebooks/pu_manifold/linear_probe.py src/effdim/` produces no output
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
</verification>

<success_criteria>
D5-03 is satisfied in fact and not only in code: the decoder-side field exists for all three sealed
seeds over all 10,000 points, produced by the corrected `chart_curvature_field` call. D5-05 is
satisfied and non-gating: inter-seed agreement is measured, reported with its direction axis, and
changes no verdict. D5-06's convention travels with every artifact. The evidence `05-03`'s one-way
pooling decision requires is measured and on record, and no probe number exists.
</success_criteria>

<output>
Create `.planning/phases/05-curvature-conditioned-linear-decodability/05-02-SUMMARY.md` when done
</output>
