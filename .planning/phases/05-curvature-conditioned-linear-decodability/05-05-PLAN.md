---
phase: 05-curvature-conditioned-linear-decodability
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - notebooks/diagnostics/curvature_probe_decodability_run.py
autonomous: true
requirements: [D5-01, D5-02, D5-05, D5-07, D5-08, D5-09, D5-10, D5-13]

flagged_assumptions:
  - "AMENDED PLAN. There is no pooled field. The bucketed run scores THREE per-seed bucketings and emits three per-seed verdicts plus one phase verdict, per 05-03-DECISION.md, which superseded 05-CONTEXT.md D5-04 at the 05-03 Task 1 blocking checkpoint. The runner decides nothing about how the three combine: it calls linear_probe.combine_seed_verdicts with the frozen SEED_VERDICT_COMBINATION_RULE, which existed in an ancestor commit before this plan ran."
  - "PLANNER DECISION, recorded rather than assumed: ONE ridge fit is computed, not three. D5-02 pre-registers one global W fit on one train/test split; the split seed, the split rule and the data are identical for all three seeds, so three fits would return three bit-identical maps. The per-seed axis enters at BUCKETING, not at fitting. Fitting three times would burn compute, invite drift between the three W matrices, and quietly contradict D5-02's one-model-everywhere guarantee. The acceptance criteria pin this: exactly one fit_probe call site, and the three per-seed rows share one selected_alpha and one r2_overall."
  - "RESEARCH A2's stated reason for ridge (the 768-d design matrix is effectively rank-deficient at the manifold's established 18-to-25 intrinsic dimension) is CHECKED here against the training split's own measured singular spectrum, per A2's own instruction that the reason should be verified before being asserted as fact. Whatever the spectrum shows is reported. If the split turns out well-conditioned and RidgeCV selects the smallest alpha in the frozen grid, that is reported as the fit degrading to near-OLS, not hidden."
  - "RESEARCH A4 (roughly 1000 test points per bucket) is NOT assumed. With three independent bucketings there are THREE realized per-bucket count vectors over the same 3,000 test points, and all three are measured and reported whatever they are. The D5-08 size-matched check runs per seed against that seed's own realized counts. This is RESEARCH Pitfall 4 and it is the exact artifact that undercut Phase 4's verdict."
  - "This plan is the first in the phase to compute a PU probe number. Every commit it makes must have the 05-04 freeze commit as a git ancestor, and notebooks/pu_manifold/linear_probe.py must be byte-identical to its state at that commit. If a genuine defect in the frozen module is discovered here, it is recorded as a pre-registration BREACH in 05-FINDINGS.md with the diff and the reason, never silently patched."
  - "Spec-less probe fallback, edge half. This plan authors 4 of the 29 probe items into must_haves.truths (2 of them as backstop markers) and surfaces 2 unclassified rows as the flagged assumptions immediately below. Unclassified rows are never auto-backstopped and never silently dropped."
  - "EDGE PROBE UNRESOLVED (D5-02, category `unclassified`): `unclassified -- review manually`. Not auto-backstopped. Surfaced here as a flagged assumption. D5-02 (`fit globally, evaluate per-region`) is a protocol structure decision, not a data-shape constraint, so no edge category applies. Its content is verified by this plan's exactly-one-fit_probe-call-site criterion and by the three per-seed rows sharing one selected_alpha."
  - "EDGE PROBE UNRESOLVED (D5-08, category `unclassified`): `unclassified -- review manually`. Not auto-backstopped. Surfaced here as a flagged assumption. D5-08 (`bucket sizes must be reported and the comparison checked against a size-matched version`) is a reporting-and-sensitivity requirement, not a data-shape constraint, so no edge category applies. Its content is verified by the per-seed size_match_n being at most that seed's smallest realized test-split bucket_n, and by the realized counts summing to n_test for every seed."

must_haves:
  truths:
    - "Exactly one linear map W is fit, on the training split only, predicting legacysurvey from hsc, both 768-dimensional, from the same resolved subsample the three curvature fields were extracted on; the three per-seed evaluations share that one W (D5-01, D5-02)"
    - "The fit is global: one W is fit on a split drawn from the whole manifold, per-point residuals are computed on the held-out test split, and only then are those same residuals bucketed three times, once per seed's frozen edges; no per-bucket and no per-seed model is fit (D5-02)"
    - "Every frozen constant used in the run is read from notebooks/pu_manifold/linear_probe.py, never re-specified at the call site, and every one is echoed into each JSONL row (D5-09)"
    - "assert_preregistered() and the existence check for all three frozen per-seed bucket artifacts both run before any data is touched in the bucketed branch (D5-10)"
    - "The guard compares each seed's artifact bucket_edges against the matching inner tuple of the frozen BUCKET_EDGES_PER_SEED as exact float64 equality with no tolerance, and raises ValueError naming the seed and both values on any mismatch (D5-10)"
    - "Per-seed realized TEST-SPLIT bucket counts are computed and recorded for all three seeds, and each seed's D5-08 size-matched check subsamples to the smallest of THAT SEED's realized counts, never to a full-field count and never to a count borrowed from another seed (D5-08)"
    - "The continuous statistic spearman(that seed's curvature magnitude on the test split, per-point residual) is computed and recorded once per seed alongside each seed's bucketed comparison, with its direction axis recorded as an explicit null and reason because both operands are scalars (D5-07, D5-05)"
    - "Three per-seed verdicts are produced by apply_verdict_rule reading the committed VERDICT_RULE, and the single phase verdict is produced by combine_seed_verdicts reading the committed SEED_VERDICT_COMBINATION_RULE; the runner never branches on a threshold written at the call site and never decides how the three combine (D5-09)"
    - "The phase verdict is a member of the frozen PHASE_VERDICT_VALUES tuple and no fourth outcome and no numeric score is emitted (D5-09)"
    - "train_test_split_indices raises ValueError rather than returning a degenerate split when it is given fewer than two points, and fit_probe raises ValueError on an empty, non-two-dimensional, row-misaligned or non-finite training block, so no degenerate map can reach the bucketing (D5-01)"
    - "The training split's singular spectrum and condition number are measured and recorded, and the selected ridge alpha is recorded as an output of the frozen selection rule (RESEARCH A2)"
    - "notebooks/.cache/05_curvature_probe_decodability.jsonl exists with 3 * N_BUCKETS bucket rows each carrying its seed_stem, three per-seed rows, one overall row and one conditioning row, every row carrying full provenance"
    - statement: "hsc and legacysurvey rows are matched by position in the resolved subsample npz and never by any string key, so no text-encoding, code-point or normalization definition of equality applies to the probe's row alignment; the row count is asserted equal across both blocks and all three field artifacts before any fit"
      verification: backstop
    - statement: "The runner appends one complete JSONL row at a time with an explicit flush, so an interrupted or parallel run leaves a prefix of complete rows and never a partially written one; and because the freeze commit is an ancestor of every commit that can carry a row, no row can exist at a commit that predates the rule that judges it"
      verification: backstop
    - statement: "The bucketed comparison and the continuous statistic are, for each seed, two views of the same underlying per-point quantity, so a disagreement between them is a fact about binning and not about two different measurements"
      verification: backstop
  prohibitions:
    - "MUST NOT modify notebooks/pu_manifold/linear_probe.py. It was frozen at 05-04 and git diff <freeze-commit> HEAD -- that file must remain empty"
    - "MUST NOT pool the three seeds' fields, average their verdicts into a score, or construct any single curvature field from more than one seed. Seed pooling was ratified as NOT DONE at the 05-03 Task 1 blocking checkpoint (05-03-DECISION.md), superseding 05-CONTEXT.md D5-04"
    - "MUST NOT decide at the call site how three per-seed verdicts combine, or what a split means. Both are frozen in SEED_VERDICT_COMBINATION_RULE and PHASE_VERDICT_VALUES and are applied by combine_seed_verdicts"
    - "MUST NOT fit a separate W per bucket or per seed as the headline; D5-02 explicitly rejects per-region independent fits because smaller buckets would get noisier W purely from having fewer training points, and three per-seed fits on one identical split would be three copies of one map"
    - "MUST NOT recompute any bucket edges here; they are read from the three per-seed artifacts and asserted equal to the frozen BUCKET_EDGES_PER_SEED"
    - "MUST NOT size-match against full-field bucket counts, or against another seed's counts; the match target is the smallest REALIZED TEST-SPLIT bucket count for that same seed (D5-08, RESEARCH Pitfall 4)"
    - "MUST NOT re-run with a different N_BUCKETS, split seed, alpha grid or residual metric after seeing a number; any such change is a pre-registration breach and must be recorded as one"
    - "MUST NOT drop, down-weight or re-order a seed because its verdict disagrees with the other two"
    - "MUST NOT let the continuous Spearman, the inter-seed diagnostics or the per-seed density confound change any verdict at either level; all three are reported and non-gating (D5-05, D5-07, D5-13)"
  artifacts:
    - path: "notebooks/.cache/05_curvature_probe_decodability.jsonl"
      provides: "Nine bucket rows, three per-seed rows, one overall row and one conditioning row, with the mechanically applied per-seed and phase verdicts and full provenance"
      contains: "phase_verdict"
    - path: "notebooks/diagnostics/curvature_probe_decodability_run.py"
      provides: "The implemented --mode bucketed branch, three bucketings over one fit"
      contains: "def run_bucketed_mode("
  key_links:
    - from: "notebooks/diagnostics/curvature_probe_decodability_run.py"
      to: "notebooks/pu_manifold/linear_probe.py"
      via: "every frozen constant is read off the module; per-seed verdicts come from apply_verdict_rule and the phase verdict from combine_seed_verdicts"
      pattern: "linear_probe\\.(VERDICT_RULE|SEED_VERDICT_COMBINATION_RULE|BUCKET_EDGES_PER_SEED|TRAIN_FRACTION|apply_verdict_rule|combine_seed_verdicts)"
    - from: "notebooks/diagnostics/curvature_probe_decodability_run.py"
      to: "notebooks/.cache/05_curvature_buckets_seed20260813.npz"
      via: "each seed's field and bucket labels are read from that seed's frozen artifact, never recomputed and never mixed"
      pattern: "bucket_labels"
---

<objective>
Compute the phase's actual result: one global ridge map from `hsc` to `legacysurvey`, one set of
held-out per-point residuals, bucketed three times by three independent per-seed curvature fields,
judged per seed under the rule frozen at `05-04` and combined into one phase read-out by the frozen
combination rule — all applied mechanically.

Purpose: this is the first plan in Phase 5 that produces a PU probe number, and every number it
produces is downstream of a commit that already contains the rule that judges it. Nothing here chooses
anything — the split fraction, the seed, the alpha grid, the residual metric, the three edge sets, the
size-match rule, the verdict text, the seed-combination rule and the three terminal phase outcomes are
all read off `notebooks/pu_manifold/linear_probe.py` and echoed into every record row, so a reader can
check them against `05-PREREGISTRATION.md` without running anything.

The three seeds' fields were measured at `05-02` to be mutually anti-correlated on rank and
directionally orthogonal, which is why they are not pooled (`05-03-DECISION.md`, superseding
`05-CONTEXT.md` D5-04). A `SPLIT ACROSS SEEDS` outcome is therefore the expected one. It is a complete
terminal outcome under the frozen rule, not a shortfall, and this plan reports it as such.

Output: `notebooks/.cache/05_curvature_probe_decodability.jsonl` with nine bucket rows, three per-seed
rows, one overall row, one conditioning row, the mechanically applied per-seed and phase verdicts, and
the sensitivity statistics that cannot change any of them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.claude/skills/spike-findings-effdim/SKILL.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-03-DECISION.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-CONTEXT.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-RESEARCH.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-PREREGISTRATION.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-04-SUMMARY.md
</context>

<superseded_decision>
`05-CONTEXT.md` **D5-04** ("pool the three cached CAE seeds into one averaged `||H||` field; the pooled
field is the verdict field") is **superseded** by `05-03-DECISION.md`. There is no pooled field, no
`POOLING_METHOD` constant and no pooled bucket edges. `--mode pool` raises. `05-CONTEXT.md` **D5-05**'s
pooled-versus-seed half has no referent and is dispositioned in
`notebooks/.cache/05_density_diagnostics.json` and in `05-PREREGISTRATION.md`. Every other D5 decision
stands.
</superseded_decision>

<artifacts_this_phase_produces>
Symbols and files Phase 5 CREATES (all new — exclude from drift/source-grounding checks).

**`notebooks/pu_manifold/linear_probe.py`** — NEW module, FROZEN at `05-04`, read-only here. 31
constants: `TRAIN_FRACTION`, `SPLIT_SEED`, `SPLIT_RULE`, `RIDGE_ALPHA_GRID`, `RIDGE_SELECTION_RULE`,
`ALPHA_PER_TARGET`, `FIT_INTERCEPT`, `EMBEDDING_PREPROCESSING`, `RESIDUAL_METRIC`, `R2_MULTIOUTPUT`,
`N_BUCKETS`, `BUCKET_RULE`, `BUCKET_EDGES_PER_SEED`, `SEED_HANDLING_RULE`,
`SEED_VERDICT_COMBINATION_RULE`, `PHASE_VERDICT_VALUES`, `SEED_STEMS`, `N_CHARTS`, `CURVATURE_MODE`,
`CURVATURE_CONVENTION`, `CURVATURE_SOURCE_FUNCTION`, `SIZE_MATCH_RULE`, `SIZE_MATCH_N_REPEATS`,
`SIZE_MATCH_SEED`, `N_BOOTSTRAP`, `BOOTSTRAP_SEED`, `CONFIDENCE_LEVEL`, `K_DENSITY`, `FIELD_D`,
`VERDICT_RULE`, `PREREGISTRATION_PATH`. Functions `train_test_split_indices`, `fit_probe`,
`predict_probe`, `per_point_residuals`, `aggregate_r2`, `pool_seed_fields` (retained, unused),
`bucket_edges_from_field`, `assign_buckets`, `bucket_by_field`, `bucket_counts`,
`bucket_residual_ci`, `size_matched_check`, `apply_verdict_rule`, `combine_seed_verdicts`,
`assert_preregistered`.

**Terminal outcome strings:** per-seed `HOLDS`, `NO DETECTABLE RELATIONSHIP`; phase-level
`HOLDS IN ALL THREE SEEDS`, `SPLIT ACROSS SEEDS`, `NO DETECTABLE RELATIONSHIP IN ANY SEED`.

**Retired names:** `POOLING_METHOD`, `BUCKET_EDGES` (removed at `05-03`).

**`notebooks/diagnostics/curvature_probe_decodability_run.py`** — NEW runner. Functions
`load_pu_pair`, `build_cae`, `load_converged_model`, `extract_seed_field`, `run_field_mode`,
`run_pool_mode` (a hard refusal), `run_perseed_mode`, `_effective_distinct_levels`,
`run_density_diagnostics`, `run_bucketed_mode`, **`_score_one_seed`** (new here), `_spearman_report`,
`_direction_axis_report`, `_load_cached_seed_field`, `run_inter_seed_diagnostics`, `selfcheck`,
`build_arg_parser`, `main`. Flags `--mode {field,pool,perseed,bucketed}`, `--selfcheck`, `--smoke`,
`--smoke-n`, `--seeds`, `--n-charts`, `--batch-size`, `--record-path`, `--field-stem`,
`--bucket-stem`, `--pooling-method` (tripwire).

**`notebooks/pu_manifold/tests/test_linear_probe.py`** — NEW test file: ten tests from `05-01`, four
added at `05-03`.

**`notebooks/05_curvature_conditioned_linear_decodability.ipynb`** — NEW notebook (`05-06`).

**Cache artifact keys** (gitignored): `05_curvature_field_seed2026081{3,4,5}`,
`05_curvature_buckets_seed2026081{3,4,5}`, `05_inter_seed_diagnostics.json`,
`05_density_diagnostics.json`, `05_curvature_probe_decodability.jsonl`, `05_probe_selfcheck.jsonl`.
**Retired:** `05_curvature_field` (the pooled npz) — never created.

**JSONL row kinds and keys** (created by this plan):

- `probe_overall` (one row): `kind`, `data_source`, `n`, `n_train`, `n_test`, `train_fraction`,
  `split_seed`, `selected_alpha`, `alpha_grid`, `r2_overall`, `mean_residual_overall`, `n_buckets`,
  `seed_stems`, `seed_handling_rule`, `bucket_edges_per_seed`, `per_seed_verdicts`, `n_holds`,
  `phase_verdict`, `phase_verdict_values`, `seed_verdict_combination_rule`,
  `curvature_source_function`, `curvature_convention`, `subsample_file`, `field_stem`,
  `bucket_stem`, `wallclock_s`.
- `probe_seed` (three rows): `kind`, `seed_stem`, `bucket_edges`, `realized_bucket_counts`,
  `full_field_bucket_counts`, `verdict`, `verdict_criteria`, `spearman_h_residual_rho`,
  `spearman_h_residual_p`, `spearman_h_residual_n`, `spearman_h_residual_undefined`,
  `spearman_direction_axis`, `spearman_direction_axis_reason`, `size_match_n`,
  `size_match_median_diff`, `size_match_sign_stable`, `size_match_ci_disjoint_fraction`,
  `size_match_n_repeats`, plus the shared provenance echo.
- `probe_bucket` (`3 * N_BUCKETS` rows): `kind`, `seed_stem`, `bucket_index`, `bucket_n`,
  `full_field_bucket_n`, `bucket_mean_residual`, `bucket_r2`, `ci_low`, `ci_high`, `degenerate`,
  `confidence_level`, `n_resamples`, plus the shared provenance echo.
- `probe_conditioning` (one row): `kind`, `singular_values_head`, `condition_number`,
  `effective_rank_1pct`, `effective_rank_0p1pct`, `effective_rank_0p01pct`, `cumvar_first_20`,
  `cumvar_first_25`, `selected_alpha`, `alpha_grid`, `alpha_at_grid_boundary`.

**Planning artifacts:** `05-03-DECISION.md`, `05-PREREGISTRATION.md`, `05-FINDINGS.md`,
`05-VERIFICATION.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the bucketed branch — one fit, three bucketings, every constant read</name>
  <reversibility rating="costly">D5-02's fit-globally-evaluate-per-region choice and the one-fit-three-bucketings shape are implemented here. Switching to per-bucket or per-seed fits after numbers exist would be a post-hoc re-specification of the pre-registered statistic.</reversibility>
  <precondition>notebooks/pu_manifold/linear_probe.py is frozen and linear_probe.assert_preregistered() returns without raising; all three notebooks/.cache/05_curvature_buckets_seed2026081{3,4,5}.npz artifacts exist.</precondition>
  <files>notebooks/diagnostics/curvature_probe_decodability_run.py</files>
  <read_first>
    - `notebooks/pu_manifold/linear_probe.py` — the FROZEN module. Read every constant and every function signature, in particular `BUCKET_EDGES_PER_SEED` (three inner tuples in `SEED_STEMS` order), `apply_verdict_rule` (per seed, returns `verdict` and `criteria`), `combine_seed_verdicts` (three seeds in, one `phase_verdict` out, raises on an empty rule and on any seed count other than three) and `size_matched_check` (requires the REALIZED test-split arrays and returns `n_match` and `realized_bucket_counts`). This task calls it; it must not edit it.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-PREREGISTRATION.md` — the committed record of what those constants mean, the verdict rule in full, and the seed-combination rule in full including what a split outcome means.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-03-DECISION.md` — the ratified refusal to pool, which is why this branch scores three bucketings rather than one.
    - `notebooks/diagnostics/curvature_probe_decodability_run.py` — the runner as written at `05-01` and extended at `05-02` and `05-03`; `run_bucketed_mode`'s body replaces its `NotImplementedError` here, behind the `assert_preregistered()` call and the three-artifact existence check that are already in place.
    - `notebooks/diagnostics/region_partition_mknn_run.py` lines 70-140 and 880-941 — `run_global_cell`'s flat-JSONL-row shape (one dict, every provenance field, `wallclock_s`) and the append loop with `json.dumps(r, default=float)` and `fh.flush()`.
    - `notebooks/pu_manifold/mknn.py` lines 124-167 — `bootstrap_ci`'s result shape, including `degenerate`, which the per-bucket CI mirrors.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-RESEARCH.md` § Common Pitfalls 3 and 4, and § Open Questions 2 (keep the probe pipeline and the curvature pipeline independent: the fit takes no curvature argument and the bucketing takes no probe argument).
  </read_first>
  <behavior>
    - Under `--mode bucketed --smoke`, the branch runs the complete path on a reduced point count, prints a SMOKE banner naming exactly what is reduced, writes nothing to the record file, and produces three per-seed verdicts each equal to one of the two per-seed terminal strings plus one phase verdict that is a member of `PHASE_VERDICT_VALUES`.
    - Exactly one probe fit happens per invocation. The three per-seed evaluations report the same `selected_alpha` and the same `r2_overall`, because they score the same residuals from the same map.
    - Every frozen constant used appears in the emitted row dicts with the value it has in the module. For each seed, the run compares that seed's artifact `bucket_edges` against the matching inner tuple of `BUCKET_EDGES_PER_SEED` and raises `ValueError` naming the seed and both values on any mismatch, rather than proceeding.
    - The bucketed comparison scores the TEST split only, for every seed; training rows never appear in any bucket statistic, and every seed's realized bucket counts sum exactly to `n_test`.
    - Each seed's size-matched check is called with that seed's realized test-split labels, and each returned match size is at most that seed's own smallest realized test-split bucket count.
    - Each per-seed verdict string is produced only by `apply_verdict_rule`; the phase verdict is produced only by `combine_seed_verdicts`; no comparison operator in the runner decides either.
    - Passing `--pooling-method` under this mode raises `RuntimeError` naming the ratified refusal, as it does under every other mode.
  </behavior>
  <action>
Implement `run_bucketed_mode`'s body and a `_score_one_seed` helper. Do not touch the
`assert_preregistered()` call or the three-artifact existence check that precede the body; the guard
order is D5-10's and it does not change.

Read every frozen constant off `linear_probe` — `TRAIN_FRACTION`, `SPLIT_SEED`, `RIDGE_ALPHA_GRID`,
`ALPHA_PER_TARGET`, `FIT_INTERCEPT`, `R2_MULTIOUTPUT`, `N_BUCKETS`, `BUCKET_EDGES_PER_SEED`,
`SEED_STEMS`, `SEED_HANDLING_RULE`, `N_BOOTSTRAP`, `BOOTSTRAP_SEED`, `CONFIDENCE_LEVEL`,
`SIZE_MATCH_N_REPEATS`, `SIZE_MATCH_SEED`, `VERDICT_RULE`, `SEED_VERDICT_COMBINATION_RULE`,
`PHASE_VERDICT_VALUES` — and pass each one explicitly into the function that needs it. Do not add a CLI
flag that overrides any of them; a flag that can change a frozen value is a hole in the
pre-registration.

Load `hsc` and `legacysurvey` with `load_pu_pair`. For each seed in `SEED_STEMS` order, load
`H_norm`, `bucket_labels` and `bucket_edges` from `cache.cache_path(f"{a.bucket_stem}_seed{seed}",
"npz")` and assert that artifact's cfg-manifest subsample path equals the resolved one, raising
`ValueError` naming both paths on mismatch. Assert each seed's loaded edges equal
`BUCKET_EDGES_PER_SEED[i]` elementwise as float64 with no tolerance, raising `ValueError` naming the
seed on mismatch — the artifacts and the pre-registration must agree or nothing runs. Assert all four
row counts (both embedding blocks and all three fields) are 10,000.

Split ONCE with `linear_probe.train_test_split_indices(n, TRAIN_FRACTION, SPLIT_SEED)`. Fit ONCE with
`linear_probe.fit_probe(X_hsc[train], X_ls[train], RIDGE_ALPHA_GRID, ALPHA_PER_TARGET,
FIT_INTERCEPT)` — one map on the whole manifold, per D5-02. There is exactly one `fit_probe` call site
in this file. Fitting once and bucketing three times is deliberate: the split seed, the split rule and
the data are identical across seeds, so three fits would return three bit-identical maps, and the
per-seed axis belongs to the bucketing, not to the fitting. Predict on the test split, then compute
`per_point_residuals` and `aggregate_r2` there, once. Those residuals are the single underlying
quantity all three seeds' comparisons are views of.

`_score_one_seed(seed, H_norm, bucket_labels, residuals_test, test_idx, ...)` then does, per seed:

- Take `bucket_labels[test_idx]` — the labels were assigned at `05-03` against that seed's full-field
  edges, so this is a lookup, not a re-cut.
- Compute per bucket: realized `n`, mean per-point residual, aggregate R-squared restricted to that
  bucket's rows, and `linear_probe.bucket_residual_ci(residuals_in_bucket, N_BOOTSTRAP,
  BOOTSTRAP_SEED, CONFIDENCE_LEVEL)`.
- Call `linear_probe.size_matched_check(residuals_test, labels_test, SIZE_MATCH_N_REPEATS,
  SIZE_MATCH_SEED, CONFIDENCE_LEVEL)` with that seed's realized test-split arrays.
- Compute the continuous statistic through `_spearman_report(H_norm[test_idx], residuals_test,
  f"D5-07 continuous seed {seed}")`. Record its direction axis as an explicit null with the reason
  that both operands are scalar fields, so no vector direction axis exists and the sign of rho is the
  direction — the same visible-non-application form `05-02` used for `r/R`. Record it; do not omit it.
- Call `linear_probe.apply_verdict_rule(bucket_stats, size_match, linear_probe.VERDICT_RULE)` and
  return its verdict string and criteria dict.

Print, per seed, the realized per-bucket test counts and the match size it chose, side by side with
that seed's full-field counts, so the distinction RESEARCH Pitfall 4 names is visible in the output
rather than buried in a dict. Print the three per-seed verdicts together on adjacent lines so the
spread is legible at a glance.

Then call `linear_probe.combine_seed_verdicts({seed: verdict for each seed},
linear_probe.SEED_VERDICT_COMBINATION_RULE)` and use its returned `phase_verdict` as the phase
read-out. Assert it is a member of `linear_probe.PHASE_VERDICT_VALUES`. The runner must contain no
comparison that could produce a verdict at either level, and must not compute any score, percentage or
majority of its own.

Emit rows: one `kind` of `probe_bucket` per (seed, bucket) — `3 * N_BUCKETS` rows, each carrying its
`seed_stem` — one `kind` of `probe_seed` per seed, and one `kind` of `probe_overall`, all with
`data_source` set to `pu` and all carrying the full key set listed in this plan's artifacts section.
Append with `json.dumps(row, default=float)` and `fh.flush()` after each row to
`notebooks/.cache/05_curvature_probe_decodability.jsonl` — and under `--smoke`, write nothing.

Verify this task with `--mode bucketed --smoke` only. The full run is Task 2.
  </action>
  <verify>
    <automated>.venv/bin/python notebooks/diagnostics/curvature_probe_decodability_run.py --mode bucketed --smoke && .venv/bin/python -c "
import pathlib, subprocess, sys
assert not pathlib.Path('notebooks/.cache/05_curvature_probe_decodability.jsonl').exists(), 'smoke wrote to the real record'
p = subprocess.run([sys.executable,'notebooks/diagnostics/curvature_probe_decodability_run.py','--mode','bucketed','--smoke','--pooling-method','per_seed_median_divide'],capture_output=True,text=True)
assert p.returncode != 0 and '05-03-DECISION' in (p.stderr + p.stdout), p.stderr[-2000:]
src = pathlib.Path('notebooks/diagnostics/curvature_probe_decodability_run.py').read_text().splitlines()
body = [l for l in src if not l.lstrip().startswith('#')]
n_fit = sum(l.count('linear_probe.fit_probe') for l in body)
assert n_fit == 1, ('expected exactly one fit_probe call site, found', n_fit)
for tok in ('linear_probe.apply_verdict_rule','linear_probe.combine_seed_verdicts','linear_probe.size_matched_check'):
    assert sum(l.count(tok) for l in body) >= 1, tok
print('smoke clean, record untouched, one fit site, pooling refused')" && .venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q && git diff --quiet -- notebooks/pu_manifold/linear_probe.py && echo 'frozen module untouched'</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python notebooks/diagnostics/curvature_probe_decodability_run.py --mode bucketed --smoke` exits 0, prints a line containing `SMOKE`, prints three per-seed verdicts each equal to `HOLDS` or `NO DETECTABLE RELATIONSHIP`, and prints one phase verdict equal to one of `HOLDS IN ALL THREE SEEDS`, `SPLIT ACROSS SEEDS` or `NO DETECTABLE RELATIONSHIP IN ANY SEED`
    - The smoke run writes no file at `notebooks/.cache/05_curvature_probe_decodability.jsonl`
    - `.venv/bin/python notebooks/diagnostics/curvature_probe_decodability_run.py --mode bucketed --smoke --pooling-method per_seed_median_divide` exits non-zero with a `RuntimeError` naming `05-03-DECISION.md`
    - `grep -v '^#' notebooks/diagnostics/curvature_probe_decodability_run.py | grep -c 'def run_bucketed_mode('` returns 1
    - `grep -v '^#' notebooks/diagnostics/curvature_probe_decodability_run.py | grep -c 'linear_probe.fit_probe'` returns exactly 1 — one global fit, not one per bucket and not one per seed
    - `grep -v '^#' notebooks/diagnostics/curvature_probe_decodability_run.py | grep -c 'linear_probe.apply_verdict_rule'` returns at least 1
    - `grep -v '^#' notebooks/diagnostics/curvature_probe_decodability_run.py | grep -c 'linear_probe.combine_seed_verdicts'` returns at least 1
    - `grep -v '^#' notebooks/diagnostics/curvature_probe_decodability_run.py | grep -c 'linear_probe.size_matched_check'` returns at least 1
    - The smoke output prints, for each of the three seeds, the realized per-bucket test counts and that seed's chosen size-match count on adjacent lines beside that seed's full-field counts
    - The smoke output prints the three per-seed verdicts on adjacent lines before printing the phase verdict
    - `git diff --quiet -- notebooks/pu_manifold/linear_probe.py` exits 0
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
  </acceptance_criteria>
  <done>The bucketed branch is implemented end to end as one fit and three bucketings, reads every constant off the frozen module, produces its per-seed verdicts and its phase verdict only through the committed rules, and proves itself on a reduced run that writes nothing.</done>
</task>

<task type="auto">
  <name>Task 2: The real run — the phase's headline numbers, three verdicts and their spread</name>
  <precondition>Task 1's smoke run exits 0 and notebooks/pu_manifold/linear_probe.py is unmodified since the 05-04 freeze commit recorded in 05-04-SUMMARY.md.</precondition>
  <files>notebooks/diagnostics/curvature_probe_decodability_run.py</files>
  <read_first>
    - `notebooks/diagnostics/curvature_probe_decodability_run.py` — the runner as completed at Task 1.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-PREREGISTRATION.md` — the frozen verdict rule and the frozen seed-combination rule including the split definition, read before the run so both verdicts are recognised rather than interpreted.
    - `.planning/phases/04-region-partitioning-regional-alignment-mknn/04-FINDINGS.md` § 5 and § 7 — how Phase 4 reported its bucketed comparison and, in § 7, the region-size artifact it discovered only at closure. Phase 5's realized-count reporting is the direct response to that.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-04-SUMMARY.md` — the recorded `freeze_commit` SHA the ancestry check uses.
  </read_first>
  <action>
Before running anything, confirm the ordering guarantee still holds: `git diff --quiet --
notebooks/pu_manifold/linear_probe.py` exits 0, and `git merge-base --is-ancestor` returns success for
the `freeze_commit` SHA recorded in `05-04-SUMMARY.md` against `HEAD`. If either fails, stop and
report — a probe number computed at a commit that is not a descendant of the freeze is worthless.

Then run `--mode bucketed` on the full 10,000 points, once, with no flag overrides.

Record in the plan summary, exactly as measured and with no rounding beyond four significant figures:
- `n_train`, `n_test`, and the selected ridge alpha with the frozen grid beside it, stated once —
  there is one fit and therefore one alpha shared by all three seeds' comparisons;
- overall test-split R-squared and mean per-point residual, likewise stated once;
- per seed, in `SEED_STEMS` order: that seed's two bucket edges, its full-field per-bucket counts, and
  its REALIZED test-split per-bucket counts side by side. If any seed's realized counts differ from
  `n_test` divided by `N_BUCKETS` by more than a few percent, say so explicitly, because that is the
  condition RESEARCH Pitfall 4 warns about — and note whether the three seeds' realized imbalances
  differ from each other, since they bucket the same 3,000 test points three different ways;
- per seed and per bucket: `n`, mean residual, R-squared, and the bootstrap CI with its `degenerate`
  flag;
- per seed: the size-matched check — match size, median difference, sign stability, and the fraction
  of repeats with disjoint CIs;
- per seed: the continuous Spearman between that seed's curvature magnitude and residual on the test
  split, with its p-value and n, and the recorded null direction axis with its reason;
- the three per-seed verdicts, quoted from their rows, on adjacent lines;
- the phase verdict, quoted from the overall row, with the note that the per-seed verdicts came from
  the module's verdict function and the phase verdict from the module's combination function, neither
  from a comparison in the runner.

Then state, in the phase's own words and in one short paragraph, what the numbers say — including the
case where they say nothing and the case where they disagree with each other. Under the frozen rule,
`NO DETECTABLE RELATIONSHIP IN ANY SEED` and `SPLIT ACROSS SEEDS` are both complete outcomes; report
whichever occurred as a result, not as a shortfall. Do not soften a null by leaning on the continuous
statistic or on any bucket other than a seed's headline pair, and do not read a split as partial
support — the frozen combination rule already says why one or two seeds out of three is a property of
those decoder fits and not of the manifold. Do not drop or down-weight a seed whose verdict disagrees.

Run the run once. If it needs to be re-run for a mechanical reason (a crash, a missing artifact), say
so in the summary and record how many times it ran. Do not re-run it after seeing a number in the hope
of a different one; that is the move the pre-registration exists to prevent.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, pathlib, sys
sys.path.insert(0,'notebooks')
from pu_manifold import linear_probe as lp
rows = [json.loads(l) for l in pathlib.Path('notebooks/.cache/05_curvature_probe_decodability.jsonl').read_text().splitlines() if l.strip()]
buckets = [r for r in rows if r['kind'] == 'probe_bucket']
seeds = [r for r in rows if r['kind'] == 'probe_seed']
overall = [r for r in rows if r['kind'] == 'probe_overall']
assert len(overall) == 1, len(overall)
assert len(seeds) == 3, len(seeds)
assert len(buckets) == 3 * lp.N_BUCKETS, (len(buckets), lp.N_BUCKETS)
o = overall[0]
assert o['data_source'] == 'pu'
assert o['phase_verdict'] in lp.PHASE_VERDICT_VALUES, o['phase_verdict']
assert tuple(o['seed_stems']) == tuple(lp.SEED_STEMS)
assert o['seed_handling_rule'] == lp.SEED_HANDLING_RULE == 'no_pooling_per_seed_verdicts'
assert o['n_train'] + o['n_test'] == 10000, (o['n_train'], o['n_test'])
assert abs(o['train_fraction'] - lp.TRAIN_FRACTION) < 1e-12 and o['split_seed'] == lp.SPLIT_SEED
assert o['curvature_convention'] == 'trace' and o['curvature_source_function'] == lp.CURVATURE_SOURCE_FUNCTION
assert o['n_holds'] == sum(1 for s in seeds if s['verdict'] == 'HOLDS'), (o['n_holds'], [s['verdict'] for s in seeds])
for i, s in enumerate(lp.SEED_STEMS):
    row = [r for r in seeds if r['seed_stem'] == s]
    assert len(row) == 1, (s, len(row))
    row = row[0]
    assert row['verdict'] in ('HOLDS','NO DETECTABLE RELATIONSHIP'), (s, row['verdict'])
    assert tuple(float(x) for x in row['bucket_edges']) == tuple(float(x) for x in lp.BUCKET_EDGES_PER_SEED[i]), s
    assert row['spearman_direction_axis'] is None and row['spearman_direction_axis_reason'], s
    br = [b for b in buckets if b['seed_stem'] == s]
    assert len(br) == lp.N_BUCKETS, (s, len(br))
    realized = sorted(b['bucket_n'] for b in br)
    assert sum(realized) == o['n_test'], (s, realized, o['n_test'])
    assert row['size_match_n'] <= realized[0], (s, row['size_match_n'], realized)
    for b in br:
        for k in ('bucket_mean_residual','bucket_r2','ci_low','ci_high','degenerate','confidence_level','n_resamples','full_field_bucket_n'):
            assert k in b, (s, b['bucket_index'], k)
assert len({s['seed_stem'] for s in seeds}) == 3
print('phase verdict:', o['phase_verdict'], '| per-seed:', {s['seed_stem']: s['verdict'] for s in seeds}, '| alpha:', o['selected_alpha'])" && git diff --quiet -- notebooks/pu_manifold/linear_probe.py && echo 'frozen module untouched'  </automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/.cache/05_curvature_probe_decodability.jsonl` exists with exactly `3 * N_BUCKETS` rows of kind `probe_bucket`, exactly three rows of kind `probe_seed`, and exactly one row of kind `probe_overall`, all with `data_source` equal to `pu`
    - Every `probe_bucket` and `probe_seed` row carries a `seed_stem`, and the three distinct seed stems are exactly `SEED_STEMS`
    - Each `probe_seed` row's `verdict` is exactly one of `HOLDS` or `NO DETECTABLE RELATIONSHIP`
    - The `probe_overall` row's `phase_verdict` is a member of the module's `PHASE_VERDICT_VALUES`, and its `n_holds` equals the count of `probe_seed` rows whose verdict is `HOLDS`
    - The `probe_overall` row's `seed_handling_rule` equals `no_pooling_per_seed_verdicts`, and no row carries a pooled field, a pooling method or a pooled edge set
    - `n_train + n_test` equals 10000, `train_fraction` equals the module's `TRAIN_FRACTION`, `split_seed` equals the module's `SPLIT_SEED`, `curvature_convention` equals `trace`, and `curvature_source_function` equals `chart_curvature.chart_curvature_field`
    - Each `probe_seed` row's `bucket_edges` equal the matching inner tuple of the module's `BUCKET_EDGES_PER_SEED` elementwise
    - For every seed, the per-bucket `bucket_n` values sum exactly to `n_test`, and that seed's `size_match_n` is at most that seed's smallest realized `bucket_n`
    - Every `probe_bucket` row carries `bucket_mean_residual`, `bucket_r2`, `ci_low`, `ci_high`, `degenerate`, `confidence_level`, `n_resamples` and `full_field_bucket_n`
    - Every `probe_seed` row carries `spearman_h_residual_rho`, `spearman_h_residual_p`, `spearman_h_residual_n`, `spearman_h_residual_undefined`, `spearman_direction_axis` (null) and a non-empty `spearman_direction_axis_reason`
    - All three `probe_seed` rows report the same `selected_alpha` and the same `r2_overall` as the `probe_overall` row — one fit, three bucketings
    - `git diff --quiet -- notebooks/pu_manifold/linear_probe.py` exits 0
    - `git merge-base --is-ancestor` succeeds for the `freeze_commit` SHA recorded in `05-04-SUMMARY.md` against `HEAD`
    - The plan summary records every quantity listed in the action, per seed where the action says per seed, including all three realized per-bucket count vectors beside their full-field counts, and states in the phase's own words what the numbers say
    - The plan summary records how many times the run was executed
  </acceptance_criteria>
  <done>The phase's headline numbers exist, produced by the frozen protocol at a commit descended from the freeze: one global map, three per-seed bucketed comparisons, three per-seed verdicts applied mechanically, one phase verdict combined mechanically, and three realized test-split bucket count vectors reported beside their full-field counts.</done>
</task>

<task type="auto">
  <name>Task 3: Check the ridge justification against the training split's own spectrum</name>
  <files>notebooks/diagnostics/curvature_probe_decodability_run.py</files>
  <read_first>
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-RESEARCH.md` § Common Pitfalls 3 and § Assumptions Log A2 — the claim being checked (that the 768-d design matrix is severely rank-deficient in effective terms at the manifold's established 18-to-25 intrinsic dimension) and A2's own instruction that the reason should be checked against the measured spectrum before being asserted as fact.
    - `notebooks/diagnostics/curvature_probe_decodability_run.py` — the runner as completed at Task 2; this task adds a diagnostic block and changes no computed statistic.
    - `notebooks/.cache/05_curvature_probe_decodability.jsonl` — the rows from Task 2; the diagnostic is APPENDED as an additional row and the existing rows are not rewritten.
  </read_first>
  <action>
Add a diagnostic row with `kind` set to `probe_conditioning`, computed from the training split's `hsc`
block alone, and append it to the same JSONL record. This changes no statistic and cannot change any
verdict at either level; it exists so the stated reason for using ridge is checked rather than
asserted. There is one training split and one fit, so there is exactly one conditioning row and it is
not per seed.

Compute the singular values of the mean-centered training design matrix with
`np.linalg.svd(X_train - X_train.mean(axis=0), compute_uv=False)`. Record: the first 40 singular values
as `singular_values_head`; `condition_number` as largest over smallest; the effective rank at three
thresholds as `effective_rank_1pct`, `effective_rank_0p1pct` and `effective_rank_0p01pct` (the count
of singular values above 1 percent, 0.1 percent and 0.01 percent of the largest); and the cumulative
variance fraction captured by the first 20 and first 25 components as `cumvar_first_20` and
`cumvar_first_25`.

Record beside them `selected_alpha` from Task 2, `alpha_grid` copied from the frozen constant, and a
boolean `alpha_at_grid_boundary` that is true when the selected alpha is the smallest or the largest
value in the grid — a selection at the boundary means the grid was too narrow and must be reported as
such, not quietly accepted.

Then state in the plan summary which way the check came out, in one short paragraph:
- If the spectrum drops sharply after roughly 20 to 25 values, RESEARCH A2's reason is confirmed and
  ridge was the right default for the stated reason.
- If the spectrum is flat and the condition number is modest, say plainly that the design matrix was
  better conditioned than A2 expected, that the selected alpha was small, and that the fit therefore
  behaved close to ordinary least squares. That invalidates nothing — the frozen rule selected the
  alpha, not the planner — but the reason recorded in `05-PREREGISTRATION.md` would then be wrong, and
  `05-FINDINGS.md` must say so.
- If the selected alpha sits at a grid boundary, say so and state that the frozen grid was too narrow,
  which is a real limitation on this phase's fit and must appear in the findings.

Do not change `RIDGE_ALPHA_GRID`. It is frozen. A too-narrow grid is a reported limitation, not a
thing to fix mid-phase.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, pathlib, sys
sys.path.insert(0,'notebooks')
from pu_manifold import linear_probe as lp
rows = [json.loads(l) for l in pathlib.Path('notebooks/.cache/05_curvature_probe_decodability.jsonl').read_text().splitlines() if l.strip()]
c = [r for r in rows if r['kind'] == 'probe_conditioning']
assert len(c) == 1, len(c)
c = c[0]
for k in ('singular_values_head','condition_number','effective_rank_1pct','effective_rank_0p1pct','effective_rank_0p01pct','cumvar_first_20','cumvar_first_25','selected_alpha','alpha_grid','alpha_at_grid_boundary'):
    assert k in c, k
assert len(c['singular_values_head']) == 40, len(c['singular_values_head'])
assert tuple(float(a) for a in c['alpha_grid']) == tuple(float(a) for a in lp.RIDGE_ALPHA_GRID)
assert isinstance(c['alpha_at_grid_boundary'], bool)
assert len([r for r in rows if r['kind']=='probe_overall']) == 1, 'the overall row was rewritten'
assert len([r for r in rows if r['kind']=='probe_seed']) == 3, 'a per-seed row was rewritten'
assert len([r for r in rows if r['kind']=='probe_bucket']) == 3 * lp.N_BUCKETS, 'a bucket row was rewritten'
print('conditioning: cond=', c['condition_number'], 'eff_rank_1pct=', c['effective_rank_1pct'], 'alpha=', c['selected_alpha'], 'at_boundary=', c['alpha_at_grid_boundary'])" && git diff --quiet -- notebooks/pu_manifold/linear_probe.py</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/.cache/05_curvature_probe_decodability.jsonl` contains exactly one row of kind `probe_conditioning`
    - That row carries `singular_values_head` of length 40, `condition_number`, `effective_rank_1pct`, `effective_rank_0p1pct`, `effective_rank_0p01pct`, `cumvar_first_20`, `cumvar_first_25`, `selected_alpha`, `alpha_grid` and `alpha_at_grid_boundary`
    - Its `alpha_grid` equals the module's `RIDGE_ALPHA_GRID` elementwise and `alpha_at_grid_boundary` is a boolean
    - The rows from Task 2 are unchanged: exactly one `probe_overall`, exactly three `probe_seed`, and exactly `3 * N_BUCKETS` `probe_bucket` rows remain
    - `git diff --quiet -- notebooks/pu_manifold/linear_probe.py` exits 0
    - The plan summary states, in the phase's own words, whether the measured spectrum confirms RESEARCH A2's stated reason for ridge, and states explicitly if the selected alpha sat at a grid boundary
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
  </acceptance_criteria>
  <done>RESEARCH A2's justification for ridge is measured against the training split's own spectrum rather than asserted, the selected alpha is recorded as an output of the frozen rule, and a too-narrow grid would be visible rather than silent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen constants -> computed result | The run reads every constant off the frozen module and echoes each into every record row, so the result and the rules that judged it travel together. |
| frozen per-seed artifact -> bucketed statistic | Each seed's bucket labels and edges come from that seed's own frozen artifact and are asserted equal to that seed's inner tuple of the frozen constant; no field is mixed across seeds and none is recomputed. |
| local filesystem -> `notebooks/.cache/` | The only persistence; every path composed through `cache.cache_path` under `_assert_inside_cache`. |
| git history -> evidential claim | The ordering guarantee is a repository-history property, checked before the run and proved mechanically at `05-06`. |

No network surface, no authentication, no session, no user-supplied input path. ASVS L1; block on
`high`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Tampering (of results, not security) | a frozen constant amended once a number exists | high | mitigate | The run reads constants only, never writes them; `git diff --quiet -- notebooks/pu_manifold/linear_probe.py` is asserted before the run and again in every task's verify; the constants are duplicated in the committed `05-PREREGISTRATION.md` so an amendment touches two files |
| T-05-15 | Tampering (of results) | a verdict decided by a comparison in the runner rather than by the committed rule | high | mitigate | Per-seed verdicts come only from `apply_verdict_rule` and the phase verdict only from `combine_seed_verdicts`, both of which raise when their rule text is empty; the acceptance criteria assert both call sites exist and both rule strings are echoed into the overall row |
| T-05-26 | Tampering (of results) | the three-into-one combination improvised at the call site — a majority vote, a score, or a fourth outcome | high | mitigate | `combine_seed_verdicts` is the only path to a phase outcome, its return is asserted to be a member of the frozen `PHASE_VERDICT_VALUES`, and `n_holds` in the overall row is asserted to equal the count of `HOLDS` per-seed rows so a hand-computed alternative would not reconcile |
| T-05-28 | Tampering (of results) | seeds pooled or a disagreeing seed dropped once the verdicts are known | high | mitigate | `combine_seed_verdicts` raises on any seed count other than three; the verify asserts three distinct `probe_seed` rows and three distinct seed stems; `--pooling-method` raises; `--mode pool` raises naming `05-03-DECISION.md` |
| T-05-16 | Tampering (of results) | a size-matched check run against full-field counts or against another seed's counts, reproducing Phase 4's region-size artifact | high | mitigate | Each check receives that seed's realized test-split label array; the verify asserts, per seed, that the returned match size is at most that seed's smallest realized bucket count and that its bucket counts sum to `n_test` and not to 10000 |
| T-05-17 | Spoofing (of provenance) | a probe fit on one subsample and bucketed by a field extracted from another | high | mitigate | The resolved subsample path is asserted equal to the one in each per-seed artifact's cfg manifest before any fit; each seed's edges are asserted equal to its frozen inner tuple; all row counts are asserted to be 10,000 |
| T-05-30 | Tampering (of results) | three separate fits drifting into three different maps and being reported as one global model | high | mitigate | Exactly one `fit_probe` call site is asserted by a comment-filtered grep, and the verify asserts all three `probe_seed` rows report the same `selected_alpha` and `r2_overall` as the overall row |
| T-05-18 | Repudiation | a smoke run's numbers mistaken for the real run's | medium | mitigate | The smoke branch writes nothing to the record and prints a SMOKE banner; the verify after Task 1 asserts the record file does not exist |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package |
</threat_model>

<verification>
- `notebooks/.cache/05_curvature_probe_decodability.jsonl` carries `3 * N_BUCKETS` bucket rows, three per-seed rows, one overall row and one conditioning row
- Every per-seed verdict is one of the two per-seed terminal strings and the phase verdict is a member of `PHASE_VERDICT_VALUES`
- `n_holds` equals the count of per-seed `HOLDS` rows
- Every frozen constant echoed into the rows matches `notebooks/pu_manifold/linear_probe.py`, including all three inner tuples of `BUCKET_EDGES_PER_SEED`
- Per-seed realized counts sum to `n_test` for every seed; every seed's size-match count is at most that seed's smallest realized count
- All three per-seed rows share one `selected_alpha` and one `r2_overall` — one global fit
- Every per-seed row records the continuous Spearman's direction axis as an explicit null with its reason
- `git diff --quiet -- notebooks/pu_manifold/linear_probe.py` exits 0
- `git merge-base --is-ancestor <freeze_commit> HEAD` exits 0
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
- `git status --porcelain src/effdim/` produces no output
- CLAUDE.md Swiss roll rule: **does not trigger.** This plan fits one ridge map and buckets residuals; it introduces no manifold-learning or representation-learning model, no bottleneck, no latent space and no decoder. The curvature estimator whose fields it reads is already covered by `notebooks/03_swiss_roll_chart_curvature_field_check.ipynb`. Recorded as a visible determination, not a silent skip.
</verification>

<success_criteria>
D5-01 and D5-02 are satisfied in fact: one global ridge map from `hsc` to `legacysurvey`, evaluated
per bucket on held-out points, with the per-seed axis entering at bucketing and never at fitting.
D5-04's superseding decision is honored in fact: three independent bucketings, three per-seed verdicts,
no pooled field, and a phase read-out produced only by the frozen combination rule. D5-07's magnitude
split and its binning-free continuous companion are reported per seed with the direction axis
disclosed as a visible non-application. D5-08's realized-test-split size match runs per seed against
that seed's own counts, built into the protocol rather than discovered afterwards. D5-10's guard ran
before any data was touched. Every verdict at both levels was applied mechanically from rules that
already existed in an ancestor commit, and RESEARCH A2's justification for ridge was measured rather
than asserted.
</success_criteria>

<output>
Create `.planning/phases/05-curvature-conditioned-linear-decodability/05-05-SUMMARY.md` when done
</output>
