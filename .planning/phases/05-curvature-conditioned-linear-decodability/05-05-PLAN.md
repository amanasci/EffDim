---
phase: 05-curvature-conditioned-linear-decodability
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - notebooks/diagnostics/curvature_probe_decodability_run.py
autonomous: true
requirements: [D5-01, D5-02, D5-05, D5-07, D5-08, D5-10, D5-13]

flagged_assumptions:
  - "RESEARCH A2's stated reason for ridge (the 768-d design matrix is effectively rank-deficient at the manifold's established 18-to-25 intrinsic dimension) is CHECKED here against the training split's own measured singular spectrum, per A2's own instruction that the reason should be verified before being asserted as fact. Whatever the spectrum shows is reported. If the split turns out well-conditioned and RidgeCV selects the smallest alpha in the frozen grid, that is reported as the fit degrading to near-OLS, not hidden."
  - "RESEARCH A4 (roughly 1000 test points per bucket) is NOT assumed. The realized post-split per-bucket counts are measured and reported whatever they are, and the D5-08 size-matched check runs against those realized counts. This is RESEARCH Pitfall 4 and it is the exact artifact that undercut Phase 4's verdict."
  - "This plan is the first in the phase to compute a PU probe number. Every commit it makes must have the 05-04 freeze commit as a git ancestor, and notebooks/pu_manifold/linear_probe.py must be byte-identical to its state at that commit. If a genuine defect in the frozen module is discovered here, it is recorded as a pre-registration BREACH in 05-FINDINGS.md with the diff and the reason, never silently patched."

must_haves:
  truths:
    - "One linear map W is fit on the training split only, predicting legacysurvey from hsc, both 768-dimensional, from the same resolved subsample the curvature field was extracted on (D5-01, D5-02)"
    - "The fit is global: exactly one W is fit on a split drawn from the whole manifold, and per-point residuals are computed on the held-out test split and then bucketed; no per-bucket model is fit (D5-02)"
    - "Every frozen constant used in the run is read from notebooks/pu_manifold/linear_probe.py, never re-specified at the call site, and every one is echoed into each JSONL row (D5-09)"
    - "assert_preregistered() and the frozen pooled-field artifact existence check both run before any data is touched in the bucketed branch (D5-10)"
    - "Per-bucket realized TEST-SPLIT counts are computed and recorded, and the D5-08 size-matched check subsamples to the smallest of those realized counts, not to the full-field count (D5-08)"
    - "The continuous statistic spearman(pooled curvature magnitude on the test split, per-point residual) is computed and recorded alongside the bucketed comparison (D5-07)"
    - "The verdict is produced by apply_verdict_rule reading the committed VERDICT_RULE, and the runner never branches on a threshold written at the call site (D5-09)"
    - "The training split's singular spectrum and condition number are measured and recorded, and the selected ridge alpha is recorded as an output of the frozen selection rule (RESEARCH A2)"
    - "notebooks/.cache/05_curvature_probe_decodability.jsonl exists with one row per bucket, one overall row and one conditioning row, every row carrying full provenance"
    - statement: "The bucketed comparison and the continuous statistic are two views of the same underlying per-point quantity, so a disagreement between them is a fact about binning and not about two different measurements"
      verification: backstop
  prohibitions:
    - "MUST NOT modify notebooks/pu_manifold/linear_probe.py. It was frozen at 05-04 and git diff <freeze-commit> HEAD -- that file must remain empty"
    - "MUST NOT fit a separate W per bucket as the headline; D5-02 explicitly rejects per-region independent fits because smaller buckets would get noisier W purely from having fewer training points"
    - "MUST NOT recompute bucket edges here; they are read from notebooks/.cache/05_curvature_field.npz and asserted equal to the frozen BUCKET_EDGES"
    - "MUST NOT size-match against full-field bucket counts; the match target is the smallest REALIZED TEST-SPLIT bucket count (D5-08, RESEARCH Pitfall 4)"
    - "MUST NOT re-run with a different N_BUCKETS, split seed, alpha grid or residual metric after seeing a number; any such change is a pre-registration breach and must be recorded as one"
    - "MUST NOT let the continuous Spearman, the inter-seed diagnostics or the density confound change the verdict; all three are reported and non-gating (D5-05, D5-07, D5-13)"
  artifacts:
    - path: "notebooks/.cache/05_curvature_probe_decodability.jsonl"
      provides: "One JSONL row per bucket, one overall row and one conditioning row, with the mechanically applied verdict and full provenance"
      contains: "verdict"
    - path: "notebooks/diagnostics/curvature_probe_decodability_run.py"
      provides: "The implemented --mode bucketed branch"
      contains: "def run_bucketed("
  key_links:
    - from: "notebooks/diagnostics/curvature_probe_decodability_run.py"
      to: "notebooks/pu_manifold/linear_probe.py"
      via: "every frozen constant is read off the module and the verdict comes from apply_verdict_rule"
      pattern: "linear_probe\\.(VERDICT_RULE|BUCKET_EDGES|TRAIN_FRACTION|apply_verdict_rule)"
    - from: "notebooks/diagnostics/curvature_probe_decodability_run.py"
      to: "notebooks/.cache/05_curvature_field.npz"
      via: "the pooled field and bucket labels are read from the frozen artifact, never recomputed"
      pattern: "pooled_H_norm"
---

<objective>
Compute the phase's actual result: one global ridge map from `hsc` to `legacysurvey`, held-out
per-point residuals, bucketed by the frozen pooled curvature magnitude, compared under the rule
frozen at `05-04` and applied mechanically.

Purpose: this is the first plan in Phase 5 that produces a PU probe number, and every number it
produces is downstream of a commit that already contains the rule that judges it. Nothing here
chooses anything — the split fraction, the seed, the alpha grid, the residual metric, the bucket
edges, the size-match rule and the verdict text are all read off
`notebooks/pu_manifold/linear_probe.py` and echoed into every record row, so a reader can check them
against `05-PREREGISTRATION.md` without running anything.

Output: `notebooks/.cache/05_curvature_probe_decodability.jsonl` with one row per bucket, one
overall row, one conditioning row, the mechanically applied verdict, and the sensitivity statistics
that cannot change it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.claude/skills/spike-findings-effdim/SKILL.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-CONTEXT.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-RESEARCH.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-PREREGISTRATION.md
@.planning/phases/05-curvature-conditioned-linear-decodability/05-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols and files Phase 5 CREATES (all new — exclude from drift/source-grounding checks).

**`notebooks/pu_manifold/linear_probe.py`** — NEW module, FROZEN at `05-04`. Constants
`TRAIN_FRACTION`, `SPLIT_SEED`, `SPLIT_RULE`, `RIDGE_ALPHA_GRID`, `RIDGE_SELECTION_RULE`,
`ALPHA_PER_TARGET`, `FIT_INTERCEPT`, `EMBEDDING_PREPROCESSING`, `RESIDUAL_METRIC`,
`R2_MULTIOUTPUT`, `N_BUCKETS`, `BUCKET_RULE`, `BUCKET_EDGES`, `POOLING_METHOD`, `SEED_STEMS`,
`N_CHARTS`, `CURVATURE_MODE`, `CURVATURE_CONVENTION`, `CURVATURE_SOURCE_FUNCTION`,
`SIZE_MATCH_RULE`, `SIZE_MATCH_N_REPEATS`, `SIZE_MATCH_SEED`, `N_BOOTSTRAP`, `BOOTSTRAP_SEED`,
`CONFIDENCE_LEVEL`, `K_DENSITY`, `FIELD_D`, `VERDICT_RULE`, `PREREGISTRATION_PATH`. Functions
`train_test_split_indices`, `fit_probe`, `predict_probe`, `per_point_residuals`, `aggregate_r2`,
`pool_seed_fields`, `bucket_edges_from_field`, `assign_buckets`, `bucket_by_field`,
`bucket_counts`, `bucket_residual_ci`, `size_matched_check`, `apply_verdict_rule`,
`assert_preregistered`.

**`notebooks/diagnostics/curvature_probe_decodability_run.py`** — NEW runner. Functions
`load_pu_pair`, `build_cae`, `load_converged_model`, `extract_seed_field`, `run_pool`,
`run_bucketed`, `_spearman_report`, `_direction_axis_report`, `selfcheck`, `build_arg_parser`,
`main`. Flags `--mode {field,pool,bucketed}`, `--selfcheck`, `--smoke`, `--smoke-n`, `--seeds`,
`--n-charts`, `--batch-size`, `--record-path`, `--field-stem`, `--pooling-method`.

**`notebooks/pu_manifold/tests/test_linear_probe.py`** — NEW test file (ten tests).

**`notebooks/05_curvature_conditioned_linear_decodability.ipynb`** — NEW notebook (`05-06`).

**Cache artifact keys** (gitignored): `05_curvature_field_seed20260813` / `..._seed20260814` /
`..._seed20260815`, `05_curvature_field`, `05_inter_seed_diagnostics.json`,
`05_density_diagnostics.json`, `05_curvature_probe_decodability.jsonl`, `05_probe_selfcheck.jsonl`.

**JSONL record keys:** `kind`, `data_source`, `n`, `n_train`, `n_test`, `train_fraction`,
`split_seed`, `selected_alpha`, `alpha_grid`, `r2_overall`, `mean_residual_overall`, `n_buckets`,
`bucket_edges`, `bucket_index`, `bucket_n`, `bucket_mean_residual`, `bucket_r2`, `ci_low`,
`ci_high`, `degenerate`, `confidence_level`, `n_resamples`, `spearman_h_residual_rho`,
`spearman_h_residual_p`, `size_match_n`, `size_match_median_diff`, `size_match_sign_stable`,
`size_match_ci_disjoint_fraction`, `verdict`, `pooling_method`, `curvature_source_function`,
`curvature_convention`, `seed_stems`, `subsample_file`, `field_stem`, `wallclock_s`. The
conditioning row adds `singular_values_head`, `condition_number`, `effective_rank_1pct`,
`effective_rank_0p1pct`, `effective_rank_0p01pct`, `cumvar_first_20`, `cumvar_first_25`,
`alpha_at_grid_boundary`.

**Planning artifacts:** `05-PREREGISTRATION.md`, `05-FINDINGS.md`, `05-VERIFICATION.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the bucketed branch — every constant read, none re-specified</name>
  <reversibility rating="costly">D5-02's fit-globally-evaluate-per-region choice is implemented here. Switching to per-bucket fits after numbers exist would be a post-hoc re-specification of the pre-registered statistic.</reversibility>
  <precondition>notebooks/pu_manifold/linear_probe.py is frozen and linear_probe.assert_preregistered() returns without raising; notebooks/.cache/05_curvature_field.npz exists.</precondition>
  <files>notebooks/diagnostics/curvature_probe_decodability_run.py</files>
  <read_first>
    - `notebooks/pu_manifold/linear_probe.py` — the FROZEN module. Read every constant and every function signature. This task calls it; it must not edit it.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-PREREGISTRATION.md` — the committed record of what those constants mean, and the verdict rule in full.
    - `notebooks/diagnostics/curvature_probe_decodability_run.py` — the runner as written at `05-01` and extended at `05-02` and `05-03`; `run_bucketed` replaces its `NotImplementedError` here, behind the guard that is already in place.
    - `notebooks/diagnostics/region_partition_mknn_run.py` lines 70-140 and 880-941 — `run_global_cell`'s flat-JSONL-row shape (one dict, every provenance field, `wallclock_s`) and the append loop with `json.dumps(r, default=float)` and `fh.flush()`.
    - `notebooks/pu_manifold/mknn.py` lines 124-167 — `bootstrap_ci`'s result shape, including `degenerate`, which the per-bucket CI mirrors.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-RESEARCH.md` § Common Pitfalls 3 and 4, and § Open Questions 2 (keep the probe pipeline and the curvature pipeline independent: the fit takes no curvature argument and the bucketing takes no probe argument).
  </read_first>
  <behavior>
    - Under `--mode bucketed --smoke`, the branch runs the complete path on a reduced point count, prints a SMOKE banner naming exactly what is reduced, writes nothing to the record file, and produces a verdict string that is one of the two terminal values.
    - Every frozen constant used appears in the emitted row dict with the value it has in the module; the run compares the bucket edges read from the pooled-field artifact against the frozen constant and raises `ValueError` on any mismatch rather than proceeding.
    - The bucketed comparison scores the TEST split only; training rows never appear in any bucket statistic.
    - The size-matched check is called with the realized test-split labels, and the returned match size is less than or equal to the smallest realized test-split bucket count.
    - The verdict string is produced only by the module's verdict function; no comparison operator in the runner decides it.
  </behavior>
  <action>
Implement `run_bucketed` and wire it to the `--mode bucketed` branch that already exists behind the
D5-10 guard from `05-01`. Do not touch the guard; it stays first.

Read every frozen constant off `linear_probe` — `TRAIN_FRACTION`, `SPLIT_SEED`, `RIDGE_ALPHA_GRID`,
`ALPHA_PER_TARGET`, `FIT_INTERCEPT`, `R2_MULTIOUTPUT`, `N_BUCKETS`, `BUCKET_EDGES`, `N_BOOTSTRAP`,
`BOOTSTRAP_SEED`, `CONFIDENCE_LEVEL`, `SIZE_MATCH_N_REPEATS`, `SIZE_MATCH_SEED`, `VERDICT_RULE` —
and pass each one explicitly into the function that needs it. Do not add a CLI flag that overrides
any of them; a flag that can change a frozen value is a hole in the pre-registration.

Load `hsc` and `legacysurvey` with `load_pu_pair` and assert the resolved subsample path equals the
one recorded in the pooled field artifact's cfg manifest, raising `ValueError` naming both paths on
mismatch. Load `pooled_H_norm`, `bucket_labels` and `bucket_edges` from
`cache.cache_path(a.field_stem, "npz")`. Assert the loaded edges equal `linear_probe.BUCKET_EDGES`
elementwise as float64 and raise `ValueError` on mismatch — the artifact and the pre-registration
must agree or nothing runs.

Split with `linear_probe.train_test_split_indices(n, TRAIN_FRACTION, SPLIT_SEED)`. Fit ONE probe
with `linear_probe.fit_probe(X_hsc[train], X_ls[train], RIDGE_ALPHA_GRID, ALPHA_PER_TARGET,
FIT_INTERCEPT)` — one map on the whole manifold, per D5-02. Predict on the test split, then compute
`per_point_residuals` and `aggregate_r2` there.

Bucket the test points by taking `bucket_labels[test_idx]` — the labels were assigned at `05-03`
against the full-field edges, so this is a lookup, not a re-cut. Compute per bucket: realized `n`,
mean per-point residual, aggregate R-squared restricted to that bucket's rows, and
`linear_probe.bucket_residual_ci(residuals_in_bucket, N_BOOTSTRAP, BOOTSTRAP_SEED,
CONFIDENCE_LEVEL)`.

Call `linear_probe.size_matched_check(residuals_test, labels_test, SIZE_MATCH_N_REPEATS,
SIZE_MATCH_SEED, CONFIDENCE_LEVEL)` with the realized test-split arrays. Print the realized
per-bucket counts and the match size it chose, side by side with the full-field counts, so the
distinction RESEARCH Pitfall 4 names is visible in the output rather than buried in a dict.

Compute the continuous statistic through `_spearman_report(pooled_H_norm[test_idx],
residuals_test, "D5-07 continuous")`.

Call `linear_probe.apply_verdict_rule(bucket_stats, size_match, linear_probe.VERDICT_RULE)` and use
its returned string as the verdict. The runner must contain no comparison that could produce a
verdict on its own.

Emit rows: one `kind` of `probe_bucket` per bucket and one `kind` of `probe_overall`, all carrying
the full key set listed in this plan's artifacts section, all with `data_source` set to `pu`. Append
with `json.dumps(row, default=float)` and `fh.flush()` to
`notebooks/.cache/05_curvature_probe_decodability.jsonl` — and under `--smoke`, write nothing.

Verify this task with `--mode bucketed --smoke` only. The full run is Task 2.
  </action>
  <verify>
    <automated>.venv/bin/python notebooks/diagnostics/curvature_probe_decodability_run.py --mode bucketed --smoke && .venv/bin/python -c "import pathlib; assert not pathlib.Path('notebooks/.cache/05_curvature_probe_decodability.jsonl').exists(), 'smoke wrote to the real record'; print('smoke clean, record untouched')" && .venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/bin/python notebooks/diagnostics/curvature_probe_decodability_run.py --mode bucketed --smoke` exits 0, prints a line containing `SMOKE`, and prints a verdict that is either `HOLDS` or `NO DETECTABLE RELATIONSHIP`
    - The smoke run writes no file at `notebooks/.cache/05_curvature_probe_decodability.jsonl`
    - `grep -v '^#' notebooks/diagnostics/curvature_probe_decodability_run.py | grep -c 'def run_bucketed('` returns 1
    - `grep -v '^#' notebooks/diagnostics/curvature_probe_decodability_run.py | grep -c 'linear_probe.apply_verdict_rule'` returns at least 1
    - `grep -v '^#' notebooks/diagnostics/curvature_probe_decodability_run.py | grep -c 'linear_probe.size_matched_check'` returns at least 1
    - `grep -v '^#' notebooks/diagnostics/curvature_probe_decodability_run.py | grep -c 'linear_probe.fit_probe'` returns exactly 1 (one global fit, not one per bucket)
    - The smoke output prints the realized per-bucket test counts and the chosen size-match count on adjacent lines
    - `git diff --quiet -- notebooks/pu_manifold/linear_probe.py` exits 0
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
  </acceptance_criteria>
  <done>The bucketed branch is implemented end to end, reads every constant off the frozen module, produces its verdict only through the committed rule, and proves itself on a reduced run that writes nothing.</done>
</task>

<task type="auto">
  <name>Task 2: The real run — the phase's headline numbers</name>
  <precondition>Task 1's smoke run exits 0 and notebooks/pu_manifold/linear_probe.py is unmodified since the 05-04 freeze commit recorded in 05-04-SUMMARY.md.</precondition>
  <files>notebooks/diagnostics/curvature_probe_decodability_run.py</files>
  <read_first>
    - `notebooks/diagnostics/curvature_probe_decodability_run.py` — the runner as completed at Task 1.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-PREREGISTRATION.md` — the frozen rule, read before the run so the verdict is recognised rather than interpreted.
    - `.planning/phases/04-region-partitioning-regional-alignment-mknn/04-FINDINGS.md` § 5 and § 7 — how Phase 4 reported its bucketed comparison and, in § 7, the region-size artifact it discovered only at closure. Phase 5's realized-count reporting is the direct response to that.
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-04-SUMMARY.md` — the recorded `freeze_commit` SHA the ancestry check uses.
  </read_first>
  <action>
Before running anything, confirm the ordering guarantee still holds: `git diff --quiet --
notebooks/pu_manifold/linear_probe.py` exits 0, and `git merge-base --is-ancestor` returns success
for the `freeze_commit` SHA recorded in `05-04-SUMMARY.md` against `HEAD`. If either fails, stop and
report — a probe number computed at a commit that is not a descendant of the freeze is worthless.

Then run `--mode bucketed` on the full 10,000 points, once, with no flag overrides.

Record in the plan summary, exactly as measured and with no rounding beyond four significant
figures:
- `n_train`, `n_test`, and the selected ridge alpha with the frozen grid beside it;
- overall test-split R-squared and mean per-point residual;
- the two bucket edges, the full-field per-bucket counts, and the REALIZED test-split per-bucket
  counts side by side — if the realized counts differ from `n_test` divided by `N_BUCKETS` by more
  than a few percent, say so explicitly, because that is the condition RESEARCH Pitfall 4 warns
  about;
- per bucket: `n`, mean residual, R-squared, and the bootstrap CI with its `degenerate` flag;
- the size-matched check: match size, median difference, sign stability, and the fraction of
  repeats with disjoint CIs;
- the continuous Spearman between pooled curvature magnitude and residual on the test split, with
  its p-value and n;
- the verdict string, quoted from the row, with the note that it came from the module's verdict
  function and not from a comparison in the runner.

Then state, in the phase's own words and in one short paragraph, what the numbers say — including
the case where they say nothing. NO DETECTABLE RELATIONSHIP is a complete outcome under the frozen
rule; report it as a result, not as a shortfall, and do not soften it by leaning on the continuous
statistic or on any bucket other than the headline pair.

Run the run once. If it needs to be re-run for a mechanical reason (a crash, a missing artifact),
say so in the summary and record how many times it ran. Do not re-run it after seeing a number in
the hope of a different one; that is the move the pre-registration exists to prevent.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, pathlib, sys
sys.path.insert(0,'notebooks')
from pu_manifold import linear_probe as lp
rows = [json.loads(l) for l in pathlib.Path('notebooks/.cache/05_curvature_probe_decodability.jsonl').read_text().splitlines() if l.strip()]
buckets = [r for r in rows if r['kind'] == 'probe_bucket']
overall = [r for r in rows if r['kind'] == 'probe_overall']
assert len(buckets) == lp.N_BUCKETS, (len(buckets), lp.N_BUCKETS)
assert len(overall) == 1, len(overall)
o = overall[0]
assert o['data_source'] == 'pu'
assert o['verdict'] in ('HOLDS','NO DETECTABLE RELATIONSHIP'), o['verdict']
assert o['n_train'] + o['n_test'] == 10000, (o['n_train'], o['n_test'])
assert abs(o['train_fraction'] - lp.TRAIN_FRACTION) < 1e-12 and o['split_seed'] == lp.SPLIT_SEED
assert tuple(float(x) for x in o['bucket_edges']) == tuple(float(x) for x in lp.BUCKET_EDGES)
assert o['pooling_method'] == lp.POOLING_METHOD and o['curvature_convention'] == 'trace'
assert o['curvature_source_function'] == lp.CURVATURE_SOURCE_FUNCTION
realized = sorted(b['bucket_n'] for b in buckets)
assert sum(realized) == o['n_test'], (realized, o['n_test'])
assert o['size_match_n'] <= realized[0], (o['size_match_n'], realized)
for b in buckets:
    for k in ('bucket_mean_residual','bucket_r2','ci_low','ci_high','degenerate','confidence_level','n_resamples'):
        assert k in b, (b['bucket_index'], k)
print('verdict:', o['verdict'], '| realized bucket n:', realized, '| size_match_n:', o['size_match_n'])" && git diff --quiet -- notebooks/pu_manifold/linear_probe.py && echo 'frozen module untouched'</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/.cache/05_curvature_probe_decodability.jsonl` exists with exactly `N_BUCKETS` rows of kind `probe_bucket` and exactly one row of kind `probe_overall`, all with `data_source` equal to `pu`
    - The overall row's `verdict` is exactly one of `HOLDS` or `NO DETECTABLE RELATIONSHIP`
    - `n_train + n_test` equals 10000, `train_fraction` equals the module's `TRAIN_FRACTION`, `split_seed` equals the module's `SPLIT_SEED`
    - The row's `bucket_edges` equal the module's `BUCKET_EDGES` elementwise, `pooling_method` equals the module's `POOLING_METHOD`, `curvature_convention` equals `trace`, and `curvature_source_function` equals `chart_curvature.chart_curvature_field`
    - The per-bucket `bucket_n` values sum exactly to `n_test`
    - `size_match_n` is less than or equal to the smallest realized `bucket_n`
    - Every bucket row carries `bucket_mean_residual`, `bucket_r2`, `ci_low`, `ci_high`, `degenerate`, `confidence_level` and `n_resamples`
    - `git diff --quiet -- notebooks/pu_manifold/linear_probe.py` exits 0
    - `git merge-base --is-ancestor` succeeds for the `freeze_commit` SHA recorded in `05-04-SUMMARY.md` against `HEAD`
    - The plan summary records every quantity listed in the action, including the realized per-bucket counts beside the full-field counts, and states in the phase's own words what the numbers say
    - The plan summary records how many times the run was executed
  </acceptance_criteria>
  <done>The phase's headline numbers exist, produced by the frozen protocol at a commit descended from the freeze, with the verdict applied mechanically and the realized test-split bucket counts reported beside the full-field counts.</done>
</task>

<task type="auto">
  <name>Task 3: Check the ridge justification against the training split's own spectrum</name>
  <files>notebooks/diagnostics/curvature_probe_decodability_run.py</files>
  <read_first>
    - `.planning/phases/05-curvature-conditioned-linear-decodability/05-RESEARCH.md` § Common Pitfalls 3 and § Assumptions Log A2 — the claim being checked (that the 768-d design matrix is severely rank-deficient in effective terms at the manifold's established 18-to-25 intrinsic dimension) and A2's own instruction that the reason should be checked against the measured spectrum before being asserted as fact.
    - `notebooks/diagnostics/curvature_probe_decodability_run.py` — the runner as completed at Task 2; this task adds a diagnostic block and changes no computed statistic.
    - `notebooks/.cache/05_curvature_probe_decodability.jsonl` — the rows from Task 2; the diagnostic is APPENDED as an additional row and the existing rows are not rewritten.
  </read_first>
  <action>
Add a diagnostic row with `kind` set to `probe_conditioning`, computed from the training split's
`hsc` block alone, and append it to the same JSONL record. This changes no statistic and cannot
change the verdict; it exists so the stated reason for using ridge is checked rather than asserted.

Compute the singular values of the mean-centered training design matrix with
`np.linalg.svd(X_train - X_train.mean(axis=0), compute_uv=False)`. Record: the first 40 singular
values as `singular_values_head`; `condition_number` as largest over smallest; the effective rank at
three thresholds as `effective_rank_1pct`, `effective_rank_0p1pct` and `effective_rank_0p01pct`
(the count of singular values above 1 percent, 0.1 percent and 0.01 percent of the largest); and the
cumulative variance fraction captured by the first 20 and first 25 components as `cumvar_first_20`
and `cumvar_first_25`.

Record beside them `selected_alpha` from Task 2, `alpha_grid` copied from the frozen constant, and a
boolean `alpha_at_grid_boundary` that is true when the selected alpha is the smallest or the largest
value in the grid — a selection at the boundary means the grid was too narrow and must be reported
as such, not quietly accepted.

Then state in the plan summary which way the check came out, in one short paragraph:
- If the spectrum drops sharply after roughly 20 to 25 values, RESEARCH A2's reason is confirmed and
  ridge was the right default for the stated reason.
- If the spectrum is flat and the condition number is modest, say plainly that the design matrix was
  better conditioned than A2 expected, that the selected alpha was small, and that the fit therefore
  behaved close to ordinary least squares. That invalidates nothing — the frozen rule selected the
  alpha, not the planner — but the reason recorded in `05-PREREGISTRATION.md` would then be wrong,
  and `05-FINDINGS.md` must say so.
- If the selected alpha sits at a grid boundary, say so and state that the frozen grid was too
  narrow, which is a real limitation on this phase's fit and must appear in the findings.

Do not change `RIDGE_ALPHA_GRID`. It is frozen. A too-narrow grid is a reported limitation, not a
thing to fix mid-phase.
  </action>
  <verify>
    <automated>.venv/bin/python -c "
import json, pathlib, sys
sys.path.insert(0,'notebooks')
from pu_manifold import linear_probe as lp
rows = [json.loads(l) for l in pathlib.Path('notebooks/.cache/05_curvature_probe_decodability.jsonl').read_text().splitlines() if l.strip()]
c = [r for r in rows if r['kind'] == 'probe_conditioning']
assert len(c) == 1, len(c)
c = c[0]
for k in ('singular_values_head','condition_number','effective_rank_1pct','effective_rank_0p1pct','effective_rank_0p01pct','cumvar_first_20','cumvar_first_25','selected_alpha','alpha_grid','alpha_at_grid_boundary'):
    assert k in c, k
assert len(c['singular_values_head']) == 40, len(c['singular_values_head'])
assert tuple(float(a) for a in c['alpha_grid']) == tuple(float(a) for a in lp.RIDGE_ALPHA_GRID)
assert isinstance(c['alpha_at_grid_boundary'], bool)
assert len([r for r in rows if r['kind']=='probe_overall']) == 1, 'the overall row was rewritten'
print('conditioning:', 'cond=', c['condition_number'], 'eff_rank_1pct=', c['effective_rank_1pct'], 'alpha=', c['selected_alpha'], 'at_boundary=', c['alpha_at_grid_boundary'])" && git diff --quiet -- notebooks/pu_manifold/linear_probe.py</automated>
  </verify>
  <acceptance_criteria>
    - `notebooks/.cache/05_curvature_probe_decodability.jsonl` contains exactly one row of kind `probe_conditioning`
    - That row carries `singular_values_head` of length 40, `condition_number`, `effective_rank_1pct`, `effective_rank_0p1pct`, `effective_rank_0p01pct`, `cumvar_first_20`, `cumvar_first_25`, `selected_alpha`, `alpha_grid` and `alpha_at_grid_boundary`
    - Its `alpha_grid` equals the module's `RIDGE_ALPHA_GRID` elementwise and `alpha_at_grid_boundary` is a boolean
    - The `probe_overall` and `probe_bucket` rows from Task 2 are unchanged: exactly one overall row and exactly `N_BUCKETS` bucket rows remain
    - `git diff --quiet -- notebooks/pu_manifold/linear_probe.py` exits 0
    - The plan summary states, in the phase's own words, whether the measured spectrum confirms RESEARCH A2's stated reason for ridge, and states explicitly if the selected alpha sat at a grid boundary
    - `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
  </acceptance_criteria>
  <done>RESEARCH A2's justification for ridge is measured against the training split's own spectrum rather than asserted, the selected alpha is recorded as an output of the frozen rule, and a too-narrow grid would be visible rather than silent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen constants -> computed result | The run reads every constant off the frozen module and echoes each into every record row, so the result and the rule that judged it travel together. |
| local filesystem -> `notebooks/.cache/` | The only persistence; every path composed through `cache.cache_path` under `_assert_inside_cache`. |
| git history -> evidential claim | The ordering guarantee is a repository-history property, checked before the run and proved mechanically at `05-06`. |

No network surface, no authentication, no session, no user-supplied input path. ASVS L1; block on
`high`.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Tampering (of results, not security) | a frozen constant amended once a number exists | high | mitigate | The run reads constants only, never writes them; `git diff --quiet -- notebooks/pu_manifold/linear_probe.py` is asserted before the run and again in every task's verify; the constants are duplicated in the committed `05-PREREGISTRATION.md` so an amendment touches two files |
| T-05-15 | Tampering (of results) | a verdict decided by a comparison in the runner rather than by the committed rule | high | mitigate | The verdict string comes only from the module's verdict function, which raises when the rule text is empty; the acceptance criteria assert the call exists and the rule text is echoed into the row |
| T-05-16 | Tampering (of results) | a size-matched check run against full-field counts, reproducing Phase 4's region-size artifact | high | mitigate | The check receives the realized test-split label array; the verify asserts the returned match size is at most the smallest realized bucket count and that the bucket counts sum to `n_test`, not to 10000 |
| T-05-17 | Spoofing (of provenance) | a probe fit on one subsample and bucketed by a field extracted from another | high | mitigate | The resolved subsample path is asserted equal to the one in the pooled artifact's cfg manifest before any fit; the bucket edges are asserted equal to the frozen constant |
| T-05-18 | Repudiation | a smoke run's numbers mistaken for the real run's | medium | mitigate | The smoke branch writes nothing to the record and prints a SMOKE banner; the verify after Task 1 asserts the record file does not exist |
| T-05-SC | Tampering | package installs | low | accept | This plan installs no package |
</threat_model>

<verification>
- `notebooks/.cache/05_curvature_probe_decodability.jsonl` carries `N_BUCKETS` bucket rows, one overall row and one conditioning row
- The overall row's verdict is one of the two terminal strings and every frozen constant it echoes matches `notebooks/pu_manifold/linear_probe.py`
- Per-bucket realized counts sum to `n_test`; the size-match count is at most the smallest realized count
- `git diff --quiet -- notebooks/pu_manifold/linear_probe.py` exits 0
- `git merge-base --is-ancestor <freeze_commit> HEAD` exits 0
- `.venv/bin/python -m pytest notebooks/pu_manifold/tests/ -q` exits 0
- `git status --porcelain src/effdim/` produces no output
</verification>

<success_criteria>
D5-01 and D5-02 are satisfied in fact: one global ridge map from `hsc` to `legacysurvey`, evaluated
per bucket on held-out points. D5-07's magnitude split and its binning-free continuous companion are
both reported. D5-08's realized-test-split size match is built into the protocol rather than
discovered afterwards. D5-10's guard ran before any data was touched. The verdict was applied
mechanically from a rule that already existed in an ancestor commit, and RESEARCH A2's justification
for ridge was measured rather than asserted.
</success_criteria>

<output>
Create `.planning/phases/05-curvature-conditioned-linear-decodability/05-05-SUMMARY.md` when done
</output>
