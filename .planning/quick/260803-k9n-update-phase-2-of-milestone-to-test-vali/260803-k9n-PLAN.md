---
phase: quick-260803-k9n
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [CAE-01, CAE-02, CAE-03, CAE-04, CAE-05, CAE-06, CAE-07]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md

must_haves:
  truths:
    - "Reading .planning/ROADMAP.md alone tells you Phase 02.2 exists, that it empirically tests the Chart Auto-Encoder method (arXiv:1912.10094) on the PU data, and exactly what PASS and FAIL each unblock or block"
    - "Phase 3's entry states it is blocked until Phase 02.2 returns PASS, and that a FAIL leaves the milestone at the phase-2 stage"
    - "The ReLU-vs-C2-smooth-activation conflict and the compact-manifold assumption are both discoverable from the Phase 02.2 text without opening the paper"
    - "Every CAE-xx ID cited in ROADMAP.md resolves to a requirement bullet and a traceability row in REQUIREMENTS.md"
    - "All five pre-existing phase sections (1, 2, 02.1, 3, 4) survive the edit intact, plus Shipped, Backlog, and Progress"
    - "Phase 02.2 appears consistently in all four ROADMAP locations: phase-list bullet, Phase Details section, Progress table row, execution-order line"
  artifacts:
    - ".planning/ROADMAP.md — new '### Phase 02.2: Chart Autoencoder Validity Test (INSERTED)' section, amended Phase 3 entry, amended Progress table and execution order"
    - ".planning/REQUIREMENTS.md — new '### Chart Autoencoder Validity (CAE)' section with CAE-01..07, seven traceability rows, corrected coverage arithmetic"
    - ".planning/STATE.md — Roadmap Evolution entry and amended Blockers/Concerns entry for the Phase 3 gate"
  key_links:
    - "ROADMAP Phase 02.2 '**Requirements**: CAE-01..07' line <-> REQUIREMENTS.md CAE bullets and traceability rows (ID drift silently breaks traceability, as GEOM already did to the coverage arithmetic)"
    - "ROADMAP Phase 3 '**Depends on**' line <-> Phase 02.2 gate semantics (the two must not contradict on whether a PASS is a precondition)"
    - "Phase-list bullet <-> Progress table row <-> execution-order line (three places state the phase sequence; all three must agree or /gsd-next routes wrong)"
---

<objective>
Insert Phase 02.2 "Chart Autoencoder Validity Test" (INSERTED) into the v1.1 roadmap as a decimal phase after Phase 02.1, defining an empirical validity test of the Chart Auto-Encoder method (Schonsheck, Chen, Lai — arXiv:1912.10094) on the PU embedding data behind a machine-readable PASS/FAIL gate, and amend Phase 3 to depend on that gate.

Purpose: Phase 2's `GATE_VERDICT = FAIL` (`m = 0.412071`, ~41% negative eigenvalue mass) falsified the flat-target assumption that Phase 3 was specified against. CAE relaxes exactly that assumption — only *local* charts are Euclidean, no global Euclidean embeddability is assumed — making it a coordinate-producing candidate under Phase 02.1's fork. Before Phase 3 spends a decoder-training budget decoding from a CAE representation, the roadmap must state the empirical test that earns the right to do so, and what happens when it does not.

Output: Documentation only. Three planning files amended. No source code, no notebooks, no package installs, no CAE implementation. Planning the *implementation* of Phase 02.2 is a separate, later invocation the user will make.

Note on the `requirements` field: this plan **authors** CAE-01..07 rather than implementing them. They do not exist in REQUIREMENTS.md yet; task 3 creates them.

Tracer-first opted out (`--no-tracer` equivalent): the architecture here is the INSERTED-decimal-phase convention, already proven end-to-end by Phase 02.1 (phase-list bullet, "Why inserted" section, Goal/Depends on/Requirements/Success Criteria, Progress row, execution-order line, REQUIREMENTS section + traceability rows). A thin slice would add no information. Tasks below are document-scoped instead, each leaving the planning docs in a coherent readable state.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/quick/260803-k9n-update-phase-2-of-milestone-to-test-vali/260803-k9n-CONTEXT.md
</context>

<critical_constraint>
Every edit in this plan is a **scoped `Edit` call on an existing file**. The `Write` tool is prohibited on all three target files. A whole-file `Write` on ROADMAP.md or REQUIREMENTS.md destroys every section outside the diff window — these files carry five phases, a Shipped section, a Backlog section, and a 47-row traceability table that no task here is authorized to touch. Use multiple `Edit` calls where one replacement is not enough.
</critical_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Author the Phase 02.2 section in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <precondition>`.planning/ROADMAP.md` still contains the line `### Phase 02.1: Geometry Representation Research (INSERTED)` and contains no occurrence of `02.2` anywhere. If `02.2` is already present, this task has already run — halt rather than double-inserting.</precondition>
  <reversibility rating="reversible">Documentation-only, git-tracked, no code or data artifacts generated; a revert restores the prior roadmap exactly.</reversibility>
  <action>
Insert a new phase section into `.planning/ROADMAP.md` immediately after the Phase 02.1 section (that is, after the `02.1-04-PLAN.md` bullet at the end of 02.1's Plans list) and immediately before the `### Phase 3: Decoder & Curvature Field` heading. Match Phase 02.1's structure element-for-element — it is the INSERTED-phase precedent and the only one in the file.

Heading, verbatim: `### Phase 02.2: Chart Autoencoder Validity Test (INSERTED)`

Then, in this order:

**Goal** (bold-label line, matching 02.1's `**Goal**:` form). State that the Chart Auto-Encoder method of arXiv:1912.10094 is trained on the frozen Phase 1 10,000-row PU subsample and put through a pre-registered empirical validity test whose machine-readable PASS/FAIL verdict decides whether Phase 3 may decode a curvature field from a CAE representation or the milestone remains at the phase-2 stage.

**Why inserted** (bold-label paragraph block, mirroring 02.1's). Make these points, citing the numbers rather than gesturing at them:
- Phase 2 measured `GATE_VERDICT = FAIL` on the frozen k*=15 fit: `m = 0.412071` against a 0.15 MARGINAL bound, ~41% of absolute eigenvalue mass negative, robust across k in {5,10,30}, survey column, a ~90% disjoint resample, and exact normalization inversion. Cite `02-FINDINGS.md`.
- What failed is the assumption of a *global* Euclidean target for the geodesic metric. CAE assumes only that *local charts* are Euclidean — it is the assumption-relief the FAIL calls for, and a coordinate-producing candidate under Phase 02.1's coordinate-producing vs graph-native fork.
- The paper's guarantee is a universal manifold approximation theorem (Thm 2): for a compact d-dimensional data manifold with reach tau, an epsilon-faithful representation exists with L > d charts, latent space homeomorphic to the manifold. Thm 1 is the contrast that motivates charts at all — a plain single-chart autoencoder *cannot* epsilon-faithfully represent non-contractible topology.
- State plainly why this is a test and not an adoption: a theorem about compact manifolds with positive reach is not evidence about this point cloud. Phase 3 is expensive and its synthetic control (CURV-06/07) provably cannot detect a bad input parameterization — a synthetic manifold that passes the Phase 2 gate never reproduces the pathology. So the input must be validated *before* Phase 3, not by it.

**Depends on**: Phase 02.1 (consumes its fork resolution and non-flat-target candidate survey; CAE sits on the coordinate-producing side of that fork) and Phase 2 (consumes its FAIL verdict, full eigenspectrum, intrinsic-dimension measurements as evidence, and the frozen Phase 1 subsample cache). Does **not** require a Phase 2 PASS — same posture as 02.1.

**Requirements**: CAE-01..07

**UI hint**: no

**Success Criteria** — seven numbered criteria in 02.1's indented `  1.` style, each closing with its requirement ID in parentheses:
  1. Gate metrics and their numeric PASS/FAIL thresholds pre-registered and committed **before** any CAE fit runs, with the ordering proved by git ancestry rather than asserted — the `02-REFIT-PREREGISTRATION.md` precedent (CAE-01)
  2. A Chart Auto-Encoder trained on the frozen Phase 1 10,000-row normalized subsample, reproducible from recorded seeds: initial encoder E from R^768 to R^l with l near 2d per the Nash-Kuiper minimal near-isometric motivation, N over-specified chart encoders E_alpha into chart spaces (0,1)^d, per-chart decoders D_alpha, one shared embedding decoder D back to R^768, and a chart predictor P emitting partition-of-unity probabilities p_alpha; trained under the paper's loss (eq. 3, `min_alpha e_alpha` minus the cross-entropy term on softmax(-e)) with Lipschitz regularization on chart-encoder spectral norms (eq. 4) and FPS-seeded per-chart pre-training (eq. 5) (CAE-02)
  3. Held-out reconstruction error reported as both an aggregate metric and a per-output-dimension distribution, against two stated baselines at matched capacity: a plain single-chart autoencoder, and the classical-MDS/Isomap reconstruction at the working dimension. A CAE that only ties the single-chart control has not demonstrated that charts bought anything (CAE-03)
  4. Chart-transition cycle residual R_cycle (eq. 8) reported over held-out points — decoded data re-encoded through a second chart — as the direct measurement of whether the chart atlas is consistent where charts overlap (CAE-04)
  5. Chart count obtained **a posteriori**: the over-specified initial N stated, unused charts decayed under weight decay plus regularization and pruned by a stated decoder-weight-norm tolerance, the surviving count reported and its stability across seeds shown. Topology preservation shown by the paper's own empirical measures — unfaithfulness (distance of latent-sampled generations from the training set) and coverage (fraction of training modes hit) (CAE-05)
  6. Two stated deviations/assumptions carried forward for Phase 3, each with its consequence: (a) the reference implementation is TensorFlow with ReLU activations, while DEC-02 and CURV-01..03 require a C2-smooth activation throughout the decoder because ReLU's second derivative is identically zero — so per-chart and embedding decoders are trained with a C2-smooth substitute (tanh, softplus, or SiLU), the substitution and any reconstruction cost it carries reported; (b) Thm 2 covers *compact* manifolds with positive reach — PU point-cloud compactness is an assumption recorded, not verified (CAE-06)
  7. A machine-readable `cae_verdict_{fit_key}.json` written on the `gate_verdict_{fit_key}.json` pattern, carrying a `CAE_VERDICT` field of PASS or FAIL plus every pre-registered metric and threshold. Downstream notebooks check it before running any expensive cell. On FAIL the notebook halts, `02.2-FINDINGS.md` records the finding, and that documented failure is itself a complete, reportable milestone outcome (CAE-07)

**Notes** paragraph. Record, in prose:
- Scope boundary: CAE validates topology and geometry preservation *of a reconstruction*. It does not adjudicate the geometric reading of the 41% negative eigenvalue mass — plan 02.1-04's terminal artifact still owns that judgment, and this phase does not reopen it.
- No new package installs. Implementation is in torch (`torch==2.13.0+cpu`, already vetted and pinned in `notebooks/requirements-notebooks.txt`); the paper's TensorFlow reference implementation is not installed and not a dependency.
- Cache posture inherited from 02.1 plan 03: the gitignored Phase 1/2 caches (the subsample `.npz`, and `isomap_43cf438bc944c509.joblib` at 1.55 GiB for the Isomap baseline in criterion 3) are not reproducible here; a runner that cannot find them halts rather than regenerating, because regenerating changes provenance.
- Working dimension d is not inherited from `D_FROZEN = 5`, flagged suspect in `02-FINDINGS.md` §6.4 against three estimates clustering at 18-25; d comes from Phase 02.1's GEOM-05 re-derivation, and the chart dimension used must be stated.

**Hard gate** line, matching the `**Hard gate**:` form used by Phases 2 and 3. State: the terminal artifact is `cae_verdict.json` (PASS/FAIL). PASS unblocks Phase 3, which then decodes from the CAE representation. FAIL blocks Phase 3, the findings are documented, and the milestone remains at the phase-2 stage — a legitimate and complete outcome, not an error to be worked around.

**Plans**: TBD (this phase is not planned yet; the user plans it separately after this doc update lands).

Do not touch any other section of the file in this task. Do not use fenced code blocks in the roadmap prose; inline backticks only, matching the surrounding file.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim && awk '/^### Phase 02\.2:/,/^### Phase 3:/' .planning/ROADMAP.md > /tmp/claude-1000/-home-akagi-Documents-Projects-EffDim/60dbf056-2df2-4d64-b232-f192c3f09322/scratchpad/sec22.txt && S=/tmp/claude-1000/-home-akagi-Documents-Projects-EffDim/60dbf056-2df2-4d64-b232-f192c3f09322/scratchpad/sec22.txt && test -s "$S" && grep -qF '### Phase 02.2: Chart Autoencoder Validity Test (INSERTED)' "$S" && grep -qF '1912.10094' "$S" && grep -qF '**Why inserted**' "$S" && grep -qF '**Depends on**' "$S" && grep -qF 'CAE-01..07' "$S" && grep -qF '**Hard gate**' "$S" && grep -qF 'cae_verdict' "$S" && grep -qF '0.412071' "$S" && grep -qF 'ReLU' "$S" && grep -qF 'C2-smooth' "$S" && grep -qiF 'compact' "$S" && grep -qF 'R_cycle' "$S" && grep -qF 'a posteriori' "$S" && [ "$(grep -c '^  [1-9]\.' "$S")" -eq 7 ] && for i in 1 2 3 4 5 6 7; do grep -qF "CAE-0$i)" "$S" || exit 1; done && [ "$(grep -c '^### Phase ' .planning/ROADMAP.md)" -eq 6 ] && echo TASK1_OK</automated>
  </verify>
  <done>ROADMAP.md carries a Phase 02.2 section between 02.1 and Phase 3 with Goal, Why inserted, Depends on, Requirements CAE-01..07, seven numbered success criteria each tagged with a distinct CAE ID, Notes, and a Hard gate line naming `cae_verdict.json`; the negative-mass evidence (0.412071), the ReLU/C2-smooth conflict, the compactness assumption, R_cycle, and the a-posteriori chart count all appear in that section; the file still holds exactly six `### Phase ` headings.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Phase 02.2 into the rest of ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Four scoped `Edit` calls on `.planning/ROADMAP.md`, all outside the Phase 02.2 section task 1 wrote.

**(a) Phase list.** In the `## Phases` bullet list, insert a new unchecked bullet between the Phase 02.1 bullet and the Phase 3 bullet, matching the 02.1 bullet's form (`- [ ] **Phase 02.2: Chart Autoencoder Validity Test** (INSERTED) — ` then a one-line description). The description states that the Chart Auto-Encoder method is empirically validity-tested on the PU data behind a PASS/FAIL gate that decides whether Phase 3 proceeds.

**(b) Phase 3 dependency amendment.** Phase 3 already carries an `> **AMENDED after Phase 2's FAIL.**` blockquote. Do not rewrite that blockquote's existing argument — extend it, or add a second sentence group at its end, recording that Phase 02.2 now tests whether a Chart Auto-Encoder representation is a valid input, and that Phase 3 does not start until `cae_verdict.json` reads PASS.

Then amend Phase 3's `**Depends on**:` line so it states: Phase 02.2 **PASS** is a precondition — Phase 3 decodes from the CAE representation and must check `cae_verdict.json` before running any expensive cell; on FAIL, Phase 3 stays blocked and the milestone remains at the phase-2 stage. Keep the existing dependency on Phase 02.1 (representation decision and working dimension) and the existing note that Phase 2 supplies eigenspectrum evidence and the FAIL verdict, and that a Phase 2 PASS is no longer a precondition.

Also update Phase 3's success criterion 1, which currently reads "A decoder maps Isomap coordinates to the 768-d embedding" — the phrase is stale twice over (Phase 02.1 replaced the representation; Phase 02.2 now proposes the CAE chart coordinates as that representation). Rewrite it to name the Phase 02.2-validated CAE chart representation as the decoder's input, preserving the rest of the criterion (C2-smooth activation throughout, no ReLU family, reproducible from a recorded torch seed, and its `(DEC-01, DEC-02, DEC-05)` tag) unchanged.

**(c) Progress table.** Insert a row between the `02.1. Geometry Representation Research (INSERTED)` row and the `3. Decoder & Curvature Field` row, matching the existing column layout: phase label `02.2. Chart Autoencoder Validity Test (INSERTED)`, milestone `v1.1`, plans complete `0/TBD`, status `Not started`, completed `-`.

**(d) Execution order.** The `**Execution Order:**` line in `## Progress` currently reads `1 -> 2 -> 02.1 -> 3 -> 4`. Update the sequence to `1 -> 2 -> 02.1 -> 02.2 -> 3 -> 4` and extend the trailing explanation so it states that Phase 02.2 was inserted to empirically test the Chart Auto-Encoder representation before Phase 3 commits to decoding from it, and that Phase 3 now depends on a Phase 02.2 PASS.

Do not touch Phase 1, Phase 2, Phase 02.1, Phase 4, Shipped, or Backlog.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim && R=.planning/ROADMAP.md && grep -qF '- [ ] **Phase 02.2: Chart Autoencoder Validity Test** (INSERTED)' "$R" && grep -qF '| 02.2. Chart Autoencoder Validity Test (INSERTED) | v1.1 |' "$R" && grep -q '^\*\*Execution Order:\*\*.*1 -> 2 -> 02\.1 -> 02\.2 -> 3 -> 4' "$R" && awk '/^### Phase 3:/,/^### Phase 4:/' "$R" > /tmp/claude-1000/-home-akagi-Documents-Projects-EffDim/60dbf056-2df2-4d64-b232-f192c3f09322/scratchpad/sec3.txt && P=/tmp/claude-1000/-home-akagi-Documents-Projects-EffDim/60dbf056-2df2-4d64-b232-f192c3f09322/scratchpad/sec3.txt && test -s "$P" && grep -qF '02.2' "$P" && grep -qF 'cae_verdict' "$P" && grep -q 'PASS' "$P" && grep -qF '(DEC-01, DEC-02, DEC-05)' "$P" && [ "$(grep -c '^### Phase ' "$R")" -eq 6 ] && [ "$(grep -c '^- \[[ x]\] \*\*Phase ' "$R")" -eq 6 ] && [ "$(grep -c '^| [0-9]' "$R")" -eq 6 ] && grep -qF '## Shipped' "$R" && grep -qF '## Backlog' "$R" && grep -qF '### Phase 02.1: Geometry Representation Research (INSERTED)' "$R" && [ "$(wc -l < "$R")" -gt 190 ] && echo TASK2_OK</automated>
  </verify>
  <done>All four ROADMAP locations name Phase 02.2 consistently — phase-list bullet, Progress table row, execution-order sequence, and Phase 3's dependency text; Phase 3's success criterion 1 names the CAE representation instead of Isomap coordinates while keeping its `(DEC-01, DEC-02, DEC-05)` tag; six phase headings, six phase bullets, six progress rows; Shipped, Backlog, and the Phase 02.1 section survive.</done>
</task>

<task type="auto">
  <name>Task 3: Register CAE-01..07 in REQUIREMENTS.md and record the insertion in STATE.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
**REQUIREMENTS.md** — three scoped `Edit` calls, following exactly how the GEOM block was added for Phase 02.1.

First, insert a new `### Chart Autoencoder Validity (CAE)` section between the `### Geometry Representation (GEOM)` section and the `### Decoder (DEC)` section. Open it with a dated preamble paragraph in the GEOM section's style: added 2026-08-03 with Phase 02.2 (INSERTED), to empirically test whether the Chart Auto-Encoder method of arXiv:1912.10094 — which assumes only local Euclidean charts, not the global Euclidean embeddability Phase 2's gate falsified — yields a representation Phase 3 can validly decode from.

Then seven unchecked requirement bullets in the file's established form (`- [ ] **CAE-0N**: ` then an observable-behaviour statement, present tense, describing what is *shown* or *written*). Content maps one-to-one onto ROADMAP Phase 02.2's seven success criteria, stated at requirement granularity:
- CAE-01: gate metrics and numeric PASS/FAIL thresholds pre-registered and committed before any CAE fit runs, ordering proved by git ancestry
- CAE-02: CAE trained on the frozen Phase 1 10,000-row normalized subsample and reproducible from recorded seeds, with the full architecture named (initial encoder to R^l, N over-specified chart encoders into (0,1)^d, per-chart decoders, shared embedding decoder, partition-of-unity chart predictor) under the paper's loss with Lipschitz regularization and FPS-seeded chart pre-training
- CAE-03: held-out reconstruction error shown as aggregate metric plus per-output-dimension distribution against a matched-capacity single-chart autoencoder control and the classical-MDS/Isomap reconstruction, so a CAE that merely ties the control cannot be read as a success
- CAE-04: chart-transition cycle residual R_cycle shown over held-out points
- CAE-05: chart count obtained a posteriori after weight-decay pruning by a stated decoder-weight-norm tolerance, with the over-specified initial N, the surviving count, its across-seed stability, and the paper's unfaithfulness and coverage measures shown
- CAE-06: decoders trained with a C2-smooth activation rather than the reference implementation's ReLU, since DEC-02 and CURV-01..03 need a non-zero second derivative, with the substitution and any reconstruction cost stated; and the compact-manifold-with-positive-reach assumption behind the paper's Thm 2 recorded as an unverified assumption about the PU point cloud
- CAE-07: `cae_verdict_{fit_key}.json` written as a machine-readable PASS/FAIL artifact carrying every pre-registered metric and threshold, checked by downstream notebooks before any expensive cell; on FAIL the notebook halts, findings are documented, and that documented failure is itself a complete milestone outcome

Second, insert seven rows into the Traceability table between the `| GEOM-05 | Phase 02.1 | Complete |` row and the `| DEC-01 | Phase 3 | Pending |` row: `| CAE-01 | Phase 02.2 | Pending |` through `| CAE-07 | Phase 02.2 | Pending |`.

Third, fix the **Coverage** block below the table. It is currently stale — it reads 43 total and omits the five GEOM requirements added for Phase 02.1 entirely. Correct it to 55 total, itemized as DATA 5 + ISO 5 + SPEC 7 + GEOM 5 + CAE 7 + DEC 5 + CURV 8 + REGN 5 + MKNN 8, mapped 55/55, unmapped 0. Add the two missing per-phase lines beneath, in phase order, matching the existing lines' form: Phase 02.1 (Geometry Representation Research): GEOM-01..05 (5 requirements), and Phase 02.2 (Chart Autoencoder Validity Test): CAE-01..07 (7 requirements). Update the trailing `*Last updated: ...*` line to 2026-08-03 noting the CAE addition and the GEOM coverage correction.

**STATE.md** — two scoped `Edit` calls.

Add a bullet to `### Roadmap Evolution` beneath the two existing 02.1 bullets: Phase 02.2 inserted after Phase 02.1 — Chart Autoencoder Validity Test, empirically testing arXiv:1912.10094 on the PU data behind a PASS/FAIL gate; PASS unblocks Phase 3 to decode from the CAE representation, FAIL documents the finding and leaves the milestone at the phase-2 stage. Doc-only insertion; the phase itself is unplanned.

Amend the third `### Blockers/Concerns` bullet — the one beginning "Phase 2's PASS/MARGINAL/FAIL gate is a hard stop" — whose final clause currently says Phase 3 must not be planned in detail until Phase 2's gate outcome is known. That outcome is known (FAIL). Replace that clause so it states the current gate: Phase 3 is now blocked on Phase 02.2's `cae_verdict.json` reading PASS, and a FAIL there leaves the milestone at the phase-2 stage. Leave the first half of the bullet, about the Phase 2 FAIL being a legitimate complete outcome, intact.

Do not edit STATE.md frontmatter, the Current Position block, Performance Metrics, the Decisions list, or the Quick Tasks Completed table — the quick-task workflow owns those.
  </action>
  <verify>
    <automated>cd /home/akagi/Documents/Projects/EffDim && Q=.planning/REQUIREMENTS.md && grep -qF '### Chart Autoencoder Validity (CAE)' "$Q" && grep -qF '1912.10094' "$Q" && for i in 1 2 3 4 5 6 7; do grep -qF "**CAE-0$i**:" "$Q" || exit 1; grep -qF "| CAE-0$i | Phase 02.2 | Pending |" "$Q" || exit 1; done && [ "$(grep -c '^- \[ \] \*\*CAE-' "$Q")" -eq 7 ] && grep -qF '55 total' "$Q" && grep -qF 'GEOM 5' "$Q" && grep -qF 'CAE 7' "$Q" && grep -qF '55/55' "$Q" && grep -qF 'GEOM-01..05' "$Q" && grep -qF 'CAE-01..07' "$Q" && [ "$(grep -c '^| [A-Z]*-[0-9]* | Phase' "$Q")" -eq 55 ] && grep -qF '**DEC-01**' "$Q" && grep -qF '**MKNN-08**' "$Q" && grep -qF '**GEOM-05**' "$Q" && S=.planning/STATE.md && grep -qF '02.2' "$S" && grep -qF 'cae_verdict' "$S" && grep -qF 'Roadmap Evolution' "$S" && grep -qF 'Quick Tasks Completed' "$S" && echo TASK3_OK</automated>
  </verify>
  <done>REQUIREMENTS.md carries a CAE section with seven unchecked CAE-01..07 bullets, seven `Phase 02.2 | Pending` traceability rows (55 requirement rows total: 48 prior + 7 new, matching the corrected 55-requirement count), and a corrected coverage block reading 55 total / 55 mapped with GEOM and CAE both itemized and both per-phase lines present; DEC, MKNN, and GEOM requirement text survive untouched. STATE.md records the Phase 02.2 insertion under Roadmap Evolution and its Phase 3 blocker now points at `cae_verdict.json` rather than at Phase 2's already-known gate outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor -> `.planning/*.md` | The only mutation surface in this task. No untrusted input, no network, no execution of fetched content. Three shared planning files whose non-target sections are the assets at risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260803-k9n-01 | Tampering | `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md` | high | mitigate | Whole-file `Write` destroys 5 phase sections / a 48-row traceability table outside the diff window. `<critical_constraint>` prohibits `Write` on all three files; every task verify asserts survivorship of untouched structure (6 phase headings, 6 bullets, 6 progress rows, Shipped, Backlog, 55 traceability rows, DEC/MKNN/GEOM bullets, line-count floor). |
| T-260803-k9n-02 | Tampering | `.planning/STATE.md` frontmatter and Quick Tasks table | medium | mitigate | Task 3 action explicitly fences STATE.md edits to two named blocks; frontmatter, Current Position, Decisions, and the Quick Tasks table are owned by the quick-task workflow and out of scope. |
| T-260803-k9n-03 | Repudiation | phase-scope change with no recorded provenance | medium | mitigate | STATE.md Roadmap Evolution entry records what was inserted and why; the ROADMAP "Why inserted" section carries the evidence; single atomic commit tagged with the quick-task id. |
| T-260803-k9n-04 | Information disclosure | secrets in planning docs | low | accept | No credentials, tokens, or dataset paths beyond already-committed public identifiers (`UniverseTBD/pu-embeddings`, arXiv id) are written. |
| T-260803-k9n-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs occur in this task — documentation-only, no code executed beyond `grep`/`awk` verification. Package Legitimacy Gate is not applicable. The Phase 02.2 text it authors pre-commits the future phase to torch, already vetted and pinned, and explicitly excludes the paper's TensorFlow reference implementation. |
</threat_model>

<verification>
1. `git diff --stat` shows exactly three files changed: `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`. No source file, notebook, or `pyproject.toml` touched.
2. All three task verify commands print their `TASK{N}_OK` sentinel.
3. `git diff .planning/ROADMAP.md` shows additions plus the four scoped Phase 3 / list / table / order amendments — no deletion of any Phase 1, Phase 2, Phase 02.1, Phase 4, Shipped, or Backlog line.
4. Read the Phase 02.2 section top to bottom: it answers what is tested, against what thresholds, what PASS unblocks, what FAIL blocks, and what Phase 3 must know about activations and compactness — without needing the paper or CONTEXT.md open.
5. Every `CAE-0N` cited anywhere in ROADMAP.md resolves to both a requirement bullet and a traceability row in REQUIREMENTS.md, and vice versa.
</verification>

<success_criteria>
- ROADMAP.md defines Phase 02.2 as an INSERTED decimal phase after 02.1, with the same section anatomy as 02.1 plus a Hard gate line
- The PASS/FAIL semantics are unambiguous in ROADMAP.md and identical in Phase 02.2's Hard gate line, Phase 3's Depends on line, and STATE.md's Blockers entry: PASS unblocks Phase 3 to decode from the CAE representation, FAIL documents findings and leaves the milestone at the phase-2 stage
- The ReLU-vs-C2-smooth conflict and the compact-manifold assumption are both stated in the phase text as Phase 3 consumption hazards
- CAE-01..07 exist in REQUIREMENTS.md with traceability rows, and the coverage arithmetic is correct for the first time since GEOM was added
- No implementation, no code, no notebook, no package install; Phase 02.2 is left at `**Plans**: TBD` for the user's separate planning invocation
</success_criteria>

<output>
Create `.planning/quick/260803-k9n-update-phase-2-of-milestone-to-test-vali/260803-k9n-SUMMARY.md` when done.
</output>
</content>
