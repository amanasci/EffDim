# Cross-model task-aligned curvature

Does the ViT-B confirmatory Q result — larger task-aligned
sphere-normal cross energy predicts larger held-out probe MSE —
replicate on DINOv3, CLIP, ConvNeXt-B and ViT-L?

Same four physics targets, frozen 512 anchors, \(d=16\), \(k=2048\).
No new autoencoders. Q first; decoder energy only if wall remains.
Prior experiment trees are read-only.
