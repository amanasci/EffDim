# Frozen ViT-B pointwise D-residual protocol.
# READ-ONLY. Decoder autodiff on real embeddings is expensive.
name: vitb_decoder_residual
estimator: D_residual
status: frozen
expensive_if_rerun: true
model: vit_base
d: 16
n_anchors: 512
decoder:
  protocol: reproduction_plain_ae_400
  hidden: [250, 250, 250]
  activation: silu
  seeds: [0, 1, 2]
paths:
  outputs: "${PLATONIC_OUTPUTS}/physics_pointwise_residual_curvature_probe_relation"
  checkpoints: "${PLATONIC_CHECKPOINTS}"
notes:
  - "C_H is (1/d) tr_g II^S, not D-full and not KHcross."
  - "Frozen P1 rho_ctl(C_H, R_G^2) = +0.02970575697233952 (null)."
  - "Do not treat a matching D-residual number as a Q replication."
